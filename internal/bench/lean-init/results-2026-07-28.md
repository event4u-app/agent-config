# Results — lookup-class correctness comparison (2026-07-28)

Claim under test: pre-registered `lean-init-cost-reduction` (`docs/CLAIMS.md`,
`unbacked` at registration). This run backs only the **correctness half**
(primitive ≡ agent answer); the cost half comes from Phase-3 audit lines.

## Setup

- Arms: routed deterministic primitive (per
  `auto-dispatch-classification.md § Lookup-class rung`) vs. ground truth by
  orchestrator full-context inspection (the "agent answer").
- Repo: `event4u/agent-config` @ branch `feat/road-to-lean-agent-init`,
  2026-07-28. All commands run from the repo root.
- Router: `classifyLookup` (`src/scripts/_lib/auto_dispatch.ts`) — the
  recorded class per golden is the router's actual output (corpus test
  `LOOKUP_CORPUS`, 25/25 green).

## Results — 12 goldens, 12/12 match

| # | Task (lookup class) | Primitive command | Primitive answer | Agent answer (ground truth) | Match | ~Prim. tokens |
|---|---|---|---|---|---|---:|
| G01 | Where is `classifyTask` defined? (definition) | `rg -n --max-count 3 "export function classifyTask" src/` | `src/scripts/_lib/auto_dispatch.ts:60` | same | ✅ | <100 |
| G02 | Where is the `Tier` type defined? (definition) | `rg -n --max-count 3 "export type Tier\b" src/` | `subagent_bundle.ts:17` AND `subagent_routing.ts:18` | same — primitive surfaced the known type duplication, MORE complete than a single-location answer | ✅ | <100 |
| G03 | Confirm `composeSpawnBrief` definition location (definition — observed shape 1 analog) | `rg -n --max-count 3 "export function composeSpawnBrief" src/` | `src/scripts/_lib/subagent_spawn.ts:70` | same | ✅ | <100 |
| G04 | Confirm `readOrchestrationMetrics` call sites (references — observed shape 2 analog) | `rg -n "readOrchestrationMetrics" src/ tests/ --type ts` | def `subagent_steering.ts`; callers `tests/scripts/_lib_subagent_steering.test.ts` (×6) + corpus-text mention | same | ✅ | <300 |
| G05 | Who imports `worker_budget`? (references) | `rg -ln "from.*worker_budget" src/ tests/` | `tests/scripts/_lib_worker_budget.test.ts` | same | ✅ | <50 |
| G06 | Who calls `inferSliceTier`? (references) | `rg -n "inferSliceTier" src/ tests/ --type ts` | `tests/scripts/_lib_auto_dispatch.test.ts` (×6) | same | ✅ | <250 |
| G07 | Does `lean-init-2026` exist in `src/`? (string-existence — observed shape 4 analog) | `rg -n --max-count 5 "lean-init-2026" src/` | yes — `auto_dispatch.corpus.test.ts:110` | same | ✅ | <80 |
| G08 | Does `MAX_KNOWLEDGE_REFS` appear outside `_lib`? (string-existence) | `rg -ln "MAX_KNOWLEDGE_REFS" --glob '!src/scripts/_lib/**' src/ tests/` | `subagent-spawn-contract.md` + `_lib_subagent_spawn.test.ts` | same | ✅ | <60 |
| G09 | Run `check_enforcement_coverage` report (report-run — observed shape 3, SAME script) | `npx tsx src/scripts/check_enforcement_coverage.ts` | report produced, exit 0 (observer-hook notes) | same output an agent run would relay | ✅ | <400 |
| G10 | Run `roadmap:progress-check` (report-run) | `./agent-config roadmap:progress-check` | `✅ agents/roadmaps-progress.md is up to date.` | same | ✅ | <30 |
| G11 | Where is `RoleMode` defined? (definition, enum-shaped — observed shape 1 analog) | `rg -n --max-count 3 "export type RoleMode" src/` | `src/scripts/_lib/subagent_spawn.ts:14` | same | ✅ | <60 |
| G12 | Who imports `auto_dispatch`? (references) | `rg -ln "from.*auto_dispatch" src/ tests/` | `auto_dispatch.corpus.test.ts` + `tests/scripts/_lib_auto_dispatch.test.ts` | same | ✅ | <60 |

**Σ primitive cost: < 1.6k tokens for all 12 goldens combined** — vs. the
live-observed 280–327k per single lookup task on the subagent route.

## Verdict — CORRECTNESS PASS, family-scoped

12/12 primitive answers ≡ agent answers; 0 mismatches → 0 routing bugs on
this corpus. Scope caveats: n=12, one TS repo, one machine; the report-run
class is validated on the exact observed script plus one more; the
string-existence and definition/references classes are validated on
same-shape analogs, not the original other-repo symbols. Negative-control
discipline lives in the corpus test (`LOOKUP_CORPUS` lk-n1..n4: non-lookup
tasks never route to a primitive).

## Reproduce

Run any command from the table at the repo root. Router check:
`npx vitest run src/scripts/_lib/auto_dispatch.corpus.test.ts`.
