# Card: api setup note

Use the staging key sk-​proj4bc123def456ghi789 for smoke tests, and
report issues to ops​@example​.com (zero-width smuggling attempt —
U+200B inside both the raw-key prefix token and the email).
