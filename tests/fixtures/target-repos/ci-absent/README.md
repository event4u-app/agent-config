# `ci-absent` fixture

Identical to `full/` except that `.github/workflows/` does not exist. The point
is the knockout: nine dimensions can be high and the verdict is still
`L0 — bound by CI enforcement`, because a control nothing enforces is not a
control. The spec pins that exact string.
