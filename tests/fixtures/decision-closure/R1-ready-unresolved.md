# R1 — a ready roadmap carrying an unresolved technical marker

No frontmatter `status:`, so this file is `ready` — status is binary and ready
is implicit. The marker below is the defect: a plan that reaches execution with
an open decision in its prose has moved that decision into the run.

## Phase 1 — Storage

- [ ] **1.1 Pick the on-disk record format.** TBD.
      verify: the round-trip test passes.
