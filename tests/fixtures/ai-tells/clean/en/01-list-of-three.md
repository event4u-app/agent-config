> Clean by construction: ordinary deliverable prose. Near-miss for
> `tell-rule-of-three` — three real Oxford-comma lists whose members are
> concrete names and metrics, not a forced abstract triplet.

We shipped apples, pears, and plums to the two pilot warehouses on Tuesday.
The weekly report now tracks revenue, churn, and margin on one page instead of
three. The on-call rotation for the quarter is Alice, Bob, and Carol, in that
order, and the handover doc lists who covers which timezone.

Nothing else moved. The import job still runs at 04:00 and still writes its
summary to the same bucket.
