# schema-review — sample gap table (fixture)

> Generated by running `lint_persistence --dir tests/fixtures/persistence/migrations/unsafe --stack raw-sql`
> and formatting per `skill:schema-review` § Output. Committed as the
> skill's reference output shape.

## Gap table

| Rule | Location | Finding | Proposed action |
|---|---|---|---|
| R-A6 (gate) | `unsafe/01_drop_table.sql:3` | DROP TABLE without migration-unsafe waiver — irreversible destructive op | fix: add backup step or `-- migration-unsafe: <reason>` waiver |
| R-A6 (gate) | `unsafe/04_add_not_null_no_default.sql:3` | ADD COLUMN ... NOT NULL without DEFAULT on existing table | fix: add DEFAULT + backfill plan |
| R-A6 (gate) | `unsafe/05_create_index_pg_not_concurrent.sql:5` | CREATE INDEX without CONCURRENTLY on postgres — locks writes during build | fix: CREATE INDEX CONCURRENTLY |
| R-A7 (gate) | `unsafe/08_create_audit_logs_no_retention.sql:3` | append-only table without retention declaration | fix: add `-- retention: <policy>` (e.g. prune > 180d) |
| R-A6 (advice) | `unsafe/06_create_index_generic_advice.sql:4` | CREATE INDEX without CONCURRENTLY (dialect unknown) — verify lock behaviour | advice — not blocking |

## Waiver density

0 waivers across 15 findings — two readings: no informed exceptions were
needed (healthy), or the rule set has not yet met code that requires them
(neutral). Density alone is never a verdict.

## Verdict

Needs fixes — 14 gate findings across R-A6/R-A7 (advice findings excluded
from the gate count).
