# NEGATIVE FIXTURE — the plausible typo, not an invented platform.
# `linkedin` is the short form a user reaches for and is NOT a flag value;
# `linkedin-post` is. Accepting it silently would make the enum decorative,
# which is why this case is pinned separately from the invented-platform one.
schema_version: drafting-channels-v1
channels:
  - id: linkedin
    length_default: 180
    cadence: 2-4 short paragraphs, hashtags per the fingerprint rules.
