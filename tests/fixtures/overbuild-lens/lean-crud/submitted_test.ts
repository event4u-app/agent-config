import { getInvoice } from './submitted';

it('returns the invoice for the owning tenant', async () => {
    /* happy path */
});

it('404s for another tenant', async () => {
    /* cross-tenant negative */
});
