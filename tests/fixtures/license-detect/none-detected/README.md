# Fixture: no-license repo

Deliberately empty of any LICENSE/LICENSE.md/COPYING file and any
`package.json`/`composer.json` `license` field — exercises the
"nothing detectable ⇒ strictest default PLUS a warning" path (S1.2).
