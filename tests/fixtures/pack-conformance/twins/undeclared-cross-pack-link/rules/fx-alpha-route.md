---
packs: [fx-alpha]
routes_to: [skill:fx-alpha-helper]
---

# Twin overlay: the rule stays conformant

Routes inside its own pack, so this twin does not also red
`lint_rule_skill_pack_reach`. Only the skill link is seeded.
