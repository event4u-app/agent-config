; Agent Config — Profile: full
; Best for: platform teams, internal standard installations
; Token overhead: moderate
;
; All major features enabled. runtime_auto_read_reports is still false
; by default — opt-in only. Matrix values are defined in
; .augment/templates/agent-settings.md.

cost_profile=full
