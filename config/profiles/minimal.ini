; Agent Config — Profile: minimal
; Best for: new users, solo developers, first-time adoption
; Token overhead: zero
;
; This is the default profile. Rules and skills are active,
; everything else is off. No files are generated, no overhead.

profile=minimal

runtime_enabled=false
observability_reports=false
feedback_collection=false
ci_summary_enabled=false
tool_audit_enabled=false
lifecycle_report_enabled=false
feedback_suggestions_enabled=false
runtime_auto_read_reports=false
max_report_lines=20
minimal_runtime_context=true
