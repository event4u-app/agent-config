; Agent Config — Profile: minimal
; Best for: new users, solo developers, first-time adoption
; Token overhead: zero
;
; This is the default profile. Runtime, observability, and feedback are off.
; Matrix values are defined in .augment/templates/agent-settings.md.
; scripts/install.py merges this file with config/agent-settings.template.ini
; to produce the user's .agent-settings.

cost_profile=minimal
