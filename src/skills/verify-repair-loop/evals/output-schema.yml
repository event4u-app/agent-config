# Behavior-schema contract for `verify-repair-loop`.
#
# Locks the load-bearing safeguard sections so a refactor cannot
# silently drift the skill away from its council-mandated guarantees
# (numeric verdict not sovereign, judge escalation, regression guard,
# bounded loop). Enforced by `scripts/skill_linter.py`
# (`lint_output_schema`, matches `^## <header>$`) — a missing section
# is a hard error.
#
# Governed by: road-to-autonomous-verify-loop.md Phase 2; the seven
# safeguards in agents/evidence/analysis/verify-repair-loop-phase1-gate.md.

version: 1

required_headers:
  - "The Iron Law"
  - "Procedure"
  - "Validation"
  - "Output format"
  - "Do NOT"
