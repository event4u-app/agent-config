; Agent Config — Profile: full
; Best for: platform teams that want read-only access to GitHub / Jira
; from skills and commands
; Token overhead: moderate
;
; Adds experimental tool adapters on top of balanced. Adapters are
; read-only and opt-in per call. See docs/customization.md for the full
; profile description.

rule_loading_tier=full

; Successor knob (ADR-110) — value preserves this preset's prior behaviour
; via the legacy mapping (minimal->off, balanced->essential, full->full).
discipline_profile=full

; Chat history (crash recovery) — full = tool-level traceability
chat_history_frequency=per_tool
