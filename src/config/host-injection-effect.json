{
  "_comment": "Per-host observations of whether a rule body delivered by the `rule-inject` concern on `user_prompt_submit` reaches the model's context. road-to-delivery-on-hook-hosts 1.1-1.3, owner ruling E3. Loader, states and the citation requirement: src/scripts/_lib/injection_effect.ts. An `observed-*` row needs host_version, transcript and date; `unobserved` needs only a reason. NOTHING here may be derived from a host binding the slot: Cowork binds eight slots and discards dispatcher output, which is why the binding and the effect are separate axes.",
  "claude": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": "2026-09-10",
    "reason": "UPDATED 2026-09-10. The premise of the earlier reason is now FALSE and the state is unchanged, which is the finding. That reason said the concern had emitted zero bytes in every session on record because `lean_projection.mode` resolved to `eager-all` throughout. It has now delivered: with `mode: delivery` set locally, a live Claude Code session (2.1.268) received 2 rule bodies / 7,685 B on `user_prompt_submit`, and later in the same session 4 bodies / 16,314 B. So delivery into a live model context is no longer the open question. AN INDEPENDENT READER — a fresh context that did not produce the turn — was given the transcript and a neutral three-way question, and returned ACTED, with a qualification it raised itself and which is why this row is NOT `observed-true`: \"the ACTED verdict rests on 'referring to its substance', not on 'doing something it asks for'\". The model quoted the injection's own byte count (a string the reader confirmed appears nowhere else in the transcript, so the reference is demonstrable) and treated the payload as an OBJECT OF STUDY. On the two delivered rules' actual obligations it returned NONE: no point in the turn surfaces a source discrepancy (`cross-source-consistency`) or classifies the prompt continuation-vs-interrupt (`user-interrupt-priority`). E3 asks for a model that VISIBLY ACTS ON a delivered body; citing that a body arrived is not that, and it is especially not that when the observed turn was one that had been WAITING for the delivery — which makes the reference cheap and is the K1 shape one layer further in. A STRUCTURAL LIMIT THE READER ALSO FOUND, and it bounds what any future attempt can prove: thinking blocks are recorded with length 0 in the transcript, so an internal application of a delivered rule is invisible by construction. Only externally visible behaviour can ever satisfy E3 here. The language pin delivered in the same payload is separately COINCIDENT rather than acted on — the prompt was German anyway, and the pin's own text concedes its trigger is not observable from the transcript. UPDATED 2026-09-11. The state is UNCHANGED and the update is the round that tried hardest to change it. Four ordinary-work turns carrying live deliveries were mechanically selected from the whole transcript store and each judged by a fresh reader on a neutral four-question prompt: one clean NONE, two UNCLEAR, and one NONE on a packet whose rule body was truncated (recorded as defective rather than repaired quietly). Condition 1 was satisfied TWICE, so the bar is not unreachable; condition 2 failed 4/4, and in one candidate the delivered clause that would have CHANGED the outcome went unfollowed while the clause the agent had independent reason to follow was honoured. Full round, method and limits: agents/evidence/analysis/e3-candidate-round-2026-09-11.md. An AI council (2/2 convergent, 2026-09-11) amended E3 prospectively rather than lowering it: a predeclared externally observable obligation class is the admissible witness set, the four conditions stand unchanged, and an eventual observed-true is CLASS-SCOPED — it may not admit this host wholesale for reasoning-only rules. unobserved stays a RESULT here, not a gap."
  },
  "_cursor_md_tree_loaded": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "road-to-delivery-on-hook-hosts 1.3. Does Cursor load `.cursor/rules/*.md` ALONGSIDE the `.mdc` files, i.e. does it carry every auto rule twice? Both trees are written today (condense.ts writes the symlink tree in the TOOL_DIRS loop and `_emit_cursor_mdc` writes the .mdc companions), so a double carry is possible by construction and unobserved in fact. It cannot be observed from a Claude Code session, and K4 forbids removing the .md bodies before it is. Carried under an underscore key because it is a property of a TREE rather than a host verdict, and putting it in the host namespace would make `cursor` answer two different questions."
  },
  "cursor": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "Binds `beforeSubmitPrompt` onto `user_prompt_submit` (hook_manifest.yaml:1300, alias :1407) and carries a `.md` rule tree, which makes it the strongest candidate — and the binding is not the question. Whether Cursor adds hook stdout to the model's context is unmeasured, and cannot be measured from a Claude Code session. `rule-inject` is absent from its concern list, so nothing would be emitted there today in any case."
  },
  "cline": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "Binds `user_prompt_submit` (hook_manifest.yaml:1315) with a `.md` rule tree, same shape as Cursor and the same unmeasured question. `rule-inject` is absent from its concern list."
  },
  "cowork": {
    "state": "observed-false",
    "host_version": "unrecorded at the time of the original observation",
    "transcript": "docs/enforcement-by-host.md:21",
    "date": "2026-09-07",
    "reason": "The one host E3 excludes by an existing measurement rather than by absence of one: its trampoline discards dispatcher output and exits 0, so a delivered body cannot reach the model however many slots are bound. Eight slots bound and zero effect is precisely why `emits` and `injection_effect` are separate axes in this record. CITATION LIMIT, stated rather than smoothed over: the host version was not captured when that observation was made, so this row does not meet the four-part protocol in full and its `host_version` says so instead of inventing one."
  },
  "windsurf": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "Three slots bound and no tool-lifecycle surface at all (docs/enforcement-by-host.md). Not observed, and not reachable from a Claude Code session."
  },
  "gemini": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "Five slots bound, no `pre_tool_use` binding, and no session available to observe from. Recorded as unobserved rather than assumed either way."
  },
  "augment": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "Binds five slots and does not honour a deny (docs/enforcement-by-host.md). It also has no `user_prompt_submit` slot, which is a DECLARED gap in the manifest rather than an oversight, so the delivery path this record measures does not exist there today."
  },
  "copilot": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "`fallback_only` with zero slots bound: this package binds nothing there, so there is no hook stdout to reach anything. Recorded as unobserved rather than `observed-false`, because nobody has looked — a structural argument that it cannot work is still not an observation that it does not."
  },
  "codex": {
    "state": "unobserved",
    "host_version": null,
    "transcript": null,
    "date": null,
    "reason": "No platform key in hook_manifest.yaml at all, so no slot is bound and no concern can fire. Same distinction as copilot: the absence of a binding is a computable fact and is not the observation E3 asks for."
  }
}
