---
status: active
complexity: structural
parent_roadmap: road-to-6.0.0-c-governance-and-evals
---

# Road to 6.0.0-D — Structural restructure (the hard break)

> Fourth `road-to-6.0.0-*` roadmap and the **deliberate hard break**. 6.0.0-A/B/C
> made the pack the surfacing/governance unit *in metadata*; this roadmap makes
> the **source tree itself show the product structure**: commands physically
> nested under their pack as `src/domains/<pack>/<verb>/`, skills + rules as one
> flat shared library, root reduced to tool/build essentials + `src/` + `tests/`
> + `taskfiles/`, profiles as curated views. There is **no backward
> compatibility** — a `migrate` command (4→6 and 5→6) plus `MIGRATION.md`
> (linked from the README) carries existing installs across.

> **Why a hard break, by design.** Few users, mostly the maintainer's team. The
> current layout has bothered the maintainer for a long time; spreading the cut
> over many versions is worse than one clean major. This roadmap optimizes for
> the cleanest end-state, not for path stability.

## Branching strategy

> **Integration branch — `refactor/6.0.0`.** Same shared integration branch as
> the other `road-to-6.0.0-*` roadmaps. Every phase branches off it and opens
> its PR against `refactor/6.0.0`. Because this is the largest, most
> path-invasive of the four, it merges **last** into the integration branch;
> only when A+B+C+D are assembled and green does `refactor/6.0.0` merge to
> `main` in one final 6.0.0 integration PR.

## Goal

The repository layout reads as the product: open `src/domains/git/` and see
`commit/` and `pr/`; open `src/skills/` and see the whole reusable building-block
library in one place; run `commands ls --profile developer` and get exactly
`work · review · fix · commit · pr`. Root carries only what tooling and the
agent host require. The cut is a single hard 6.0.0 break with a migration path.

## Target structure (the end state)

> Council-converged (anthropic/claude-sonnet-4-5 + openai/gpt-4o, three deep
> design rounds, 2026-06-03). `*` marks generated/derived (gitignored or
> regenerated by `task sync` / `task generate-tools`).

### Repo root — only essentials

```text
agent-config/
├── .github/                      # workflows
├── .claude/  .cursor/  .clinerules/  .windsurfrules*   # tool projections (some gitignored)
├── package.json  package-lock.json  pyproject.toml      # package manifests (tools expect at root)
├── tsconfig*.json  eslint.config.js  vite*.config.ts  vitest.config.ts  playwright.config.ts
├── README.md  MIGRATION.md  CHANGELOG.md  BREAKING_CHANGES.md  LICENSE  CONTRIBUTING.md
├── AGENTS.md  CLAUDE.md  llms.txt  mcp.json              # agent-host root files (expected at root)
├── agent-config                  # maintainer bin shim
├── src/                          # ALL package source (see below)
├── tests/                        # all tests
├── taskfiles/                    # task includes (folder, not a single Taskfile.yml)
├── Taskfile.yml                  # thin root that includes taskfiles/*
├── dist/*                        # build + condensation/projection output (gitignored)
└── node_modules/*
```

### `src/` — the package source, structured

```text
src/
├── app/                          # TypeScript runtime (today's src/{cli,server,install,shared,ui})
│   ├── cli/   server/   install/   shared/   ui/
│   └── index.ts
│
├── domains/                      # THE COMMAND SURFACE — pack-physical, <pack>/<verb>/
│   ├── _core/                    # always-on core commands (profile-independent)
│   │   ├── work/   review/   fix/   status/   help/
│   │   └── pack.yaml
│   ├── git/
│   │   ├── commit/command.md   pr/command.md   sync/command.md
│   │   └── pack.yaml            # id, size_class, requires, suggests, dependencies
│   ├── ticket/   { refine/  estimate/  implement/ }      + pack.yaml
│   ├── review/   { changes/  security/  tests/ }         + pack.yaml
│   ├── laravel/  { audit/  quality/  upgrade/ }          + pack.yaml
│   ├── …                        # one folder per capability pack
│   └── _internal/               # internal commands (uncapped, never surfaced)
│
├── skills/                       # FLAT — every skill, one global namespace
│   ├── git-workflow/SKILL.md   code-review/SKILL.md   laravel/SKILL.md   …
│
├── rules/                        # FLAT — every rule, one global namespace
│   ├── commit-policy.md   scope-control.md   …
│
├── flows/                        # FIRST-CLASS FLOWS (feedback-5) — `Profile → Pack → Flow → Command → Skill → Rule`
│   ├── README.md                 # USER-WORK flows only: discovery · implementation · review · delivery
│   ├── implementation.yaml       # entry_points · default_path · skills (schema lands in 6.1; 6.0-D scaffolds stubs)
│   ├── discovery.yaml  review.yaml  delivery.yaml
│   #                              # NOTE: agent-admin is NOT here — it is the platform/system surface, not a user-work flow (feedback-6)
│
├── profiles/                     # PROFILES-AS-VIEWS (commands→profile aggregation)
│   ├── developer.yaml            # view: [_core:work, review:changes, _core:fix, git:commit, git:pr]
│   ├── founder.yaml  creator.yaml  …
│
├── config/                       # discovery vocab: packs.yml, workspaces.yml, command-verbs.yml, session-profiles.yml
├── schemas/                      # JSON schemas (command, pack.yaml, discovery-manifest, …)
├── templates/                    # consumer-facing templates
├── docs/                         # contracts, decisions (ADRs), guidelines
└── internal/                     # maintainer-only: bench/  evals/  workers/  docker/
```

Notes:
- **Skills + rules stay flat and shared** (not duplicated per pack) — easier
  overview and a single namespace so no skill / rule / command can ever collide
  on name. A command references skills by slug; the dependency is declared in the
  pack's `pack.yaml`.
- **`src/domains/<pack>/`** replaces today's flat `commands/` + `pack:`
  frontmatter: the path *is* the ownership.
- **Profiles never hold files** — `developer.yaml` lists the command set it
  surfaces; the curated tree is rendered, not stored.
- **Thin command over skills + scripts (maintainer principle).** A command is a
  THIN base: its `.md` is a short orchestration spec that triggers the needed
  skills + (preferably TS, else py) scripts — heavy logic lives in `src/skills/`
  and scripts, never in the command. When commands merge or are cut, the removed
  pieces become **skills** the survivor composes.
- **Interactivity ENABLES merging (lowest barrier to entry).** A command MAY ask
  ONE short clarifying question to disambiguate instead of splitting into several
  commands — e.g. `fix-pr-comments` detects new bot/human comments and asks which
  to fix, replacing `fix-pr-bot-comments` + `fix-pr-developer-comments`. The
  north star is the smallest visible surface + the easiest entry; a confirm
  prompt is an acceptable merge mechanism (destructive defaults still never
  silent). Concrete merge verdicts live in the worksheet (Phase 4b reference).
- **`_core` vs `engineering-base` vs `git` — no two "core" concepts** (feedback-2):
  - `_core` = the **universal operating surface**, profile-independent: `work`,
    `review`, `fix`, `status`, `help`.
  - `engineering-base` = **engineering capability**: tests, quality, refactor.
  - `git` = its **own domain pack** (`git/commit`, `git/pr`, `git/sync`) — these
    move OUT of `engineering-base` (where 6.0.0-B put `commit`/`create-pr`) into a
    dedicated `git` pack. `commit`/`create-pr` are no longer engineering-base.

## Context

**Council convergence (two members, three deep rounds, 2026-06-03).** Verdicts:
pack-physical commands = **ADJUST** (do it, but gated); domain foldering = **GO**
(after the condenser fix); root→`src/` = the council flagged consumer risk, but
the maintainer's **explicit hard-break mandate overrides** that — the value kept
from the council is the *sequencing*, not a "don't break". Single biggest risk,
named by both members: **the condensation hash gate breaks silently** unless
`condense.py` hashes skill/rule includes **transitively** before any move. The
dependency lint + `pack.yaml` schema + name-collision lint must all land in
Phase 0, never after the moves.

## Phase 0: Preconditions (foundation — NO files moved yet)

- [x] **Step 1:** Make `condense.py` hash skill/rule includes **transitively** —
  a change to an included skill invalidates the hash of every command that
  includes it. Ship standalone and validate (test: modify a skill, assert a
  dependent command's hash flips). This is the non-negotiable gate; nothing moves
  until it is green.
- [x] **Step 2:** Define the `pack.yaml` schema (under `src/schemas/`): `id`,
  `size_class`, `requires`, `suggests`, and explicit `dependencies` (skills /
  rules / packs the pack's commands reference). Validate-on-load with a clear
  fail message on a typo'd / non-existent reference.
- [x] **Step 3:** Add the **dependency lint** (`pack.yaml` declared deps vs.
  actual includes), the **pack-graph lint** (`requires`/`dependencies` is a DAG —
  no cycles, so a pack stays extractable), and run both against the CURRENT
  `packages/` layout (expect ~0 violations — packs are physically isolated today).
- [x] **Step 4:** Add the **single-namespace collision lint**: every skill / rule
  / command name is globally unique after normalization (lowercase, `_`→`-`).
  Wire all Phase-0 lints into `task ci` + pre-commit. <!-- carve-out: new-gate-verification -->

## Phase 1: Laravel prototype + extraction proof

- [x] **Step 5:** Move ONE pack — `packages/pack-laravel/` → `src/domains/laravel/`
  (commands to `laravel/<verb>/command.md`, its skills/rules to the flat
  `src/skills/` `src/rules/`), author its `pack.yaml` dependencies, update
  references to slugs. Validate the transitive hash gate end-to-end on this pack.
- [x] **Step 6:** **Extraction proof** — prove `src/domains/laravel/` + its
  declared deps can be lifted into a standalone package whose own tests pass.
  This de-risks the monorepo collapse (Phase 3) by proving re-split stays
  possible. If extraction fails (cycles, missing shared code), halt and redesign
  the dependency model before Phase 2.

## Phase 2: Flat skills + rules library (before the remaining packs)

- [x] **Step 7:** Move ALL skills `packages/*/.../skills/` → `src/skills/` (flat)
  and ALL rules → `src/rules/` (flat). Resolve any name collisions surfaced by
  the Phase-0 lint. Update every command reference to the shared slug.
- [x] **Step 8:** Re-run the transitive hash gate + collision lint over the full
  flat library; confirm green. Skills/rules now live in their final home so the
  remaining packs move into a structure with the shared library already in place.

## Phase 3: Monorepo-collapse decision (evidence-gated)

- [ ] **Step 9:** *Decision gate.* Gather the install-pattern evidence: do any
  consumers install pack subsets (vs. the whole package)? Given few/team-internal
  users, the expected answer is "no subset installs" → **collapse** `packages/`
  into `src/domains/`. Record the decision (ADR). If evidence shows subset
  installs, keep per-pack publishing and nest `src/domains/` inside each package
  instead. The Phase-1 extraction proof keeps re-split open either way.

## Phase 4: Move the remaining packs

- [ ] **Step 10:** Move every remaining `packages/<pack>/` command set →
  `src/domains/<pack>/<verb>/` with `pack.yaml`, per the Phase-3 decision. Demote
  internal commands to `src/domains/_internal/` (uncapped, never surfaced).
  Re-run all Phase-0 gates after each pack (move-one-validate-one cadence).
  **Rename packs to capability-friendly names** (feedback-3): `engineering-base`
  → `engineering`, `product-basic` → `tickets`, `product-discovery` →
  `research`, etc. — capability names, not internal org names. Old pack ids kept
  as aliases for `migrate`.

## Phase 4b: Command (and skill) naming + skill-conversion

> Better names — **rename + alias only, NO consolidation here** (feedback-2 hard
> guardrail: "Diese Roadmap darf nicht gleichzeitig Konsolidierung machen. Erst
> Strukturbruch. Dann Konsolidierungsroadmap."). The naming scheme is settled by
> the claude-code-guide finding (authoritative) + the council: **hyphenated
> `<pack>-<verb>` names, no colon namespacing for project commands.** Claude Code
> SHADOWS a bare colon cluster-head when a same-named subdir exists (the observed
> `/create-pr` hidden, `/create-pr:description-only` shown) and does **NOT**
> support two-colon multi-level (`git:pr:create` ✗). So `git:pr:create` becomes
> `git-pr-create`; every cluster head folds into flat hyphenated subs. Merging
> clusters, dropping old aliases, and converting surplus leaf commands to skills
> all wait for Phase 8 (its own roadmap).

- [ ] **Step 11:** Author the naming-scheme ADR: `<pack>-<verb>`, hyphenated,
  short + descriptive; cluster heads fold away (no bare orchestrator commands);
  a sub-action is a separate command only if it is a distinct workflow, else a
  flag; the controlled-verb allowlist (ADR-041) still applies. The same
  short-descriptive-name discipline applies to skills/rules (the flat library
  benefits from a single clean namespace — see the collision lint, Phase 0).
- [ ] **Step 12:** Apply the rename worksheet
  ([`agents/reports/command-classification-6.0.0-d.md`](../reports/command-classification-6.0.0-d.md)
  — all 150 commands, new name + old in parens + KEEP/SKILL recommendation, for
  the maintainer to curate with `[x]`/`[-]`): rename every KEEP command to its
  `<pack>-<verb>` form **and keep the old invocation working as a deprecation
  alias** (`replaces:` / `superseded_by`, routed by `migrate`). This is a
  structural rename, NOT a deletion — nothing is removed in 6.0-D. Update
  references + routing evals. (Worksheet lives under `agents/reports/`, not in
  this file, so its 150 checkboxes do not skew the roadmap dashboard.)
- [ ] **Step 13:** Orchestrator HEADS — **structural part only in 6.0.0.** Move
  the head files into the flat hyphenated layout so the subs (`feature-plan`,
  `fix-ci`, …) render as standalone commands (Claude shadows bare colon heads
  anyway). But the actual **folding** — collapsing N routers into one and
  changing the routing/concurrency model — is **architectural** (council): it
  ships **staged in 6.0.x** with dual-mode (old head + new flat both resolve),
  the default flips in the next patch once the new routing is verified green
  (static + smoke), legacy removed after. NOT a big-bang flip in 6.0.0, and NOT
  gated on a telemetry window — the staging is for routing safety, not a usage
  wait. The `[-]` **leaf** skill-conversions and command
  removals stay in 6.1 (behavioral).
- [ ] **Step 13b:** *Evidence gate for the interface merges.* `commit-in-chunks`
  → `commit` and `feature-explore`/`feature-roadmap` → `feature-plan` are
  **interface** changes (identical functionality, new canonical invocation) — they
  ship in 6.0.0 **with an alias + a 2-release deprecation grace period**, BUT only
  after confirming the split is genuinely artificial (the old variants share
  >95% of their implementation / have no distinct usage). Where that evidence is
  missing, move both and decide the merge in 6.1 (council: don't merge-then-unmerge
  on an untested "obviously duplicate" assumption).

## Phase 5: Profiles-as-views + curated command tree

- [ ] **Step 14:** Implement the inverse mapping (commands→profile aggregation):
  `src/profiles/<profile>.yaml` declares the surfaced command set. Built-in
  profiles are **opinionated templates, immutable in 6.0** (user customization of
  built-ins deferred to 6.1; custom profiles use a different name). Document the
  name-collision behavior for a user `developer.yaml`.
  **Profiles are INTERNAL, not a day-one concept** (resolved product decision):
  the wizard/CLI never says "choose a profile". It asks **"What are you doing?"**
  → `Software Development` / `Product & Roadmaps` / `Content Creation` /
  `Finance & Planning`, which maps internally to a profile id. The wizard offers
  a **Simple (default) vs Advanced** toggle — Simple picks sensible profile+pack
  defaults from the one answer; Advanced reveals profiles/packs for those who
  want them. README says "Choose your primary workflow", never "choose a profile".
- [ ] **Step 15:** Extend `agent-config commands` (6.0.0-C CLI) with
  `--profile <id>` AND a **default vs expanded view** (feedback-2): `commands ls
  --profile developer` renders exactly `work · review · fix · commit · pr`;
  `commands ls --profile developer --expanded` adds the rest of the active packs'
  commands so helpful pack commands are not lost, only de-prioritised. Routing
  evals prove both the curated five and the expanded set.
  <!-- carve-out: new-gate-verification -->
- [ ] **Step 15b:** **Scaffold `src/flows/` as a first-class artefact**
  (feedback-5: "make Flows real artefacts, not just worksheet tags — the next big
  jump after the structural break"). This is **structural prep, NOT wiring**: create
  `src/flows/` with a `README.md` naming the USER-WORK flow set (discovery ·
  implementation · review · delivery) and one stub file per flow. **agent-admin
  is NOT a flow** — it is the platform/system surface (feedback-6: it describes
  system administration, not user work), so it stays out of `src/flows/`. The
  flow SCHEMA
  (`entry_points` / `default_path` / `skills`) and the resolver that renders a
  flow are defined and built in 6.1 (`road-to-6.1.0` Step 8b/9). Scaffolding the
  directory now makes Flows part of the tree — it already lives in three docs
  (worksheet tags, 6.0-D, 6.1), so it is already part of the architecture.

- [ ] **Step 16:** Move every non-essential root tree under `src/`
  (`config/`, `schemas/`, `templates/`, `docs/`, `scripts/`→`src/app` or
  `src/internal`, `internal/`, `hooks/`, the maintainer `agents/` workspace) and
  fold `taskfiles/` to a folder with a thin root `Taskfile.yml`. Update every
  hardcoded path in `.github/workflows/*.yml` and `taskfiles/*.yml`.
- [ ] **Step 17:** Update the **install contract**: `package.json` `files` / `bin`
  / prepack, the installer paths, and the discovery/MCP manifest paths to the new
  `src/` locations. The break is intentional — old `node_modules/.../{scripts,
  templates,config}` paths are gone.
- [ ] **Step 18:** Ship a **first-class `migrate` command** (feedback-2: "ohne
  Dry-Run wird der Break unnötig riskant") — `agent-config migrate` with
  `--dry-run` (preview every change, mutate nothing), `--from 4|5` (explicit
  source major), and `--check` (report whether an install is on the old layout
  and what would change). Detect a 4.x/5.x install and rewrite it to the 6.0
  layout. Write `MIGRATION.md` (4→6 and 5→6), link it from the README. README
  hero stops advertising artefact counts; it advertises "choose a profile, add
  packs, focused commands".

## Phase 7: Final verification + ADRs

- [ ] **Step 19:** CI-path audit: every workflow + taskfile + script path
  resolves to the new tree; `task ci` green end-to-end on the restructured repo.
- [ ] **Step 20:** Record the structural ADRs (supersede ADR-017 monorepo layout
  and ADR-028 root-layout as needed; new ADR for `src/domains/` + flat
  skills/rules + profiles-as-views + the hard-break decision). **Plus a dedicated
  ADR for the Thin-Command Principle** (feedback-3: it is an architecture
  decision, not just structure — command = thin orchestration; logic lives in
  skills + scripts) and an ADR for the framework-neutrality / stack-adaptive rule
  for global commands.
- [ ] **Step 20b:** Author the **command-justification ADR** (feedback-5/7: "the
  rule that ends every future discussion"). A new top-level command must be a
  **flow-entry** (daily starting point), a **state-query** (read-only, typed many
  times/day), or a **product-surface** feature started deliberately (`council`,
  `challenge-me`, `research`, `roadmap`, …) — nothing else. Destructive ops are
  skills with a mandatory confirmation gate (destructive ≠ command); agent-admin
  is the platform surface, not a flow. Sweet spot ~40–50 commands. The rule is
  captured in [`command-clusters.md`](../../docs/contracts/command-clusters.md)
  § Command justification; this ADR locks it.

## Phase 8: Post-move consolidation — NEXT roadmap, not this one

> Named here so the trajectory is explicit, but **executed in its own follow-up
> roadmap, NOT in 6.0-D** (feedback-2: "Erst Strukturbruch. Dann
> Konsolidierungsroadmap. Sonst wird 6.0 zu riskant."). 6.0-D moves + renames
> with aliases and shrinks nothing; consolidation is where the surface actually
> shrinks.

- [ ] **Step 21:** Hand the deferred work to the consolidation roadmap
  ([`road-to-6.1.0-product-consolidation.md`](road-to-6.1.0-product-consolidation.md),
  draft only until 6.0.0 ships — no telemetry wait): interactive merges with
  the non-TTY contract, the stack-adaptive resolver, command→skill conversions,
  the meta-pack split (evidence-gated), command removals, and the Flows layer.
  6.0-D performs NO consolidation itself — this step only confirms the handoff is
  complete and the 6.1 roadmap covers every `[-]`/`future_state` worksheet item.

## Scope-line rule — 4 categories, 3 lanes (council, 2026-06-03)

> A critical council pass challenged BOTH "defer everything to 6.1" (the external
> feedback) AND "pull everything into 6.0" (the maintainer). The defensible line
> is by change category, not by cost:
>
> | Category | What it is | Lane | Safeguard |
> |---|---|---|---|
> | **Structural** | file moves / renames | **6.0.0** | static analysis + unit tests |
> | **Interface** | new canonical invocation, identical behavior | **6.0.0** | alias + 2-release grace |
> | **Architectural** | new trust boundary / routing-concurrency change | **6.0.x staged** | dual-mode → flip default once routing verifies green |
> | **Behavioral** | detection / runtime change | **6.1** | engineering validation (CI-safety, detection tests) first |
>
> **The rule:** verifiable with static analysis + unit tests → 6.0.0. Needs
> dual-mode staging or detection/CI-safety work → 6.0.x or 6.1. **No 30-day
> telemetry wait** — this package's few/team-internal users mean the maintainer
> knows the usage directly; telemetry corroborates, it never gates. The
> "we touch the files anyway" argument is valid for structural/interface, **invalid
> for architectural** — cost savings never justify an unvalidated trust-boundary
> change. This is how 6.0 takes the MAX safe scope (more than "defer all") without
> becoming a risky all-at-once rebuild.

## Future architecture & consolidation (6.0.x staged + 6.1)

> Recorded so the next lanes are pre-planned. The AI council deliberately did NOT
> rubber-stamp the design — it returned "do not greenlight V6 as-is" with this
> staged path. 6.0.0 is the structural+interface break; the items below are
> 6.0.x (architectural, dual-mode) or 6.1 (behavioral).

- **6.0.x staged (architectural, dual-mode) — runs in the 6.0 patch line, NOT
  deferred to 6.1.** It's architecture, not a user feature, so it needs no usage
  data — only a static green check.
  - **Meta-pack split** (`meta` → `agent-admin` / `memory` / `analytics` /
    `governance`). The maintainer's lean: do it **as soon as the dependency gate
    is green**, in 6.0.x. The gate is exactly the **Phase-0 dependency + pack-graph
    lint** (already built in this roadmap): once it shows `meta`'s sub-areas have
    zero cross-pack dependencies, split them into real packs (dual-mode: a virtual
    `meta` keeps resolving for back-compat, flipped/removed in a later patch). No
    telemetry, no 6.1 wait — `meta` is already, by our own admission,
    agent-admin + memory + analytics + governance.
  - **Orchestrator-head folding completion.** Dual-mode ships in 6.0.0
    (Step 13); flip the default + remove legacy in 6.0.x once routing verifies
    green.

- **Interactive-merge contract (gate for every merge in 6.1).** A merged command
  (`fix-pr-comments` ← bot/human, `analytics` ← show/prune, `tests` ← create/run,
  conditionally `judge`/`override`) MAY ask one short question — but MUST work
  non-interactively: detect a non-TTY (CI), honor `--yes`/`--json`/explicit
  flags, and fall back to a safe default without hanging. Council: interactive
  prompts that block CI are "provably wrong for CI/CD". No merge ships without
  this escape.
- **Framework-neutral / stack-adaptive commands — APPROVED, scoped to
  engineering** (resolved: "yes, worth it — but only engineering"). The
  stack-adaptive set is `test-run`, `test-create`, `quality-fix`,
  `review-changes`, `work`. A resolver DETECTS the toolchain (phpunit / pest /
  playwright / vitest / jest / …) and runs the right one — NO per-stack command
  explosion (`php-test-run`/`jest-test-run` is the bad world). Monorepo guard:
  default to FAST tests, `--include-slow`/`--include-e2e` opt-in. Per-stack
  commands captured in an auto-generated project config. Only genuine PHP-space
  commands stay PHP-locked.
- **KILL list (delete, not merge).** Separate `security`/`performance` review
  commands (fold into `review` lenses), stack-variant command duplicates,
  low-usage system-introspection (`skill-preview`, `skills-discover`,
  `review-routing`, `rule-compliance-audit`) → behind `agents-audit` /
  `agent-admin`, not top-level.
- **Meta-pack split.** `meta` is several products (memory, council, analytics,
  context, override, profile, agents, governance). Split into `agent-admin` /
  `memory` / `analytics` / `governance` (feedback-3).
- **Flows layer (the biggest post-break improvement, feedback-3).** Make the
  implicit flow explicit: `Profile → Pack → Flow → Command → Skill → Rule`. A
  Flow names a multi-command journey (e.g. "implementation flow" = ticket-implement
  · work · feature-dev). Spec it as its own architecture roadmap.
- **`future_state` worksheet axis.** Each command's intended end-state
  (command / skill / alias / remove) is annotated in
  `agents/reports/command-classification-6.0.0-d.md`; it drives the 6.1 cut.
- **Day-one experience — RESOLVED.** Profiles are internal, never sold as a
  day-one concept. Wizard/CLI asks "What are you doing?" (Software Development /
  Product & Roadmaps / Content Creation / Finance & Planning) → maps to a profile
  id; Simple (default) vs Advanced toggle. The first-5-minutes flow shows what
  was detected (`/test → Jest, 23 files found`) with error recovery.
- **Routing/activation telemetry — APPROVED, NOT product analytics** (resolved).
  Collect only the gold metrics: `command · intent · pack · activated skills ·
  activated rules · duration · success/abort`. Never "user clicked X". This is
  the data 6.1 prunes on (a never-routed skill → remove; an internal-only command
  → `internal`; a 90%-used command → promote). Extends the existing local-only,
  opt-out analytics surface (6.0.0-C), never POSTed.
- **Flows are the real 6.1 theme.** Per the reviewer: after the structural break,
  the next discussion is "what are the 5 most important product Flows?" — not more
  command/skill debates. Make Flows the headline of the 6.1 architecture roadmap.

## Acceptance Criteria

- [ ] `condense.py` hashes includes transitively (skill change flips dependent
  command hashes), proven by a test.
- [ ] Single-namespace collision lint green; no skill/rule/command name collides
  (normalized); `pack.yaml` schema + dependency + DAG lints wired into CI.
- [ ] Source layout matches the target MD-box: `src/domains/<pack>/<verb>/`, flat
  `src/skills/` + `src/rules/`, `src/profiles/`, `src/app/`, `src/internal/`.
- [ ] Root contains only the defined essentials + `src/` + `tests/` + `taskfiles/`.
- [ ] Naming scheme shipped (ADR): `<pack>-<verb>` hyphenated, no colon
  namespacing, cluster heads folded; `git` is its own pack (`git-commit`,
  `git-pr-create`), no longer engineering-base; `_core` (work/review/fix/status/
  help) is the only universal surface.
- [ ] `commands ls --profile developer` renders exactly {work, review, fix,
  commit, pr}; `--expanded` shows the active packs' full set; routing evals prove
  both.
- [ ] `agent-config migrate` supports `--dry-run`, `--from 4|5`, and `--check`.
- [ ] One pack (Laravel) proven extractable as a standalone package; monorepo
  collapse decision recorded with evidence (ADR).
- [ ] `agent-config migrate` carries a 4.x and a 5.x install to 6.0;
  `MIGRATION.md` linked from the README.
- [ ] `task ci` green on the new structure; all path audits pass.
- [ ] No skill/rule/command deleted by this roadmap — move + re-home only
  (content consolidation is a later roadmap).
