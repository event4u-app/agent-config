# Roadmap: Anthropic Alignment

> Adopt two proven patterns from [`anthropics/skills`](https://github.com/anthropics/skills):
> Claude Code Plugin Marketplace distribution, and the "pushy description"
> triggering pattern. Empirical trigger evaluation was split into its own
> roadmap — see [`road-to-trigger-evals.md`](road-to-trigger-evals.md).

- **Source analysis:** [`agents/analysis/compare-anthropics-skills.md`](../analysis/compare-anthropics-skills.md)
- **Status:** Draft, 2026-04-20
- **Author:** Generated by `/analyze-reference-repo anthropics/skills`. Phase 3
  extracted into `road-to-trigger-evals.md` on 2026-04-20 for focus.

**Guiding principle:** Every item below either opens a real new audience or
makes our existing 95+ skills measurably better. No speculative scaffolding.

## Prerequisites

- [ ] [`road-to-9.md`](road-to-9.md) Phase 1 landed — real minimal runtime.
- [ ] Read [`skills/skill-creator/SKILL.md`](https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md)
      once more for Phase 2 reference.
- [ ] Branch off `main` after current MCP work merges.

## Phase 1 — Claude Code Plugin Marketplace (v1.7.0)

### Implementation notes (2026-04-20)

Phase 1 shipped with these deltas from the original draft below:

- **Bundle scope**: chose *all 98 skills* (Q1 option 3) rather than 7 agent-infra
  skills. Plugin name is therefore `agent-config` (full catalog), not
  `agent-infrastructure`.
- **Skill paths**: `./.claude/skills/<name>` (canonical symlinks already in place)
  rather than `./.agent-src/skills/<name>`. Either would work, the `.claude/`
  path mirrors Anthropic's `./skills/` convention.
- **Version source**: `package.json` (composer.json has no version field).
- **Owner email**: `dev@event4u.app` (not the personal noreply).
- **Rules + commands** are **not** part of `marketplace.json` — the Claude Code
  Plugin schema only carries `skills[]`. Rules and commands continue to be
  installed via `scripts/install.sh`. This matches Q2 option 1 after research.
- **Removed** the pre-existing `.claude-plugin/plugin.json` — Anthropic's
  canonical pattern uses `marketplace.json` only, no sibling plugin descriptor.

### 1.1 Add `.claude-plugin/marketplace.json`

Format taken verbatim from anthropics/skills:

```json
{
  "name": "event4u-agent-config",
  "owner": { "name": "event4u", "email": "..." },
  "metadata": {
    "description": "Agent infrastructure skills for AI coding tools",
    "version": "<package-version>"
  },
  "plugins": [
    {
      "name": "agent-infrastructure",
      "description": "Skills for writing, reviewing, and managing agent content",
      "source": "./",
      "strict": false,
      "skills": [
        "./.agent-src/skills/skill-writing",
        "./.agent-src/skills/skill-reviewer",
        "./.agent-src/skills/skill-management",
        "./.agent-src/skills/agent-docs-writing",
        "./.agent-src/skills/learning-to-rule-or-skill",
        "./.agent-src/skills/override-management",
        "./.agent-src/skills/analysis-skill-router"
      ]
    }
  ]
}
```

Start with **one bundle**, not five. Add more only if a real user asks.

### 1.2 CI validation

New task: `task lint-marketplace`:
- JSON schema validation (name/owner/plugins shape)
- Existence check: every listed skill path exists on disk
- Version sync: `metadata.version` matches `composer.json` / `package.json`

Add to `task ci` pipeline before `test`.

### 1.3 Document in README

Install commands in the README's "individual use" table use the canonical
`<plugin-name>@<marketplace-name>` syntax:

```
claude plugin install agent-config@event4u-agent-config
```

The marketplace itself is registered once per user via:

```
claude marketplace add event4u-app/agent-config
```

(Registration syntax takes the GitHub repo path, not the marketplace name.)

### Explicitly deferred

- Additional plugin bundles (laravel-essentials, project-analysis, devops-tools)
  — wait for a consumer to ask, don't pre-group.
- `.skill` file packaging for single-skill distribution — no signal.

## Phase 2 — Description quality pass (v1.7.1, small)

### 2.1 Update `skill-quality` rule

Add the "pushy description" pattern from skill-creator. One new section
(~10 lines) documenting:

> Claude undertriggers on polite, generic descriptions. Make triggering explicit.
> Pattern: end the description with "Use when the user mentions X, Y, or Z, even
> if they don't explicitly ask for `<skill-name>`."

Cite the source (skill-creator) in the rule so future maintainers see where it
came from.

### 2.2 Audit existing descriptions

Mechanics:

- Script: `scripts/audit_skill_descriptions.py` — flag descriptions shorter than
  150 chars, or missing explicit trigger phrasing ("use when", "use if")
- Report: sorted worst-to-best
- Rewrite the top 10-15 in small PRs (3-5 skills each)

Do **not** rewrite all 95+ at once. Incremental, reviewable, reversible.

### 2.3 Update `skill-writing` guideline

Before/after example using one of the rewritten descriptions as canonical.

## Phase 3 — Split out: trigger evaluation

Originally scoped in this roadmap, the empirical trigger-eval system is large
enough to warrant its own roadmap with its own gate. See
**[`road-to-trigger-evals.md`](road-to-trigger-evals.md)** — same source
(skill-creator), same hard budgets, but described with the detail a real
implementation requires. Phases 1 and 2 of this roadmap are prerequisites
for that one: `skill-quality` must already carry the "pushy description"
pattern before we measure trigger rates.

## What's **not** on this roadmap (already solved / unused)

| Pattern from anthropics/skills | Why absent here |
|---|---|
| Progressive disclosure (3 levels) | Already enforced by `skill-quality` rule. |
| `<500` line size budget | Already in `size-enforcement` rule. |
| Domain-organized skills | Already in `laravel-*`, `project-analysis-*`. |
| `## Related Skills` | Already use `## Related` with same semantics. |
| `scripts/` + `references/` + `assets/` layout | Already canonical here. |
| Agent Skills spec link | We follow agentskills.io via our templates. |

## Related

- [`agents/analysis/compare-anthropics-skills.md`](../analysis/compare-anthropics-skills.md) — findings
- [`road-to-trigger-evals.md`](road-to-trigger-evals.md) — split-out child roadmap (trigger evaluation)
- [`road-to-9.md`](road-to-9.md) — orthogonal (runtime depth)
- [`road-to-mcp.md`](road-to-mcp.md) — orthogonal (MCP config generation)
- [`archive/road-to-ultimate.md`](archive/road-to-ultimate.md) — rejected predecessor
