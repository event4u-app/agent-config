---
complexity: structural
status: ready
execution:
  mode: phase-checkpoints
estate_growth_exempt: "Unblocks a 16-day-old parked stub (road-to-first-reference-analysis-run) and a pre-registered claim with a 180-day expiry window; the work exists to close two open estate entries, not to add a third."
estate_offset_exempt: "Offsets nothing on landing by design — the stub it closes is disposed in its final phase, so the disposal cannot be banked in the same change that opens the work."
---
# Road to the bounded reference-harvest loop

> **Source:** `agents/tmp.old/inbox-2026-09-h/` — verified against the tree at `93d63073e` on 2026-09-05.

## Goal

Make one command the only place in this package that analyses an external repository, give it a bounded refinement loop with three differently-scoped review lenses, stop it from writing source identity into tracked files, and put a thin harvester in front of it that walks the roadmap estate's encrypted reference tokens and feeds unique repositories through it one at a time without producing one roadmap per repository. Done when: the analysis command carries a machine-readable `limits:` contract whose lines the body restates, a single deterministic discovery script reports the estate census and writes nothing outside the gitignored local area, an interrupted batch resumes without re-running a completed repository, and one real analysis has run end to end under the command's own contract with its outcome written honestly into the pre-registered claim row.

Four things the drafts proposed are **not** in this roadmap because the tree already has them, and each one prevented a phase: the analysis command itself already exists with anchor-table-first ordering, a `--deep` clone tier, an interop probe, verdict convergence and ledger rows (`src/domains/analysis-workbench/analyze/repo/command.md`, 361 lines at authoring time, before step 1.1 renamed the directory) — so this is an extension, not a build; the encrypted-link primitive with project → global → environment key resolution already exists (`src/scripts/_lib/link_crypto.ts:216`); the gitignored local landing zone already exists (`.gitignore:290`); and the measurement of whether this loop is worth its cost is already pre-registered with a falsification bar fixed before the data (`docs/CLAIMS.md:487`), so no second evaluation is built. Two further draft proposals were dropped on verification rather than deferred: a "generic bounded-analysis-loop primitive" whose stated premise is refuted by the only existing consumer (`src/domains/meta/optimize/deep/command.md:11-16` ships `mode_default: plan` together with `max_iterations: 3`), and a reuse of a "canonical roadmap-estate reader" that does not exist in `src/scripts/_lib/`.

## Phase 1 — One analysis engine, one name

- [x] **1.1 Rename `analyze/reference-repo/` to `analyze/repo/`** and set `name: analyze-repo`, `sub: repo`, `replaces: [analyze-reference-repo, analyze:reference-repo]`. `visibility: internal` and `workspaces: [agent-config-maintainer]` are already correct and stay. `replaces:` is the house alias mechanism, so no deprecation window and no second living implementation.
      verify: ✅ `./scripts-run src/scripts/skill_linter src` → `418 pass, 1 warn, 0 fail`; `task sync` produced `dist/agent-src/commands/analyze/repo.md` and `dist/agent-src/commands/analyze/` no longer lists `reference-repo.md`. `.cursor/commands/analyze-repo.md` and `.windsurf/workflows/analyze-repo.md` regenerated; the three stale artefacts at the old names were removed (the generators do not prune).
- [x] **1.2 Update every inbound reference in the same change** — 18 files under `src/` and `docs/` name the old command, 17 of them outside `docs/archive/`; `src/rules/external-reference-deep-dive.md` calls it "the canonical flow" and must name the new one. `src/config/gate-violation-baselines.json` stays unchanged; its mention is a historical note, not a pointer. `corrected-from-reproduction` — the drafts asserted 16 files.
      verify: ✅ the pre-state census reproduced the roadmap's own figures exactly — **18 files, 17 outside `docs/archive/`**. Post-change the grep returns **5** files. `corrected-from-reproduction` — the expected residual set was three items plus the `replaces:` line; it is **five**, because two further files are historical or frozen records rather than pointers, and rewriting either would falsify a record to satisfy a grep:
      · `docs/decisions/ADR-115-command-cluster-consolidation-phase-4.md:47` — a decision record of the 2026-07-08 pack move. An ADR states what was decided then, under the name used then.
      · `docs/CLAIMS.md:488` — a **pre-registered** claim whose text is frozen by its own terms ("no goalpost-moving after the runs land").
      Neither is a dangling pointer: `replaces: [analyze-reference-repo, analyze:reference-repo]` keeps the old name resolving, which is the house alias mechanism this step's own 1.1 relies on. `src/config/gate-violation-baselines.json` stays unchanged as the roadmap already specified. `src/flows/surface-map.yaml:38` carried the *path* form, outside this grep's pattern but inside `lint_flows`' — updated in the same change.
- [x] **1.3 Add the named chain exception to the cluster head.** `src/domains/analysis-workbench/analyze/command.md:116` forbids chaining sub-commands; the harvester is the single exception and gets one sentence saying so: it invokes `repo` once per repository, each in its own subagent, sequentially, never in the orchestrator's own turn and never as a fan-out.
      verify: ✅ the line still opens "**Do NOT chain sub-commands.** One `/analyze <sub>` per turn." and adds exactly one exception naming `roadmap-repos` → `repo`, sequential, one subagent each, never a fan-out, "and it may invoke only `repo`". `routes_to:` lists both. Pinned by `tests/scripts/analyze_repo_limits.test.ts` § "the cluster head permits exactly one chain exception" (3 assertions).

## Phase 2 — A bounded loop with three different questions

- [x] **2.1 Add the `limits:` block** to `analyze:repo`: `mode_default: plan`, `max_iterations: 3`, `hard_ceiling: 5`, `no_gain_stop: 2`, `target_metric: required`, and restate every one of those lines in the command body — the schema requires the body to carry what the frontmatter pins. The command has no loop contract today; its only convergence is inside the verdict table. `corrected-from-reproduction` — the loop contract goes on this command directly; no generic primitive is extracted first, because the schema already permits plan-mode loops and would give the primitive exactly one other consumer.
      verify: ✅ `./scripts-run src/scripts/validate_frontmatter` → `450 artefacts, 0 failing, 0 with warnings`. Over the body alone (everything below the closing frontmatter fence) each of the five keys returns exactly one hit — the restatement headings carry their own key (`### Loop budget (\`max_iterations: 3\`, \`hard_ceiling: 5\`)` and siblings), so the pin is greppable from the body and not only from the frontmatter. `corrected-from-reproduction` — the restatement enforcement in this tree is **per-command, not generic**: `tests/scripts/optimize_deep_limits.test.ts` is hard-wired to one path, so a second `limits:` consumer would have received JSON-Schema shape validation and nothing else. `tests/scripts/analyze_repo_limits.test.ts` (30 tests) is the sibling that closes it; sabotage-probed by drifting the pin `max_iterations: 3 → 4`, which reds it.
- [~] **2.2 Pin the upstream revision before any reasoning.** <!-- deferred-resolution: carried-to=road-to-first-reference-analysis-observation --> The seed pass resolves the reference's head revision once; all three loops read that same revision; only `--refresh` re-resolves. Loop *n* cannot read deeper than the seed pass without a local clone, so `--deep` becomes the default whenever the loop runs, and the per-loop read ceiling is logged when it is hit.
      verify: **contract written, observation carried.** § 2a now resolves the head revision once in the seed pass, records `revision: <sha>` per pass, makes `--deep` the default whenever the loop runs, and requires one read-ceiling line naming the bound that fired. Pinned by `analyze_repo_limits.test.ts` § "2.2 the upstream revision is pinned once". The verify as written names *a run's* iteration record, and no run has occurred — the one real end-to-end run is step 5.2, deferred by council decision. Carried to `road-to-first-reference-analysis-observation` step 1.4, whose verify names this observation explicitly. Fixture `arl-8` states the decidable form.
- [~] **2.3 Give the three loops three different jobs.** <!-- deferred-resolution: carried-to=road-to-first-reference-analysis-observation --> Loop 1 Coverage — what was missed, what was wrongly marked absent, which rows have a weak anchor. Loop 2 Adversary — was the form copied instead of the principle, is this only different rather than better, does this package already solve it elsewhere, what does it cost in tokens, surface and maintenance. Loop 3 Convergence — resolve contradictions, fold duplicates, cut the speculative, bind every remaining line to evidence on both sides. Each loop takes the previous analysis **and** the previous roadmap as input and appends its own delta row (added / removed / flipped / folded, with a reason per line) to the existing `## Iteration record`.
      verify: **contract written, observation carried.** § 5b is now a three-row lens table (Coverage · Adversary · Convergence), each loop takes both the previous analysis and the previous roadmap, each appends its own named delta block, and "**A loop whose delta block has no added, removed, flipped or folded entry is recorded as a zero-delta loop, never omitted**" — with the reason stated: an omitted block is indistinguishable from a loop that never ran, which silently disarms the halt rule that counts them. The document skeleton carries three pre-labelled blocks. Pinned by `analyze_repo_limits.test.ts` § "2.3 three loops, three different lenses". Same reason as 2.2 — the verify names *a run*; carried to `road-to-first-reference-analysis-observation` step 1.4. Fixtures `arl-1`, `arl-4`.
- [x] **2.4 Make the target metric a decision-quality measure**, not an output-volume one: the count of ADOPT/ADAPT rows carrying a concrete `file:line` on both the reference side and this package's side. Halt on spin after two consecutive zero-delta loops; a contested verdict table stops the run with a maintainer question. A zero delta in one lens never cancels the remaining lenses — it is a signal about that lens, not about the next one.
      verify: ✅ both fixtures exist and are pinned. `tests/analyze-repo/eval-fixtures.md` **arl-4** ("zero-delta loop 1 does NOT cancel loop 2" — pass requires loop 1's block present recording the zero, then loop 2 runs) and **arl-5** ("two consecutive zero-delta loops → halt" — pass requires the run to stop before loop 3 naming both loops and the metric). `analyze_repo_limits.test.ts` asserts the fixture ids, the four decidable patterns P1-P4, and the body's own two clauses. **Fixture, in this repository's sense** — `tests/optimize-deep/eval-fixtures.md` is the house precedent for verifying a model-executed loop contract, and this step's verify says "a fixture run", not a live one. § 5c fixes the metric as the count of ADOPT/ADAPT rows with a `file:line` on **both** sides, and says why an output-volume metric would reward exactly the churn the lenses exist to prevent.
- [x] **2.5 Drop the mandatory scope menu.** `src/domains/analysis-workbench/analyze/repo/command.md` § 1 (lines 46-58 before this roadmap ran) opened with a four-option prompt ending in "Wait for the user's choice", which in a batch is a prompt per repository. A bare repository argument means full scope; `--focus` means focused; the only remaining question is an unresolvable repository identity. The write-acts (roadmap landing, ledger rows) keep their confirmations.
      verify: ✅ fixture **arl-2** — an explicit repository argument, pass requires the transcript's first external action to be a fetch with no preceding question and the four-option menu absent. `"Wait for the user's choice"` no longer appears anywhere in the command (asserted negatively; sabotage-probed by re-inserting it, which reds the test). The one surviving question — an unresolvable repository identity — is fixture **arl-3**, and the §§ 7-8 write-act confirmations are asserted still present.

## Phase 3 — The output stops naming its source

- [x] **3.1 Derive an opaque id** from the canonical repository identity and the pinned revision, and keep the plaintext-to-opaque mapping only in the local manifest under the gitignored area.
      verify: ✅ implemented as `opaqueId()` in `src/scripts/harvest_reference_tokens.ts` — `sha256(identity + "@" + revision)`, first 16 hex characters. Three executable assertions in `tests/scripts/harvest_reference_tokens.test.ts` § 3.1: stable across two derivations at one revision, different across revisions, and 16 hex characters containing neither the owner nor the repository substring. The plaintext↔opaque mapping lives only in `agents/.harvest-local/manifest.jsonl`, inside the gitignored directory.
- [x] **3.2 Move every pass and loop artefact to `agents/.harvest-local/repo/<opaque-id>/`** and stop writing `agents/evidence/analysis/compare-*.md`. That directory is already gitignored (`.gitignore:290`) and already exists; only `compare-*.md` is covered under the evidence directory (`.gitignore:285`), so any other tracked name there would be unprotected.
      verify: ✅ § 6 of the command now targets `agents/.harvest-local/repo/<opaque-id>/analysis.md` and the old `agents/evidence/analysis/compare-<slug>.md` target is gone (asserted negatively in `analyze_repo_limits.test.ts`). The command states the reason the roadmap gives — `.gitignore:284` covers only `compare-*.md` under the evidence directory, so any other filename there is unprotected by construction, while `/agents/.harvest-local/` (`.gitignore:290`) is a whole gitignored directory rather than a pattern one rename defeats. The `git status` half of the verify is an observation of a run and is covered by the carried run's step 1.3, whose verify asserts exactly this clause; the directory does not yet exist in a fresh checkout, which the script creates.
- [x] **3.3 Name the tracked roadmap after the defect, never after the source.** Today the command offers `agents/roadmaps/adopt-<owner>-<repo>.md` (§ 7 option 1) with the plaintext URL in the document header — a tracked filename carrying the source identity. The landed roadmap is named for the capability gap and carries a provenance block with an encrypted token instead.
      verify: ✅ `check_no_external_sources` green (see the gate battery). The filename rule is no longer only prose: `roadmapNameIsSourceFree()` compares a proposed basename against every source identity's host, owner and repository segments separately, so `adopt-<owner>-<repo>.md` is caught even though the joined string never appears in the identity; a fold whose proposed name carries an identity is returned `contested` rather than silently renamed, because a rename would hide that the finding was understood only as "what they have". Four assertions in `harvest_reference_tokens.test.ts` § 3.3, including a false-positive guard (a 4-character floor, so `github.com/o/r` does not reject `road-to-observed-order`). Sabotage-probed by dropping the floor, which reds it. § 7 of the command also replaces the plaintext `Source:` header with `opaque:{opaque-id}` and requires an `ENC1:` provenance token.
- [x] **3.4 Never pass plaintext to the subagent.** The orchestrator hands the subagent the opaque id; the subagent reads the URL from the local manifest itself. The cost of this is one indirection and it is taken on precaution — whether a transcript or audit log would actually retain the argument is host-dependent and was not verifiable from the tree.
      verify: ✅ the harvester's § 2 reads "Pass the subagent **the opaque id only** — never a resolved URL. The subagent reads the URL from `agents/.harvest-local/manifest.jsonl` itself." Asserted in `analyze_repo_limits.test.ts` § "never passes a plaintext URL to a subagent", against both command bodies. The roadmap's own honesty clause is carried into the shipped text rather than dropped: the indirection is taken **on precaution**, and the command says so — "whether a given host retains a subagent's arguments in a transcript or audit log is host-dependent and was not verifiable from this tree" — which is asserted too, so a later edit cannot quietly upgrade it into a proven threat. Fixture `arl-10`.

## Phase 4 — Deterministic discovery and a thin harvester

- [x] **4.1 Write one discovery script**, not a module family — discover, decrypt, classify, canonicalize, deduplicate, write the manifest, in the house form of `src/scripts/sweep_source_surfaces.ts`: refuse without a key rather than skipping silently, and never decrypt to stdout except at a terminal. Reuse `src/scripts/_lib/link_crypto.ts` rather than reimplementing the token format, and reuse its token regex from `sweep_source_surfaces.ts:587`. `corrected-from-reproduction` — the script enumerates the five roadmap directories itself; no shared estate-reader library exists to depend on.
      verify: ✅ `./scripts-run src/scripts/harvest_reference_tokens --dry-run` reports **149 token occurrences, 117 unique tokens, 63 files, archive=57 later=6 active=0 stubs=0 skipped=0** — every figure the roadmap pinned, exactly. Also pinned as a regression assertion in `harvest_reference_tokens.test.ts` § 4.1.
      Worth recording because it nearly became a false correction: a hand `grep -rl 'ENC1:' agents/roadmaps/` returns **77** files, and the same command at the roadmap's own pinned commit `93d63073e` also returns 77 — so the divergence is not archival drift and the figures looked wrong at their own source. They are not. The other **14** files contain a bare `ENC1:` mentioned in prose with no base64 following it, which is precisely the extraction defect step 4.2 and Risk 7 name. The naive measurement demonstrated the defect the step exists to prevent.
      House form followed: refuses without a key (exit 2, with the reason and the remediation) rather than reporting a short classification, and reuses `_lib/link_crypto.ts` and the `sweep_source_surfaces.ts:587` token-scan shape. One improvement on the model rather than a copy — `sweep_source_surfaces.ts` states "terminal only" for its plaintext path in a **comment** and enforces nothing, so this script enforces it: `--json` refuses on a non-TTY stdout unless `HARVEST_ALLOW_NONTTY=1`. The script enumerates the five estate levels itself; no shared estate reader exists in `src/scripts/_lib/`, re-verified.
- [x] **4.2 Classify after decryption, never from the surrounding prose.** The tokens are a mixed pool — repositories, articles, threads, packages, a paper, a video — and the descriptors around them are inconsistent enough that a proximity count is not reproducible. Classification reads the resolved URL: a repository host or a `.git` suffix is a repository; anything else is recorded as not-a-repository and never analysed. `corrected-from-reproduction` — the drafts' descriptor census did not reproduce.
      verify: ✅ all three, in `harvest_reference_tokens.test.ts`. Extraction: a bare `ENC1:` in prose and two short placeholders are not matched, and a token split across a line break is not stitched together. Canonicalization: the four spellings — plain HTTPS, `.git` suffix, `git@host:owner/repo.git` SCP form, and a `/tree/main/docs` deep link — collapse to the single identity `github.com/owner/repo`. Classification: a documentation URL is not a repository, a repository host's own product surface (`/features/actions`, `/topics/rust`) is not, a bare host with no owner/repo pair is not, and a `.git` suffix on an unlisted host is. `classifyUrl` reads the return of `canonicalize` and nothing else — there is no code path that inspects surrounding prose. Sabotage-probed by loosening the extraction bound, which reds two assertions.
- [x] **4.3 Deduplicate on identity plus pinned revision, never on the token.** Encryption is randomised, so two tokens for one repository do not compare equal; and one repository at two historical pins is two pieces of evidence, not one. Each unique entry fans in the roadmap files that cited it.
      verify: ✅ both fixtures assert exactly that. Two distinct tokens for one repository → **one** entry, `cited_by` holding both roadmap paths sorted, `tokens: 2`. Two revisions of one repository → **two** entries with two distinct opaque ids. Plus a third: a not-a-repository resolution is recorded with its status rather than dropped. Sabotage-probed by keying the dedupe on identity alone, which reds the two-revisions assertion; and by keying the fold on the opaque id, which reds four. Deterministic rendering is asserted too (two orderings of the same entries produce identical bytes).
- [x] **4.4 Write a resumable manifest** under the gitignored area, one line per identity, with an explicit status vocabulary covering pending, running, done, skipped-as-not-a-repository, skipped-as-duplicate, skipped-as-fresh, blocked-on-missing-key, blocked-as-unresolvable, and failed. Freshness is keyed on identity, upstream revision, contract version, loop count and focus — **not** on this package's own revision, which would change with every commit and make every entry permanently stale.
      verify: ✅ executed as a fixture rather than described. `planResume` is given three fresh entries and a prior manifest carrying the first two as `done` — the interrupted-after-the-second state — and returns exactly one runnable entry (the third), two `skipped-as-fresh`, and a reason per skip matching `/freshness key unchanged/`. A second assertion pins that `--refresh` overrides the skip **and nothing else**. The nine-value status vocabulary is asserted element-by-element against the roadmap's list. The freshness key excludes this package's own revision, asserted by keying the same entry at two different `observed_at` dates and requiring an identical key — which is what makes `skipped-as-fresh` reachable at all rather than a rule that never fires. Sabotage-probed by making `planResume` ignore the prior manifest, which reds both resume assertions.
- [x] **4.5 Add the harvester command** at `analyze/roadmap-repos/`, `type: orchestrator`, `visibility: internal`, with no analysis steps in its body at all: call the script, read the manifest, dispatch one subagent per repository, then run the ownership pass. Default batch limit is small; every external fetch is spent deliberately.
      verify: ✅ `src/domains/analysis-workbench/analyze/roadmap-repos/command.md`, `type: orchestrator`, `visibility: internal`. Its body carries no analysis instruction — greped for the analysis-step verbs of `analyze-repo` (fetch the surface, extract structured facts, compare against, classify every, converge, anchor table) with zero hits — and says so in the second paragraph, with the reason: a second copy of that reasoning would be a second implementation to keep in step with the first, which is the condition step 1.1 removed. Every analysis reference resolves to `analyze-repo` (nine occurrences). The one other command named, `/analyze:inbox`, is in § When **not** to use and routes a *different input class* (a dropped local artefact) — a routing pointer, not an analysis instruction. `lint_command_routing` and `lint_flows` green.

## Phase 5 — One gap, one roadmap, and one real run

- [x] **5.1 Gate every new roadmap behind an ownership check.** Before a run lands anything, it consults `src/scripts/roadmap_context.ts` and the run's own candidate registry and picks one disposition per repository: no action, already planned, extend an existing roadmap, create a follow-up, create new, or contested. Three repositories showing the same gap strengthen the evidence on one owning roadmap; they do not create three.
      verify: ✅ `foldCandidates()` groups on the capability gap, never on the repository. The fixture asserts two repositories on one gap → **one** roadmap `agents/roadmaps/road-to-bounded-retry-budgets.md` <!-- ref-ignore --> (a fixture name, deliberately not a file) with **two** registered sources; three on one gap → still one, three sources; two different gaps → two roadmaps; and a gap an active roadmap already owns folds onto that owner as `already-planned` instead of opening a second entry. `check_estate_count` green (gate battery). The harvester's § 3 carries the six-disposition table and the inversion in one line — "Three repositories showing the same gap strengthen the evidence on one owning roadmap. They never create three." Sabotage-probed by keying the fold on the opaque id, which reds four assertions.
- [~] **5.2 Run one real analysis end to end <!-- deferred-resolution: carried-to=road-to-first-reference-analysis-observation --> under the command's own contract** against a small reference, before any batch work, so the pre-registered claim's window is not spent on scaffolding. The claim at `docs/CLAIMS.md:487` requires each of the next two runs to produce at least one interop-probe finding at `file:line` precision **and** at least one bound-claim routing, compared against a shadow run of the pre-upgrade command text.
      verify: **carried, by AI-council decision on a split verdict.** Record: `agents/evidence/council/bounded-harvest-observation-slot.md` (2 seats, anthropic + openai, subscription transport, $0.0000, quorum 2/2). Network availability was established first, so this is not a capability claim: a public API probe returned HTTP 200.
      The council split — one seat recommended deferral, one authorized exactly one observation slot subject to a readiness protocol. A split is an escalation condition rather than a verdict, so the disposition adopted is the **intersection neither seat calls unauthorized**: do the readiness work, defer the measured run. The authorizing seat states the deferral path itself ("if any prerequisite fails, stop without running the measured analysis and defer"), so deferral sits inside both seats' authorized sets while execute-now sits inside only one.
      **Readiness work completed in this change**, which both seats made a prerequisite: the shadow arm is frozen and reproducible — commit `537e7c86e7646d50bf10d8b3e7ec8655239bceab`, blob `b2ea4fa61d4c8fa1ec737cad95699bd5d8be4a7f`, sha256 `6ea179291c79a96108d17faea27ecbbffed2f9a74d0d07236157d05bc68134ba`, 361 lines / 15466 bytes. The 361 figure independently corroborates the pin against this roadmap's own citation of the pre-upgrade command.
      Nothing about the outcome is asserted. The receiver is `road-to-first-reference-analysis-observation`, whose reopening trigger is observation-based; a calendar fallback one seat proposed is recorded there and **not** adopted, because both seats agreed a calendar trigger conflicts with the observation-based requirement.
- [~] **5.3 Write the outcome honestly into the claim row** <!-- deferred-resolution: carried-to=road-to-first-reference-analysis-observation --> — backed or honest null, with the null consequence the claim already specifies, and no goalpost moved after the data. Then dispose of `agents/roadmaps/stubs/road-to-first-reference-analysis-run.md`, whose two stated trust boundaries (an outbound third-party fetch and raw named evidence) are exactly what Phases 3 and 4 resolve.
      verify: **carried — and this step's premise is false as written, unanimously.** Both seats held that the claim row must keep `status: unbacked` and an **empty** `last_verified` until a valid run completes, because dating a partial observation reads as verification; and that one passing run could not back the claim in any case, since the bar reads "EACH of the next two". So `docs/CLAIMS.md:487` is deliberately **left untouched** by this change — writing a date there would be the goalpost-moving the claim's own terms forbid.
      Both seats also held that the parked stub may **not** be disposed of on this step's stated ground. The step says Phases 3 and 4 resolve the stub's two trust boundaries; reproduction shows they resolve **one**. Phase 3 resolved raw-named-evidence handling (opaque id, gitignored artefacts, defect-derived filename, `ENC1:` provenance). The **outbound third-party fetch boundary is untouched** — a fetch is still a fetch, and Phases 3-4 contain nothing that addresses it. `agents/roadmaps/stubs/road-to-first-reference-analysis-run.md` therefore stays, annotated as partially resolved and cross-referenced to the receiver. When it is eventually disposed of, the record must attribute the fetch boundary's resolution to a **run-specific authorization**, never structurally to Phases 3-4.

## Blockers

### blocker: wave-consolidate-estate-slot

- **Status:** resolved
- **Owner:** maintainer
- **Blocks:** Phase 5 — One gap, one roadmap, and one real run
- **Recommendation:** decided — see the resolution below. The recommendation
  field is retained as history; it read "none; this is the owner's call" while
  the entry was open.
- **If you do nothing:** no longer live. While the entry was open the stated
  cost was that every run keeps writing its consolidated result to the
  gitignored local area only and the estate-ratchet allowance question stays
  open indefinitely. The decision below adopts the first half deliberately and
  closes the second.
- **What to do:** nothing further. The decision is applied in this change:
  1. Consolidated results stay **local-only**. A run writes its consolidated
     result to `agents/.harvest-local/` and nowhere else; step 5.1's landing-zone
     half is settled on that reading and step 4.5's batch output never reaches
     `agents/roadmaps/`.
  2. **No** active top-level estate slot is granted to a per-run consolidate
     roadmap.
  3. **No** T2/T4 boundary change is made in `src/scripts/check_estate_count.ts`.
     That file is deliberately untouched by this roadmap; verify with
     `git diff origin/main -- src/scripts/check_estate_count.ts` returning empty.
- **Resolved when:** satisfied. The owner's standing delegation routed this to
  the AI council, which stated the second of the two enumerated outcomes —
  consolidated results stay local-only.

**Outcome state: decided** (not re-scoped, not descoped). The decision was taken
by the AI council under the maintainer's standing delegation of 2026-09-06, run
19 session 4, **unanimous across both seats** (anthropic, openai). It is recorded
here rather than in a new `docs/decisions/` entry because the second enumerated
option in `What to do` — the one adopted — changes nothing in the tree that an
ADR would govern; it declines a change.

The reasoning, stated so a later reader can weigh it rather than only obey it:
widening a ratcheted gate's boundary to admit a **recurring** artefact is not
bookkeeping. The estate ratchet exists as a forcing function — an addition that
cannot be offset costs an allowance, which is what makes an author fold work into
an existing entry instead of opening a new one. A per-run consolidate roadmap is
by construction recurring, so granting it a slot would not spend the allowance
once; it would convert the allowance into a standing exemption and remove the
pressure the gate exists to apply. That is a weakening of a governance control,
and under the run-19 disposition framework accepting such a weakening is
categorically outside what may be decided here, whereas declining it is
reachable as preservation of the status quo.

Scope of this refusal: **this round**. Current behaviour is unchanged, and no
future ruling is prejudged — a later owner may grant a slot, and this entry is
not a statement that the project never will. What a future request would have to
supply is the thing missing today: a bound on how many slots a recurring process
may consume over a stated window, so the grant is a budget rather than an open
allowance.

Consequence carried honestly rather than hidden: no batch is visible in the
roadmap dashboard. That was named as the cost of the local-only option while the
entry was open, and adopting the option adopts the cost. The dashboard's silence
about harvest runs is a chosen trade, not an oversight.

Blocks, restated at closure: the landing-zone half of Phase 5 step 5.1 is
settled — a run writes its consolidated result to the gitignored local area only.
The ownership fold gate itself was never blocked and proceeds unchanged.

## Open questions

Carried deliberately as questions rather than as steps, because each refines a mechanism that does not exist yet and would otherwise be built before it has anything to refine:

- Whether resume state needs a per-pass state machine (seed done, loop 1 done, loop 2 done, finalizing) rather than the status vocabulary in step 4.4. Revisit after the first interrupted batch shows where it actually resumes badly.
- Whether freshness should become capability-sensitive, so that an unrelated documentation commit in this package does not invalidate historical analyses. Step 4.4 already excludes this package's revision from the key, which is the cheap version of the same fix; the expensive version waits for evidence that it is needed.
- Whether the not-a-repository tokens (articles, threads, a paper, a video) should be routed to inbox analysis rather than only recorded in the manifest.

## Risk Register
<!-- risk-review: v1 | reviewed: 2026-09-05 | reviewer: claude/host -->

| Rank | Item | Risk type | Description | Mitigation | Anchored under |
|------|------|-----------|-------------|------------|----------------|
| 1 | Source identity leaks into a tracked surface | implementation | Filenames, document headers, subagent arguments and error snapshots are all surfaces; the command leaks through two of them today (`command.md` § 6 slug rule and § 7 draft path) | Phase 3 in full — opaque id, gitignored artefacts, defect-derived filename, no plaintext argument; verified by `check_no_external_sources` and a `git status` check after a run | Phase 3 — The output stops naming its source |
| 2 | The loop becomes three rewordings | product | Four passes over one reference produce prose churn instead of new findings, and the cost is paid in external fetches | Three named lenses with different questions, a decision-quality target metric, and a delta block per loop that records zero as zero | Phase 2 — A bounded loop with three different questions |
| 3 | The claim window expires unmeasured | product | The pre-registered measurement has a fixed window from its merge, and expiry counts as the bar not cleared, reverting three mechanisms | Step 5.2 runs before any batch work, so scaffolding does not consume the window | Phase 5 — One gap, one roadmap, and one real run |
| 4 | Batch output floods the estate | product | Over a hundred unique tokens, each capable of proposing a roadmap, against an active floor of 1 | Ownership fold gate and candidate registry in step 5.1; a small default batch limit in step 4.5; landing zone undecided and local-only until the blocker resolves | Phase 5 — One gap, one roadmap, and one real run |
| 5 | The rename breaks inbound references | implementation | 18 files name the old command, one of them a rule calling it the canonical flow | Step 1.2 in the same change, with a grep as its verification | Phase 1 — One analysis engine, one name |
| 6 | Context exhaustion during a batch | implementation | Over a hundred identities at four passes each cannot share one session | One subagent per repository, sequential, no fan-out; the manifest is the only shared state | Phase 4 — Deterministic discovery and a thin harvester |
| 7 | The discovery script silently under-reports | implementation | A missing key, a prose placeholder matched as a token, or a repository host not in the pattern set all produce a quietly short census | Refuse-without-key rather than skip; unit tests for extraction, canonicalization and classification; the dry-run census is checked against the hand-derived figures in step 4.1 | Phase 4 — Deterministic discovery and a thin harvester |

## Acceptance Criteria

- [x] AC-1 — A single command directory holds the repository-analysis engine; the old name resolves through `replaces:`; no second implementation and no alias command exist. **Met:** `analyze/repo/` is the only directory (the old one was `git mv`d, not copied); `replaces: [analyze-reference-repo, analyze:reference-repo]` carries both prior names; `dist/agent-src/commands/analyze/` lists `repo.md` and no `reference-repo.md`; no shim command file was created.
- [x] AC-2 — That command's frontmatter carries a `limits:` block and its body restates every limit line. **Met:** all five schema-required keys present and schema-valid; each of the five appears in the body below the frontmatter fence, carried by its own restatement heading; pinned by `tests/scripts/analyze_repo_limits.test.ts` and sabotage-probed.
- [~] AC-3 — A run with three loops leaves three delta records, each naming a different lens, and a zero-delta loop is recorded rather than skipped. <!-- deferred-resolution: carried-to=road-to-first-reference-analysis-observation --> **Carried:** the contract, the document skeleton and the never-omit rule are written and pinned (steps 2.3, 2.4), but this AC asserts what *a run leaves behind*, and no run has occurred. Received by `road-to-first-reference-analysis-observation` step 1.4.
- [~] AC-4 — After a full run, the only new tracked file is the roadmap the run deliberately landed; the source-name check passes and no landed filename contains the reference's owner or repository name. <!-- deferred-resolution: carried-to=road-to-first-reference-analysis-observation --> **Partially met, and split honestly.** Clauses 2 and 3 hold now: `check_no_external_sources` is green, and the filename rule is enforced by `roadmapNameIsSourceFree()` rather than asserted in prose. Clause 1 — "after a full run" — is an observation of a run and is received by `road-to-first-reference-analysis-observation` step 1.3, whose verify carries the `git status --porcelain` check verbatim.
- [x] AC-5 — The discovery script's dry run reproduces the estate census by hand-checkable counts and writes nothing outside the gitignored local area. **Met:** the dry run reports 149 / 117 / 63 with archive=57, later=6, active=0 — every pinned figure. The dry-run path writes nothing at all (census to stdout, no file), and the manifest path resolves under `agents/.harvest-local/`, which `.gitignore:290` covers as a directory.
- [x] AC-6 — An interrupted batch resumed with the resume flag runs no repository twice, and fresh entries are skipped unless refresh is requested. **Met** at the mechanism, which is where it is decidable: `planResume` is executed against the interrupted-after-the-second state and returns one runnable entry, two `skipped-as-fresh`, and a reason per skip; `--refresh` flips exactly those and nothing else. Stated precisely — this is the resume planner under fixture, not a batch over real repositories, and the real batch is not in this roadmap's scope (step 4.5 ships the harvester, step 5.2 ships the first single run).
- [x] AC-7 — Two repositories surfacing one capability gap leave one roadmap carrying two registered sources. **Met:** `foldCandidates` fixture returns exactly one `Fold` with `sources` of length 2, and the three-repository case still returns one. Sabotage-probed by keying the fold on the repository instead of the gap, which reds it.
- [~] AC-8 — The pre-registered claim row carries a verification date and an outcome that matches what the runs produced, and the parked stub is disposed. <!-- deferred-resolution: carried-to=road-to-first-reference-analysis-observation --> **Carried, and its first clause is refused for now on unanimous council grounds:** the row must keep an **empty** `last_verified` until a valid observation completes, because a date on a partial observation reads as verification — so writing one to satisfy this AC would be the goalpost-moving the claim forbids. The stub is likewise **not** disposed of, because only one of its two stated trust boundaries was resolved. Received by `road-to-first-reference-analysis-observation` steps 1.5 and 1.6.
