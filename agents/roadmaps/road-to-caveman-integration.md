---
complexity: structural
---

# Roadmap: Caveman Integration (Option 1 hardened)

> Wire caveman (`compress` + `shrink` + `speak`) into agent-config as a required, hash-locked dependency so output tokens are reduced via the caveman-speak grammar — without path-conflict against the seven host-tool projections.

## Prerequisites

- [ ] Read `AGENTS.md`, `.agent-src.uncompressed/rules/language-and-tone.md`, `.agent-src.uncompressed/rules/direct-answers.md`, `.agent-src.uncompressed/rules/user-interaction.md`
- [ ] Read the locked pitch decisions (Goal / Constraints / AC) captured in this file's `## Context` section
- [ ] Confirm `juliusbrussee/caveman` upstream still publishes `caveman-shrink` to npm (MIT) and that the `caveman-compress/` source tree remains in the upstream repo (not on npm — see Phase 2 preamble "Package reality" note)
- [ ] Capture the upstream commit SHA for `caveman-speak.md` (Source-of-Truth pin)

## Context

agent-config is a distribution package (composer + npm) that owns seven projection paths
(`.augment/`, `.claude/`, `.cursor/`, `.clinerules/`, `.windsurfrules`, `GEMINI.md`,
`AGENTS.md`, `copilot-instructions.md`). Caveman ships its own installer that writes into the
same paths, which is the structural conflict driving Option 1: agent-config stays the single
owner of all projections and inlines caveman content as locked source.

The caveman suite has three layers:

- **`caveman-speak`** — telegram-style output grammar, ~40-60 % output-token reduction (markdown grammar file in upstream repo, mirrored under SHA-pin via Phase 1)
- **`caveman-compress`** — build-time `.md` compressor, ~46 % input-token reduction (Python/Shell source tree at `caveman-compress/` in upstream repo; **NOT** an independent npm package; install mechanism decided in Phase 2)
- **`caveman-shrink`** — MCP stdio proxy that compresses tool descriptions at runtime (real npm package `caveman-shrink`, MIT)

License: MIT for the entire upstream repo (`juliusbrussee/caveman`) — bundling, vendoring, and modification of `caveman-compress/` are all license-compatible.

This roadmap integrates all three as required dependencies (not optional like `rtk`), with a
three-switch toggle (`caveman.speak` / `.compress` / `.shrink`) plus a master `caveman.enabled`
flag in `.agent-settings.yml` for runtime opt-out without reinstall.

The `language-and-tone`, `direct-answers`, and `user-interaction` Iron-Law rules need a
rewrite to make caveman-speak the global default while preserving full prose in two
exception classes: numbered-options blocks and Iron-Law responses.

- **Council convergence (Sonnet + GPT-4o, 2026-05-07):** both confirmed Option 1 as
  structurally sound. Sonnet contributed the three hardening additions (SHA-pin,
  content-hash-verify, `.local`-override). Trace: `agents/council-responses/caveman-integration.json`.
- **Predecessor:** `agents/roadmaps/archive/caveman-compress-integration.md` — earlier roadmap
  that explicitly replaced caveman-compress with manual agent-driven compression. This roadmap
  reverses that decision via Phase 2 (Hard Cutover variant): caveman-compress is the only
  compression path; the 225-line agent-driven compress procedure is removed; `caveman.compress:
  false` makes `task generate-tools` copy uncompressed sources directly to projections.
- **Feature:** none (challenge-vision pitch)
- **Jira:** none

## Phase 0: Lock schema + settings schema

Resolves the council-flagged ordering issues: Phase 2's settings reads and Phase 3's derivative-artifact lock status both depend on schemas that were originally defined later. Phase 0 commits both schemas before any consumer phase runs.

- [ ] **Step 1:** Define `.build-deps.lock` schema with three artifact classes — (a) upstream artifacts requiring SHA-pin + content-hash (initial entry: `caveman-speak.md`), (b) derivative artifacts requiring their own lock entry (criteria: file directly quotes upstream verbatim under a stable anchor), (c) generated projections explicitly EXEMPT from locking (criteria: file is fully regenerated by `task generate-tools` from agent-config-owned source).
- [ ] **Step 1.5:** Write `.build-deps.lock.schema.json` (JSON Schema Draft 2020-12, R4-B2 fix) formalising the three artifact classes from Step 1. Required top-level keys: `schema_version`, `artifacts` (array). Per-artifact required keys: `path`, `artifact_class` (enum `["upstream", "derivative", "exempt"]`), `upstream_url` (string OR array for mirror fallback), `pinned_commit_sha` (matches `^[a-f0-9]{40}$`), `expected_content_hash` (matches `^[a-f0-9]{64}$`). Per-artifact optional keys: `mirror_urls` (array), `update_policy` (enum). Add a CI check that validates `.build-deps.lock` against the schema on every push.
- [ ] **Step 2:** Document the lock / no-lock decision tree in `docs/architecture.md` under a new "External source pinning" section. Cross-reference from `AGENTS.md`.
- [ ] **Step 3:** Add the full `caveman:` block to `.agent-src.uncompressed/templates/.agent-settings.yml` — `enabled` master switch + `speak` / `compress` / `shrink` sub-switches, default all `true`. (This was originally Phase 4 Step 4; pulled forward so Phases 2 / 3 / 4 can read settings without forward-references.)
- [ ] **Step 4:** Add a settings-read helper in `scripts/` (or extend the existing config loader) that returns the resolved caveman toggle state with documented precedence (master switch overrides sub-switches when `false`).
- [ ] **Step 5:** Commit Phase 0 schema decisions + settings template before any Phase 1 work begins.

## Phase 1: Source mirror + SHA pin + content-hash verify

- [ ] **Step 1:** Populate `.build-deps.lock` (schema defined in Phase 0 Step 1) with the initial upstream entry for `caveman-speak.md`: upstream URL, pinned commit SHA, expected SHA-256 content hash.
- [ ] **Step 2:** Implement `scripts/sync-upstream.sh` — fetch each `.build-deps.lock` entry from `https://raw.githubusercontent.com/juliusbrussee/caveman/<SHA>/<path>` into `.agent-src.uncompressed/contexts/communication/caveman-speak.md.vendor`, recompute SHA-256, fail hard on hash mismatch with a clear error message that names the drifted file.
- [ ] **Step 3:** Add `.local`-override path: when `.agent-src.uncompressed/contexts/communication/caveman-speak.md.local` exists, `sync-upstream.sh` copies it to `.vendor` instead of fetching upstream and prints a dev-mode warning.
- [ ] **Step 4:** Wire `task sync-upstream` into the existing `task sync` task before `task generate-tools` runs.
- [ ] **Step 5:** CI workflow check: fail the build if `.agent-src.uncompressed/contexts/communication/caveman-speak.md.local` is committed to a non-feature branch.
- [ ] **Step 6:** Extend the `docs/architecture.md` "External source pinning" section (created in Phase 0 Step 2) with the runtime fetch flow, the `.local`-override semantics, and the CI gate from Step 5.
- [ ] **Step 7:** Write the SHA-drift recovery runbook in `docs/troubleshooting.md` covering three named scenarios — (a) transient outage / GitHub unreachable: retry policy + last-known-good window, (b) intentional upstream update: PR template requiring upstream-diff review + local test run + maintainer sign-off + lock-file bump, (c) suspected compromise: halt builds + revert to `.local` override + notify security contact.
- [ ] **Step 8:** Add CI escalation: after >3 consecutive `sync-upstream` failures, the workflow surfaces a clearly-labeled job failure (paging hook is project-policy, not in scope of this roadmap).

## Phase 2: caveman-compress Hard Cutover

Variant A from the 2026-05-07 follow-up: caveman-compress is the **only** compression path. The 225-line agent-driven compress procedure is removed; the dual-path / fall-back design is rejected because (a) caveman-compress is a required dep — an *LLM-driven* compression fallback contradicts that (pass-through with `compress: false` is a valid runtime mode, **not** an install-failure fallback — see R4-C2 clarification below), (b) two implementations drift, (c) the agent-driven path defeats the whole token-saving rationale of the integration. The Step 5 "Enrich (SKILL.md only)" agent work is *out of scope* for this roadmap and stays inline in `commands/compress.md` for now (separate roadmap item will extract it into `/skill:enrich`).

**Package reality (2026-05-07 correction):** `caveman-compress` is **not** an independent npm package. It lives as a Python/Shell source tree at `caveman-compress/` in the upstream `juliusbrussee/caveman` repo (MIT). Three install-mechanism candidates exist for getting it onto consumer machines across all distribution channels (npm, composer, marketplace, manual copy): (1) **vendor-under-SHA-pin** — extend the Phase 1 `.build-deps.lock` mechanism to a directory tree, (2) **postinstall-bridge** — call upstream `install.sh` from a postinstall hook (requires Node + bash on consumer), (3) **bundled binary** — pre-build and ship `caveman-compress` as a platform-binary alongside the package (no consumer toolchain needed).

**Council R4 verdict (2026-05-07):** mechanism (3) **bundled binary** is the primary path; mechanism (1) **vendor-under-SHA-pin** is the transparent fallback when the platform slug is unsupported. Mechanism (2) is rejected — postinstall hooks do not run inside marketplace ZIP / manual-copy channels. Trace: `agents/council-responses/road-to-caveman-integration-roadmap-r4.json` (deep tier, $0.117 actual). The verdict-rationale and the cross-channel scoring matrix are reproduced in the new R4 review section at the bottom of this roadmap.

**R4-C2 clarification — maintainer vs. consumer:** Both maintainer-side (agent-config development → `.agent-src/` build) and consumer-side (downstream projects re-compressing local overrides under `agents/overrides/`) treat `caveman-compress` as a required dep so consumers also benefit from token savings. `caveman.compress: false` is a runtime mode where pass-through is acceptable (uncompressed sources flow to projections); it is **not** a graceful-degradation path for install failures. Install failures are handled by the bundled-binary → vendor-fallback chain in Step 1, which terminates in a clear, named error only when **both** paths fail.

- [ ] **Step 1:** Bundle `caveman-compress` as platform-specific binaries (PyInstaller or Nuitka, 6× CI matrix: `linux-x64`, `linux-arm64`, `darwin-x64`, `darwin-arm64`, `win32-x64`, `win32-arm64`). Ship the matrix under `bin/caveman-compress-<platform>` inside the published package. `scripts/install.sh` resolves the platform slug at install time and symlinks (or copies, on Windows) the matching binary into `node_modules/.bin/caveman-compress` (npm), `vendor/event4u/agent-config/bin/caveman-compress` (composer), or the equivalent path inside the marketplace ZIP after extraction. **Vendor fallback (mechanism 1):** when the platform slug is unsupported (e.g. exotic CPU arch, Alpine musl, BSD), `install.sh` falls back transparently to the vendored Python source tree at `caveman-compress/` (SHA-pinned via `.build-deps.lock` per Phase 0 Step 1.5 schema, mirrored by `scripts/sync-upstream.sh`). The vendor path requires `python3 ≥ 3.10` on the consumer; missing-Python triggers a clearly-labelled install-time error naming the binary slug attempted, the vendor fallback attempted, and the install command for `python3`. Add `caveman-shrink` to `package.json` `dependencies` (npm — real package; required dep, no `optionalDependencies` clause).
- [ ] **Step 2:** Refactor `scripts/compress.py` into a thin caveman-compress wrapper: keep the SHA-256 hash-tracking (`.compression-hashes.json`), keep `check_compression.py` post-checks, delegate the actual prose compression to caveman-compress. Delete every inline grammar rule, every Iron-Law fence, every "copy-paste-first" branch — that logic lives in caveman-compress now.
- [ ] **Step 3:** Update `scripts/compress.sh --sync` to call the simplified Python wrapper; preserve `.compression-hashes.json` tracker semantics.
- [ ] **Step 4:** Make `task generate-tools` honor `caveman.compress: false` by copying `.agent-src.uncompressed/` sources directly to the seven projection paths (no compression, no agent-time work). When `caveman.compress: true` (default), it consumes `.agent-src/` (caveman-compressed output) as today.
- [ ] **Step 5:** Slim `.agent-src.uncompressed/commands/compress.md` from 225 lines to ≤ 30 lines: it now describes "run `task compress`" + the hash-tracking workflow + the toggle behavior. Move the deleted grammar-rule content to a one-paragraph upstream-reference pointing at caveman-compress's own docs. The Step 5 "Enrich (SKILL.md only)" block stays as a "Skill enrichment" appendix until the future `/skill:enrich` extraction.
- [ ] **Step 6:** Update `tests/test_compress.py`: drop dual-path fixtures, assert only the simplified wrapper behavior (caveman.compress true → caveman invoked + hashes tracked; caveman.compress false → no compression invoked, sources passed through). Existing 22 tests collapse into the smaller, single-path set.
- [ ] **Step 7:** Define the simplified failure-mode precedence in `docs/architecture.md`: (a) `caveman.enabled: false` OR `caveman.compress: false` → no compression, sources pass through; (b) `caveman.enabled: true` + `caveman.compress: true` + binary present → delegate to caveman-compress; (c) `caveman.enabled: true` + `caveman.compress: true` + binary missing → fail hard with a clearly-labeled error naming the missing binary and the install command. Apply the same matrix shape to caveman-shrink in Phase 4.

## Phase 3: Dedicated caveman-speak rule + minimal cross-references

Restructured per follow-up convergence (2026-05-07): a single new tier-1 rule owns the entire caveman-speak behavior. The three existing Iron-Law rules get only one-line cross-references, so toggling `caveman.speak: false` is a router-level no-load (no fall-back branches in three rules) and removal collapses to deleting one file.

- [ ] **Step 1:** Add new tier-1 rule `.agent-src.uncompressed/rules/caveman-speak.md` — frontmatter `tier: 1`, `triggers: [always]` (gated by `caveman.speak: true` at router-compile time, see Step 6); body covers (a) caveman-speak grammar applies to all free prose, (b) two exception classes preserved in full prose: numbered-options blocks and Iron-Law responses, (c) cross-reference to `.agent-src.uncompressed/contexts/communication/caveman-speak-grammar.md` for the locked grammar source. Size budget ≤ 800 chars (≤ 3 % of the 26k kernel/tier-1 bucket).
- [ ] **Step 2:** Add the locked grammar context `.agent-src.uncompressed/contexts/communication/caveman-speak-grammar.md` that quotes the `caveman-speak.md.vendor` mirror (from Phase 1) under a stable anchor and documents the exception classes in one canonical place. The new rule from Step 1 is the only caller.
- [ ] **Step 3:** Add a one-line cross-reference to `.agent-src.uncompressed/rules/language-and-tone.md` pointing at `caveman-speak` for output grammar; do NOT inline caveman-specific content. Iron Law 1 (mirror user language) stays unchanged.
- [ ] **Step 4:** Add a one-line cross-reference to `.agent-src.uncompressed/rules/direct-answers.md` Iron Law 3 (brevity) noting that caveman-speak grammar from `caveman-speak` rule coexists with "shortest reply"; do NOT inline grammar.
- [ ] **Step 5:** Add a one-line cross-reference to `.agent-src.uncompressed/rules/user-interaction.md` confirming numbered-options blocks fall under the exception class defined in `caveman-speak`; the `Recommendation:` label rule stays unchanged.
- [ ] **Step 6:** Extend `scripts/compile_router.py` so the new `caveman-speak` rule is omitted from `router.json` when `caveman.speak: false`. No runtime branching inside the rule body — load decision is a build-time toggle.
- [ ] **Step 7:** Update `.agent-src.uncompressed/templates/AGENTS.md` and `.agent-src.uncompressed/templates/copilot-instructions.md` with a one-paragraph "caveman-speak default + exceptions" notice that points consumers at the `caveman-speak` rule.

(Note: the original Phase 3 step 6 — manual inspection of projected outputs for caveman co-writes — has been replaced by `tests/test_projection_isolation.py` in Phase 5 Step 1, per first-pass council convergence.)

## Phase 4: caveman-shrink MCP-proxy wiring

- [ ] **Step 1:** Add `caveman-shrink` to `package.json` `dependencies` (npm).
- [ ] **Step 2:** Add a configuration block in `.agent-src.uncompressed/templates/.augment/mcp.json` (or the equivalent MCP wiring spec) that starts `caveman-shrink` only when `caveman.enabled` and `caveman.shrink` are both true.
- [ ] **Step 3:** Document the wiring in the `mcp` skill (`.agent-src.uncompressed/skills/mcp/SKILL.md`) with the toggle precedence and a short troubleshooting block.
- [ ] **Step 4:** Apply the failure-mode precedence matrix from Phase 2 Step 6 to caveman-shrink (binary-missing case fails hard with named error + install hint).

(Note: the previous Phase 4 Step 4 — adding the `caveman:` block to the settings template — was pulled forward to Phase 0 Step 3.)

## Phase 5: Tests, docs, and release notes

- [ ] **Step 1:** Add `tests/test_projection_isolation.py` — runs `task generate-tools` in a clean checkout and asserts that none of the seven projection paths (`.augment/`, `.claude/`, `.cursor/`, `.clinerules/`, `.windsurfrules`, `GEMINI.md`, `AGENTS.md`, `copilot-instructions.md`) contains files matching `caveman-shrink|juliusbrussee/caveman` markers. Replaces the manual-inspection step from the original Phase 3 Step 6.
- [ ] **Step 2:** Add a Pytest case in `tests/test_caveman_toggle.py` that flips `caveman.enabled: false` and asserts: caveman-speak grammar is absent from sample outputs, no caveman-compress process is spawned, no caveman-shrink MCP child is started.
- [ ] **Step 2.5:** Add `tests/test_router_compilation.py` (R4-C1 / R3-P-B fix) — set `caveman.speak: false` in `.agent-settings.yml`, run `task compile-router` (or the compile step of `task generate-tools`), parse the produced `.augment/router.json` (or equivalent projection) as JSON, assert that `"caveman-speak"` (the rule's `id` key) is **not** present in the `rules` array. Flip `caveman.speak: true`, recompile, assert the rule **is** present. Compile-time exclusion is a contract beyond the runtime test in Step 2 — Step 2 verifies output behaviour, Step 2.5 verifies build artefact contents.
- [ ] **Step 3:** Add a Pytest case that flips each sub-switch independently and asserts only the corresponding subsystem is affected.
- [ ] **Step 4:** Add a Pytest case that asserts numbered-options blocks and Iron-Law responses keep full prose under every toggle combination, using a "≥ 6 words per option line" heuristic on rendered samples.
- [ ] **Step 5:** Add a Pytest case that simulates a SHA-drift on `caveman-speak.md.vendor` and asserts the build fails with the documented error message.
- [ ] **Step 6:** Update `README.md`, `AGENTS.md`, and `docs/architecture.md` with the caveman section: required dependency, three-switch toggle, exception classes, predecessor reference to the archived `caveman-compress-integration.md`.
- [ ] **Step 7:** Run the full quality pipeline (`task ci`) and capture a fresh-output evidence block for the verification gate.

## Acceptance Criteria

- [ ] `task sync` fetches `caveman-speak.md` from the pinned SHA, verifies SHA-256 against `.build-deps.lock`, and fails with a clear, named error on drift or unreachability.
- [ ] `task generate-tools` writes zero caveman-owned files into `.augment/`, `.claude/`, `.cursor/`, `.clinerules/`, `.windsurfrules`, `GEMINI.md`, `AGENTS.md`, or `copilot-instructions.md`.
- [ ] `caveman.enabled: false` in `.agent-settings.yml` deactivates output-style + compress-step + shrink-proxy in one toggle, with no reinstall and no repo revert.
- [ ] `caveman.speak: false` while `compress` and `shrink` stay `true` keeps build-time + runtime token savings while restoring full prose in chat output.
- [ ] Every numbered-options block and every Iron-Law response in the rendered output remains in full prose under all toggle combinations (asserted by tests).
- [ ] `scripts/install.sh` fails with a clear, actionable error when `caveman-shrink` cannot be installed via npm, or when `caveman-compress` (deployed via the R4-selected mechanism) is unavailable on the consumer machine.
- [ ] `.agent-src.uncompressed/contexts/communication/caveman-speak.md.local` (when present) overrides the locked upstream mirror in dev mode; CI rejects the file on non-feature branches.
- [ ] `docs/architecture.md` documents the lock / no-lock decision tree (Phase 0 Step 2) covering upstream artifacts, derivative artifacts, and exempt projections.
- [ ] `docs/troubleshooting.md` documents the SHA-drift recovery runbook with the three named scenarios (transient outage, intentional update, suspected compromise); CI escalates after >3 consecutive `sync-upstream` failures.
- [ ] `docs/architecture.md` documents the failure-mode precedence matrix for `caveman-compress` and `caveman-shrink`; runtime invocation with `caveman.enabled: true` + binary missing fails hard with a clearly-labeled error naming the missing binary and the install command (no silent degradation).
- [ ] `tests/test_projection_isolation.py` passes against a fresh `task generate-tools` run, asserting no caveman markers in any of the seven projection paths.
- [ ] The new `caveman-speak` rule owns 100 % of the caveman output-grammar logic; `language-and-tone`, `direct-answers`, and `user-interaction` contain only one-line cross-references and zero inline caveman grammar (asserted by grep + size-diff check).
- [ ] `caveman.speak: false` removes the rule from `router.json` at build time (no runtime branching inside the rule body).
- [ ] `caveman-speak.md` rule body fits the per-rule budget (≤ 2.5k chars, target ≤ 800).
- [ ] `.agent-src.uncompressed/commands/compress.md` is ≤ 30 lines after the Hard Cutover; the previous 225-line agent-driven grammar procedure is removed (zero inline grammar rules, zero "copy-paste-first" branches in `scripts/compress.py`).
- [ ] `caveman.compress: false` makes `task generate-tools` copy `.agent-src.uncompressed/` sources directly to the seven projection paths with zero agent-time work and zero caveman invocation.
- [ ] `task compress` is a thin caveman-compress wrapper: it preserves SHA-256 hash-tracking and `check_compression.py` post-checks, and contains no LLM-driven prose-rewrite logic.
- [ ] All quality gates pass (`task lint-skills`, `task check-portability`, `task check-refs`, `task lint-rule-budget`, `pytest`, `tests/test_install.sh`).

## Notes

- **Out of scope:** caveman's stats / tracking features, Classical-Chinese mode and other caveman-output variants, the upstream PR for npm-`exports` (Council's "Option 5" — separate roadmap item, only after this one ships), extraction of the Step 5 "Enrich (SKILL.md only)" agent procedure into a dedicated `/skill:enrich` command (separate roadmap item; stays inline in `commands/compress.md` until then).
- **Open assumption:** pitch / plan / roadmap markdown is currently NOT an extra exception class — caveman-speak applies. If reviewer feedback flags this as friction, a follow-up ADR widens the exception list.
- **Open assumption:** caveman-shrink remains maintained on npm and the upstream `juliusbrussee/caveman` repo continues to host `caveman-compress/` under MIT; stagnation > 6 months on either triggers re-evaluation.
- **Open assumption:** caveman-speak's upstream path stays stable. Content-hash catches drift in place; an upstream path move requires a manual SHA + URL update in `.build-deps.lock`.
- **Council trace (pitch):** `agents/council-responses/caveman-integration.json` (Sonnet + GPT-4o, 2026-05-07).
- **Council trace (this roadmap):** `agents/council-responses/caveman-integration-roadmap.json` (Sonnet + GPT-4o, 2026-05-07).
- **Council trace (R3, post-Hard-Cutover):** `agents/council-responses/road-to-caveman-integration-roadmap.json` (Sonnet + GPT-4o, 2026-05-07, deep tier, $0.0645).
- **Council trace (R4, install-mechanism + structural review):** `agents/council-responses/road-to-caveman-integration-roadmap-r4.json` (Sonnet + GPT-4o, 2026-05-07, deep tier, $0.117 actual).
- **Predecessor:** `agents/roadmaps/archive/caveman-compress-integration.md` is reversed by Phase 2 of this roadmap.

## Council review (2026-05-07)

Pre-execution review by `anthropic/claude-sonnet-4-5` + `openai/gpt-4o` (mode: roadmap, 2 rounds, total $0.0684). Trace: `agents/council-responses/caveman-integration-roadmap.json`. Findings surfaced as-is — the roadmap body above is not auto-rewritten; act on the items deliberately.

### Convergence (both reviewers agree)

- **Settings schema arrives too late** — Phase 2 Step 2 reads `caveman.enabled` / `caveman.compress`, but the schema is only defined in Phase 4 Step 4. Sonnet flags this as a blocker; GPT-4o views it as resolvable via dev-time mocks.
- **Phase 3 Step 6 is manual inspection, not a test** — the "no co-write into the seven projection paths" constraint is asserted by visual review, with no automated verification.
- **SHA-drift recovery runbook is missing** — the build fails hard on hash mismatch, but no documented workflow tells a developer what to do at 3am (transient outage vs. intentional update vs. suspected compromise).
- **Runtime binary unavailability is undefined** — AC covers install-time failure for `caveman-compress` / `caveman-shrink`, but not runtime ("`caveman.enabled=true` + binary missing → fallback? fail? silent degradation?").
- **`caveman-speak-grammar.md` lock status is ambiguous** — Phase 3 Step 4 creates the file by quoting the locked vendor mirror, but it is unclear whether this derivative artifact requires its own `.build-deps.lock` entry or is exempt as a generated projection.

### Divergence

- **Circular dependency Phase 2 ↔ Phase 4 (settings):** Sonnet flags it as a structural blocker requiring schema relocation; GPT-4o judges it overstated since mocks suffice during phase-internal development.
- **Recommendation style:** Sonnet is technically specific (concrete pseudocode, new Phase 0, explicit step-numbering); GPT-4o is procedural (risk matrix, environment handling, dependency-review checkpoint).

### Sonnet's concrete proposals (not yet adopted)

1. Insert **Phase 0: Lock schema definition** — covers upstream artifacts, derivative artifacts requiring locks, generated projections explicitly exempt; commits the decision tree before Phase 1 begins.
2. Pull the `.agent-settings.yml` `caveman:` schema into **Phase 1 Step 0**, before Phase 2 reads it.
3. Replace Phase 3 Step 6 with `tests/test_projection_isolation.py` (regex-match for `caveman-shrink` / `juliusbrussee/caveman` across all seven projection paths) and move the test into Phase 5.
4. Add **Phase 1 Step 2.5 / 2.6** — SHA-drift recovery runbook (`docs/troubleshooting.md`) covering transient outage, intentional update, suspected compromise; CI page-on-call escalation after >3 consecutive failures.
5. Add **Phase 2 Step 2.5** — explicit failure-mode precedence table (`enabled=false` / `enabled=true + binary missing` / etc.) with documented behavior per cell.

### Truncation notice

Sonnet's response hit the 2048-token output cap mid-list at amendment 5 of an announced 9. Amendments 6–9 are not in the trace. Re-running with `--max-tokens 4096` would surface the remaining four; not done here to keep cost contained.

### GPT-4o's additional themes (not yet adopted)

- Granular per-phase-transition test cases (modular over end-to-end).
- Dependency-management checkpoint for every new `.build-deps.lock` entry (necessity + scope review).
- Explicit handling for dev / staging / production environments, especially around the `.local` override.
- Risk matrix as a roadmap appendix (likelihood × impact, prioritized mitigations).

## Council review R3 (2026-05-07, post-Hard-Cutover)

Deep-tier review by `anthropic/claude-sonnet-4-5` + `openai/gpt-4o` (mode: roadmap, deep, total $0.0645). Trace: `agents/council-responses/road-to-caveman-integration-roadmap.json`. The host has applied a critical-evaluation pass — see the verdict block below. Member text is preserved verbatim in the trace.

### Sonnet's findings (Round 3)

1. **Phase 0 Settings-File-Instantiation gap** — Phase 0 Step 3 adds the `caveman:` block to the *template*, but the roadmap never specifies when/how the consumer's actual `.agent-settings.yml` is instantiated, what happens on upgrade, or how schema-evolution is verified. Phase 0 Step 4 assumes the file exists with the schema intact.
2. **Hard Cutover contradiction (claimed)** — Sonnet asserts `caveman.enabled: false` + required dep + fail-hard form an incompatible triangle, breaking the "no reinstall" promise.
3. **Phase 5 tests runtime, not compile-time `router.json`** — AC mandates `caveman-speak` rule absence at compile time; no test parses `router.json` directly to verify the rule is excluded by the compiler when toggled off.
4. **SHA-drift runbook untestability** *(truncated at token cap; partial reasoning only)*.

### GPT-4o's findings (Round 3)

1. **Contingency for upstream deprecation** — tripwire fires on > 6 months stagnation but no defined response (fork / alternative / removal).
2. **Justify non-optional dependency** — request for explicit decision analysis on why `optionalDependencies` / peer-dep variants were rejected.
3. **Per-phase documentation update protocol** — link doc updates to specific commits per phase.
4. **More granular acceptance criteria** — per-toggle test cases with metrics.

### Truncation notice

Sonnet's Round-3 output hit the 2048-token output cap mid-Finding 4 (SHA-drift runbook reasoning). Re-running with `--max-tokens 4096` would surface the rest. Not done here — Findings 1–3 + GPT-4o cover the substantive material.

## Host verdict (R3)

Critical-evaluation pass per `ai-council` SKILL § convener-skeptic stance. Each finding rated against codebase evidence and locked decisions.

| # | Finding | Source | Verdict | Evidence |
|---|---|---|---|---|
| 1 | Phase 0 Settings-File-Instantiation gap | Sonnet | `accept` | Overlaps user's install question; Phase 0 Step 4 reads settings, instantiation flow not defined |
| 2 | Hard Cutover contradiction (`enabled: false` breaks no-reinstall) | Sonnet | `reject` | Phase 2 Step 7 line 80: `enabled: false` → pass-through bypasses binary check; matrix is internally consistent |
| 3 | Phase 5 tests runtime, not compile-time `router.json` | Sonnet | `accept` | AC line 129 mandates compile-time removal; no test verifies `router.json` content |
| 4 | Tripwire reaction for caveman upstream deprecation | GPT-4o | `accept-with-modification` | Line 140 has tripwire without action; collapse to a single `## Note` line, not its own phase |
| 5 | Justify non-optional dependency | GPT-4o | `reject` | Phase 2 preamble line 72 documents the 3 reasons; user-locked in commit `fb459be` |
| 6 | Per-phase documentation update protocol | GPT-4o | `reject` | Generic process advice; covered by `commit-conventions` + Phase 5 Step 6 |
| 7 | More granular acceptance criteria | GPT-4o | `reject` | AC lines 118–134 already concrete + testable; Phase 5 Steps 2–3 have per-switch tests |
| 8 | Consumer-side install contract (npm-transitive caveman in consumer projects) | host-originated | `accept` | Not in roadmap; AC line 122 covers `install.sh` failure only, not the consumer-install path when agent-config is a transitive dep |

**Reject rationale is evidence-based**, not preference-veto. Finding 2 is the most important reject — if applied unread it would unwind the Hard Cutover (Variant A) the user just locked in commit `fb459be`.

### Patches to apply (`accept` + `accept-with-modification`)

- **P-A (Finding 1):** Add Phase 0 Step 3.5 — *Document the settings-file instantiation contract* (template → consumer, first-install vs upgrade, schema-evolution policy) with a CI check that the template stays a superset of all runtime-read keys.
- **P-B (Finding 3):** Add Phase 5 Step 2.5 — *Pytest case parses `router.json` and asserts the `caveman-speak` rule's `id` is absent from the loaded rule set when `caveman.speak: false`*.
- **P-C (Finding 8):** Add an AC bullet + a Phase 5 step covering the consumer-install path — when agent-config is installed as a composer/npm dep into a consumer project, the consumer's `npm install` (or equivalent) must pull `caveman-compress` / `caveman-shrink` transitively, OR the consumer install path must be explicitly documented as out-of-scope (e.g. compression runs only at agent-config maintainer-time, not in consumer projects).
- **P-D (Finding 4):** Convert the tripwire line into a `## Note` listing three response options (fork, switch alternative, remove caveman path) without prescribing which.


## Council review R4 (2026-05-07, install-mechanism + structural review)

Deep-tier review by `anthropic/claude-sonnet-4-5` + `openai/gpt-4o` (mode: roadmap, deep, total $0.117 actual). Trace: `agents/council-responses/road-to-caveman-integration-roadmap-r4.json`. Original ask: pick the install mechanism for `caveman-compress` across all distribution channels (npm, composer, marketplace ZIP, manual copy) and score the three candidates A / B / C. Both members pivoted from a direct A/B/C scorecard into a broader structural review; the verdict below is therefore host-synthesised from Sonnet's B1 analysis (cross-channel scoring is contained within it) plus the user's prior greenlight on Bundled Binary + Postinstall-Bridge.

### Sonnet's findings (Round 4)

1. **B1 — Phase 2 Step 1 placeholder blocked execution.** Three install candidates have radically different implementations: vendor-under-SHA-pin (300–400 LOC, requires Python interpreter discovery on consumer), postinstall-bridge (50–100 LOC, **breaks marketplace ZIP** — no postinstall hook in non-npm channels — requires bash + Node), bundled binary (200 LOC + 6× CI matrix, 15–40 MB package bloat, binary signing). Decision unblocks Step 2 (wrapper code path), Step 6 (test matrix), Step 7 (failure-mode matrix).
2. **B2 — `.build-deps.lock` schema committed in prose, not as machine-readable artefact.** Phase 0 Step 1 defines the schema in English; Step 5's "commit Phase 0 schema decisions" never specifies a schema file. Phase 1 Step 2's `sync-upstream.sh` cannot be implemented without a formal contract for `mirror_urls`, optional vs. required keys, hash regex, etc.
3. **C1 — Phase 3 Step 6 (router compile-time exclusion) has no corresponding test in Phase 5.** AC line 133 mandates compile-time removal; Phase 5 Step 2 tests *runtime* behaviour with `caveman.enabled: false`, which is a proxy metric — the rule could be present-but-not-firing and still pass. Re-raises R3-P-B which was accepted but never pulled into the Phase 5 body.
4. **C2 — Pass-through mode contradicts the Hard Cutover preamble.** Phase 2 preamble cites "no fallback because caveman is a required dep", but Step 4's `caveman.compress: false` → uncompressed copy is structurally a fallback. The maintainer-vs-consumer distinction is implicit in the preamble; the install-failure cliff (Step 7's "fail hard") leaves the consumer unable to recover when binary install explodes mid-`npm install`.

Sonnet's response cuts off mid-C2 fix recommendation at the 3000-token output cap (last line: "Option A (Graceful Degradation): - Restore a limited fallback: If caveman-compress binary install fails, auto…"). The host has resolved C2 via the preamble clarification + bundled-binary → vendor-fallback chain in Step 1, removing the install cliff without re-introducing LLM-driven compression.

### GPT-4o's findings (Round 4)

GPT-4o provided a meta-review: agreed B1, C1, C2 with Sonnet; rejected B2 (claimed prose suffices for schema). Added three procedural refinements: explicit consumer-environment setup checklist, per-phase prerequisite validation steps, named resolution path for upstream stagnation. The procedural refinements are deferred — they overlap with R3 host-verdict items already absorbed (P-A, P-D) and `commit-conventions`.

### Host verdict (R4)

| # | Finding | Source | Verdict | Action taken |
|---|---|---|---|---|
| B1 | Install-mechanism choice | Sonnet | `accept` | Phase 2 Step 1 locked: bundled-binary primary + vendor-under-SHA-pin transparent fallback. Postinstall-bridge rejected on marketplace-ZIP incompatibility. |
| B2 | `.build-deps.lock` schema as machine-readable artefact | Sonnet | `accept` | New Phase 0 Step 1.5 — `.build-deps.lock.schema.json` (JSON Schema Draft 2020-12) with explicit key contracts and CI validation. GPT-4o's reject overruled — implementer-time disambiguation outweighs the prose-readability argument. |
| C1 | Router compile-time exclusion needs its own test | Sonnet + R3-P-B | `accept` | New Phase 5 Step 2.5 — `tests/test_router_compilation.py` parses `router.json` and asserts rule absence/presence per `caveman.speak` toggle. Closes the R3-P-B documentation-sync failure. |
| C2 | Hard Cutover preamble vs. pass-through Step 4 | Sonnet | `accept-with-modification` | Phase 2 preamble rewritten: "no fallback" qualified to mean "no LLM-driven compression fallback"; `compress: false` re-framed as a valid runtime mode (not a fallback); install-failure cliff resolved by Step 1's bundled→vendor chain ending in a clear named error only when both paths fail. R4-C2 maintainer-vs-consumer note added inline. |
| GPT-1 | Consumer-environment setup checklist | GPT-4o | `defer` | Overlaps with R3-P-A (settings-file instantiation contract); will land when R3-P-A's Phase 0 Step 3.5 is added. Tracked, not lost. |
| GPT-2 | Per-phase prerequisite validation | GPT-4o | `reject` | Generic process advice — `commit-conventions` + Phase 5 quality gates already cover this. |
| GPT-3 | Named resolution path for upstream stagnation | GPT-4o | `defer` | Overlaps with R3-P-D (tripwire → 3 response options) which was accepted but landed only as a vague "triggers re-evaluation" line. Deferred to a dedicated Notes-section rewrite. |

### Cross-channel scoring matrix (B1 verdict basis)

Synthesised from Sonnet's B1 narrative — head-to-head scoring the council was asked for but did not produce as a discrete table.

| Criterion | (1) Vendor-under-SHA-pin | (2) Postinstall-Bridge | (3) Bundled Binary |
|---|---|---|---|
| **Maintenance burden** | medium (sync-upstream extension) | low (50–100 LOC) | high (6× CI matrix + signing) |
| **Consumer toolchain required** | `python3 ≥ 3.10` | `bash` + `node` | none (binary is self-contained) |
| **Marketplace ZIP compatibility** | ✅ files only | ❌ **no postinstall hook** | ✅ binary in package |
| **Manual-copy compatibility** | ✅ files only | ❌ requires explicit setup step | ✅ binary path resolved by `install.sh` |
| **Version-drift risk** | low (SHA-pin + content-hash) | medium (upstream `install.sh` evolves) | low (pinned in CI) |
| **Install-failure-mode clarity** | medium (Python missing → clear error) | low (bash/node/install.sh failure modes layer) | high (named platform slug + binary hash mismatch) |
| **Package size impact** | ~2–5 MB (Python source tree) | negligible | 15–40 MB (6 binaries) |
| **Cross-channel coverage** | universal | npm-only | universal |

**Winner:** (3) bundled binary on coverage + clarity, (1) vendor-under-SHA-pin as transparent fallback for unsupported platforms (Alpine musl, BSD, exotic CPU arch). (2) eliminated by marketplace-ZIP incompatibility — the user's distribution surface includes channels without postinstall hooks.

### Truncation notice (R4)

Sonnet's Round-4 output hit the 3000-token cap mid-C2-fix-recommendation. The C2 resolution applied here is host-synthesised from the partial reasoning + the locked Step 1 mechanism. Re-running with `--max-tokens 4096` would surface the rest, but would also breach the council's `max_output_tokens: 20000` budget over 3 rounds — see `scripts/ai_council/orchestrator.py:218–237`.
