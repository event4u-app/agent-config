---
complexity: lightweight
status: later
parent_roadmap: road-to-install-path-convergence
---

# Roadmap: Follow-up to install-path convergence — delist decision checkpoint

> Decide keep-shim vs delist for the Claude Code marketplace listing after
> a real monitoring window — the explicitly-not-autonomous Stage 2 of the
> council's Q1 tie-break (bootstrap-shim now, delist checkpoint later).

## Context

This roadmap collects the one item deferred from
[`agents/roadmaps/archive/road-to-install-path-convergence.md`](archive/road-to-install-path-convergence.md).
See the parent's archive entry for the original rationale, the council
convergence (anthropic/claude-sonnet-4-5 + openai/gpt-4o, 2026-07-07), and
the recorded revisit-if conditions.

> Blocked until the bootstrap shim has shipped and a monitoring window
> (suggested: ~4 weeks post-merge) has elapsed. Execution starts when the
> maintainer opens the checkpoint — this is a maintainer call, explicitly
> NOT autonomous.
>
> **Moved to `later/` 2026-07-12 (road-to-opt-portfolio-consolidation
> Phase 3):** the single open item is monitoring-window-gated and
> maintainer-only — parked-shaped, not workable-now. Resume when the
> ~4-week window has elapsed and the maintainer opens the checkpoint.

## Phase 1: Delist decision checkpoint

- [ ] Delist decision checkpoint (maintainer call, explicitly NOT
      autonomous): after the monitoring window, review shim-install
      traffic and duplicate-surface reports; decide keep-shim vs delist;
      record the decision + revisit-if in
      `agents/settings/contexts/claude-code-single-surface-decision.md`
      (parent phase: Phase 5)

## Acceptance Criteria

- The keep-shim vs delist decision is recorded with scope + revisit-if in
  the decision context, citing the monitoring-window evidence
  (shim-install traffic, duplicate-surface reports).
