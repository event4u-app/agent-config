<!-- evidence-type: analysis -->
# Host injection effect — does a delivered rule body reach the model?

Pinned to commit `7f80272acdb31ff41f9add7a621d96c2b5817e13`. Generated by
`./scripts-run src/scripts/report_host_injection_effect --emit --pin 7f80272acdb31ff41f9add7a621d96c2b5817e13`.
Observations come from `src/config/host-injection-effect.json`; the binding column is
read fresh from `src/scripts/hook_manifest.yaml` on every run.

## The two columns, and why neither is the other

**Slots bound** is computed and cheap. **Body reaches the model** is observed and
expensive, and owner ruling E3 admits a host to `lean_projection.hosts` on the second
only: one transcript in which a model visibly acts on a delivered body. Documentation
of a host's hook API is not admission.

`cowork` is why the distinction is not pedantry: **eight slots bound, and measured
`observed-false`** — its trampoline discards dispatcher output and exits 0. A table
with only the binding column would admit it.

## Per host

| Host | Slots bound | `rule-inject` on `user_prompt_submit` | Body reaches the model | Citation |
|---|---:|:-:|---|---|
| `augment` | 5 | no | `unobserved` | — |
| `claude` | 9 | yes | `unobserved` | — |
| `cline` | 5 | no | `unobserved` | — |
| `codex` | 0 | no | `unobserved` | — |
| `copilot` | 0 | no | `unobserved` | — |
| `cowork` | 8 | no | `observed-false` | `docs/enforcement-by-host.md:21` (2026-09-07) |
| `cursor` | 5 | no | `unobserved` | — |
| `gemini` | 5 | no | `unobserved` | — |
| `windsurf` | 3 | no | `unobserved` | — |

## Why each row reads as it does

- **`augment`** — Binds five slots and does not honour a deny (docs/enforcement-by-host.md). It also has no `user_prompt_submit` slot, which is a DECLARED gap in the manifest rather than an oversight, so the delivery path this record measures does not exist there today.
- **`claude`** — UPDATED 2026-09-10. The premise of the earlier reason is now FALSE and the state is unchanged, which is the finding. That reason said the concern had emitted zero bytes in every session on record because `lean_projection.mode` resolved to `eager-all` throughout. It has now delivered: with `mode: delivery` set locally, a live Claude Code session (2.1.268) received 2 rule bodies / 7,685 B on `user_prompt_submit`, and later in the same session 4 bodies / 16,314 B. So delivery into a live model context is no longer the open question. AN INDEPENDENT READER — a fresh context that did not produce the turn — was given the transcript and a neutral three-way question, and returned ACTED, with a qualification it raised itself and which is why this row is NOT `observed-true`: "the ACTED verdict rests on 'referring to its substance', not on 'doing something it asks for'". The model quoted the injection's own byte count (a string the reader confirmed appears nowhere else in the transcript, so the reference is demonstrable) and treated the payload as an OBJECT OF STUDY. On the two delivered rules' actual obligations it returned NONE: no point in the turn surfaces a source discrepancy (`cross-source-consistency`) or classifies the prompt continuation-vs-interrupt (`user-interrupt-priority`). E3 asks for a model that VISIBLY ACTS ON a delivered body; citing that a body arrived is not that, and it is especially not that when the observed turn was one that had been WAITING for the delivery — which makes the reference cheap and is the K1 shape one layer further in. A STRUCTURAL LIMIT THE READER ALSO FOUND, and it bounds what any future attempt can prove: thinking blocks are recorded with length 0 in the transcript, so an internal application of a delivered rule is invisible by construction. Only externally visible behaviour can ever satisfy E3 here. The language pin delivered in the same payload is separately COINCIDENT rather than acted on — the prompt was German anyway, and the pin's own text concedes its trigger is not observable from the transcript. UPDATED 2026-09-11. The state is UNCHANGED and the update is the round that tried hardest to change it. Four ordinary-work turns carrying live deliveries were mechanically selected from the whole transcript store and each judged by a fresh reader on a neutral four-question prompt: one clean NONE, two UNCLEAR, and one NONE on a packet whose rule body was truncated (recorded as defective rather than repaired quietly). Condition 1 was satisfied TWICE, so the bar is not unreachable; condition 2 failed 4/4, and in one candidate the delivered clause that would have CHANGED the outcome went unfollowed while the clause the agent had independent reason to follow was honoured. Full round, method and limits: agents/evidence/analysis/e3-candidate-round-2026-09-11.md. An AI council (2/2 convergent, 2026-09-11) amended E3 prospectively rather than lowering it: a predeclared externally observable obligation class is the admissible witness set, the four conditions stand unchanged, and an eventual observed-true is CLASS-SCOPED — it may not admit this host wholesale for reasoning-only rules. unobserved stays a RESULT here, not a gap. SUPERSEDED THE SAME DAY, AND THE SUPERSESSION IS THE POINT. The enumeration that amendment called for found a confound that makes the four verdicts non-identifying: 106 of 106 `type: auto` rules already carry a byte-equivalent obligation body in the same session unconditionally — 89 in the user-global rule layer, 3 differing by a single blank line, and the 14 maintainer-scoped ones inline in the system prompt. All ten rules judged in the four candidates are body-identical across both channels. So the injected copy adds no text, removing the injection does not remove the body, and the intervention varies one copy versus two rather than absent versus present. The corpus explanation and duplicate exposure fit the four results equally and nothing in the round separates them. Council round 3 (2/2 present, same day) split on the label — anthropic not-measurable-as-specified, openai continue-only-in-a-clean-environment — and converged on the terms: suspend condition-2 judgement wherever a second copy of the obligation exists, record an exposure audit over every auto-loaded channel before admitting a candidate, and require injection to be the SOLE source of the tested body. The residual measurable set in this repository is empty.
- **`cline`** — Binds `user_prompt_submit` (hook_manifest.yaml:1315) with a `.md` rule tree, same shape as Cursor and the same unmeasured question. `rule-inject` is absent from its concern list.
- **`codex`** — No platform key in hook_manifest.yaml at all, so no slot is bound and no concern can fire. Same distinction as copilot: the absence of a binding is a computable fact and is not the observation E3 asks for.
- **`copilot`** — `fallback_only` with zero slots bound: this package binds nothing there, so there is no hook stdout to reach anything. Recorded as unobserved rather than `observed-false`, because nobody has looked — a structural argument that it cannot work is still not an observation that it does not.
- **`cowork`** — The one host E3 excludes by an existing measurement rather than by absence of one: its trampoline discards dispatcher output and exits 0, so a delivered body cannot reach the model however many slots are bound. Eight slots bound and zero effect is precisely why `emits` and `injection_effect` are separate axes in this record. CITATION LIMIT, stated rather than smoothed over: the host version was not captured when that observation was made, so this row does not meet the four-part protocol in full and its `host_version` says so instead of inventing one.
- **`cursor`** — Binds `beforeSubmitPrompt` onto `user_prompt_submit` (hook_manifest.yaml:1300, alias :1407) and carries a `.md` rule tree, which makes it the strongest candidate — and the binding is not the question. Whether Cursor adds hook stdout to the model's context is unmeasured, and cannot be measured from a Claude Code session. `rule-inject` is absent from its concern list, so nothing would be emitted there today in any case.
- **`gemini`** — Five slots bound, no `pre_tool_use` binding, and no session available to observe from. Recorded as unobserved rather than assumed either way.
- **`windsurf`** — Three slots bound and no tool-lifecycle surface at all (docs/enforcement-by-host.md). Not observed, and not reachable from a Claude Code session.

## `cursor.md_tree_loaded` (step 1.3)

`cursor.md_tree_loaded: unobserved` — road-to-delivery-on-hook-hosts 1.3. Does Cursor load `.cursor/rules/*.md` ALONGSIDE the `.mdc` files, i.e. does it carry every auto rule twice? Both trees are written today (condense.ts writes the symlink tree in the TOOL_DIRS loop and `_emit_cursor_mdc` writes the .mdc companions), so a double carry is possible by construction and unobserved in fact. It cannot be observed from a Claude Code session, and K4 forbids removing the .md bodies before it is. Carried under an underscore key because it is a property of a TREE rather than a host verdict, and putting it in the host namespace would make `cursor` answer two different questions.

## What this census does NOT do

It computes no verdict. Admission under E3 is a one-line read of the observed column
and is applied by whatever would change a projection, never here — a report that also
decided admission would be the one place an inference could quietly replace an
observation.

`unobserved` is a RESULT, not a gap: the host keeps its full rule bodies and loses
nothing while it waits, which is the roadmap's K6 and is asserted per PR by
`check_host_tree_parity` in the predecessor.
