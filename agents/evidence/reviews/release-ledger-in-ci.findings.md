# Findings: release-ledger-in-ci
<!-- completion-review: v1 | reviewed: 2026-09-11 | scope: 94dd16a11567b34a0ff4491562954d84e3af2ba4e378c69c8dcedd64c5def9fe | diff: f9ddbf849890df6a00676a71bdf332934a803858 | reviewer: r2-fresh-subagent-release-ledger-in-ci | prompt_hash: ad84a9b995ee809edbe072d0eac563e1f2acfa4ff764e48c0ecd6f8d3835c96e -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-release-ledger-in-ci"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-11 -->

<!-- context-manifest: v1
inputs:
  diff_sha: f9ddbf849890df6a00676a71bdf332934a803858
  scope_hash: 94dd16a11567b34a0ff4491562954d84e3af2ba4e378c69c8dcedd64c5def9fe
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-11T07:03:26Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | .github/workflows/self-review-gate.yml:180,188 | The job cannot produce a ledger for a version that does not already have one — which is every release, and the entire purpose. `agents/evidence/release-findings/$version.json` is a NEW file on first ingest, so it is untracked, and `git diff --quiet -- "$ledger"` does not see untracked paths: measured in a scratch repo, `git diff --quiet -- <untracked>` exits **0**. The script therefore takes the `::notice::ledger for $version already current — nothing to commit` branch and `exit 0`s — green, silent, having written nothing. Fix that guard and the next line fails too: with no `git add`, `git commit -m … -- "$ledger"` on an untracked path is `error: pathspec … did not match any file(s) known to git`, exit 1 (measured). The path-scoped commit was adopted to close round-1 finding 16 (`git add <rel>` + an unscoped `git commit` staged the whole index) without the `git add` a new file still needs; `git add -- "$ledger"` before the probe, or `git status --porcelain -- "$ledger"` as the emptiness test, is what these two lines need. Observable consequence, and note the ordering with findings 3 and 2: the job is green, the ledger never appears, and `release.ts` step 7 stops EVERY release with `ledgerAbsentMessage` — the six-release failure this change exists to end, now with an extra green check asserting it was handled. | open |  |
| 2 | critical | .github/workflows/self-review-gate.yml:192 · src/scripts/release_publication.ts:327-390 | The ingest push necessarily lands INSIDE step 6's check wait, on a head that has no checks, and the release dies there. Not a race: `ingest-release-ledger` is a check run of the same `pull_request` event, so `gh pr checks <branch> --watch` (step 6) cannot finish before that job does, and the push is the job's last command. After it the PR head is commit B. GitHub's `GITHUB_TOKEN` recursion guard means no workflow run is created for B — asserted by this same diff at `docs/release-runbook.md:164` and again at `:193` ("local runs use your user token, so no recursion guard applies"), and by `.github/workflows/release.yml:16-22`. B therefore carries zero check runs, `gh pr checks` reports `no checks reported`, and `_no_checks_action` returns `retry` then `die` because `main` declares a required context (`docs/contracts/branch-protection-policy.md:71` — `Sync + Generate Tools Consistency`, "the only required one"). Step 6 waits `_NO_CHECKS_ROUNDS`=10 × 60 s and `die()`s; `--resume` re-enters the identical state; `gh pr merge` in step 8 would be blocked on B as well. Recovery needs a human commit. Round-2 finding 1 named this shape ("one that produced it BEFORE the wait pushed a commit whose checks had not started") and round-1 finding 2 named it too — moving the push from `release.ts` into CI relocated it into a window the release cannot re-trigger out of rather than removing it. | open |  |
| 3 | high | .github/workflows/self-review-gate.yml:138-139,166-167 | The job's token cannot read its own artifact, and the failure is swallowed and then mislabelled. A job-level `permissions:` block REPLACES the workflow-level grant — every scope not listed is `none` — so `contents: write` alone leaves `actions: none`, while `gh run download` (GET `/repos/{owner}/{repo}/actions/runs/{run_id}/artifacts`) needs `actions: read`. This repo already knows that: `.github/workflows/release-adjacent-health.yml:31-32` declares `actions: read` for exactly this kind of read. The 403 is then absorbed by `\|\| true`, the file is absent, and the script prints `no findings artifact for $version — nothing to ingest` under a comment asserting the benign cause ("Ordinary, not broken: the upload carries `if-no-files-found: ignore` …") — so a permission defect, an API outage, a renamed artifact and a genuinely clean review are one indistinguishable green path. `actions: read` has to be granted, and the download's exit status has to separate "no artifact exists" from "the download failed". | open |  |
| 4 | high | .github/workflows/self-review-gate.yml:132 | `github.actor != 'github-actions[bot]'` skips the job on the CI-native release path — the one path with no human to fall back on. When `RELEASE_PR_TOKEN` is unset, a configuration `.github/workflows/release.yml:12-15` explicitly supports ("falling back to the default `GITHUB_TOKEN`"), `release.ts` opens the release PR as `github-actions[bot]`, so `github.actor` on that `pull_request` event IS `github-actions[bot]` and the ingest is skipped. That the event fires at all in that configuration is measured in this repo: `release.yml:30-34` records 14.0.0 (2026-08-18, no `RELEASE_PR_TOKEN`) where "the checks started IMMEDIATELY". The guard is also aimed at a loop that cannot happen (finding 2: a `GITHUB_TOKEN` push creates no run), so it costs the unattended release path and buys nothing. A loop guard, if one is wanted, belongs on the commit author or the ledger diff — not on the actor who opened the PR. | open |  |
| 5 | high | .github/workflows/self-review-gate.yml:112-119 · agents/roadmaps/later/road-to-release-finding-ordering.md:2,40-47 | "This job holds the artifact already, so the ledger exists before any gate looks for it" is not true of the gate it names. `finding-dispositions` lives in a DIFFERENT workflow (`release-validation.yml:356-390`), triggered by the same `pull_request` event with no ordering relation to `ingest-release-ledger` — `needs:` cannot cross workflows. It runs concurrently, reads the ledger and the self-review PR comment, and is measured in this repo completing **91 seconds before the comment it reads existed**: `road-to-release-finding-ordering.md`, which this same diff edits (adding an "Arrivals: 3" note) while leaving `status: later` and "Resume when a maintainer can create a synthetic `release/*` pull request". By the diff's own artifact the ordering guarantee is still owed. What the design does change is that the ledger reaches the branch before the MERGE; it changes nothing about when a gate reads it, and per finding 2 `finding-dispositions` never evaluates the committed ledger at all. The comment should claim the narrower thing it actually buys. | open |  |
| 6 | high | src/scripts/release.ts:1013,1032-1039,1095,1270 | The stated invariant "the release cannot pass a weaker check than the one it is trying to pre-satisfy" is false on every non-resume run — i.e. the normal `task release`. `const pr_info = resume ? _pr_for_branch(branch) : null` (:1095) is probed before step 5 creates the PR and never refreshed, so on a fresh release step 7 passes `pr = null` (:1270) and `argv` omits `--pr` (:1036-1039). The docstring (:1013, "Asked with `--pr`, the same mode the CI job uses") and the inline comment (:1032-1035, "Without it the release would pass a strictly weaker check") both assert the opposite. `--pr` is the script's own durable trigger for an un-ingested finding (`check_finding_dispositions.ts:18-23`), so only a `--resume` ever gets the strong check. This is round-2 finding 6 unfixed, now additionally claimed as fixed. Re-probing with `_pr_for_branch(branch)` at step 7 closes it. | open |  |
| 7 | medium | src/scripts/release.ts:40-45 | The module's pipeline docstring documents the design this change reverted. Step 7 reads "ingest the self-review artifact into the release findings ledger for this version, **commit it to the branch**, and STOP …", while the implementation is verify-only and its own docstring leads with "VERIFIES, NEVER PRODUCES" (:996-999) and the drill asserts that no `--ingest` and no `gh run download` ever happen in step 7 (`release_drill.ts:563-571`). The pipeline list is the first thing a maintainer reads in this file and the last place still promising that the release produces the ledger. | open |  |
| 8 | medium | src/scripts/release.ts:1263-1265 | The `pr_merged` branch asserts a fact it does not check, and the state it asserts is reachable and is the original failure. "PR already merged — the ledger rode in with it, skip" prints whenever `pr_info.state === 'MERGED'`; nothing reads the ledger. Reachable path: the ingest silently no-ops (findings 1/3), step 7 stops with `ledgerAbsentMessage`, the operator merges the PR by hand (or an earlier run merged it), and `--resume` then skips step 7 entirely and proceeds to tag and publish — producing exactly a shipped version with no ledger, which `consistency.yml:393-396` turns into a repo-wide red on the next push to `main`. The skip has the right shape (the branch is gone, so the remote probe cannot work) but it has to verify the ledger on the merge commit rather than assume it. | open |  |
| 9 | medium | .github/workflows/self-review-gate.yml:189-191 | The loop-termination argument is internally inconsistent, and the half that is not the actor guard is unsound. It reads "The push re-triggers `pull_request`, which is why the job skips a bot actor above: the second run ingests the same finding ids, the diff is empty, and the loop stops without a third" — but if the actor guard fires there IS no second ingest, and if it does not fire the empty-diff claim fails: `findingId` is `sha256(kind\|title\|file)` over an LLM-authored title (`self_review_gate.ts:62-67`), and the second run reviews a DIFFERENT diff (it now contains the ledger commit), so reworded or new findings yield new ids, a non-empty ledger diff, another commit and another push. Per finding 2 the premise is false anyway. Three claims in three lines of which at most one can be load-bearing; state the actual terminator and drop the rest. | open |  |
| 10 | medium | .github/workflows/self-review-gate.yml:138-150 | This is the only job in the workflow that both holds a `contents: write` token and persists it into `.git/config` (`persist-credentials: true`), and it then runs branch-controlled code: `npm ci` (arbitrary third-party lifecycle scripts) followed by `./scripts-run src/scripts/check_finding_dispositions` from the checked-out release branch. The same-repo `if:` bounds the human actor — pushing `release/*` already requires write access — but it does not bound the dependency tree, so a compromised transitive package executes with a repo-write credential on disk, where every other job here is `persist-credentials: false` and read-only. The workflow's own permissions comment cites `road-to-ci-supply-chain-integrity`, which is the discipline this widens. `lint_workflow_security` does not catch it: its `persist-credentials` rule only requires the key to be EXPLICIT (`:302-308`), so `true` passes. Splitting the job — ingest under a read-only token, commit in a second job that checks out with credentials and installs nothing — keeps the write scope away from `npm ci`. | open |  |
| 11 | medium | .github/workflows/self-review-gate.yml:192 | The push has no retry and no rebase, and its failure is not a skip but a red check on the release PR. `git push origin "HEAD:$HEAD_REF"` onto a branch that moved after the checkout at :141-145 (a maintainer pushing a disposition fix, a `--resume` re-push from step 4) is a non-fast-forward; `set -euo pipefail` fails the job, and that failing check run is then what `watch_pr_checks` reports, so step 6 dies with `PR checks failed` naming a bot job rather than anything about the release. A fetch + rebase (or re-ingest onto the current tip) before the push, or treating a non-fast-forward as a clean no-op, is the difference between a recoverable and a confusing failure. | open |  |
| 12 | medium | src/scripts/release_drill.ts:184,204,207,212,216,782,809,838,1023 · src/scripts/release_env.ts:101 · src/scripts/release_publication.ts:959 · src/scripts/check_release_published.ts:60 · .github/workflows/release.yml:167 · docs/decisions/ADR-113-ci-native-release-label-trigger.md:135 | The renumbering sweep is partial again — round-2 finding 8, same defect, largely the same file set. The diff moved four drill summaries to "step 8" and `release.yml:17,21` to "step 10", and left: the drill's doc comments ("step 8 (annotated-tag message)" :184; "step 8 skips entirely and step 9 — the GitHub Release" :204-207; "steps 1 through 7 skip and step 8 is …" :212-216; "the drill's step 8 reads the repository's REAL changelog" :1023) and three marker-scenario summaries the drill PRINTS on every run (:782 and :809 call the tag step 8 — it is 9; :838 calls the GitHub Release 9 — it is 10). Observable: `./scripts-run src/scripts/release_drill` now prints "step 8" for both the merge and the tag. Outside the drill: `release_env.ts:101`, `release_publication.ts:959`, `check_release_published.ts:60` (all "step 8" for the tag), `release.yml:167` ("step `[2/10]`" — the total is 11) and `ADR-113:135` ("release.ts step 9's" — 10). A `grep -rn 'step [0-9]'` over `src/`, `docs/` and `.github/` is the whole sweep. | open |  |
| 13 | medium | docs/contracts/release-sizing.md:1-13 | An unrelated beta→stable contract promotion rides in this change and its central claim is contradicted by its timing — round-2 finding 11, unchanged. The file carried `keep-beta-until: 2026-09-10`; `check_beta_review_markers` errors on a lapsed contract absent from `src/config/lapsed-beta-baseline.json`, and `release-sizing` is absent from it (0 matches, verified). Run today (2026-09-11) the pre-promotion file is a FRESH lapse and therefore red — the gate's output lists six other fresh lapses, due 09-15 onward. So "Promoted on evidence rather than on elapsed time" describes a motive the date contradicts: the promotion lands on the first day the elapsed-time gate would have failed, inside a diff about the findings ledger. The three supporting evidence claims do hold (`lint_changelog_rollback.ts` and `docs/distribution/consumer-matrix.md` both reference the contract), which is why the honest move is either its own change or one of the gate's other dispositions — extend the window with a reason, or record it superseded. | open |  |
| 14 | low | tests/scripts/release_findings_ingest.test.ts:47-50,67-69 | Two assertions pin the defective constructs from findings 1 and 3 in place, so both are now regression-protected. `commits path-scoped, so nothing else rides in` asserts `/git commit -m .+ -- "\$ledger"/` — the exact command that cannot commit an untracked ledger — and `grants that job the write scope it needs, and nothing broader` asserts `permissions:` + `contents: write` for the job, which is precisely the absence of `actions: read`. Neither test executes the shell, so the 35 green tests are evidence about the workflow's TEXT and none about its behaviour: the whole `run:` block is untested. A fixture repo plus `bash` over the extracted script body is the reachable test, and it would have caught both. | open |  |
| 15 | low | src/scripts/release.ts:1901-1902 | `settle_findings_ledger` and `_blocking_without_disposition` were added to the `export {}` block and are imported by nothing — no test, no script, no other module (checked across `src/`, `tests/`, `docs/`, `.github/`; the single hit is the runbook's staleness `grep`). One of them is `_`-prefixed, i.e. private by this tree's own convention, and exported anyway. Round-1 finding 12 asked for a test on `settle_findings_ledger`; exporting it without adding one lands the surface and not the coverage. Either add the unit test the export exists for, or drop both — the drill already reaches them through `execute`. | open |  |
| 16 | low | tests/scripts/release_findings_ingest.test.ts:119-128 | `produces the same field set a hand-written ledger carries` asserts key-set equality against `agents/evidence/release-findings/14.23.0.json`, so any later edit to that one historical data file — a field added to a past release's ledger, a re-ingest, a schema bump — fails a test about `merge_ingest`. Round-2 finding 17, unchanged. The property worth pinning is that the ingest emits what `check_review_schema` requires; deriving the expectation from `INTEGRITY_FIELDS` plus that schema states it without coupling to one artifact. | open |  |
| 17 | low | docs/release-runbook.md:140,189 · src/scripts/release.ts:1249-1252 | `--no-wait` no longer gets a fresh release to the merge, and nothing says so. Step 7 requires the ledger to be on `origin/release/X.Y.Z` already, which only happens after the CI review has finished and committed it, so skipping the wait guarantees the step-7 stop on a first run. The runbook still lists step 6 as "skippable with `--no-wait`" (:140) and §3.B presents the flag as a plain override (:189). `ledgerAbsentMessage` does tell the operator to wait and resume, so the path is survivable — but the flag's meaning changed and the doc that documents it did not. | open |  |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
