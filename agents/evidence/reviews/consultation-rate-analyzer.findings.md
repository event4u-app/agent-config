# Findings: consultation-rate-analyzer
<!-- completion-review: v1 | reviewed: 2026-08-12 | scope: be0b4351f847ae1f7d1443a7519fcc724c3c09fe1e3b2d51e428731a1f6abe0e | diff: 347cb4702b55f22723ecf4d7fb00fe202fb4d72c | reviewer: r2-fresh-subagent-consultation-rate-analyzer | prompt_hash: 3ac8b3ee979326927a7ca3239d20165b04bfa3147041798a6fa176d14c40f810 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 347cb4702b55f22723ecf4d7fb00fe202fb4d72c
  scope_hash: be0b4351f847ae1f7d1443a7519fcc724c3c09fe1e3b2d51e428731a1f6abe0e
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-12T07:20:00Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/report_consultation_rate.ts:263 | `--min-sessions` is parsed with `Number.parseInt` and never validated, unlike `--limit` two lines below which has an explicit `Number.isFinite` guard. A non-numeric or missing value yields `NaN`, and `report.sessionsWithUiWrite < NaN` is `false`, so the provisional-corpus warning is **silently suppressed** and the run prints what reads as a settled baseline. The failure is asymmetric: the missing guard removes a caveat rather than adding noise. Reachable by a plain flag-order slip (`--min-sessions --json` makes the threshold the string `--json`), not only by a typo. The baseline doc claims "the tool says so on every run" — under `NaN` it does not. | open | Guard mirrors `--limit` at :264-267 |
| 2 | medium | src/scripts/report_consultation_rate.ts:256 | `argValue` returns the fallback when the flag is present but is the last argv element, so `report_consultation_rate --store` silently measures the **default** store instead of erroring. Compounding: the text `render` deliberately never prints `report.store` (asserted at tests/scripts/consultation_rate.test.ts:207-209), so the substitution leaves no trace in the output a reader would paste into the baseline. The roadmap step that consumes this tool instructs `--store ~/.claude/projects/<flattened-consumer-path>`, i.e. a hand-built path is the expected invocation and a shell slip is the expected error. This is the "clean number out of a corpus you never measured" class the file's own docblock and `storeExists` handling exist to prevent, except it produces a confident non-zero from the wrong corpus rather than a zero. | open | `--store` read at :261; store echoed only under `--json` |
| 3 | medium | src/scripts/report_consultation_rate.ts:131 | Unit mismatch between what is computed and what is published. `uiWrites` increments once per `tool_use` **part** (`readSessionEvents` flattens every part of every assistant message with no turn boundary), but it is documented and published as "UI-write **turns**" — at :64-65, in the docblock at :16, in agents/settings/contexts/skill-catalogue-baseline.md:117 ("UI-write turns \| **3**") and in both roadmap blocker texts. One assistant turn editing three UI files reports as three. Two consequences: the published `3` may be a single turn, and the consultation rate mixes a per-session latch numerator with a per-tool-call denominator, so one `fe-design` read before twenty writes reports 100 %. The diff's own thesis is that the denominator is the finding, which makes the denominator's unit load-bearing. | open | Rename to tool calls, or group parts per assistant entry |
| 4 | low | src/scripts/report_consultation_rate.ts:198 | `fs.statSync` is called inside the sort comparator, so each comparison costs two syscalls and the sort runs over the **unsliced** listing — `--limit` is applied after. Decorate-sort-undecorate would be one stat per file. Second, sharper issue: `statSync` throws if a transcript disappears between `readdirSync` and the comparator, and the store is live while it is being measured (the host appends to it from the running session), so an ENOENT would surface as a bare top-level catch and exit 1. | open | Inefficiency plus a crash path on a concurrently-written directory |
| 5 | low | src/scripts/report_consultation_rate.ts:201 | `report.sessions` is assigned after `.slice(0, limit)`, so "sessions scanned" silently reports the cap. A store holding 5,000 transcripts and a store holding exactly 200 print an identical `200`, with nothing marking truncation. The tool applies the make-the-denominator-legible standard rigorously to the missing-store case (:192-194 and its test) and not at all here. | open | Emit a truncation marker when `files.length` was capped |
| 6 | low | src/scripts/report_consultation_rate.ts:88 | The `defaultStore` docblock diagnoses a live defect in another helper — "the shape the older census helper carries" replaces only `/`, producing a directory that does not exist so "the caller then reads a clean zero out of a store it never found" — and then neither fixes nor files it. The named failure stays reachable from that other call site, and the only record of it is a comment in an unrelated file. A defect found in one place is presumed to recur until searched. | open | `downstream-changes` § defect-pattern search |
| 7 | low | src/scripts/report_consultation_rate.ts:145 | `reviewOpenIndices.some((r) => r > writeIndex)` scans the whole open-list for every write, giving O(writes x opens), while `reviewOpenIndices` is built in ascending order — comparing `writeIndex` against the last element is exact and O(1). Simplification more than a hot path, but the current form invites a future edit that breaks the ordering invariant without any test noticing. | open | Ordering invariant is implicit and untested |
