# Findings: feat-inbox-harvest-b-ledger-truth
<!-- completion-review: v1 | reviewed: 2026-08-11 | scope: d6e4da308b5d9f8f56afd74f16e59b3a3b583870ed507a8ade4d28cae85f4710 | diff: 4204a99a118ab8e8ed04fb3674821b69c0616f1a | reviewer: r2-fresh-subagent-feat-inbox-harvest-b-ledger-truth | prompt_hash: 93dfa4448256b320dceba6c629731bfa4b5a6dc12668bc1a22ff65b26f651044 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 4204a99a118ab8e8ed04fb3674821b69c0616f1a
  scope_hash: d6e4da308b5d9f8f56afd74f16e59b3a3b583870ed507a8ade4d28cae85f4710
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-11T02:22:35Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | docs/contracts/cost-summary-schema.md:123 | The new `## Served-model attribution` section documents `model_served` as a v1 additive extension of the cost-summary spend row, but nothing produces or consumes it for that schema: `cost_summary.ts` contains zero references to `model_served` (neither `aggregate()` nor any bucket reads or emits it), and the "First producer" the section cites — `ai_council/session.ts` `_serialise_response` — writes the **council session manifest**, not a cost-summary row (its own docstring at session.ts:178 reads "Project a `CouncilResponse` into the manifest schema"; the manifest lands under the council sessions dir, while cost-summary/v1 reads `agents/cost-tracking/sessions.jsonl`). The same mis-citation is mirrored in the code comment at session.ts:196-199, which justifies a manifest field by quoting `cost-summary-schema:60-62`. A consumer implementing the contract will look for a field that no cost-summary producer writes — the "defined but not wired" surface roadmap step 1.3 explicitly says it declined to create for `by_model`, reintroduced one section lower. | fixed | 4204a99a1 — section replaced: the served id is written by `_serialise_response` into the COUNCIL SESSION MANIFEST, not a cost-summary row, so the contract now documents `rate_missing` — the field this JSONL actually gained a producer for. The manifest comment stops citing `cost-summary-schema`. |
| 2 | medium | src/scripts/cost/track.mjs:228 | `rate_missing` / `rate_missing_models` are appended to the session row that lands in `agents/cost-tracking/sessions.jsonl` — the exact JSONL `cost-summary/v1` describes — but they are documented nowhere in `cost-summary-schema.md` (which this diff otherwise edits, so the doc-impact pass was live), and `cost_summary.ts` ignores them. Consequence: `totals.total_cost_usd` and every `by_session` / `by_date` / `by_model` row can be understated by an unpriced model, and the summary a reader actually consumes reports that number unqualified — the same silent zero step 2.4 removes at the row layer survives one layer up. The stderr warning only reaches whoever ran `track.mjs`, never the summary consumer. Note the inversion against finding 1: the contract gained a section for a field no producer writes, and gained nothing for the two fields a producer now writes. | fixed | 4204a99a1 — `rate_missing` is documented in the contract AND aggregated: `rate_missing_sessions` on totals and every grouping, `rate_missing_models` union on totals. A non-zero count marks that row as a floor, not a figure. |
| 3 | low | src/scripts/cost/track.mjs:198 | `if (tier === 'unknown') rateMissingModels.add(model)` has no usage guard, so an assistant record with an unrecognised model and all-zero token counts sets `rate_missing: true` and triggers a stderr warning asserting "the session total is understated" — false for that record, since a zero-token message contributes zero either way. `model` is `m.message.model \|\| 'unknown'` (:194), so a record that carries `message.usage` but no `message.model` also injects the literal string `'unknown'` into `rate_missing_models`, which is not a model id and gives a backfill nothing to work against. A flag that fires on records with no spend is a flag readers learn to ignore, which defeats the step. | fixed | 4204a99a1 — usage guard added (`billableTokens(u) > 0`), so a zero-token record no longer flags the session or warns. |
| 4 | low | src/scripts/cost/track.mjs:62 | The added comment states the rule as "A tier with no PRICING row prices at zero … so the ROW carries `rate_missing`", but the implemented condition is `tier === 'unknown'` (:198), not `!PRICING[tier]` — the condition `costForUsage` actually returns 0 on (`if (!p \|\| !u) return 0`, :69). The two coincide only because `unknown` happens to be the sole tier `modelTier()` can return that `PRICING` lacks. Adding a tier to `modelTier()` (e.g. a new vendor family) without a matching `PRICING` row restores the exact undetected silent zero this step exists to remove, and no test would fail. | fixed | 4204a99a1 — the condition is now `!PRICING[tier]`, the condition `costForUsage` actually zeroes on, so a future tier without a rate row is caught. |
| 5 | low | src/scripts/ai_council/pricing.ts:76 | The new prefix fallback reconstructs the composite-key encoding by hand — a template literal building `provider + " "`, plus `key.slice(prefix.length)` — while the exact-match path one line above goes through `priceKey(provider, model)`. The key format now has two sources of truth, and they are coupled silently: if `priceKey`'s separator ever changes, exact match follows it while the fallback loop matches nothing and quietly returns `null`, i.e. regresses to the silent-zero mispricing this function was changed to fix. Deriving the prefix from `priceKey(provider, '')` (or splitting on the same helper) keeps one owner of the encoding. | fixed | 4204a99a1 — the prefix is derived from `priceKey(provider, empty)`; the composite-key encoding has one owner again. |
| 6 | low | src/scripts/ask_transport.ts:153 | `model: r.model ?? 'unknown'` substitutes a sentinel for a missing model id, and that sentinel is fed straight into the audit line as `model_requested` (:100). `modelDivergent` treats any non-empty string as a real id, so a member that returns no `model` while the provider reports a served id yields `model_divergent: true` — a fabricated substitution on a line whose whole purpose is to make real substitutions credible. This is the same failure the design guards against everywhere else (a non-string served id collapses to `''` precisely so a false `true` cannot occur, clients.ts:209-212), and it contradicts the field's own contract in `orchestration-telemetry.md` ("Id only, never free-form"; `null` = not recorded). Passing `null` when `r.model` is absent keeps the three-valued semantics intact. | fixed | 4204a99a1 — the sentinel is named `UNKNOWN_MODEL` and refused at the audit boundary: `model_requested` is null when the member reported no id, so `model_divergent` stays null. |
| 7 | low | docs/contracts/cost-summary-schema.md:36 | `by_date` keys on the row's `startedAt` only, so a session spanning midnight — or several days — attributes its entire cost to its **start** day. A reader of a "day-by-day breakdown" will reasonably take a day's figure as that day's spend, which it is not, and the roadmap step itself notes every row carries `endedAt` as well. The contract documents the key derivation and the `unknown` bucket but states no such limit, while the sibling field added in the same change (`cache_savings_input_token_equivalents`) carefully spells out two "honest limits" — the standard is set inside this diff and not met here. | fixed | 4204a99a1 — the start-day attribution limit is stated in the contract, with why a per-day split would be an invented number rather than a better one. |
