# Findings: worktree-feat-turn-end-gate-always-on
<!-- completion-review: v1 | reviewed: 2026-08-12 | scope: 7ef8f34dc265fa36a287bdeebc17cb6ed4224e65abaad59817ac99d564e7f3e8 | diff: 0992c92219364bc646e78c29fa3072b5e46361ef | reviewer: r2-fresh-subagent-worktree-feat-turn-end-gate-always-on | prompt_hash: f2355f7096631a3f30e38285ac25499af794a2349528a6d363e3e09bda1e9285 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 0992c92219364bc646e78c29fa3072b5e46361ef
  scope_hash: 7ef8f34dc265fa36a287bdeebc17cb6ed4224e65abaad59817ac99d564e7f3e8
  roadmap: agents/roadmaps/road-to-skill-ecosystem-executable-payloads.md
  roadmap_hash: 7281c665233882d54968615e50598f663633348d63de1bb96f3ce15646ee82e7
  ac_hash: 96710f43b484b1eacdce63e8cd7dfb4b98e3d7f90caf8ce9c91779ba20f4ccab
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-12T10:52:09Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/schemas/skill.schema.json:60 | `primary_env` carries a normative constraint — "Must also appear in `env`" — that nothing enforces, and the schema itself permits violating: `runtime_requires` declares no `dependencies`/`required`, so `primary_env` is valid with `env` absent entirely or with a name not in `env`. Draft-07 (line 2) supports `"dependencies": {"primary_env": ["env"]}`, so at least the absent-`env` half is expressible and was not applied; no linter rule or test covers either half. | accepted-risk | Partially closed in 0992c9221, and labelled `accepted-risk` rather than `fixed` on purpose: `dependencies: {primary_env: [env]}` now enforces the half JSON Schema can express, and the false claim is gone — but the MEMBERSHIP check still does not exist, and calling that fixed would be the same overclaim the finding is about. The description now says so outright and points at the doctor wiring (gated on S0.3), where the value is parsed rather than regex-matched. Zero skills use the field today, so the accepted exposure is nil. Original reviewer note: Same shape the diff itself condemns: `untrusted_content.ts:12` cites ADR-127 — "a prose rule with no check at the enforcement point is a promised check that may not run". The field's stated purpose (letting a doctor report distinguish a hard block from a partial one) depends on the invariant holding. |
| 2 | medium | src/scripts/_lib/untrusted_content.ts:113 | The bare `catch` maps **every** `statSync` failure to `verdict: 'missing', message: ''`, conflating "no such file" with "could not determine". `PermissionVerdict` has no `unknown`, so a caller cannot distinguish, and the documented contract (line 106) claims only the missing-file case. Failure: a `.env` inside a directory the process cannot traverse (EACCES), reached through a symlink loop (ELOOP), or on a path segment that is not a directory (ENOTDIR) returns `missing` with an empty message — a security probe reports a benign result for a credential file whose permissions were never examined. | fixed | Fixed in 0992c9221: added an `unknown` verdict — only ENOENT/ENOTDIR now map to `missing`, every other errno reports UNVERIFIED with the errno named. New test plants a `0o000` parent directory (the hostile state in the field under test) and skips rather than asserting a false pass under root. Original reviewer note: Fail-open in the one direction that matters; the test at `untrusted_content.test.ts:618` pins ENOENT only, so the conflation is untested. Contradicts the sibling family thesis "a gate that scanned nothing must never exit green". |
| 3 | low | src/scripts/skill_linter.ts:246 | `RUNTIME_REQUIRES_BLOCK_PATTERN = /^runtime_requires:\s*$/m` accepts only block-style YAML, so the blocking `missing_runtime_requires` error (line 1402) is decided by a text pattern rather than by the frontmatter structure. Failure: a skill declaring `runtime_requires: {bins: ["bash"], network: []}` — valid YAML, valid against the new schema, passes `validate_frontmatter` — still errors, and the message tells the author to add a block they already have, with no suppression path. | fixed | Fixed in 0992c9221: the pattern now accepts flow style as well as block style, and uses `[ \t]*` instead of `\s*` so it cannot span lines and accept a bare key. Flow-style test added. Original reviewer note: Mitigating: `parseExecutionBlock` (line 1035) uses the same block-only `^execution:\s*$` shape, so flow style is unsupported by house convention. Distinction that keeps it a finding: there the consequence is a silent skip, here it is a hard error that contradicts the schema the same diff added. |
| 4 | low | src/scripts/_lib/untrusted_content.ts:56 | The `wrapUntrusted` JSDoc states the delimiter as `<untrusted_content id="<nonce>">` / `</untrusted_content>` — an unnonced closing tag. The implementation (line 75) emits `</untrusted_content id="${nonce}">`, and the sentence two lines below plus the in-prompt instruction (line 71) both depend on the closing tag carrying the nonce. | fixed | Fixed in 0992c9221: JSDoc now documents the nonced closing tag that the code actually emits, and a test pins both delimiters plus asserts the unnonced form is NOT the terminator — so doc and code cannot drift apart silently again. Original reviewer note: The delimiter contract is the module's entire security property; a caller who parses or asserts on the documented form (an unnonced `</untrusted_content>`) will not match the emitted output. |
| 5 | low | src/scripts/_lib/untrusted_content.ts:50 | `WrapOptions.nonce` is public, exported, and interpolated into both delimiters and the instruction line with no validation, while the far less sensitive `source` is sanitised (line 85). Failure: `wrapUntrusted(payload, { nonce: '' })` emits `</untrusted_content id="">` — trivially forgeable from inside the payload; a caller passing a fixed nonce for byte-stable output (the pattern every test in the companion file demonstrates) silently voids the sole property the module claims to provide. Nothing rejects a non-hex, empty, or `">`-bearing nonce. | fixed | Fixed in 0992c9221: `wrapUntrusted` now throws below `MIN_NONCE_LENGTH` (16) lowercase hex chars, so a weak nonce fails loudly instead of producing output that looks protected. Four rejection cases pinned (empty, short, non-hex, uppercase) plus the exact-minimum boundary. Original reviewer note: "Production callers omit it" (line 46) is a comment, not a guard. Cheapest fix is a hex/length assertion, or moving the injection point behind a test-only export. |
| 6 | low | src/scripts/_lib/untrusted_content.ts:124 | The `too-open` message hardcodes "readable beyond its owner", but `GROUP_OTHER_MASK = 0o077` (line 36) matches write and execute bits too. Failure: mode `0o620` or `0o601` reports "readable beyond its owner" when the file is not group/other-readable at all — the message misstates the exposure and omits the real one (another account can overwrite the credentials). | fixed | Fixed in 0992c9221: the message now derives the exposure kinds from the mask (readable/writable/executable) instead of asserting "readable". Two tests added — `0o620` must say writable and must NOT say readable, `0o666` must say both. Original reviewer note: Test coverage pins only `0o644`/`0o604`/`0o600` (`untrusted_content.test.ts:601`, `:611`, `:592`), so no case exercises the write/execute-only wording. Widening the mask is the right call; only the message text is wrong. |
| 7 | low | src/scripts/skill_linter.ts:1281 | `lint_execution_metadata(execution, frontmatter = '')` defaults the new evidence source to the empty string, and `''` never matches the pattern — so a caller that omits the argument gets a **false** `missing_runtime_requires` error on every external-handler-plus-command skill rather than a skipped check. The parameter is optional purely for call-site convenience (one caller exists, line 1571), and the default fabricates a finding instead of failing loudly or being required. | fixed | Fixed in 0992c9221: the `frontmatter` parameter is required now, so a future omission is a compile error rather than a fabricated finding. Original reviewer note: Wrong failure direction for an optional parameter; making it required turns a future omission into a compile error instead of a spurious CI red. |
| 8 | low | agents/roadmaps/road-to-skill-ecosystem-executable-payloads.md:276 | Acceptance criterion 5 says `missing_runtime_requires` "stays silent on all four near-misses — `internal` + command, external without command, external + command + block, and a pack-edge `requires:` **list**". The shipped test asserts the opposite for the fourth: `tests/scripts/skill_linter.test.ts:1114` expects the code to **fire** on a pack-edge `requires:` list, which is also what the roadmap's own Phase 1 step 4 verify line says at line 177 ("must NOT satisfy the rule"). | fixed | Fixed in 0992c9221: AC 5 now states that the rule FIRES when a pack-edge `requires:` list is present (that key satisfies nothing here) and stays silent on the three legitimate near-misses. This one is the reviewer catching a contradiction introduced one commit earlier — the criterion, the test and the step-4 verify line now agree. Original reviewer note: The AC is the decidable gate; as written it is unsatisfiable and contradicts both the test and the step it is meant to certify. Three near-misses are silent, one is a positive case. |
