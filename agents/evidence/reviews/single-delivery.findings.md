# Findings: single-delivery
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: fade276373c8eb0cc82aa83c822365692ee763e6faa20d54d690aceec40aa8e4 | diff: 2de07e2fd672e428d173794f0f36f398f125424a | reviewer: r2-fresh-subagent-single-delivery | prompt_hash: a319dcedcc9b467dad63b72359469b802970bdd1d02635708b3d8adaafadd8ce -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 2de07e2fd672e428d173794f0f36f398f125424a
  scope_hash: fade276373c8eb0cc82aa83c822365692ee763e6faa20d54d690aceec40aa8e4
  roadmap: agents/roadmaps/road-to-single-delivery.md
  roadmap_hash: a01b027224377c2176bcd3afbf6cbfa1bbfecfa953e0df9807c1c14cb75aadbe
  ac_hash: 508e3882142046224dd1cb4c555b8792f1858c690aab57cec4a19d071e68a95f
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T20:03:34Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/check_single_delivery.ts:278-294 | The gate exits 1 unconditionally on any machine that lacks BOTH layers, which is every CI runner and every fresh clone — so the Phase-4.3 binding surface it was written for cannot receive it. `.claude/` is gitignored (`git ls-files .claude/` = 0 entries; `.gitignore:123,126`) and no CI leg performs a user-scope install (`taskfiles/dev.yml:129-131` states this for its sibling gate), so `namesRead` is 0, `reportScanned` throws `DeadScopeError`, and the handler returns 1 BEFORE the `readNothing` branch and before `enforce` is consulted. Probed: `--global /tmp/cirun/nohome/.claude --project /tmp/cirun/norepo/.claude` → exit 1 with and without `--enforce`. This also contradicts the gate's own `--help` at :252-256, which promises "Reports by default and exits 0". | fixed | 406cb22a0 — OPTIONAL_SURFACE allowEmpty reason; absent layers now exit 0 without --enforce and 1 with, asserted in tests/scripts/check_single_delivery.test.ts |
| 2 | high | src/scripts/check_single_delivery.ts:73-84 | The gate counts directory *names* and never checks that a name delivers an artefact, and that flaw already produced a false evidence finding. Project `.claude/commands/` holds 40 top-level dirs containing 140 symlinks to `src/domains/**/command.md` (UNCONDENSED SOURCE), while `~/.claude/commands/` holds 192 real `.md` files under 41 dirs — `find .claude/commands -type f` returns 0, `-type l` returns 140. The 40 "in BOTH" are directory names shared between two different payload shapes, not duplicated artefacts. Proven independently: a deliberately BROKEN symlink is reported as `BOTH 1 / duplicated=1` (fixture `/tmp/bs`). Consequence: census:42,45-52 presents this as the branch's headline discovery ("a third artefact type was duplicated … reported 40 in both on its very first run"), and condense.ts:2628-2630 repeats "40 commands delivered twice". | fixed | 406cb22a0 — per-layer shape recorded; name_overlap_different_shape reported on its own line; census split to 110 confirmed vs 330 unverified |
| 3 | high | agents/evidence/analysis/single-delivery-partition-census.md:9-16 | The mandatory projection-shape field — which the artefact calls "the point of the artefact" and whose absence it says produced two refuted drafts — declares `projection: EMITTED REAL FILES` / `project rule files: 111 real files, 0 symlinks`, and that is true of the RULES row only. Measured: `.claude/skills/` is 290 symlinks into `dist/agent-src/skills/` out of 338 top-level entries, and `.claude/commands/` is 140 symlinks; only `.claude/rules/` has 0 symlinks. The census's own §7 makes symlink-vs-emitted the reason a reading is uninterpretable, so two of the four rows it reports (skills 290, the largest, and commands 40) sit outside the field that is supposed to make them interpretable. | fixed | 406cb22a0 — the projection-shape field is per type, per layer |
| 4 | medium | src/scripts/_lib/scope_guard.sh:94-105 | Field 6 is `-1` in exactly the scenario it was added for. `count_overlap` compares the other scope against `this_scope_path` — the directory the install is ABOUT to write — so on a first project install with a populated `~/.claude/skills` the target does not exist yet and the overlap is unavailable. Probed (`/tmp/sgtest`, populated user layer, no project layer): `WARN claude-code … 14.3.0 14.3.0 -1`, which install.sh:794 renders as "overlap not counted (not a directory)". The number is obtainable only on a RE-install over an existing project layer, so ":29-36" here and install-scopes.md:23 ("the cost is visible at the moment of the decision") do not hold for the decision they name. The `-1` message text also misattributes the cause to a non-directory probe when the dominant cause is a target that does not exist yet. | fixed | 4510f6dae — the -1 label now names which reachable case the reader is looking at (empty-or-unreadable this scope), instead of the false 'not a directory' |
| 5 | medium | src/scripts/_lib/scope_guard.sh:100-104 | `count_overlap` returns **0**, not `-1`, when a directory exists but is unreadable — the precise failure its own docstring (:89-93) says it exists to prevent ("reporting '0 overlap' for 'could not look' is how a gate starts reading as coverage"). `cd "$a"` fails inside the process substitution, the failure does not propagate, `comm` sees empty input and `wc -l` yields 0. Probed with `chmod 000` on the target: `WARN claude-code … 0`, plus a raw `cd: … Permission denied` on stderr which install.sh:770 discards via `2>/dev/null` — so the operator reads a confident "0 entries in both scopes" with no signal that nothing was looked at. | fixed | 4510f6dae — readability checked before the comparison; verified -1 on chmod 000 and 1 when readable |
| 6 | medium | src/scripts/_lib/scope_guard.sh:89-93 | The documented justification for `-1` describes an unreachable path. The docstring names the copilot probe as "the concrete case … a single `copilot-instructions.md` file, not a directory, so -1 is its normal and correct answer", but `probe_tool` gates the whole WARN branch on `dir_nonempty "$other_path"` (:81-83, :125), whose first test is `[[ -d ]]` — a file path always short-circuits to `OK` and never reaches `count_overlap`. Probed with the file present at BOTH scopes: `OK copilot - - 14.3.0`. The comment therefore justifies the sentinel with a case that cannot occur, and separately conceals that cross-scope copilot duplication is invisible to the guard entirely. | fixed | 4510f6dae — docstring corrected: the copilot case is unreachable via dir_nonempty's [[ -d ]]; the two reachable cases are named |
| 7 | medium | docs/decisions/ADR-236-one-artefact-one-layer.md:33-36 | The load-bearing premise is cited to an artefact that disclaims it. ":33" asserts "Claude Code loads `~/.claude/**` and `<project>/.claude/**`, both, user layer first, with no dedup. Measured 2026-08-19 …" and cites the census — which measures the filesystem only, and whose own Honest limits (:198-206) decline any host-behaviour claim; `check_standing_rule_delivery` prints the same caveat in its output ("this is what the filesystem projects, not what the host confirmed loading"). The tree's one first-party loading observation is `agents/evidence/analysis/claude-code-rules-dir-contract.md:23-29` (a 2026-08-08 session carrying rules in two copies each) — it covers RULES only and is not cited here. Nothing in the tree establishes dual delivery for the 290 skills or the 40 commands, i.e. for the larger half of the "440 names" headline that install-scopes.md:23 now states as contract fact. | fixed | 406cb22a0 — ADR-236 now states the no-dedup premise as established for RULES only, citing the first-party observation, and says skills/commands are unverified |
| 8 | medium | docs/decisions/ADR-236-one-artefact-one-layer.md:127-128 | The gate whose "bad precedent" justifies leaving the new check unbound is described with the wrong registration and the wrong character. ADR-236 says `check_standing_rule_delivery` "is registered only in `ci-fast`, which no workflow invokes". It is registered at `taskfiles/dev.yml:136` as `task dev:standing-rule-delivery`; `grep -n standing taskfiles/ci-fast.yml` finds only a prose line at :972. dev.yml:129-134 further documents it as "Local-only on purpose" with a stated reason (both inputs are empty on a runner, so CI could only be green-by-blindness) — a deliberate scoping decision, not the orphaned-in-a-dead-aggregate precedent the ADR argues from. | fixed | 406cb22a0 — corrected to taskfiles/dev.yml:136 in ADR-236 and the roadmap; dev.yml's deliberate-local framing is stated, so the precedent is weaker than claimed |
| 9 | medium | agents/roadmaps/road-to-single-delivery.md:500-503 | The `overlap-check-binding-surface` recommendation rests on a prediction the branch's own artefact refutes: "CI would measure a topology no contributor has and **be green** while every local session pays." Measured on `check_single_delivery` as shipped, CI is **red**, not green — the dead-scope assertion fires and returns 1 (finding 1). The argument for preferring preflight over CI therefore does not describe the check it is choosing a surface for. | fixed | d52c41e9c — the recommendation no longer predicts CI would be green; the measured red was finding 1 in the gate, and the reason to prefer preflight is the topology |
| 10 | medium | src/scripts/check_single_delivery.ts:259-262 | Single-dash flags and bare positionals are silently ignored, so `-enforce` downgrades an enforcing run to a report with exit 0 and no diagnostic. The unknown-flag guard tests `a.startsWith('--')` only. Probed against a real 1-name overlap: `--global … --project … -enforce` → "⚠️ … Reported, not enforced", exit 0; `… junkarg` → same; `--badflag` → correctly exit 1. `--enforce` is the one flag preflight/CI will pass, so a one-character typo silently removes the gate's teeth. | fixed | 406cb22a0 — any unrecognised argument is an error, dash or not; -enforce and a bare positional both exit 1, mutation-checked in the test suite |
| 11 | medium | agents/evidence/analysis/single-delivery-partition-census.md:134-137 | The artefact that defines AC-3's target set does not enumerate it. ":137" lists 14 names for a set of 16 and closes with "plus two more". The two are `token-budget-discipline` and `token-optimizer-maintenance` (verified by parsing `workspaces:` over all 117 files in `src/rules/`; the exclusive set is exactly 16). AC-3 requires `<repo>/.claude/rules/` to carry "exactly the exclusively-package-only set", and no artefact on the branch states it in full — the elision costs nothing to remove and is the one list a Phase-2 implementer has to diff against. | fixed | 406cb22a0 — all 16 partition rules enumerated in the census and the roadmap |
| 12 | medium | src/server/routes/wizard.ts:634-661 | The wizard silently drops field 6, so the browser surface where an operator actually picks a scope still shows the WARN with no cost number while the CLI now shows one. `parseScopeGuardOutput` destructures five fields at :650 and `ScopeGuardFinding` (:618-624) has no overlap member — no crash (extras are discarded), but the change's stated goal is unmet on that surface. Its documented contract at :615-616 is now stale ("Each non-SUMMARY line carries verdict, tool, otherScopePath, otherVersion, thisVersion"), and `tests/server/wizard.scope-guard.test.ts` asserts nothing about the new field, so nothing will notice. | accepted-risk | The wizard shows the verdict and the per-tool rows; field 6 is additive and its absence costs a number, not a decision. Fixing it means a server route plus its test for a surface whose verdict is already correct, and this branch is already carrying the gate, the ADR and the contract. Recorded rather than silently left: the stale docstring at wizard.ts:615 and the dropped field are real, and belong to whoever binds the check under blocker overlap-check-binding-surface. |
| 13 | low | src/scripts/_lib/scope_guard.sh:21-22 | The three surfaces this change touched publish three different headline measurements of the same reading: scope_guard.sh:21-22 and install-scopes.md:23 say "110 rules and 290 skills delivered twice"; condense.ts:2628-2630 says "110 rules, 290 skills and 40 commands"; census:45 says "Total delivered twice: 440 names" across three types; ADR-236:39-43 omits commands as well. One change, one measurement, four inconsistent citations of it. | fixed | d52c41e9c — the three surfaces now publish one split: 110 confirmed duplicated (same shape) and 330 name collisions (different shape). The 440 sum is withdrawn in the census, the roadmap headline and ADR-236. |
| 14 | low | src/scripts/check_single_delivery.ts:106-185 | No test exercises the new gate, and nothing registers it. `evaluate`, `readType`, `declaresPaths` and `render` are exported — the shape used for testability — yet `grep -rln "single_delivery" tests/` returns nothing, and the name appears in no Taskfile, no `.github/workflows/`, and not in `src/config/gate-coverage.yml`. The `--enforce` path, the `readNothing` path and the dead-scope path are all unexercised by CI, which is how findings 1 and 10 survived authoring. | fixed | 4510f6dae — tests/scripts/check_single_delivery.test.ts, 13 cases, each shaped by a defect this review found; mutation-checked (reverting the single-dash fix fails exactly 2). Registration stays open under overlap-check-binding-surface. |
| 15 | low | agents/roadmaps/road-to-single-delivery.md:511-523 | Four of the seven acceptance criteria are satisfied by this branch and left `[ ]`: AC-1 (census committed with its shape field), AC-2 (ADR-226 carries `superseded_by: ADR-236`, successor records the decision and its owner at ADR-236:83-89), AC-5 (neither scope_guard.sh nor install-scopes.md now says duplication is free), AC-6 (a check asserts all four artefact types and counts scope defeat separately). AC lines are counted as steps by the dashboard (21 total = 14 phase steps + 7 ACs, `agents/roadmaps-progress.md:42`), so the 44 % it reports understates the branch while `roadmap-progress-sync` Iron Law 2 asks for the flip in the reply that lands the work. | fixed | d52c41e9c — the four satisfied acceptance criteria are flipped to [x]; the three that depend on the halted Phase 2 and on Phase 5 stay open |
| 16 | low | src/scripts/check_single_delivery.ts:93-104 | `declaresPaths` fails silently in two directions and the render loses the direction of the finding. An unreadable file returns `false` from the catch at :98 and a BOM-prefixed file fails `startsWith('---')` at :100, so an I/O or encoding accident on ONE copy manufactures or hides a scope-defeat row with no diagnostic. Separately, `render` (:202-212) never says WHICH copy carries `paths:`, though the remedy differs entirely by direction — which is why the census had to record it out of band ("Project carries `paths:`, global does not, in all seven cases", :105). Both directions currently read the same because the global layer has 0 `paths:` files (verified, consistent with ADR-228), so the omission is latent rather than live. | fixed | 4510f6dae — declaresPaths is three-valued; an unreadable copy is reported as a disagreement rather than silently matching an unscoped one |
| 17 | low | src/scripts/condense.ts:2637-2668 | `_warn_layer_overlap` covers three artefact types where the gate covers four (`agents` omitted), so the build-time warning and the check disagree about the invariant's surface; it inherits the name-counting flaw of finding 2 and will print `commands=40` on the same false basis; and it is silently inert whenever `HOME` is unset (:2638-2639), which is a plausible CI/container state and produces no note that the check was skipped. | fixed | 4510f6dae — _warn_layer_overlap covers agents too, so both surfaces read the same four types |
