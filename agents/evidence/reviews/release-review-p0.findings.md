# Findings: release-review-p0
<!-- completion-review: v1 | reviewed: 2026-08-17 | scope: 3044e3d2e3c109974ee491bc4d66dbf93cbe8e9e77659da4763c82bf4e3d8e03 | diff: e69a10b52a94d0cb8920d11430f3c29ecd93f9b0 | reviewer: r2-fresh-subagent-release-review-p0 | prompt_hash: c034ceb3e2031a33a39a64be4fe2c0c269a99176d37ff0c8ba5bc8efff060208 -->

<!-- context-manifest: v1
inputs:
  diff_sha: e69a10b52a94d0cb8920d11430f3c29ecd93f9b0
  scope_hash: 3044e3d2e3c109974ee491bc4d66dbf93cbe8e9e77659da4763c82bf4e3d8e03
  roadmap: agents/roadmaps/road-to-release-review-p0.md
  roadmap_hash: fe9b312dcf961e5cebc7ad9ea5cc216141c4da63dd89542e0828c8052ffd67f4
  ac_hash: b8e459f524b34b2697dd3b00e1246f945edc2379a3077dcf58b67ac12d730edb
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-17T06:14:39Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/ai_council/qualification.ts:181-186 | `IMPAIRED_FAILURES` includes `'other'`, which is `classifyCliFailure`'s catch-all (`transport_resolver.ts:418`, `return 'other'`). `_postRunQuorum` classifies a member that produced nothing via `classifyCliFailure('empty response body')` / `'no response'` (council_cli.ts:807-812) — both fall through to `'other'`. So the exact shape Phase 3 exists to catch (a seat that constructs, is dispatched, and returns nothing) is recorded as `other`, and on the next pass `_liveProbe` reads it `degraded` → `isCountableForQuorum` true → counted present again. The set's own docstring justifies only `quota_exhausted` / `timeout` / `server_error`; `'other'` is unjustified and contradicts the module header's "the verdict is the weakest claim the evidence supports" — an unclassified failure has not established reachability, so it belongs with `unknown`, not `degraded`. | fixed |  e69a10b52 |
| 2 | medium | src/scripts/council_cli.ts:1139-1151 | The pre_run quorum line loses its `total - present == absent.length` invariant. `present` is now reduced by `unqualified.length`, but no entry is pushed to `absent_entries`, so `_emitQuorumEvent('pre_run', …)` can emit `present: 0, total: 2, absent: []`. `quorum-attendance-budget.json` § `absent_reason_distribution` reads `absent[].reason` split by phase and explicitly treats a pre_run absence as configuration — those seats are now invisible to it, and the shortfall cannot be reconstructed from the line. | fixed |  e69a10b52 |
| 3 | medium | src/scripts/council_cli.ts:2502 vs 2557 | `cmd_status` now reads `agents/runtime/state/council-probes.json` under `REPO_ROOT` and publishes verdicts derived from it, while the same JSON payload still asserts `project_tree_consulted: false`. The field is a machine-readable honesty claim and is now false. | fixed |  e69a10b52 |
| 4 | medium | src/scripts/ai_council/probe_store.ts:63 | Provider health is stored per project (`<REPO_ROOT>/agents/runtime/state/…`) although the council config it qualifies is user-global (ADR-104) and provider reachability is a machine fact, not a project fact. Consequence: the same seat reads `available` in one checkout and `unknown` in another, and in every consumer repo `council:status` prints "⚠️ N of N seat(s) cannot be counted toward a quorum" until a pass is run *in that project*. The docstring names the fresh-worktree case but frames it as honest rather than as the scope mismatch it is; the user-global root (`~/.event4u/agent-config/`) is the location that matches the fact being recorded. | fixed |  e69a10b52 |
| 5 | medium | tests/scripts/lint_evidence_artifacts.test.ts:99-107 | The gate's failing path is untested. Both `checkFiles` tests assert `[]` (empty list; nonexistent path), and `main()` has no test at all — so nothing executable asserts that a real untyped file on disk yields a finding or that the gate exits 1. Acceptance criterion "A typeless evidence artifact fails its check" is marked `[x]` on coverage that only exercises `resolveEvidenceType` returning `null`. | fixed |  e69a10b52 |
| 6 | medium | src/scripts/council_cli.ts:988-1005, 2503-2516 | Two of the seven ladder rungs are unreachable in production. Neither `build_members` nor `cmd_status` supplies `systemPrompt` or `toolsIsolated`, so `system_prompt_path` and `tool_isolation` always report `skipped` outside unit tests — yet Phase 3 step 1 (marked `[x]`) states the pass verifies "the system-prompt path is valid … tools are isolated". `probeMaxAgeDays` likewise has no production supplier. | fixed |  e69a10b52 |
| 7 | medium | src/config/gate-coverage.yml:45-49 | The population count is bumped 260→261 but the emitter half of the same paragraph is left untouched: "**37 of them emit the machine-readable `scanned: <N>` line under their CI-identical argv, and all 37 are listed below.**" `lint_evidence_artifacts` calls `reportScanned` under both of its CI argvs (bare in consistency.yml, `--quiet` in ci-fast) and is deliberately unlisted → 38 emitters, 37 listed, and the "complete with respect to its own contract" claim is now false. The cited precedent does not cover it: `check_rule_projection_integrity` calls `reportScanned` nowhere. This is the third recurrence of the drift the same paragraph documents. | fixed |  e69a10b52 |
| 8 | medium | src/scripts/lint_evidence_artifacts.ts:94-127 | An explicit author declaration can be silently overridden. The three pre-existing grammars are scanned over the **whole** file before the `evidence-type:` marker is looked for in the first 40 lines, so an artifact declaring `<!-- evidence-type: analysis -->` on line 1 that later quotes a real `<!-- completion-review: … -->` line resolves to `current-binding` — i.e. it claims to bind the current tree. A document analysing markers is exactly such a file, and `review-binding-drift.md` is cited by the contract as one. The asymmetry is the tell: unbounded scan for the inherited grammars, 40-line cap for the new one. | fixed |  e69a10b52 |
| 9 | medium | src/scripts/ai_council/probe_store.ts:117-120 | `outcome` is validated only as a non-empty string and then cast to `'ok' \| CliFailureClass`. A corrupt or hand-edited value survives, and `_liveProbe` reads it as `!== 'ok'` and not in `IMPAIRED_FAILURES` → `fail` → `unavailable`. The docstring's stated concern is that one bad row must not "silently downgrade healthy seats to `unknown`"; the actual outcome is worse than the one guarded against. Validate against the known class set and drop the row otherwise. | fixed |  e69a10b52 |
| 10 | low | src/scripts/lint_evidence_artifacts.ts:128 | `relPath.split(path.sep)` over paths that come from `git diff --name-only` / `git ls-files`, which always emit `/`-separated paths. On Windows `path.sep` is `\`, so the whole path is one segment and the `.review-input` rule never fires. The test masks it by constructing the path with `path.join` (test:78) instead of the `/` form the production input actually has. | fixed |  e69a10b52 |
| 11 | low | src/scripts/council_cli.ts:998-1001 vs 1048 | Divergent null-coalescing on the model override: qualification uses double-question-mark while construction uses double-pipe. An empty-string `--model` override makes the ladder report "no model identifier configured" → `unavailable`, while the client is still constructed with the config model — the verdict then describes a seat that is not the one being run. | fixed |  e69a10b52 |
| 12 | low | src/scripts/council_cli.ts:2594-2601 | The status warning is wrong for part of the set it fires on: it prints whenever any seat is uncountable, including `unavailable` ones, but says "A seat with no recorded exchange reads `unknown` until one succeeds — run a council pass and it resolves itself." Running a pass does not resolve an `unavailable` seat. | fixed |  e69a10b52 |
| 13 | low | src/scripts/ai_council/qualification.ts:259-263 | `_modelIdentifier` misattributes `cli_unsupported`. `classifyCliFailure` returns that class for `parse_failed` (a response the CLI could not parse), yet the rung hard-fails the seat and reports "`<model>` was rejected by the transport on `<date>`" — a wrong cause on a terminal verdict. | fixed |  e69a10b52 |
| 14 | low | src/scripts/council_cli.ts:3241 | `cmd_debate` supplies `probe_store`, so debate passes are gated on qualification, but the debate path has no `_postRunQuorum` / `recordProbes` — so it consumes observations and never produces one. A debate-only operator's seats stay `unknown` permanently, which the store's "normal use decays `unknown`" rationale does not hold for. | fixed |  e69a10b52 |
| 15 | low | src/scripts/council_cli.ts:1143-1145 | One `[council] UNQUALIFIED …` stderr line per uncountable seat is emitted on every `run`, `debate` **and** `estimate` — on any machine with no probe store that is the full roster, on every invocation, including the spend-free preview. | fixed |  e69a10b52 |
| 16 | low | agents/roadmaps/road-to-release-review-p0.md:75 | Phase 2 step 1 is marked `[x]` while stating the type set distinguishes five kinds "including a re-bind event"; the contract deliberately ships four and derives the fifth from git. The deviation is argued only inside `docs/contracts/evidence-artifact-types.md`; neither the step text nor the exit criteria were amended to match what shipped. | fixed |  e69a10b52 |
