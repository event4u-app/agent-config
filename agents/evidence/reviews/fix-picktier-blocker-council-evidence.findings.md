# Findings: fix-picktier-blocker-council-evidence
<!-- completion-review: v1 | reviewed: 2026-08-16 | scope: 7697a0225ecda29d208f135f8d0b9a4272bae412b17bded8667ecfd1c7322394 | diff: 8d49432a615fd5f3f67f9ef9be1f0885912739fd | reviewer: r2-fresh-subagent-fix-picktier-blocker-council-evidence | prompt_hash: 644983b614783470507a13dd9bde9983ecdc575833a3ae6c56d01a754d8ea492 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 8d49432a615fd5f3f67f9ef9be1f0885912739fd
  scope_hash: 7697a0225ecda29d208f135f8d0b9a4272bae412b17bded8667ecfd1c7322394
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-16T18:47:09Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | agents/roadmaps/road-to-inbox-harvest-2026-08-d-top-band-model-economy.md:264-266 | The 968,101 ms is decomposed as "roughly three capped calls (round 1, round 2, and the `blind_chairman` pass)". The tree contradicts every part of that. (a) No summation of `latency_ms` exists anywhere — it is set once per `ask()` from `_elapsedMs(t0)` (`clients.ts:1490`), serialised per response (`session.ts:205`, `council_cli.ts:1963`), and never reduced. (b) `consult()` returns only the FINAL round's responses (`orchestrator.ts:471-472`; restated at `council_cli.ts:2761-2762` — "one `CouncilResponse` per member from the final round"), so round 1's latency is not in the artefact at all. (c) `blind_chairman` in the payload is the CLI flag `args.blind_chairman === true` (`council_cli.ts:2838`, written at `:2884`), recorded regardless of whether a chairman call ran; on the default `chairman.mode: 'host'` `_maybe_run_chairman` returns `null` before spawning anything (`council_cli.ts:1543-1544`) and the blind mapping is computed in-process by `build_blind_labels` — zero subprocesses. The entry's own pre-existing text one paragraph up reads the figure the other way ("`anthropic` returned `timeout` after 968,101 ms against its own 300 s ceiling" — line 230-231), i.e. as ONE call that overran its 300 s cap by ~668 s. That points at `spawnSync`'s `timeout`/SIGTERM not killing the child (`clients.ts:1327-1331`, `1392-1395`), not at orchestration overhead. Both conclusions the block draws from the decomposition — "the cause lies in what the orchestration adds" and "raising `DEFAULT_CLI_TIMEOUT_SECONDS` would have shipped a number that changes nothing" — rest on it, and route the next reader away from the actual defect. | fixed | decomposition withdrawn and verified against the tree in 8d49432a6; the replacement names no cause, because the obvious one is contradicted by the 10s-cap measurement |
| 2 | medium | src/scripts/ai_council/clients.ts:1839-1840 | The `model_unsupported_on_transport` detail leads with "Fix: remove the `model:` line from the openai member". Following it literally hard-fails config load: `_build_member` throws `CouncilConfigError` on `member_enabled && !model` (`config.ts:1809-1813`, no default is applied — `model` is `''` when the key is absent, `config.ts:1775-1776`), and the throw aborts the whole load, so `council:status` reads NOT CONFIGURED for **every** provider, not just openai. The working form is the parenthetical `codex-default`, which the ladder check explicitly exempts (`config.ts:1856-1860`). This diff establishes exactly that in writing (roadmap:247-253) and edits this same file, but leaves the misleading prescription live. The parenthetical should be the primary instruction. | fixed | both messages now lead with `codex-default` and state that deleting the line fails config load — 8d49432a6; no test pinned the strings |
| 3 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-d-top-band-model-economy.md:366-380 | The `Recommendation:` bullet was left asserting the position the other two bullets in the same entry were just corrected away from. It still says the quorum fix "is unconditional — it costs one line" and "the quorum fix is first and not optional", while the `What to do:` bullet this diff updated now says the remaining half "is no longer something this entry can name a one-line fix for" (:362-365) and the `Resolved when:` bullet adds a branch where the maintainer answers Yes and "(b) executes on the plan below" with no converged council at all (:399). Two of three bullets carry a dated supersession marker; this one carries none, so a maintainer reading the field named for the decision gets the pre-correction instruction. It propagates verbatim into the generated dashboard at `agents/roadmaps-progress.md:390`. | fixed | dated supersession marker on the Recommendation bullet in 8d49432a6 — the (b) half stands, the one-line-quorum-fix ordering does not |
| 4 | medium | agents/roadmaps-progress.md:411 | Dangling pointer in the generated decision surface. The `Resolved when:` text ends "**Yes** → (b) executes on the plan below", but the 5-step execution plan (roadmap:403-419) sits after a blank line, and `_blockerField` terminates a field at the first blank line (`src/agent-src/scripts/update_roadmap_progress.ts:426-427`). Verified by regenerating into a scratch root: the plan is absent from `roadmaps-progress.md` (`grep -c "Archive the smallest coherent boundary"` → 0), so the dashboard — and `agent-config gates`, which the dashboard header routes the maintainer to ("10 need you") — tells the reader to execute a plan that is not on the surface they were sent to. The plan needs to be inside the field (no blank line) or the pointer needs to name the roadmap file. | fixed | blank line removed so the plan stays inside the field, with the `_blockerField` reason inline — 8d49432a6 |
| 5 | low | src/scripts/ai_council/clients.ts:1695-1697 | The correction block contradicts itself in adjacent clauses: "`cwd: os.tmpdir()` for a **DIFFERENT** contamination … Two repairs, **one symptom**". The two documented symptoms are not one — the tools flag was found on a context-window overflow (`is_error: true`, ~214331 tokens, exit 1, `clients.ts:1683-1690`), the cwd fix on a member answering this repo's `end-review-nudge` instead of the question (`clients.ts:1332-1339`). The shared item is the *cause* (inherited cwd), not the symptom. In a comment whose stated job is correcting inaccurate prose, this leaves the next reader believing either repair alone would cover both failures. | fixed | "one symptom" corrected to "one cause" in 8d49432a6 |
| 6 | low | agents/roadmaps-progress.md:403-411 | The file's own header says "Auto-generated — do not edit", and this diff hand-edits it rather than regenerating: a regeneration produces 9 further hunks this diff does not contain (verified against a scratch `--repo-root`), and `update_roadmap_progress --check` exits non-zero at branch head. The picktier hunk itself matches generator output exactly, and the 9 other hunks are all in roadmaps this branch never touched — so the staleness is pre-existing, NOT introduced here. Recorded because whoever pushes will hit the red `--check`, and because a hand-edit of a generated artefact only stays correct by coincidence. | accepted-risk | did not reproduce: `roadmap:progress-check` reports the dashboard up to date at branch head, and `roadmap:progress` was re-run in 8d49432a6. Accepting the residual risk that the reviewer measured a staleness this re-run cannot see, rather than recording a fix that changed nothing |
