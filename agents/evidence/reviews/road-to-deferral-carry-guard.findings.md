# Findings: road-to-deferral-carry-guard
<!-- completion-review: v1 | reviewed: 2026-09-02 | scope: bb11b8382ede49e22f5af2a651ed5067ea4fbc7ce19281278091523b84855be6 | diff: bd3f1e6b3e0a4f50319d342ae7071865c3685acc | reviewer: r2-fresh-subagent-road-to-deferral-carry-guard | prompt_hash: d8e58609ae90f5c97f72278154591e6ae014060a5126a2c5e3a0ae22a8572ecb -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-road-to-deferral-carry-guard"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-02 -->

<!-- context-manifest: v1
inputs:
  diff_sha: bd3f1e6b3e0a4f50319d342ae7071865c3685acc
  scope_hash: bb11b8382ede49e22f5af2a651ed5067ea4fbc7ce19281278091523b84855be6
  roadmap: agents/roadmaps/archive/road-to-deferral-carry-guard.md
  roadmap_hash: 4c36395e4fcf138de9af49e9b7e2f4303b3fcc70a194abf4a612d4f0bd7a6b42
  ac_hash: dbcc8e1af9cae92ff6bc354ed981f99c836cf2004705c3ecf730ce9ac05a347a
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-02T05:11:12Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | agents/roadmaps/archive/road-to-deferral-carry-guard.md:223 | `status: carrier` is a **free, unguarded** exclusion, and Risk 1's mitigation asserting otherwise is false. Measured on throwaway fixtures: a top-level roadmap with an open `- [ ]` step, no phase heading, no Risk Register and **no archived parent naming it** is (a) invisible to `lint_carrier_integrity` (`scanned: 0`, exit 0), (b) invisible to `check_roadmap_trackable` ("0 active roadmap(s)"), (c) `draft-exempt` in `lint_plan_risk_register` (exit 0), (d) absent from `collect()` and therefore from `check_estate_count.active_roadmaps`. Flipping one existing roadmap's frontmatter to `status: carrier` moved the measured estate `active_roadmaps 3 → 2 (floor 3, -1)` with `✅ check_estate_count: estate within its ratchet` — the count is read as a *shrink*, so the ratchet floor drops and the file cannot be flipped back without paying. The register's mitigation says "a file that calls itself a carrier without an archived parent pointing at it gains exclusion and gains a red at the same time"; the gate enumerates archived parents (`deadRoadmaps`, `lint_carrier_integrity.ts:107`) and never enumerates self-declared carriers, so no red exists. Concrete failure: any inconvenient active roadmap can be laundered out of four gates and out of the estate count by adding one word, which is the risk the register names as closed. | open | |
| 2 | medium | src/scripts/lint_carrier_integrity.ts:130 | `_linksBackTo` re-opens the spoofable-back-link defect that a prior review closed in the twin it is meant to reuse. It accepts `- slug: <parent>` matched **anywhere in the file** (and `^parent_roadmap: <parent>` anywhere, not only in frontmatter). `deferralProblems` deliberately requires a *structured frontmatter declaration* — `archive_completed_roadmaps.ts:492-499` records why: "`destText.includes(sourceSlug)` was satisfied by any incidental mention — a filename, an example, an HTML comment … Both review seats called it trivially spoofable … Prose cannot satisfy it", and `_relatesTo` (`:349-358`) breaks at the first non-indented line so a body mention cannot qualify. Measured: a receiver whose only back-link is `  - slug: road-to-parent` inside a fenced `yaml` block in the body passes (`exit 0`, "every annotated carry resolves"). Concrete failure: AC-2 ("removing the `parent_roadmap:` back-link makes the gate red") holds only for receivers that do not happen to carry that slug in any list item anywhere, and the module header's claim that reuse means the check "cannot drift" is untrue for the back-link half. | open | |
| 3 | medium | src/scripts/lint_carrier_integrity.ts:153 | `OPEN_STEP_RE` (`:153`) and `CARRIER_STATUS_RE` (`:154`) are matched against the whole file — neither frontmatter-scoped nor fence-aware — in a class that hard-fails at zero with no baseline. Two measured false reds on legitimate archived-and-completed receivers whose bodies merely *document* roadmap syntax: a fenced `markdown` block containing `- [ ] **1.1 An example open step.**` yields "destination … is archived but still has OPEN steps — it was moved by hand rather than completed, so the carried obligation is stranded"; a fenced `yaml` block containing `status: carrier` yields "destination … is a `status: carrier` roadmap that has been archived … strands them". The same change scopes the identical test correctly one file over (`check_estate_count.isCarrierText:557` anchors to the frontmatter block), and the repo already has a fence-stripping helper (`lint_plan_risk_register._linesOutsideFences`). Concrete failure: the gate is wired into `task ci` and `.github/workflows/rule-backstops.yml:150`, so a documentation paragraph in any archived carry destination reds CI with a diagnostic that names the wrong cause and offers remediation ("move the carried items back to open") that does not apply. Not hypothetical here: `agents/roadmaps/stubs/road-to-carrier-transition-vocabulary.md`, added by this diff, is exactly such a document. | open | |
| 4 | medium | src/scripts/check_estate_count.ts:1170 | The self-test case AC-4 rests on is over-determined and does not exercise the mechanism it claims to assert. Its fixture is base = 3 ordinary roadmaps + 1 carrier, HEAD = carrier deleted + one ordinary roadmap added. Because `collect()` now excludes carriers, the floor is `active_roadmaps 3` and HEAD measures 4, so the run I reproduced emits **both** `❌ the roadmap estate grew: active_roadmaps 3 → 4` and `❌ 1 new active roadmap(s) with no offset`. The growth half is computed only from `counts`, `floor` and `allowance` (`:846-865`), and `allowance.active_roadmaps` is `offsets.exempt.length` — never `offsets.offsets` — so the case rejects unchanged with the `isCarrierText` branch at `:615` reverted. Algebraically the two conditions coincide for ordinary additions (growth iff `n > a`; `unpaid_new > 0` iff `n > a`), so the only configuration the new branch actually decides is a **draft** addition plus a carrier deletion, which I measured (unpaid fires alone, no growth finding) and which no case covers. `isCarrierText` and the `readBase` argument have no unit coverage either: `tests/scripts/check_estate_count.test.ts` is untouched by this diff and contains 0 references to either. Concrete failure: AC-4's stated evidence ("asserted in `check_estate_count`'s own case table") would stay green if the feature were removed. | open | |
| 5 | medium | src/scripts/lint_plan_risk_register.ts:344 | The validator now exempts a status its contract does not name, in a file whose own header (`:5-8`) declares "Implements exactly `docs/contracts/plan-review-gates.md` § 1 … A divergence from that contract is a validator bug, never a contract reinterpretation." `docs/contracts/plan-review-gates.md:34-36` says only "`status: draft` frontmatter exempts the file until it flips to ready"; the diff touches no file under `docs/contracts/`. Measured: a `status: carrier` file with an open step and no `## Risk Register` is reported `draft-exempt` and the gate exits 0. Secondary: `hasDraftStatus` now returns true for a non-draft status and the diagnostic still prints `draft-exempt`, so the gate's own output misnames the state it acted on (visible on the real tree — `draft-exempt: agents/roadmaps/road-to-council-topology-evidence-followups.md`). Concrete failure: the Gate-R1 contract and its sole validator disagree about the exempt set, which is the condition the header defines as a validator bug. | open | |
| 6 | low | src/scripts/lint_carrier_integrity.ts:67 | Bullet grammar is narrower than both the repository's canonical checkbox regex (`check_roadmap_trackable.ts:42` and `update_roadmap_progress.ts:81`, each `[-*]`) and the `DEFERRED_STEP_RE` the gate itself calls through `parseDeferredItems` (`archive_completed_roadmaps.ts:333`, `[-*]`). `CARRY_CANDIDATE_RE` (`:67`) and `OPEN_STEP_RE` (`:153`) accept only `-`. Measured: an archived receiver whose open step is `* [ ] **1.1 Still open, asterisk bullet.**` passes (exit 0) where the `-` form reds; a `* [~]` step with no annotation is skipped by the pre-filter before `carryProblems` runs, so it is absent from the 243 `unannotated` baseline. Zero instances in the tree today (`grep -rlE '^[ \t]*\*[ \t]+\[~\]'` over both dead directories returns 0), so this is latent, not live. Concrete failure: the pre-filter comment at `:62-66` describes exactly this class of blindness as the one its own self-test caught, and one half of it is still open. | open | |
| 7 | low | agents/roadmaps/archive/road-to-deferral-carry-guard.md:208 | The § Evidence link is one directory level short after the promotion-then-archival move. From `agents/roadmaps/archive/`, `../evidence/analysis/topology-followups-disposition-evidence-2026-09-01.md` resolves to `agents/roadmaps/evidence/…`, which does not exist (`ls` confirms); the target is at `agents/evidence/analysis/…` and the deleted stub used the correct `../../` for its own depth. `check_references` skips this directory (measured, `excluded_directory ×875`, exit 0), so nothing catches it. Concrete failure: the roadmap's only pointer to the derivation it says "rewriting would lose" is dead for any reader following the link. | open | |
| 8 | low | src/scripts/lint_carrier_integrity.ts:244 | `deadRoadmaps(root)` is walked twice per run — once as `walked` at `:476` for the ledger, once inside `auditCarries` at `:244` — so the readdir plus 657 `statSync` calls happen twice, and every one of the 657 files is then read **whole** (`:247`) before the cheap candidacy filter at `:253`. The roadmap's Risk 2 mitigation records the opposite design: "Read only the frontmatter and the annotation lines rather than parsing whole files". Measured 0.28 s over 16 MB, so the risk itself has not materialised; what is wrong is that the recorded mitigation is not what shipped, which is the same class of unfalsifiable claim the register exists to prevent. | open | |
| 9 | low | src/scripts/check_estate_count.ts:455 | `laterRoadmaps()` still filters on `isDraft` alone, so a carrier parked under `later/` has its blockers counted in `open_blockers` while a draft's are not. This state is legal by this change's own design: `_locate` (`lint_carrier_integrity.ts:141`) treats `later/` as live and self-test case 2 pins "a receiver parked under later/ is equally live and is accepted". Three of the four status readers were taught `carrier` in this diff (`update_roadmap_progress` `collect`/`collect_parked`, `check_roadmap_trackable`); this fourth one, in a file the diff edits, was not. Concrete failure: parking a carrier changes `open_blockers` and therefore the ratchet verdict, for no stated reason. | open | |
| 10 | low | agents/roadmaps/stubs/road-to-carrier-transition-vocabulary.md:25 | The transition table attributes a refusal to "the `status: carrier` archival check", which does not exist. `archive_completed` iterates `collect(roadmap_root)` (`archive_completed_roadmaps.ts:540`), and `collect` now *skips* carriers, so the archival sweep never considers a carrier and nothing in it refuses one; the actual red comes from `lint_carrier_integrity`'s destination-side branch at `:207`, and only when an archived parent still names the file. Concrete failure: a reader looking for the named check finds nothing, and the row's promise does not hold for a carrier no archived parent points at (see finding 1). | open | |
| 11 | low | src/config/gate-violation-baselines.json:87 | The new `lint_carrier_integrity` baseline is dated `2026-09-02` and describes a population it also states cannot be repaired ("They are NOT repaired: rewriting an archived roadmap's step semantics retroactively invents an intent nobody recorded, and the honest alternative glyph `[-]` is owner-reserved"). `_lib/gate_baseline.ts:41` fails any baseline that has not moved in `STALE_AFTER_DAYS = 56`, i.e. from 2026-10-28, and the note offers only "Shrink-only from here" without naming the one remedy that fits an unrepairable population — a `reaffirmed: {date, reason}` block (`gate_baseline.ts:54`, `:337-348`). Concrete failure: a dated, foreseeable CI red whose stated remedy ("fix the findings" / "lower the count") is the one the note rules out. | open | |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
