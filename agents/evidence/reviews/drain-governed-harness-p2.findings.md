# Findings: drain-governed-harness-p2
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 0a388c9fb6decc3578171695b5d48a77701cc456e8a632e19ae6c07bfc501710 | diff: b1063e0248451c2327d8b3b5b014c0af0852e7b0 | reviewer: r2-fresh-subagent-drain-governed-harness-p2 | prompt_hash: 163532112bc230beb6e45473c90c203862e952539d192fac6c5d07928d2406b3 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-governed-harness-p2"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: b1063e0248451c2327d8b3b5b014c0af0852e7b0
  scope_hash: 0a388c9fb6decc3578171695b5d48a77701cc456e8a632e19ae6c07bfc501710
  roadmap: agents/roadmaps/road-to-governed-harness-evolution.md
  roadmap_hash: 52f187388b4953ecb345210ef59d16dc45925474f6057353a11fb03fcfd2b956
  ac_hash: b1b82ecc9b038697d2727423516659df31c4228e1a7add1cb3afb7963c9174a9
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T17:45:05Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/lint_skill_trigger_corpus.ts:319-334 | `changedUnits` swallows EVERY git failure (`catch { return new Set(); }`) and `evaluate` at :359/:372 feeds that empty set straight into `judge(..., FORWARD_ALL \|\| changed.has(name))`. Wherever `origin/main` does not resolve — shallow clone, fork with a differently-named remote, tarball install, worktree without the ref fetched, a CI job that does not fetch the base — the whole forward-only discipline (the German rule AND all five new `class-*` rules) becomes a silent no-op, yet `main()` at :619-626 still prints "Diff-touched files also carry a declared case class per query" and returns 0. Reproduced twice: copied `src/skills` to a non-repo root, stripped every `class` and `language` from `security-audit/evals/triggers.json` -> exit 0, green line; then `git init` in that root with no `origin/main` -> `fatal: Needed a single revision`, still exit 0, still green. `evaluate` already guards the analogous empty population (`scanned === 0` -> DeadScopeError, :379-384) but has no guard for an unresolved base ref, so the newly-added discipline rests entirely on the one population that can silently empty. This also refutes the roadmap's own claim at `road-to-governed-harness-evolution.md:642` that the six corpora "could not have been merged without it". | fixed | fixed in 416901a31 — changedUnits returns a three-state ChangedScope; evaluate throws DeadScopeError on no-repo and base-unresolvable. Both polarities pinned in lint_skill_trigger_corpus.test.ts; self-test fixtures are now real repos with a resolvable origin/main, so the not-diff-touched arms supply the condition instead of simulating it. |
| 2 | high | src/skills/threat-modeling/evals/triggers.json:4 | The `description` asserts "Near-misses are the three neighbours that named it" — false, and falsifiable with the census's own recipe. At `origin/main` the skills whose `trigger:false` notes name `threat-modeling` are `agent-security-review`, `ai-code-blindspots` and `security-maturity-assessment`; the file's near-misses are `security-audit`, `security-maturity-assessment` and `authz-review`. Only 1 of 3 overlaps: two declared "namers" never recorded a confusion, and the two edges that DID record one stay untested on the receiving side — the exact defect wave 1 exists to close, left open on the queue's #2 item. Same class of error in two siblings: `src/skills/security-audit/evals/triggers.json:4` names "the four neighbours that named it" starting with the pre-implementation threat pass, but `threat-modeling` carried no corpus at base and so named nothing, while the real namer `evaluate-llm-feature` is absent from the near-misses; `src/skills/markitdown/evals/triggers.json:4` says "the three neighbours that named it" when only `docx-authoring` and `pdf-tools` did (3 edges, 2 distinct) and `image-analyser` is not a namer. The `dist/agent-src/skills/*/evals/triggers.json:4` copies carry the same text byte-identically. | fixed | fixed in 416901a31 — the three descriptions now state what their near-misses actually are and record the discrepancy. Corpora unchanged: the near-misses are semantically adjacent and remain good near-misses; only the provenance claim was false. |
| 3 | medium | agents/roadmaps/road-to-governed-harness-evolution.md:636-638 | The 2.4 closure generalises finding #2 to the whole batch — "Each new corpus uses its own recorded inbound edges as its near-miss cases, so the neighbour that reported the confusion is the neighbour the new corpus is tested against — which is what makes this a defect fix rather than a coverage bump" — and `agents/evidence/analysis/trigger-corpus-census-2026-08-30.md:179` repeats it. It holds for `prompt-engineering-patterns`, `logging-monitoring` and `incident-commander`, whose descriptions state the mix honestly, and fails for `threat-modeling`, `security-audit` and `markitdown`. Since that sentence is the stated basis on which the step is closed as defect-driven rather than as the coverage bump K12 killed, half the batch is closed on an unestablished justification. The step's literal `verify:` (presence gate green, allowlist < 205) IS satisfied — I ran both: `check_trigger_eval_presence` -> 100/299, 199 grandfathered, exit 0; `jq '.skills\|length'` -> 199. | fixed | fixed in 416901a31 — step 2.4 and the census now say half the batch is a defect fix and half a coverage bump on a defect-ranked ordering. The selection claim was re-verified and is untouched. |
| 4 | low | agents/evidence/analysis/trigger-corpus-census-2026-08-30.md:120 | Wave-1 row reads "34 skills / 38 edges (was 42 / 63 before this run's six)". The current pair reproduces exactly under the row's own criterion (routable, no corpus, >=1 recorded edge). The parenthetical does not: 42 / 63 is the section-3 figure measured WITHOUT the routable filter — it includes four non-self-activating judge skills the wave-1 criterion excludes. Under the row's criterion the prior value at `origin/main` is 38 / 54 (recomputed with the census's own reproduce recipe). As written, the only before/after pair the executable queue has reads as a shrink of 8 skills / 25 edges when the queue actually moved 38->34 and 54->38, and 42 - 6 = 36 contradicts the 34 in the same cell. | fixed | fixed in 416901a31 — corrected to 38 / 54, the prior value of that row under its own criterion. |
| 5 | low | src/scripts/lint_skill_trigger_corpus.ts:35-37 | "zero of the 76 existing files carry the field ... would red 76 files" and the live DeadScopeError rationale at :382 ("76 exist in the shipped tree, so an empty scan is a moved root") are stale: the tree carries 100 corpus files (gate output, this branch) and 6 of them now DO carry `language`. The drift predates this branch (94 at `origin/main`), but the branch rewrote the surrounding docstring section — "Forward-only, in two different senses" -> "in three senses now" — and added the 6 files without re-anchoring the number, so a guard message that is meant to prove a moved root now under-states the shipped population by 24. | fixed | fixed in 416901a31 — restated against the live tree (25 of 100) and marked as dated illustration, since nothing reads the number. |
| 6 | low | src/scripts/lint_skill_trigger_corpus.ts:267-274 | The `class-shape` rule cannot fire on any file in the current tree. The only two legacy `should_trigger`/`should_not_trigger` corpora are `brand-asset-generation` and `estimate-ticket` (verified by scanning all 100 files; no file mixes both shapes), and those are exactly the two members of `GRANDFATHERED`, for which `judge` returns at :238 before `judgeClasses` runs. Its live population is structurally zero unless someone authors a NEW legacy-shaped file. It is exercised by synthetic fixtures in both the self-test and the unit test, so it is not an untested rule — but the 2.3 closure presents it as one of five enforced findings, which over-states what is reachable against the corpus that exists. | fixed | fixed in 416901a31 — recorded, not removed. The rule is a forward guard; step 2.3 now says four of five are reachable today and names the fifth as forward-only. <!-- ref-ignore --> <!-- `class-shape` is a lint finding id, not a rule artefact --> |
