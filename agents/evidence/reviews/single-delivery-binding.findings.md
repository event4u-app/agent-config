# Findings: single-delivery-binding
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: d3eeb0f3b0ab1f48731270290a5863916a58df67f8c07929473d15e8ccf15948 | diff: bd00405f695d101627f433d8216ebf5473de5212 | reviewer: r2-fresh-subagent-single-delivery-binding | prompt_hash: bd27f18a28f803344f25fd3e08e05b3f00860770c5a5303d9fabded739b40be0 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: bd00405f695d101627f433d8216ebf5473de5212
  scope_hash: d3eeb0f3b0ab1f48731270290a5863916a58df67f8c07929473d15e8ccf15948
  roadmap: agents/roadmaps/road-to-single-delivery.md
  roadmap_hash: 1845d0fd641401ec4cfbe33dbbeb1b5a8b7e0ec2ece2f5969bb7ef357fc1088d
  ac_hash: 508e3882142046224dd1cb4c555b8792f1858c690aab57cec4a19d071e68a95f
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T21:31:29Z
-->

> **Re-bound 2026-08-19** to scope `d3eeb0f3` / diff `bd00405f`. The reviewed
> content moved under this artefact for the ordinary reason: the ten findings were
> committed BEFORE their fixes (contract §2.5), so every fix, plus a merge of
> `origin/main` and the regeneration it forced, changed the diff the review binds to.
> `roadmap_hash` moved with it — the roadmap carries the corrections findings 3, 4 and 7
> asked for. `ac_hash` is unchanged, which is the check that matters here: the
> acceptance criteria were not edited to fit the work. This commit touches ONLY this
> artefact, so the binding it writes is a fixed point rather than a hash that is stale
> again the moment it lands.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | taskfiles/ci-fast.yml:103 | The registration reds a CI-enforced shrink-only ratchet. `check_single_delivery` appeared in no taskfile before this branch, so registering it in `preflight` (which is inside the `task ci` closure) adds it to the local-only set: measured `./scripts-run src/scripts/check_ci_local_parity` exits 1 with "167 violation(s) against a baseline of 166 — 1 new", and `check_single_delivery` is the named new entry. That checker is a CI step at `.github/workflows/consistency.yml:245`, so the Consistency workflow goes red. The ci-parity baseline note states this outcome is intended: "a NEW gate registered in `task ci` with no workflow reds immediately". The prescribed fix is one `local_only:` declaration with a reason in `src/config/ci-local-parity.yml` — and the reason is already written verbatim at the call site (developer-machine topology, CI has no `.claude/` layers) but was never recorded in the manifest. | fixed | d3d1cfe4b — declared `local_only` in `src/config/ci-local-parity.yml` with the measured reason: CI has no `.claude/` layers to compare, so a remote equivalent would measure a topology no contributor has. Parity check green, 4 declared local-only. |
| 2 | high | tests/scripts/preflight_single_delivery_binding.test.ts:33-56 | The test proves PRESENCE IN THE FILE, not reachability from `task preflight` — which is the exact failure it was written to prevent. All three recipe assertions treat `ci-fast.yml` as a flat string (`toContain`, `split('\n').find`, `indexOf`); none parses the `preflight:` task, its `cmds:` list, or the task graph. Probed on a temp copy: relocating the command line together with its comment block into a new `single-delivery:` task that nothing invokes leaves all three assertions green (t1 true, t2 true, t3 true) while `preflight` no longer lists the gate. So roadmap 4.3's "The binding is PROVEN live" and the call-site's "That proof is tests/scripts/preflight_single_delivery_binding.test.ts" overstate the test: it cannot distinguish "bound to the pass contributors run" from "registered in the same file, reachable by nobody". | fixed | d3d1cfe4b — the assertions parse the `preflight` task's own command block now, plus a vacuity guard on the parser itself. Mutation re-run: moving the command into an uninvoked task fails **3** assertions where the old version failed **0**. The 'PROVEN live' claim is corrected in the test header and the roadmap. |
| 3 | high | agents/roadmaps/road-to-single-delivery.md:306 | False claim about the binding path, asserting a council refinement was adopted when it was not. The resolution reads "the surface is named (preflight, reached via `task generate-tools`)" and the entry above it claims both seats' refinements "are adopted", refinement (i) being "bind the check so it is reached by the path that can actually introduce the defect — `task generate-tools`". Measured at `taskfiles/content.yml:46-56`: `generate-tools` runs `condense --generate-tools` and `report_layer_overlap` behind a shell or-true guard, nothing else. It does not invoke `preflight` and does not invoke `check_single_delivery`; the only thing on that path is an advisory notice that cannot fail. Refinement (i) is unimplemented and preflight is not reached via `generate-tools`. | fixed | d3d1cfe4b — claim withdrawn. Refinement (i) is recorded as NOT implemented; `generate-tools` runs `condense` plus an advisory reporter and invokes neither preflight nor the check. The surface decision (preflight) stands; the second entry point is a stated gap. |
| 4 | high | agents/roadmaps/road-to-single-delivery.md:250-262 and :469 | Phase 2's halt is recorded as prose only, so the estate ratchet banks an undecided decision as headroom. `partition-requires-global-layer` — the only blocker whose Blocks field read "Phase 2 entirely" — is flipped to `resolved`, while step 2.0 records a NEW halt ("HALTED AGAIN … `process-full` halt condition 4") inside the step body. After the change no open blocker in the file blocks Phase 2: the three remaining read "nothing in this roadmap", "Phase 5 step 5.1 only", "Phase 5 step 5.2 only". Three consequences. The generated dashboard's blocker list no longer explains why Phase 2 cannot proceed. `lint_roadmap_blockers`' decidability fields (Recommendation, If you do nothing, enumerated options) never apply to the live halt, so the undecidable-predicate decision is not decidable by its owner. And `open_blockers` walks 74 to 73 while the number of open decisions is unchanged — two resolved, two opened, one of them recorded. `check_estate_count` measures 73 and passes, which is the point: the ratchet was satisfied by not recording the second decision, not by closing it. | fixed | d3d1cfe4b — blocker `partition-current-layer-undecidable` opened for the halt, with three enumerated options and a recommendation, and `open_blockers` restored to 74 with a history entry that records the wrong walk-down rather than hiding it. This was the sharpest finding: the ratchet had been satisfied by not writing a decision down. |
| 5 | medium | taskfiles/ci-fast.yml:103 | A second ratchet reds. `./scripts-run src/scripts/check_gate_completeness` reports "217 violation(s) against a baseline of 215 — 2 new", and `check_single_delivery` is in the un-adopted list (output line 80). The registration is what admitted it to the parsed `registered` population of 261 — it was in no taskfile before — and the baseline note states "headroom is zero either way, so the next un-adopted arrival reds this gate immediately", so at least +1 is attributable to this branch. The gate runs in `task ci` via `Taskfile.yml:189`, so the local aggregate is red. Honest bound on this row: I could not isolate the second new violation without mutating the tree, so one of the two may be pre-existing on the trunk. | fixed | d3d1cfe4b — two `// ledger-exempt:` markers, each carrying an argument and the condition that voids it: `check_single_delivery` has no skip semantics (all four types appear in the output, absent ones as `absent`), and `report_layer_overlap` cannot fail so has no verdict to account for. Gate at baseline 215, 0 new. |
| 6 | low | tests/scripts/preflight_single_delivery_binding.test.ts:54-56 | The "states why it binds" assertion has no guard that the registration was found, so on absence it silently widens instead of failing. `indexOf` returns -1, and `recipe.slice(Math.max(0, idx - 1200), idx)` becomes `slice(0, -1)` — the whole file minus one character — which is then regex-tested. Latent today only because the single match for the pattern (`gitignored`, ci-fast.yml:90, verified the only hit in the file) sits inside the same added comment block, so a whole-block deletion still fails; delete only the command line and the assertion passes over an unrelated span. `expect(idx).toBeGreaterThanOrEqual(0)` closes it. | fixed | d3d1cfe4b — the slice is guarded by `expect(idx).toBeGreaterThan(-1)` before it is taken, and it is scoped to the preflight block so prose elsewhere in the file cannot satisfy it. |
| 7 | low | agents/roadmaps/road-to-single-delivery.md:337-338 | Over-generalised reproduction in the record of a falsified claim. The Phase 3 defect note states a live guard run reports `DRIFT · claude-code · unknown · 14.5.0` "for every tool". Measured with the command the blocker itself supplies: 3 DRIFT rows (claude-code, augment, cursor) and 3 OK rows (cline, windsurf, copilot), `SUMMARY DRIFT 3 0 3`; the emitted line also carries the layer path in field 3, so the quoted four-field form is not what the guard prints. The substantive half is verified correct — `count_overlap` is called only from the WARN branch at `src/scripts/_lib/scope_guard.sh:175`, WARN requires `other_ver == this_ver`, and the global layer reports `unknown` — and the blocker body's narrower phrasing ("every real cross-scope install classifies as DRIFT") is accurate. Only the Phase 3 note over-reaches. | fixed | d3d1cfe4b — corrected to the measured 3 of 6 (claude-code, augment, cursor have a layer at both scopes; the other three report OK because no second layer exists). |
| 8 | low | agents/roadmaps-progress.md:816 | The new free-form `- **STILL OPEN, and the council SPLIT …**` bullet added inside the `compact-survival-of-package-only-rules` entry is not a field the dashboard generator models, so the regenerated dashboard folds it onto the end of the preceding line: "… the one nobody chose. - **STILL OPEN, and the council SPLIT — …**". A decision-status paragraph is now rendered as part of the cost-of-inaction field. | accepted-risk | The free-form bullet is prose inside a blocker entry, and the dashboard's field extraction folds trailing prose into the cost-of-inaction field. Splitting it into a schema-shaped field would mean either a new blocker field the contract does not define, or dropping a measurement that is the whole reason the question narrowed. The generated view is slightly wrong; the source of truth is not. Recorded rather than left silent. |
| 9 | low | taskfiles/ci-fast.yml:103 | Every `task preflight` run now prints roughly 30 lines about an invariant nobody can satisfy until Phase 2 ships. Measured live: `rules global 115 · project 111 · BOTH 110`, two SHAPE MISMATCH warnings, and a seven-entry scope-defeat list. The call-site comment rejects `--enforce` because a gate that reds "on a defect nobody can currently fix … teaches people to skip it"; an unactionable 30-line report on a task declared `silent: true` is the same dynamic one register lower, and the report-mode window lasts as long as Phase 2 stays halted (see finding 4). | fixed | d3d1cfe4b — `--quiet` added and registered in preflight: 2 lines instead of ~30. A gate that floods a `silent: true` chain is one people learn to scroll past, which ends where an unbound gate ends. |
| 10 | low | taskfiles/ci-fast.yml:95-97 | The justifying claim "two gates in this exact area already report to nobody" is loose for one of the two. `check_rule_projection_integrity` is registered in this very `preflight` list at line 75 and runs as a CI step in `consistency.yml`; it is conditionally inert locally, not unwired. The parenthetical that follows states the narrower fact correctly ("inert when `.agent-tools.yml` selects zero tools"), so the summary sentence and its own evidence disagree. `check_standing_rule_delivery` is accurate on both counts — `taskfiles/dev.yml:136` is its only registration, and its 185.3 % of the standing cap reproduces exactly. | fixed | d3d1cfe4b — corrected at the call site: `check_rule_projection_integrity` DOES run, here and in CI; it is inert when `.agent-tools.yml` selects zero tools, which is a different and weaker claim than 'reports to nobody'. |
