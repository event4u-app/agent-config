# Findings: drain-source-silence-cutover
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 5d1b901782ce676ffca5b3e5bd47cc0bc0b569aacd739e89d5d2f40bc73ed913 | diff: 1393fde93bb92c6a441c3306b57adfcd736742d0 | reviewer: r2-fresh-subagent-drain-source-silence-cutover | prompt_hash: 6dc94d8012465e5e22ee070cc0bc6957f96ec34de6ea2217b178606563818e80 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-source-silence-cutover"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 1393fde93bb92c6a441c3306b57adfcd736742d0
  scope_hash: 5d1b901782ce676ffca5b3e5bd47cc0bc0b569aacd739e89d5d2f40bc73ed913
  roadmap: agents/roadmaps/later/road-to-source-silence-cutover.md
  roadmap_hash: cbb702422c8be00d8b351e7a809973225dfd239c18fa0ace9a8689a51c98005a
  ac_hash: 6822ec15b26b319e9082e03e34f8e76b145bca0a56b89001aad038ab99f6920b
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T03:59:59Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/config/publish-surface.json:2 | `package.json` gained four `files[]` entries (NOTICE, CREDITS.md, docs/THIRD-PARTY-NOTICES.md, provenance/borrows.jsonl) and the GENERATED publish-surface artefact was not regenerated. Verified red: `./scripts-run src/scripts/check_publish_surface` exits non-zero with "publish surface drifted: what ships changed and the committed artefact did not". The gate names the fix (`--write` and commit the diff) and the diff does not contain it. | open | Run in-tree on branch head; roots[] in publish-surface.json still ends at llms.txt |
| 2 | high | src/scripts/_lib/source_shape.ts:288 | Recall hole in the narrowed matcher: the `after` guard `if (/^[/\\]/.test(after)) continue;` drops any `owner/repo` that is followed by a further path segment, and the `before` guard at :287 drops any preceded by `/` or `.`. Measured on the shipped module — `> **Source:** somevendor/some-suite/skills/foo.md`, `> **Source:** ported from somevendor/some-suite/README.md` and `> **Source:** ./somevendor/some-suite` ALL return `[]`. Citing a file inside an external repository is the most natural Source-header form and `tests/fixtures/source-headers/leaking.md` contains no such case, so the corpus cannot see the gap. The test file's own stated rollback trigger ("revert the matcher and restore the 243 baseline if a readable external identifier in a `**Source:**` fixture escapes detection") is met. | open | npx tsx probe over src/scripts/_lib/source_shape.ts; github.com/<o>/<r> is still caught by repoSlugHits, non-github slugs are not |
| 3 | high | src/scripts/cost/budget.mjs:5 | False attribution pointer in shipped source, duplicated at src/scripts/cost/track.mjs:5. Both headers state the upstream, its license and the transformation record are "in NOTICE, docs/THIRD-PARTY-NOTICES.md and provenance/borrows.jsonl". NOTICE was NOT touched by this change and names only a different project (openai/codex-plugin-cc, Apache-2.0); it says nothing about the cost-tracker upstream or its MIT terms. The file that DOES carry the reproduced MIT copyright and permission notice is CREDITS.md, which the comment does not name. The pointer is inverted on exactly the sentence that carries the license-discharge argument. | open | cat NOTICE on branch head vs CREDITS.md hunk of diff.patch |
| 4 | medium | src/scripts/_lib/source_shape.ts:255 | The domain branch runs BEFORE `isInternalToken` and no branch other than the bare-slug one applies it, contradicting the function's own docstring at :247 ("Internal tokens are excluded first: this repository's own paths ... name nothing external"). Any internal path whose file extension collides with a TLD in DOMAIN_RE is therefore reported as a leaked identifier. Measured: `> **Source:** agents/tooling/setup.sh` -> `setup.sh` (block tier), `src/scripts/install.sh` -> `install.sh`, `docs/design/logo.ai` -> `logo.ai`, `src/app/main.app` -> `main.app`. `.sh` is the sharp case; none of the three fixtures covers it. | open | npx tsx probe; DOMAIN_RE tld set at :224 includes sh, ai, app, io, dev, co |
| 5 | medium | src/scripts/_lib/source_shape.ts:258 | `ALLOWED_HOSTS` is matched by exact host only, with a suffix carve-out for `.example.com` alone, so documentation subdomains of vendors the rule explicitly permits are flagged as leaks. Measured: `docs.anthropic.com`, `platform.openai.com`, `docs.github.com`, `raw.githubusercontent.com` and `sub.example.org` all return a non-null identifier. The author demonstrably considered subdomains (the `.example.com` clause) and applied the carve-out to one host out of nine. | open | npx tsx probe over readableIdentifierIn |
| 6 | medium | src/scripts/_lib/source_shape.ts:264 | The `@scope/package` branch validates the npm SCOPE against `ALLOWED_OWNERS`, which is a GitHub-OWNER list whose own docstring calls it "a precision guard for a URL-host pattern". npm scopes and GitHub org names are different namespaces, so integrated vendors whose scope differs from their org are flagged: `@anthropic-ai/claude-code` and `@types/node` both return a hit, though source-confidentiality explicitly carves out naming an integrated tool. Reusing the list avoided a second allowlist at the cost of applying it to a namespace it was never measured against. | open | npx tsx probe; ALLOWED_OWNERS at src/scripts/_lib/source_shape.ts:133 |
| 7 | medium | package.json:59 | `provenance/borrows.jsonl` is newly published to npm. The internal borrow ledger carries the real source URL, source SHA and full transformation note of every borrow, present and future, and nothing gates what future rows it will publish. MIT discharge is already carried by the two surfaces added beside it (docs/THIRD-PARTY-NOTICES.md, generated FROM this ledger, and CREDITS.md, which reproduces the notice in full), so shipping the ledger itself buys no license coverage and permanently widens the published surface. | open | borrows.jsonl hunk of diff.patch; THIRD-PARTY-NOTICES note states the generated file is what discharges the obligation |
| 8 | medium | src/scripts/check_no_external_sources.ts:582 | The shadow metric that is the council's stated condition is emitted only on the human stdout path. It is absent from the `--json` payload (:545-557) and from the `--report` artefact (:505-535), so "keep the old detector shadow-reporting until at least one subsequent corpus-changing release passes both detectors" cannot be checked by anything mechanical, and no artefact records it across releases. The live run prints 107 while the audited delta is 95; the message asserts the difference is dedup but publishes no arithmetic that lets a reader reconstruct it, which is the exact reconstructability the comment at :437 says the shadow exists to preserve. | open | ./scripts-run src/scripts/check_no_external_sources on branch head |
| 9 | medium | src/scripts/_lib/source_shape.ts:342 | `legacySourceHeaderHits` re-implements the eight-line prefilter of `sourceHeaderHits` (:315-327) by verbatim copy rather than by extracting the shared `normalisedHeaderValue` step. The function is documented as "the PRE-narrowing predicate" whose purpose is auditability, but any future edit to the shared prefilters (a new ENC/redaction/opaque-id form) will silently change what the "pre-2026-08-30" baseline means, in one copy or in both. It also re-runs SOURCE_HEADER_RE and two string replaces for every line in agents/** on top of the live predicate. | open | src/scripts/_lib/source_shape.ts:315-356 |
| 10 | medium | src/config/gate-violation-baselines.json:96 | The narrowed `source-header` class now contributes ZERO of the 148 block findings — the baseline note itself states 148 = 127 tmp-quote + 21 repo-slug. In the tracked tree the class is therefore operationally indistinguishable from option (c), deleting it, which the note records both council seats as refusing. Its only remaining evidence of discrimination is the five-line leaking.md fixture, which the recall hole above shows is not representative. No mechanism will notice if the class silently stops firing. | open | ./scripts-run src/scripts/check_no_external_sources output on branch head |
| 11 | low | src/config/gate-violation-baselines.json:1 | The whole file was re-indented from four-space to two-space, rewriting 95 of its 98 lines to make one semantic edit (shape-block count 243 -> 148, landed, note). Sibling artefacts in src/config/ use both widths, so this is churn rather than convergence on a house style, and it forces a reviewer to reconstruct the single real change from a full-file replacement in a ratchet file where the numbers are the whole point. | open | difflib comparison of the removed and added hunk lines |
| 12 | low | package.json:56 | The four added `files[]` entries are indented with four spaces inside an array whose every other member uses eight, and they are inserted between LICENSE and MIGRATION.md, breaking the list's alphabetical run (CREDITS.md belongs before LICENSE, NOTICE after MIGRATION.md). | open | sed -n '45,70p' package.json |
| 13 | low | src/scripts/_lib/source_shape.ts:214 | Two of the twenty INTERNAL_PATH_PREFIXES entries are unreachable. `isInternalToken` is called only with a BARE_SLUG_RE capture, which always starts with `[A-Za-z]` and can contain neither a space nor `#`, so `'pr #'` and `'.agent-src'` can never prefix-match. The docstring at :206 says the set is listed rather than pattern-matched precisely so a reviewer can read it; two inert entries make a reviewer believe PR references and .agent-src paths are handled here when they are handled elsewhere or not at all. | open | src/scripts/_lib/source_shape.ts:211-215 and 216-219 read against BARE_SLUG_RE at :223 |
| 14 | low | agents/roadmaps/later/road-to-source-silence-cutover.md:436 | The risk register was not re-reviewed after this change closed the thing it relies on. Risk 3's mitigation is "one item — Phase 3 — has a hard date rather than a condition, so at least one thing here cannot be deferred silently"; step 3.1 is now `[x]` and Phase 3 is complete, so every remaining item is maintainer-condition-gated in a `later/` file that is excluded from the dashboard and from /roadmap:process-*. The register still carries `reviewed: 2026-08-29`, and `review_by: 2026-10-24` (:7) is one day before the 2026-10-25 expiry the same change wrote into the baseline note. | open | roadmap frontmatter :1-13, risk register :431-436, AC-4 :461-471 |
| 15 | low | src/scripts/_lib/source_shape.ts:252 | `readableIdentifierIn` allocates two Sets from module-level `readonly` constant arrays on every invocation. Both are invariant and hoistable to module scope beside the arrays they derive from, exactly as DEFAULT_ALLOWLIST already is at :198. | open | src/scripts/_lib/source_shape.ts:252-253 |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
