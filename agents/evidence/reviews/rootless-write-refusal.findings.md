# Findings: rootless-write-refusal
<!-- completion-review: v1 | reviewed: 2026-08-12 | scope: e0eaff52114087cfc49aa251548e285668229914c705a55e1e7b82a392c1b0ae | diff: ff8be31c5aea39a2b85874b1d1224f463a12b0d7 | reviewer: r2-fresh-subagent-rootless-write-refusal | prompt_hash: a310f3bcc1b04c777de48e485093eef3f37b82301de770e9f06f9d44cee63544 -->

<!-- context-manifest: v1
inputs:
  diff_sha: ff8be31c5aea39a2b85874b1d1224f463a12b0d7
  scope_hash: e0eaff52114087cfc49aa251548e285668229914c705a55e1e7b82a392c1b0ae
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-12T13:10:08Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_cli/cmd_session_recycle.ts:124 | The two halves of the fix resolve the workspace by different mechanisms, so the sanctioned escape can still write where the successor never reads. The command writes to `resolve_project_root(--project ?? cwd)`; the successor-side consumer is a session_start hook, and the hook convention for the same tree is `envelope.workspace_root ?? process.cwd()` (`session_eol_hook.ts:236`). `--project` is documented as the escape for exactly the case where cwd is not the repo — i.e. exactly when the two roots can diverge — yet nothing in the diff ties the flag to the host session's `workspace_root`, and no test exercises the pair. | fixed | ff8be31c5 — the coupling is now stated and pinned, not unified: the refusal message and `--help` say the path must be the workspace root of the SESSION that resumes, and a new case writes through the command and reads through `consume_recycle_envelope` so the pair breaks loudly if either side moves. Deliberately NOT fixed: the two sides still resolve by different mechanisms, which is a wider change than this branch's subject. Failure: operator invokes `session:recycle --project /repo` from an anchorless dir (the reported consumer shape). Exit 0, absolute path printed, resume instruction advises `/clear`. The successor session, whose `workspace_root` is still the anchorless dir, finds no envelope at its own root and resumes from nothing — the pre-fix outcome, reached through the new escape. Partial mitigation: the new counter-check would fire once on a later Stop, but only if the session continues past another Stop. |
| 2 | medium | src/scripts/hooks/session_eol_hook.ts:142 | `envelopeExists` states "every failure reads as exists, not missing", but `fs.existsSync` does not throw on stat/permission errors — it returns `false`. The `catch { return true; }` fail-safe is unreachable for the failure class it names, so the invariant is inverted, not enforced. | fixed | ff8be31c5 — reads the file and treats only `ENOENT` as absence; every other errno reads as present, which is the invariant the comment claimed and `existsSync` could not deliver. Failure: `agents/runtime/state/` unreadable (EACCES on a mounted or root-owned tree) while the envelope is present. `existsSync` returns `false`, `shouldWarnMissing` is true, and the hook emits "no envelope exists at ... /clear now starts the successor from nothing" about an envelope that is there — the exact manufactured warning the comment and roadmap Phase 2 step 3 forbid. Dead-code half: the catch can only fire on an invalid-argument throw, which `path.join` would raise first. |
| 3 | medium | src/scripts/_cli/cmd_session_recycle.ts:99 | `--project` is accepted with no validation of any kind — not existence, not anchor. The new guard fires only on `ORIGIN_CWD_FALLBACK`, and an explicit path yields `ORIGIN_EXPLICIT` (roadmap Phase 1 step 2), so the flag bypasses the guard unconditionally. `atomic_write_json` creates the tree, so a wrong value succeeds silently. | fixed | ff8be31c5 — with a correction to the finding: `resolve_project_root` DOES validate an explicit path (`_validate_root_path`, existence + directory, `agent_settings.ts:711`), so the typo case never wrote silently. It threw, uncaught, out of a command whose whole subject is legible failure — which is the real defect and is now caught and reported at exit 1, with a test. The anchor half stands and is deliberate: an explicit path is explicit, and requiring an anchor there would break the reported consumer shape. Failure: `session:recycle --project /repoo` (typo, stale shell alias, or a path that is not a project) creates `/repoo/agents/runtime/state/recycle-envelope.json`, exits 0, and prints the resume instruction whose next step is `/clear`. Detectability rests entirely on the operator re-reading the printed path. No test covers a nonexistent or unanchored `--project` value; `session_recycle.test.ts:147` only covers a `--project` that is already anchored. |
| 4 | medium | src/scripts/hooks/session_eol_hook.ts:277 | `shouldWarnMissing` does not consult `threshold`, so the advisory lane's documented emergency off-switch no longer switches the whole lane off. The docstring at :56-61 still claims "`0` or any non-positive value disables the advisory lane while recording continues"; after this change one more warn from that lane can still be emitted. | fixed | ff8be31c5 — `shouldWarnMissing` now requires `threshold !== null`, so the emergency switch silences both warn paths; a test drives the exact interaction (advisory fires under a live threshold, switch goes in, next Stop is silent). Failure: session crosses the threshold, advisory fires and stamps `advisory_fired_at`; operator sets `AGENT_RECYCLE_THRESHOLD_TOKENS=0` to silence the lane; the next Stop still returns `EXIT_WARN` with the counter-check line. Untested: the emergency-off case (`session_eol_hook.test.ts:245`) starts at threshold 0, so `advisory_fired_at` is never set and the interaction is never exercised. |
| 5 | medium | src/scripts/hooks/session_eol_hook.ts:280 | The counter-check tests envelope *existence*, never freshness — no comparison against `advisory_fired_at`, the session key, or the envelope's own `written_at` / `workspace`. A leftover envelope silences the check in the case it exists to catch. | fixed | ff8be31c5 — `envelopeExists` takes the advisory stamp and compares it against the envelope's own `written_at`; an envelope predating the advisory is another session's and no longer silences the check. An unparseable or undated envelope still counts as present — warning about a file that is visibly there is the worse error. Failure: session A writes an envelope; the operator never clears, so the successor-side hook never consumes it (the command's own doc: "consumed — moved, not copied" at session_start). Session B in the same workspace crosses the threshold, the advisory fires, the operator forgets to recycle. A's envelope still exists, so `envelopeExists` is true and no warning is emitted; `/clear` then resumes the successor from session A's stale state — a wrong resume rather than an empty one. |
| 6 | low | tests/scripts/session_eol_hook.test.ts:202 | Three pre-existing advisory-lane tests were relocated into the new `describe('missing-envelope counter-check')` block. The advisory describe now closes at :200, leaving "advisory lane is disabled by the 0-override" (:245), "the committed constant is the threshold" (:256) and "keeps the advisory to one line under the injection budget" (:260) reported under the counter-check suite they do not test. | fixed | ff8be31c5 — the three relocated guards now sit in their own `advisory lane configuration (Phase 3.2)` block instead of under a suite that does not test them. Failure: a later change that skips, renames, or removes the counter-check block silently drops three unrelated advisory-lane guards, and a `--reporter` scan attributes advisory regressions to the wrong suite. Mechanical: the added `});` closes the advisory describe one test early. |
| 7 | low | src/scripts/hooks/session_eol_hook.ts:160 | `buildMissingEnvelopeLine` has no single-line or byte-budget test, unlike its sibling `buildAdvisoryLine`, which is pinned at `not.toContain('\n')` and `< 512` bytes (`session_eol_hook.test.ts:260`). The new line is longer in fixed prose and embeds an unbounded absolute workspace path. | fixed | ff8be31c5 — pinned at the sibling's budget (single line, < 512 bytes) with a 200-char workspace root, which is the case the finding names. Failure: fixed prose is roughly 205 chars, leaving about 300 for the path. A deeply nested workspace root (worktree paths in this repo already run past 120 chars, and nested worktrees compound) pushes the injected line past the budget the sibling line is gated on, with no test to catch it. |
| 8 | low | tests/scripts/session_recycle.test.ts:45 | The two scratch-root helpers added or touched in this diff make opposite environment assumptions. `scratch()` here is not `realpathSync`-normalized, while the sibling `scratchRoot()` in `recycle_roundtrip.test.ts:667` is — and the new case at :164 makes an exact absolute-path string assertion against the un-normalized value. `rootlessScratch()` (:52) additionally assumes no anchor exists above `os.tmpdir()`. | fixed | ff8be31c5 — both helpers are `realpathSync`-normalized, matching the sibling. The tmpdir-has-no-anchor assumption stands and is the finding's own reading: it fails loudly rather than silently. Failure: on a platform where the resolver normalizes symlinks (macOS `/var` to `/private/var`) the printed target carries the realpath while the expectation carries the mkdtemp path, and `toContain` fails. Not reproduced — flagged as an inconsistency between two helpers in the same change, with the sibling's `realpathSync` as the evidence that the discrepancy matters for this resolver. A CI runner whose `TMPDIR` sits inside an anchored tree inverts the refusal cases at :125 and :138 (loud failure, not silent). |
| 9 | low | agents/roadmaps-progress.md:5 | The regenerated dashboard hunk carries deltas this branch does not produce: open roadmaps 30 to 31, blockers 42 to 44, steps 391 to 396, and a new row 23 for a roadmap this diff does not add. This branch's own roadmap is added already-archived and therefore contributes nothing to any of these counts. | accepted-risk | Accepted by the implementing session, and the deltas are real rather than spurious: the branch merged `origin/main` twice mid-run, and the dashboard is a generated view of the WHOLE tree, so it must carry the tree's counts or it is wrong. Regenerated after the last merge and re-verified as a no-op diff, which is the only check available — a hunk restricted to this branch's own contribution would be a hand-edited generated file. Failure: a reviewer cannot attribute the counts to the change under review, and the generated file becomes a clean-auto-merge surface against whichever branch actually owns `road-to-skill-ecosystem-executable-payloads.md` — an auto-merge that resolves without conflict and still records the wrong totals. |
| 10 | low | src/scripts/hooks/session_eol_hook.ts:13 | Module-header doc drift: the header still describes the hook as injecting exactly "ONE line" past threshold (Phase 3.2) and does not mention the counter-check at all, although the hook now has a second independent warn path returning `EXIT_WARN` (:309). | fixed | ff8be31c5 — the header now describes both warn paths, names `missing_envelope_warned_at`, states the shared threshold gate, and says the slot budget from this hook is two lines, not one. Failure: a reader auditing the Stop slot's injection budget from the header concludes at most one line per session originates here and misses the second emitter; the same header is the file's only description of which state fields exist, and `missing_envelope_warned_at` is absent from it. |
