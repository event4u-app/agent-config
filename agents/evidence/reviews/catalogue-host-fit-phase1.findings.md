# Findings: catalogue-host-fit-phase1
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: 341e1312d15903ffd7ec76ddd028208c861293ff1b449aa086c62931d2953574 | diff: 7e9b5086ee55407f53d9f842bbf754f3b036bd2f | reviewer: r2-fresh-subagent-catalogue-host-fit-phase1 | prompt_hash: b5c4b34da68380d7fca95b673dd0c700fd51d4c9601501c040274d50475d4adb -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 7e9b5086ee55407f53d9f842bbf754f3b036bd2f
  scope_hash: 341e1312d15903ffd7ec76ddd028208c861293ff1b449aa086c62931d2953574
  roadmap: agents/roadmaps/road-to-catalogue-host-fit.md
  roadmap_hash: 72efcb83e01802a56c813c8dd36f26f29cbd73cb61be15d062564cf29a768d9e
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T00:41:29Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/capture_skill_catalogue.ts:277-287 | `--pointable-bare` guards only `rankerRoot === null`, never an empty catalogue — but `resolveSkillsRoot` returns the first *existing directory* (`.claude/skills` before `src/skills`), so a present-but-empty or partially-generated projection yields `rankable = new Set([])`, every row scores `pointableBare: 0`, and the report prints "D-4 divergence: 0 on every joined observation — the ranker points at nothing the host degraded." That is a zero inferred from an unread catalogue. The error text at :282 already asserts the guard exists ("An empty catalogue is never a clean join: it would report 0 pointable entries because nothing was read"), the module header forbids exactly this ("A ZERO IS NEVER INFERRED FROM SILENCE"), and the sibling path at :532 does guard it (`projected.length === 0` → exit 1). Add the same `catalogueNames.length === 0` → exit 1 check. | fixed | 87ec63a15 |
| 2 | medium | src/scripts/capture_skill_catalogue.ts:202,240,247,360 | `projectionModeFlagFor` is called once per due host inside the loop at :202, and each call runs `projectionModeCounts()` → `scoped_projection_stats(REPO, iter_skills())`, which walks every skill and parses its frontmatter (`is_pruned_under_scoped` → `frontmatter_packs(md_path)`, ~300 file reads). With two due hosts the tree is walked twice for a value that cannot differ between hosts. The refactor's own docstring at :359 says "The package's two projection-mode counts, **one walk**. Shared by both modes." — the call site contradicts it. Hoist one `projectionModeCounts()` above the loop and pass it in. | fixed | 87ec63a15 |
| 3 | medium | src/scripts/capture_skill_catalogue.ts:203-212 | The host-event branch interpolates `${row.host}` into `--host` and `--host-root ~/.${row.host}` but hardcodes the literal `codex exec …` pipeline. Any host other than codex whose headline record reads `host-event` gets a command that runs **codex** and records codex's truncation under that other host's name — a cross-host mislabelling in a module built around "no host's count is ever used to say anything about another's". The branch is also selected from `row.source`, which `cadenceStatus` takes from the single headline record; `_supersedes` breaks a same-date tie on the larger `dropped_count`, and a self-report record has none (→ 0), so one same-date host-event record flips a self-report host onto the codex pipeline. Key the command off the host, not off the latest record's source. | fixed | 87ec63a15 |
| 4 | medium | src/scripts/_lib/skill_catalogue.ts:1383-1388 | `formatPointableBare`'s headline is `rows.reduce(Math.max)` over **all** rows — every host and every date pooled. This is the one thing this module's own machinery exists to prevent: `_supersedes` (:869) was introduced because two reducers over one log broke a tie in opposite directions and printed two drop counts for one host on one day, and `formatPerHostVerdicts` (:1059) refuses pooling on the stated grounds that two hosts truncate by different mechanisms. Here a superseded 2026-01 observation can supply the "up to N skill(s)" headline while the latest observation is 0, with no date or host attached to N. Quote the headline per host off `headlineRecordPerHost`, or state in the line that it is a max across the whole corpus. | fixed | 87ec63a15 |
| 5 | medium | src/scripts/_lib/skill_catalogue.ts:1328-1329 | `joinPointableBare` dereferences `record.bare_names.filter(...)` on records that `readObservationLog` (:1037-1050) produced by an unchecked `JSON.parse(trimmed) as ObservationRecord` — no shape validation at all. One appended line missing `bare_names` (or carrying `null`) throws `TypeError` and takes down the whole mode. The sibling reducer `knownHostLimits` (:896) defends against exactly this with `typeof record.dropped_count !== 'number'`, so the new join breaks a defensive precedent set in the same file over an append-only log with two producers. Guard with `Array.isArray(record.bare_names)` and skip (or fail loudly with the line number), rather than crashing. | fixed | 87ec63a15 |
| 6 | medium | src/scripts/capture_skill_catalogue.ts:277 | Two catalogue resolvers now coexist in one file: `resolveCatalogueRoot` (:110, honours `--catalogue-root`, throws on miss) for the main path, and the imported `resolveSkillsRoot` (:277, ignores `--catalogue-root`, returns `null`) for `--pointable-bare`. Passing `--catalogue-root` to `--pointable-bare` is silently ignored, so one CLI can report on one tree and join against another in the same invocation. `resolveSkillsRoot`'s own docstring names this hazard verbatim: "two resolvers over one catalogue is how a ranker and the tool that exposes it start ranking different trees". Either honour the flag or reject it explicitly in this mode. | fixed | 87ec63a15 |
| 7 | medium | agents/roadmaps/road-to-catalogue-host-fit.md:91,169-183 | Step 1.1 is flipped to `[x]` while its own landed-note states part (c): "The verify clause is met on host and on pooling, and **NOT on scope**". The step's `verify:` clause has three named conditions (two consecutive captures land **with their scope** and host recorded, verdicts unpooled) and one is self-declared unmet. The `<!-- verify: npx vitest run tests/scripts/catalogue_capture.test.ts -->` annotation points at a unit test that cannot exercise scope recording end-to-end either. Under the roadmap glyph contract this reads as `[~]`-shaped work marked done; if the scope half is genuinely out of reach on any machine, the verify clause should be amended rather than left standing and unmet. | fixed | 89a5c50db — clause amended to require the scope wherever the install's scope is determinable, with the amendment and its reason recorded in the step. The clause was unsatisfiable by construction, not by effort. |
| 8 | medium | src/scripts/capture_skill_catalogue.ts:240-265 | `projectionModeFlagFor`'s two omit-with-a-reason branches — root absent, and `row.matches === 'indeterminate'` — are the correction the roadmap's part (c) treats as load-bearing (it replaced a `<scoped\|legacy-all>` placeholder that "invites the operator to pick"). Neither branch has a test: the function is module-private and unexported, and `tests/scripts/catalogue_capture.test.ts` covers `cadenceStatus`/`formatCadenceStatus`/`joinPointableBare`/`formatPointableBare`/`buildObservationRecord` only. Nothing pins the decision that regressing to a placeholder would break. Extract the branch to an exported pure function over `(installedSkills, counts, rootExists)` and fixture both directions. | fixed | 87ec63a15 |
| 9 | low | src/scripts/capture_skill_catalogue.ts:198-200 | Empty-corpus path prints a header with no body: the early return is `due.length === 0 && rows.length > 0`, so with zero records `rows` is `[]`, `due` is `[]`, and the run prints `formatCadenceStatus`'s "no observations recorded yet — every host is due." followed by "next round — run what is due:" and then nothing. The one state in which the operator has no command is the state the tool declares maximally overdue. The mode's docstring promises "prints the next command"; here it cannot, because `cadenceStatus` only knows hosts it has already seen (its own docstring says so). Print the bootstrap commands for the known host set, or say plainly that no host is known yet. | fixed | 87ec63a15 |
| 10 | low | src/scripts/capture_skill_catalogue.ts:161-170 | `argToday` validates shape, not date validity: `--today 2026-13-45` passes `^\d{4}-\d{2}-\d{2}$`, then `Date.parse('2026-13-45T00:00:00Z')` is `NaN`, `wholeDaysBetween` returns `NaN`, and every host reads DUE with the message `stamp unparseable (${row.lastObservedAt})` (skill_catalogue.ts:1260) — which blames the *record's* stamp for a bad `--today`. A misleading diagnostic pointing at the corpus for an operator typo. Range-check the parsed date (round-trip `toISOString().slice(0,10)`) in `argToday`. | fixed | 87ec63a15 |
| 11 | low | src/scripts/capture_skill_catalogue.ts:189-192 | `--cadence --json` serialises `CadenceRow` straight through, and `JSON.stringify(NaN)` emits `null`. A machine consumer therefore cannot distinguish "stamp unparseable" from "field absent" — the absence/zero conflation this module refuses everywhere else (`truncation_mode` absent, `projection_mode` absent and `dropped_count` absent all carry explicitly stated distinct meanings). Emit an explicit sentinel or a `daysSinceKnown: boolean` alongside it. | fixed | 87ec63a15 |
| 12 | low | src/scripts/capture_skill_catalogue.ts:287 | The join's disk side carries no scope or freshness discipline while the host side is built entirely around it. `resolveSkillsRoot` picks `.claude/skills` first, which in this checkout holds 338 entries against the roadmap's stated scoped 219 / legacy-all 290 — i.e. the joined catalogue matches neither projection mode, the same `indeterminate` condition `projectionModeFlagFor` refuses to label three functions earlier. The output prints the root and raw entry count, so it is traceable, but `pointableBare` grows with a stale-large root and the row carries no record of which projection it was joined against. Record the resolved root's mode classification in the `--json` payload. | fixed | 87ec63a15 |
| 13 | low | agents/roadmaps-progress.md:37,46,79-98,125 | The regeneration folds in blocker-section churn for two roadmaps this branch does not touch — `road-to-gate-autonomy` (blocker count 2→3, new `b-estate-prose-pass-from-1-3`) and `road-to-mixed-trigger-activation-cost` (1→2, new `b-matrix-semantics-amendment`) — and drops both sections' `_1 blocker resolved._` footers. Neither source roadmap appears in the diff, so either the base dashboard was stale (benign, but unstated) or the regeneration read a tree state this commit does not contain. Per `minimal-safe-diff` this is unrelated drift riding in a Phase-1 change; state which of the two it is in the commit message, so a later bisect does not attribute those blockers here. | accepted-risk | It is the FIRST of the two, verified rather than assumed: at `origin/main` those roadmap files already carry 3 and 2 `### blocker:` sections while the committed dashboard says 2 and 1, so the base dashboard was stale and this regeneration corrected it. Nothing in the branch produced those blockers. Stated here and in the PR body so a bisect does not attribute them to this change; the correction itself is kept, since reverting it would re-ship a dashboard known to disagree with the tree. |
| 14 | low | agents/evidence/reviews/catalogue-host-fit-phase1.review-input/acceptance-criteria.md:1 | The AC input handed to this review is 0 bytes, and the manifest's `ac_hash` is `e3b0c442…b7852b855` — the SHA-256 of the empty string — while `roadmap.md` plainly carries `**AC-0:**` through `**AC-3:**`. The extractor produced an empty file and the pipeline hashed it without complaint, so this review could not check the diff against its acceptance criteria and the artefact records that silently. Out of the diff's scope, reported because it degraded this review: the extractor should fail loudly on a roadmap that contains `AC-` lines but yields none. | deferred | `agents/roadmaps/later/road-to-plan-gates-measurement.md` § Defect to fix BEFORE the enforced flip — filed there because the defect is in `dispatch_r2_reviewer`, not in this diff, and that roadmap owns the R2 advisory window this corrupts. An enforced gate whose AC input can be empty is worse than an advisory one. |


## Acceptance-criteria binding — this review was AC-BLIND

Recorded 2026-08-18, after the fact.

The manifest above carries `ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`,
which is the SHA-256 of the **empty string** rather than of any criteria. This
review was dispatched against `agents/roadmaps/road-to-catalogue-host-fit.md`, <!-- ref-ignore -->
which declares its criteria as inline `- **AC-n:**` bullets per phase and
carries no `## Acceptance Criteria` heading at all, while the extractor of the
day matched the heading form and nothing else. The reviewer therefore received a **0-byte** `acceptance-criteria.md`
under a prompt stating that the acceptance criteria had been extracted for it.

**So the findings above review the DIFF, and nothing in them was checked against
the acceptance criteria** — the criteria were not in the reviewer context at all.
The same roadmap yields 19 lines of criteria under the extractor as it stands
today. The extractor learned the inline form in PR #1419 (2026-08-18).

The manifest is deliberately left unchanged. `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
is the honest record of the input this review actually received, and rewriting it
to today's value would assert a binding that never existed — the precise failure
this note exists to prevent. The artefact binds a scope that no longer exists, so
no gate re-derives it and nothing here is stale.
