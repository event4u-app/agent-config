# Findings: drain-roadmap-6-one-resolver-r5
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: 6583e109bdd6fa6ede9f689ef72597544636b098b62db82cdd6a94c5407535d5 | diff: 4030de2b13a5fe01fe7228f5d6b23c4936643783 | reviewer: r2-fresh-subagent-drain-roadmap-6-one-resolver-r5 | prompt_hash: 791c53e5260d99fba3c81efce18f3352d5b33ea6e8738a42961790f7631ebb17 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-roadmap-6-one-resolver-r5"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 4030de2b13a5fe01fe7228f5d6b23c4936643783
  scope_hash: 6583e109bdd6fa6ede9f689ef72597544636b098b62db82cdd6a94c5407535d5
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: 3c36213cb1d9b8a821accc9ae2325e95bcb5c3024d9bb881f2b3e590edae393c
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T13:09:29Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/one_resolver_invariant.ts:113 | **BLOCKING.** `parse()` hard-codes the file name `'probe.ts'` and `ts.ScriptKind.TS` for every file, while `SCANNED_EXTS` at :83 now admits `.tsx`. A `.tsx` module is therefore parsed as non-JSX TypeScript, so JSX text is tokenized as ordinary TS — which resurrects **the exact two defect classes that killed rounds 2 and 3**, inside the repair that closed round-4 finding 2. Measured end-to-end on synthetic trees (baseline clean and non-empty first, control arms RED): `src/ui/Nav.tsx` = `export const Nav = () => <p>files live in src/*/ui</p>;` followed by a literal `export class CouncilTopologyRouter {}` returns **zero violations** while `Nav.tsx` IS listed in `scanned` — the `/*` in the JSX text opens a block comment that swallows the router (round 2's defect). `src/ui/Hint.tsx` with `<p>press ` + backtick + ` to open</p>` above the same router likewise returns **zero violations** — the backtick opens a template literal that does not end at a newline (round 3's defect, the one that cost 54 files and 231 exports). The identical body in `src/ui/Nav.ts`, and the same router alone in `src/ui/Router.tsx`, both go RED, so the discriminator is the JSX, not the extension. This is INSIDE the frozen claim: the claim asserts "no module outside the sanctioned resolver EXPORTS A BINDING WHOSE NAME matches a router pattern, in any export form the TypeScript parser recognises" and `.tsx` modules are counted in `scanned`, so they are claimed as covered; the module docstring at :41-46 states the class is "gone rather than relocated", which is now false for `.tsx`. Live-tree severity, measured: all **28** non-test `.tsx` files under `src/` parse differently as `.ts` — 28 of 28 acquire parse diagnostics (0 to 893 on `src/ui/pages/WizardPage.tsx`; 0 to 386 on `SettingsChangesPage.tsx`) and statement counts shift wildly (e.g. `WorkspacePage.tsx` 66 to 174), so the whole `.tsx` half of the scan runs over resynced garbage. No live `.tsx` file loses an exported NAME today (measured with the module's own `exportedNames` against a ScriptKind.TSX variant: 0 files, 0 names), which is why this is a hole and not a current miss — the fourth consecutive round in which a blind spot hides behind a non-empty `scanned`. Root cause is one design gap the round-4 repair did not carry: the extension list was widened, the parser configuration was not. **The shipped test cannot see it by construction** — `tests/scripts/one_resolver_invariant.test.ts:339` writes `src/ui/Router.tsx` containing a bare `export class CouncilTopologyRouter {}` and no JSX at all, which is the round-4 reproducer's literal spelling rather than the violated property, i.e. the second principle both council seats asked to be recorded. Repair is bounded and NOT a new model: parse with `ScriptKind.TSX` (file name `probe.tsx`) unconditionally — measured blast radius across every file the walker admits, all 1324 of them, is **0 files and 0 exported names lost**, because `<T>expr` type assertions do not occur in this tree. Alternatively thread the extension into `parse()`. Either way it is one expression, well inside the council's stopping rule, so this is repairable rather than an owner exit. | fixed | probe: 4 synthetic trees (2 GREEN, 2 RED controls) + 28-file live parse comparison + 1324-file remedy blast radius; test at test:339 contains no JSX — fixed in 47f02fa6c |
| 2 | medium | src/scripts/_lib/one_resolver_invariant.ts:165 | NON-BLOCKING. `collect` walks a namespace body only when `st.body` is a `ModuleBlock`. A **dotted** namespace name produces a nested `ModuleDeclaration` as the body instead, so the walk is skipped by the `continue` at :169. Measured: `export namespace Dispatch.Inner { export class CouncilTopologyRouter {} }` returns `exportedNames` = `["Dispatch"]` and `checkOneResolver` GREEN, while the one-level form `export namespace Dispatch { ... }` (pinned at test:355) goes RED. Same for `export namespace A.B { ... }`. This is round-4 finding 3's defect surviving one syntax to the side of its own repair. Non-blocking because the router is reachable only as a qualified name, which the frozen claim's stated symbol-resolution limit arguably already excludes — but that reading is now inconsistent with the guard's own behaviour, since the round-4 repair deliberately pulled the **block** form INSIDE the claim on exactly the same qualified-name grounds. The honest statement for a future reader: the claim boundary at the namespace axis is currently drawn by a parser-shape accident (ModuleBlock vs nested ModuleDeclaration), not by any stated rule, and nothing in the module or the roadmap's "What this step does NOT close" says so. Disposition options are (a) recurse on a nested `ModuleDeclaration` body, one added branch, or (b) state in the frozen claim that namespace members are covered only for the single-identifier form. No live instance: zero namespaces of either form exist under `src/`. | accepted-risk | probe: dotted form GREEN, single-identifier control RED, same tree and same router body — DISPOSITION: recorded as a disclosed limit in the module frozen-claim block, deliberately not repaired; the review loop was terminated on the blocking finding only |
| 3 | medium | src/scripts/_lib/one_resolver_invariant.ts:238 | NON-BLOCKING. The round-4 namespace repair was applied to `collect` (which backs `exportedNames` / `declaresRouter`) and **not** to `exportsRouterFunction`, which still visits only `sf.statements` and never descends into a `ModuleBlock`. That reintroduces round-4 finding 1's exact defect on a new axis: one syntax counts as "declares a router" for every other file and as "is not a resolver" for the sanctioned one. Measured on the identical source `export namespace L { export function classifyLadder(x: unknown) { return x; } }` — `declaresRouter` = **true**, `exportsRouterFunction` = **false**. Consequence: if `judgment_ladder.ts` were ever wrapped in a namespace (a behaviour-preserving refactor), the guard emits `resolver-is-not-a-resolver` with the diagnostic at :359-366 asserting the resolver "was RENAMED rather than gutted" while `classifyLadder` sits inside the block — a false POSITIVE on conforming code, which is the direction round 4 named as "a gate that reds on a legitimate refactor is a gate that gets bypassed". A second, unrelated false positive in the same function, verified: `export const classifyLadder = memoize((x) => x)` is REJECTED because a `CallExpression` initializer is not in the callable set — `memoize`/`once`/`withTelemetry` wrapping is idiomatic and would red the gate on a genuine resolver. The second half is arguably the declared "declaration KIND" design; the first half is not declared anywhere. Non-blocking: both are false positives, so neither opens a hole, and no live instance exists (the real `judgment_ladder.ts` passes — test:218 green). | accepted-risk | probe: declaresRouter=true / exportsRouterFunction=false on one source; factory-initialised resolver REJECTED — DISPOSITION: recorded as a disclosed limit in the module frozen-claim block, deliberately not repaired; the review loop was terminated on the blocking finding only |
| 4 | low | src/scripts/_lib/one_resolver_invariant.ts:239 | NON-BLOCKING. `localCallable` maps a `FunctionDeclaration` to `true` on the strength of the declaration kind alone, and an **ambient** declaration is a `FunctionDeclaration` with no body. Measured: `export declare function classifyLadder(x: unknown): unknown;` returns `exportsRouterFunction` = **true**, so a `judgment_ladder.ts` gutted to that single line scans GREEN even though `declare` emits nothing and the module exports no runtime binding at all. This is the round-2 / round-3 "gutted resolver" class one keyword to the side of its repair: `export const classifyLadder = "moved"` is correctly REFUSED (test:333) and `export declare function classifyLadder(...)` is not. Weakest-form finding of the round and genuinely close to the declared limit — the docstring at :200-203 already says "a function that merely `return`s a constant satisfies this" — but an ambient declaration is a step further than that: there is no function. A one-line discriminator exists (`st.body !== undefined`, or reject a `DeclareKeyword` modifier). Recorded so the asymmetry with the string-stub refusal is a decision rather than an accident. | accepted-risk | probe: exportsRouterFunction=true on the ambient form, false on the string stub — DISPOSITION: recorded as a disclosed limit in the module frozen-claim block, deliberately not repaired; the review loop was terminated on the blocking finding only |
| 5 | low | src/scripts/_lib/one_resolver_invariant.ts:165 | NON-BLOCKING, false-positive direction of the same round-4 namespace repair. The body walk at :165 carries no export-modifier guard on the enclosing declaration, so members of a namespace the module does not export are counted as module exports. Measured, both reporting `second-resolver`: `namespace Internal { export class CouncilTopologyRouter {} }` plus an unrelated `export const used = 1` — nothing named `CouncilTopologyRouter` leaves the module; and `declare module "vendor-x" { export class CouncilTopologyRouter {} }` — an ambient declaration DESCRIBING a third party's API, which declares nothing in this tree at all. Both contradict the frozen claim's own wording ("no module ... EXPORTS A BINDING WHOSE NAME matches"). Round-4 finding 3 did name the non-exported-namespace case and treated detection as desired, so this may be intentional; if so it is undocumented, and the ambient-third-party case was not considered. Practically harmless today (no namespaces and no ambient module declarations exist under `src/`), and it fails in the safe direction. Recorded because a `.d.ts`-style ambient declaration is the plausible future trigger and it would red the gate on code that holds the invariant. | accepted-risk | probe: both forms RED with the enclosing form non-exported / ambient — DISPOSITION: recorded as a disclosed limit in the module frozen-claim block, deliberately not repaired; the review loop was terminated on the blocking finding only |
| 6 | low | src/scripts/_lib/one_resolver_invariant.ts:325 | NON-BLOCKING. Four discovery and robustness behaviours of the walk, none disclosed at the claim level, measured on synthetic trees. (a) **Symlinked directory is invisible**: `entry.isDirectory()` is false for a symlink, so a directory symlinked into `src/` is never walked — a router inside it returns GREEN and the file does not appear in `scanned` (a symlinked FILE is handled correctly and goes RED, so the two halves disagree). (b) **UTF-16 file scans clean while counting as scanned**: `fs.readFileSync(abs, 'utf-8')` at :381 turns a UTF-16LE module into mojibake, the parser finds nothing, and the path is nevertheless pushed to `scanned` — a fifth instance of a blind spot hiding behind the anti-vacuity discriminator, and the only one on this list that is silent-green rather than loud. (c) **Unreadable file crashes the scan**: a mode-000 file under `src/` throws a raw `EACCES` out of `checkOneResolver` with no diagnostic. (d) **Dangling symlink crashes it too**: a broken symlink is pushed by `walk` (not a directory, ends in `.ts`) and then throws `ENOENT` at :381 — including when the dangling link IS `judgment_ladder.ts`, where `fs.existsSync` correctly returns false and would have produced the intended `resolver-missing`, but the walk reaches the read first. (c) and (d) fail LOUD and are acceptable as-is; (a) and (b) fail silent-green and are the ones worth a stated limit or a `readdir` / encoding guard. Binary content and syntactically broken source were also probed and behave correctly (no throw; a broken file still yields its exports). | accepted-risk | probe: 8 robustness arms on synthetic trees — 3 GREEN, 3 RED, 2 THROW — DISPOSITION: recorded as a disclosed limit in the module frozen-claim block, deliberately not repaired; the review loop was terminated on the blocking finding only |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
