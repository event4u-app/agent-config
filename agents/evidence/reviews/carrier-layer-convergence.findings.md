# Findings: carrier-layer-convergence
<!-- completion-review: v1 | reviewed: 2026-08-10 | scope: f494e78b7c8a43d2b3cd000302df6f77a45a2e68031478f509732f6ea75df618 | diff: 195523465db8fabb84be3f4ad27f8293cf5e4ac5 | reviewer: r2-fresh-subagent-carrier-layer-convergence | prompt_hash: d6db1729b27a90432716265ac5c5bf386459555e6322eaeed16000983fe231f8 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 195523465db8fabb84be3f4ad27f8293cf5e4ac5
  scope_hash: f494e78b7c8a43d2b3cd000302df6f77a45a2e68031478f509732f6ea75df618
  roadmap: agents/roadmaps/road-to-carrier-layer-convergence.md
  roadmap_hash: b47ba58a310005fb4b55e42c6658a7c8811adedaae46ffe9c93880a380a0107c
  ac_hash: d81df67982028afbaab2d9e1a238d4a19a3f72ac9a8c8dc375c6c4941ced7077
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-10T15:34:05Z
-->

<!-- RE-BOUND IN PLACE 2026-08-10 (contract 2.7): the fix pass changed the review
scope, so `scope:`, `roadmap_hash` and `ac_hash` now bind the shipping content.
`diff:` / `diff_sha` deliberately still name 195523465db8fabb84be3f4ad27f8293cf5e4ac5,
the head the reviewer actually read: that field is provenance and never compared,
and moving it would misrepresent when the review ran. `reviewer:` and `prompt_hash`
are unchanged for the same reason — they identify who produced these rows and with
which prompt. The review-input/ package is likewise left as the reviewer read it.
No second dispatch: this is the same round, re-bound (2.7).
The round ran on branch feat/carrier-layer-convergence; PR #1250 merged before the
fixes landed, so the round and its repairs ship here instead. -->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/report_carrier_divergence.ts:301 | The new `frontmatter-only` class is declared "nothing to act on" unconditionally, but the very next two lines state the host DOES read `paths` and that `generate-tools` emits `paths` while `install.ts` writes agent-config's vocabulary instead. If the two writers disagree on `paths` for a path-scoped rule, the frontmatter difference changes WHEN the host loads that rule — project copy conditional, global copy always-on — which is a delivery difference a reader must act on, not metadata the host discards. The change never checks whether `install.ts` emits `paths`, yet the classification doc sizes the path-scoped subset at 25 of 110 project files. Same unconditional claim repeated at report_carrier_divergence.ts:54-56 and report_conformance_funnel.ts:163-164, and it is load-bearing for the roadmap's "no correctness hole" conclusion. | fixed | 318fb818e — `frontmatterPaths` + `pathsScopeDiff` break the 24 `paths:` disagreements out of `frontmatterOnly`; both report surfaces print them as ACT ON THESE with the rules named, and the clean-prose line refuses an all-clear while they are open. Verified against the tree first: 24 project-side `paths:`, 0 global-side of 114. Evidence doc, roadmap correction block, acceptance criteria and the Phase 3 layer-choice note updated. |
| 2 | medium | src/scripts/_lib/carrier_divergence.ts:128 | `proseEqual` compares `stripFrontmatter(a).trim() === stripFrontmatter(b).trim()`, so the predicate proves "prose equal after edge-trim", not "differs only in frontmatter". A pair with no frontmatter on either side that differs only in leading/trailing whitespace — two independent writers is exactly where a trailing-newline difference arises — lands in `frontmatterOnly` and is printed as "differ ONLY in the frontmatter block — prose byte-identical" (render:299), where both halves of the sentence are false. The classification doc's headline claim "the entire difference is the YAML frontmatter block" therefore rests on an instrument that cannot distinguish the two, and no new test pins the trim edge, so nothing stops the strip widening later. Guard by requiring `stripFrontmatter` to have actually changed at least one side, or rename the class to what it measures. | fixed | 318fb818e — predicate tightened, not renamed: `proseEqual` now requires a frontmatter block on at least one side AND tolerates only LEADING newlines (the fence artefact), so a trailing difference stays a prose difference. `trim()` was measured unnecessary: all 109 live pairs match leading-only, 0 needed a trailing trim. Four tests pin the edge, including the no-frontmatter-either-side refusal. |
| 3 | medium | agents/evidence/analysis/carrier-layer-divergence-classification.md:21 | All 109 pairs are assigned to the class "the project copy is generated differently (the generator is the fix)" while the same table row, and the Phase 2 skip note, state the remedy does not apply and that no generator defect exists (`task sync` + `generate-tools` leaves a clean tree). The taxonomy carries no bucket for a deliberate two-writer metadata policy, so the total is force-fit into a bucket and refuted in place. The bare total then propagates without its refutation into road-to-carrier-layer-convergence.md:189 (acceptance criteria) and the Phase 1 step comment at :72, where a downstream reader sees "109 generator-difference" and would conclude there are 109 generator defects. | fixed | 318fb818e — the taxonomy row carries its own refutation plus a fourth honest bucket (deliberate two-writer metadata policy = 24 actionable + 85 inert); the bare total no longer travels — the Phase 1 step comment and the acceptance criteria both state it with the refutation attached, and the flat list is relabelled as all 109 with the 24 as a subset. |
| 4 | low | src/scripts/report_carrier_divergence.ts:226 | `proseEqual` is evaluated for every shared pair, including the pairs `comparePair` already resolved as `identical` or `provenance-only`, where `proseSame` is never read. Each such pair pays two extra full-buffer `toString('utf-8')` calls plus two fence scans and two `trim()`s, and on the provenance path `comparePair` has already converted both buffers, so a shared pair converts each buffer twice. Compute it lazily in the `else` branch that is the only consumer. | fixed | 318fb818e — `proseEqual` and `frontmatterPaths` moved into the `else` branch that is their only consumer, so an `identical` / `provenance-only` pair no longer pays two `toString` conversions and two fence scans. |
| 5 | low | src/scripts/_lib/carrier_divergence.ts:89 | The docstring justifies `stripFrontmatter`'s unterminated-fence behaviour as "the same tolerance `report_carrier_divergence`'s own `type:` reader applies", but the two are opposite: `_ruleType` (report_carrier_divergence.ts:165) sets `fm = text` and treats the whole file as frontmatter, while `stripFrontmatter` returns the text unchanged and treats it as having none. The fence parser is now duplicated across two files, which is the drift mechanism this library's own header (lines 5-12) cites as the reason the comparison lives here in the first place. | fixed | 318fb818e — `splitFrontmatter` is now THE single fence parser and `_ruleType` reads it, so the two can no longer disagree; the docstring states the unterminated-fence behaviour plainly instead of claiming a parity that did not hold. A test pins both directions. |
| 6 | low | src/scripts/_lib/carrier_divergence.ts:24 | The library header still documents a three-way split and asserts "`bodyDiff > 0` means the two carriers are delivering different text — the only class where a reader has to act". The measurement this change lands (109 `body-diff`, 0 differing in prose) falsifies that sentence, and the new `proseEqual` docstring added ~100 lines below in the same file states the opposite, so one file now carries both readings. The consumer's header was updated to four classes; this one was not. | fixed | 318fb818e — library header rewritten: `bodyDiff > 0` no longer claims to be the only class a reader acts on, the measurement that falsifies it is cited, and it points at the report's reader-facing split while stating why the dedup predicate stays three-way. |
