# Findings: carrier-layer-convergence
<!-- completion-review: v1 | reviewed: 2026-08-10 | scope: d4a4c73175c2a07c95f189385bd5f46bd96b49093d8d5e6edeac510739cceee9 | diff: 195523465db8fabb84be3f4ad27f8293cf5e4ac5 | reviewer: r2-fresh-subagent-carrier-layer-convergence | prompt_hash: d6db1729b27a90432716265ac5c5bf386459555e6322eaeed16000983fe231f8 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 195523465db8fabb84be3f4ad27f8293cf5e4ac5
  scope_hash: d4a4c73175c2a07c95f189385bd5f46bd96b49093d8d5e6edeac510739cceee9
  roadmap: agents/roadmaps/road-to-carrier-layer-convergence.md
  roadmap_hash: 84fe85fb4777d429f16cc88e3926074a043c42f2d60153d59dd9e7f0759a5f0e
  ac_hash: ac73f0742c734b8a0a8da6c6ea670e75e9d70514a9e869f437b6b508c47051cb
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-10T15:34:05Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/report_carrier_divergence.ts:301 | The new `frontmatter-only` class is declared "nothing to act on" unconditionally, but the very next two lines state the host DOES read `paths` and that `generate-tools` emits `paths` while `install.ts` writes agent-config's vocabulary instead. If the two writers disagree on `paths` for a path-scoped rule, the frontmatter difference changes WHEN the host loads that rule — project copy conditional, global copy always-on — which is a delivery difference a reader must act on, not metadata the host discards. The change never checks whether `install.ts` emits `paths`, yet the classification doc sizes the path-scoped subset at 25 of 110 project files. Same unconditional claim repeated at report_carrier_divergence.ts:54-56 and report_conformance_funnel.ts:163-164, and it is load-bearing for the roadmap's "no correctness hole" conclusion. | open | |
| 2 | medium | src/scripts/_lib/carrier_divergence.ts:128 | `proseEqual` compares `stripFrontmatter(a).trim() === stripFrontmatter(b).trim()`, so the predicate proves "prose equal after edge-trim", not "differs only in frontmatter". A pair with no frontmatter on either side that differs only in leading/trailing whitespace — two independent writers is exactly where a trailing-newline difference arises — lands in `frontmatterOnly` and is printed as "differ ONLY in the frontmatter block — prose byte-identical" (render:299), where both halves of the sentence are false. The classification doc's headline claim "the entire difference is the YAML frontmatter block" therefore rests on an instrument that cannot distinguish the two, and no new test pins the trim edge, so nothing stops the strip widening later. Guard by requiring `stripFrontmatter` to have actually changed at least one side, or rename the class to what it measures. | open | |
| 3 | medium | agents/evidence/analysis/carrier-layer-divergence-classification.md:21 | All 109 pairs are assigned to the class "the project copy is generated differently (the generator is the fix)" while the same table row, and the Phase 2 skip note, state the remedy does not apply and that no generator defect exists (`task sync` + `generate-tools` leaves a clean tree). The taxonomy carries no bucket for a deliberate two-writer metadata policy, so the total is force-fit into a bucket and refuted in place. The bare total then propagates without its refutation into road-to-carrier-layer-convergence.md:189 (acceptance criteria) and the Phase 1 step comment at :72, where a downstream reader sees "109 generator-difference" and would conclude there are 109 generator defects. | open | |
| 4 | low | src/scripts/report_carrier_divergence.ts:226 | `proseEqual` is evaluated for every shared pair, including the pairs `comparePair` already resolved as `identical` or `provenance-only`, where `proseSame` is never read. Each such pair pays two extra full-buffer `toString('utf-8')` calls plus two fence scans and two `trim()`s, and on the provenance path `comparePair` has already converted both buffers, so a shared pair converts each buffer twice. Compute it lazily in the `else` branch that is the only consumer. | open | |
| 5 | low | src/scripts/_lib/carrier_divergence.ts:89 | The docstring justifies `stripFrontmatter`'s unterminated-fence behaviour as "the same tolerance `report_carrier_divergence`'s own `type:` reader applies", but the two are opposite: `_ruleType` (report_carrier_divergence.ts:165) sets `fm = text` and treats the whole file as frontmatter, while `stripFrontmatter` returns the text unchanged and treats it as having none. The fence parser is now duplicated across two files, which is the drift mechanism this library's own header (lines 5-12) cites as the reason the comparison lives here in the first place. | open | |
| 6 | low | src/scripts/_lib/carrier_divergence.ts:24 | The library header still documents a three-way split and asserts "`bodyDiff > 0` means the two carriers are delivering different text — the only class where a reader has to act". The measurement this change lands (109 `body-diff`, 0 differing in prose) falsifies that sentence, and the new `proseEqual` docstring added ~100 lines below in the same file states the opposite, so one file now carries both readings. The consumer's header was updated to four classes; this one was not. | open | |
