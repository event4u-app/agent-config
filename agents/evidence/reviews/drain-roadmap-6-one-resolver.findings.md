# Findings: drain-roadmap-6-one-resolver
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: 1ad3bc4dbd86274758a4d1f35d58486004175c0d4bed95e3c65167d67f3d4db0 | diff: cb52ac76f6478b7d52ad2ccaf38a4ac7cdb8b03e | reviewer: r2-fresh-subagent-drain-roadmap-6-one-resolver | prompt_hash: 4e022280f8e51fb98d44191d09f102f25b6958de85fde31a8f80f3ea09b1a650 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-roadmap-6-one-resolver"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: cb52ac76f6478b7d52ad2ccaf38a4ac7cdb8b03e
  scope_hash: 1ad3bc4dbd86274758a4d1f35d58486004175c0d4bed95e3c65167d67f3d4db0
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: 50ce6405b4513521873a13a4444ae8b9a73e123f22f53f3414d5218b8107da90
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T12:08:18Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | tests/scripts/one_resolver_invariant.test.ts:41 | The anti-vacuity test never calls `checkOneResolver`; it only calls `fs.existsSync` and `declaresRouter` on the ladder file directly. Verified by sabotaging the scanner's traversal (`scanRoot` -> `src/scriptsTYPO`): a scanner that walks ZERO files still passes both real-tree tests. The scanner also never "recognises" the ladder as the resolver — `checkOneResolver` skips `SANCTIONED_RESOLVER` from the router check entirely. The roadmap's claim (line 203, "A fifth test guards the guard ... by asserting it actually read judgment_ladder.ts and recognised it as the resolver") is false on both halves. | fixed | probe: `checkOneResolver` with `scanRoot` mutated -> `[]`, tests 1+2 both PASS — fixed in fc606f1fb |
| 2 | high | src/scripts/_lib/one_resolver_invariant.ts:40 | `ROUTER_EXPORT_PATTERNS` matches only `export class/interface` and `export (async) function`. The SAME class name evades via ordinary export syntax: `export default class CouncilTopologyRouter`, `export abstract class CouncilTopologyRouter`, `export const CouncilTopologyRouter = class`, `export { CouncilTopologyRouter } from './x.js'`, a separate `export { X }` statement, and `export const resolveCouncilRoute = () => …`. All six measured `declaresRouter === false`. A synthetic tree carrying three competing routers (default-exported `CouncilTopologyRouter`, abstract `CouncilTopologyRouter`, arrow-const `resolveCouncilRoute`) scans GREEN. The module documents narrowness on NAMING only; this is unhandled export SYNTAX, and it defeats AC 1 ("a test fails on any second one") for the most idiomatic declaration form. | fixed | probe: `declaresRouter` over 11 shapes; 2 true, 9 false — fixed in fc606f1fb |
| 3 | medium | src/scripts/_lib/one_resolver_invariant.ts:91 | `scanRoot` is hard-pinned to `src/scripts`, so a second task-side resolver anywhere else is invisible — `src/cli` (50 .ts), `src/shared` (13), `src/server` (22), and `src/agent-src/templates/scripts/work_engine` (78 .ts) are all unscanned. Verified: a literal `export class CouncilTopologyRouter` written into `src/cli/router.ts` returns `[]`. Neither the module docstring nor the roadmap's "What this step does NOT close" section discloses this scope limit. | fixed | probe: match placed in `src/cli` -> `[]` — fixed in fc606f1fb |
| 4 | medium | src/scripts/_lib/one_resolver_invariant.ts:97 | `checkOneResolver` never asserts that `SANCTIONED_RESOLVER` EXISTS. It only enforces the negative half (no second router) and skips the sanctioned path. A tree in which `judgment_ladder.ts` has been deleted outright scans green, so the positive half of AC 1 ("judgment_ladder.ts remains the single task-side orchestration resolver") is enforced by nothing inside the scanner — it rides entirely on the inert test at finding 1. | fixed | probe: ladder deleted + 3 competing routers -> `[]` — fixed in fc606f1fb |
| 5 | medium | src/scripts/_lib/one_resolver_invariant.ts:64 | `importsCouncilInternal` is a text regex requiring `from '<quote>…ai_council/'`. False negatives measured: dynamic `await import('../ai_council/necessity.js')`, CJS `require('../ai_council/necessity.js')`, and the index form `from '../ai_council'` (no trailing slash) all return `false`. False positive measured: a plain COMMENT or string literal containing `from "…ai_council/…"` returns `true`, so a docstring reword in `judgment_ladder.ts` can red the gate with no code change. | fixed | probe: 6 import shapes; 3 true (1 of them a comment), 3 false — fixed in fc606f1fb |
| 6 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:192 | Closure record overclaims the sabotage evidence: "Each asserts the baseline clean first" is true of only 1 of the 3 sensitivity arms — the second (`goes red when the resolver imports…`, test line 66) and third (`goes red on a second function-shaped resolver`, test line 79) assert no baseline. The substance survives because `beforeEach` seeds a fresh tmp tree, but the stated evidence claim does not match the test file. | fixed | test lines 52 vs 66 / 79 — fixed in fc606f1fb |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
