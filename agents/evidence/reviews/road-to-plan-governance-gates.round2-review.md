# Findings: road-to-plan-governance-gates
<!-- completion-review: v1 | reviewed: 2026-08-04 | scope: 2e8caaabb46584d9c7fe74150c66ea2e57f97acd107d1bdf6ad7d479c84a06d0 | diff: 6bc2a2673daf568d39de86710625108a84a03e13 | reviewer: r2-fresh-subagent-road-to-plan-governance-gates -->

<!-- context-manifest: v1
inputs:
  diff_sha: 6bc2a2673daf568d39de86710625108a84a03e13
  scope_hash: 2e8caaabb46584d9c7fe74150c66ea2e57f97acd107d1bdf6ad7d479c84a06d0
  roadmap: agents/roadmaps/archive/road-to-plan-governance-gates.md
  roadmap_hash: 5b087b623a17d4019701ae24a8c0c5d6d5b7e8a01065277499bb017b208a3c92
  ac_hash: 547d6024c1af2654c850bd4193e15c008919a686583db09fa8d8adc1d77a8a36
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-04T09:21:34Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | .github/workflows/consistency.yml:238 | The manifest re-derivation loop runs `dispatch_r2_reviewer --verify` on EVERY `*.findings.md` and blocks on exit 1, but `runVerify` compares the artefact's recorded `scope_hash` against the CURRENT branch scope (dispatch_r2_reviewer.ts:473). Any artefact from a previous branch therefore mismatches by construction, and contract § 2.6 states the directory is tracked and accumulates. The step is guaranteed to red on the next gated PR and can only be un-stuck by editing an unrelated branch's artefact — precisely the directory-wide poisoning § 2.6 forbids (correctly avoided inside `check_completion_review`, re-introduced by this wiring). The same loop exists locally at taskfiles/ci-fast.yml:753, and neither is `--advisory`-gated during the Stage-A window. | fixed | f9102d7be + 8a2bf767b — selection moved into the single-source script (`--verify-current`, reusing the validator's exported relevance notion); both wrong shell selectors documented at the call site. Tests: foreign artefact → exit 0, tampered relevant artefact → exit 1 |
| 2 | high | src/scripts/check_completion_review.ts:434 | `gitTry` calls `execFileSync` without `maxBuffer`, so the review-scope diff is read under Node's 1 MiB default. A branch whose scope diff exceeds that throws ENOBUFS → `gitOut` → `InternalError` → exit 2, which every caller warns-and-allows: Gate R2 silently self-disables on large PRs (this branch's own scope diff is already ~289 KB). `dispatch_r2_reviewer.ts:227` sets `maxBuffer: 256 * 1024 * 1024` for the identical `git diff`, so the dispatcher succeeds exactly where the validator degrades — the shared-definition guarantee does not extend to the injected git runner. | fixed | 8a2bf767b — GIT_MAX_BUFFER 256 MiB on the validator's git runner; same class also found and fixed in lint_plan_risk_register, where it failed OPEN (>1 MiB plan blob → null → staleness silently skipped). Both pinned by >1 MiB fixtures |
| 3 | high | .github/workflows/consistency.yml:217 | Both R2 CI steps depend on `git fetch origin main --quiet \|\| true` to materialize `origin/main` in the shallow PR-merge checkout, and swallow its failure. The same workflow documents this exact pattern as broken at lines 168-170 and 313-318 ("`git fetch origin <base>` can race with the configured auth extraheader … Sidestep the fetch entirely"), which is why other PR-scoped steps use `gh pr diff` / `gh pr view`. When the fetch fails, `--base origin/main` is unresolvable → exit 2 → "degraded advisory, allowing", so the layer the contract calls authoritative silently never runs. Same defect at line 237 for the manifest step. | fixed | f9102d7be — no fetch at all: the PR base sha is already a parent of the merge checkout and is passed directly; both R2 steps are PR-scoped, so no swallowed failure can leave --base unresolvable |
| 4 | high | src/config/agent-settings.template.yml:596 | The `planning.completion_review: false` escape hatch is documented in three places — template ("escape hatch; the validator skips"), src/server/schemas/settings.ts:215 ("false = escape hatch, the validator skips") and src/domains/git/pr/create/command.md:158 — but `check_completion_review.ts` never reads `.agent-settings.yml` at all (Gate R1 does, via `riskReviewDisabled`). Setting the key to `false` has no effect: the gate still blocks at pre-push and CI, and the only way out is deleting the CI step. | fixed | 8a2bf767b — completionReviewDisabled() mirrors riskReviewDisabled byte-for-byte; false → note + `scanned: 0` + exit 0. Tests cover false / missing file / sibling-key-only / true |
| 5 | high | src/domains/git/pr/create/command.md:153 | Step 1d tells the agent an existing artefact "is re-used when its `diff:` sha equals the current HEAD". Contract § 2.1 states `diff:` is "provenance only; never compared", and § 2.0 proves that comparison unsatisfiable (committing the artefact moves HEAD; CI checks out a synthetic merge commit). The documented re-use condition is the scope hash. As written the surface can never re-use the roadmap-completion artefact and re-introduces the forbidden binding the rest of the change was built to remove. Identical text in the projection dist/agent-src/commands/pr/create.md:153. | fixed | f9102d7be — /create-pr now re-uses on the scope hash, never the diff sha, plus an explicit review-last note (a content commit after the review invalidates the binding) |
| 6 | medium | src/scripts/check_completion_review.ts:480 | The header marker's `scope:` is never compared with the current scope. `claimsScope` only ORs marker/honest-null/skip for *relevance*, `evaluate` decides staleness on the manifest (:556), and `runVerify` never checks header-vs-manifest agreement (dispatch_r2_reviewer.ts:473). Contract § 2.1 says the header `scope:` is "the only field staleness is decided on" and § 5 requires the manifest's `scope_hash` to agree with the header. An artefact whose header points at an older scope but whose honest-null line and manifest carry the current one passes both gates. | fixed | 8a2bf767b — header `scope:` decides staleness and must agree with the manifest; disagreement is the new `manifest-header-mismatch` kind, named in contract § 5 |
| 7 | medium | src/scripts/check_completion_review.ts:316 | A findings artefact with NO `completion-review:` header at all is accepted: `markerMalformed` is set only when a line containing `completion-review:` fails to parse, so an artefact consisting of the manifest plus an honest-null line for the current scope produces no violation, although § 2.1 makes all header fields mandatory. Every fixture in tests/scripts/check_completion_review.test.ts carries a marker, so the hole is untested and contradicts the "0 false-pass on the fail fixtures" acceptance criterion. | fixed | 8a2bf767b — a review-bearing artefact with no parseable header marker is `bad-marker`; a bare § 2.4 skip still needs no header |
| 8 | medium | src/scripts/dispatch_r2_reviewer.ts:238 | `deriveSlug` derives the branch slug from `rev-parse --abbrev-ref HEAD`, which on a `pull_request` checkout is the detached synthetic merge commit (acknowledged in the workflow comment at consistency.yml:232), so it returns `detached-<sha>` and `isOwnArtifactSlug` can never match. On the authoritative layer no artefact is ever "own", which inverts contract § 2.6: an own artefact with a malformed marker is skipped as foreign and reported as `missing-artifact` instead of the root-cause `bad-marker`, and the actionable `stale-review` path never fires in CI. | fixed | 8a2bf767b — deriveSlug resolves GITHUB_HEAD_REF → GITHUB_REF_NAME first, ignoring empty/HEAD/detached-*; tested with env set (both keys) and unset |
| 9 | low | src/scripts/lint_plan_risk_register.ts:564 | Staleness compares the working-tree text against the blob of the newest commit whose date is `<= reviewed:`. While a substantial edit and its freshly written register are still uncommitted, that baseline is the previous commit, whose features differ, so the gate reports `stale_review` for a register written the same minute — a false positive in the normal local authoring flow (it self-heals only once the change is committed). | fixed | 8a2bf767b — staleness is skipped while the file has uncommitted modifications (the gate judges committed history); the two pre-existing staleness tests now commit and still assert stale_review |
| 10 | low | src/scripts/check_completion_review.ts:849 | The `--base` resolution check throws before the `scanned:` line is written (:887), so an unresolvable base ref exits 2 emitting no `scanned:` output at all — even though the gate is registered in gate-coverage.yml with `argv: ["--advisory", "--base", "origin/main"]` and its own comment claims `scanned:` is emitted before the pass/fail branch. In any environment without an `origin/main` tracking ref the coverage guard has no number to read. | fixed | 8a2bf767b — the artefact inventory moved above the base-ref check, so `scanned:` is emitted on every exit path including exit 2 |
| 11 | low | src/scripts/check_completion_review.ts:319 | `markerMalformed` is sticky: the first non-fenced line containing `completion-review:` that fails the grammar sets it permanently, and a valid marker parsed later does not clear it. Any prose or comment mentioning `completion-review:` above the header (e.g. a reviewer note citing the grammar) therefore yields a spurious `bad-marker` violation on an otherwise valid artefact. | fixed | 8a2bf767b — a marker candidate must start with `<!--` and a later valid marker clears the flag; prose quoting the grammar no longer fakes a violation |

## Round-2 disposition (fix pass complete)

All 11 findings `fixed`, refs per row. Two of them had silently DISABLED a
gate rather than weakened it — the missing git `maxBuffer` (exit 2 →
warn-and-allow on any diff over 1 MiB) and the `planning.completion_review`
escape hatch that three surfaces documented and no code read. The critical
one was the CI wiring added to close round-1 finding 2: verifying every
artefact reds by construction once the tracked reviews directory holds
another branch's artefact.

Bound to scope `2e8caaab…` (head `6bc2a2673`). Renamed out of the
`*.findings.md` glob because the fixes above moved the content past that
scope; contract § 2.1 re-dispatches such an artefact rather than migrating
it. The binding artefact is the final round, reviewed on frozen content.
