# Findings: road-to-plan-governance-gates
<!-- completion-review: v1 | reviewed: 2026-08-04 | scope: 224a8100d178b43a6e992fa0ff117432b12d10dc18bb9105e8b4512edaff96a9 | diff: 7a0f7b33aed30921f2404ae1955a8e5aa76dde9b | reviewer: r2-fresh-subagent-road-to-plan-governance-gates -->

<!-- context-manifest: v1
inputs:
  diff_sha: 7a0f7b33aed30921f2404ae1955a8e5aa76dde9b
  scope_hash: 224a8100d178b43a6e992fa0ff117432b12d10dc18bb9105e8b4512edaff96a9
  roadmap: agents/roadmaps/archive/road-to-plan-governance-gates.md
  roadmap_hash: 7be2dc5ef4ca9bbda0e022e39a2a62c55c5fb9823dbcac734a0bbe2756cd7241
  ac_hash: 1c3cd7678aacae91ea045d13cde1f09e0bd97738d2f5a63857a2da04efc48dca
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-04T11:59:42Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/check_completion_review.ts:371 | Unbalanced code fence silently drops findings rows — the same fail-open the `malformed-row` hardening closed by a different route. `parseArtifact` toggles `inFence` on any line matching `/^\s*```/` and `continue`s on every line while `inFence` is true, so an ODD number of ```-prefixed lines makes every later line invisible: table rows, `**Honest-null:**` and `**Skipped:**` alike. The repo's own `markdown-safe-codeblocks` rule tells authors to wrap fence-containing content in an OUTER `~~~` fence with inner ``` — `FENCE_RE` does not match `~~~`, so following that rule yields exactly the odd count. If one well-formed terminal row precedes the swallow point, `rows.length > 0` keeps the "neither table nor honest-null" fallback silent, and an `open` / `deferred`-without-ref row after it is never parsed → exit 0. No test in `tests/scripts/check_completion_review.test.ts` exercises a fenced findings artefact. | open | |
| 2 | medium | src/scripts/check_completion_review.ts:815 | `findings-before-fixes` binds only the artefact's FIRST round, and the shipped guidance contradicts the pass fixture. §2.5's ancestry check resolves the artefact's first-add commit (`git log --diff-filter=A`, last line) and compares it against every `fixed` ref. But §2.1 forces the binding artefact's `scope:` to equal the CURRENT scope, and any fix commit changes that scope — so the artefact must be re-bound after fixes. Contract §2.7 (docs/contracts/plan-review-gates.md:402) says a review whose content moved on is "renamed out of that glob rather than edited in place"; the validator's own happy path (`passes with the finalized artifact COMMITTED`) instead rewrites `<slug>.findings.md` in place with the new scope. Under that in-place path the first-add commit stays pre-fix forever, so a round-2+ finding may cite a fix commit that predates the review it belongs to and still pass; under the §2.7 rename path the new file's first-add postdates the fixes and a `fixed` row reds as `fix-before-artifact`. The two documented rules are not jointly satisfiable, and the anti-silent-fixing property is the one §2.5 exists to hold. | open | |
| 3 | low | src/scripts/lint_plan_risk_register.ts:258 | Gate R1 accepts risk rows with MORE than six cells; only `cells.length < 6` is rejected. The row is then destructured to its first six cells and the surplus is dropped, so an unescaped `\|` in the final `Anchored under` cell passes silently, and one earlier in the row shifts `Mitigation` / `Anchored under` so the validator judges the wrong cells. Contract §1.2 specifies exactly six columns, and Gate R2 rejects a non-six row for precisely this reason (`malformed-row`, "an over-long row is as malformed as a short one"). Same defect class, same contract, opposite handling. | open | |
| 4 | low | src/scripts/dispatch_r2_reviewer.ts:145 | `REVIEW_SCOPE_GIT_CONFIG` omits `diff.suppressBlankEmpty`, a documented git config that changes patch BYTES for identical content (it drops the leading space git prints on empty context lines) and has no command-line canceller — it can only be pinned as `-c diff.suppressBlankEmpty=false`. Both the code comment here and contract §2.0 claim every output-shaping knob is neutralised at its git default and then name their residuals explicitly (`$GIT_DIR/info/attributes`, `etc/gitattributes`); this knob is neither pinned nor named, so a maintainer carrying it in `~/.gitconfig` records a scope hash CI cannot reproduce — the "manifest mismatch (stale review) that no content change explains" the section exists to prevent. | open | |
| 5 | low | docs/contracts/plan-review-gates.md:583 | §6 states the `scanned:` line "is emitted on **every** exit path, exit `2` included", but both validators have exit-2 paths that print no count: `parseArgs` / `parse_args` call `process.exit(2)` on an unknown flag or an invalid `--format` before any `scanned:` write (src/scripts/check_completion_review.ts:944, src/scripts/lint_plan_risk_register.ts:743). `lint_plan_risk_register` at least carries a last-resort `scanned: 0` in its CLI catch; `check_completion_review` has no equivalent, so the two validators also disagree on the obligation the contract states identically for both. | open | |
