# Findings: run-continuation-provenance — round 5
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 2ac4c88bb50e67e662a0080ee768fe3b7ac9e92096e11b8191e06bad8603697f | diff: a01b5a0bcb2761b953871c57fc728e4fe027f4d3 | reviewer: r2-fresh-subagent-run-continuation-provenance | prompt_hash: 84c1ea1e93275ed1406e91987267eb0a2cefefc7e9bf15f3ea631d953c9efa8e -->
<!-- evidence-type: v1 | type: original-review | declared: 2026-08-19 -->
<!-- Round 5 of this branch, archived. Bound to scope
     2ac4c88bb50e67e662a0080ee768fe3b7ac9e92096e11b8191e06bad8603697f, which the
     fixes below moved past; the live `findings.md` carries the current round. -->

<!-- context-manifest: v1
inputs:
  diff_sha: a01b5a0bcb2761b953871c57fc728e4fe027f4d3
  scope_hash: 2ac4c88bb50e67e662a0080ee768fe3b7ac9e92096e11b8191e06bad8603697f
  roadmap: agents/roadmaps/road-to-run-continuation-observation.md
  roadmap_hash: 1dd7051bf33c25b94468e2a1fdec0b13b9a67786d690fb0bfdac3c487809d0db
  ac_hash: bb34537a4ce90a2ac144c0346d9d3817fc8ddd788722900f17cdb6b7ed59bea7
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T18:06:46Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/run_continuation_hook.ts:864 | `ladder()` is evaluated at line 864 but the `history_source` reset only runs at line 908 inside the engage branch, so on the very fire where the source document changes the stall test still compares counts from two different files and the non-engage branch returns at line 901 before the reset can execute — fires 1-3 read the parent copy at `open: 4` giving `history [4,4,4]`, then the session tree gains `agents/roadmaps/` (or the host starts sending `cwd`) and its copy also reads `open: 4`, so fire 4 returns `halt-stall` on the parent's counts and declares a working run finished, the exact "coincidentally equal one contributed a false match toward `halt-stall`" failure the field's own docblock at :158-165 claims to prevent. | fixed | 544f0fba5 |
| 2 | high | src/scripts/hooks/run_continuation_hook.ts:749 | The per-directory resolution makes the `complete` rung unreachable in the flow `resolveRoadmap`'s own docblock cites: `roadmap-progress-sync` mandates archival in the same change that closes the last step, so the last box flips and the file is `git mv`d in one reply, the next stop fire finds the session copy gone and returns `EXIT_ALLOW` at :753 — before any event and before any state handling — so no `complete` line reaches `run-continuation.jsonl` (the new test pins `events(main).length === 0`) and `fs.rmSync(stateFile)` at :883 never runs, leaving `iterations` and `started_at` from the finished run in place so a later claim by the same session id starts with part of the 25-iteration budget spent and the 4 h wall clock already running, directly contradicting the comment at :879-881. | fixed | 544f0fba5 |
| 3 | medium | agents/roadmaps/road-to-run-continuation-observation.md:101 | The roadmap enumerates the provenance contract as `workspace_root` / `session_root` / `session_cwd` / `git_dir` / `git_common_dir` / `claim_path` — "six fields" — while this same branch adds a seventh, `roadmap_path` (:827), which the roadmap never mentions; a reader auditing a real ledger line against this bullet finds an undocumented field and cannot tell whether `roadmap_path` is part of the contract, the identical count drift the hook's own docblock at :425-431 records being caught in rounds 2, 3 and 4. | fixed | 544f0fba5 |
| 4 | medium | agents/roadmaps/road-to-run-continuation-observation.md:117 | The bullet states "that file now holds twelve cases, not nine, and THREE of them assert the provenance rather than one" and justifies the correction with "this bullet exists so a reader can check the artefact against a falsifiable number" — but `tests/hooks/run_continuation_dispatch.test.ts` now holds 18 `it(` cases, six of them added by this branch, and more than three assert provenance fields; the roadmap's `## Context` line 26 still says "7 cases", so both falsifiable numbers in the artefact are contradicted by the tree in the same branch that corrected them. | fixed | 544f0fba5 |
| 5 | medium | src/scripts/hooks/run_continuation_hook.ts:646 | `resolveRoadmap` treats the session tree as authoritative whenever `agents/roadmaps/` exists there, and its docblock reads a missing slug file as "the roadmap is gone from this run", conflating that with "never created in THIS worktree" — a worktree branched before the roadmap file landed (the exact commit-age asymmetry `session_register_hook.claim_is_stale` documents at :364-373 as R2 finding 1 and deliberately resolves the other way) makes the concern read a path that only exists in the parent, return `EXIT_ALLOW` at :753 on every stop, and write no ledger line at all — indistinguishable from "the concern never ran", the failure this file's header calls out. | fixed | 544f0fba5 |
| 6 | medium | src/scripts/hooks/run_continuation_hook.ts:203 | `isDuplicateFire` compares `state.history[last]` against the freshly scanned `open` count without consulting `history_source`, so it performs the same cross-document count comparison the new field was added to stop: when the resolved source changes to a copy that happens to read the same open count, within the 60 s window and on an unchanged turn ordinal, the fire is misclassified as a duplicate stop fire and returns `EXIT_BLOCK` at :861 without consuming an iteration and without calling `appendEvent`, so a genuine engagement is silently absent from the ledger the acceptance criteria count. | fixed | 544f0fba5 |
| 7 | medium | src/scripts/hooks/run_continuation_hook.ts:641 | `resolveRoadmap` builds `${slug}.md` without stripping a trailing `.md`, while `claim_is_stale` (session_register_hook.ts:351) explicitly does `slug.trim().replace(/\.md$/, '')` before resolving the same string — so a claim file whose slug carries the suffix renders as live work in the register and in `sessions:list`, while this concern resolves `agents/roadmaps/<slug>.md.md`, fails the read, and allows every stop with no event; two functions in the same flow normalise one claim string differently. | fixed | 544f0fba5 |
| 8 | medium | src/scripts/hooks/run_continuation_hook.ts:170 | `history_source`, its round-trip in `readState` (:403) and the reset at :908 have no test in either changed test file (the identifier appears only at those three source sites), while every other round-4 fix on this branch landed with a pinning case — the ordering defect in finding 1 is precisely what a two-fire source-change case would have caught, so the field ships asserting a protection nothing verifies. | fixed | 544f0fba5 |
| 9 | low | src/scripts/hooks/run_continuation_hook.ts:649 | The `workspaceRoot` fallback branch joins the RAW `workspaceRoot` while the emitted `workspace_root` field is `normalizeDir`'d (:490), so `roadmap_path` escapes the normalisation `provenance`'s docblock declares mandatory ("a symlinked form defeats the containment test"): on a repo reached through a symlinked ancestor, with the session tree lacking `agents/roadmaps/` but the reader's holding the roadmap, the line carries `/var/…/agents/roadmaps/x.md` against `workspace_root: /private/var/…` and a reader's containment test fails on a healthy line — the regression R2 finding 3 fixed for the other six fields. | fixed | 544f0fba5 |
| 10 | low | src/scripts/hooks/run_continuation_hook.ts:645 | The containment guard rejects any normalised path whose text contains `..`, not only a traversal, so a legitimate slug such as `road-to-a..b` returns `null` and the concern silently allows every stop; and because the `null` path returns `EXIT_ALLOW` before `roots` is built, neither a malformed nor a hostile slug leaves any ledger trace, so a refusal is indistinguishable from "the concern never fired". | fixed | 544f0fba5 |
| 11 | low | src/scripts/session_register_hook.ts:458 | `session_checkout` now returns `canonical(workspace_root)` on every fallback branch, but `foreign_sessions_block` compares stored worktree strings with `path.resolve` only (:691-692), so during the TTL window a live register record written by the previous version under a symlinked ancestor holds `/var/…/repo` while a new session in the SAME tree holds `/private/var/…/repo`: the comparison reads them as different worktrees and prints the benign "separate trees" note where the same-worktree COLLISION prompt belongs — the same symptom round 4 finding 5 fixed, reintroduced across the upgrade boundary. | fixed | 544f0fba5 |
