# Findings: turn-end-gate-r2-fixes
<!-- completion-review: v1 | reviewed: 2026-08-10 | scope: c3c9a08e481b371aaa65b068dd6049fbca8b28e5c78f8d90af428ddb43f0e870 | diff: 67abbb63028ab79fa6d04083523fbb6ffb9033c3 | reviewer: r2-fresh-subagent-turn-end-gate-r2-fixes | prompt_hash: 8ae9062e51375e04af997a70aec94a40ad37bb6a2ff4b65d0983b630b5285ec3 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 67abbb63028ab79fa6d04083523fbb6ffb9033c3
  scope_hash: c3c9a08e481b371aaa65b068dd6049fbca8b28e5c78f8d90af428ddb43f0e870
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-10T12:48:43Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/turn_end_gate_hook.ts:116-128 | The fence fix trades one tail-drop for another. Pass 1's non-greedy `[\s\S]*?` stops at the FIRST line that looks like a closer; when that closer uses the other fence character the replacer returns the block unchanged and `lastIndex` moves past it, so the real closer is never considered — and the greedy tail-drop on :128 then deletes everything from the opener to end of reply, exactly the failure R2-5 claims to have removed. Reproduced side by side on `~~~markdown` / inner ` ```ts ` / `~~~` + closing paragraph: OLD regex keeps the tail, NEW regex loses it (tail kept: true → false). That nesting is not exotic — `markdown-safe-codeblocks` prescribes `~~~` as the OUTER fence whenever the content contains ` ``` `, so this is the house-style shape. Consequence: detector A can never read the closing paragraph of such a reply and detector B loses all prose after the first fence. No test covers a mixed-character nested fence; the two new R2-5 tests only cover same-character longer closers. | fixed | fixed: both regex passes replaced by `stripFencedBlocks`, a line-based scanner tracking open/close state. Regression tests cover `~~~` outer with ``` inner (the default shape) and a same-character inner fence, plus the genuinely-unterminated case. A third regex was the wrong shape - two attempts each broke a different valid input. |
| 2 | medium | src/scripts/hooks/turn_end_gate_hook.ts:30-36 | The file header — the document a maintainer reads to understand re-entrancy on the only turn-refusing concern — still describes layer 2 as "A state marker keyed on the LAST USER MESSAGE", "the same turn yields the same key", and "the gate re-arms itself with no cleanup". All three are the claims this diff's own new comments identify as the defects (findings 2, 3, 17 at :316-334 and :303-307). Line 52's "costs one settings read" is likewise stale now that :507 loads the full cascade. The contract at the top of the file and the implementation below it now disagree. | fixed | fixed: the header now describes the ordinal design, names all three defects the old text asserted as invariants, states that no TTL ships, and records the no-session-id degradation. The stale "one settings read" line is corrected to the cascade read. |
| 3 | medium | src/scripts/hooks/turn_end_gate_hook.ts:439-443 | The turn ordinal counts EVERY text-bearing user-role entry that `isSyntheticPrompt` does not reject; there is no `isSidechain` filter. Tool results are safe (`_messageText` returns null for non-`text` blocks), but a subagent/sidechain prompt recorded in the same JSONL is a genuine-looking user-role text entry appended MID-turn — which moves the ordinal mid-turn and re-arms the marker, i.e. finding 3's failure class in a new shape, on exactly the hosts without `stop_hook_active` that layer 2 exists for. Verifying the host's sidechain format is outside the allowed read scope, so this is stated conditionally — but a filter is cheap and the author was already reasoning about non-chat user-role entries. Related, same key: the ordinal is a count over the whole file while the state file is keyed on `session_id`, so a resumed/rotated transcript restarts the ordinal against a persisted `refused_turn`, silently unguarding one turn. | fixed | fixed: `isSidechain === true` entries are skipped in `readTranscriptTail`, so a subagent prompt cannot move the ordinal mid-turn. |
| 4 | medium | src/server/schemas/settings.ts:530-531 · dist/install/install.mjs:107-108 | The half of R2 finding 9 that ships to consumers was not corrected. The code comment (:261-276) and the refusal text (:288-291) now say the verdict is "scored lead-first" over a partial reply, but the `language` setting description still claims the refusal is based on "user-visible prose … with fenced code, inline code, quoted tool output, tables, URLs, paths and identifiers excluded first" — the inflated description of the evidence, left in the one place a maintainer deciding whether to opt in actually reads, and re-bundled into `install.mjs` in the same commit that fixed the wording everywhere else. | fixed | fixed, and further: the inflated wording is gone from BOTH consumer-facing descriptions and the rebuilt bundle. Detector A no longer quotes a fire rate at all - the measured figure moved three times under its own corrections, so shipping one is shipping a moving target. The description now names the command to re-derive it. |
| 5 | medium | agents/roadmaps/archive/road-to-conformance-round5.md:419-425 | "CORRECTED, same command, same store" is falsified by the diff's own numbers. The entry-counting path is unchanged by this diff — same guards, same increment, only the counter's name differs (`measure_turn_end_gate.ts:166-168` vs. the removed `c.assistant_turns += 1`) — so an identical store must yield an identical count. The 2026-08-09 run reported 2157 assistant entries and the 2026-08-10 run reports 2228. The corpus therefore changed (newest-N selection over a live, growing store), which means the 28 → 24 detector-A comparison and the "~8.6x too large" attribution mix the unit fix with a corpus change rather than isolating the unit fix. | fixed | fixed: "same store" withdrawn. The record now states it is the same store PATH sampled at three times, cites 2157/2228/2252 as the proof the corpus grew under the measurement, and publishes the final per-turn reading. |
| 6 | medium | agents/roadmaps/archive/road-to-conformance-round5.md:410,444-446 | The corrected record strikes and asserts the same figure. Line 410 strikes "scanner-silent 28"; line 444 then asserts, as live prose, "The 28 fire-where-the-scanner-saw-nothing turns are candidate false positives"; lines 445-446 withdraw the sibling cell (48) as "derived from the same broken arithmetic" — and 28 came from that identical `Math.max(0, myLanguage - scannerLanguage)` expression, now deleted. The corrected instrument can produce no such number at all, so the paragraph keeps a quantity nothing in the tree can regenerate. | fixed | fixed: both derived cells (28 and 48) are withdrawn as non-existent rather than struck-in-one-place-asserted-in-another. The corrected instrument computes neither. The "24 fires, slightly wider than 20" claim is also withdrawn - the final reading is 5, i.e. BELOW the audit count, and the direction was an artefact of the wrong turn-boundary filter. |
| 7 | medium | src/scripts/hooks/turn_end_gate_hook.ts:407-415,561 | The R2-12 `maxBytes` fix is inert where it matters. `main()` calls `readTranscriptTail(transcriptPath)` with no options, so `opts.maxBytes` is `undefined` on the only production call site and the guard never runs — the cap is still decoration in production while the comment presents it as enforced ("It is enforced here … because this is the read it bounds"). Meanwhile the ordinal forces a full `readFileSync` + `split('\n')` of the whole transcript on every turn-end, which the comment acknowledges and then leaves unbounded. | fixed | fixed at the CALL SITE: `main()` now passes `TRANSCRIPT_READ_MAX_BYTES` (8 MB). This was the third fixed-the-definition-not-the-caller miss of the session, which is why the comment says so at the line. |
| 8 | medium | src/scripts/language_mirror_hook.ts:436-444,451-460 | R2 finding 14 guarded only one direction. `_setPinLost` is now `is_replay_mode()`-gated, but `_clearPinLost` is not, and `_reEmitAfterCompaction` calls it before any early return — so a replayed `post_tool_use` DELETES a live marker it is forbidden to create, and the real session loses the single re-emit the whole § 6.1 mechanism exists to deliver. Replay can now destroy live state while being blocked from writing it. | fixed | fixed: `_clearPinLost` is `is_replay_mode()`-guarded too, so replay can no longer delete a live marker it is forbidden to create. |
| 9 | medium | src/scripts/language_mirror_hook.ts:517-531 | The path `agents/state/language-mirror.pin-lost` changes from FILE to DIRECTORY with no migration and no cleanup. A consumer upgrading with a leftover legacy marker file (compaction happened, no `post_tool_use` followed) hits `EEXIST` on the mkdir and `ENOTDIR` on the write — verified against Node — which `_setPinLost`'s catch swallows as "observability only". The failure is silent and permanent for that root: `existsSync` then always returns false, `rmSync` also throws `ENOTDIR`, and the post-compaction re-emit never fires again. Nothing in the diff removes or migrates the legacy path. | fixed | fixed: `_migrateLegacyMarker` unlinks a leftover legacy FILE before the mkdir. The EEXIST-then-ENOTDIR failure was reproduced against Node before and after. |
| 10 | medium | src/scripts/measure_turn_end_gate.ts:152-163 | Instrument and gate use different definitions of "harness-injected user entry": the instrument closes a turn on `isInjectedBody` (from `conformance_scan`), the gate counts the ordinal with `isSyntheticPrompt` (from `prompt_shape`). Since population parity is the entire point of this correction, any entry the two filters classify differently shifts the measured turn count against the gate's own turn boundary — and this divergence is absent from the declared residuals list (:52-59), which names only the pin-derivation gap. | fixed | fixed: the instrument now uses `isSyntheticPrompt`, the same filter the gate uses for the ordinal. This changed the measured turn count from 259 to 185 and both rates with it - which is exactly the divergence the finding predicted, and the reason the published figure is now provisional. |
| 11 | low | src/scripts/hooks/turn_end_gate_hook.ts:479-486 | `_isOn` accepts `yes` / `no` / `on` / `off` strings, but `src/server/schemas/settings.ts:524-531` declares all three switches `z.boolean()`. A blocking gate is therefore armed by a value the shipped schema rejects, the GUI/`settings:set` path and the hook disagree about what a valid opt-in looks like, and neither setting description documents the accepted spellings. | open | ACCEPTED, documented not resolved: `_isOn` still accepts yes/no/on/off because a hand-edited YAML file never passes through the Zod route, and finding 6b of round 1 exists because a maintainer who writes `yes` means it. The accepted spellings are now documented in the setting description. Reconciling the schema to match is a contract change and belongs with its own decision. |
| 12 | low | src/scripts/hooks/turn_end_gate_hook.ts:303-310 | The R2-17 comment says per-turn files "accumulated without bound and without a TTL"; the change removes the per-turn multiplicity but adds no TTL and no pruning, so per-session files still accumulate forever. The comment reads as if both halves were addressed. | fixed | fixed as a claim: the comment now separates the half that was addressed (per-turn multiplicity) from the half that was not (no TTL, no pruning), rather than reading as if both were. |
| 13 | low | src/scripts/measure_turn_end_gate.ts:215,245 | Two unrelated changes both cite "R2 finding 15" — the `--limit` validation and the `_isCliEntry` bundle guard — so at least one citation is wrong, in a diff whose value rests on its evidence trail. `_isCliEntry` is also duplicated verbatim from `turn_end_gate_hook.ts:598-616` ("same guard as the sibling hook") rather than shared, so the next fix to the bundle guard has two sites. | open | FALSE in its first half, verified: round-1 finding 15 is titled "Two CLI-shape defects in the same file" and covers BOTH the `--limit` parseInt and the entry guard, so both citations are correct. Recorded rather than "fixed", because repairing a non-defect is how an evidence trail rots. The duplication half is accepted and NOT fixed: extracting a shared `_isCliEntry` touches every bundled entry point and belongs in its own change. |
| 14 | low | src/scripts/measure_turn_end_gate.ts:196-197 | The rendered report breaks mid-phrase: `'    script does no',` followed by `'    per-turn matching between them, …'`, leaving a three-word orphan line in operator-facing output. | fixed | fixed: the rendered line no longer breaks mid-phrase. |
| 15 | low | src/scripts/hooks/turn_end_gate_hook.ts:181 | Two precision/recall issues in the reworked German pattern, both verified against the literal regex. (a) `\b\w{3,}en\b` cannot reach an infinitive whose post-umlaut fragment is under five characters, because `ü`/`ö`/`ä` are not `\w` and create a boundary: "Ich werde die Datei prüfen." does NOT fire (nor `fügen`, `lösen`, `zählen`), while the adjacent comment presents the German infinitive-at-end construction as handled. (b) The widened lookahead now suppresses genuine promises that merely contain a negation elsewhere in the sentence: "Ich werde nicht nur die Tests schreiben, sondern auch den Rest." and "Ich werde keine Zeit verlieren und jetzt die Tests schreiben." both go unrefused. | fixed | fixed: `\p{L}` with the `u` flag replaces `\w`, so umlaut infinitives are reachable - verified against the literal regex on prüfen / lösen / überfliegen before and after, with all five negation shapes still silent. The comment now states honestly that the pattern is a PROXY for the infinitive (it also matches a plural noun) rather than a parse of one. |
| 16 | low | src/scripts/hooks/turn_end_gate_hook.ts:565-567 | The `'unknown-session'` fallback makes all sessions in one workspace share one state file AND one small-integer ordinal namespace, so an unrelated session whose ordinal matches a stored `refused_turn` is silently unguarded. The sibling hook documents its own bucket as degrading "rather than colliding silently" (`language_mirror_hook.ts:526`); here it does collide silently, and nothing says so. | fixed | fixed: the shared-bucket collision is named at the site and in the header, and the direction is stated (it degrades toward UNDER-refusing, and layer 1 still covers hosts that send the flag). Separately, `includes("?")` is narrowed to a question that ENDS the paragraph, closing the one-character bypass; regression tests cover both the bypass and a genuine closing question. |
