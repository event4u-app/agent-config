# Findings: conformance-round5-stop-refusal
<!-- completion-review: v1 | reviewed: 2026-08-10 | scope: 8b1ce05989ba8ea4750e4fbb2a1cd523ae256c54f6bdb6f6137e10eb01796de2 | diff: f16f84697417b41d79838857d3cdaf7bcdf8b7e3 | reviewer: r2-fresh-subagent-conformance-round5-stop-refusal | prompt_hash: ad22c2f3b17426be9e8f1c8b221f5d2ae5444551266fe0785a4f7ed8073b5f6f -->

<!-- context-manifest: v1
inputs:
  diff_sha: f16f84697417b41d79838857d3cdaf7bcdf8b7e3
  scope_hash: 8b1ce05989ba8ea4750e4fbb2a1cd523ae256c54f6bdb6f6137e10eb01796de2
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-10T12:05:46Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/measure_turn_end_gate.ts:120-130 | The published fire rates measure a population the gate never inspects. `measure` increments `assistant_turns` and evaluates both detectors once **per assistant entry that carries text**, while the shipped gate evaluates only the **last** assistant text of a turn (`turn_end_gate_hook.ts:444-466`). A turn with k prose entries contributes k samples to the instrument and 1 to the gate, so "18.0% of turns" / "1.3%" are per-entry rates mislabelled as per-turn. Detector A is by construction a *closing*-paragraph detector, so running it over mid-turn prose also makes the roadmap's "28 fires vs the audit's 20 turn-closings — the same order, slightly wider" a comparison between two different units. The 18.0% figure is the stated justification for the default-OFF decision and is shipped consumer-visible in `src/server/schemas/settings.ts:531`, `dist/install/install.mjs:14973` and the archived roadmap § 3.4. | fixed | fixed in this branch: `measure` now scores ONE sample per turn (the last assistant text), and `render` prints turns beside entries so the size of the old error stays visible. Re-measured: 259 turns / 2228 entries, detector A 24 (9.3%), detector B 39 (15.1%). Published figures corrected in the schema, the install bundle and the archived roadmap, with the withdrawn numbers struck rather than deleted. |
| 2 | high | src/scripts/hooks/turn_end_gate_hook.ts:264-278 | Re-entrancy layer 2 silently disables the gate for every **repeated** user prompt. `deriveTurnKey` = sha256(session_id + last user text), and the marker file is never removed, so a second turn whose prompt text is byte-identical to an earlier refused one ("weiter", "ok", "1", "mach weiter", "fix it") resolves to the same key, `alreadyRefusedThisTurn` returns true, and the turn is allowed unconditionally. The header's invariant "the next genuine prompt yields a new key and the gate re-arms itself" holds only for *distinct* prompt text; the covering test (`tests/scripts/turn_end_gate_hook.test.ts:3036-3047`) uses two different strings and cannot see this. Repeated short continuations are the dominant prompt shape in the corpus this gate was built from. | fixed | fixed: the turn key is now the ORDINAL of genuine user prompts, never the prompt text, and state is one file per session holding the last refused ordinal. Regression test `R2-2` refuses two byte-identical "weiter" turns. |
| 3 | medium | src/scripts/hooks/turn_end_gate_hook.ts:326-360 | The turn key is derived from *any* user-role transcript entry with a text block, including harness-generated ones. `readTranscriptTail` filters on `type === 'user'` only; the round-5 audit itself records that a compaction summary "arrives as a `user` entry" (archived roadmap § Three corrections) and `<system-reminder>` injections land the same way. When such an entry appears after the human prompt, `lastUser` changes **within one turn**, minting a new key and re-arming the marker — so the same turn can be refused more than once on any host that does not send `stop_hook_active`, which is precisely the case layer 2 exists to cover ("each sufficient alone", file header). The sibling module exports `isSyntheticPrompt` for exactly this defect class (round-5 § 6.5) and it is not applied here. | fixed | fixed: `readTranscriptTail` applies `isSyntheticPrompt` to every user-role entry, so a harness injection cannot move the ordinal mid-turn. Regression test `R2-3`. |
| 4 | medium | src/scripts/hooks/turn_end_gate_hook.ts:152 | The `ich werde` pattern's negation lookahead excludes only the literal `nicht`, so it fires on explicit refusals to act — the shape the adjacent comment claims to exclude. Reproduced against the shipped regex: "Ich werde nichts anfassen." → fires (`nichts` has no `\b` after `nicht`), "Ich werde keine Tests schreiben." → fires, "Ich werde niemals raten." → fires, and the passive non-promise "Ich werde gefragt, ob die Regeln passen." → fires. None is caught by `HANDBACK` and none contains `?`, so each is a live false refusal on a BLOCKING guard — the precision failure the council warning and § 3.5 are about. | fixed | fixed: the lookahead now covers the negation family (`nicht / nichts / kein* / niemals / nie`) plus the passive forms. Regression test asserts all five refusal shapes stay silent AND the affirmative still fires. |
| 5 | medium | src/scripts/hooks/turn_end_gate_hook.ts:104-106 | The unterminated-fence fallback deletes the whole tail of a reply on **valid** markdown. Pass 1 requires the closing fence to be byte-identical to the opener (the `\1` backreference), but CommonMark allows a longer closing fence; when the lengths differ, pass 2 — greedy to end-of-string and anchored to no closer — replaces everything from that fence line onward. Reproduced against the shipped function: a reply of "Vorher." + a three-tilde-opened block closed with four backticks + "Ich melde mich, sobald die CI grün ist." yields prose "Vorher." plus whitespace only — the closing paragraph vanishes, so detector A cannot see the promise and detector B loses the whole tail. Same result for three-backtick opened / six-backtick closed. Silent detection loss, and it also perturbs the measured rates in finding 1. | fixed | fixed: pass 1 matches the fence CHARACTER with a length floor per CommonMark instead of a `\1` backreference. Regression tests cover tilde-3-closed-by-4 and backtick-3-closed-by-6, plus the genuinely-unterminated case. |
| 6 | medium | src/scripts/hooks/turn_end_gate_hook.ts:374-407 | The hand-rolled settings reader mis-resolves the switch in three concrete ways. (a) It reads only `<workspaceRoot>/.agent-settings.yml`, so the user-global layer of the cascade the contract row added in this same diff describes (`docs/contracts/settings-classes.md:316-318`) is ignored — a maintainer who opts in globally gets no gate and no diagnostic; the envelope already carries a resolved `settings` field (see the test's own envelope, `tests/scripts/turn_end_gate_hook.test.ts:2952-2961`), so the stated performance reason for skipping the cascade does not apply. (b) `flag()`'s `^\s+name:\s*(true or false)\s*$` requires end-of-line, so `enabled: true  # soak opt-in` and the valid YAML boolean `enabled: yes` both read as **false** — verified against the shipped code. (c) The walker never checks the block's parent, so a `turn_end_gate:` map at top level or under any other key arms the gate — verified. | fixed | fixed: replaced with `load_agent_settings` plus an explicit YAML-boolean coercion. Three regression tests: trailing comment, `yes`/`no`/unknown, and a `turn_end_gate:` block under the wrong parent. NOTE on 6a: the suggested remedy was wrong — the dispatcher hardcodes an empty `settings` field in the envelope (`dispatch_hook.ts`), so the cascade is the fix, not the envelope. |
| 7 | medium | src/scripts/measure_turn_end_gate.ts:135-137 | The cross-tab cells are per-session count arithmetic, not per-turn matching: `agree = min(myLanguage, scannerLanguage)` and `only = max(0, myLanguage - scannerLanguage)`. Two instruments flagging entirely **disjoint** turns in equal numbers report as full agreement, so `render()`'s "of which the independent scanner also flagged" / "fired where the scanner saw nothing" (:151-152) and the roadmap's "the two instruments agree on 360 of this detector's 388 fires" are not supported by what is computed. `render()` also never prints the scanner's own total, so the roadmap's companion figure ("the scanner flagged 48 this detector did not") is not reproducible from the command whose stated purpose is that the numbers be re-derivable. A `scanSession` throw is swallowed to `scannerLanguage = 0` (:96-98), which silently reclassifies that session's fires as scanner-disagreements. | fixed | fixed: the per-session `min`/`max` cross-tab is gone. `render` reports the scanner total side by side and states explicitly that no overlap is derivable from two totals without per-turn matching. A `scanSession` throw is now surfaced on stderr instead of being swallowed to 0. |
| 8 | medium | src/scripts/measure_turn_end_gate.ts:113-122 | The measured detector-B rate is not the rate the shipped gate would produce, because the two derive the pin differently. The instrument re-derives it per user entry with `classify(u)` gated only by `isInjectedBody`; the gate reads the mirror hook's persisted pin, which is produced by `isSyntheticPrompt` filtering, lead-first classification, a system-locale fallback and the keep-previous-pin rule (`language_mirror_hook.ts:489-522`, `nextState`). Any of those diverging moves a turn between "pinned de", "pinned en" and "no obligation", which is exactly the input detector B branches on. | open | RECORDED, not repaired: named as a known residual in the `measure_turn_end_gate` header. Parity means reusing `nextState` (locale fallback + keep-previous + `isSyntheticPrompt`), which is a larger change than a measurement correction and would itself need re-measuring. Deferred deliberately rather than half-done. |
| 9 | medium | src/scripts/hooks/turn_end_gate_hook.ts:234-235 | `detectLanguage` classifies with `classify`, which is prompt-shaped, so the detector does not do what the hook, the instrument and the roadmap all say it does. `classify` first runs `instructionText` — which drops any line whose head matches its output-head regex (four-plus leading spaces, a dollar sign, a blockquote marker, a bare pipe, or `error:`/`fatal:`/`warning:`) and then every following line until the next blank one (`language_mirror_hook.ts:146-170`) — and then `humanAuthoredLead`, returning on the **lead** alone when the lead is determined (:188-196). So indented prose is discarded and, in the common case, only the reply's opening chunk decides the verdict. The claim repeated in `turn_end_gate_hook.ts:230-231`, `measure_turn_end_gate.ts:26-28` and `render()`:154-155 ("this detector reads the whole stripped reply") is therefore inaccurate, and the `de_markers`/`en_markers` counts quoted back in the refusal text may describe only the lead. | fixed | fixed (claim, not scoring): all three sites now state that `classify` scores lead-first after `instructionText` / `humanAuthoredLead`, including the refusal text the user sees. The scoring surface is deliberately unchanged - sharing one classifier with `language_mirror_hook` is what stops the pin and the detector disagreeing, and changing it would change the measured rate and need its own measurement. |
| 10 | medium | src/scripts/language_mirror_hook.ts:95,425-435 | The pin-lost marker is session-agnostic while the pin state it restores is not. `PIN_LOST_MARKER` is a single path per consumer root, and `_reEmitAfterCompaction` ignores `PinState.session_id` (:325) entirely. With two sessions live in one project — the normal shape for this repo's worktree workflow — session B's next `post_tool_use` consumes the marker session A's compaction set, so A never receives its one re-emit (the whole point of § 6.1) and B receives a pin notice derived from whichever session last wrote the shared state. The marker also survives session end, so a compaction with no following tool call re-emits into the next session. | fixed | fixed: the marker is per-session (`pinLostMarker(session_id)`), so a concurrent session can no longer consume another one's re-emit. Regression test `R2-10` proves session B stays silent and does not clear session A's marker. |
| 11 | low | src/scripts/hook_manifest.yaml:537 | `turn-end-gate` is the first blocking concern ever bound to `stop`, and it is inserted mid-list ahead of `self-repair`, `session-register` and `session-eol`. If the dispatcher short-circuits the concern list on a blocking verdict, a refused turn-end silently skips that bookkeeping; if it does not, nothing here says so. The diff ships no test that asserts the remaining `stop` concerns still run when the gate refuses, so the ordering decision is untested either way. | open | |
| 12 | low | src/scripts/hooks/turn_end_gate_hook.ts:308-322 | `readTranscriptTail` accepts `maxBytes` in its options type and never uses it: the body reads the entire transcript with `fs.readFileSync(...).split('\n')` and walks every line to keep the last two entries. On an enabled gate this is a full read of a session file that grows for the life of the session, once per turn-end, to obtain its tail. Dead parameter plus an avoidable O(file) read. | fixed | fixed: `maxBytes` is enforced at the read it bounds. The full walk stays - the turn ordinal is a count over all entries, so a tail cannot answer it - which makes the cap a guard rather than an optimisation. |
| 13 | low | src/scripts/hooks/turn_end_gate_hook.ts:439-448 | `AGENT_CONFIG_TRANSCRIPT_HOME` is read on the production path to widen the one security-relevant check in the hook (transcript confinement under `$HOME`), documented as existing "for the test harness only". An env var that relaxes a path-confinement predicate is a soft bypass of it; the test could pass `homeDir` through an injected option instead, which `readTranscriptTail` already accepts. | fixed | fixed: `AGENT_CONFIG_TRANSCRIPT_HOME` removed from the production path. Tests set `HOME` on the spawned process (`os.homedir()` honours it), which also makes the settings cascade hermetic. |
| 14 | low | src/scripts/language_mirror_hook.ts:391-397 | `_setPinLost` writes with a raw `mkdirSync` + `writeFileSync`, bypassing both conventions the surrounding code follows: the module's own `atomic_write_json` for its state file (:525) and the `is_replay_mode()` guard the sibling new hook applies before any state write (`turn_end_gate_hook.ts:280-282`). Under replay the marker is written for real, so a replayed compaction event mutates live state. | fixed | fixed: `_setPinLost` uses `atomic_write_text` behind an `is_replay_mode()` guard, matching both conventions the surrounding module follows. A replayed compaction no longer mutates live state. |
| 15 | low | src/scripts/measure_turn_end_gate.ts:173,192 | Two CLI-shape defects in the same file. `parseInt(argv[i+1], 10)` is unvalidated, so `--limit abc` yields NaN, `slice(0, NaN)` returns an empty list, and the script exits 2 with the misleading message "no .jsonl sessions under <store>". And the entry guard is `process.argv[1].includes('measure_turn_end_gate')`, a substring test, where the new sibling file in this same diff uses the hardened `__AGENT_CONFIG_BUNDLE__` + realpath guard (`turn_end_gate_hook.ts:481-503`) precisely because a naive guard auto-runs when the module is inlined into a bundle. | fixed | fixed: `--limit` is validated (positive integer, else exit 2 with the real reason), and the entry guard is the `__AGENT_CONFIG_BUNDLE__` + realpath guard the sibling hook uses. |
| 16 | low | src/scripts/hooks/turn_end_gate_hook.ts:185 | `detectPromissory` returns null whenever the final paragraph contains a `?` anywhere. That is broader than the stated rule ("a blocking question IS the stop condition"): a rhetorical or quoted question alongside a promise disables the detector, which is a one-character, trivially learnable bypass of a blocking guard. | open | |
| 17 | low | src/scripts/hooks/turn_end_gate_hook.ts:252-258 | The refusal markers accumulate without bound and without a TTL: one JSON file per refused turn under `agents/runtime/state/turn-end-gate/`, written on every fire, never pruned or expired by anything in this diff. The header's "re-arms itself with no cleanup" describes the key rotating, not the files being removed. | fixed | fixed: one state file per session instead of one per refused turn. Regression test `R2-17` asserts three refused turns leave exactly one `.json`. |
