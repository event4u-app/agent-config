# Findings: feat-design-intent-conformance
<!-- completion-review: v1 | reviewed: 2026-09-09 | scope: beb47058c54503d450fd8b6656250825f7ab07d0e0783ec225201f0432390ab2 | diff: b40531ab91c3331867592cade6f774cb89dd9e20 | reviewer: r2-fresh-subagent-feat-design-intent-conformance | prompt_hash: ba981d71a1dbada3e098be5e07e8234c2f2681839b1ac2c039abf8d608a46845 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-feat-design-intent-conformance"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-09 -->

<!-- context-manifest: v1
inputs:
  diff_sha: b40531ab91c3331867592cade6f774cb89dd9e20
  scope_hash: beb47058c54503d450fd8b6656250825f7ab07d0e0783ec225201f0432390ab2
  roadmap: agents/roadmaps/road-to-design-intent-conformance.md
  roadmap_hash: 72917882805ce3dd370d18a0e50759f71c4d396daee1e40627fb3e8deb391b70
  ac_hash: cf07eb2e59f724518d44e21796c8ea4e2022571e26e1b38d56a7a87ee5f2ed56
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-09T10:07:03Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/skills/tailwind-engineer/SKILL.md:61-65 | The distance table instructs writing the project token in place of the artifact's own value with no `design.fidelity_mode` gate at all. `design-fidelity.md:121` makes ANY deviation under `hard-floor` a Hard-Floor action that "no autonomy setting, roadmap, or standing instruction lifts", and the Iron Law at `design-fidelity.md:60-61` forbids swapping SPACING or COLOUR "WITHOUT EXPLICIT CONFIRMATION" — unqualified by visibility, i.e. stricter than the `strict`-mode row's "every visible deviation". The roadmap's own step 3.2 states "`hard-floor` disables it", so step 1.2 shipped the approximation semantics ahead of the gate its own plan names, and ahead of `blocker: approximation-tolerance` and `blocker: rule-body-cap`. Row 2 of the table ("cannot see side by side" to the project token) is precisely the unconfirmed swap the rule forbids. | fixed | e69fd4f00 |
| 2 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:133-140 | `_READ_ONLY_VERBS` is documented as "segment-leading command words that cannot bring a path into existence", and at least four entries can: `sed` (`-i`, and the `w` flag writes a file), `find` (`-exec`, `-fprint`), `awk` (`system()` and `print >`), and `echo` piped into `xargs mkdir -p` — where the path token sits in the read-only segment and the creating segment carries no path. Probed on this branch: `sed -i`, `sed …w <inbox>/out.txt`, `find … -exec mkdir -p <inbox>`, `find … -fprint <inbox>/list.txt`, `awk 'BEGIN{system("mkdir -p <inbox>")}'` and the xargs form all return zero candidates; the token scan this diff removed blocked all six. Risk-register row 1's mitigation ("an unknown verb … is still judged") does not cover a KNOWN verb that executes or writes. | fixed | e69fd4f00 |
| 3 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:184-197 | `creatableTokens` delegates segmentation to `invokedSegments`, which strips heredoc BODIES (`git_command_classifier.ts:642-645`). That segmenter answers "which commands are invoked" and deliberately discards data; this guard asks "which paths appear", so the discarded text is exactly this guard's input. Probed: `cat <<'EOF' > <inbox>/x.md` (the heredoc regex swallows the redirect that follows the delimiter) and `python3 - <<'PY'` whose body calls `os.makedirs('<inbox>')` both yield zero candidates — and both were blocked by the pre-diff token scan. The second form matters concretely: a python heredoc is the documented way to run the inbox move loop past this guard. The new docstring line "a shell command whose own segments would bring one into existence" over-claims the scope the code now has. | fixed | e69fd4f00 |
| 4 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:156-163 | `_redirectTargets` captures the target with its surrounding quotes — its character class stops at whitespace and shell separators but not at a quote, while `INBOX_RE` (:201) and `INBOX_DIR_TERMINAL_RE` (:217) both require start-of-string or `/` immediately before `agents`. Probed: `echo hi > "<inbox>/x.md"` yields the token with quotes attached and `verdictFor` returns "not inside an inbox subdirectory" — ALLOWED; the unquoted form is blocked. The split class this diff removed contained both quote characters as separators, so the quoted form was blocked before it. This falsifies the risk-register claim "redirect targets are judged whatever the verb". | fixed | e69fd4f00 |
| 5 | medium | src/skills/tailwind-engineer/SKILL.md:64-65 | The reporting channel the whole table rests on is undefined. "port notes" occurs exactly once in the tree — this line — and "report the project gap" names no destination either, while the skill's own output template (:137-142) was left unchanged: it carries `Token map:` and `Risks:` and gained no field for the distance or the project gap. AC-2 is satisfied only by the words "without reporting the distance", and the reporting half has nowhere to land, so the obligation is unobservable in the skill's own deliverable shape. | fixed | e69fd4f00 |
| 6 | low | agents/roadmaps/road-to-design-intent-conformance.md:371 | AC-2 is marked `[x]` and asserts a universal over the whole corpus ("No shipped skill instructs the agent to replace a provided artifact's exact value …"). The branch changes one skill; nothing in the diff establishes the universal, and this review's tool allowlist (prompt.md:18-20) forbids the repo-wide scan that would. Unbacked as written — an AC about one skill would be checkable, an AC about all of them is not from this scope. | fixed | e69fd4f00 |
| 7 | low | src/scripts/hooks/block_speaking_inbox_dir.ts:282-291 | The re-anchor regex is greedy (`(?:.*\/)?`) and therefore resolves to the LAST inbox segment in the path, while `inboxDirName` (:231) matches the FIRST. A path carrying two inbox segments judges one directory and probes another: probed, `<inbox-speaking>/agents/tmp/round-a91f3c/x.md` judges the speaking name and probes the opaque one, returning "directory already exists — rename, do not re-refuse". The pre-existing comment at :271-272 still asserts the probe path "is derived from the SAME anchored match that found the directory", which this change made false. Contrived path shape, hence low — but the comment now documents an invariant the code does not hold. | fixed | e69fd4f00 |
| 8 | low | tests/hooks/block_speaking_inbox_dir.test.ts:164-169 | The case named "does not read a stderr redirect as a creating target" measures nothing about stderr redirects: the only redirect target in its command is `/dev/null`, and the assertion passes because `ls` is in the verb allowlist, not because `2>` is classified. `_redirectTargets` DOES capture `2>` targets — probed, `ls -d <opaque> 2> <inbox>/err.log` is judged — so the test's name asserts the opposite of the implemented behaviour, and the assertion cannot go red if the redirect classification changes. Under the roadmap's own emptied-allowlist sensitivity probe it reds for the verb reason, not the named one. | fixed | e69fd4f00 |
| 9 | low | agents/roadmaps/road-to-design-intent-conformance.md:61,102 | Two figures the Context block declares "all measured on this branch" are wrong on this branch: `condense.ts` is 2712 lines, not 2735 (`wc -l`), and `AUGMENT_SYMLINK_DIRS` is declared at `src/scripts/condense.ts:2461`, not :2464. Both are cited as the constraint that shapes Phase 2.1's net-zero requirement. | fixed | e69fd4f00 |
| 10 | low | agents/evidence/reviews/feat-design-intent-conformance.review-input/prompt.md:24 | Scope-integrity note, not a code defect: commit `b798f4fc0` landed on this branch four minutes after this review was dispatched and reverted three of the eight files in scope — `src/rules/brand-source-of-truth.md`, `dist/agent-src/rules/brand-source-of-truth.md`, `src/domains/brand/pack.yaml` — plus 96 lines of the roadmap, flipping step 1.3 and AC-3 back to open. No conclusion about those three files transfers to the branch as it now stands, and this artefact will be read as covering it. For the re-land the reverted table is worth one carry-over: its value row reconciles colour and spacing onto the brand token UNCONDITIONALLY, which needs the same confirmation clause finding 1 names before it lands anywhere. The hook, its tests and both `tailwind-engineer` copies are byte-identical between the scope sha and HEAD, so findings 1-5, 7 and 8 are live. | accepted-risk | Scope-integrity note, not a code defect. The mid-review revert is recorded in the roadmap step 1.3 and in blocker: standing-payload-headroom; the carry-over it names (the reverted value row reconciled unconditionally) is folded into that blocker so the re-land inherits finding 1's confirmation clause. |
