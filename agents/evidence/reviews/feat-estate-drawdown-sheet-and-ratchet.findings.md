# Findings: feat-estate-drawdown-sheet-and-ratchet
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: b075215bd99dac985aff466deda9a5033f75689188669b0dfb9004663077836f | diff: c1789aa28ce4d9c4eb2dba59a271989e40a75667 | reviewer: r2-fresh-subagent-feat-estate-drawdown-sheet-and-ratchet | prompt_hash: e109c88c3c1aac54cfb4b50d807ca34ab3c01de57d2d74cb67b2283d26e6231f -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: c1789aa28ce4d9c4eb2dba59a271989e40a75667
  scope_hash: b075215bd99dac985aff466deda9a5033f75689188669b0dfb9004663077836f
  roadmap: agents/roadmaps/road-to-estate-drawdown.md
  roadmap_hash: 0f63f72e34b0abc0849059a2d49b761f171d65d95161598ecf98cdd3ebeeff4e
  ac_hash: 0308d5582ca2f060845f17b92dd930b0fe6865baf31272e8b3fad4c2880611bd
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T15:54:42Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/check_estate_count.ts:265 | The ratchet baseline is read from the WORKING TREE and no raise-reason is enforced, so the change under review can silence the growth half by editing the number alone — yet the failure text at :559 asserts "a number change on its own is what a ratchet is built to refuse" and roadmap step 3.1 records "the raise-reason discipline for any increase" as shipped. The imported `_lib/ratchet_base_ref` exists for exactly this (`git show <baseRef>:<path>` plus `RATCHET_RESET_KEY`, whose header states a checked-in number "the same commit can edit" is the hole); only its base-ref ladder is used. `estate-count-budget.json` has no reader but this gate, and `baseline_history` is validated by nothing. Both of the diff's own fixtures — self-test case 3 and the test "is RED on an addition with no offset" — raise the baseline bare precisely to make the growth half pass, which is the bypass demonstrated in-tree. | fixed | 9c7e0cfc6 |
| 2 | medium | src/scripts/check_estate_count.ts:518 | `--json` stdout is not valid JSON. `reportScanned` at :492 writes `scanned: N` to stdout unconditionally (its own contract), so stdout is `scanned: 82` followed by the object — reproduced by running the gate. The comment here claims the opposite ("stdout stays parseable … a JSON mode whose stdout needs a second parser is not a machine-readable mode"), and `reportScanned` accepts a writer override for this purpose, used that way in check_review_prompt_binding.ts:488. tests/scripts/check_estate_count.test.ts strips the prefix with a regex before parsing, which turns the defect into the calling convention. | fixed | 9c7e0cfc6 |
| 3 | medium | src/scripts/check_estate_count.ts:175 | `open_blockers` sums the ACTIVE tree only (`collect()` excludes `later/`), so parking a roadmap into `later/` drops the gated blocker count by that roadmap's whole load without resolving anything — and the gate then prints the "free tightening" text at :547, inviting the baseline to be walked down permanently on a burial. estate-count-budget.json:9 states the later/ file count exists precisely to stop "the burial T1's quality anchor exists to prevent"; that protection covers file counts only, while AC-1 is phrased on the blocker count. | fixed | 9c7e0cfc6 |
| 4 | medium | src/scripts/check_estate_count.ts:190 | `isDisposed` recognises only `archive/`, `later/`, `skipped/`, and `countEstate` gates only `later/`, so `agents/roadmaps/stubs/` (6 roadmap files today) is an ungated destination: moving an active roadmap there drops `active_roadmaps` with nothing counting the file, and un-stubbing (`stubs/x.md` to `x.md`, which stubs/README documents as the promotion path) is classified as neither an addition nor an offset, so T3 can never charge it. estate-count-budget.json:11 `not_gated` enumerates archive/ and skipped/ with a terminality rationale and omits stubs/ — the one member of that set that is explicitly non-terminal. | fixed | 9c7e0cfc6 |
| 5 | medium | src/agent-src/scripts/roadmap_gates.ts:403 | The measurement in this shipped docstring is contradicted by the artefact committed in the same change: it states "14 of the 21 live entries record a `Recommendation:` and 7 do not", but the six drafted defaults were written INTO `Recommendation:` fields, so the generator's own header now prints "14 maintainer-recorded · 6 agent-drafted · 0 with no recommendation at all · 1 legacy" — 20 of 21 record the field, 1 does not. The stale figure is the justification this paragraph gives for the provenance column. | fixed | 9c7e0cfc6 |
| 6 | low | src/scripts/check_estate_count.ts:168 | `later_roadmaps` counts every `*.md` under `later/`, including `later/README.md`, so the registered baseline of 44 describes 43 parked roadmaps plus a README — and deleting or renaming that README would register as a "free tightening" of the estate. Inconsistent with `active_roadmaps`, which excludes README.md via update_roadmap_progress's EXCLUDE_NAMES; `isRoadmapCandidate` is applied to the active side only. | fixed | 9c7e0cfc6 |
| 7 | low | src/scripts/check_estate_count.ts:477 | `--base` consumes the next argv token unchecked: `--base` as the final argument yields `undefined` and silently falls back to `resolveBaseRef` instead of erroring, and `--base --json` sets the base ref to `--json`, producing "git diff against --json failed — one-in-one-out not evaluated" plus markdown output on a run the caller asked for as JSON. | fixed | 9c7e0cfc6 |
| 8 | low | src/agent-src/scripts/roadmap_gates.ts:476 | The sheet identifies a legacy blocked-until row by the parser's placeholder id — `## 8 · legacy` and a table cell reading `legacy` in the committed artefact — where the sibling `renderEntry` at :187 deliberately substitutes "blocked-until note" for exactly that id. Two legacy notes would render two rows both labelled `legacy`, separable only by the roadmap column. | fixed | 9c7e0cfc6 |
| 9 | low | src/config/gate-coverage.yml:569 | `min_scanned: 50` over active-plus-later reds when the campaign succeeds: with `active_roadmaps` at the budget's registered `target` of 15, the floor trips as soon as `later/` falls below 35 — a 9-file drain from today's 44. The entry's own note argues the floor sits below 82 so it will not "red on the very change this gate exists to encourage", which is the failure this arithmetic reintroduces one drawdown later. | fixed | 9c7e0cfc6 |
| 10 | low | .github/workflows/consistency.yml:285 | Exit 2 is documented as could-not-run (check_estate_count.ts:58) and `DeadScopeError` maps to it, but neither new call site tolerates 2: this step and taskfiles/ci-fast.yml:1029 fail the build on it, while sibling steps in both files explicitly downgrade 2 to a degraded advisory citing the exit-code contract. docs/contracts/plan-review-gates.md:828 states a dead scan scope is exit 1 "never an internal error", on the reasoning that callers warn-and-allow 2. Neither direction is documented here, so aligning these call sites with their neighbours later would silently disarm the dead-scope assertion. | fixed | 9c7e0cfc6 |
| 11 | low | src/scripts/dispatch_r2_reviewer.ts:506 | The doc at :489 says a bullet ends "at a blank line followed by something that is not an indented continuation", but the code closes the bullet at ANY blank line, so a wrapped AC bullet whose continuation follows a blank line silently loses it; indented sub-bullets under an AC are dropped by the same branch. Behaviour and its stated contract disagree in a function whose purpose is to stop the reviewer receiving a partial AC list. | fixed | 13f7aba78 — superseded on the trunk: origin/main shipped an equivalent inline-AC fix independently, and the merge resolution took its version, so this file left this diff. My repair is no longer the code that ships. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
