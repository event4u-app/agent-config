# Findings: packed-payload-cap
<!-- completion-review: v1 | reviewed: 2026-09-10 | scope: b0d243a9b7b60595103a5a4b69c7d202f902db8311f51db60de8027c1d2d1468 | diff: 368ccf2376e186980c543e311ade4b4e2750da8c | reviewer: r2-fresh-subagent-packed-payload-cap | prompt_hash: abf5ee908a7e02e5e102498c8665ead6d5b951253f7fe539c553197353dec567 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-packed-payload-cap"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-10 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 368ccf2376e186980c543e311ade4b4e2750da8c
  scope_hash: b0d243a9b7b60595103a5a4b69c7d202f902db8311f51db60de8027c1d2d1468
  roadmap: agents/roadmaps/archive/road-to-the-packed-payload-cap.md
  roadmap_hash: 3d3ffebb0178c1286432e2bfa52b22ea03dca27ea53502ec46f01b37b9779b35
  ac_hash: b265507e57579f2021bc38749796c9fa131ae7566a0c2fdc1629b0f973601dfb
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-10T04:32:13Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | package.json:83 | The clean covers 1 of the 3 esbuild invocations that share `--outdir=dist/cli-delegate --splitting`. `build:agent-src-delegate` (:84) and `build:delegate-aux` (:85) still never clean, so the content-hashed-chunk accretion resumes on any standalone or incremental run of either. ADR-273 says the prefix means cause 2 "cannot recur" and the roadmap says "Fixed in this change" — both overstate what shipped. The durable fix is a clean bound to the outdir (one prefix on the first of the three plus the same on the other two, or a separate `clean:cli-delegate` all three depend on), not to one script. | fixed | e1-fix — the clean is bound to the OUTDIR, not to one script: new `clean:cli-delegate` plus a `build:delegates` composite that runs it once and then all three producers, with `build` calling the composite. ADR-273 Decision bullet 3 and roadmap 1.1 rewritten to what shipped (368ccf237) |
| 2 | high | package.json:32-70 | Cause 1 got no durable fix. ADR-273 names the exact mechanism — `files[]` lists `src/scripts/` and `.npmignore` cannot withhold what `files[]` admits — and the remedy recorded is "removing the local caches", a one-time deletion. `files[]` already carries eight `!` negations for precisely this purpose, yet no `!**/__pycache__/**` or `!**/*.pyc` was added, so the binary axis returns to 28-observed-against-3-allowed the moment anyone regenerates the caches, and a publish from such a tree ships them. `.npmignore:12-13` already lists both patterns, which makes the tree read as covered when it is not. Note the asymmetry: the sibling cause in the same paragraph did get a durable fix in the same diff. | fixed | e1-fix — `!**/__pycache__/**` and `!**/*.pyc` added to files[]. SENSITIVITY PROVEN: a planted 200 KB .pyc under src/scripts/__pycache__ stays out of the pack (0 entries), and the reviewer was right that the existing subtree assertion is untouched (368ccf237) |
| 3 | medium | package.json:83 | The new `rm -rf` introduces an undeclared ordering precondition. It is correct only because `build` (:92) happens to run `build:cli-delegate` before the other two producers. Run in any other order — a targeted rebuild after editing an agent-src script, or a future reorder of the `build` chain — it deletes their output and nothing regenerates it in that invocation, producing a tarball silently missing those delegates with no error. Nothing in package.json records the constraint. | fixed | e1-fix — the precondition is gone rather than documented: the clean is its own step and the composite fixes the order, so no standalone producer can delete a sibling bundle (368ccf237) |
| 4 | medium | tests/scripts/pack_payload_reduction.test.ts:77-87 | The only assertion that bounded `max` absolutely (`max < 9.2`) was deleted and not re-anchored to the new value. The replacement pair — `max <= last_measured * 1.095` and `max > last_measured` — is satisfied by raising both numbers in lockstep, which is exactly the "cap nudged up to clear a failing check" the block's own doc-comment says it is guarding against, and nothing anywhere pins `last_measured` to a measurement. ADR-273 §Consequences claims "the caps may only walk down"; after this diff that property is asserted by nothing. Re-anchoring (`max <= 11.5`) would have kept the ratchet and still been updatable at the next recorded reset. | fixed | e1-fix — `max <= 11.5` re-anchored beside the derivation check, and the block says in prose that it is deliberately a moment pin updated at each recorded reset (368ccf237) |
| 5 | medium | tests/scripts/pack_payload_reduction.test.ts:95-102 | The test named "every cap value in the file is reachable from a dated baseline note" does not test that. `notes.length >= 4` is already 6 today, the count only ever grows, and it is tied to nothing about the number of cap values — so the move its own comment says it exists to catch ("A raise landing without one") leaves it green. The comment is also wrong on the count it reasons from: it lists four resets, while the regex matches six keys, including `baseline_note_2026_08_10` and `baseline_note_2026_08_13`, the latter being a `per_skill_share` exception rather than a packed-size reset at all. | fixed | e1-fix — the count assertion is gone. Replaced by one that resolves the NEWEST dated note and asserts it names the cap value in force, which is what "a raise landing without one" actually breaks. SENSITIVITY PROVEN: max 11.5 -> 11.4 reds it (368ccf237) |
| 6 | medium | src/config/pack-size-budget.json:78 | The 0.0352 MB gap between the two built readings (12.4891 vs 12.4539) is declared "fully attributed rather than waved at", but one of the two attributed causes is `dist/catalog-index-v1.json` at "0.225 MB" present in one tree and absent from the other. A 0.225 MB presence-or-absence cannot net to a 0.035 MB gap. Either that figure is unpacked — which `packed_attribution.method` in the same record explicitly forbids reasoning from, calling it "the error the council made in rounds 1 and 2" — or the attribution is incomplete. The built comparator 12.4539 and the derived ceiling 13.699 rest on the reading this sentence certifies. | fixed | e1-fix — and the reviewer caught this record making the exact error it accuses the council of. MEASURED by files[] negation: the catalog packs at 0.0437 MB, not 0.225 (unpacked). The gap now reconciles: 12.4891 - 0.0437 + 0.0094 = 12.4548. Both units stated wherever the file is named (368ccf237) |
| 7 | medium | src/config/pack-size-budget.json:8-19 | The 9.47 % headroom is measured on a tree whose `dist/` carries only tracked content, which is not the state the gate usually judges. The roadmap in this same diff measures `dist/mcp` + `dist/ui` at 0.601 MB packed and records that a checkout carrying those but not `dist/cli` is classified UNBUILT by `payloadIsBuilt` and therefore judged against `max`. In that state — which the roadmap describes as the ordinary post-test workstation tree — real headroom is about 0.39 MB (3.7 %), not the 0.994 MB the reset was sized for. The stated outcome "task ci discriminates again" is exposed to the same mechanism that made it stop discriminating, and no step in the change addresses the classifier. | accepted-risk | recorded not fixed, in a named `headroom_is_sized_on_one_tree_state` key and in ADR-273 Consequences with the ~3.7 % figure. Widening `payloadIsBuilt` is a gate change, outside a roadmap whose own text says the change must be only the budget move. Revisit-if stated (368ccf237) |
| 8 | low | tests/scripts/pack_payload_reduction.test.ts:7-20 | The file-level docblock still says the suite asserts "that the cap moved DOWN". That assertion was deleted in this diff and the cap moved up. The describe-block comment below it was rewritten; the file header was not. | fixed | e1-fix — the file-level docblock now says the suite asserts the cap is DERIVED, and names the superseded clause rather than silently dropping it (368ccf237) |
| 9 | low | tests/scripts/pack_payload_reduction.test.ts:60-92 | The new doc-comment argues that pinning a superseded moment is wrong because such a test "fails on the change that supersedes it and says nothing about whether that change was sound", then the block pins two new moments: the literal `HEADROOM IS 9.47 %, NOT 9.5 %` and the literal `"baseline_note_2026_09_10"`. Both must be hand-edited at the next reset, which is the maintenance cost given as the reason for deleting `HEADROOM IS 7.4 %, NOT 8 %`. The generalisable form (a headroom literal exists in the newest dated note and matches the computed value) was available and not taken. | fixed | e1-fix — the headroom literal is COMPUTED from max and last_measured, so no reset has to hand-edit it. The one remaining pin (`max <= 11.5`) is kept deliberately, because finding 4 is right that the derivation alone is satisfiable in lockstep; the trade is stated in the block (368ccf237) |
| 10 | low | src/config/pack-size-budget.json:19 | The note that justifies the reset carries four renderings of the unbuilt reading without saying which tree each came from: the derivation uses 10.5056, the accretion sentence uses 10.5035, and the roadmap's verify line uses 10.503 and 10.506. 10.5035 appears once and is never labelled — it is presumably the pre-branch `origin/main` reading rather than the branch reading `last_measured` records, but the note does not say so, in a file whose stated discipline is that a rounded or unlabelled figure is how a previous entry had to be corrected. | fixed | e1-fix — all four renderings labelled in `baseline_note_2026_09_10`: 10.5035 pre-branch, 10.5056 mid-edit, 10.5090 final and what last_measured carries, 10.509/10.508 the gate rendering. The self-reference bound (<0.005 MB against 0.99 MB headroom) is stated (368ccf237) |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
