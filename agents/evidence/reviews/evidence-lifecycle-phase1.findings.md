# Findings: evidence-lifecycle-phase1
<!-- completion-review: v1 | reviewed: 2026-08-15 | scope: bcdcb5b97d5b6aca78e6b9fc9554702d8cf59948139cc9748f7a975ed0909058 | diff: 5efb15097a8567ce2666b06669dbcbdb45e90bfb | reviewer: r2-fresh-subagent-evidence-lifecycle-phase1 | prompt_hash: 1c78d83b83f9352e77e59b1e8e8499e2a636ee8dec993087d19f5a92872a4fe2 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 5efb15097a8567ce2666b06669dbcbdb45e90bfb
  scope_hash: bcdcb5b97d5b6aca78e6b9fc9554702d8cf59948139cc9748f7a975ed0909058
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-c-evidence-lifecycle.md
  roadmap_hash: 2bc23b8e24739e5b7b4262f5d3ec1b41c4ca06d252035c7f66878edcc583aa5b
  ac_hash: 694bb735c11ffdfc843d3ab31cd5f9393745195f4647c9b5ef199fe34d685a26
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-15T07:07:13Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/probe_review_binding_drift.ts:206 | The `code` bucket absorbs trunk-merge churn, so the deciding ratio does not measure what its own row label claims. `RebindCause` (lines 75-79) states that a re-bind caused by the merge base moving must NOT be counted as `code`, because "counting these as `code` would overstate the gate's usefulness". The implementation cannot honour that: the span is `previousState...commit`, and when `previousState` is an ancestor of `commit` a merge of the trunk into the branch brings all of trunk's paths into that span, so the event is classified `code` and only an event where literally nothing in-scope changed reaches `base-moved`. Verifiable from the shipped report alone: 18 of the 81 events carry a commit subject naming a trunk merge as the reason for the re-bind (`408b56162`, `ab4272b98`, `dd58e0c7c`, `eb84ecb6c`, `756b226de`, `3b4bdb6a0`, `6518a6a12`, `cfd269516`, `3fc625f8b`, `a2d1b7803`, `3e03c75c4`, `7184ae89e`, `8f852d3c0`, `fb5426dd0`, `8c85ea05c`, `04a92fca8`, plus two already classified non-code), and 16 of those sit inside the 64-event `code` bucket. The published "Code changed - the review correctly noticed it, 64, 79.0 %" therefore includes about a quarter of its own bucket where no branch author touched a reviewed file. Correcting it moves code to roughly 48 of 81 (59 %), which still dominates, so the STOP outcome survives - but the number the decision cites is not the number the classifier computed. | fixed | 5efb15097 |
| 2 | high | agents/evidence/analysis/review-binding-drift.md:22 | `base-moved` is computed as a residual but published as a stated cause. The classifier assigns it when `paths.length === 0` (probe line 207) - that is "nothing else explained this", not "the merge base moved" - yet the decision table row reads "No in-scope path changed at all - the merge base moved", and Decision finding 1 (line 34) builds on it: "In the 7 `base-moved` events no in-scope path changed at all - the scope is `base...HEAD`, so merging the trunk into a branch rewrites the diff". The report's own event table contradicts that for at least two of the seven: `2ebdb87f0` is "re-derive the context-manifest scope_hash for the re-bound round (contract 5 header-manifest agreement)" and `85e703c65` is "re-bind the context manifest, not only the marker" - both are corrections of a wrongly recorded hash, which no merge base explains. The same sentence is repeated in the generated section (line 102, probe line 476) and in the roadmap step 1.2 note (roadmap line 68), so the unestablished cause propagates into three artefacts. A residual bucket should be labelled as one ("no in-scope path changed - cause not attributable from the diff") rather than asserting the mechanism the plan happened to expect. | fixed | 5efb15097 |
| 3 | medium | src/scripts/probe_review_binding_drift.ts:357 | `retention()` enumerates findings artefacts, never directories, so a `review-input/` directory with no matching `*.findings.md` is invisible to the retention pass with no diagnostic. It iterates `rows` (built at line 221 from `readdirSync(REVIEWS_DIR).filter(f => f.endsWith('.findings.md'))`) and only skips a row whose directory is absent (line 361); the reverse case is never enumerated. `agents/evidence/reviews/pr-target-base-freshness.review-input` is tracked, predates this branch, and holds 44 kB, and it appears in neither the tier table nor the re-derivability table - 29 such directories existed at measurement time and 28 are reported. That falsifies acceptance criterion 4, "Every `review-input/` directory carries a retention tier and a reproducible-or-not verdict", which the roadmap marks `[x]` (roadmap line 169), and it understates the 3.11 MB total that the roadmap's blocker note now uses to bound `evidence-compaction-approval` at 33 %. Step 3.1's own `verify:` line says "every one of the 28", so the check was written against the roadmap's assumed count rather than against the directory listing, and could not catch the omission. | fixed | 5efb15097 |
| 4 | medium | src/scripts/probe_review_binding_drift.ts:370 | An unresolvable `origin/main` is silently reported as a substantive verdict rather than as an unavailable input. `gitOrNull('merge-base', '--is-ancestor', head, 'origin/main')` returns null both when the head is not an ancestor and when the ref does not resolve at all, so on any checkout without that ref (a consumer clone, a differently named remote, a CI fetch that pins a SHA) every head reports not-merged and every directory falls into `active`/`recent` - tiers the report's own legend defines as factual claims about the trunk. `tryReproduce` has the same shape at line 348: an unresolvable `origin/main` makes both candidates fail and the row degrades to "no - not re-derivable from the recorded head alone", inflating the byte total the report says "stays regardless". The file already knows how to handle this class honestly - `eraCommits()` returns null when its tags do not resolve and the renderer prints "section skipped" (line 285 and the render branch at 488) - so the inconsistency is within one module, not a missing convention. | fixed | 5efb15097 |
| 5 | medium | src/scripts/probe_review_binding_drift.ts:371 | The tier assignment conflates "scope hash moved" with "scope hash was not comparable". `tier = merged ? 'archived' : r.scope === 'same' ? 'active' : 'recent'` treats every non-`same` verdict as `recent`, but `SegmentVerdict` also carries `no-input`, `missing` and `prose-bound` (line 711 of the diff, type declaration), none of which mean the binding moved - `prose-bound` means the artefact has no manifest to compare, `no-input` means there was no stored patch to compare against. The rendered legend then asserts the stronger reading: "`recent` = re-bound, and the reviewed head is not yet in the trunk" (report line 212). Eleven artefacts in this tree are `prose-bound` and several are `no-input`; the defect is latent only because every head happened to be merged in this run, so the entire population landed in `archived`. | fixed | 5efb15097 |
| 6 | low | src/scripts/probe_review_binding_drift.ts:546 | The count and the byte figure on the same line are computed over different populations. `unproven` matches `reproducible.startsWith('no')` (line 524), but the bytes printed beside it are `totalBytes - provenBytes`, which also absorbs any row whose verdict is `'n/a - no stored patch'` (line 391) - a row that is in neither `proven` nor `unproven`. "Not re-derivable from the record: N - X MB" would then pair a count that excludes those directories with a byte total that includes them, and the derived compaction bound at line 551 inherits the same mismatch. It did not fire in this run (10 + 18 = 28), so this is a latent inconsistency rather than a wrong published number. | fixed | 5efb15097 |
| 7 | low | src/scripts/probe_review_binding_drift.ts:551 | `((provenBytes / totalBytes) * 100).toFixed(0)` has no zero guard, so an empty or absent `review-input` population renders `NaN %` into the sentence that bounds the `evidence-compaction-approval` blocker. The file already defines `pct()` (line 434 region) precisely to avoid this, returning `n/a` when the denominator is zero, and uses it for all three ratio-table rows; this one call site bypasses it. | fixed | 5efb15097 |
| 8 | low | src/scripts/probe_review_binding_drift.ts:198 | Every artefact revision is fetched from git twice. Inside the walk, `hashAt(commits[i])` and `hashAt(commits[i+1])` are both evaluated each iteration, so each commit's blob is materialised once as `now` and again as `before` on the next pass - `git show` is invoked 2N times where N would do. Carrying the previous iteration's hash forward in a local halves the subprocess count across 52 artefacts and their full histories, and the loop needs no other change. | fixed | 5efb15097 |
| 9 | low | src/scripts/probe_review_binding_drift.ts:202 | A failed `git diff` silently removes a re-bind event from the denominator of the published ratio. When `gitOrNull(...reviewScopeNameOnlyArgs(...))` returns null the transition is `continue`d away, so the event vanishes from `events` rather than being reported as unattributable. `gitOrNull`'s contract (line 152 region) is "a failure is an answer" and swallows stderr deliberately, which is right for probing a commit for a file it predates - but here the swallowed failure changes a percentage that a recorded decision cites, and nothing in the output would show that the event count is short. An `unattributable` cause value would keep the denominator honest. | fixed | 5efb15097 |
