# Findings: drain-roadmap-6-one-resolver-r4
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: 0f3ce1c5d2f0298bf32669f13a298c2a04a1d8a04d0faa21e8ee8da865241729 | diff: 68cd4a8084496db0b29a8930a6c1d11902748323 | reviewer: r2-fresh-subagent-drain-roadmap-6-one-resolver-r4 | prompt_hash: a20dd4f1ba5feedb8d715719e050bb28ca2146ac0a4e0d786f289a6513cf765e -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-roadmap-6-one-resolver-r4"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 68cd4a8084496db0b29a8930a6c1d11902748323
  scope_hash: 0f3ce1c5d2f0298bf32669f13a298c2a04a1d8a04d0faa21e8ee8da865241729
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: cef440e56ec594875ba36adc8a7aa1368b49898fc43b70367ca5c352ae033c5a
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T12:56:42Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/one_resolver_invariant.ts:182 | `exportsRouterFunction` recognises a router only when it is declared **inline** — a `FunctionDeclaration`/`ClassDeclaration` with an export modifier, or a variable whose initializer is literally an arrow / function / class expression — so five ordinary, behaviour-preserving spellings of the SAME resolver are rejected and `checkOneResolver` emits `resolver-is-not-a-resolver`. Measured end-to-end on synthetic trees (all RED, baseline clean first): `function classifyLadder(x){return x}` + `export { classifyLadder };`; `export default classifyLadder;`; `export const classifyLadder = ((x)=>x) as (x)=>unknown;`; `export const classifyLadder = ((x)=>x) satisfies unknown;`; and the rename `export function resolveLadderRung()`. The unsoundness is **internal to this module**: `exportedNames`/`declaresRouter` DO recognise `export { X }` and `export default X` (verified: `declaresRouter`=true, `exportsRouterFunction`=false on the identical source), so one syntax counts as "declares a router" for every other file and as "is not a resolver" for the sanctioned one. The emitted detail at :283-289 then asserts the opposite of the truth — "the resolver it anchors has gone somewhere this guard does not look" — while `classifyLadder` is a function two lines above. INSIDE the frozen claim, which reads "that the resolver itself exports a callable one" (roadmap:236), not "declares one inline". It is a false POSITIVE on conforming code, so it blocks an ordinary refactor of `judgment_ladder.ts` rather than admitting a second resolver, and it defeats the positive half of AC 1 as a *check* (the property asserted is not the property implemented). R3 finding 2 named this direction verbatim ("Fires on a legitimate resolver: renaming the entry point to `resolveLadderRung` … produces `resolver-is-not-a-resolver` with that same misleading detail") and is marked `fixed`; only its false-negative half was repaired. Scope for the council's stopping rule: ONE visitor omission in ONE function — `exportsRouterFunction` never visits `ts.isExportDeclaration` / `ts.isExportAssignment` and never unwraps `ParenthesizedExpression`/`AsExpression`/`SatisfiesExpression`, all of which `exportedNames` already handles. | open | |
| 2 | medium | src/scripts/_lib/one_resolver_invariant.ts:239 | `walk` admits a file only on `entry.name.endsWith('.ts')`, so `.tsx`, `.mts` and `.cts` modules under `SCAN_ROOT` are never read and a second task-side resolver in one of them scans GREEN. Measured end-to-end: `src/ui/Router.tsx`, `src/scripts/r.mts` and `src/scripts/r.cts`, each containing a literal `export class CouncilTopologyRouter {}`, all return zero violations, while the same body in `src/scripts/r.ts` returns `second-resolver`. This is live, not theoretical: the tree carries **28 `.tsx` files under `src/`** today, and the gap is *partially* masked — `src/ui/` contributes 14 `.ts` paths to `scanned`, so the directory appears covered in the anti-vacuity report while two thirds of its modules are invisible. The same discriminator that R1 finding 1 added (`scanned.length > 100`) cannot see it, exactly as R2 finding 1 and R3 finding 1 could not be seen by it. Disclosure: the only statement of the limit is the private helper's one-line docstring at :238 ("Walk `.ts` files"); the module's frozen-claim block (:63-72) says "no module outside the sanctioned resolver EXPORTS A BINDING WHOSE NAME matches", `SCAN_ROOT` is documented as `'src'` (:76), and the roadmap's "What this step does NOT close" (roadmap:261-265) names only the third 0.5 clause. Per the round's stopping rule this sits OUTSIDE the *syntax* policy but the guard's name, its diagnostic and its documentation all imply whole-tree coverage, and a private-helper caveat is the buried kind the rule declares insufficient. Same class as R1 finding 3 (undisclosed scan-scope limit), recurring on the file-extension axis after being repaired on the directory axis. | open | |
| 3 | low | src/scripts/_lib/one_resolver_invariant.ts:140 | `exportedNames` visits `ts.isModuleDeclaration` for the declaration's OWN name but never walks its body, so a router declared inside a namespace or an ambient module block is invisible while the enclosing form is caught. Measured: `export namespace CouncilTopologyRouter {}` → detected; `export namespace Dispatch { export class CouncilTopologyRouter {} }` → `exportedNames`=[] and `checkOneResolver` GREEN; `declare module "d" { export class CouncilTopologyRouter {} }` → GREEN. The same asymmetry holds for a plain `namespace N { export class CouncilTopologyRouter {} }`. I place this on the OUTSIDE side of the frozen line — the module-level binding is `Dispatch`, so the router is reachable only as a qualified name, which the claim's own limit ("a router exported under an unrelated name … is outside the claim") arguably already covers — but the limit is stated as a *symbol-resolution* limit, and nothing anywhere says module/namespace bodies are unwalked while `isModuleDeclaration` is explicitly in the visitor list. Reporting it so the asymmetry is a recorded decision rather than an accident; no live instance exists in the tree. | open | |
| 4 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:251 | Two closure-record claims are wider than the artefact, the same class as R1 finding 6 / R2 finding 5 / R3 finding 5. (a) "Every reproducer from all three killed rounds is retained as a permanent regression" — R3 finding 4's false-POSITIVE reproducer (a template literal in the resolver holding `import { x } from '../ai_council/y.js';`) has no test; `grep -n 'template' tests/scripts/one_resolver_invariant.test.ts` returns only the dynamic-import *specifier* row at :230, and the nearest pin is the plain-string arm at :247. R3 finding 1's false-positive reproducer (regex backtick plus a string carrying the router name) is likewise unpinned; only its false-negative half is at :190. Both behave correctly today under the parser — verified false/false — so the gap is the retention claim, not the behaviour. (b) "What this step does NOT close" (:261-265) names only the third 0.5 clause and therefore omits both scope limits this round measured: the `.ts`-only file filter (finding 2) and `exportsRouterFunction`'s inline-declaration restriction (finding 1), the latter contradicting the frozen claim's own wording at :236. Verified accurate in the same prose and worth recording: 60 tests exist and pass, `tsc --noEmit -p tsconfig.json` and `eslint` on both files exit 0, the four sensitivity arms each call `expectCleanBaseline()`, the real-tree test asserts `scanned` > 100 and contains the resolver by path (1240 files scanned, 0 violations), the empty-tree arm pins `scanned: []` + `resolver-missing`, the round table's 12 / 54 / 231 figures match R2 finding 1 and R3 finding 1 verbatim, and `src/scripts/check_portability.ts:741` does hold `const re = /` + backtick + `([^` + backtick + `]+)` + backtick + `/g;`. | open | |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
