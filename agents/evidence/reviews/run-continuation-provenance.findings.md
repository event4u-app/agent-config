# Findings: run-continuation-provenance
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 3fbe0a1736aaa05d5e35ff433b98faa8ea052fa9cc6c2a1d2f40f95fbb1ec655 | diff: d8a4cdcaaffc5be7fa095f69963de226d68a4925 | reviewer: r2-fresh-subagent-run-continuation-provenance | prompt_hash: 8a225a58adfd95be9943bb5e0060efabc52d27e3f15e061e630d4aabca0de3b8 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: d8a4cdcaaffc5be7fa095f69963de226d68a4925
  scope_hash: 3fbe0a1736aaa05d5e35ff433b98faa8ea052fa9cc6c2a1d2f40f95fbb1ec655
  roadmap: agents/roadmaps/road-to-run-continuation-observation.md
  roadmap_hash: f2e8583a8b6f8c45f109f59938f369582efe5dd95bcdae2473fa3c4a80a26a04
  ac_hash: bb34537a4ce90a2ac144c0346d9d3817fc8ddd788722900f17cdb6b7ed59bea7
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T20:16:52Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/run_continuation_hook.ts:1044 | `absent_fires` is never reset on a successful read. `readState` round-trips it (:589) and the engage and halt writes persist `state` verbatim (:1290, :1280), so it counts a LIFETIME total rather than consecutive fires — the field's own docblock (:195-203) asserts the opposite ("Reset by any successful read (the engage/halt paths never carry it forward)"). Consequence: two NON-consecutive absences in one run (two unlink-then-write windows, a transient EACCES, a checkout without the file) reach `ABSENT_CONFIRM_FIRES` and delete a LIVE budget — iteration 20 becomes iteration 1 with a fresh 4 h clock — and that second clear emits NO ledger line at all, because the line is gated on `absentFires === 1` (:1045). Round 7 finding 1's defect, restored and now silent. | open | |
| 2 | high | src/scripts/hooks/run_continuation_hook.ts:1289 | The engage-path state write is `try { writeState(...) } catch { return EXIT_ALLOW; }`, but the extracted `writeState` (:543-550) swallows every error in its own `catch { /* fail-open */ }`. The outer catch is now unreachable dead code, so the protection documented inside it — "If the counter cannot persist, the ladder cannot bound the loop, so the loop must not run" — no longer exists. With an unwritable state dir (EACCES, read-only mount, ENOSPC) the concern returns EXIT_BLOCK on every stop while `iterations` stays frozen at 0, so `halt-max-iterations` is never reached: an unbounded re-engagement loop, the exact failure the branch existed to prevent. | open | |
| 3 | high | src/scripts/hooks/run_continuation_hook.ts:458 | `ladder` maps `openCount === 0` to `complete` with no regard for `scan.blocked`, and `scan.open` excludes every `blocked-by:`-marked step (:391). A scope whose only remaining open boxes are blocked therefore emits `event: complete` and unlinks the state (:1269) while the roadmap's `count_open` is still > 0 — the state ADR-235 and the new terminal-outcomes sections (src/agent-src/contexts/execution/roadmap-process-loop.md:516, src/domains/product-basic/roadmap/process-full/command.md:139) define as `blocked` and forbid in an Iron Law: "`blocked` IS NEVER PRESENTED AS COMPLETION. count_open STAYS > 0." The ledger the acceptance criteria are counted from thus records partial progress as a completion, and the budget is cleared under the "only a COMPLETION clears the state" rule. Neither the ADR nor either doc mentions this concern. | open | |
| 4 | medium | src/scripts/hooks/run_continuation_hook.ts:1269 | The legacy per-session state file is read for migration (`readRunState`, :516-538) but never deleted or invalidated, so both clearing paths become no-ops once a legacy state has been adopted. On `complete`, `fs.rmSync(stateFile)` removes only the keyed file; the next claim of the same roadmap by the same session re-adopts the legacy file (an absent `roadmap` field is trusted) and resumes the finished run's `iterations` and `started_at`, which is the round 5 finding 2 defect reachable again and contradicts the comment's promise that a later run "starts a fresh budget". The absent branch has the same hole as a loop: the confirm-delete at :1069 is followed on the next absent fire by re-adoption of the legacy state, so `absentFires` returns to 1, another `halt-roadmap-absent` line is appended, and the cycle repeats every two fires without the budget ever being reclaimed. | open | |
| 5 | medium | src/scripts/hooks/run_continuation_hook.ts:1045 | When the keyed state file exists but cannot be parsed (`driven === null` with `from === 'keyed'` — the case :1040-1043 deliberately does not delete), `absentFires` recomputes to 1 on every fire because nothing is persisted, so `halt-roadmap-absent` is appended on EVERY subsequent stop. The header (:44) says that event fires "ONCE" and this branch says "the ledger line is written ONCE (on the first)"; both are false on that path. Unbounded duplicate records in the ledger the acceptance criteria count from — the defect class round 6 finding 6 fixed for halt lines, one branch over. | open | |
| 6 | medium | src/scripts/hooks/run_continuation_hook.ts:1107 | The new `inert-transcript-over-cap` event carries none of the seven provenance fields. The header (:75-82) states as the file's canonical contract that "Every event this concern writes carries `workspace_root`, `session_root`, `session_cwd`, `git_dir`, `git_common_dir` and `claim_path` ... plus `roadmap_path`. Seven fields", and warns that the count exists so a later edit dropping a field would have to contradict a documented promise — this edit contradicts it silently. `sessionRoot`, `claim.path`, `payload['cwd']` and `roadmapFile` are all in scope there, so nothing forced the omission, and this is the one event where a ledger-only reader most needs the two-tree fields since an inert run emits nothing else. No test asserts the every-event contract and no test covers this rung at all, so the gap is uncaught in both directions. | open | |
| 7 | medium | src/agent-src/contexts/execution/roadmap-process-loop.md:196 | Two disjoint definitions of "blocked" ship in one branch. § 3c decides runnability from the roadmap's `## Blockers` section (pre-existing entry, `Class: 2` or `3`, `Blocks:` naming the step) and explicitly "and nothing else"; the armed stop concern decides it from a `blocked-by:` marker on the step's own line (src/scripts/hooks/run_continuation_hook.ts:391). A run whose remaining steps are non-runnable under § 3c but carry no line marker has `scan.open > 0`, so the concern returns EXIT_BLOCK and pushes "Continue with this step now" at work the roadmap declares human-only: the documented `blocked` terminal outcome is unreachable while the concern is armed, and the run instead burns iterations to `halt-stall` or `halt-max-iterations` and is recorded in the ledger as a stall rather than as blocked. Neither `blocked` nor `blocked-preflight` exists in `LadderAction` or the event vocabulary. | open | |
| 8 | low | agents/roadmaps/road-to-run-continuation-observation.md:26 | The sentence this diff rewrote updates one of its two count claims and leaves the other stale. The integration count gained a precise as-of qualifier ("7 cases when this roadmap was written; the file holds 26 at the close of this branch, of which 24 drive the dispatcher and 2 only read the hook manifest" — verified accurate: 26 `it(` cases, 2 before the dispatcher describe at :352 and 24 after), while the adjacent "it is unit-tested (21 cases)" stays undated and is wrong — tests/scripts/hooks/run_continuation.test.ts holds 34. Same sentence, same class of falsifiable claim, in a branch whose own code comments argue that a stated count is load-bearing. | open | |
