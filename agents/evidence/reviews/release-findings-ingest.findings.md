# Findings: release-findings-ingest
<!-- completion-review: v1 | reviewed: 2026-09-11 | scope: 1754f56af6dc55ed37b21f3c2247d2462b4c725c7ca2ffbc0dcd773a42e49c09 | diff: b8cddc341b9c0e29ce58767cef66c7c96f1f6eaa | reviewer: r2-fresh-subagent-release-findings-ingest | prompt_hash: d8eeeb1e5cc080c7d4d8c9dad95f1526f5a524a762b78d49f4aea22fc8985e13 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-release-findings-ingest"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-11 -->

<!-- context-manifest: v1
inputs:
  diff_sha: b8cddc341b9c0e29ce58767cef66c7c96f1f6eaa
  scope_hash: 1754f56af6dc55ed37b21f3c2247d2462b4c725c7ca2ffbc0dcd773a42e49c09
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-11T05:38:56Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/release.ts:1068 | On the step's PRIMARY path the operator is shown `scanned: N` and nothing else. `(verdict.stdout \|\| verdict.stderr)` prefers stdout, and on the undispositioned-findings path `check_finding_dispositions` writes `scanned: N` to **stdout** (via `reportScanned`, `_lib/scan_scope.ts:119`, which runs BEFORE the problem list) while the per-finding diagnosis goes to **stderr**. Verified by running the gate against a fabricated ledger with one undispositioned critical/security finding: stdout was exactly `scanned: 1`, exit 1, and the "- `<id>` (critical security: …): no disposition status" lines were on stderr. So the release stops and the whole reason it stopped is discarded — this is the exact case the step exists to produce, and the one where the message has to carry content. Use `[stdout, stderr].filter(Boolean).join('\n')`, or read stderr first. | open | |
| 2 | high | src/scripts/release.ts:1052-1055 | Step 7 pushes a new commit to the release branch AFTER step 6 waited for checks, and step 8 never re-waits. `merge_release_pr` (src/scripts/release_publication.ts:840-878) calls `gh pr merge` directly and only re-runs `watch_pr_checks` on the `BEHIND` (moving-base) path — a head whose checks were invalidated by this step's own push is `BLOCKED`/pending, not `BEHIND`. Two outcomes, and which one occurs is a race with GitHub's check-status propagation: the merge is refused and step 8 throws `CalledProcessError`, or it succeeds on a head whose required checks never ran — including `finding-dispositions`, the very job this change exists to satisfy, and `surface-equality`/`release-shape`, which then gate nothing for this release. Either re-wait after the push or move the commit so the merge follows a settled head. | open | |
| 3 | high | src/scripts/release.ts:1063-1073 | A review that found NOTHING deadlocks the release with a self-contradictory message. An artifact with `findings: []` (the clean outcome) ingests to a ledger carrying an empty array and no `no_findings_reason` — `merge_ingest` never writes that field — and `empty_ledger_problem` (src/scripts/check_finding_dispositions.ts:524-532) returns 1 for exactly that shape. Step 7 then dies with `undispositionedMessage(version, 0, …)`: "**0** blocking self-review finding(s) … carry no disposition … Fill each {status, rationale, verified_by}". There is nothing to fill, the instruction cannot be followed, and `--resume` re-enters the `present` path and repeats the same death forever. Reproduced end to end: `--ingest` of a zero-findings artifact into a temp dir wrote `{…, "findings": []}` with no reason, and the gate then exited 1 on it. | open | |
| 4 | high | src/scripts/release.ts:1027-1034 | The `no-run` outcome the module designed for a missing artifact is unreachable in the common case; the release dies with a raw `gh` error instead. `pickRun` (src/scripts/_lib/release_findings_ingest.ts:82-86) keys on the run's **conclusion**, but the artifact is **conditional**: `self-review-gate.yml` uploads with `if-no-files-found: ignore`, and `self_review_gate.ts` returns 0 without writing the file on at least five paths (no `ANTHROPIC_API_KEY` → NEUTRAL:761-773, no reviewable files:754-756, zero chunks:783-803, no chunk completed:840-843, any exception:889-895). Each of those yields a FINISHED run with conclusion `success` and no artifact, so `planIngest` returns `ingest`, `gh run download --name self-review-findings` fails, and `run(…, {check: true})` dies with "command failed (1): gh run download …". The `if (!fs.existsSync(artifact)) die(…)` guard below it never fires, because the download dies first. Eligibility must be artifact presence, not run conclusion. | open | |
| 5 | high | src/scripts/release.ts:1023-1025 | `planIngest`'s `ledgerExists` probes the WORKING TREE (`fs.existsSync(abs)`), while the message and the whole design claim the branch carries the file. Two reachable divergences. (a) If `push_release_branch` dies at :1055 — the function exists precisely because pushes fail here (pre-push gate, credentials, protection) — the ingest commit is already local. A later `--resume` takes the `present` path, prints "ledger already on the branch" (false: it is local only), never retries the push, and when the ingested findings carry nothing blocking the verdict passes and step 8 merges a PR head WITHOUT the ledger — silently restoring the six-release failure this change exists to end, under a line asserting the opposite. (b) A stray untracked `X.Y.Z.json` — e.g. from the by-hand `--ingest` the old docs instructed — produces the same false skip. Probe `git ls-tree <branch> -- <rel>` / `git cat-file -e origin/<branch>:<rel>`, not the filesystem. | open | |
| 6 | medium | src/scripts/release.ts:1067-1073 | `verdict.returncode !== 0` is reported as "undispositioned" whatever the cause. `check_finding_dispositions` returns 1 for an unexplained empty ledger and 2 for a malformed ledger or an undeterminable release status; all three are rendered as "N blocking self-review finding(s) … carry no disposition". `_blocking_without_disposition` (:1086) compounds it: it counts only `isBlocking(f) && !f.status`, while `missing_dispositions` also fails an unknown status, an empty `rationale`/`verified_by`, and `status: "fixed"` without a `commit`. A ledger whose blocking findings are all `fixed` but commit-less therefore stops the release with the message "0 blocking … carry no disposition", and a parse failure in `_blocking_without_disposition` silently returns 0 as well. | open | |
| 7 | medium | src/scripts/release.ts:1013-1021 | Three distinct states collapse into one warn-and-continue. A non-zero `gh run list`, unparseable JSON, and a genuinely absent run all become `runs = []` → `no-run` → a printed warning, `return`, and the release proceeds to merge and tag with no ledger. That IS the six-release failure mode, now reachable through (a) `--no-wait`, where the review run has not finished and `pickRun` filters `conclusion: null`, (b) any transient `gh` failure of the lookup, and (c) more than ten pushes on one release branch, where `--limit 10` plus `cancel-in-progress` leaves only cancelled rows. `--no-wait` is a supported, documented flag and neither the new runbook step 7 nor the code mentions the interaction. A step whose entire purpose is that nobody notices the absence should not degrade to a warning nobody reads. | open | |
| 8 | medium | src/scripts/release.ts:1324 | Renumbering miss: the `_step(` call for "Create GitHub Release" was left at `9` while its own skip twin two lines above was moved to `10`. Two different steps now print `[9/11]` — the tag step (:1303, :1305, :1316) and the GitHub-Release step. The runbook this change edits states the banner's purpose verbatim: "Each step prints what it will do before doing it, so a crash localises to a step". After this diff it no longer does. | open | |
| 9 | medium | src/scripts/release.ts:25-47 | The file's own pipeline docstring was not renumbered and now contradicts the code it documents: it lists nine steps ending at "9. GitHub Release", omits the new findings step entirely, and numbers merge/tag/release as 7/8/9 where `execute()` prints 8/9/10 of 11. `docs/release-runbook.md` was updated for exactly this reason, so the in-file list is the same drift the diff's own runbook note claims to have written down. Four in-code step references are stale for the same reason: `:43` "under `--ci`, see step 9" (now 10), `:873` "`git pull --ff-only` enforces in step 8" (now 9), `:1294` "crash on step 9 from leaving a half-tagged release" (meant the GH-Release step; now reads as a self-reference to the tag step it sits above), `:1311` "merge FIRST (step 7)" (merge is step 8), and the `9b.` banner at `:1350` (now 10b). | open | |
| 10 | medium | src/scripts/release.ts:1037 | The expected artifact FILENAME is inferred from the artifact NAME, and nothing checks it. `gh run download` extracts by the basename of the uploaded `path:` (`/tmp/self-review-findings.json`), which coincides with the artifact name today by accident. `release_findings_ingest.ts`'s docstring claims the coupling is guarded — "a rename there without one here is caught by `release_findings_ingest.test.ts`, which reads the workflow rather than trusting these strings" — but the test reads only `name:` and `upload-artifact`, never `path:`. This is the concrete case where the code is wrong and all 23 tests still pass: change the workflow's `path:` to `/tmp/findings.json` and the suite stays green while every release dies at the `die()` on :1039. | open | |
| 11 | medium | src/scripts/check_finding_dispositions.ts:107 | `interface Ledger` gained `[k: string]: unknown` to hold six known fields, and that disables typo/excess detection on the ENTIRE type — which `check_finding_dispositions.ts` and now `release.ts` both consume. Verified under `--strict`: `function probe(l: Ledger) { return l.findigs; }` type-checks with exit 0. The six fields are known and finite (`INTEGRITY_FIELDS` lists them); declaring them as six optional properties, or keeping them in a `Record<string, unknown>` side field, gets the same merge behaviour without spending the type. | open | |
| 12 | medium | src/scripts/release.ts:1009 | `settle_findings_ledger` — the only part of this change that touches a live release — has no test. The 23 new tests cover pure helpers only (`pickRun`, `planIngest`, argv builders, `merge_ingest`, the message). The repository already ships the seam for this: `_set_exec_override` + `release_drill.ts` exercise release orchestration against simulated `gh`/`git` results through the real error paths. None of the paths in findings 1-7 — download, ingest, empty-commit guard, push failure, verdict rendering, `no-run` return, resume re-entry — is exercised by anything. | open | |
| 13 | low | src/scripts/release.ts:1036 | The download bypasses the `gh()` wrapper that every other `gh` call in the pipeline uses. `runLookupArgv` goes through `gh()` (transient 5xx/429/ETIMEDOUT retry, elided argv in the death message — `_lib/gh_transient.ts`); `downloadArgv` goes through `run(['gh', …])` directly, so the single largest network transfer in the step (the 15.0.0 artifact was 71,629 B) is the one call with no retry budget. | open | |
| 14 | low | src/scripts/_lib/release_findings_ingest.ts:71-79 | The stated justification for accepting a `failure` run is inverted. `live-advisory` is `continue-on-error: true`, so a run whose review job failed reports conclusion **`success`**, not `failure`; a `failure` conclusion means the *other* job (`gate-dry-run`) failed. Accepting `failure` is harmless, but the comment's reasoning ("the review job is continue-on-error … so a red run can still carry the findings") describes a state the workflow cannot produce, which is the kind of comment a later reader will act on. | open | |
| 15 | low | src/scripts/release.ts:1035 | `fs.mkdtempSync` is never cleaned up — every release leaves an `os.tmpdir()/release-findings-*` directory containing the full findings artifact, including on the `die()` paths. | open | |
| 16 | low | src/scripts/release.ts:1053 | The ingest commit is not path-scoped: `git add <rel>` then `git(['commit', '-m', …])` commits the whole index. Anything already staged when step 7 runs — reachable on a resume that re-enters after a partial step 2/3, which `git add -A` stages — rides into a commit whose message says it ingests self-review findings. `git commit -m <msg> -- <rel>` removes the coupling. | open | |
| 17 | low | src/scripts/check_finding_dispositions.ts:145-165 | `merge_ingest` mutates its first argument and also returns it, and the caller depends on the mutation rather than on the return: `main` destructures `{added, carried}` and then writes the outer `ledger` variable. Correct today, silently wrong the day someone reads the signature as pure. No test pins the mutation either — `adds each finding once, by id` feeds `first.ledger` back in and passes under both semantics. Related: the pushed finding objects are the raw artifact ones; the `parse_ledger` validation at :450 runs over a `JSON.stringify` copy whose validated result is discarded, so the validation is a shape assertion only. | open | |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
