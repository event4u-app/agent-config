# Findings: feat-conformance-round6-measurement
<!-- completion-review: v1 | reviewed: 2026-08-08 | scope: e2d29bb326f9671d61af05bf0be534f99972484548b9d75be7c1fddf6ff85b8b | diff: 6ab06f759c02774f02f37dd5b85aaf6009da44d4 | reviewer: r2-fresh-subagent-feat-conformance-round6-measurement | prompt_hash: c5e178d0f394254c93ac6dac78e5bc7ea58bdf2803275acb184b641a3b5c01ad -->

<!-- context-manifest: v1
inputs:
  diff_sha: 6ab06f759c02774f02f37dd5b85aaf6009da44d4
  scope_hash: e2d29bb326f9671d61af05bf0be534f99972484548b9d75be7c1fddf6ff85b8b
  roadmap: agents/roadmaps/road-to-conformance-round6.md
  roadmap_hash: cbf9e17ad72248a8b0840dca3d81360a39d3f8a5d48831bea0d325c77c245ac0
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-08T13:05:43Z
-->

<!-- RE-BINDING, per plan-review-gates.md § 2.7. The rows below were produced by a
fresh reviewer against scope 56dab2457838851f…
(head 0ee1275af). Fixing them changed the reviewed content, so this artefact
is re-bound IN PLACE to the scope that actually ships — new `scope:` / `diff:`,
rows already terminal with their fix commit — rather than re-reviewed a third
time. § 2.7 names re-binding as the expected end of a fix pass; renaming instead
would leave the shipping content with no review at all.

The `prompt_hash` is deliberately NOT touched: it records which prompt produced
these findings, which is the property that makes a self-commissioned review
admissible under `evaluator-independence`.

An EARLIER round (14 findings, 1 high — the string-only transcript reader) is not
in this directory: re-dispatching with `--force` overwrites `<slug>.findings.md`
rather than archiving it, so round 1's table was replaced before it could be
renamed to `<slug>.round1-review.md`. Its findings, their disposition
(`fixed in 0ee1275af`) and that dispatcher behaviour are all recorded in
`road-to-conformance-round6.md` § R2 completion review.
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/report_skill_obligation_violations.ts:193 | Polarity is decided only by pivot POSITION, never by the obligation's verb: `polarity = head.includes(artefact) ? 'forbidden' : 'prescribed'`. But `DETERMINISTIC_RE` (re-used at :75 to build `OBLIGATION_LINE_RE`) admits `MUST` and `ALWAYS` — positive obligations whose named artefact is the REQUIRED act. An `ALWAYS run \`task ci\`` / `MUST use \`npm ci\`` line has its artefact in the head, so it lands in `forbidden`, and `scanSessionForViolations` then flags the agent for DOING the required thing. Same inverted-polarity class as the `PIVOT_RE` defect the previous review found, one level up; no fixture covers it — every case in `describe('polarity …')` uses `NEVER`. Latent only because none of today's 3 forbidden artefacts comes from a MUST/ALWAYS line, and the extraction is deliberately re-derived from live skill bodies, so a wording change makes it live with no code change. | fixed | Search grid: inconsistent logic / bug-producing pattern. Fix: gate polarity on the matched verb (`NEVER` ⇒ prohibition; `MUST`/`ALWAYS` ⇒ prescription, excluded), and add a `MUST`/`ALWAYS` fixture beside the two `NEVER` ones. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 2 | medium | agents/roadmaps/road-to-conformance-round6.md:856 | Acceptance-evidence row 3 cites `npx vitest run tests/scripts/conformance_scan.test.ts` → **29 passed**, but the R2 repairs later in this same branch add 4 more tests to that file (`rate_pct and band cannot contradict each other` ×1, `measureDelivered — absent is not a measured zero` ×2, `DEFAULT_RATE_SERIES` ×1) — re-running the cited command at this commit yields 33. Same staleness in two step notes: :390 says the SK-2 suite has "15 tests" (25 `it()` in the shipped file — 15 + 10 repair fixtures) and road-to-conformance-round5.md:262 says the divergence suite has "16 tests" (17 shipped). The section's own thesis is that "every verdict below is a command run at this commit"; three cited figures are pre-repair. | fixed | Verifiable from the diff alone (count `it(` in the two new test files, and the added `describe` blocks in conformance_scan.test.ts). Same "figure right in the table, wrong in the sentence" class § 4.1 records about its own 244. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 3 | medium | agents/roadmaps-progress.md:111 | The regenerated dashboard folds the roadmap's **12 acceptance-criteria checkboxes** into `Phase 6 — What this roadmap will not do`, reporting that phase as 14 steps / 13 done / 93%. The phase contains exactly **2** checkboxes (`- [` counts per `## ` section: P1 6, P2 4, P3 4, P4 5, P5 1, P6 2, Acceptance criteria 12 = 34, the dashboard's step total). Consequences published as fact by this diff: round 6 reads 32/33 · 97% and the repo total reads 189/301 · 63%, with 12 of round 6's "steps" being acceptance criteria rather than work; Phase 6 shows 93% when its own work is 1 of 2. Pre-existing generator attribution (checkboxes land in the last preceding `## Phase` heading), amplified here by +2 criteria and +10 flips. | deferred | Iron-Law-adjacent for the roadmap whose Phase 5 exists to correct exactly this accounting. Fix is either generator-side (stop attributing post-phase checkboxes to the last phase) or file-side (move `## Acceptance criteria` above `## Phase 6`, or use a non-checkbox glyph there). DEFERRED, not fixed: the fold is a `roadmap:progress` attribution rule (post-phase checkboxes land in the last preceding `## Phase`), so repairing it changes a generator every roadmap in the tree depends on. Recorded in the roadmap's § Round 2 with the inflated figures named (round 6 32/33 97%, repo 189/301 63%) and filed for the successor beside the `autonomous-execution` item. |
| 4 | medium | src/scripts/conformance_scan.ts:523 | `scanStore` always measures the delivered payload from `REPO_ROOT` (`path.join(REPO_ROOT, ".claude", "rules")` and `$HOME/.claude/rules`), regardless of `--store`. So a `--record` run over another project's transcript store — the exact M5 cross-project comparison 4.5 exists to keep observable — persists a `RateRecord` pairing a FOREIGN store's `rate_pct` with THIS package's `delivered_project_tokens`. The field names carry no hint of that, and the series' stated purpose is comparability across projects. The `present` flag correctly distinguishes absent-from-zero for the local tree but cannot express "this carrier is not the scanned project's". | fixed | Search grid: inconsistent logic. Fix: derive the project carrier from the scanned store's project (or omit both delivered_* fields whenever `--store` is not the default) rather than from `REPO_ROOT`. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 5 | medium | src/scripts/report_skill_obligation_violations.ts:472 | `--store` accepts a following FLAG as its value (`args[i + 1] !== undefined` only). `--store --json` pushes `"--json"` as a store path and consumes it, so `json` stays false, `scanStore` swallows the readdir failure, and the tool prints a normal-looking report with `Scanned 0 session(s)` / `Flags: 0` — a usage error dressed as a null. This is the flag-VALUE hole the sibling `report_carrier_divergence.ts:306` closes deliberately in this same diff (`value()` returns null on a `-` prefix) and that the R2 section lists as fixed; `--limit` here is protected only incidentally, by the `Number.isFinite` check at :487. | fixed | Same class as the repaired sibling, and the failure shape is the one the whole branch is about: an empty result indistinguishable from compliance. Fix: reuse the sibling's `value()` guard for `--store`. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 6 | low | src/scripts/report_skill_obligation_violations.ts:400 | `scanStore` parses every session twice: it calls `_parseSession(lines)` + `loadedSkillsFrom(entries)` for the `sessionsWithASkill` count, discards both, then calls `scanSessionForViolations(id, lines, …)` which re-parses the same lines and recomputes `loadedSkillsFrom`. `_parseSession`'s own docstring states the opposite intent ("Parse a session once … parsing the file three times for one report is the kind of waste that also invites the two passes to disagree about what they read"). | fixed | Search grid: inefficiency + docstring contradicting the code. Fix: add an entries-taking overload (as `loadedSkillsFrom` already does) and pass `entries` through. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 7 | low | agents/roadmaps/road-to-conformance-round6.md:641 | The published § 4.3 table does not sum: 101 626 + 102 402 = 204 028, the union row states **204 027**. The code is right — `union_tokens` is `_tokens(pChars + gChars)`, rounded once from summed chars, while the rows round independently — but the table gives a reader no way to tell that from an arithmetic error, in a section whose figure is cited as the input to the `essential` default-flip decision. | fixed | Fix: one-clause note that the union is rounded from summed characters, or print the union as the sum of the published rows. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 8 | low | src/scripts/_lib/carrier_divergence.ts:114 | `censusRuleDir` accumulates `fs.statSync(...).size` — BYTES — into a field named `chars`, and every caller divides it by 4 for a token estimate (`_tokens`, `preamble_byte_census`, `measure_scope_dedup`). Rule bodies are UTF-8 with multibyte content (`—`, `⚠️`, `×`, box glyphs), so every published payload figure is inflated by the multibyte overhead, silently. Moved verbatim in this diff, but the new docstring and the `chars` name both assert a property the code does not measure. | fixed | Fix: either rename to `bytes` and say the token basis is bytes/4, or read the file and count code units. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 9 | low | src/scripts/report_skill_obligation_violations.ts:95 | `CMD_HEADS` contains `'vendor'`, which `CMD_RE` can never produce: the first token class is `[a-z][a-z0-9-]*`, so the `/` in `` `vendor/bin/phpunit …` `` breaks the match and no backticked span parses. The PHP-tool family the entry was added for is therefore unreachable, and the head list overstates what the extractor covers. | fixed | Search grid: inconsistent logic. Fix: allow `/` in the head token, or drop the dead entry. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
| 10 | low | src/scripts/report_carrier_divergence.ts:201 | An unreadable shared copy is un-counted (`out.shared -= 1`) and pushed into no list, so it vanishes from the report entirely: the printed classes no longer account for the union of names, and a half-finished install (the dangling-symlink shape the test fixture models) renders as a clean run with a smaller `shared`. Correct not to call it `body-diff`; wrong to make it invisible in a report whose stated job is that the condition becomes VISIBLE. | fixed | Fix: add an `unreadable: string[]` class and print it, so the counts reconcile. fixed in 6ab06f759; rationale in § Round 2 of road-to-conformance-round6.md |
