# Findings: hook-payload-unwrap
<!-- completion-review: v1 | reviewed: 2026-08-20 | scope: 39d459fc667af82236979c0b29341d48e08840c907d702935d07b3a3243d560e | diff: bd16cbbe76b3fb5c3378ec70733b54d336eae498 | reviewer: r2-fresh-subagent-hook-payload-unwrap | prompt_hash: ac01668d01442a2d7f46c09dbbbfe1394f05d51f8e3d0aeee9086145e882fa26 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-20 -->

<!-- context-manifest: v1
inputs:
  diff_sha: bd16cbbe76b3fb5c3378ec70733b54d336eae498
  scope_hash: 39d459fc667af82236979c0b29341d48e08840c907d702935d07b3a3243d560e
  roadmap: agents/roadmaps/road-to-per-turn-hook-economy.md
  roadmap_hash: 26d111490cd53ba022e9577fa5ffb6533a339155e18cf8ecf9cdea312b31723d
  ac_hash: 8b3d8200231b13c05e62f5b58611857c85c94ade33509e924fb0420f8e18c9ba
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-20T03:46:37Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/hooks/ship_diff_volume_hook.ts:130 | The fix makes a previously-inert concern LIVE on the blocking `pre_tool_use` slot of both augment and claude, and it carries no `tools:` filter (`hook_manifest.yaml:108-113`) and no command-position anchoring. `SHIP_PATTERNS` are bare word-boundary regexes, so any tool call whose `tool_input.command` merely MENTIONS a ship verb — `grep -rn "git push" docs/`, an `echo`, a heredoc writing this very roadmap — now spawns two `git` subprocesses (`merge-base`, then `diff --numstat` with a 64 MB buffer) and can emit a spurious warning. Pre-fix that path returned the empty string and cost nothing, so the diff activates a latent false-positive class rather than inheriting it. Six sibling concerns declare `tools:` (`code-graph-nudge`, `edit-shape`, `reread-guard`, `orchestration-record`, `telemetry-usage`, `spawn-guard-shadow`) and `block-unauthorized-git` on the same slot additionally gates on `tool_name` via `COMMAND_TOOLS` before extracting; this concern does neither. In tension with AC-5, which wants a non-matching PreToolUse payload to cost no dispatch at all. No test covers the tool-name dimension. | fixed | 15785e5d5 |
| 2 | medium | tests/hooks/ship_diff_volume.test.ts:24 | `dispatcherStdin`'s docstring claims it emits "Exactly the text the dispatcher writes to a concern's stdin", and the roadmap repeats that as the reason "a hand-written fixture cannot drift into agreeing with the bug" (roadmap:1136-1138). It is not exact: `dispatch_hook.ts:1286-1300` writes `shapes.shapeFor(keep_classes)` — the `planPayloadShapes` STUBBED envelope — not `_build_envelope`'s output. The gap is the concern's second silent-death path and it is untested: if `needs_payload_bodies: [input]` is ever dropped or the class mapping changes, `tool_input` arrives as a `PayloadStub` object, `extractCommand` finds no `command` inside it, falls through to `payload['command']` and returns the empty string — the exact pre-fix behaviour, with all five new cases still green. `extractCommand` never checks `STUB_MARKER`, unlike `tool-result-bytes`, which reads its stub back through `stubbedBytes`. | fixed | 15785e5d5 |
| 3 | medium | agents/roadmaps/road-to-per-turn-hook-economy.md:1128 | "the same accessor `design-slop` adopted for the identical defect" is false, and the same claim is embedded in the shipped code comment (`ship_diff_volume_hook.ts:100-102`). `design_slop_hook.ts:80` defines its OWN private `_unwrap(envelope)`; `ui_route_nudge_hook.ts:72` defines a second local `unwrap`; neither imports `hooks/envelope.js`. The diff contradicts itself thirteen lines later, where the same paragraph credits those two "via their own `unwrap`". The two accessors are also not equivalent: the local ones descend into `payload` whenever it is an object, while the shared `unwrap` descends only when all four `ENVELOPE_KEYS` are present (`envelope.ts:63-68`), so any producer emitting a partial envelope returns this concern to its pre-fix dead behaviour with no test noticing. | fixed | 15785e5d5 |
| 4 | low | tests/hooks/ship_diff_volume.test.ts:107 | "stays silent on a non-ship tool call rather than guessing" is non-discriminating: it asserts only that `isShipCommand(...)` is false, which was already true pre-fix because extraction returned the empty string. It cannot distinguish "extracted `git status --short` and correctly declined" from "extracted nothing at all" — precisely the defect class this change exists to close, and the reason the roadmap counts it among the three cases that stay green. Assert the extracted string itself. The title also says "tool call" while the case varies the command, not the tool. | fixed | 15785e5d5 |
| 5 | low | src/scripts/hooks/ship_diff_volume_hook.ts:104 | `extractCommand` was private before this diff and is now exported, but nothing imports it — only `commandFromStdin` is consumed, by `main` and by the test. The docstring's stated reason for exporting ("so a regression can exercise the same text the dispatcher writes") applies to `commandFromStdin`, not to this symbol. The new export also collides by name with `block_unauthorized_git.ts:193`'s exported `extractCommand`, which takes the ENVELOPE (descending into `payload` itself) and returns a nullable string. Two same-named exports in sibling modules with inverted input contracts and different return types: an import-site mix-up silently yields the empty string, which is the defect this PR just fixed. | fixed | 15785e5d5 |
| 6 | low | agents/roadmaps/road-to-per-turn-hook-economy.md:1141 | The construct-count arithmetic does not close in a paragraph explicitly framed as a measured count. "found 8 of the 47 registered concerns; 6 of those descend into `payload` first" then names SEVEN concerns (`block-config-weakening`, `block-kernel-rule-writes`, `code-graph-nudge`, `rtk-wrap`, `tool-result-bytes`, `design-slop`, `ui-route-nudge`), and the paragraph closes with "The first predicate alone would have reported one", which requires 8 minus 7 equals 1. Either the figure is 7 or one of the names does not belong; as written a reader reconstructing the audit gets 6 plus 1 equals 7 against a stated denominator of 8. | fixed | 15785e5d5 |
