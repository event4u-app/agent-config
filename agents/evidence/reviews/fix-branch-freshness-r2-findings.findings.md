# Findings: fix-branch-freshness-r2-findings
<!-- completion-review: v1 | reviewed: 2026-08-14 | scope: 52ae5ab38221cd9b4c253320894b61af2c964449c10127c65467247b67459ed2 | diff: 96f2743418e6017c8fc95c82247b1a67b444b13e | reviewer: r2-fresh-subagent-fix-branch-freshness-r2-findings | prompt_hash: 248ac6158e54065b024026bc4313d22118fac20cad07c32289bbab22bfc7b4fa -->

<!-- context-manifest: v1
inputs:
  diff_sha: 96f2743418e6017c8fc95c82247b1a67b444b13e
  scope_hash: 52ae5ab38221cd9b4c253320894b61af2c964449c10127c65467247b67459ed2
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-14T00:00:51Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/check_branch_freshness.ts:254-262 | The new default-branch short-circuit decides a **verdict** from the stale local ref it is documented never to decide verdicts from. `localDefaultBase()` returns `origin/HEAD` (written at clone time, never refreshed) or the `DEFAULT_BASE` constant when unset; if `branch === local.base`, `resolveBase` returns immediately, and `main()` turns that into `branch === base` → `scanReport(0, "standing on &lt;base&gt; itself")` → exit 0, without asking the forge or the server symref. A branch whose name collides with a stale/fallback local default (e.g. `origin/HEAD` unset → fallback `main`, in a repo whose server default is `develop`, with an open PR from `main`) is now skipped entirely, where the previous code asked the forge and got the real base. The justifying comment — "a wrong answer here only means we pay the round trip we would have paid anyway" — is false: a wrong answer here means no round trip and no check. It also contradicts the `localDefaultBase` docstring at :178-186 ("Whenever the name actually decides a verdict, `serverDefaultBase` asks the server instead"). | open | |
| 2 | medium | taskfiles/ci-fast.yml:41-47 | The rewritten budget comment states a bounded worst case ("FORGE_TIMEOUT_MS in the gate is 10s, so one cold or hung `gh` can spend 10s") but the two `git ls-remote` calls in the same step run through `git()` (src/scripts/check_branch_freshness.ts:79-85), which passes **no** `timeout` — unlike `gh` at :124. An unresponsive remote (SSH host with no ConnectTimeout) hangs unbounded, so 10s is not the worst case. The same comment also still describes the step as two calls ("`gh pr list`… then the `git ls-remote`") while this diff adds a **third** network round trip — `serverDefaultBase()`'s `ls-remote --symref` at :210 — on the most common `/create-pr` path, where no open PR exists yet so the forge answers `none`. | open | |
| 3 | medium | src/scripts/check_branch_freshness.ts:416-423 | `runTwoBase` dropped `--base`, so both two-base self-test cases now depend on the real `gh` returning `unavailable`/`none` in order to reach `serverDefaultBase()`. `runGateCli` spawns the child with the full inherited environment (`env: { ...process.env, … }`, src/scripts/_lib/gate_self_test.ts:56) — only `CI`/`GITHUB_ACTIONS` are deleted, not `GH_REPO`/`GH_TOKEN`/`GH_HOST`. That is exactly the ambient-credential hazard this diff's own `main()` docstring (:482-488) claims to have closed for the unit tests, left live in the one place that is now the sole coverage of the new symref resolution: an environment carrying `GH_REPO` can return a real PR base for head `feat/x` and silently change the base both raised-floor cases are asserting on. | open | |
| 4 | low | src/scripts/check_branch_freshness.ts:168-174 | Rejecting a usable `baseRefName` because `number` is missing/non-integer returns `{kind:"unavailable"}`, which makes `resolveBase` discard a base the forge actually reported and fall through to the repo default — the gate then checks the *wrong* base and warns "could not ask the forge" when it did ask and got an answer. This trades a correct verdict for a cosmetic message; the stated motivation (never print `#0`) is a `describeBase` concern, and the `r.pr ?? 0` fallback that produces `#0` is still present at :279. | open | |
| 5 | low | tests/scripts/check_branch_freshness.test.ts:276 | The assertion `expect(describeBase({ base: "develop", source: "pull-request", pr: 7 })).not.toContain("#0")` is vacuous — `pr` is 7, so it can never render `#0`. The test's name ("no message may claim PR #0") promises coverage of the `r.pr ?? 0` branch that this line does not exercise; only the loop above it tests the new guard. | open | |
| 6 | low | src/scripts/check_branch_freshness.ts:229-242 | `explicitBase` uses `argv.indexOf("--base")`, so the `--base=develop` form never matches and is silently ignored — the gate auto-resolves a different base. That is the same "typo reads as something other than what was asked" class the function was extracted to close. Related: a rejected `--base --quiet` is discarded with no diagnostic, and under `--quiet` a successful run prints nothing, so a scripted invocation checks a base the caller did not request with zero signal. | open | |
| 7 | low | tests/scripts/check_branch_freshness.test.ts:30-38, :141 | Two falsifiable claims introduced by this diff are false. The new stub docblock says "Every `main()` call below passes one", and `main()`'s JSDoc (src/scripts/check_branch_freshness.ts:484) says "Tests MUST pass a stub" — but line 141, `expect(main(["--base", "no-such-base"])).toBe(0)`, passes no stub. (It is harmless in practice because the `--base` flag short-circuits before the forge, but the stated invariant is not the one the file holds.) | open | |
| 8 | low | src/domains/git/pr/create/command.md:82-94 (and the identical dist projection + src/skills/git-workflow/SKILL.md:56-60) | "Exit codes are two-valued; the verdicts are three" understates the code: exit 0 also covers the CI no-op (:500-503), detached HEAD (:506-509), and standing-on-base (:516-519) paths, none of which print `branch is current` or `NOT VERIFIED`, so a reader following the new decision table finds no matching row. Additionally the ✅ line is suppressed under `--quiet` (:547), which is how `taskfiles/ci-fast.yml` invokes the gate — a genuine pass there prints nothing, so the `exit 0, prints branch is current` row matches no observable output for that invocation. | open | |
| 9 | low | src/scripts/check_branch_freshness.ts:512-513 | `forge === undefined ? resolveBase(argv, branch) : resolveBase(argv, branch, forge)` is a dead branch: `resolveBase`'s third parameter has a default (:248), so passing `undefined` positionally already selects it. The ternary duplicates the call for no behavioural difference. | open | |
