# Findings: feat-top-band-model-economy
<!-- completion-review: v1 | reviewed: 2026-08-16 | scope: 2d373a27c678d806d1d3bea5a5a2febc3f05668bef52af260ca08fde128e6e79 | diff: 1c078dbbdfcd65074434707fa8cb61fee53fe05b | reviewer: r2-fresh-subagent-feat-top-band-model-economy | prompt_hash: eea1357afbaeab0b133e865c32cc388172ae0a03d6878a5ea30ed6807ab819d5 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 1c078dbbdfcd65074434707fa8cb61fee53fe05b
  scope_hash: 2d373a27c678d806d1d3bea5a5a2febc3f05668bef52af260ca08fde128e6e79
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-16T10:03:25Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/agent-src/contexts/subagent-configuration.md:26,29-31,36,72-76 | The `frontier` implementer/session case is unhandled, and the judge cap makes such a judge WEAKER than its author. The ladder is `lite → medium → high` with no `frontier` rung; the walk-through covers only `high`/`medium`/`lite` sessions; the cap states "IT NEVER CLIMBS TO `frontier`". A slice that declares `model_tier: frontier` — the escape hatch `subagent-routing.md:60` instructs authors to use — therefore resolves a `high` judge, i.e. a reviewer strictly below the author. That is worse than the "reviewer is no stronger than the author" condition the same file (line 48-50) names as what the one-tier-up rule exists to avoid, and it contradicts line 75-76 "Downshifting an implementer never downshifts its judge below what the verify contract requires". The saturation guidance is written only for the `high` implementer; the `frontier` implementer has no stated judge policy at all. | open | |
| 2 | medium | src/agent-src/contexts/execution/subagent-routing.md:11-12 vs :60 | The new clamp's escape hatch is documented as illegal in the same file. § Inputs enumerates the declarable set as `lite \| medium \| high \| inherit`; the new invariant instructs "A slice that needs `frontier` **declares `model_tier: frontier`**". The whole cost-visibility argument for clamping `inherit` at `high` rests on that declaration path being available, so an author who reads § Inputs first concludes the clamp has no opt-out and either drops the slice to `high` or declares a value the file says does not exist. The diff introduced the `frontier` declaration instruction without updating the enumeration two sections above it (downstream-changes, same file). Mirrored verbatim in dist/agent-src/contexts/execution/subagent-routing.md. | open | |
| 3 | medium | src/agent-src/contexts/execution/subagent-routing.md:72-73 | Missing blank line before `3. **Quota arbitrage**` breaks the policy list. The inserted "Honest scope" paragraph ends at line 72 and line 73 resumes the ordered list at `3.`. In CommonMark/GFM an ordered list may interrupt a paragraph only when it starts at `1.`, so lines 73-98 are lazy paragraph continuation: policy items 3 (quota arbitrage) and 4 (budget routing — the `pickTier` relation this roadmap is about) render as running prose inside the Honest-scope paragraph instead of as numbered rules. Byte-identical defect in dist/agent-src/contexts/execution/subagent-routing.md, so it ships to consumers. | open | |
| 4 | medium | src/scripts/routing_doctor.ts:649-652 | A runtime string asserts a code-structure fact with nothing binding it to the code: "no producer — tripCooldown has no production caller". Nothing re-derives that at build or test time, so the moment `tripCooldown`/`pickTier` acquires a caller the doctor prints a falsehood to every operator who runs it. This is not hypothetical on this branch — option (a) of its own open blocker `picktier-wire-or-archive` is "wire it", and wiring is the change that makes the line wrong. A claim about caller counts belongs behind a check (or phrased as "no cool-down was observed"), not hardcoded in `_render`. | open | |
| 5 | low | src/scripts/routing_doctor.ts:637-652 | The conditional `COOLING` marker (line 639) was left in place while the new line unconditionally reports "cool-down state: unavailable". `readCooldowns` can still return a live entry (operator-written or test-written state file), in which case one report prints `high=$5 COOLING` and, three lines down, that cool-down state is unavailable. The repair described in the roadmap ("It now reports cool-down state as *unavailable, no producer*") reads as a replacement of the misleading marker; the diff is additive and leaves both. | open | |
| 6 | low | agents/evidence/analysis/downshift-vs-cache.md:70-87 | Claim 2 is declared "**True**, and bounded to roughly 2 %" without any measurement of the thing it claims. The claim is that downshifting "splits a cohort's shared prefix into two caches", but nothing on the page measures cohort prefix sharing — the 3.1 % figure bounds total write volume, not split-induced writes. The page's own claim-1 figure argues the premise is absent: if a leg's first call realizes 2.8 % cache read in a corpus where 0 dispatches were downshifted (§ Limits), legs are not reading each other's prefix now, so there is no shared cohort cache to split. The error is conservative (it over-states the cost against the page's own conclusion) so the reading does not flip, but "true" is asserted, not shown, on a page whose stated purpose is to settle the question by measurement. | open | |
| 7 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-d-top-band-model-economy.md:308 | Step 4.2's note says the cache "is then read across a median 18 **further** calls", contradicting the evidence page it cites: `downshift-vs-cache.md` reports median **18 calls per leg** and states the cold start "amortizes it across ~17 further calls". Off by one against the source in the sentence that summarizes it. | open | |
