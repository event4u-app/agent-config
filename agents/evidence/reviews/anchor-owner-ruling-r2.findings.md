# Findings: anchor-owner-ruling-r2
<!-- completion-review: v1 | reviewed: 2026-09-10 | scope: c84d2e713dfe807d25e9c9e67f7222ca8d950e3eda895c25974ff318b4fd87cb | diff: 1c73771663e2f1e4d355f8e2654b9cf8eb8d3ac3 | reviewer: r2-fresh-subagent-anchor-owner-ruling-r2 | prompt_hash: 8833b94cd2298df968a4c7d8d7a417019c568d22b04b90429c70b0cf737c9cc5 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-anchor-owner-ruling-r2"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-10 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 1c73771663e2f1e4d355f8e2654b9cf8eb8d3ac3
  scope_hash: c84d2e713dfe807d25e9c9e67f7222ca8d950e3eda895c25974ff318b4fd87cb
  roadmap: agents/roadmaps/road-to-typed-grants-that-persist.md
  roadmap_hash: 290bf6222daca3d432d6fe780df825988baf46d55a47ec4e62487de19cfdb447
  ac_hash: 41b7b784a462de3c07c67dc8ece3c0c82e4d53bcf1f6855152c7521afd993856
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-10T14:50:36Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/check_platform_anchor.ts:234 | `readWaivers` refusal findings are printed into `lines` and never merged into `reading.findings`, so they cannot change the exit code. An expired, incomplete or impermissible waiver committed in the policy therefore leaves the gate GREEN whenever the dimension it names happens to be satisfied by the forge, while a well-formed waiver for that same satisfied dimension reds via `waiver-unused` (platform_anchor.ts:936). Malformed is thus strictly BETTER than well-formed, which inverts the Iron Law this diff states at platform_anchor.ts:51 and repeats in `accepted_risk_note` and the contract. Reproduced: policy carrying `arr-2026-09-10-strict-status-checks` with `expires: 2020-01-01` PLUS an `arr-illegal` waiver over `allow_unconditional_bypass`, forge fully compliant, `evaluateGate` prints `[waiver-expired]` and `[waiver-not-permitted]` and then `✅ platform anchor COMPLIANT`, exit 0. | fixed | f0f2e398f — the whole WaiverReading is passed into evaluateAnchor and its refusals seed the findings array; both early-return paths carry the seed too. Re-proven end to end against the live forge: expired -> exit 1 with [waiver-expired], restored -> exit 0. Two regression tests, one per path |
| 2 | high | src/scripts/_lib/platform_anchor.ts:717 | `authority` is presence-checked only, never value-checked, so `authority: "agent-self-service"` (and `authority: null`) is honoured with zero findings. Every prose surface in this diff rests on this field: `owner_ruling_2026_09_10` says "only the owner may AUTHORIZE a floor reduction", the contract says "complete, unexpired, owner-authorised waiver" three times, and the type doc says eligibility is bounded "by a committed list rather than by the quality of the prose in the waiver" — but authority IS prose here. A cheap `authority === 'owner'` bound is absent, so an agent-authored waiver is indistinguishable from an owner-authorised one to the only mechanism that reads it. | fixed | f0f2e398f — WAIVER_AUTHORITIES bounds `authority` to owner; non-owner and empty values are refused with their own finding, with a loop over four values as the failing direction |
| 3 | high | src/scripts/_lib/platform_anchor.ts:214 | `required_review_thread_resolution` sits in `NON_NEGOTIABLE_FLOOR` (:143) but is absent from `NEVER_WAIVABLE`, so a waiver nullifies a dimension the code declares non-negotiable. The comment at :135-142 justifies restoring it to the floor as removing "an unauthorized reduction" that had left the check "policy-lowerable" — and the same diff opens a second route to the identical reduction, with no owner check (see finding 2). The module doc at :36 calls `NEVER_WAIVABLE` "the harder tier inside it", asserting a containment that does not hold. Reproduced: waiver over `required_review_thread_resolution` + forge with thread resolution off → `compliant-with-accepted-risk`, zero findings. | fixed | f0f2e398f — NEVER_WAIVABLE is DERIVED from NON_NEGOTIABLE_FLOOR, so the two cannot disagree; strict_required_status_checks left the floor because a floored dimension is unwaivable by construction, and a test pins both halves |
| 4 | high | src/config/platform-anchor.json:43 | `strict_status_checks_note` still reads "`strict_required_status_checks` STAYS in the enforced set and is currently NOT satisfied", "The gate consequently reds on exactly this one dimension" and "Restoring it is one setting and needs no reviewer" — flatly contradicted by `accepted_risk_reductions` (:19-38) and `accepted_risk_note` (:40) in the same file and by the shipped `PASS_WITH_ACCEPTED_RISK` path. `threat_model_note` (:44) was explicitly marked SUPERSEDED when it went stale; this note was left standing as present-tense fact, so the config asserts both that the gate reds and that it exits 0. | fixed | f0f2e398f — strict_status_checks_note marked SUPERSEDED with the original kept, matching what its sibling threat_model_note already did |
| 5 | high | agents/evidence/ratifications/drain-anchor-owner-ruling.md:75 | The ratification record that authorizes this change says the council "refused the exemption shape (option c) on the ground that this is a permanent policy choice, not a temporary waiver, and an exception mechanism is how a control gets hollowed out" — while the same diff ships exactly an exception mechanism (`accepted_risk_reductions`). Its scope line (:16-18) covers only "the removal of two dimensions ... with the contract rewrite", and :108 asserts `strict_required_status_checks` "is still enforced by the gate, and is therefore the single dimension the anchor now reds on", which the shipped code makes false. The waiver mechanism — the larger governance change in this diff, and a new floor-relaxation route — is covered by no ratification record at all. | fixed | f0f2e398f — the ratification artifact is amended to cover the waiver mechanism, distinguishes the refusal (approval dimensions) from this case, and records the three defects round 2 found in the first implementation |
| 6 | high | agents/roadmaps/road-to-typed-grants-that-persist.md:407 | The `Status:` block added by this diff states "What the gate now reds on is exactly one dimension: `strict_required_status_checks`" and (:412) "Restoring it is one setting and needs no reviewer", while the same diff writes at :437, :509-510 and :530 that the gate exits 0 with `PASS_WITH_ACCEPTED_RISK` and that the dimension is covered by a dated waiver. One roadmap entry therefore carries two mutually exclusive present-tense verdicts, both authored in this change, and the `Status:` half is the one a reader hits first. | fixed | f0f2e398f — the roadmap Status block is corrected, with the three-hour window in which the red was true recorded rather than erased |
| 7 | high | tests/scripts/platform_anchor.test.ts:599 | The waiver tests do not exercise the failing direction of the rules the diff claims bound the mechanism. `refuses an incomplete waiver` only DELETES keys, which is the single shape the `!(f in entry)` check catches — there is no case for a non-owner `authority` (not refused, finding 2), for an empty-string or `null` documentation field (not refused, finding 11), for a `waiver-not-permitted`/`waiver-expired` entry actually reddening the gate (it does not, finding 1), or for a waiver over `required_review_thread_resolution` (honoured, finding 3). The file's own header comment says a gate whose failing direction is never exercised has unknown sensitivity; the four rules that matter most here are in that state. | fixed | f0f2e398f — validation is no longer presence-only: empty, whitespace, null, empty array, non-boolean, baseline-equals-accepted, unknown dimension, duplicate and unparseable date are each refused, each with a failing-direction case |
| 8 | medium | tests/scripts/platform_anchor.test.ts:711 | Time bomb. `evaluateGate` gained an injectable `now` (check_platform_anchor.ts:167) precisely so an expiry is testable, and neither gate test passes it — both assert against the real clock over a waiver whose `expires` is 2026-12-09. Verified by injection: with `now = 2026-12-10` the identical forge exits 1 with `[waiver-expired]` instead of 0 with `PASS_WITH_ACCEPTED_RISK`. On 2026-12-10 this test and `reports a stale waiver` (:695, which then gets exit 0 rather than the asserted 1) both go red on a date rather than on a defect. | fixed | f0f2e398f — both gate tests inject a fixed clock instead of reading the real one against a 2026-12-09 expiry |
| 9 | medium | src/scripts/_lib/platform_anchor.ts:930 | The comment says the stale-waiver report is "Reported rather than ignored, and deliberately NOT a failure: the configuration is safer than the waiver assumed" — while :936 pushes it into `findings`, so :950 returns `noncompliant` and the gate exits 1 (asserted at tests:695). A forge that is safer than the policy expected therefore reds the governance gate, and the comment tells the next maintainer the opposite of what the code does. | fixed | f0f2e398f — the waiver-unused comment is corrected to match the code, which reds; the repair is deleting one stale entry |
| 10 | medium | src/scripts/check_platform_anchor.ts:259 | The `PASS_WITH_ACCEPTED_RISK` line is dimension-specific but printed unconditionally: it claims "every hard dimension is present" and "Current-base compatibility is NOT guaranteed while `strict_required_status_checks` is waived" whatever was actually waived. With the thread-resolution waiver of finding 3 the output asserts a strictness caveat that is false and omits the one that is true, and "hard" is not the tier `required_review_thread_resolution` belongs to. The generic mechanism hardcodes today's single waiver into its verdict text. | fixed | f0f2e398f — the PASS_WITH_ACCEPTED_RISK message lists the waived dimensions from the verdict instead of hardcoding one |
| 11 | medium | src/scripts/_lib/platform_anchor.ts:717 | Validation is presence-only (`!(f in entry)`), so `failure_mode: ""`, `residual_protection: null`, `authority: null` and `review_triggers: []` are all honoured with zero findings (reproduced). The diff's stated rule — "every field is required and an incomplete waiver is REFUSED rather than honoured" — holds for a deleted key and not for an empty or null one, so a waiver carrying no documentation whatsoever can still trade a dimension away. `review_triggers` is typed `string[]` and never checked to be an array, so a bare string also passes into `AcceptedRiskWaiver` through the `as unknown as` cast at :758. | fixed | f0f2e398f — closed with finding 1 — the refusals now reach the exit code |
| 12 | medium | docs/contracts/ratification-artifact.md:272 | "Six dimensions are hard ... The rest are baseline expectations a complete, unexpired, owner-authorised waiver may trade against a documented benefit" is wrong for `required_status_check_contexts` / `minimum_required_contexts`: the required-context check at platform_anchor.ts:902-912 pushes straight to `findings` and never consults `violated()`, so a waiver there is honoured, never consumed, and reds as `waiver-unused`. Of the nine floor dimensions only two (`required_review_thread_resolution`, `strict_required_status_checks`) are actually waivable, so the contract over-states the tradeable set by one dimension and mis-describes what happens if a reader tries. | fixed | f0f2e398f — the contract states the two tiers explicitly and no longer overstates the tradeable set |
| 13 | low | src/scripts/_lib/platform_anchor.ts:728 | `dimension` is not validated against the set of dimensions the evaluator can judge, so a typo (`strict_status_checks` for `strict_required_status_checks`) is honoured, never consumed, and then reported as `waiver-unused` with the message "which the forge now satisfies" — a statement about the forge that may be false; the real cause is an unrecognised name. The intended waiver is silently not in effect and the diagnostic points the reader at the wrong thing. | fixed | f0f2e398f — unknown dimension names are refused rather than honoured and then mis-diagnosed as unused |
| 14 | low | src/scripts/_lib/platform_anchor.ts:758 | `honoured.set(dimension, ...)` accepts duplicate waivers for one dimension silently, last-in wins, with no finding. Two contradicting records for the same dimension (different expiry, different authority, different accepted risk) leave no trace in the output of which one the verdict rested on. | fixed | f0f2e398f — a duplicate entry for one dimension is refused rather than resolving last-wins |
| 15 | low | src/scripts/_lib/platform_anchor.ts:717 | `decided` is presence-checked but never parsed or bounded, so a waiver may carry a non-date or a `decided` in the future while `expires` is the only date the reader validates. The evidence line at :806 prints `decided` verbatim as part of the accepted-risk record, so an unparseable or future decision date is reported as if it were a measured fact. | fixed | f0f2e398f — `decided` is parsed as an ISO date |
| 16 | low | src/scripts/_lib/platform_anchor.ts:804 | `baseline` and `accepted` are typed `boolean` and only presence-checked, then interpolated straight into the accepted-risk evidence line. A waiver asserting `baseline: false, accepted: true` — i.e. claiming the unsafe value is the expectation — is honoured and printed as a coherent record, so the two fields that state which direction the risk runs cannot be trusted by a reader of the gate output. | fixed | f0f2e398f — `baseline` and `accepted` are type-checked, and a waiver whose two values match is refused because it waives nothing |
