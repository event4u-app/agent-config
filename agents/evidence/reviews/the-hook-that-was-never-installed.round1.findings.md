# Findings: the-hook-that-was-never-installed (round 1)

> Round 1 of the R2 completion review, against head `500da8b78` / scope
> `826f4ce1…`. All nine findings are terminal. The fixes changed the design,
> which moved the review scope, so a SECOND blind pass ran over the fixed head
> rather than these rows being re-pointed at code their reviewer never saw —
> that round is in the sibling `.findings.md`.
<!-- completion-review: v1 | reviewed: 2026-09-05 | scope: 826f4ce189d4b03a4f002ece7862caf7ef5a38a41c573f2d3fe69a963cfd5ed5 | diff: 500da8b781ee8af1a5c44946123964d8c0c94440 | reviewer: r2-fresh-subagent-the-hook-that-was-never-installed | prompt_hash: b4bd7bc83976e2bf8a0899ec1c0ec9ea880baf2df88a5566fdc3ee9b3ad97c85 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-the-hook-that-was-never-installed"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-05 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 500da8b781ee8af1a5c44946123964d8c0c94440
  scope_hash: 826f4ce189d4b03a4f002ece7862caf7ef5a38a41c573f2d3fe69a963cfd5ed5
  roadmap: agents/roadmaps/archive/road-to-the-hook-that-was-never-installed.md
  roadmap_hash: 7c46ffadb914e5f0d765c40ad73c03b7198c65a21ef2050f3996494328ee12b5
  ac_hash: a431b5b379fa9397651331e62983f444b86a237fcbbb302d4b88393356d59868
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-05T13:25:55Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/install-hooks.sh:98-132 | The pre-push gate refuses the push and prescribes `task install-hooks` for EVERY mismatch, without distinguishing why the installed set differs. The most frequent cause is the one this change itself measures: the current checkout is behind a base that moved the installer. Running the prescribed fix from that checkout renders the OLDER installer and writes it over the shared `.git/hooks` — the exact regression the same file's post-merge comment (:481-486) and push-closes-its-loop.md cite as the reason auto-repair was refused. It also preempts BASE FRESHNESS (:134, demoted to second), which is the gate that would have named the correct action (merge the base). The gate therefore hands the contributor a harmful instruction before the correct one can run. | fixed | 6b7e6c2a6 — Gate moved AFTER base freshness (install-hooks.sh:98 vs :149) and the notice no longer prescribes a re-install unconditionally — it names the behind-base and sibling-worktree cases first. Ordering pinned by a test. |
| 2 | medium | src/scripts/install-hooks.sh:98-132 | The blocking predicate is `installed == what THIS checkout renders`, which the diff states elsewhere has no unique referent: linked worktrees share one `.git/hooks` through the common dir (eight in this checkout per the roadmap). With two or more worktrees whose install-hooks.sh bodies differ, at most one can pass this gate at a time, and each `task install-hooks` repair blocks the others. The `-f src/scripts/check_installed_hooks_fresh.ts` guard at :116 only covers branches that predate the gate, not branches that carry it with a different installer body. The only remaining escape is setting AGENT_CONFIG_SKIP_PREPUSH_HOOKFRESH=1 routinely, which retires the gate in practice. | fixed | 6b7e6c2a6 — Carrier is ADVISORY now: it prints and never refuses. Council round 3 (claude-sonnet-4-5 + codex-default, 2026-09-05, 2/2 seats) chose this unanimously over keeping the block, on this finding. Pinned by a test that reds if `exit 1` / `fail=1` returns to the block. |
| 3 | medium | src/scripts/install-hooks.sh:123 | The documented three-value CLI contract (check_installed_hooks_fresh.ts:44-47: 0 match/none, 1 stale, 2 could-not-render) has no consumer that honours it. The pre-push carrier tests only `if ! ...`, so exit 2 — an installer that failed, an unwritable TMPDIR, a usage error — prints `Push blocked — the hook that just ran is not the hook this tree ships` and names a fix that cannot address it. A gate that reports a false cause is the misdiagnosis class this roadmap exists to remove. | fixed | 6b7e6c2a6 — The carrier captures the exit code and reports 2 as "could not compare … not a staleness verdict" (install-hooks.sh:186-192). Pinned by the advisory test. |
| 4 | medium | src/scripts/install-hooks.sh:116 | The two carriers guard asymmetrically, and the weaker guard is on the blocking one. post-merge/post-checkout require `-x ./scripts-run` AND `-d node_modules` AND `-f ...ts` (:491-492); pre-push omits the `-d node_modules` check. On a checkout that has hooks installed but no dependencies (node_modules removed to debug, an interrupted `npm ci`, a worktree that never installed), `./scripts-run` cannot start tsx, the command exits non-zero, and the push is refused with a staleness claim that is false. The author demonstrably knew the dependency-absent case — it is guarded in the sibling carrier. | fixed | 6b7e6c2a6 — `-d node_modules` added to the pre-push guard, matching the sibling carrier (install-hooks.sh:176-177). Pinned by a test asserting all three guards. |
| 5 | medium | src/scripts/check_installed_hooks_fresh.ts:147-155 | The comparison is content-only: `fs.readFileSync` + `Buffer.equals`, with no check of the executable bit that the installer sets (`chmod +x`, install-hooks.sh:265/369/403). Git silently skips a hook that is not executable, so a hook that lost `+x` is reported `match` while being completely inert — the installed-but-not-running class this gate was written to detect. Mode is part of what the installer writes, so it belongs in the byte-comparison's scope. | fixed | 6b7e6c2a6 — The comparison now reads the mode; a byte-identical hook without +x reports NOT EXECUTABLE. New test; sensitivity proven by forcing `executable = true`, which reds it. |
| 6 | medium | src/scripts/check_installed_hooks_fresh.ts:110-130 | The render seam fails OPEN. `AGENT_CONFIG_HOOKS_DIR` unset or empty makes install-hooks.sh (:23) fall through to the REAL shared `.git/hooks`, and neither side asserts the redirect took effect — `renderExpected` only observes an empty stage dir and reports it as DeadScopeError/exit 2. Because this runs from inside the executing pre-push, a mis-propagated env var (a refactor dropping the `...process.env` spread, a caller passing an empty string, an installer without the seam reached via cherry-pick) converts a read-only check into an install that overwrites `pre-push` mid-run — the self-overwrite truncation this same change measured, documented and refused elsewhere. The installer has no guard refusing an unredirected inspection run. | fixed | 6b7e6c2a6 — Both sides: the installer refuses a set-but-empty AGENT_CONFIG_HOOKS_DIR (exit 2) instead of falling through to the real hooks dir, and renderExpected refuses a non-absolute stage. NOT preventive — reproduced live: the sabotage run that removed this guard let the test suite install over the real shared .git/hooks. New test. |
| 7 | low | src/scripts/check_installed_hooks_fresh.ts:148-149 | `catch { installed = null }` conflates a read failure (EACCES, EISDIR, a dangling symlink) with absence, so an unreadable hook is reported `MISSING (source <sha>)` and the fix line points at a re-install that will fail for the same underlying reason. Distinguishing ENOENT from other errno values costs one branch. | fixed | 6b7e6c2a6 — ENOENT is `missing`; any other errno is `unreadable` and reports the code. New test using a directory in a hook path (EISDIR). |
| 8 | low | src/scripts/check_installed_hooks_fresh.ts:232-241 | `--hooks-dir` consumes the next argv unconditionally, so `--hooks-dir --quiet` sets the hooks directory to the literal string `--quiet`. Every managed hook then reports `missing`, and the gate exits 1 with a staleness verdict instead of the exit 2 the contract reserves for a usage error — the failure mode the adjacent `next === undefined` check was written to prevent. | fixed | 6b7e6c2a6 — `--hooks-dir` rejects a following `--flag` as a usage error (exit 2) instead of treating it as a path. New test. |
| 9 | low | src/skills/git-workflow/references/push-closes-its-loop.md:154 | The new link `[docs/development.md](../../../../docs/development.md)` is correct from `src/skills/git-workflow/references/` and wrong in the projected copy: `dist/agent-src/skills/git-workflow/references/` needs five levels, so it resolves to `dist/docs/development.md`, which does not exist. Both copies are byte-identical on this branch (same blob 6747d7c), so no path rewrite corrects it, and this is the only up-and-out link in the file — verified by resolving both paths. | fixed | 6b7e6c2a6 — The up-and-out link is replaced by the path in prose, with the reason: the file is projected byte-identically one level deeper, so a repo-root-relative link cannot resolve in both copies. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
