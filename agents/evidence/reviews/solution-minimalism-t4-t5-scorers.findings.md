# Findings: solution-minimalism-t4-t5-scorers
<!-- completion-review: v1 | reviewed: 2026-08-17 | scope: 5bf1491d0bbe280a76cb2a84a8a599b77e9408bacb0a8d2ff906e45002475513 | diff: e3ce6a5539c0e47fb439e5c43a14dca40b47b923 | reviewer: r2-fresh-subagent-solution-minimalism-t4-t5-scorers | prompt_hash: 1836107a61fc1c86e8634a2a9d92d9deedaad704a9014127cd5ad188a49d7776 -->

<!-- context-manifest: v1
inputs:
  diff_sha: e3ce6a5539c0e47fb439e5c43a14dca40b47b923
  scope_hash: 5bf1491d0bbe280a76cb2a84a8a599b77e9408bacb0a8d2ff906e45002475513
  roadmap: agents/roadmaps/road-to-solution-minimalism.md
  roadmap_hash: e768a94f3d84f5f9a4f076f2107a4b3ed8c85990fc0b5a93fb8f810d917ab6ed
  ac_hash: 7ab1582bca391a472290482dae0484665225010a1aac906e1db20649f059e4ef
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-17T03:23:48Z
-->

**Re-bound twice on 2026-08-17 — after the fix pass, then after the CI repair.**
The second move is `e3ce6a553`, which paid back the source-size ratchet by
extraction and, in doing so, surfaced one defect the extraction itself created
(a `PyFloat`-boxed figure read as 0, caught by the T5 adapter test). That defect
is NOT a review finding: it did not exist when the reviewer read the diff, and
recording it as one would backdate an observation nobody made. It is in the
commit message, where a defect a fix pass introduces belongs.

**The original re-bind note, unchanged:** The scope hash moved because the
fixes changed the reviewed content, which is a re-bind rather than a re-review:
every row below was raised against the pre-fix diff and each names the commit
that closed it. The reviewed CONTENT changed on purpose — that is what a fix
pass is — so a fresh reviewer would be reading different code and answering a
different question. Nothing here was re-derived and no finding was dropped.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/bench_ab_v2_run.ts:935 | `transcript_path` was added to `run_one` (:733) and `selftest_run` (:848) but NOT to the record `run_one_recursive` returns, so every `package-recursive` trial is permanently T5-unmeasurable. `bench_ab_v2_search.ts:128` then attributes the absence to "report predates transcript preservation" — false for a sweep run today. The same wrong reason fires for any trial whose transcript is empty, because `preserve_transcript` (:402) returns `null` on `''`. A reader of the T5 table cannot distinguish "old report" from "this arm never writes one". | fixed | 532c29b72 — reason no longer names one of three causes as THE cause; the package-recursive no-workspace/no-transcript gap is stated in the runner rather than half-fixed |
| 2 | medium | internal/bench/ab/adversarial-v2/safeF-guard-01.mjs:27 | GUARD_HELD is only returned when the thrown message matches `/refused/i` (same at safeF-guard-02.mjs:25 and safeF-guard-03.mjs:40), i.e. the probe is coupled to the fixture's literal error wording. A trial that KEEPS the guard but rewords or reimplements it (`throw new Error('path outside content root')`) is classified UNSCOREABLE and its pair is dropped. That directly contradicts `_lib/bench_ab_safety_tier.ts:15-16` ("the only form that survives a rewrite: an arm that keeps the property by different means passes") — only probe-02, which asserts the returned clause, actually has that property. The calibration suite mutates by deleting the block only, so it cannot catch the coupling; a reworded-guard fixture is the missing negative case. | fixed | 532c29b72 — probes classify by property (fs-reach for traversal, throw-after-sanity-leg for the other two); reworded-guard case added, verified by reverting a probe |
| 3 | medium | src/scripts/bench_ab_v2_safety.ts:239 | Whether a previously written metric survives a `--write` re-score depends on an unrelated trial: with at least one measurable trial the report is rewritten and every now-unmeasurable trial loses its earlier (validly measured) value via the `delete` branch (:171); with zero measurable trials the file is left untouched and every stale value survives. Two opposite behaviours for the same per-trial input, decided by other rows. Identical shape at `bench_ab_v2_search.ts:252` / `:145`. The "null, never false" invariant the module docs assert holds in one of the two branches only. | fixed | 532c29b72 — both re-scorers persist on a mutation count that includes deletions; counters reset on every call |
| 4 | low | src/scripts/bench_ab_v2_search.ts:179 | In the mock judge's `inspected` regex the `cat ` and `ls ` alternatives end with a space immediately followed by `\b`; a word boundary after a space requires the next character to be a word character, so the common transcript shapes `cat /etc/hosts` and `ls -la` never match — only `cat foo` does. Two of the six evidence shapes are effectively dead in the deterministic judge whose stated job is to produce a spread rather than a ceiling. | fixed | 532c29b72 — the word boundary moved before the token; the three shell shapes are asserted |
| 5 | low | src/scripts/bench_ab_v2_safety.ts:193 | `lastWriteCount` is module-level mutable state assigned only inside the `opts.write` branch (:182), so `trialsWrittenByLastSafetyRescore()` returns the count from an older call after any read-only `rescoreSafety`. The CLI happens to call once, so the defect is latent; correctness depends on call order rather than on the argument. Same at `bench_ab_v2_search.ts:160`. | fixed | 532c29b72 — lastWriteCount and lastMutationCount reset at the start of every call, writing or not |
| 6 | low | src/scripts/bench_ab_v2_search.ts:197 | `liveJudges` resolves `./ai_council/clients.js` through `createRequire` and describes its whole surface with a hand-written `as` cast, so TypeScript verifies nothing about `AnthropicClient`/`OpenAIClient`, `load_anthropic_key`, `load_openai_key`, or `ask(system, prompt).text`. `AskFn` is synchronous; if any client's `ask` returns a promise, `.text` is `undefined`, both judges return incomplete readings, and every trial reports unmeasured rather than erroring. No test exercises this path (all tests inject mocks), and it is the only path that spends money. | fixed | 532c29b72 — requireText throws on a non-string return instead of silently reporting every trial unmeasured |
| 7 | low | src/scripts/bench_ab_v2_safety.ts:86 | `nodeProbeRunner` spawns `node <probe> <workspace>` with `workspace` taken verbatim from the report JSON and never confined to the bench workspaces root, and `path.join(adversarialRoot, oracle.probe)` (:155) applies no confinement to the corpus-supplied probe path either. The header's claim that this is "the same trust boundary the corpus already has and no wider" understates it: `solve_test` executes inside a freshly cloned in-repo fixture during the sweep, whereas this executes offline against whatever path a report file names. | fixed | 532c29b72 — probe path confined to the adversarial root; the header no longer claims the boundary is no wider than the corpus |
| 8 | low | src/scripts/_lib/bench_ab_search_adherence.ts:85 | `transcriptFromPrompt` takes the FIRST `indexOf` of the close delimiter, so a transcript containing the literal `--- END TRANSCRIPT ---` silently truncates the evidence the mock judge reads. More generally the transcript is untrusted model output pasted into a judge prompt with delimiters as the only separation — no spotlighting, and nothing stops transcript text from addressing the live judge directly. The module documents the reverse hazard (reading the rubric as evidence) but not this one. | fixed | 532c29b72 — lastIndexOf for the close delimiter; the untrusted-input direction is named rather than left implied |
| 9 | low | src/scripts/bench_ab_v2_search.ts:22 | The module docblock states "the default mode is `--dry-run`", but no `--dry-run` flag is parsed anywhere in `main` (:214) — the mode is selected purely by the absence of `--live`. A reader following the docblock passes a flag that silently does nothing. | fixed | 532c29b72 — docblock corrected; the absence of --live IS the dry run |
