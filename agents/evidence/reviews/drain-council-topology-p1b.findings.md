# Findings: drain-council-topology-p1b
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 421c02fddbf42e8b0c612dfdfeada67f837707e1b17cb29373a9396516aca19d | diff: afd81fe73a22d806efaac9b0fc377f7b6ad6a703 | reviewer: r2-fresh-subagent-drain-council-topology-p1b | prompt_hash: 1dddcb5dd708e220cfb1ba5f9823466a4ede8f143befe23707e9306856c40a6c -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-council-topology-p1b"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: afd81fe73a22d806efaac9b0fc377f7b6ad6a703
  scope_hash: 421c02fddbf42e8b0c612dfdfeada67f837707e1b17cb29373a9396516aca19d
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: d20f912964bb8988a87e6858505efd2d5d5a5cdaa32fa358e2e67941aa7e1996
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T18:17:45Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/ai_council/inline_findings.ts:172 | The "not a findings block after all" guard is `if (ex.outcome !== 'parsed') continue;`, but `parse_findings_outcome` returns `outcome: 'parsed'` for ANY syntactically valid JSON array (`consensus.ts:351` — `_collect_findings` silently skips items lacking `id`/`text` and the outcome is still `parsed`). So the guard never fires for the exact case its own comment names ("most likely a JSON array the member quoted from the artefact"). Probe, production sequence `harvest_inline_findings` → `run_consensus_scoring(inline_extractions)`: a reply that emits NO inline block but quotes ```` ```json\n[{"file":"src/a.ts","line":42}]``` ```` in its prose is recorded `parsed-inline`, contributes **0 findings**, makes **0 extraction calls**, and has the quoted span **deleted** from the text peer review / chairman / artefact read. Second probe: a reply quoting another reviewer's `[{"id","text"}]` array to disagree with it has those findings **attributed to the member** and its extraction call skipped. This falsifies three shipped claims — `docs/contracts/ai-council-config.md:798-800` ("A reply whose block is absent or unreadable is **not** modified … worst case is exactly today's call count"), `inline_findings.ts:219-223` ("an entry in the map is always a short-circuit and never a partial one"), and roadmap 1B.2's structural argument — and it records the failure as a SUCCESS in the 1B.4 pre-registered rate, so the gate that decides promotion cannot see it. Reachable whenever the feature is on and a member is contract-non-compliant, i.e. precisely the population the repair path exists for. | open | Verified by probe against the shipped call sequence; `docs/contracts/ai-council-config.md:798`; `src/scripts/ai_council/consensus.ts:351` |
| 2 | high | src/scripts/ai_council/inline_findings.ts:179 | The marker text asserts "the raw reply is retained in the session record", and `docs/contracts/ai-council-config.md:794` repeats it as a contract guarantee (it was a stated AI-council condition: the raw reply must survive for audit). It is not retained anywhere. `harvest_inline_findings` mutates `resp.text` in place (`inline_findings.ts:180`); the only persisted record is `council_cli.ts:2795` `responses: _serialise_responses(responses)`, which reads `text: r.text` (`council_cli.ts:1790`) off the SAME objects and runs AFTER the harvest at `council_cli.ts:2719`. Nothing captures the pre-mutation text: `recordRoundObservations` (`qualification_wiring.ts:201-221`) stores only an ok/failure-class probe, `all_responses` at `council_cli.ts:2763` is a shallow array copy of the same objects and is built after the harvest, and `council:render` re-reads the mutated payload. Effect: the verbatim member reply is destroyed, so the "observable marker instead of a silent edit" mitigation buys observability of the fact but not recoverability of the content, and the auditability condition the strip was approved under is unmet. | open | Traced end-to-end in `council_cli.ts`; no raw copy exists in the tree |
| 3 | medium | src/scripts/ai_council/inline_findings.ts:74 | `split_inline_findings` selects "last FENCED, else last bare", not "last in the text". Its own docstring at :60-63 claims the opposite ("`INLINE_FINDINGS_CONTRACT` asks for the block LAST, so LAST is what this reads"), and that is the stated defence against a quoted array winning. Probe: a reply that quotes a fenced ```` ```json ```` array in the prose and emits its own findings as a trailing BARE array (a shape the parser and the sibling test at `inline_findings.test.ts:296` explicitly support) selects the QUOTED fenced array — the quoted evidence is stripped out of the middle of the prose and the member's real trailing block is never read. The precedence test at `inline_findings.test.ts:303` places the bare array FIRST, so it pins the pattern order and never exercises the collision where the bare array is the trailing one. | open | Verified by probe on `split_inline_findings` |
| 4 | medium | src/scripts/ai_council/prompts.ts:245 | With `stance_tally` also on, the composed final-round suffix carries two mutually exclusive finality instructions: `INLINE_FINDINGS_CONTRACT` (`prompts.ts:245`) — "Wrap the array in a ```json``` fenced block, as the LAST thing in your reply. No commentary after it." — followed by `STANCE_LINE_CONTRACT` (`prompts.ts:168`) — "Close your reply with EXACTLY this line, and nothing after it: STANCE: …". `composeFinalRoundSuffix` orders the two instruction TEXTS; it does not reconcile what they demand, so `inline_findings.ts:189-194`'s claim that the ordering makes the composition bug "impossible to reintroduce" covers a different bug than the one now present. `docs/contracts/ai-council-config.md:790-792` states "the `STANCE:` line stays last" as a property of the run. A member that resolves the conflict by omitting the stance line yields `parse_stance_line === null` → a paid stance-repair call inside `consult` (`council_cli.ts:2641-2648`) — a call-count path entirely outside the surface 1B.2's parity arm (`inline_findings.test.ts:152-163`) measures, which compares only the consensus round. The test named "inline on + stance tally on → the STANCE line is still LAST" (`inline_findings.test.ts:217`) asserts the order of the two contracts in the PROMPT, not the position of the stance line in any reply, so its name overstates what it checks. | open | Composed suffix printed via `composeFinalRoundSuffix({is_final:true,inline_findings:true,stance_tally:true})` |
| 5 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:745 | The pre-registered 1B.4 denominator argues an inline `[]` "counts in the denominator AND as a success" because `parse_findings_outcome` classifies it `parsed` (`consensus.ts:337`, correctly cited). That rationale does not survive the locator: `split_inline_findings` cannot LOCATE a bare `[]` at all — `_BARE_ARRAY_SRC` requires at least one `{…}` and `_JSON_BLOCK_SRC` requires a fence — so an unfenced trailing `[]` returns `found:false`, falls through to the extraction call, and is recorded `parsed`, i.e. counted as a MISS in the pre-registered rate. Only a FENCED `[]` reaches the parser, which is the only shape the test at `inline_findings.test.ts:114-120` exercises. The gate that decides promotion is biased downward by a case its own pre-registration says should count as a success. | open | `inline_findings.ts:74-96` + `consensus.ts:37-39` |
| 6 | low | src/config/gate-violation-baselines.json:39 | The lowering note states "orchestrator.ts goes 2,225 -> 2,067 lines" for the tree it says it measured (the merged tree at `7b57734cf`). The file is **2,038** lines — `wc -l src/scripts/ai_council/orchestrator.ts` = 2038, and `check_source_size_budget` itself prints `2038  (+ 538)`. The baseline TOTAL (18,266) is correct and equals the live tree total, so the gate and `check_source_size_budget.test.ts` are green; only the per-file figure recorded as evidence for the lowering is wrong by 29 lines. | open | `./scripts-run src/scripts/check_source_size_budget` → total 18266, orchestrator.ts 2038 |
| 7 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:710 | Step 1B.3 is marked `[x]` and its evidence cites the gating predicate as `_inline_findings_active`. No such symbol exists; the shipped function is `inlineFindingsActive` (`src/scripts/ai_council/inline_findings.ts:256`). The step's substance holds — three conjuncts, each tested — but the closed step's citation does not resolve in the tree. | open | `grep -rn "_inline_findings_active" src/` → no matches |
