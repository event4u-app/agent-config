# Findings: loss-class-reachability
<!-- completion-review: v1 | reviewed: 2026-09-10 | scope: 379b069250ee3dd70a9de39c36862e6ff22c4feb0e56b4b1f06db9f011985252 | diff: 0a9837ab444a4067866ac05bae2f24535063520c | reviewer: r2-fresh-subagent-loss-class-reachability | prompt_hash: 22058a9b4b13db9a126974537d6448352930d517900752a4898ecba3f2c3320f -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-loss-class-reachability"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-10 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 0a9837ab444a4067866ac05bae2f24535063520c
  scope_hash: 379b069250ee3dd70a9de39c36862e6ff22c4feb0e56b4b1f06db9f011985252
  roadmap: agents/roadmaps/archive/road-to-continuity-writer-activation.md
  roadmap_hash: 443b2c16bed9c29d476cd21aaa877062988b7e59590cd54e6f305a597e38742c
  ac_hash: 2fb0f356a9bd37c49de56d689199f1c5717e8b3f8bd9fb967e44caff33c76e60
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-10T09:31:22Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/check_loss_class_declared.ts:415 (`MARKER_SUBSTITUTION`), :447 (`isLossyViews`) | `MARKER_SUBSTITUTION` is `/\.(?:replace\|replaceAll)\s*\([^)]*?(?:redact\|truncat)/i`. The `[^)]*?` class excludes every `)`, so it can only see the marker word before the *first* nested `)` in the call's arguments. A real redaction shaped as `s.replace(buildPattern(), '[REDACTED]')` or `s.replace(SECRET_RE, someHelper('[REDACTED]'))` — marker after a nested call's closing paren — is invisible to this test. This is an under-match ("silent-green") gap in the exact mechanism the docstring at :417-440 says was built to close that direction (`isLossyModule`'s own comment: "a module cutting content with nothing watching" is called the unaffordable direction). No self-test fixture exercises a nested-paren `.replace(...)` call, so the gap is untested as well as undisclosed — the docstring's "Residual error" paragraph (:437-440) names only the opposite (over-match) residual, not this one. | fixed | e1-fix — `[^)]*?` became `(?:[^()]|\([^()]*\))*?`, so one level of nesting is crossed and `s.replace(buildPattern(kind), '[REDACTED]')` matches. The reviewer was right that it was also UNTESTED: a self-test case now pins exactly that shape. Deeper nesting stays a bounded under-match rather than an unbounded scan that would run into the next statement |
| 2 | medium | src/scripts/check_loss_class_declared.ts:164 (`IMPORT_SPEC`), :537-553 (`reachedModules`) | `IMPORT_SPEC` (`/['"](\.\.?\/[^'"\n]*)['"]/g`) is matched against the *entire* `withLiterals` view of a file, not against actual `import`/`require`/dynamic-import call sites. Any quoted string literal that happens to start with `./` or `../` — e.g. a CLI usage message, an error string, or prose inside a template literal that survives comment-stripping — is treated as a reach edge. If such a string coincidentally resolves (via `resolveSpecifier`) to a real code file relative to the source file's directory, that file is silently pulled into the "target" set and, if it matches `LOSSY_PATTERNS`, is added to the gated population even though the emitter never actually imports it — an over-match that can manufacture a pro-forma declaration requirement, the exact failure class this contract's comment-stripping already exists to prevent one layer up. The docstring at :518-533 discloses that the walk deliberately "reads relative SPECIFIERS rather than `import` statements" (justified by `createRequire`), but does not discuss the risk of a non-specifier string coincidentally matching. No self-test fixture exercises a non-import quoted relative-path string. Not confirmed to currently mis-fire in the 5-module measured corpus (the 8 branch-touched files were checked and contain no such string); the risk is over the ~50 untouched hook scripts and their `_lib` graph, which is outside this review's read scope. | fixed | e1-fix — `IMPORT_SPEC` is anchored on `from` / `(` / `,` instead of matching any quoted `./`-shaped string anywhere. MEASURED before taking it: the corpus is the same 5 either way, so the narrowing loses no real edge, and every `createRequire` alias this tree writes is still a call argument |
| 3 | low | src/scripts/check_loss_class_declared.ts:453-482 (`resolveSpecifier`) | The code-extension test `/\.(?:js\|ts\|mjs\|cjs)$/` does not recognise `.mts`, `.cts`, `.tsx`, or `.jsx`. A specifier ending in one of those falls into the "no extension" branch, which appends `.ts`/`.js`/`index.ts`/`index.js` onto the already-suffixed path (e.g. `foo.mts` → tries `foo.mts.ts`, `foo.mts.js`, …), none of which will exist, so the specifier silently fails to resolve — an under-match if this tree ever writes a reached-path import with one of those extensions. Currently moot: the branch-touched files are all plain `.ts`/`.js`/`createRequire('*.js')`, so no evidence this mis-resolves today; flagged because the function's own docstring claims to resolve "every form this tree actually writes" specifiers, which is narrower than the set of extensions Node recognises. | fixed | e1-fix — `.mts`, `.cts`, `.tsx` and `.jsx` added. Moot today by the reviewer's own reading and taken anyway: the failure direction is a silently smaller corpus the day one appears, which is what this gate exists to refuse |
