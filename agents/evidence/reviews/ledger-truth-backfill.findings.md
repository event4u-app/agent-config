# Findings: ledger-truth-backfill
<!-- completion-review: v1 | reviewed: 2026-08-16 | scope: a3520ee952fd24f180d57d399b9af1162fd159d6f53d397355070bd6ceae26cd | diff: daa3d0d0b633d7aba27126b7512aa9f7be520790 | reviewer: r2-fresh-subagent-ledger-truth-backfill | prompt_hash: 04c9f442a9df5f94f8be4dc73280b2b70b90a8d122d0b9958ac922d7d044b2ad -->

<!-- context-manifest: v1
inputs:
  diff_sha: daa3d0d0b633d7aba27126b7512aa9f7be520790
  scope_hash: a3520ee952fd24f180d57d399b9af1162fd159d6f53d397355070bd6ceae26cd
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-16T20:42:16Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/cost/backfill_rates.mjs:76 | Rate validation is `typeof spec[k] !== 'number'` only, so a non-finite, negative, or all-zero operator rate is accepted, priced, persisted under `--apply`, and **clears `rate_missing`**. Probed on a fixture ledger: `{"input":1e999,…}` (JSON parses to `Infinity`) writes `total_cost_usd: null` and `byModel[m].cost_usd: null` into the ledger; `{"input":-15,…}` writes `total_cost_usd: -15`; an all-zero table writes `cost_usd: 0` — each with `rate_missing: false`, `rate_missing_models: []` and a `rate_backfill` stamp claiming the row was repaired. That is the silent zero 2.4 removed, re-created with the flag switched off and the original row already overwritten. The adjacent comment ("a silently-defaulted rate would reintroduce the understatement this pass exists to remove") describes an invariant the predicate does not enforce; `Number.isFinite(v) && v >= 0` would. | open | probed 2026-08-16 against a temp ledger; three writes reproduced |
| 2 | medium | src/scripts/cost/backfill_rates.mjs:55 | `PRICING` is copied verbatim into a **third** rate table (values verified identical to `track.mjs:41-45` today) and guarded only by the comment "Keep the two in step." The parity check this roadmap's own step 2.1 shipped (`tests/scripts/ai_council/pricing.test.ts:438-480`) parses `pricing.ts` against `track.mjs` and does not know this file exists. Phase 2's whole premise is that untracked duplicate rate tables silently disagree; a later vendor-price edit to `track.mjs` would leave this pass re-pricing at stale rates AND clearing `rate_missing`, i.e. a confidently wrong figure that no longer carries its own warning, with the parity test still green. The existing test already parses `track.mjs` source, so extending it to a third path is cheap. | open | verified: parity test covers pricing.ts and track.mjs only |
| 3 | medium | docs/contracts/cost-summary-schema.md:145 | The new section states `rate_backfill` exists so "a re-priced total is not mistaken for an originally priced one — without it, `rate_missing: false` on a repaired row and on a row that was never flagged are indistinguishable." The field has a producer and no consumer: `grep rate_backfill src docs` returns only the producer and this contract text, and `src/scripts/cost_summary.ts` — which reads the same `agents/cost-tracking/sessions.jsonl` (:27) and aggregates `rate_missing` into `rate_missing_sessions` / `rate_missing_models` on totals and every grouping (:184-209, :232-233) — never reads it. So at the summary layer the two cases stay exactly as indistinguishable as before, and an operator-supplied estimate carrying two stated limits is aggregated as if measured. This is the same "defined but not wired" surface, and the same "reproduces the zero one layer up" argument, that step 1.3's own recorded correction is about. | open | grep + read of cost_summary.ts aggregate() |
| 4 | low | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-b-ledger-truth.md:411 | Newly authored link `../evidence/analysis/rate-missing-observed-row.md` in the blocker-resolution note resolves to `agents/roadmaps/evidence/…` and is dead; the same target is linked correctly as `../../evidence/…` at :242 in the same file. The header's deliberate carve-out covers "the pre-existing `../../` links below", which this is not — it is a new `../` link written in this change, and it is the pointer a reader follows from the resolved blocker to its evidence. Ungated: `check_references` excludes `archive/` (ran clean, exit 0). | open | resolved both paths on disk: `../` MISSING, `../../` EXISTS |
| 5 | low | src/scripts/cost/backfill_rates.mjs:61 | `cache_write_1h` is a required key in `RATE_KEYS`, and the CLI rejects an otherwise complete table by naming it (pinned by a test), yet `costForModelEntry` (:87-92) prices cache writes solely at `cache_write_5m` — the 1h rate cannot affect any output. An operator must supply a number that provably changes nothing, which reads as an oversight rather than the documented 5m limit. Either drop it from the required set or say in the header that it is reserved for a future 1h-aware pass. | open | code read; no reference to `rates.cache_write_1h` anywhere |
| 6 | low | src/scripts/cost/backfill_rates.mjs:203 | `--apply` is a whole-file read-modify-rewrite (`readFileSync` :203, `writeFileSync` + `renameSync` :225-227) with no lock, against a ledger whose producer appends (`track.mjs:258` `appendFileSync`). A `track.mjs` run landing between the read and the rename is silently discarded — the rename replaces the file the append went into. temp-then-rename makes the write atomic but does not make the read-write pair safe. Narrow (manual invocation) but it is silent data loss on an append-only ledger. | open | code read of both producer and pass |
| 7 | low | src/scripts/cost/backfill_rates.mjs:40 | The `Usage:` line documents only `--rates` and `--apply`; `--store` / `--store=` is a supported flag (`parseArgs` :165, :167, exercised by every CLI test) and appears in the header only as the `BACKFILL_STORE` env var. The file header is the pass's only usage documentation — the contract section links the file itself. | open | code read |
