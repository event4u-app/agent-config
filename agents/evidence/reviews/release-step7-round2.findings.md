# Findings: release-step7-round2
<!-- completion-review: v1 | reviewed: 2026-09-11 | scope: c2d6738ea24c8a2504f9fef409a2a32c0d5661b7d1b65cc60fd9679b01c4fdc1 | diff: 2c6486910bc3a0bfa803245aa109a99d099c3db9 | reviewer: r2-fresh-subagent-release-step7-round2 | prompt_hash: 72ab8db8678604ef6603ca884d3322e3bc3b240298e3563e24cf55b773776c65 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-release-step7-round2"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-11 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 2c6486910bc3a0bfa803245aa109a99d099c3db9
  scope_hash: c2d6738ea24c8a2504f9fef409a2a32c0d5661b7d1b65cc60fd9679b01c4fdc1
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-11T06:02:43Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/release.ts:1354-1357, 1090-1096 | The step's central path — ingest, commit, stop for a human disposition — is unreachable, so the manual `--ingest` this change exists to retire is still required. `release-validation.yml:356-388` runs `check_finding_dispositions --release <v> --pr <n>` on every `release/*` PR (the `paths` filter always matches: `release.ts` bumps `package.json`), `:623-626` adds `unrecorded_findings`, and `:406-413` reddens on **any** blocking finding the self-review reported that the ledger lacks — which is every one of them while the ledger is absent. `self_review_gate.ts:664-666` posts that machine block for every finding on every reviewed PR. So with blocking findings present, `finding-dispositions` is already failing when step 6 calls `watch_pr_checks`, which exits non-zero on any failing check and `die()`s with `PR checks failed (exit 1)` plus a check-name list. Step 7 never runs. On the other ordering — the job read the comments before `live-advisory` posted them, so step 6 was green — step 7 ingests, commits, pushes, and the NEW re-wait at :1090-1096 then watches a head whose ledger carries undispositioned blocking findings, so `finding-dispositions` is red and `watch_pr_checks` dies there instead. Either way `dispositionStopMessage` — the whole operator-facing contribution, rebuilt this round to carry the gate's own stderr — cannot print, and the ledger for the case that matters is still produced by hand. The re-wait added for round-1 finding 2 is right about the head it protects and one block too early: it has to run AFTER the disposition verdict, or the verdict has to run before the push. | open |  |
| 2 | high | src/scripts/_lib/release_findings_ingest.ts:45-58 | Nothing ties the ingested artifact to the commit being released. `runLookupArgv` asks for `databaseId,conclusion,createdAt` only, `eligibleRuns` sorts by `createdAt`, and `_download_findings` takes the first run that yields a file — so the ledger is built from whichever finished run still has an artifact, on whatever head it reviewed. Reachable without anything breaking: the newest run is `cancelled` (filtered at :105) or its artifact download 404s, and the walk falls through to a run from an earlier push. With `findings: []` in that older artifact the ingest writes `no_findings_reason` = "The self-review ran and reported no findings over N of M changed file(s)" into the committed durable record for a head that was never reviewed, the local verdict passes, and the release proceeds. That is the inversion `check_finding_dispositions`'s own header exists to stop ("ABSENCE IS NOT EVIDENCE OF ZERO"), now produced automatically rather than by omission. `gh run list --json` exposes `headSha`; requesting it and refusing a run whose head is not the branch head is the missing guard. The runbook (`docs/release-runbook.md:141`) says "the newest finished run", which describes neither the list walk nor the risk. | open |  |
| 3 | high | src/scripts/release.ts:1029-1036 · .github/workflows/self-review-gate.yml:75-88 | The step turns an explicitly optional secret into a hard release blocker, and the comment this diff rewrote asserts the opposite. `self_review_gate.ts` returns 0 WITHOUT writing `--findings-out` on the no-`ANTHROPIC_API_KEY` path (`:761-773`), and `upload-artifact` carries `if-no-files-found: ignore`, so that run finishes `success` with no artifact. `eligibleRuns` keeps it, `_download_findings` returns null, `settle_findings_ledger` `die()`s. Consequence: in any repository or fork without the secret — and in this one the day the key lapses or is rotated out — **every** release stops at step 7 and needs a hand-written ledger carrying a `no_findings_reason` before `--resume`, on every release, indefinitely. The workflow's own header states the contract this breaks ("ADVISORY + INERT-WITHOUT-SECRET … No key → logged no-op, never a failure") and the block this diff replaced still closes with "this job stays advisory by design" — true of the job, false of the pipeline that now consumes it. `noArtifactMessage` names the cause and an escape, which makes the stop survivable but not honest about its cost; neither the runbook nor the workflow says a keyless repository cannot release unattended. | open |  |
| 4 | medium | src/scripts/release.ts:1005-1010 | The function's docstring documents the behaviour this round deliberately reversed. It still reads "there is no finished review run to ingest from, which is reported and does not stop the release because a release with no review artifact is a different problem from one with an unadjudicated review", while `:1035-1036` is `die(noArtifactMessage(...))`. The warn-and-continue it describes is exactly what round-1 finding 7 identified as the six-release failure mode, and a reader of this docstring will conclude the release survives a missing artifact. `_lib/release_findings_ingest.ts:132-140` and the drill summary at `release_drill.ts:633` both state the stop correctly, so the contradiction sits in the one comment a maintainer reads first. | open |  |
| 5 | medium | src/scripts/_lib/release_findings_ingest.ts:66-76 | The probe does not remove the defect its own docstring leads with; it removes the second one. The doc says `fs.existsSync` is wrong because "a push that failed after the ingest commit leaves the file local" — but `git cat-file -e release/X.Y.Z:<rel>` reads the LOCAL branch ref, which the failed-to-push ingest commit has just advanced. Both probes answer true in that state: `planIngest` returns `present`, `:1044` prints "ledger already committed on release/X.Y.Z" (false for the remote the merge will read), and no push is retried. The test at `tests/scripts/release_findings_ingest.test.ts:131-138` pins the local-ref form under the comment "a failed push makes [existsSync] true while the branch has nothing", asserting the shape without testing the claim. What actually saves the release is unrelated to this fix: step 4 (`:1313-1316`) re-pushes on any re-entry before step 7 runs. The stray-by-hand-ingest sub-case IS genuinely fixed. `origin/<branch>` — or `git ls-remote --exit-code` — is what the stated scenario needs. | open |  |
| 6 | medium | src/scripts/release.ts:1109-1112 | The release's own verdict is strictly weaker than the CI job it exists to pre-satisfy. Step 7 runs `check_finding_dispositions --release <v>`; `finding-dispositions` runs it `--release <v> --pr <n>`, and `--pr` is the mode the script's header calls the durable TRIGGER for an un-ingested finding (`check_finding_dispositions.ts:18-23`, `:611-613`). So step 7 can pass while the job is red — concretely whenever the self-review comment reports a blocking finding the ingested artifact did not carry, which is the same newest-run-vs-newest-comment skew as the `headSha` gap above. The release then proceeds to step 8 and either bounces on the required check or merges over a red one. The PR number is already in hand (`_pr_for_branch`, `:1198`), so passing `--pr` would make the two readings identical. | open |  |
| 7 | medium | src/scripts/check_finding_dispositions.ts:190-212 | `merge_ingest` can assert that a review ran and found nothing on the strength of a file it never checked is a findings artifact. `:496` reads the JSON, `:503` takes `(artifact['findings'] as LedgerFinding[] \| undefined) ?? []`, so any object without a `findings` key becomes an empty finding set — after which `:191-193` writes `_clean_review_reason`, whose text is "The self-review ran and reported no findings…; ingested from its own artifact by the release, so the empty set is a result rather than an absence". With no `coverage` block the sentence carries no checkable part at all (`:202-206`), so the record asserts the one thing it cannot support. The guard that would have caught a wrong file was removed in this same round: `_download_findings` now accepts "whatever single JSON file the artifact holds" and the test at `release_findings_ingest.test.ts:176-186` pins that deliberately, leaving the artifact NAME as the only remaining coupling. An `Array.isArray(artifact['findings'])` check that refuses rather than defaults costs one line and is the difference between a result and a guess. | open |  |
| 8 | medium | src/scripts/release_drill.ts:190, 210-222, 867, 894, 923, 996, 1016 | The renumbering never left `release.ts`, and the file this diff edits is now self-contradictory about what step 7 is. Two scenario summaries still call the MERGE step 7 — `:996` "step 7: head BEHIND main … merge (9.16.0)" and `:1016` "step 7: main moves between the CLEAN probe and the merge" — beside four new summaries calling step 7 the findings ledger. Five more are off by one: `:190` "step 8 (annotated-tag message)" (tag is 9), `:210`/`:213` "step 8 skips entirely and step 9 — the GitHub Release" (9/10), `:218` "steps 1 through 7 skip and step 8 is the first step that does work", `:867`/`:894` "step 8: … the annotated tag" (9), `:923` "step 9: … before the Release is created" (10). Outside the diff and invalidated by it: `src/scripts/release.ts:1313` ("a second `[4/10]`" — the total is 11), `.github/workflows/release.yml:17` and `:21` ("step 9's explicit dispatch", "see release.ts step 9" — now 10) and `:167` ("release.ts step `[2/10]`"), `docs/decisions/ADR-113-ci-native-release-label-trigger.md:135` ("release.ts step 9's"), `src/scripts/release_env.ts:101` ("the drill's step 8 read the repository's REAL changelog" — that read is step 9). Round 1 enumerated five sites and presented the list as the set; the sweep was never widened past one file. | open |  |
| 9 | medium | src/scripts/release_drill.ts:691-712 | `findings-ledger-on-disk-but-not-on-the-branch` tests neither half of its name. Its config is `findings-ledger-ingested-and-rechecked`'s with a different run id — `ledger_on_branch: false`, one successful run, artifact present — and the world models no filesystem state for the ledger at all, so "on disk" is never simulated. Its two assertions are that `git cat-file -e` was called and that `--ingest` ran; both hold in the first scenario, so the only thing it adds over a duplicate is that the probe is a git command rather than a stat — the implementation's shape, not its behaviour. The summary's claim "the push is retried" is asserted by nothing: no `git push` appears in `verify`, and per finding 5 the retry does not happen on that path. A scenario that pinned this would have to make the remote lack what the local branch has, for which the world has no knob. | open |  |
| 10 | medium | src/scripts/release.ts:1090-1096 · docs/release-runbook.md:160-177 | The new stop is placed inside the one window the CI-native entry point documents as non-resumable, and nothing says so. `.github/workflows/release.yml:177-183` states that `--resume` from a fresh `main` checkout "recovers a crash AFTER the release PR merged … but not a crash BEFORE merge (an open, unmerged release/X.Y.Z PR) — that case's stale branch/PR needs a manual close or a local `task release -- --resume`". Step 7 dies before the merge by design, so a label-triggered release that meets a blocking finding, or a missing artifact, leaves precisely that state. The runbook's own step-7 text tells the operator to "Resume with `task release -- --resume --yes`" — a local command — while §3.A step 4 still reads "Watch the `release.yml` run to green. It merges the release PR, tags, cuts the GitHub Release". A reader of the normal release route is not told it now has a human checkpoint it cannot resume through. | open |  |
| 11 | medium | docs/contracts/release-sizing.md:1-11 | The promotion asserts it was not time-driven on the first day the time gate would have failed. The file carried `keep-beta-until: 2026-09-10`; `check_beta_review_markers` errors on a lapsed contract absent from `src/config/lapsed-beta-baseline.json`, and `release-sizing` is absent from it (no match in that file; the gate's own report lists 84 inherited warnings and six upcoming FRESH lapses, none of them this one). Run on 2026-09-11 the pre-promotion file is a fresh lapse and therefore red. The three evidence claims do check out — `src/scripts/lint_changelog_rollback.ts` and `docs/distribution/consumer-matrix.md` both reference it — but "Promoted on evidence rather than on elapsed time" describes a motive the timing contradicts, and the gate's two other dispositions (extend the window with a reason, record it as superseded) were not taken. Separately: a beta→stable promotion of an unrelated contract is not traceable to the findings-ledger step and belongs in its own change. | open |  |
| 12 | low | src/scripts/release.ts:1152-1163 | Three of the step's four `die()` paths are exercised by nothing. The `found.length > 1` refusal (`:1155-1161`) is unreachable in the drill — the download handler at `release_drill.ts:530-540` writes exactly one fixed filename — and has no unit test, so deleting the whole block leaves all 20 drill tests and all 42 unit tests green while a two-file artifact silently degrades to `no-run` and a misleading "no artifact" stop. The same holds for the two `gh run list` deaths at `:1031-1045`: the drill always answers status 0 with valid JSON, so neither the non-zero branch nor the unparseable-JSON branch is entered. The two cases are exactly the ones a reader would want covered — a download yielding two JSON files, and a `gh run list` returning malformed JSON. | open |  |
| 13 | low | src/scripts/check_finding_dispositions.ts:496-508 | `--ingest` parses the artifact unguarded. `JSON.parse(fs.readFileSync(ingest, 'utf-8'))` at `:496` and `parse_ledger` at `:500` both throw, and nothing in the `if (ingest)` branch catches either, so a truncated or corrupt artifact exits the child on an uncaught exception and the release dies with `command failed (1): ./scripts-run src/scripts/check_finding_dispositions --ingest …` plus a stack trace. The step took the trouble to `die()` with three sentences of prose when `gh run list` returns non-JSON (`release.ts:1025-1030`); the artifact — the larger, network-delivered input — gets the raw trace. | open |  |
| 14 | low | src/scripts/release.ts:1035-1036 | `IngestOutcome` separates `no-run` ("No finished run at all — distinct from finished-but-carrying-no-artifact", `_lib/release_findings_ingest.ts:112-113`) and the caller then renders both with `noArtifactMessage`, so the `no-run` operator is told "no `self-review-findings` artifact for X on Y (0 finished `self-review-gate` run(s) checked)" — a sentence about artifacts for a state whose defining property is that no run finished. The distinction the type was created to preserve is discarded at its only call site. | open |  |
| 15 | low | src/scripts/check_finding_dispositions.ts:416-433, 497-499 | The new comment overstates what the round-trip buys. It says the merged findings "are the validated ones, not the raw artifact objects a discarded validation would have left unchecked" — but `parse_ledger` validates only `schema_version`, `release`, that `findings` is an array, and that each entry has a non-empty `finding_id`. `severity` and `kind` are never checked against their unions, so an artifact finding with `severity: "CRITICAL"` or `kind: "sec"` is ingested unchanged, `isBlocking` (`:215-220`) returns false for it, and it sits in the ledger never demanding a disposition. The real gain over round 1 is the deep copy — the merged objects now come from `JSON.parse`, not from the artifact — which is worth saying instead. | open |  |
| 16 | low | src/scripts/release_drill.ts:1111 · tests/scripts/release_findings_ingest.test.ts:204 | `run_scenario` always calls `execute(plan, { wait_for_checks: true, … })`, so step 7's `--no-wait` branch (`release.ts:1097-1102`) — the one that pushes the ingest commit and then warns that the merge may land on an unchecked head — is reached by no drill and no unit test, on a flag the runbook documents as supported. Minor in the same pair: the test named "asks for the fields pickRun reads, and no others it does not" names `pickRun`, replaced by `eligibleRuns` this round, and its body only checks containment — it never asserts the "no others" half it claims. | open |  |
| 17 | low | tests/scripts/release_findings_ingest.test.ts:241-250 | `produces the same field set a hand-written ledger carries` asserts set equality between the ingest output's keys and `agents/evidence/release-findings/14.23.0.json`'s, so any later edit to that one committed data file — a field added to a past release's ledger, a re-ingest, a schema bump — fails a test about `merge_ingest`. The property worth pinning is that the ingest emits the fields `check_review_schema` requires; deriving that from `INTEGRITY_FIELDS` plus the schema rather than from one historical artifact would say it without the coupling. | open |  |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
