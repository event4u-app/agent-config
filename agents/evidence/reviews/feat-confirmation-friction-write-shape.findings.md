# Findings: feat-confirmation-friction-write-shape
<!-- completion-review: v1 | reviewed: 2026-09-10 | scope: 59be5a1b45adbf6de0c0b75ec50c3fd93032b689f01ea5a3475f780e9b3e14d8 | diff: 6c543f0a268124d8018b108566d8561348623dc3 | reviewer: r2-fresh-subagent-feat-confirmation-friction-write-shape | prompt_hash: 724d4c0c78eebd54360752213378e4d265ce05041635a7f6fdfcce3124a6cf45 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-feat-confirmation-friction-write-shape"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-10 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 6c543f0a268124d8018b108566d8561348623dc3
  scope_hash: 59be5a1b45adbf6de0c0b75ec50c3fd93032b689f01ea5a3475f780e9b3e14d8
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-10T06:07:46Z
-->

## Reviewer, and what it reviewed

Fresh single reviewer, no implementation context. Inputs were the dispatcher's
own `prompt.md` and `diff.patch` verbatim, plus reads of the nine branch-touched
paths. Search grid as specified: errors, inconsistent logic, inefficiencies,
bug-producing patterns.

Four claims below were not read off the diff but **executed** against the
shipped modules — the write-shape detector over sixteen commands, and
`classify` / `firstDisqualifier` over four — because the change's value rests on
what the detectors do rather than on what their comments say. Two of those runs
refuted a comment in the file under review.

Not consulted, per the tool allowlist: repository history beyond the branch,
`agents/runtime/`, and the transcript store the probe measures.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/hooks/chain_nudge_hook.ts:32 | Three mutually inconsistent populations for the SAME instrument, window and date ship in one change: this header says 7,488 calls / 326 (4.4 %) / 214 `cat >`, 61 `sed -i`, 49 `python3 -c`; `autonomy_friction_traffic.ts:11` says 8,171 calls / 126 category A / 1.5 %; the analysis record, `hook_manifest.yaml:424` and the mechanics doc say 7,518 / 124 (1.6 %) / 293 (3.9 %) / 215 + 60 + 11 + 7. The analysis record pins **3.9 % (293 of 7,518)** as the number a later run must beat, so two shipped code headers now contradict the falsification target. This header's own components also fail to sum (214+61+49 = 324, not 326), and its `49 python3 -c` against the record's `11` implies it was measured by a python detector different from the one at `:200` — i.e. it documents a detector that no longer exists. | open | Same-change internal contradiction; the pinned falsification number is stated three ways. Verified by grep over the five artefacts that carry a figure. |
| 2 | medium | src/scripts/autonomy_friction_traffic.ts:52 | `SHELL_METACHARACTERS` is a private, byte-identical copy of the module-private const at `src/scripts/hooks/category_a.ts:60`, which is not exported. The file's own header (`:33`-`:37`) states the opposite as an invariant: "SHARED DETECTORS, NOT A SECOND COPY … A private regex here would let the two drift and the report would then describe a carrier that does not exist." Nothing pins the two equal — no test compares them — and the copy is what computes the 7,095 / 299 split that the analysis record calls "the load-bearing consequence". A change to the classifier's refusal class would move the live gate and leave this report unchanged, which is precisely the drift the header claims cannot happen. | open | The shape detectors ARE shared (`detectEditByShell` / `detectChaining` imported at `:48`); the refusal class is not. The mechanics doc repeats the claim as "shares the live detectors, so the report and the carrier cannot drift" (mechanics `:228`). |
| 3 | medium | src/scripts/autonomy_friction_traffic.ts:113 | `firstDisqualifier` recognises two reasons (empty, metacharacter) and collapses **every** remaining refusal into `head-or-subcommand-not-allowlisted`, but `isCategoryABashCommand` refuses on at least three further grounds: the consequence-word rule (`category_a.ts:238`), the absolute-value global-option rule (`category_a.ts:201`) and the subcommand allowlists. Executed against the shipped module: `git push origin main`, `gh pr merge 12` and `git -C /abs/worktree status` all return `head-or-subcommand-not-allowlisted`. The consumer-facing mechanics doc then reads that bucket literally — "only 299 fail on the head token" (`:231`) — and the analysis record's own § 3 names the absolute-path class as a member of the same bucket that § 2 labels a head failure. The directional conclusion survives (the bucket is an upper bound on head failures) but the label, the doc sentence and the table row are wrong. | open | Reproduced with `npx tsx` importing the shipped `classify` / `firstDisqualifier`; `git` is the 2nd-ranked head token (482) in the same table, so the mislabel is not hypothetical. |
| 4 | medium | src/scripts/hooks/chain_nudge_hook.ts:200 | False positive on a quoted mention, contradicting the rule's own comment at `:196`-`:199` ("Matched on the raw string it needs the write MODE to be named … so a path merely being read, **and a quoted mention of the shape in prose**, both stay silent"). The rule is matched against the RAW command by design, so a quoted argument naming the mode fires it. Verified: `git commit -m "switch to python3 -c open(p,'w') form"` returns `edit-by-shell`. This change itself ships that exact string in the substitutions table (`token-efficiency-mechanics.md:267`), so a commit, `grep` or `rg` quoting it triggers the nudge — and the file header calls a nudge that fires on correct usage "worse than none". The silence suite covers only the `sed -i` mention (`chain_nudge_hook.test.ts:127`); no case covers a quoted python mention. | open | Fires the advisory line and burns the session latch for the class, so the one real write-shape nudge that session is spent on a false positive. |
| 5 | medium | src/scripts/hooks/chain_nudge_hook.ts:190 | Three false negatives in `EDIT_BY_SHELL`, each a genuine fill-a-file shape, so each is both an un-nudged call and a call missing from the pinned 3.9 %. (a) `cat <<'EOF' > out.txt` — `stripLiterals` jumps from `<<` to `\nEOF` and swallows the redirect with the body; the stripped text is `cat `, so the heredoc-before-redirect form never matches. (b) `tee -a out.txt` — the `(?!-)` lookahead at `:194` excludes every flagged `tee`, including the append form, which is exactly a write; the intent appears to be "needs a path", but the lookahead tests for a flag. (c) `sed -ri` / `sed -Ei` — the `(-[A-Za-z]+\s+)*` prefix at `:191` requires each flag to be a separate token, so the combined-flag form (and `sed --in-place`) is silent while `sed -i` and `sed -i.bak` fire. | open | All three executed against the shipped `detectEditByShell`; each returns `null`. The 293 / 3.9 % figure is therefore a floor, not the measurement the analysis record offers for comparison. |
| 6 | low | tests/scripts/hooks/chain_nudge_hook.test.ts:126 | The silence case `['tee -a', 'tee with no path argument']` passes for a reason other than the one it states: what makes it silent is the leading `-`, not the missing path. The test therefore certifies a behaviour the code does not have and is what let the `tee -a f` gap in the row above ship — a case with the path present would have failed. | open | Testing anti-pattern (assertion true, rationale false); the neighbouring `['tee f', …]` positive case is the one that actually exercises the path requirement. |
| 7 | low | src/agent-src/contexts/communication/rules-auto/token-efficiency-mechanics.md:234 | "**60.3 %** carry the chain class. `cd` is the head of 3,512 of them" attributes 3,512 to the chain class, but the instrument's `heads` histogram is built only over non-category-A calls (`autonomy_friction_traffic.ts:145`-`:146`), never over the chain class. 3,512 is `cd` among the 7,394 uncovered; the chain class is 4,534. The analysis record states it correctly ("Top head tokens among the uncovered", `:63`); the consumer-facing doc changes the denominator. Same text in the `dist` projection. | open | Two different populations; no output of the probe supports the sentence as written. |
| 8 | low | agents/evidence/analysis/confirmation-friction-traffic-2026-09-10.md:58 | "7,095 | 94.4 % of the uncovered" — 7,095 / 7,394 = **96.0 %**. 94.4 % is 7,095 / 7,518, i.e. the share of ALL calls, not of the uncovered. The neighbouring row (`299 | 4.0 % of the uncovered`) is correct, so the two rows in one table use different denominators under one label. | open | Arithmetic/label error in the record a re-run is meant to compare against; the raw counts (7,095 + 299 = 7,394) are consistent. |
| 9 | low | agents/evidence/analysis/confirmation-friction-traffic-2026-09-10.md:112 | "the store, the window and the command are in the table above, so the comparison is reproducible rather than remembered" does not hold for this instrument. The window is "the 40 most recent transcripts by mtime" (`transcriptPaths`, `:158`-`:178`) — a moving set that includes the measuring session's own growing transcript — and `extractCommands` carries no per-call identity, so a store whose files repeat history (a fork or resume copying prior turns) counts the same call once per copy. The three contradicting populations in row 1 are the visible symptom of exactly this. A reproducible pin would need a fixed transcript list or a recorded set of file names and mtimes. | open | Diff-visible from the code; the transcript store itself was not read (tool allowlist), so the duplication path is stated as what the code does not guard, not as a measured cause. |
| 10 | low | src/scripts/hooks/chain_nudge_hook.ts:228 | Hot-path double work in a file whose header makes predicate ordering load-bearing ("getting it wrong cost 26 ms at p95", `:365`). `detectShape` calls `detectEditByShell` and then `detectChaining`, and each calls `stripLiterals` independently — so every shell tool call that is not a write pays the O(n) scan twice. On the firing path the latch file is also read twice (`alreadyNudged` → `readLatch`, then `latch` → `readLatch`). Both are trivially avoidable: strip once and pass `bare` down; read the latch once and reuse the parsed object. | open | No behaviour change; cost only. Named because the same file rejects a cheaper-first ordering elsewhere for the same reason. |
| 11 | low | src/scripts/autonomy_friction_traffic.ts:229 | `--limit` is accepted without a range check: `--limit -5` passes `Number.isFinite` and reaches `slice(0, -5)`, silently measuring the sorted set minus its 5 oldest members instead of refusing; `--limit 0` measures nothing and then reports it through the store-unreadable path ("no Bash call found under <store>", `:244`), which names the wrong cause; a non-numeric value falls back to 40 with no message. `--store` with no following token also falls back to the default store silently. | open | A probe whose whole justification (`:27`-`:31`) is "a probe that reports zeros from an absent store is worse than one that refuses" should not accept an argument that silently changes the window. |
| 12 | low | src/scripts/hook_manifest.yaml:432 | Comment formatting: two sentences run onto one over-long line — "… silence the other for the rest of the session. The inverted default is deliberate and differs from" — breaking the wrap of an otherwise 78-column block, which reads as a merge artefact. Companion nit: `chain_nudge_hook.test.ts:101` says "fires on the four write shapes" while `EDIT_BY_SHELL` holds five and the suite lists seven cases. | open | Cosmetic only; no gate reads it. |
| 13 | low | src/agent-src/contexts/communication/rules-auto/token-efficiency-mechanics.md:238 | The two host facts the entire second shape class rests on are asserted as "verified against the host's own permission documentation" with no citation, version or quoted line — in a doc shipped to consumers, about a host surface that changes independently of this package. The same change's analysis record § 5 is careful to say the adjacent question (whether a hook `permissionDecision: allow` suppresses the auto-mode classifier) "is not documented", so the standard is applied unevenly within one change. | open | Not a contradiction (settings `permissions.allow` and a hook allow are different surfaces), but the load-bearing claim is the one left uncitable. |
