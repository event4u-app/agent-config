# Findings: road-to-supervised-telemetry-collector
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: 9029cb67c8ed6ebcbcfa40c84b7ba956898e1eb12324e70f749344ed03ce2cfc | diff: d18786b032cfdc80c1ee1de61b8c0cd3f1782bb4 | reviewer: r2-fresh-subagent-road-to-supervised-telemetry-collector | prompt_hash: 0cc8b3c481062acd425ca9ec79a85f0119f64dea0d08146d1c335f7a936bc33b -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-road-to-supervised-telemetry-collector"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: d18786b032cfdc80c1ee1de61b8c0cd3f1782bb4
  scope_hash: 9029cb67c8ed6ebcbcfa40c84b7ba956898e1eb12324e70f749344ed03ce2cfc
  roadmap: agents/roadmaps/road-to-supervised-telemetry-collector.md
  roadmap_hash: 1d432258a3f0cc18c0afd5c803d6cd12884f53eb8c65cfbad62ea0a74d42d72d
  ac_hash: dcf41e95bfe37e0459d43763a01b20c60bdf07d9d42e3ea09ea364e15ded7b8d
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T18:24:18Z
-->

> **RE-BOUND IN PLACE, 2026-08-29** (contract §2.7 path 1, not a fresh
> skeleton). The review ran against scope `b614ba41…` / head `2e708585`. Acting
> on its findings moved the reviewed content, so the scope hash moved with it;
> the artefact is re-bound to the scope the fixes produced (`9029cb67…` / head
> `d18786b0`) rather than archived or re-dispatched, because archiving would
> leave the shipping content with no review at all. Every row was already
> terminal before the re-bind.
>
> **Two honesty notes about the anchors.** The rows below are the ORIGINAL
> findings against the original scope — the re-bind changes what they are bound
> to, never what was found. And the `File:Line` anchors are against the reviewed
> scope, not the working tree: the reviewer's own return flagged that an
> uncommitted read-time-dedup rewrite was already in the tree outside its scope
> hash, so several line numbers no longer resolve. That drift was
> self-caught work on metric item 4, not a response to these findings, and it is
> named here rather than left for a reader to trip over.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | tests/scripts/collector_store.test.ts:339 | The growth-budget test is tautological. `quarantining the same bytes twice does not accumulate copies` asserts only `names.length >= 1` and `new Set(names).size === names.length` — the second is true of every `readdirSync` result by definition, the first accepts unbounded growth. The test stays green if `quarantine()` reverted to timestamped names, so the R-A7 budget claimed at `agents/roadmaps/road-to-supervised-telemetry-collector.md:366` is asserted, not enforced. The fixture also never produces the same bytes twice: round 2 writes a fresh record before re-stamping, so the idempotence path may not even be the one exercised. | fixed | 7366ab942 — replaced by two tests: identical bytes quarantined twice leave EXACTLY one artefact (exact count, and the preserved bytes are compared), and DIFFERENT bytes leave two — the second direction, which an exact-count assertion alone would let a delete-the-old-one implementation satisfy. The fixture now rebuilds the store to identical bytes rather than writing a fresh record. Probe: naming the target by timestamp instead of content digest reds 1 of 32. |
| 2 | high | src/scripts/_lib/collector_store.ts:283 | Transition 2 performs no migration, and its test cannot detect that. The branch runs `db.exec(SCHEMA)`, which is `CREATE TABLE IF NOT EXISTS` (line 171) and therefore a no-op on an existing table of ANY shape, then stamps `user_version` forward — the whole `migrate` is a version stamp. Roadmap line 350 contracts records as `migrated forward inside a marked window`. TRANSITION 2 seeds only the version integer backwards over the CURRENT table shape (test line 205), so it passes for a stamp-only implementation; the first real v1 to v2 column change will stamp an old-shaped store as current with no test able to red. | fixed | 7366ab942 — the finding that mattered most. An explicit `MIGRATIONS` ladder plus an exported pure `migrationPath(from, to)`: a version with no registered path returns `null` and the store is QUARANTINED rather than stamped forward. The one registered step (0 → 1) is a no-op BECAUSE no schema delta exists yet, and that is written in the code instead of implied by an empty body. Four pure-function tests carry the gap direction (`migrationPath(0, 3)` and `(1, 2)` are null) plus one end-to-end refusal. Probe: a ladder that stops refusing an unregistered step reds 1 of 32. |
| 3 | medium | src/scripts/_lib/collector_store.ts:246 | A crash inside crash-recovery leaves a stale `MIGRATION-IN-FLIGHT` marker that is never cleared, and evicts a healthy store one restart later. The guard requires marker AND db file; `quarantine()` renames the db away (line 247) BEFORE the marker is removed (line 248), so a crash between them leaves marker-present with db-absent. The next open takes the `!existed` branch (line 260), creates a fresh store and never clears the marker; the open after that sees marker plus db and quarantines a live store. Fix shape: clear the marker whenever it exists, independent of the db file. | fixed | 7366ab942 — the marker is now cleared whenever it EXISTS, independent of the database file. A test drives marker-present / db-absent through two opens and asserts the healthy store from the first survives the second. Probe: restoring the conditional clear reds 1 of 32. |
| 4 | medium | src/scripts/_lib/collector_store.ts:276 | The quarantine refusal is session-scoped and disappears on restart with nothing changed on disk. `quarantined: true` is returned only by the open that performed the move, and the fresh store it creates is stamped at the current version (line 275), so the next `openCollectorStore` over identical on-disk state returns `quarantined: false` and writes land. `refusal: 'schema-quarantined'` thus reports a state one process restart from evaporating, and no test pins post-restart behaviour — deliberate one-shot lockout and durable refusal are indistinguishable from the code. | fixed | 7366ab942 — renamed `quarantine_performed`, documented as a ONE-SHOT signal for the open that performed the move, and the post-restart behaviour is pinned by a test: same on-disk state, next open reports `false` and writes land. The behaviour was intended; what was missing is that nothing distinguished it from a durable refusal. |
| 5 | medium | src/scripts/_lib/collector_store.ts:398 | `uninstall` removes only the migration marker while its own docstring (line 385) and roadmap line 353 both say `the markers`. `OPT_OUT_MARKER_NAME` is a declared marker and survives — the privacy-safe choice, but stated nowhere, so a future edit reconciling code to its docstring would silently revoke a user's opt-out. TRANSITION 5 asserts only `expect(removed).toContain(location.path)` (test line 314) over a fixture where no marker exists at all, leaving the `and the markers` half of AC-5 unverified in either direction. | fixed | 7366ab942 — the opt-out marker deliberately SURVIVES uninstall (reinstalling must not silently revoke a consent choice), and that is now stated in the docstring instead of contradicted by it. A test asserts both directions: the migration marker is removed, the opt-out marker is not, and `isOptedOut` still returns true. Probe: adding the opt-out marker to uninstall's removal list reds 1 of 32. |
| 6 | medium | src/scripts/_lib/collector_store.ts:213 | `quarantine()` DELETES the `-wal` and `-shm` sidecars instead of moving them, so a WAL-mode store's preserved artefact is missing any committed data still in the WAL while the transition-3 byte-equality assertion, which compares only the main file, stays green. No `PRAGMA journal_mode` appears anywhere in the diff, so the sidecar handling in `quarantine` and `uninstall` is unreachable today and roadmap line 353's `its WAL sidecars` describes a mode never enabled: dead defensive code now, a latent violation of `preserve without reading or rewriting` the day WAL is turned on. | fixed | 7366ab942 — sidecars are MOVED, not deleted. **The finding demoted itself while being fixed, and that is recorded rather than smoothed over:** the loss is not reachable through `openCollectorStore`, which opens the database before reading its version, and opening removes an unrecognised `-wal` (a real WAL close checkpoints it into the main file first). The first version of the test went red for that reason and not the finding's. The assertion is therefore on `quarantine()` directly, and a SECOND test pins the removal-on-open that makes the end-to-end path unreachable — so enabling WAL later reds it and re-opens the question deliberately. Probe: deleting instead of moving reds 1 of 32. |
| 7 | medium | src/scripts/_lib/collector_store.ts:379 | `deleteMachine` returns the pre-delete match count from a full `readRecords`, not the DELETE's `changes`, while its docstring claims `how many records were removed` — the number over-reports whenever the DELETE removes less than the SELECT matched, on the one path the module frames as serving a GDPR Art. 17 request. It also materializes every matching row into `CollectorRecord` objects purely to take `.length`, where `run()` already returns `changes`. | fixed | 7366ab942 — now returns the DELETE's own `changes`, and no longer materialises every matching row into objects to take `.length`. The over-report mattered on the one path this module frames as serving a legal request. |
| 8 | low | src/scripts/_lib/collector_store.ts:171 | `collector_events` is append-only with no TTL, pruning job, partition rotation or archive path; the only removal verb is user-triggered `deleteMachine`. The diff reasons about R-A7 explicitly for the neighbouring quarantine DIRECTORY (roadmap line 366) while leaving the table that actually grows per event unbudgeted, with the disk ceiling deferred to step 3.2, which is open. | fixed | 7366ab942 — `RETENTION_DAYS = 63` is the declared policy (the metric definition's own hard stop for the observation window) and `pruneOlderThan` is the job, with three tests including the boundary. The finding's sharpest point is conceded verbatim: the module reasoned about R-A7 for the neighbouring quarantine DIRECTORY while leaving the table that actually grows per event unbudgeted. The DISK CEILING remains 3.2's — a retention policy is not a ceiling, and the docstring says so. |
| 9 | low | tests/scripts/collector_store.test.ts:67 | `isStoreAvailable() ? describe : describe.skip` silently skips all 14 tests where `node:sqlite` is absent, so the `14 of 14 green` evidence recorded for 2.3 and 2.4 degrades to zero executed assertions with no distinguishing signal — while AC-8 in the same roadmap holds that a skip counts as a failure. The skip should at least be observable in the evidence line. | fixed | 7366ab942 — one always-run test now asserts `isStoreAvailable()`, so an unavailable runtime is a RED rather than a silent zero. This file deliberately differs from `runtime_journal.test.ts:56`'s silent `describe.runIf`: its evidence line names a test count, and a count nobody executed is not evidence. The finding's own argument — AC-8 of this roadmap holds that a skip counts as a failure — is the reason. |
| 10 | low | src/scripts/_lib/collector_store.ts:204 | `quarantine()` reads the entire database into memory via `fs.readFileSync` to compute its digest, on a store whose disk ceiling is still open (3.2). It also contradicts row 5's `a rename, never a read` as literally written; the intended claim is presumably `never interpreted as records`, which is not what the text says. | fixed | 7366ab942 — wording corrected to what it means: the bytes are never INTERPRETED AS RECORDS. The digest read is real, is acknowledged, and is recorded as a line item owed to step 3.2's disk budget rather than described away. The read itself is retained: the content digest is what makes a repeated quarantine of identical bytes idempotent, which is finding 1's fix. |
| 11 | low | src/scripts/_lib/collector_store.ts:100 | `WriteRefusal` declares `'sqlite-unavailable'` but no code path returns it — unavailability throws `CollectorStoreError` from `openCollectorStore` (line 396) instead, so an exhaustive consumer switch must handle an unreachable case while the real failure arrives as an exception. `CollectorStoreError`'s docstring, `asked to do something the contract forbids` (line 92), also does not describe its single use, a runtime capability gap. | fixed | 7366ab942 — `'sqlite-unavailable'` removed from `WriteRefusal` (no path returned it; unavailability throws before a write is reachable, so the member forced consumers to handle an impossible case while the real failure arrived as an unhandled exception). `CollectorStoreError`'s docstring now describes its single actual use — a runtime capability gap — instead of a forbidden-by-contract class it has never had a member of. |
| 12 | low | src/scripts/_lib/collector_store.ts:313 | `writeRecord` calls `isOptedOut()` and therefore `fs.existsSync` on every record — one stat syscall per write on the hot path of a module whose stated premise is that removing it changes no dispatch. Honouring a mid-session opt-out immediately is a genuine benefit, so this is a trade-off owed to 3.2's CPU budget rather than a defect, but it is currently unrecorded. | accepted-risk | 7366ab942 — ACCEPTED and now RECORDED, which is what the finding asked for: it is labelled a trade-off rather than a defect in its own text. One `existsSync` per write is the price of honouring a mid-session opt-out immediately; caching the marker for the life of a handle would keep recording a user who opted out until their session ended. The module docstring now names it as a line item owed to step 3.2's CPU budget, so 3.2 meets it as a known cost instead of discovering it. |

<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
