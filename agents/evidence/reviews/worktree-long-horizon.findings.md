# Findings: worktree-long-horizon
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: c630322b9ce787e87747a354c5239856825bb9be115909e1f9c59aef31467593 | diff: 6b60f30622fe663bc2ae249966835126a818a64c | reviewer: r2-fresh-subagent-worktree-long-horizon | prompt_hash: 6037a32b4db768f62cdb39c62962e7f131ad22713ecce2da7829c81a9951c191 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 6b60f30622fe663bc2ae249966835126a818a64c
  scope_hash: c630322b9ce787e87747a354c5239856825bb9be115909e1f9c59aef31467593
  roadmap: agents/roadmaps/road-to-long-horizon-execution.md
  roadmap_hash: 89257b7a50dc2827c00e9a9f8ca568627c2d70da3aa48b2188d664a14a29884a
  ac_hash: cc0790f4722b1540ced5602c39fd46db00ac1547495b3ab0e6082de1ede2c413
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T04:31:20Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/server/io/yamlIO.ts:81 | `detectIndentWidth` fixed the WRITE half of `upsertScalar` but not its "does this key already exist?" probe: `replaceScalar` still hardcodes two spaces (`indentLen % 2 !== 0 → continue`, `level = indentLen / 2`, `targetIndent = '  '.repeat(...)`). On any non-2-space `.ai-council.yml` the probe misses an EXISTING key, `upsertScalar` falls through to the insert branch, and the new line lands INSIDE the matched ancestor block — a duplicate mapping key, the same corruption round 4 fixed, relocated from EOF into the block. Executed: `upsertScalar('enabled: true\nfallback:\n    api_on_quota: false\n', ['fallback','api_on_quota'], true)` emits a second `    api_on_quota: true`; `yaml@1.1` (the parser `load_council_config` uses) throws `Map keys must be unique at line 5, column 5`. Round 4's own fix makes this self-inflicting rather than hand-edit-only: on a 4-space file the FIRST wizard toggle correctly writes `    api_on_quota: true` at the detected width, and the SECOND toggle of the same switch cannot find what the first one wrote — executed, two calls, file unparseable. `wizard.ts:931` writes the result via `writeAtomic` with no re-parse and returns `{ok: true}` both times, so the user sees success twice and the council is dead on the next `load_council_config` (`CouncilConfigError: invalid YAML`). `tests/server/yamlIO.upsert.test.ts:110-139` covers the 4-space ABSENT-key case only; no test toggles an existing key at a non-2-space width. | fixed | fixed — `replaceScalar` reads the same `detectIndentWidth` the writer uses, in all three places it hardcoded two (the emitted indent, the `% 2` alignment test, the `/ 2` level). A test performs the reviewer's own executed scenario — two successive toggles on a 4-space file — and reverting the level computation reds it with "expected length 1 but got 3", the duplicate key. |
| 2 | medium | dist/agent-src/contexts/execution/roadmap-process-loop.md:282 | The shipped projection of the loop contract is stale against `src/` and still asserts the coupling round 4 finding 4 refuted. `src/agent-src/.../roadmap-process-loop.md:282-289` now reads "**The rung is MODEL-CARRIED: … no TypeScript path reads it at runtime** — so 'bounded by the `cli_call_budget` counter' is what the rung must respect, never something a booking consumer enforces"; `dist/…:282-283` still reads "bounded by the same `cli_call_budget` counter the council books against". `git log` on the two paths: dist last moved at `561b486e0` (round 2), src at `8c9041488` and `6b60f3062` (rounds 3 and 4). `dist/agent-src/` is the tree consumers load, so the correction never reached the artifact that carries the obligation, and the shipped instruction tells an operating agent a guard exists that does not — the exact three-surfaces-at-once error round 4 recorded, now on the surface round 4 did not update. No gate catches it: `check_condensation` pairs `src/<rel>` to `dist/agent-src/<rel>`, so a file under `src/agent-src/contexts/` has `rel = agent-src/contexts/…` and a target of `dist/agent-src/agent-src/contexts/…` which never exists — the missing-target guard at `check_condensation.ts:516` skips it, and the src/-rooted iteration is skipped in turn at `:538` in favour of the agent-src copy. Verified green: `check_condensation` (scanned 449) and `check_rule_projection_integrity` (333 entries) both pass on the drifted tree. | fixed | fixed by regeneration; the GATE GAP is NOT fixed and is surfaced to the maintainer instead. `check_condensation` pairs `src/<x>` with `dist/agent-src/<x>`, and contexts live at `src/agent-src/contexts/`, so the pair never resolves and the missing-target guard skips it silently. Only `condense.sh --changed` catches it. This is the second time in this PR the same class went stale (round 2, finding 3), which makes it a population rather than an instance — but widening a CI gate's scope is beyond the fix-now bar, so it is noted and asked rather than taken quietly. |
| 3 | low | src/scripts/_lib/run_checkpoint.ts:350 | The round-4 null-tolerance on `head` is right in direction but renders an UNKNOWN as an AGREEMENT. the new `agrees` expression (true when `cp.head` is null, OR `head` is null, OR the two are equal) makes the field agree whenever `readHead` fails, and `readHead` returns `null` on a missing gitdir line, an unresolvable `commondir`, or an unreadable ref. `renderVerification` then prints `  ok       head: null` and, when every other field matches, `  the tree still matches the checkpoint.` — a positive claim about the one field whose job is to say whether the tree moved, made from an unreadable HEAD, with the claimed sha (`cp.head`) not shown at all. A resumed run in a checkout whose git metadata does not read reports "nothing moved" rather than "could not tell". Conservative-but-imperfect: the fix is a third render state (`unknown`) plus excluding it from the `agrees` roll-up, not a revert to strict `===`. | fixed | fixed — UNKNOWN is a third render state beside `ok` and `CHANGED`, because the reader's next action differs: `ok` needs nothing, `CHANGED` names what moved, UNKNOWN says the axis carries no information and the verdict rests on the others. The summary line now reports how many fields were comparable. |
