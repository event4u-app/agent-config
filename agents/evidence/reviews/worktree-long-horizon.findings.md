# Findings: worktree-long-horizon
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 340aec3f9bbe6ec0e583b04fbff172e078f8fc38a7ab33f4b51f65866f4468d6 | diff: ebc4b33850d907b5eab65fe4b9d7bedabd3efc37 | reviewer: r2-fresh-subagent-worktree-long-horizon | prompt_hash: 651a2a151e30cf6d3cde6e255accaef4ed68887c473e84b7810ad6e93ab4c423 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: ebc4b33850d907b5eab65fe4b9d7bedabd3efc37
  scope_hash: 340aec3f9bbe6ec0e583b04fbff172e078f8fc38a7ab33f4b51f65866f4468d6
  roadmap: agents/roadmaps/road-to-long-horizon-execution.md
  roadmap_hash: 89257b7a50dc2827c00e9a9f8ca568627c2d70da3aa48b2188d664a14a29884a
  ac_hash: cc0790f4722b1540ced5602c39fd46db00ac1547495b3ab0e6082de1ede2c413
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T04:53:59Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/_lib/council_fallback_wiring.ts:63 | `isRefusal` matches only `CouncilConfigError` / `CouncilDisabledError` / `CliClientError`, but the KEYLESS api-twin path throws `KeyGateError` (`clients.ts:184`, raised by `_loadKey` on an absent or non-0600 key file). Scenario: an anthropic/openai member on the cli rung with no `api_key_ref` — the documented subscription shape in `.ai-council.yml.example` § members — and no local key installed; its CLI fails eligibly (`binary_missing` / `auth_rejected` / `cli_unsupported`) → `establishTwin` → `construct()` → `_construct_api_member` keyless branch (`council_cli.ts:973/984`) → `KeyGateError` → rethrown at :111, escapes `consult()`, and `main`'s catch list (`council_cli.ts:3905-3921`) does not carry it either → unhandled crash, every already-answered seat discarded. This is the SAME defect this docblock says it closed one branch over ("It was missing once, and the escalation escaped the factory … A mechanism that turns a recoverable seat loss into a total loss is worse than no mechanism"): the explicit-ref branch throws the covered `CouncilConfigError`, the keyless branch throws the uncovered `KeyGateError`. `hasApiRung` is `_API_PROVIDERS.has(p)` (`council_cli.ts:888`), a name-set with no key check, so it does not gate this. It falsifies `docs/CLAIMS.md § council-fallback-loses-zero-seats` outside that claim's one stated exception, and contradicts `council_fallback_posture.ts:47-51`, which wraps the same loader in try/catch so `council:status` reports `none` for a seat the runtime dies on. Verified: `buildFallbackOptions(...).construct('openai')` with a `constructApi` throwing `KeyGateError` → escapes uncaught. | fixed | fixed — `KeyGateError` added to the refusal set, and the module got its FIRST tests (8): nothing covered `buildFallbackOptions` at all, which is how a one-name-short set survived. Removing the name reds them. |
| 2 | critical | src/scripts/_lib/roadmap_checkboxes.ts:58 | `phaseLines` recomputes `inPhase` on ANY H2 **or H3**, so an H3 sub-heading INSIDE a phase closes the phase span and every step under it is dropped. The dashboard — which this module's header says it matches "character for character" — slices phase-heading-to-next-phase-heading (`src/agent-src/scripts/update_roadmap_progress.ts:640-648`) and is not closed by a non-Phase heading. Live on the current tree: `agents/roadmaps/road-to-ui-track-integrity-followup.md` has `## Phase 1` (line 105) with 5 open steps under `### Measurement A` (111) / `### Measurement B` (132); `countRoadmap` returns `{open:0,done:0,parked:0,next:null}` over a file carrying 16 checkbox lines. Both consumers act on that zero: `run_continuation_hook.ts:335` `ladder(...)` → `'complete'` → appends a `complete` event, `rmSync`s the run state and ALLOWS the stop, so an autonomous run terminates at its first Stop with 5 Phase-1 steps open; `run_supervise.ts:219` → disposition `complete`, reason "no open steps remain — the run finished, the session just never said so". That is the false-`complete` this module's own header calls "the more dangerous of the two errors", and it defeats the branch's headline mechanism on a legal roadmap shape. Fix direction: close a span only on a heading at the same or shallower level than the one that opened it, which preserves the deliberate `## Blockers` / `## Acceptance criteria` exclusion. Verified: `countRoadmap` over the committed roadmap, plus a sweep of all 45 active roadmaps. | fixed | fixed — a phase span now closes only at a heading AT ITS OWN LEVEL OR SHALLOWER, so a deeper sub-heading stays inside it. `## Blockers` and `## Acceptance criteria` still close a `## Phase` by the level rule rather than by a section allowlist nobody would keep current. Verified live: the named roadmap went from open 0 to open 5. A CORPUS test now walks every committed roadmap and fails on any phase-bearing file the scanner reads as empty — that is the test that would have caught this, and it reds against the old logic. |
| 3 | high | src/server/io/yamlIO.ts:131 | `detectIndentWidth` returns the indent of the FIRST indented mapping line, which is not the document's width when that line is unrepresentative — and this branch newly routes `replaceScalar` (:86) through it, so a misdetection now breaks the existence-probe as well as the writer. Concrete legal trigger from this schema: a dashed key. `^[A-Za-z_][A-Za-z0-9_]*\s*:` rejects `first-principles:` — the shipped template's own advisor key (`.ai-council.yml.example:426`) — so on a 2-space file ordering `advisors:` before `members:`/`defaults:`, detection falls through to the depth-2 line and returns 4. Then (a) EVERY `replaceScalar` write in `POST /api/v1/wizard/ai-council` silently no-ops — `enabled`, `defaults.min_rounds`, `cost_budget.max_total_usd`, every `members.<p>.*`, every `decision_resolution.classes.<c>.mode` — while `wizard.ts:934-937` atomic-writes and returns `{ok:true, written}`; and (b) `upsertScalar(['fallback','api_on_quota'])` inserts at width 4 inside the existing 2-space `fallback:` block, producing a duplicate key at a bad indent that `js-yaml` REJECTS ("bad indentation of a mapping entry") — a valid config in, an unloadable one out, under a 200. Regression: the pre-branch `replaceScalar` (blob 39982398, hardcoded 2) handled the same document correctly. Untested — every case in `tests/server/yamlIO.upsert.test.ts:110-139` uses a representative first line. Verified: `detectIndentWidth` → 4; `replaceScalar(['members','openai','enabled'])` → unchanged; `load()` on the `upsertScalar` output → parse error. | fixed | fixed — `detectIndentWidth` takes the SMALLEST indent of any mapping line, not the first, and the key pattern accepts `-` and `.` (the shipped template has a dashed key, which was being skipped so a deeper line won). Both probes now read 2 where they read 4 and 6. An end-to-end test toggles the real template twice and asserts it still parses. |
| 4 | high | src/scripts/ai_council/transport_resolver.ts:429 | `classifyCliFailure` recognises `model_unservable` only via `s.startsWith('model_unservable')` or the literal vendor sentence, but the only producers write `error: 'model_unsupported_on_transport'` (`clients.ts:1857` preflight, `clients.ts:2002` turn-failed) and put the vendor sentence in `metadata.detail`, never in `error`. `orchestrator.ts:763` passes `String(response.error)`, so the token never matches. Scenario: an openai member pinned to a `CODEX_MEASURED_UNSERVABLE` id on the codex rung → preflight refuses without spawning → class `other` → `isFallbackEligibleUnder` falsy → `establishTwin` returns `null` → the seat is lost although the api twin serves that id. The class is DEAD while `docs/contracts/ai-council-config.md:178-181` (added by this diff) advertises it as one of the four eligible classes. Newly live here: this diff removed the base file's own "HONEST SCOPE: this set is not consumed by any shipped path today" caveat and wired the set up. Two secondary consumers are silently disabled by the same mismatch — `qualification.ts:286` (`lastProbe.outcome === 'model_unservable'`, stored via `classify(String(r.error))` at `qualification_wiring.ts:218`, so it stores `other` and the model-identifier rung never fires) and the chairman guarantee asserted at `council_cli.ts:1425-1429`. Verified: `classifyCliFailure('model_unsupported_on_transport')` → `other`; no producer anywhere emits the `model_unservable` token. | fixed | fixed — the classifier also matches `model_unsupported_on_transport`, the token the producers actually write. Matched here rather than renamed at the producers because `code:` values reach the events log and a rename would break every reader of the history. |
| 5 | medium | src/scripts/_lib/unattended_guard.ts:106 | `checkRemotes` parses only `(fetch)` rows, so a remote whose `pushurl` diverges from its `url` is judged on the fetch URL alone. Scenario: a worktree with `url = /srv/mirrors/repo.git` and `pushurl = git@github.com:org/repo.git` — `git remote -v` emits two rows, the parser keeps only the local fetch path, `isProductionRemote` returns false, and `checkRemotes` returns `{safe: true, reason: 'no production-reachable remote'}` while a push from that worktree reaches GitHub. The precondition fails OPEN, against this function's own stated posture ("the question is not which forge but can a push from here reach something real") and the fail-closed rule its docblock sets two lines above. Latent today only because the lane ships disabled (both ceilings `0`) and the spawn is deliberately unbuilt — the hole is in the guard, not in the lane. Fix: parse `(push)` rows too, or key on `name+url` pairs without the `(fetch)` anchor. Verified: `git -c remote.origin.pushurl=... remote -v` in this worktree emits the two-row shape; the regex matches only the fetch row. | fixed | fixed — both `(fetch)` and `(push)` rows are parsed and a remote is production if EITHER URL is. The precondition guards a PUSH and was judging the fetch URL, so a mirror-fetch/forge-push remote cleared it — fail-open in the one module whose stated posture is fail-closed. |
| 6 | medium | src/scripts/_lib/run_checkpoint.ts:375 | The new UNKNOWN state keys on `f.claimed === null \|\| f.actual === null`, a predicate written for `head` (where `null` means "could not read") and applied to all five fields — but for `next_step` `null` is a documented VALUE ("or `null` when none remains", :56) produced by `countRoadmap` when no open step is left. Two wrong outputs. (a) A checkpoint on a closed roadmap: every field agrees, `res.agrees` is true, and the report prints `UNKNOWN next_step: not readable (claimed null, actual null) — not compared` plus `the 4 comparable field(s) match; 1 could not be read and were not compared` — a false statement about a field that WAS read and DID match, on the one axis that says the work is finished. (b) The last open step landing between checkpoint and resume: `agrees` is false (so it drives `res.agrees` and the "the tree moved" line) yet the same block renders it `not compared` — verdict and render contradict each other in one output, hiding the transition a resumed run most needs. The loop contract (`roadmap-process-loop.md:389-397`) makes this report the resumed run's first act, so it is the mechanism's whole output surface. Untested: every fixture in `tests/scripts/run_checkpoint.test.ts:194-232` exercises `head` as the null field. Verified by driving `renderVerification` over both field sets. | fixed | fixed — UNKNOWN is scoped to `head`, the only field where `null` means "could not read". For `next_step` a null is a documented VALUE, so a finished roadmap was claiming a field had not been compared when it had. |
| 7 | medium | docs/contracts/ai-council-config.md:229-233 | The one copy-pasteable YAML example for `fallback.api_on_quota` (added by this diff) nests it under an `ai_council:` root that appears nowhere else in the document and that the loader never reads: `config.ts:904` does `buildFallback(_get(raw, 'fallback', {}))` against the TOP level of `.ai-council.yml`, the shipped template puts `fallback:` at column 0 (`.ai-council.yml.example:224`), and the wizard upserts `['fallback','api_on_quota']` at top level (`wizard.ts:931`). An operator who follows the paragraph directly above the block — "converting exhausted plan quota into API spend is a decision the operator states" — and pastes it gets `api_on_quota: false` with no error and no warning: the stated decision is silently discarded, and `ai_council:` is tolerated as an unknown top-level key. Verified: the documented nested shape loads to `fallback.api_on_quota = false`; the top-level shape loads to `true`. Fails safe (withholds spend), which is why this is medium and not high. | fixed | fixed — the example is top-level and says so, plus one line on why the root matters: a pasted `ai_council:` wrapper parses fine and resolves to the default, so the switch reads as set and is not. |
| 8 | medium | src/scripts/interruption_report.ts:428-431 | `asks` / `handbacks` / `contacts` / `halts` are built unconditionally from `rows`, so a run present in chat history but absent from `interruptions.jsonl` (the normal case for any session predating the ledger) emits `"contacts": 0, "asks": 0, "halts": 0` — while the report's OWN note at :464-467 tells the reader those runs are "reported with null contact counts rather than as zero contacts". A `--json` consumer computing "share of runs that contacted the user" reads no-instrument as measured-zero. That is the exact arithmetic the pre-registered `user-out-of-loop-baseline` claim forbids in falsification criterion (3): "a run present in the ledger but not the history, or the reverse, is reported with the missing axis null and is never scored as zero — scoring an unmeasured run as zero contacts is the arithmetic that would manufacture the result". Scoped medium because the headline medians DO filter on `ledgerRuns` (:478); only the per-run rows and the note disagree. Fix: type the four fields `number \| null` outside `ledgerRuns`, or correct the note. | fixed | fixed — the four ledger-derived fields are `null` for a run absent from the ledger, matching the report's own note. The aggregate median already excluded such runs; the ROWS a downstream reader consumes did not, which is the arithmetic falsification criterion (3) of the pre-registered baseline claim forbids by name. |
