# Findings: worktree-long-horizon
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 4292e4a7f3a36b0daa59776f857d398d672b4429453dea3c087aa78d39ebe845 | diff: 8c9041488ae67aa58d90fbd85fef39866bb2e921 | reviewer: r2-fresh-subagent-worktree-long-horizon | prompt_hash: b770c8796caad07253addc846002f00dfe8e52f74dc5dc30721b6149638415c3 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 8c9041488ae67aa58d90fbd85fef39866bb2e921
  scope_hash: 4292e4a7f3a36b0daa59776f857d398d672b4429453dea3c087aa78d39ebe845
  roadmap: agents/roadmaps/road-to-long-horizon-execution.md
  roadmap_hash: 89257b7a50dc2827c00e9a9f8ca568627c2d70da3aa48b2188d664a14a29884a
  ac_hash: cc0790f4722b1540ced5602c39fd46db00ac1547495b3ab0e6082de1ede2c413
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T02:51:29Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/hooks/run_continuation_hook.ts:95 | `TRANSCRIPT_READ_MAX_BYTES = 2 MB` here, while `turn_end_gate_hook.ts:182` exports the SAME-named constant at **8 MB** and passes it to the same `readTranscriptTail`. Over its cap that helper returns `empty` — `turnOrdinal: 0` — without throwing (`turn_end_gate_hook.ts:844`). So on any transcript between 2 MB and 8 MB the gate reads the real ordinal N, refuses the turn and writes `refused_turn: N`, while this concern reads **0**; `refusedThisTurn` then compares `N === 0`, returns false, and the ladder proceeds to `engage` and BLOCKS the stop the quality gate just refused. That is the single failure the design ranks #1 (Risk Register row 1), the hook's own difference #2 ("a turn refused for unverified claims is never re-engaged past the refusal"), and the manifest comment ("quality gates outrank continuation, always"). The comment on the line — "Same cap the interruption ledger reads its tail under" — is accurate about `interruption_ledger_hook.ts:67`, and that is the defect: the ledger only classifies a turn and never has to agree with the gate's ordinal, whereas this concern joins on it. Multi-hour 25-iteration runs are exactly the regime that produces >2 MB transcripts, and the dispatcher test drives a small fixture, so nothing sees it. | fixed | fixed — the cap is IMPORTED from `turn_end_gate_hook` so the two agree by construction, plus an explicit fail-open above it: over the shared cap the ordinal is untrusted and must not decide a block. A test builds a transcript in the old 2-8 MB dead zone and shows `under2mb === 0` vs `shared === 3`. |
| 2 | high | src/config/gate-violation-baselines.json:60 | `check_source_size_budget` is pinned at 19,604 and the branch head measures **19,656 — 52 new**, so the gate is RED as shipped (`npx tsx src/scripts/check_source_size_budget.ts --quiet`, the entry's own falsifier). The 52 is exactly attributable: `git diff --numstat 1fef09b51 8c9041488 -- src/scripts/ai_council/orchestrator.ts` is `53 1`, i.e. the finding-3 fix grew an already-over-ceiling file by net 52 after the baseline had been pinned. The note added in this same change-set asserts "Lowered 19,611 -> 19,604 on 2026-08-19 by the long-horizon branch", which no longer describes the tree it ships with, and the note's own closing lesson is "pin the baseline ONCE, at the end" — the discipline the R2 fix round then skipped. | fixed | fixed — the spend gate came out whole into `ai_council/spend_gate.ts` (CostBudget, _breach, OverrunEvent, _total_usd), re-exported so no caller changed; the baseline is LOWERED to 19,571, never re-pinned. The red is recorded in the budget history before the lowering is. |
| 3 | high | dist/agent-src/contexts/execution/roadmap-process-loop.md:384 | The projection was not regenerated after the finding-7 fix, so src and dist now disagree on exactly that hunk and the CONSUMER-facing copy still carries the defect the fix removed: dist says "The first act of a resumed run is `verifyCheckpoint` against the current tree" — the run-id-keyed path a relaunched session cannot perform — while `src/agent-src/.../roadmap-process-loop.md:384` says to look the checkpoint up by roadmap slug via `latestCheckpointFor`. `bash src/scripts/condense.sh --changed` reports exactly one stale projection, this file. Two gates miss it (`task sync-check` only checks counterpart existence; `check_condensation` skips `src/agent-src/`, which has no `dist/agent-src/` counterpart path), so it surfaces only at the consistency workflow's `task sync` + `git diff --quiet` step — as a CI red, after the artefact consumers install has already shipped the wrong instruction. | fixed | fixed — `task sync` run; `condense.sh --changed` reports every projection current, and the dist copy now carries `latestCheckpointFor`. |
| 4 | medium | src/scripts/ai_council/mid_flight_fallback.ts:239 | `escalateUnmetered` emits `outcome: 'retried'` at ESTABLISHMENT, before the round's projected-spend gate has run. When that gate then refuses, `orchestrator.ts:785` emits a second event with `outcome: 'cost_budget'` for the same escalation. One escalation therefore writes TWO `transport_fallback` lines with contradictory outcomes, and the `retried` one reports a seat as saved by the fallback when the twin was never called. It contradicts the contract (`ai-council-config.md`: "One line per ESTABLISHING escalation, not per substituted call") and `establishTwin`'s own docblock twelve lines up, which gives the reason for NOT emitting `retried` early: "emitting `retried` here would report an escalation that the budget then refused". The stated purpose of the action — separating a seat SAVED by the fallback from one that was natively api — is exactly what the false positive breaks. The cost-budget test (`orchestrator.test.ts:596`) passes no `on_event`, so the event stream on this path is unasserted. | fixed | fixed — `escalateUnmetered` emits nothing; the caller emits once on the outcome it observed (`refuseUnmeteredEscalation` for the refusal, `retried` immediately before the metered call). |
| 5 | medium | src/scripts/hooks/session_eol_hook.ts:384 | The only production writer of a checkpoint calls `buildCheckpoint(workspaceRoot, eolSessionKey(id), slug)` with no `BuildOptions`, so `opts.head ?? null` makes `RunCheckpoint.head` **always null** in every checkpoint the tree can produce. Three surfaces claim otherwise: the field's own docblock (`run_checkpoint.ts:58` — "Commit the tree was on. A resume onto a different commit is normal, and visible"), roadmap Phase 3.0 ("open / done / parked counts, the next open step, the head"), and the loop contract § 5d ("and the commit the tree was on"). `verifyCheckpoint` does not check `head` either, so a resumed run cannot see that the tree moved commits — the one thing the field exists for. `run_checkpoint.test.ts:103` injects `head` explicitly, which is why the 16 green tests cannot see the producer never sets it. | fixed | fixed — `buildCheckpoint` resolves HEAD off `.git` with no subprocess, handling the worktree case where `.git` is a FILE, packed-refs, and detached HEAD. Six tests, including the no-git null. |
| 6 | medium | src/server/routes/wizard.ts:924 | `set(['fallback','api_on_quota'], p.fallbackApiOnQuota)` routes through `replaceScalar`, which returns the template UNCHANGED when the key is not already present (`src/server/io/yamlIO.ts:105`) — `set` never appends, unlike `mergeIntoTemplate`. `fallback:` is a block this change-set introduces, so on every `.ai-council.yml` that predates it the POST returns `200 {ok:true}`, writes nothing, and the following GET still reads `api_on_quota: false`. The operator is told the billing-class switch was saved and it was not; the endpoint's own new comment says it exists "so the operator can flip the one BILLING-CLASS decision … without hand-editing". `wizard.aiCouncil.test.ts` seeds from the shipped template, which now carries the key at `.ai-council.yml.example:225`, so the round-trip passes on exactly the one file shape that is not the upgrade case. | fixed | fixed — new `upsertScalar` creates the nesting when absent. Not `mergeIntoTemplate`: its flat `a.b:` append is a top-level key a YAML reader never sees as nested, so the toggle would still be inert. |
| 7 | medium | src/scripts/_lib/council_fallback_wiring.ts:74 | The `on_event` sink calls `deps.emit` (wired to `events_log.appendEvent`) with no try/catch, and neither `emitOutcome` nor `escalateUnmetered` guards it either. `appendEvent` does `fs.mkdirSync` + `fs.appendFileSync` on `agents/runtime/council/events.log`, so an unwritable path, ENOSPC or ENOTDIR throws out of `_run_round`, out of `consult`, and kills the whole council pass — converting a recoverable single-seat cli failure into total loss. `appendQuorumEvent` (`events_log.ts:386`) wraps the identical call in try/catch and states the rule in its docblock: "attendance telemetry must never be able to kill a council pass". This module's own `REFUSAL_NAMES` comment states the same principle two blocks up ("a mechanism that turns a recoverable seat loss into a total loss is worse than no mechanism") and then leaves the telemetry path unguarded. | fixed | fixed — `latestCheckpointFor(repoRoot, slug)`; the loop contract and Risk 5 now name the reachable path. |
| 8 | low | src/scripts/ai_council/orchestrator.ts:795 | The budget-refusal path rolls back the substitution map (`twins?.delete(declared.name)`) so "a later round must re-decide", but nothing rolls back `MidFlightFallback.used`, which `attempt()` adds to permanently and which grants `'api'` at most once per provider per invocation (`transport_resolver.ts:505`). The later round therefore cannot re-decide: it spawns the dead cli binary again, fails again, is refused by the ledger, and loses the seat — the exact state `TwinMap`'s docblock says an invocation-scoped ledger without the map produces and calls "strictly WORSE than a per-round one". The rollback comment describes an outcome the ledger makes unreachable. | fixed | fixed — `MidFlightFallback.release` returns the once-per-provider claim when the twin was built and never called, so the seat can re-decide instead of re-spawning a dead binary every round. |
| 9 | low | src/scripts/run_supervise.ts:55 | `MAX_RELAUNCHES_PER_RUN` is documented "UOTL 6.2 verbatim: at most three relaunches per run" and surfaced in USAGE, but the ledger it is compared against is keyed by `rec.session_id` (`classify:141`) — and a relaunch by construction produces a NEW session id with its own register record and its own count of 0. Each generation of a chronically-dying run therefore gets a fresh budget of three, so the cap bounds a death rather than a run and cannot produce the `budget-exhausted` disposition it exists for. Inert today because `writeLedger` has no caller (finding 13's note), which is why no test can see it; the key space is wrong now and will silently mis-bound a spend-bearing loop the moment the Phase 4.0 spawn increments it. | fixed | fixed — the relaunch ledger is keyed by ROADMAP, the key that survives the new session id a relaunch produces. A test pins the cap binding across generations, and another pins the old session key reading as never-relaunched. |
| 10 | low | src/scripts/hooks/run_continuation_hook.ts:230 | `scanOpenSteps` reads the next step's verify line only from `<!--\s*verify:\s*…-->` on the step's OWN line. 18 roadmaps in `agents/roadmaps/` — including both roadmaps this branch ships (`road-to-long-horizon-execution.md:160`, `road-to-council-api-fallback.md`) — write it instead as a backticked `` `verify:` `` on the bullet's continuation line, which this regex cannot match and which is not on `m[1]` anyway. For those roadmaps `scan.next.verify` is always null and the continuation prints the step with no verify line, which is the half Phase 1.0 ("naming the next step + its `verify:` line") and Risk Register row 7 ("The continuation must name the next unchecked step AND its `verify:` line") name as the mitigation against a bare keep-going prompt. The only positive test (`run_continuation.test.ts:81`) uses the HTML-comment shape, so the suite pins the convention the target roadmaps do not use. | fixed | fixed — `extractVerify` reads both forms and gathers the step's continuation lines; the comment form wins when both are present. Five tests including the do-not-absorb-the-next-step case. |
