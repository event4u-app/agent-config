# Findings: worktree-long-horizon
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: cfe2ad720bdc641950609441409050f1e621644810dc2d7f166c18a818a3d0dc | diff: f631031b7748e016df070f8bf2a01060779dfe93 | reviewer: r2-fresh-subagent-worktree-long-horizon | prompt_hash: 2f28eee74523ba818ab014f9a063d592ff84d02c276106e397d7b5ba8f2bb271 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: f631031b7748e016df070f8bf2a01060779dfe93
  scope_hash: cfe2ad720bdc641950609441409050f1e621644810dc2d7f166c18a818a3d0dc
  roadmap: agents/roadmaps/road-to-long-horizon-execution.md
  roadmap_hash: 9a5c0864ba80c51d5f1c88a5719479775391a4b0d754a757a7d0b6619a95dbbc
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T02:11:54Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/hook_manifest.yaml:688 | `run-continuation` is declared `severity: advisory`, and `dispatch_hook.ts:1273` downgrades EXIT_BLOCK to EXIT_WARN for any advisory concern ("on any host"); `host_semantics.emitFor` then maps stop+warn to **exit 0** plus `additionalContext`. The concern's `return EXIT_BLOCK` therefore never blocks a Stop and the whole Phase-1 re-engagement mechanism is inert on the one verified host. Its own docblock ("returns the dispatcher's block verdict … the dispatcher translates a stop-slot block to host exit 2") and the manifest comment ("BLOCKS the stop") both describe behaviour the manifest declaration forbids; the precedent it cites, `turn-end-gate`, declares `severity: blocking` (line 613). The new dispatcher test asserts only the event log and explicitly declines to pin the exit code, so nothing catches it. | open | |
| 2 | high | src/scripts/interruption_report.ts:371 | The Phase 5.0 autonomy axis joins three sources on `run_id`, but the branch writes three different key spaces: the interruption ledger uses `derive_session_tag` = `sha256(id).slice(0,16)` (`chat_history.ts:326`), `run-continuation.jsonl` writes `deriveSessionKey` = `sha256(id).slice(0,32)` (`run_continuation_hook.ts:353`), and `supervise-relaunches.json` is keyed by the **raw** `session_id` (`run_supervise.ts:129`). Map and record lookups are exact, so `reengagements`, `stall_halts`, `stall_halt_rate` and `relaunches` read 0 / 0% for every real run while data exists. The report deliberately prints "NO INSTRUMENT" for two other axes to avoid a misleading zero — these four print exactly that zero. The tests hand-write `r1` into all sources, so they verify the reader and cannot see a producer/consumer mismatch. | open | |
| 3 | high | src/scripts/ai_council/orchestrator.ts:771 | A non-billable vendor-CLI seat that escalates now falls through into the projected-spend gate priced as its metered twin. When `on_overrun` is null or `estimates` is null (e.g. the chairman `consult`), a breach takes the v1 short-circuit and pushes `_aborted(left, 'cost_budget_exceeded')` for **every remaining member**. Before this change a `billable:false` member `continue`d before that branch, so a dead free CLI could only ever cost its own seat; now a missing vendor binary near a `max_total_usd` or token cap can terminate the whole round. That contradicts the shipped claim `council-fallback-loses-zero-seats` in docs/CLAIMS.md. On the `on_overrun`-false path the seat is aborted as the twin, the original cli error is discarded, and no `fallback_skipped: cost_budget` is set — the marker the contract promises for a budget-refused fallback. | open | |
| 4 | high | src/scripts/hooks/run_continuation_hook.ts:405 | Every non-`engage` ladder action — including `halt-max-iterations`, `halt-wall-clock` and `halt-stall` — deletes the state file. The next Stop finds `prev === null`, builds a fresh state (`iterations: 0`, `started_at: now`, empty history) and engages again, so the iteration cap is per-25-block rather than per-run and the 4 h clock restarts. The file itself documents that "a host may fire `stop` more than once for one reply", which re-arms the loop inside the same reply. The clear is justified in the comment only for the `complete` case. Risk 2 of the roadmap's register ("the agent loops forever on a roadmap it cannot finish") is therefore not mitigated by the rungs it names; the tests exercise only the pure `ladder()` function, never the clear-then-refire path. | open | |
| 5 | medium | src/scripts/_lib/unattended_guard.ts:209 | `UnattendedBudget.max_tokens` is documented as "`0` disables" (line 129) and `emptyBudget` repeats it, but `checkBudget` refuses only when **both** ceilings are 0 and then skips the token ceiling entirely under `b.max_tokens > 0`. An operator writing `{max_usd: 5, max_tokens: 0}` to disable token spend gets an unbounded token allowance instead; `digest()` then prints `N/0 tokens`, which reads as an exhausted cap while runs are permitted. | open | |
| 6 | medium | src/scripts/run_supervise.ts:286 | The digest's memo scan puts the `try` **outside** the loop: one non-directory entry under `agents/runtime/state/decisions/` (a `.DS_Store`, a stray note) throws ENOTDIR, aborts the whole walk and reports whatever partial count was reached — 0 when the bad entry comes first. The sibling implementation of the identical scan, `interruption_report.readMemoCounts`, puts the `try` inside the loop and comments the case ("a file where a directory was expected — not a run"); the two disagree on the same data. | open | |
| 7 | medium | src/scripts/hooks/session_eol_hook.ts:384 | The checkpoint is written under `eolSessionKey(session_id)` — the full sha256 of the **dying** session's id — and nothing in the tree ever reads one: `verifyCheckpoint` and `renderVerification` have zero production call sites, and `run_supervise.ts:400` only re-exports `readCheckpoint` under the comment "so a resumed run can read its own checkpoint through one path", which a relaunched session (new id, no slug index) cannot perform. Meanwhile `roadmap-process-loop.md` and its dist projection instruct a resumed run that "the first act … is `verifyCheckpoint`" against `agents/runtime/state/checkpoints/<run>.json`. Risk 5's stated mitigation ("3.2 makes re-verification the relaunched session's first act") is unreachable as shipped. | open | |
| 8 | medium | src/scripts/ai_council/orchestrator.ts:756 | The breach decision was correctly re-priced for a substituted twin (`est` recomputed at :736, `projected_total_usd` at :760), but `OverrunEvent.next_estimate` still passes `estimates[idx]` — the pre-loop row for the **unmetered declared** seat. The interactive confirm therefore shows a ~$0 next-call estimate for the very call the gate just refused as over budget, i.e. exactly the "$0 pricing of a metered call" the comment two lines above says it fixed. | open | |
| 9 | medium | agents/roadmaps/road-to-long-horizon-execution.md:448 | Acceptance criteria ARE present in the roadmap — five checkbox bullets under `## Done means` — but in neither shape the dispatcher's extractor recognises (`## Acceptance criteria`, or inline `- **AC-n:**`), so `acceptance-criteria.md` is empty and this artefact's header records `ac_hash: none`. The AC-versus-diff half of this review was silently skipped rather than declared unavailable. Reported per the prompt's explicit instruction to decide which of the two causes produced the empty extraction. | open | |
| 10 | medium | src/scripts/decision_memo.ts:169 | `nextIndex(dir)` followed by `writeFileSync` is a read-then-write with no lock and no `wx` flag. Two memos written for the same run id concurrently (the unattended or parallel lane this roadmap builds toward, or two shell calls) compute the same index and the second silently overwrites the first. That breaks the property the module header sells as its reason to exist — "the index is monotonic and gap-free per run, so … a reader can tell a pruned memo from an unwritten one" — because a lost memo leaves no gap at all. | open | |
| 11 | low | src/scripts/_lib/run_checkpoint.ts:79 | `countRoadmap`'s regex accepts only a `-` bullet and lowercase `x`, and `scanOpenSteps` (`run_continuation_hook.ts:180`) is narrower still, while the dashboard's `CHECKBOX_RE` (`update_roadmap_progress.ts:68`) accepts `[-*]` bullets and `[ xX~-]`. A roadmap authored with `*` bullets or `[X]` reads `open === 0` here, so `run_supervise.classify` reports disposition `complete` — "the run finished, the session just never said so" — for a run with real open work, which is the exact confusion the watcher exists to remove. Both docblocks claim the vocabulary is `update_roadmap_progress.ts`'s. | open | |
| 12 | low | src/scripts/ai_council/response_render.ts:31 | `response_render` defines a private `_metaGet` that returns the default whenever the value is `undefined`, while `py_parity._metaGet:54` — added in the same change-set, and the implementation the extracted body used before the move — returns the stored value whenever the key is present (`key in obj`). A present-but-`undefined` `billable` or `tokens_estimated` renders differently after the move, in a file documented as extracted plumbing and in a branch whose stated rationale is de-duplication. | open | |
| 13 | low | src/scripts/_lib/unattended_guard.ts:299 | `preflight` reads the job ledger but never writes it, and `writeJobs` / `bookSpend` / `writeBudget` have no production caller — so two callers racing the same `roadmap@head` both get `ok: true` and the dedup precondition the module lists as one of its three cannot fire. Same shape in `run_supervise.writeLedger:100`, whose catch comment asserts "the caller treats a write failure as a stop below" while no caller exists. Consistent with the deliberately-unbuilt spawn, but the comments state controls that are not there. | open | |
| 14 | low | src/scripts/run_supervise.ts:321 | `--interval SECONDS` is documented in USAGE with a default of 60 and is read by nothing (`argValue` is never called for it); the documented default behaviour (loop) unconditionally returns exit 2 with "looping is not implemented yet". An operator following the command's own help gets a non-zero exit and no watcher. | open | |
| 15 | low | src/scripts/hooks/run_continuation_hook.ts:187 | `scanOpenSteps` and `countRoadmap` scan the whole file, while the dashboard attributes checkboxes to `## Phase` spans. On the roadmap under review every phase step is closed and the only `- [ ]` sits under `## Done means`, so the continuation would name "A killed session resumes via the watcher…" — an observation of a live run, not an executable step — as the next step, and by construction with no `verify:` line. Re-engaging on acceptance-criteria and blocker checkboxes is a guaranteed stall of the kind the `blocked-by:` exclusion was added to avoid. | open | |
