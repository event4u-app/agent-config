# Findings: drain-delivery-for-every-host
<!-- completion-review: v1 | reviewed: 2026-09-08 | scope: d28fdd6d596504189c29bd75419a3da48eeec22876ab20a9c206200ee1bb9408 | diff: cbb26d378fc54365751f1dc58b813dd79fca5a73 | reviewer: r2-fresh-subagent-drain-delivery-for-every-host | prompt_hash: 43641691ac1462c1e331867877ba8f2dca53d15b4ed719526620279071a1c7cd -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-delivery-for-every-host"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-08 -->

<!-- context-manifest: v1
inputs:
  diff_sha: cbb26d378fc54365751f1dc58b813dd79fca5a73
  scope_hash: d28fdd6d596504189c29bd75419a3da48eeec22876ab20a9c206200ee1bb9408
  roadmap: agents/roadmaps/road-to-delivery-for-every-host.md
  roadmap_hash: fc3c0e21f32e962760cb419489088a929ae509e04ca76f98c217ad1d033f16d3
  ac_hash: a4845f2ffc491dee07e6da04ac1d7feac7e4557874aea2dee6e39b6ee9c247dc
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-08T10:19:22Z
-->
<!-- rebind: v1
rebound: 2026-09-08T13:20:00Z
  RE-BOUND IN PLACE per contract 2.7 path 1 after the remediation of all 14
  rows. scope 8aed75f4b813 -> d28fdd6d5965, diff 0311da7c4d9d -> cbb26d378fc5,
  roadmap_hash 168ce7480f71 -> fc3c0e21f32e (the roadmap gained the step-3.2
  amendment for finding 3). ac_hash is unchanged, so the acceptance criteria
  this round was reviewed against did not move. `reviewer` and `prompt_hash`
  are deliberately NOT updated: the prompt package under
  `drain-delivery-for-every-host.review-input/` is the one that produced these
  14 findings, and rewriting it would destroy the binding
  `check_review_prompt_binding` exists to keep. It therefore describes the
  round-1 scope, not this one, and that is stated rather than papered over.
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/project_thin_rules.ts:106 | `path_only_ids` classifies a rule path-only only when EVERY trigger is path-shaped (`kinds.every`). Rules with BOTH path and non-path triggers are therefore thinned on claude-code, and their path-shaped half then reaches the host at no scope: the stub carries no frontmatter and is written by `_writeText` (condense.ts:1186), so `_emit_claude_rule`'s host-native `paths:` key is bypassed; `pre_tool_use` lost the binding (hook_manifest.yaml:1283); and `user_prompt_submit` never populates `openFiles` (rule_inject_hook.ts:289-301), so a path trigger cannot match there. Verified over dist/router.json: 18 such rules are thinned (design-fidelity, doc-screenshot-hygiene, lethal-trifecta-guard, image-likeness-and-rights, settings-ask-protocol, roadmap-ci-steps-policy, augment-edit-discipline, …). The branch's own artifact measures 32 gate-open `pre_tool_use` fires covering 21 rules, of which only 3 are exempt — so 18 of those 21 deliveries silently become zero while the manifest comment states the affected rules "lose nothing". | deferred | Receiver: `agents/roadmaps/stubs/road-to-a-path-route-under-delivery.md`. The finding is real and MEASURED: 18 rules carry both path and non-path triggers, all thinned. Two narrowings established by running branch code — they are NOT unreachable (shipped `user_prompt_submit` reach is 98/101 labelled rules, and no thinned rule is in the difference), and on claude-code they never held a separate host-native path route to lose, because `src/install/claudePathsPlan.ts:250` emits no `paths:` for a mixed-trigger rule on purpose, so under `eager-all` they loaded UNCONDITIONALLY. What is lost is unconditional-vs-prompt-triggered. Both closures are owner-reserved: widening the exemption to `some` costs +16,329 GPT tok (23,592 -> 39,921, +69 %), and re-binding `pre_tool_use` needs its 2,048 B slot sum raised against a measured 19,649 B p90. A third option (host-native `paths:` on the stub) was designed and rejected — it composes under `delivery` and breaks `thin`. FIXED in `08707501c`: the endpoint now publishes the shipped reach and names the path-only-reachable rules, `loadCorpus` parses the `command:` field it had dropped, and `path_only_ids` / `hook_manifest.yaml` / ADR-265 Consequences all name the mixed class, so the tree no longer reads as covering it. |
| 2 | high | src/scripts/_lib/hook_settings.ts:159 | The indentation reader for `lean_projection.hosts` requires `indent > blockIndent`, so the legal YAML block-sequence written at the KEY's own indent (`hosts:` then `  - cursor`) parses as `[]`. Verified by probe: same-indent -> `[]` -> `resolveLeanProjectionHosts` reads that as "nothing written" and returns the default `[claude-code]`, while `condense._lean_projection_settings` parses the same file with the real YAML loader and thins `cursor`. The two readers then disagree about the delivery-host set — `check_host_tree_parity` (check_host_tree_parity.ts:142) excludes claude-code from the parity comparison and asserts parity on the host that actually WAS thinned, and `check_rule_projection_integrity` reports every cursor stub as a `thinned` defect. The docstring claims coverage of "the two list shapes a human writes"; only the deeper-indent one is covered, and no test exercises the block form at all. | fixed | `e1743b508` — `indent >= blockIndent`, so the same-indent block sequence parses. 3 of 4 new fixtures in `tests/scripts/lean_projection_hosts.test.ts` seen RED first; the fourth pins the shallower-item direction and was green before and after. Note on the finding text: block-sequence fixtures did exist, but only for the deeper-indented form. |
| 3 | high | src/config/hook-token-budget.json:39 | The activation charge is set to the p90 gate-open fire size (16,384 B) while the concern's own hard cap is `CAP_BYTES = 20480` (rule_inject_hook.ts:115) and the branch's measured max fire is 20,406 B. `per_slot_sum_caps_bytes` is a CROSS-CONCERN SUM for a slot that carries 12 other concerns on claude, so the top decile of rule-inject fires alone already exceeds the whole slot's budget. Either the cap is unenforced or a delivery is truncated/dropped — and a truncated body is the D1 failure the flip is meant to repair. Choosing p90 rather than the concern's own cap makes the overrun designed-in rather than accidental; nothing in the row or the ADR reconciles 20,480 with 16,384. | fixed | `80769d76a` — reconciled DOWNWARD, never by raising the slot: `CAP_BYTES` and the `rule-inject` row are both 16,384 (E2's own charge). The two numbers were one statistic in two units. Answer to the finding's dilemma: TRUNCATED — `selectForInjection` drops whole bodies at emission. Measured cost: fires truncated 33 -> 45, bodies withheld 63 -> 88, p90 16,865 -> 14,507 B. Residue stated, not closed: the cross-concern slot sum is authoring-time only; runtime enforces `per_turn_aggregate_bytes.ceiling_bytes` alone. Tripwire seen RED first. |
| 4 | medium | src/scripts/hooks/rule_inject_hook.ts:14 | "DEFAULT OFF, AND OFF MEANS ZERO BYTES … Under every shipped default it emits nothing, costs no injection budget, and leaves the standing corpus exactly as it is today … there is no measured emission to register yet" is now false in all four clauses: the shipped template carries `mode: delivery`, `gateOpen` (line 252) returns true on that mode, and this change registers a measured emission. The diff edits this very docstring (adding the 2.4 compaction contract) and leaves the contradicted paragraph above it standing. | fixed | `e1743b508` — the paragraph is rewritten to state what ADR-265 ships and to keep only the half that still holds (off still means zero bytes, and `gateOpen` is what decides). |
| 5 | medium | src/scripts/hooks/rule_inject_hook.ts:252 | `gateOpen` keys on `lean_projection.mode` alone and ignores `lean_projection.hosts`, while the projector gates stub-writing on both (`thinsHost`). With `mode: delivery` and a hosts list that resolves to empty — the documented "thin nothing" state, which is also what a fully typo'd list produces — claude-code keeps a full-bodied tree AND the hook still injects bodies on trigger match, so every matched rule is delivered twice. Same for `hosts: [cursor]`: cursor gets stubs with no bound slot, claude gets body-plus-injection. | fixed | `e1743b508` — `gateOpen` now requires `deliversBodies(mode) && hosts.includes(DELIVERY_HOST)`. `deliversBodies` is checked separately from the host list rather than via `thinsHost`, which is also true for `thin`. 2 of 5 new fixtures seen RED first. |
| 6 | medium | src/scripts/schemas/agent-settings.schema.json:45 | Both schemas constrain `lean_projection.hosts` items to `enum: [claude-code, cursor, cline]` (and `src/server/schemas/settings.ts:35` mirrors it with `z.enum`), so any other id is a hard validation failure — yet both descriptions, and `describeDroppedHosts`/`KNOWN_NON_THINNABLE_HOSTS`, promise that such an id "is dropped with a warning". For any config that passes validation the drop path and its two carefully-worded reasons are unreachable; for one that does not, the operator gets a validation error rather than the documented warning. The behaviour contract and the schema state opposite things. | fixed | `f121cf975` — documentation, because there is no contradiction to remove: the enum is the AUTHORING gate (`validate_agent_settings`, the wizard) and `resolveLeanProjectionHosts` is the READ-TIME gate for a hand-edited `.agent-settings.yml`, which `condense._lean_projection_settings` and `rule_inject_hook.gateOpen` both read with no validation in between. Both descriptions and the resolver docstring now say which layer answers. |
| 7 | medium | docs/CLAIMS.md:365 | The re-measured figures (616/616 byte-equal, 101/101 reachable, 0 of 212 near-misses, $0.7167 vs $4.0401, latency p95 125/81 ms) are attributed to `internal/bench/reports/thin-inject-2026-08-23.md#endpoints` — the same pointer that previously carried 579/579 and 94/94 — and that report is NOT among this branch's changed files. `check_claims` only checks that the pointer resolves, so the row stays `backed` while the cited artifact predates the 2026-09-07 re-measurement it is now credited with. ADR-265's Evidence table and docs/proof.md:103 inherit the same pointer. | fixed | `ae3267ff5` — pointer moved to `internal/bench/reports/thin-inject-2026-09-08.md` (tracked in `966940967`), written from the run it reports; ADR-265 Evidence rows and `docs/proof.md` follow. The 2026-08-23 report is kept, not edited: it describes the run it does describe. The figures moved with the pointer and both movements are stated in the row. |
| 8 | medium | src/scripts/report_standing_payload_by_host.ts:198 | `assertWritersResolve` checks only that the cited file exists and that the line number is within the file's line count — it never inspects the cited line. A citation that drifted by one commit (the failure the docstring at line 69 says this replaces prose assertion to prevent) passes unchanged, and so would a wholesale renumbering, as long as the file is long enough. The check is a bounds test presented as a provenance test; the diff even moves the surrounding condense.ts code, which is exactly when drift happens. | fixed | `2145f1844` — every `HostSurface` carries an `anchor` the cited LINE must contain, so a drifted or renumbered citation is reported. Fixture seen RED against a 6,000-line file carrying none of the anchors, which passed the old bounds test. |
| 9 | medium | src/scripts/report_standing_payload_by_host.ts:444 | `--pin` is free text with no comparison against `headSha(root)`, but the emitted artifact opens with "Pinned to commit <sha> … a re-run at the same pin is byte-identical". Every figure is read from the working tree, so the pin documents nothing and the reproducibility sentence is only true if the tree happens to be unchanged. The committed artifact carries pin 9ae0ff28… while the branch head is 0311da7c…, and the branch has since edited condense.ts, whose line numbers the artifact cites. | fixed | `2145f1844` — `pinProblem` makes `--pin` an assertion about HEAD, and `treeIsDirty` refuses `--emit` when the paths the figures are read from are dirty (`--allow-dirty` is the explicit escape). The stale committed artifact was re-emitted from a clean tree in `23e390baf`'s successor, so its pin is now honest under the new assertion. |
| 10 | low | src/scripts/report_standing_payload_by_host.ts:354 | `readCorpusPositives` is a hand parser that accepts only `- prompt: "…"` with a double-quoted same-line scalar and only an inline `open_files: [...]`. Any other legal YAML shape (single quotes, unquoted, folded, block sequence for open_files) is silently skipped, dropping fires from the distribution the 16,384-byte cap is derived from. A measurement that governs a budget row should fail loudly on a shape it cannot read rather than under-count. | fixed | `2145f1844` — `corpusShapeProblems` reports every unreadable entry with `file:line` and the CLI refuses rather than under-counting. Reported rather than parsed: the script must not grow a second YAML implementation and the corpus is frozen. Fixture seen RED on a single-quoted prompt that had parsed to `[]` silently. |
| 11 | low | src/scripts/project_thin_rules.ts:94 | `path_only_ids()` is a second implementation of `_lib/rule_injection.ts::pathOnlyRuleIds`, which the report script added in this same diff imports (report_standing_payload_by_host.ts:151-156). Verified identical over the current router (symmetric difference empty), so this is drift risk rather than a live defect — but it is the exact hazard the neighbouring `THIN_ENTRY_MARKER` comment argues against ("a gate that re-spelled this string would drift from the writer silently"), and the two consumers here are the projector's exemption set and the injector's reachability set. | fixed | `08707501c` — `path_only_ids` delegates to `_lib/rule_injection.ts::pathOnlyRuleIds`. Defect-pattern sweep found the SECOND instance one function down (`no_trigger_ids` vs `triggerlessRuleIds`) and it is delegated in the same change. Count: 2 of 2 duplicated router-derived sets in the file, 0 remaining. Verified identical output over the current router and `thin_gpt` unchanged at 23,592. |
| 12 | low | src/scripts/check_host_tree_parity.ts:174 | The gate uses non-asserting `reportScanned` and exits 0 when `compared === 0`. With all three thinnable hosts enrolled in `lean_projection.hosts` — the configuration in which cursor and cline are thinned with no bound slot to deliver a body back — the gate scans nothing and prints a green "0 non-delivery host tree(s) byte-identical". The one configuration it exists to police is the one in which it is a no-op. | fixed | `f121cf975` — `parityScopeNotice` returns a `⚠️  NOTHING COMPARED` line naming the gate that does carry the question (`check_rule_projection_integrity.thinnedTreeFindings`). Deliberately NOT exit 1: all three hosts enrolled is a legal configuration, and reddening a legal configuration is how a gate gets switched off. Fixture neutralised (`if (true) return null`) and seen RED, then restored. |
| 13 | low | src/scripts/check_host_tree_parity.ts:142 | `configured` is resolved without consulting `lean_projection.mode`, unlike the sibling gate's `_lean_projection_settings_for` (check_rule_projection_integrity.ts), which checks the mode first precisely so "a `hosts:` list left behind after a rollback exempts nothing". Here a rolled-back `eager-all` with a leftover `hosts: [claude-code]` still excludes `.claude/rules` from the byte-identity comparison, so the two gates disagree about which trees are under assertion after a rollback. | accepted-risk | `f121cf975` — accepted as correct-as-is, and the mode-first variant was WRITTEN AND RUN before accepting: it reds this gate on this repository's own settings, which carry no `.agent-settings.yml` at all, so `configured` resolves empty while `main` still seeds its fixture with `mode: delivery` and every stub in the delivery tree is reported as a parity break. The two gates ask different questions — the sibling about the CURRENT tree, this one a counterfactual about the PROJECTOR, which supplies the mode itself. The docstring now records the divergence, the running evidence, and the consequence the finding correctly names: after a rollback a leftover `hosts:` list still exempts a tree here, which hides nothing because under `eager-all` nothing is thinned and the on-disk state is the sibling gate's subject. |
| 14 | low | src/scripts/report_standing_payload_by_host.ts:458 | The success line hardcodes `HOST_SURFACES.length - 1` for both numerator and denominator of "writer citations resolved", deriving the `-1` from the single current `writer: '—'` row. Adding a second surface-less host silently reports a wrong count, and the ratio can never read anything but N/N even though `assertWritersResolve` already returns the exact problem list. Count the non-`—` writers instead of subtracting a constant. | fixed | `2145f1844` — `citedWriterCount()` counts the non-`—` writers and the numerator subtracts the actual problem list. Fixture seen RED on the missing export. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
