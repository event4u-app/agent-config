# Findings: drain-delivery-for-every-host
<!-- completion-review: v1 | reviewed: 2026-09-08 | scope: 8aed75f4b813b39e98b662af6b2ab59c5c69d07ce29707e3991f6490b71feba7 | diff: 0311da7c4d9d547be599eb90c1f86961878504de | reviewer: r2-fresh-subagent-drain-delivery-for-every-host | prompt_hash: 43641691ac1462c1e331867877ba8f2dca53d15b4ed719526620279071a1c7cd -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-delivery-for-every-host"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-08 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 0311da7c4d9d547be599eb90c1f86961878504de
  scope_hash: 8aed75f4b813b39e98b662af6b2ab59c5c69d07ce29707e3991f6490b71feba7
  roadmap: agents/roadmaps/road-to-delivery-for-every-host.md
  roadmap_hash: 168ce7480f71175464ab0e7a19fed5ae342221fd4ce67dfe248d4f27b5c8a6bc
  ac_hash: a4845f2ffc491dee07e6da04ac1d7feac7e4557874aea2dee6e39b6ee9c247dc
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-08T10:19:22Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/project_thin_rules.ts:106 | `path_only_ids` classifies a rule path-only only when EVERY trigger is path-shaped (`kinds.every`). Rules with BOTH path and non-path triggers are therefore thinned on claude-code, and their path-shaped half then reaches the host at no scope: the stub carries no frontmatter and is written by `_writeText` (condense.ts:1186), so `_emit_claude_rule`'s host-native `paths:` key is bypassed; `pre_tool_use` lost the binding (hook_manifest.yaml:1283); and `user_prompt_submit` never populates `openFiles` (rule_inject_hook.ts:289-301), so a path trigger cannot match there. Verified over dist/router.json: 18 such rules are thinned (design-fidelity, doc-screenshot-hygiene, lethal-trifecta-guard, image-likeness-and-rights, settings-ask-protocol, roadmap-ci-steps-policy, augment-edit-discipline, …). The branch's own artifact measures 32 gate-open `pre_tool_use` fires covering 21 rules, of which only 3 are exempt — so 18 of those 21 deliveries silently become zero while the manifest comment states the affected rules "lose nothing". | open | |
| 2 | high | src/scripts/_lib/hook_settings.ts:159 | The indentation reader for `lean_projection.hosts` requires `indent > blockIndent`, so the legal YAML block-sequence written at the KEY's own indent (`hosts:` then `  - cursor`) parses as `[]`. Verified by probe: same-indent -> `[]` -> `resolveLeanProjectionHosts` reads that as "nothing written" and returns the default `[claude-code]`, while `condense._lean_projection_settings` parses the same file with the real YAML loader and thins `cursor`. The two readers then disagree about the delivery-host set — `check_host_tree_parity` (check_host_tree_parity.ts:142) excludes claude-code from the parity comparison and asserts parity on the host that actually WAS thinned, and `check_rule_projection_integrity` reports every cursor stub as a `thinned` defect. The docstring claims coverage of "the two list shapes a human writes"; only the deeper-indent one is covered, and no test exercises the block form at all. | open | |
| 3 | high | src/config/hook-token-budget.json:39 | The activation charge is set to the p90 gate-open fire size (16,384 B) while the concern's own hard cap is `CAP_BYTES = 20480` (rule_inject_hook.ts:115) and the branch's measured max fire is 20,406 B. `per_slot_sum_caps_bytes` is a CROSS-CONCERN SUM for a slot that carries 12 other concerns on claude, so the top decile of rule-inject fires alone already exceeds the whole slot's budget. Either the cap is unenforced or a delivery is truncated/dropped — and a truncated body is the D1 failure the flip is meant to repair. Choosing p90 rather than the concern's own cap makes the overrun designed-in rather than accidental; nothing in the row or the ADR reconciles 20,480 with 16,384. | open | |
| 4 | medium | src/scripts/hooks/rule_inject_hook.ts:14 | "DEFAULT OFF, AND OFF MEANS ZERO BYTES … Under every shipped default it emits nothing, costs no injection budget, and leaves the standing corpus exactly as it is today … there is no measured emission to register yet" is now false in all four clauses: the shipped template carries `mode: delivery`, `gateOpen` (line 252) returns true on that mode, and this change registers a measured emission. The diff edits this very docstring (adding the 2.4 compaction contract) and leaves the contradicted paragraph above it standing. | open | |
| 5 | medium | src/scripts/hooks/rule_inject_hook.ts:252 | `gateOpen` keys on `lean_projection.mode` alone and ignores `lean_projection.hosts`, while the projector gates stub-writing on both (`thinsHost`). With `mode: delivery` and a hosts list that resolves to empty — the documented "thin nothing" state, which is also what a fully typo'd list produces — claude-code keeps a full-bodied tree AND the hook still injects bodies on trigger match, so every matched rule is delivered twice. Same for `hosts: [cursor]`: cursor gets stubs with no bound slot, claude gets body-plus-injection. | open | |
| 6 | medium | src/scripts/schemas/agent-settings.schema.json:45 | Both schemas constrain `lean_projection.hosts` items to `enum: [claude-code, cursor, cline]` (and `src/server/schemas/settings.ts:35` mirrors it with `z.enum`), so any other id is a hard validation failure — yet both descriptions, and `describeDroppedHosts`/`KNOWN_NON_THINNABLE_HOSTS`, promise that such an id "is dropped with a warning". For any config that passes validation the drop path and its two carefully-worded reasons are unreachable; for one that does not, the operator gets a validation error rather than the documented warning. The behaviour contract and the schema state opposite things. | open | |
| 7 | medium | docs/CLAIMS.md:365 | The re-measured figures (616/616 byte-equal, 101/101 reachable, 0 of 212 near-misses, $0.7167 vs $4.0401, latency p95 125/81 ms) are attributed to `internal/bench/reports/thin-inject-2026-08-23.md#endpoints` — the same pointer that previously carried 579/579 and 94/94 — and that report is NOT among this branch's changed files. `check_claims` only checks that the pointer resolves, so the row stays `backed` while the cited artifact predates the 2026-09-07 re-measurement it is now credited with. ADR-265's Evidence table and docs/proof.md:103 inherit the same pointer. | open | |
| 8 | medium | src/scripts/report_standing_payload_by_host.ts:198 | `assertWritersResolve` checks only that the cited file exists and that the line number is within the file's line count — it never inspects the cited line. A citation that drifted by one commit (the failure the docstring at line 69 says this replaces prose assertion to prevent) passes unchanged, and so would a wholesale renumbering, as long as the file is long enough. The check is a bounds test presented as a provenance test; the diff even moves the surrounding condense.ts code, which is exactly when drift happens. | open | |
| 9 | medium | src/scripts/report_standing_payload_by_host.ts:444 | `--pin` is free text with no comparison against `headSha(root)`, but the emitted artifact opens with "Pinned to commit <sha> … a re-run at the same pin is byte-identical". Every figure is read from the working tree, so the pin documents nothing and the reproducibility sentence is only true if the tree happens to be unchanged. The committed artifact carries pin 9ae0ff28… while the branch head is 0311da7c…, and the branch has since edited condense.ts, whose line numbers the artifact cites. | open | |
| 10 | low | src/scripts/report_standing_payload_by_host.ts:354 | `readCorpusPositives` is a hand parser that accepts only `- prompt: "…"` with a double-quoted same-line scalar and only an inline `open_files: [...]`. Any other legal YAML shape (single quotes, unquoted, folded, block sequence for open_files) is silently skipped, dropping fires from the distribution the 16,384-byte cap is derived from. A measurement that governs a budget row should fail loudly on a shape it cannot read rather than under-count. | open | |
| 11 | low | src/scripts/project_thin_rules.ts:94 | `path_only_ids()` is a second implementation of `_lib/rule_injection.ts::pathOnlyRuleIds`, which the report script added in this same diff imports (report_standing_payload_by_host.ts:151-156). Verified identical over the current router (symmetric difference empty), so this is drift risk rather than a live defect — but it is the exact hazard the neighbouring `THIN_ENTRY_MARKER` comment argues against ("a gate that re-spelled this string would drift from the writer silently"), and the two consumers here are the projector's exemption set and the injector's reachability set. | open | |
| 12 | low | src/scripts/check_host_tree_parity.ts:174 | The gate uses non-asserting `reportScanned` and exits 0 when `compared === 0`. With all three thinnable hosts enrolled in `lean_projection.hosts` — the configuration in which cursor and cline are thinned with no bound slot to deliver a body back — the gate scans nothing and prints a green "0 non-delivery host tree(s) byte-identical". The one configuration it exists to police is the one in which it is a no-op. | open | |
| 13 | low | src/scripts/check_host_tree_parity.ts:142 | `configured` is resolved without consulting `lean_projection.mode`, unlike the sibling gate's `_lean_projection_settings_for` (check_rule_projection_integrity.ts), which checks the mode first precisely so "a `hosts:` list left behind after a rollback exempts nothing". Here a rolled-back `eager-all` with a leftover `hosts: [claude-code]` still excludes `.claude/rules` from the byte-identity comparison, so the two gates disagree about which trees are under assertion after a rollback. | open | |
| 14 | low | src/scripts/report_standing_payload_by_host.ts:458 | The success line hardcodes `HOST_SURFACES.length - 1` for both numerator and denominator of "writer citations resolved", deriving the `-1` from the single current `writer: '—'` row. Adding a second surface-less host silently reports a wrong count, and the ratio can never read anything but N/N even though `assertWritersResolve` already returns the exact problem list. Count the non-`—` writers instead of subtracting a constant. | open | |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
