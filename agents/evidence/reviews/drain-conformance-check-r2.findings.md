# Findings: drain-conformance-check-r2
<!-- completion-review: v1 | reviewed: 2026-09-13 | scope: e96df5a35425c4c4b8f0a2df1d5d5e2dae3aaee21d3124d16e09df051924054c | diff: d7a2e78667c95e7fb60128b91cfa56488f1bc849 | reviewer: r2-fresh-subagent-drain-conformance-check-r2 | prompt_hash: 6bfcd5194564f8a2639c2a211376a1f4213ff445eff7daa2488a63221ca81c02 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-conformance-check-r2"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-13 -->

<!-- context-manifest: v1
inputs:
  diff_sha: d7a2e78667c95e7fb60128b91cfa56488f1bc849
  scope_hash: e96df5a35425c4c4b8f0a2df1d5d5e2dae3aaee21d3124d16e09df051924054c
  roadmap: agents/roadmaps/road-to-a-conformance-check-that-can-fail.md
  roadmap_hash: e80321534148b46fa75e74acc81f62ccb814fea0b0e31305fe11676f46f1e3d6
  ac_hash: 6b6e2ebffa7415bc2211e4f0d5e8840e1ae1ed0912ee3da97a34189f7367dbd4
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-13T20:52:33Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
| 1 | high | src/scripts/_cli/cmd_conformance.ts:178 | The new `unknown` remedy tells the operator: "only the browser install route (`agent-config setup`) writes the log today". No install route writes it. `appendTxLog` has exactly one call site in the tree — `src/server/routes/install.ts:450`, inside `app.post('/api/v1/install/recovery/dismiss')`, which appends a single `rollback` marker with `path: ''` and `sha256: null`. The route list in that file (lines 341, 353, 380, 388, 398, 440) contains no apply route at all, and `docs/contracts/gui-wizard.md:171-173` — a file this diff edits — records why: `The TypeScript apply engine and its POST /api/v1/install/apply SSE route were removed`. So an operator who follows this remedy and runs `agent-config setup` still gets no transaction log. This is the same unbacked-claim class Phase 2 exists to remove, and the new guard `claims no recovery this tree cannot perform` scans only for `/revers/i` and `/undo\|roll ?back/i`, so nothing covers it. | fixed | d04ed00f2 - unknown remedy no longer names a route that writes no log; docstring premise corrected |
| 2 | high | docs/contracts/conformance.md:45 | The published contract states `The install transaction log is written by the browser install route only. A command-line install therefore produces none`, and § `How far this reaches` (lines 67-71) concludes `a machine that has run the browser installer once, anywhere, has a log with a clean tail and returns ok for every headless install in every other project on it`. Both are false for the reason in finding 1: no install path writes the log, browser included. The consequence runs the opposite way from the one documented — `unknown` is not the narrow answer for `the machine that has only ever installed headlessly`, it is the answer for **every** tree carrying `agents/installed-tools.lock`, so every installed consumer's `agent-config conformance` now prints a `❔ txlog-clean` row and a `4/5 ok` report line. The blast radius of the new status is understated in the one document that defines it. | fixed | d04ed00f2 - conformance.md reach rewritten - unknown is every installed tree, fail is unreachable today |
| 3 | medium | agents/roadmaps/road-to-a-conformance-check-that-can-fail.md:36 | The roadmap contradicts itself about one call site. Line 12 (`estate_growth_exempt`) and line 36 (Goal) describe `src/server/routes/install.ts:442` as `the only writer of that log ... the browser install route`, while line 188 describes the same reference as `the only rollback-shaped entry in the tree is a dismiss marker carrying an empty path and a null hash` — which is what it actually is. The first reading is the premise under the estate exemption, under the Goal paragraph, and under the Phase-3 blocker's recommendation at line 168 (`the log is the missing half of a mechanism that already ships`). Nothing ships that writes a `write` entry, so that recommendation overstates what exists by one whole writer. | fixed | d04ed00f2 - roadmap Goal and estate_growth_exempt corrected to match its own Blockers section |
| 4 | medium | src/install/txlog.ts:5 | `Each successful atomic write appends one entry` is false against the same evidence as finding 1 — no writer appends a per-write entry. This sentence is the first half of the hunk whose second half the branch corrected (`recovery on crash is tail -n plus the wizard's resume / dismiss decision`), so the touched lines fixed the reverse-apply claim and left the adjacent claim of the same class one line above it. `TxLogEntry.kind`'s own doc at line 37 (`write is the only success path`) repeats it. | fixed | d04ed00f2 - txlog.ts header states the one real call site and that write/skip/abort are unproduced |
| 5 | medium | src/scripts/_cli/cmd_conformance.ts:190 | The new absent-log discriminator is bypassed by an empty or unparseable log. `readRecentEntries` drops malformed lines silently (`src/install/txlog.ts:59-60`, `readRecentEntries` at 62-77), so on a tree that carries a manifest a zero-byte or fully-corrupt `install-log.jsonl` falls into the pre-existing `entries.length === 0` branch and returns `ok` — the same false green Phase 1 set out to close, reached by a different door. The `projectRoot` discriminator is consulted only on the `!existsSync(logPath)` path. No test covers manifest-present + empty-log, and the contract table at `docs/contracts/conformance.md:52-55` has no row for it. | fixed | d04ed00f2 - absent, empty and unreadable are one branch now; two tests added, one an over-fire guard |
| 6 | medium | src/install/preflight.ts:170 | The comment added at line 162 states `No remedy here may describe what an install does to the file`, and the untouched arm of the very ternary it introduces does exactly that: `resolve interactively, or pass --force to overwrite`. Both halves are unbacked by the same evidence the branch cites for the other arm — `src/scripts/install.ts:556-558` returns `write` unconditionally so there is no interactive resolution to reach, and line 22 of that file documents `--force` as `accepted (no-op): installs always overwrite`, so `--force` changes nothing. The new guard test asserts `not.toMatch(/leaves? it alone\|survives?\|untouched\|is safe\|preserv/i)`, which this string passes. | fixed | d04ed00f2 - both ternary arms corrected - the untouched one named an unreachable interactive path |
| 7 | medium | src/ui/wizard/state.ts:439 | In a file this diff edits, twelve lines below the new `ownership` docstring, `ConflictResolutionWire` still documents `skip` as `leaves the file untouched` and `overwrite` as `writes the planned bytes verbatim` — the identical claim class corrected in `preflight.ts`, `conflict.ts`, `types.ts`, `conformance.md` and `gui-wizard.md` on this branch, whose guard test was written `so the class cannot come back through a neighbouring string`. Nothing consumes these values: `ConflictResolution` (`src/install/types.ts:175`) and the `conflictResolutions` signal (line 463) have no reader or writer anywhere in `src/`, and line 447 cites `ApplyRequestSchema`, which no longer exists in the tree. | fixed | d04ed00f2 - ConflictResolutionWire docstring corrected in place |
| 8 | medium | src/install/conflict.ts:91 | `resolveFileConflict` has no caller outside `tests/install/conflict.test.ts` — the only references in `src/` are its own definition and the `{@link}` at line 35. The diff adds an `ownership` input (line 54), four tests for it, and a docstring reading `It now reads the recorded digest first when the caller supplies one` (line 72); no caller supplies anything. The same docstring's pre-existing sentence at lines 87-89 (`Headless callers (B1 CLI) collapse surface to skip ... the apply layer records the entry under ApplyResult.conflicts so the wizard can pick it up next`) describes a pipeline that is likewise absent. The four new tests read as coverage of the Phase-5 ownership behaviour while exercising a function no production path reaches; the behaviour change that does ship is entirely in `computeConflicts`. | fixed | d04ed00f2 - the ownership input and its four tests are removed - no caller supplies it |
| 9 | low | src/install/conflict.ts:216 | `recorded: RecordedHashes = recordedHashesForRoot(plan.root)` is a default parameter, so it is evaluated at call entry — before `if (plan.policy.force) return [];` on the next line. Every force-mode plan now reads and YAML-parses the installed-tools manifest to build a map it immediately discards, where the previous signature returned on the first statement. | fixed | d04ed00f2 - recorded resolved after the force early-return instead of as a default parameter |
| 10 | low | src/install/conflict.ts:228 | The comment claims the reordering costs `one extra digest per recorded path`. The extra digests are one per path in `policy.knownPaths` that exists on disk — those are the entries the removed `knownPaths.has()` guard used to short-circuit *before* `sha256File`. Paths that are recorded in the manifest but absent from `knownPaths` were already being hashed. The stated cost names the wrong set, and understates it for the `/api/v1/install/plan` caller, which receives `knownPaths` from the client (`src/server/routes/install.ts:257`). | fixed | d04ed00f2 - cost comment now names existing paths in knownPaths, the set the removed guard skipped |
| 11 | low | tests/scripts/_cli/cmd_conformance.test.ts:78 | The new tests silently depend on `AGENT_CONFIG_INSTALLED_TOOLS` being unset in the ambient environment: `_installLogExpected` resolves through `installed_tools.manifest_path(projectRoot)`, which returns the override for any root when the variable is set (`src/scripts/_lib/installed_tools.ts:94-100`). With it set, `does not over-fire: a never-installed tree stays green` and `_installLogExpected is false for a tree with no install manifest` both invert, and every single-argument `computeConflicts` call in `tests/install/conflict.test.ts` reads a foreign manifest. `tests/_lib/hermetic-env.ts` neutralises locale variables only, and its own header documents this exact failure class (`a test that reads process.env is invisibly broken on the machine whose environment happens to suit it`). `tests/install/recordedOwnership.test.ts:139-149` sets and deletes the same variable. | fixed | d04ed00f2 - both suites save, clear and restore AGENT_CONFIG_INSTALLED_TOOLS; verified with it set |
| 12 | low | docs/contracts/gui-wizard.md:151 | The new table's third column, `Screen should say`, and the paragraph at lines 156-165 (`a screen rendering this field must tell the user their edit will be replaced, and must not offer "leave it alone"`) prescribe behaviour for a conflict screen that does not exist: `src/ui/wizard/state.ts` is the only file under `src/ui/` that mentions `conflicts`, and it carries types and signals, not a component. No code reads `ownership`. The contract publishes a requirement with no implementation and no test to hold it. | fixed | d04ed00f2 - gui-wizard.md states that no component reads the field before it states the requirement |
