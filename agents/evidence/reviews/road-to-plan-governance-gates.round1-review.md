# Findings: road-to-plan-governance-gates
<!-- completion-review: v1 | reviewed: 2026-08-04 | diff: 01323d17b03e01610c526cf34cd5134002899518 | reviewer: r2-fresh-subagent-road-to-plan-governance-gates -->

<!-- context-manifest: v1
inputs:
  diff_sha: 01323d17b03e01610c526cf34cd5134002899518
  diff_hash: 76a9f36498a5a29d3c2537b69fe8e2070f5764544dbfd3ec8e97b205fedccc2a
  roadmap: agents/roadmaps/archive/road-to-plan-governance-gates.md
  roadmap_hash: f949bd1c19a3c42c1cc01ee2457f3e400bead6673c857dc0b6467e8e4decd5a3
  ac_hash: 093a28fc458fa10f926a8bb003d14440290f2215ffc812d5eaf502a47449f829
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-04T07:43:28Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/check_completion_review.ts:385 | Self-referential diff-sha binding makes enforced mode unsatisfiable in CI: a committed artifact can never carry the sha of the HEAD that contains it — committing the finalized artifact (which §2.5 requires and CI can only see committed) moves HEAD past the recorded `diff:` sha, so every PR fails `stale-review` once `--advisory` is removed. The tests institutionalize the escape (test header: final `diff:`-matching content is "a working-tree update on top"), which cannot be pushed. Compounded: `actions/checkout` on `pull_request` checks out the synthetic merge commit, so `git rev-parse HEAD` in CI never equals any branch-head sha the dispatcher could have recorded. | open | |
| 2 | high | .github/workflows/consistency.yml:236 | The Step-3b manifest re-derivation is structurally dead in CI: the loop only invokes `--verify` for artifacts whose header greps `diff: $(git rev-parse HEAD)`, which per finding 1 (artifact-commit advance + merge-commit HEAD) no committed artifact can ever match — so "CI re-derives the hashes and blocks on mismatch" (verdict #18, Phase 6 Step 3b, AC "manifest-checked") never executes. Even at the "right" HEAD, `runVerify` re-derives `git diff base...HEAD` which now includes the artifact commit itself → guaranteed `diff_hash` mismatch. | open | |
| 3 | high | dist/install/install.mjs:43 | The tracked install bundle was rebuilt from a worktree: 189 esbuild module-key/comment lines rewrote `node_modules/...` → `../../../node_modules/...`, leaking the local checkout layout into the shipped artifact and breaking reproducibility — a rebuild in a root checkout (the CI "Install Aux Tests" bundle diff the roadmap's own Context section cites) produces different bytes. | open | |
| 4 | high | src/scripts/dispatch_r2_reviewer.ts:357 | A findings artifact with NO context manifest is classified internal error (exit 2 → warn-and-allow per §6) instead of policy violation (exit 1 → block), and `check_completion_review` explicitly tolerates absent manifests — so simply omitting the manifest bypasses the entire verification layer, defeating "manifest = verification, not self-attestation" (verdict #18). | open | |
| 5 | high | src/scripts/check_completion_review.ts:382 | Directory-wide poisoning by leftover artifacts: (a) any committed skip declaration with `diff none` stays relevant for every future HEAD forever — it silently satisfies the gate for all later docs-only diffs (defeating "a push after review forces re-review") AND raises a false `skip-on-code-diff` block on every future code-touching branch once merged to the tracked `agents/evidence/reviews/`; (b) `bad-marker` / malformed-line violations (line 368) are emitted for ALL artifacts in the directory regardless of diff relevance, so one malformed legacy artifact blocks every subsequent PR in enforced mode. | open | |
| 6 | medium | agents/roadmaps/archive/road-to-plan-governance-gates.md:363 | Phase 5 Step 3 is checked `[x]` claiming "register the R1 validator in the pre-push hook path (`hook_manifest.yaml`)", but the diff touches no hook file — enforcement was added to `taskfiles/ci-fast.yml` `preflight` instead. If `preflight` is not the wired pre-push chain, the AC "fails pre-push" is unmet; either way the step text and the implementation diverge. | open | |
| 7 | medium | agents/roadmaps/archive/road-to-plan-governance-gates.md:420 | Phase 6 Step 6(b) and the R2 AC claim "a blocklisted tool call (`git log --all`, repo-wide grep) is rejected in the review context" and "leak-tested" — the diff contains no rejection mechanism and no E2E for it; the tool allowlist exists only as prompt prose (dispatch test asserts the prompt *contains* the text). The acceptance criterion is marked `[x]` without diff-visible evidence. | open | |
| 8 | medium | src/config/gate-coverage.yml:319 | Floors without teeth: `check_completion_review` emits `scanned = artifacts + 1`, which is ≥ 1 by construction, so `min_scanned: 1` can literally never fail (the corpus note's "0 would mean the gate no longer evaluates" is unreachable code); and `lint_plan_risk_register` maps a dead scope (assertScanned failure, lint_plan_risk_register.ts:707) to exit 2, which every caller (CI step, ci-fast task) warns-and-allows — a moved/renamed roadmaps root degrades both gates to advisory instead of blocking, contradicting the AC "registered … with real floors". | open | |
| 9 | low | src/scripts/lint_plan_risk_register.ts:521 | Grandfather baseline uses day-granularity committer dates: a substantial change committed ON the activation date (2026-08-04 — the day this very branch lands) becomes its own baseline and the file is grandfathered despite a post-gate substantial change (the "first commit ON the activation day → grandfathered" test pins this). Same-day granularity also lets a substantial change and a stale register committed on one day pass the staleness check. | open | |
| 10 | low | src/scripts/check_completion_review.ts:89 | `CODE_EXTENSIONS` omits common consumer-stack code extensions (rb, java, c/cpp, kt, swift, vue, svelte, sql, blade templates other than .php) — a completion touching only those files classifies as "no code surface", so a skip declaration is accepted on genuinely code-bearing work in consumer repos the suite installs into. | open | |
| 11 | low | src/scripts/check_completion_review.ts:385 | An artifact containing both a findings table for the current HEAD and a skip line is excluded from `relevantFindings` (`art.skip === null` filter) — appending a `**Skipped:** … diff none …` line to a findings artifact hides its `open` rows from validation on any non-code diff. | open | |
