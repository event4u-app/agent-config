# Findings: feat-per-turn-hook-economy
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: 5461cd483a0d274787f9804e7c18ae801b78d604a5dc79b87f9a13339bcf07fa | diff: 98756efff570a539ee4c2bf1f4f6106034bc4d7d | reviewer: r2-fresh-subagent-feat-per-turn-hook-economy | prompt_hash: 7c63256aa9348b25571bf6336272bd16aa785d490c0efd1ec9aa97c0a8264afb -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 98756efff570a539ee4c2bf1f4f6106034bc4d7d
  scope_hash: 5461cd483a0d274787f9804e7c18ae801b78d604a5dc79b87f9a13339bcf07fa
  roadmap: agents/roadmaps/road-to-per-turn-hook-economy.md
  roadmap_hash: f8a30788a713798e1833f2d69a34bcfab9246cfa3186e606ef8a0d093611d8fb
  ac_hash: e2024c1786cce1263c46df6184edd473cda7307782b8515b1f59e7953a09fd17
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T18:40:00Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/roadmap_progress_hook.ts:121 | `mark_dirty` does an unlocked read-modify-write with plain `fs.writeFileSync` under `agents/runtime/state/`, while `hook-architecture-v1.md:443` requires the `.dispatcher.lock` → tmp → rename sequence that `hooks/state_io.ts` already implements as `atomic_write_json`. Two parallel `post_tool_use` dispatches in one turn both read `[]` and the second write drops the first root; `mark_dirty` returns `true`, so the design's own net (an unwritable ledger regenerates inline) never fires. The outcome is exactly the silently-stale dashboard the debounce claims to prevent | fixed | Ledger reshaped to one entry file per root, so the read-modify-write is gone by construction; writes go through `atomic_write_json` and a new case asserts two roots survive independent marks (ef0f2dc78) |
| 2 | high | src/scripts/hooks/dispatch_hook.ts:1320 | `_readStdin` and `hook_stdin.readHookStdin` still convert **any** read failure to `""`. The diff removed the known trigger but left the fail-open conversion: an exhausted EAGAIN budget, `EIO` or `EBADF` still yields an empty envelope, the chain runs with no `tool_name`, and the dispatcher exits 0 — including for `fail_closed: true` blocking guards, with nothing on stderr. `_lib/stdin.ts:20-26` names this exact shape as worse than the crash | deferred | The SILENT half is fixed in ef0f2dc78 (stderr plus a dispatch issue); the allow-versus-deny decision is deferred to blocker `b-stdin-read-failure-policy` because turning a failed read into a DENY is a policy change on a security surface. The failure is now LOUD (stderr plus a dispatch issue) instead of silent. Turning a failed read into a DENY is a policy change on a security surface, so it is filed as `b-stdin-read-failure-policy` rather than improvised (ef0f2dc78) |
| 3 | medium | src/scripts/roadmap_progress_hook.ts:85 | `FLUSH_EVENTS` misses native aliases its own comment exists to cover: cline maps `TaskComplete`→session_end and `TaskCancel`→stop, gemini maps `AfterAgent`→stop. The write path keys on `tool_name`, not the event, so those hosts accumulate dirty roots nothing flushes | fixed | `TaskComplete`, `TaskCancel` and `AfterAgent` added to `FLUSH_EVENTS` (ef0f2dc78) |
| 4 | medium | agents/roadmaps/road-to-cost-parity-1-rule-payload-diet.md:294 | The census advertised as read straight off the manifest was taken BEFORE step 3.1's manifest edit in the same PR. Post-change: claude `stop` 11 (table says 10), claude `session_end` 4 (says 3), the five other hosts `stop` 6 (says 5) and `session_end` 4 (says 3). Step 4.3's `verify:` is therefore false | fixed | Re-measured after the manifest edit; table corrected (`stop` 6/11, `session_end` 4) with the stale-baseline miss named in both roadmaps (ef0f2dc78) |
| 5 | medium | src/scripts/hooks/end_review_nudge_hook.ts:406 | `fs.readFileSync` of every untracked non-doc file with no SIZE bound plus a per-byte JS loop, on the Stop slot. `UNTRACKED_FILE_CAP` bounds file count, not bytes, and the replaced `git diff --no-index` never materialised content in the hook process. One large untracked file now costs a full allocation plus O(n) iteration per turn end | fixed | `UNTRACKED_FILE_READ_CAP_BYTES` (8 MiB) with a size check before the read; an over-cap file counts as over-threshold, never 0 (ef0f2dc78) |
| 6 | medium | src/scripts/bench_hook_latency.ts:206 | `benchEvent` forces `AGENT_CONFIG_REPLAY=1` and `roadmap_progress_hook.run()` short-circuits on replay for BOTH paths, so the per-turn composite step 4.1 registers is structurally blind to the step-3.1 change it is meant to track — it can show neither the removed `post_tool_use` spawn nor the new `stop` flush cost | fixed | Recorded as a stated limitation in step 4.1 rather than left implied: the composite bounds dispatch count, and the debounce-exercising fixture is named as not built (ef0f2dc78) |
| 7 | medium | tests/scripts/hooks/roadmap_progress_hook.test.ts:363 | The new debounce `describe` carries no `.skipIf(!tsx)` although five cases assert the tsx-run regenerator's marker; the sibling `run()` block carries that guard for exactly this reason | fixed | `describe.skipIf(!tsx)` on the debounce block, matching its sibling (ef0f2dc78) |
| 8 | medium | src/scripts/bench_hook_latency.ts:73 | `BUNDLE_OVERRIDE` and `PAYLOAD_BYTES` are module globals never reset at the top of `main()`, while their measurement-only guard is per-invocation. Two `main()` calls in one process can write a foreign or padded reading into the budget row — the precise outcome the guard's message says must never happen | fixed | Both globals reset at the top of `main()` (ef0f2dc78) |
| 9 | medium | tests/scripts/hooks/dispatch_large_payload_guard.test.ts:63 | `spawnSync` timeout is 120 000 ms against a 10 000 ms vitest `testTimeout` and a ~10 s EAGAIN budget, so a regression of the class this test exists to catch surfaces as a vitest timeout rather than the bypass assertion | fixed | Spawn timeout lowered to 8 s, under the 10 s vitest `testTimeout` (ef0f2dc78) |
| 10 | low | src/scripts/hooks/end_review_nudge_hook.ts:232 | Two docstrings still describe the removed subprocess (`UNTRACKED_FILE_CAP` at :232 and `totalNonDocMutatedLinesWithMeasure` at :444) | fixed | Three docstrings corrected, and the count-versus-bytes split is now stated where the cap is defined (ef0f2dc78) |
| 11 | low | src/config/hook-latency-budget.json:18 | `aggregation` and `definition` are never read — the bench hardcodes p50 and re-implements the formula, so editing either changes nothing while the printed label keeps asserting p50 | fixed | `_aggregation_note` states plainly that `aggregation` and `definition` document the code rather than feed it (ef0f2dc78) |
| 12 | low | src/config/hook-latency-budget.json:22 | `_observe_only_note` says the composite sums six measurements; the definition sums four slots | fixed | Corrected to four slot measurements, two of them multiplied by `tool_calls` (ef0f2dc78) |
| 13 | low | docs/contracts/hook-architecture-v1.md:404 | "eight of the twelve claude concerns" contradicts its own enumeration of nine names — only three declare `tools:`, so 12 − 3 = 9. The cancellation conclusion holds; the number is wrong in a contract file presenting it as verified | fixed | Corrected to nine in the contract and in the roadmap; the enumeration was already right (ef0f2dc78) |
| 14 | low | src/scripts/bench_hook_latency.ts:137 | `TOOL_EVENTS` pads `tool_response` onto `pre_tool_use`, where that field never occurs, while the same comment rejects padding `stop` because the shape does not occur. The realistic large `pre_tool_use` field is `tool_input` | fixed | `post_tool_use` pads `tool_response`, `pre_tool_use` pads `tool_input.description` (ef0f2dc78) |
| 15 | low | tests/scripts/hooks/dispatch_large_payload_guard.test.ts:47 | The test points `--project-dir` at `REPO_ROOT` with replay cleared, so three real 12-concern chains execute against the working repo during the suite; a temp project dir exercises the same property | fixed | A throwaway temp project dir per case instead of the working repo (ef0f2dc78) |
