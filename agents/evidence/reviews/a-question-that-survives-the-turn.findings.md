# Findings: a-question-that-survives-the-turn
<!-- completion-review: v1 | reviewed: 2026-09-11 | scope: 6ff9891ca4d6cc5016736e87f601aa01dbb6b7a79423c893e6fdbd00929d69ae | diff: 7fc661f148898f63944b8d4011e238a7df245d8e | reviewer: r2-fresh-subagent-a-question-that-survives-the-turn | prompt_hash: 6f21b1e72bed974063153024fd65ac9600b40e1f0da3f92d3482ae30503c7b0d -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-a-question-that-survives-the-turn"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-11 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 7fc661f148898f63944b8d4011e238a7df245d8e
  scope_hash: 6ff9891ca4d6cc5016736e87f601aa01dbb6b7a79423c893e6fdbd00929d69ae
  roadmap: agents/roadmaps/road-to-a-question-that-survives-the-turn.md
  roadmap_hash: 5bf806949684426516d94b3defadc864a4de09e5bf9619d91ba2f81d9c3906f5
  ac_hash: 042a1b4078f8e9528ea0c0d3d625ff47f267374efeaafc7a06a1135ca02476b3
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-11T15:05:51Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/review_baseline.ts:224 | `applyBaseline` guards only the ASYMMETRIC measure case (`baseline.measure !== measuredMeasure`), so when BOTH sides are `capped_approximation` it subtracts. The capped value is `MUTATION_LINE_THRESHOLD + 1 + trackedLines` (end_review_nudge_hook.ts:507) — a synthetic over-the-bar constant plus the tracked count, with the untracked lines that caused the cap absent from it entirely. Subtracting `51 + T0` from `51 + T1` cancels the constant and yields `T1 - T0`, so a session that starts past `UNTRACKED_FILE_CAP` (>20 untracked non-doc files — the dirty-accumulator state this mechanism exists for) and then creates an unbounded number of NEW untracked files measures 0 after subtraction and the nudge goes SILENT, where before this change it fired on 51. That is the one direction the module's own header forbids ("a safety advisory must not be disarmed by its own instrumentation failing"), and the same header already argues the fix — "subtracting it from — or out of — an exact count is meaningless in both directions" — while the code applies that reasoning in one direction only. `capped_approximation` on EITHER side should fall back to `mixed-measure`, or the subtraction should be restricted to the tracked component. No test covers it: `review_baseline_hook.test.ts` exercises capped-vs-exact and exact-vs-capped, never capped-vs-capped. | open | |
| 2 | high | src/scripts/check_reply_consistency.ts:479 | `turn_end_gate_hook.ts:179` now imports this CLI lint into a hook that is bundled into `dist/hooks/dispatch.js`, but this module's `_isCliEntry()` lacks the `__AGENT_CONFIG_BUNDLE__` early-out that `turn_end_gate_hook.ts:1311` and `review_baseline_hook.ts:115` both carry. Inside the bundle every module shares one `import.meta.url`, so the entry guard alone does not settle it and module load ends in `process.exit(main())` — with no recognised argv, `main()` reaches `_usage_error` and exits 1, killing the WHOLE dispatch process at import time and silently taking every other concern on that slot with it. The import comment (turn_end_gate_hook.ts:172-178) identifies this precisely and then leaves the only thing preventing it as the `.__direct__` argv rewrite in the `build:hooks` banner — protected by a comment and pinned by no test. A two-line `__AGENT_CONFIG_BUNDLE__` guard here matches every other bundled entry module and removes the dependency on that banner. Secondary cost: the import also pulls `_lib/agent_src.js` and `_lib/scan_scope.js` into the per-dispatch bundle load. | open | |
| 3 | medium | src/scripts/hooks/turn_end_gate_hook.ts:804 | Detector E reads a BLOCKQUOTED options block as a live ask. `OPTION_LINE_RE` is `/^\s*>?\s*(\d+)\.\s+\S/` (check_reply_consistency.ts:56) and accepts a leading `>`, and `REC_LINE_RE` matches anywhere in a line, so a reply that QUOTES an options block plus its recommendation line as an illustration registers as a decision put to the user. A `>`-only separator line is non-blank, consumes one of the two adjacency slots and breaks nothing, so the ordinary quoted shape resolves. That is a false positive in a detector that REFUSES a turn, in exactly the domain this repository writes about — its rules are about options blocks — and it is the one masking case the tests omit: the fenced-example near-miss is asserted (turn_end_gate_pending_decision.test.ts, "does not read a fenced example block as a live ask") while the blockquoted one is not. The 592-turn replay cited in the docstring measured the recommendation-line filter, not a quotation filter, so the residual fires are unclassified on this axis. | open | |
| 4 | medium | src/scripts/hooks/end_review_nudge_hook.ts:825 | The new `baseline` telemetry field can only ever be written on the FIRE path: `appendReviewSkippedTelemetry` runs after the threshold test, the once-per-session latch and the reviewer scan, so a session the subtraction SILENCED emits no `review_skipped` row at all. Three places claim the opposite and are load-bearing for accepting the design — risk-register row 3 ("the telemetry row records which path was taken, so the rate is measurable rather than assumed"), row 5 (the same sentence for `head_moved`), and review_skipped_record.ts ("the rate at which the mechanism is degrading would be unmeasurable from its own stream"). The observable population is exactly the sessions where the mechanism did NOT silence the nudge, i.e. the complement of the risk being mitigated, so risk 3's mitigation is inoperative as written — including for the scalar-baseline cancellation it names (a session that reverts 100 pre-existing dirty lines and writes 100 new ones nets to zero, goes quiet, records nothing). Either record below the threshold too, or state that the fallback rate is observable only among firing sessions. | open | |
| 5 | medium | agents/roadmaps/road-to-a-question-that-survives-the-turn.md:300 | AC-5 ("`review-baseline` resolves … in every host's `session_start` row") and step 2.3 (:192, "the seven per-host `session_start` rows") are both ticked `[x]`, but the shipped manifest binds `review-baseline` on `claude` alone (hook_manifest.yaml:1322 — no other platform's `session_start` row carries it). The narrowing is deliberate and well argued in the manifest comment (:1067: the only reader, `end-review-nudge`, is bound on claude's stop slot and nowhere else), so the code is right and the record is wrong — a ticked acceptance criterion asserts a state the tree contradicts, and a later reader auditing AC-5 against the manifest finds six hosts missing with nothing saying that was the decision. Correct the AC and step 2.3 to name the single binding and its reason. | open | |
| 6 | low | src/scripts/check_reply_consistency.ts:174 | `recommendationsUnder` re-runs `mask_fences(_splitlines(text))` over the WHOLE text on every call, where the code it was extracted from computed `lines` once per reply. `check_reply` now pays that per block, and `detectDroppedDecision`'s `asksIn` (turn_end_gate_hook.ts:804) pays `find_option_blocks` (one further split+mask) plus one `recommendationsUnder` per block for every assistant text of the turn — on the stop path, which runs at every turn end. Passing the already-masked lines, or memoising per text, keeps it linear. | open | |
| 7 | low | src/scripts/hooks/review_baseline_hook.ts:103 | The mechanism's own state file inflates the count it exists to correct. `agents/runtime/state/review-baseline/<key>.json` is an untracked non-doc file, so on any consumer that does not gitignore `agents/runtime/` it is counted by the nudge's untracked sweep as session mutation at every later stop — and it is written AFTER the baseline is measured, so the baseline cannot subtract it. The test names the problem exactly ("a real observation about an unignored consumer", review_baseline_hook.test.ts, `repoWithDirt`) and then works around it by writing a `.gitignore` into the fixture; no shipped code excludes the state path from the count. Small in magnitude, but it is the measurement contaminating itself, and it also adds one file toward `UNTRACKED_FILE_CAP`, which is finding 1's trigger. | open | |
| 8 | low | src/scripts/hooks/turn_end_gate_hook.ts:1078 | `if (text === null) continue;` runs BEFORE the user-role reset, and `_messageText` (:1132) returns null unless a `type: 'text'` block is present. A genuine user turn carrying no text block — an image-only paste, an attachment-only message — therefore does not reset `assistantTurnTexts`, so an ask from before that message stays inside the current turn and detector E can refuse a turn the user actually answered. Harmless for the four existing detectors (a stale `toolCalls` list, a lagging turn ordinal); newly load-bearing for E, which is defined entirely by the absence of a reset. | open | |
| 9 | low | src/config/continuity-surface.json:281 | The new row's `locus` is `src/scripts/_lib/review_baseline.ts:35`, which is the blank line between the imports and the first doc comment. The thing the row describes — `BASELINE_STATE_REL`, the state path — is at line 61. The gate only requires the locus to resolve, so this passes while pointing a reader at nothing. | open | |
| 10 | low | agents/evidence/analysis/standing-payload-by-host-2026-09.md:3 | The diff re-pins this analysis and regenerates the `rule-router.md` host-cost table (485826 → 486411 bytes, +146 chars/4 tok), but this branch touches no file under `src/rules/`, so the delta it publishes measures work that arrived from elsewhere. No roadmap step, acceptance criterion or in-diff note covers the regeneration or names the gate that required it, so a reader cannot tell a required freshness re-pin from a drive-by. One line naming the gate that demanded it would make it traceable. | open | |
