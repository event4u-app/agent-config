# Findings: feat-design-system-onramp-blockers
<!-- completion-review: v1 | reviewed: 2026-08-13 | scope: 20ab2467c1fc50349d9abc032922ef0ea1a5e3ab4b60b06dc7122832ad7d0d25 | diff: 2a969a0c4835e9a4db031d01e70ebcb5e0bb051d | reviewer: r2-fresh-subagent-feat-design-system-onramp-blockers | prompt_hash: 1b3e4599eb6bb486136c262a7acd7a381db15b8ab39fc58996e231611de914c1 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 2a969a0c4835e9a4db031d01e70ebcb5e0bb051d
  scope_hash: 20ab2467c1fc50349d9abc032922ef0ea1a5e3ab4b60b06dc7122832ad7d0d25
  roadmap: agents/roadmaps/archive/road-to-design-system-onramp.md
  roadmap_hash: 3953219ecbcb68665d3fffc0f16c6f9299925a28fbc1e883176d0aff8ef9f9cc
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-13T13:47:59Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/skills/corpus-grounding/scripts/decision_engine.ts:843 | `_render_markdown` renders only `selections`, `rules_evaluation.matched` and `evidence_gap`. It was not extended for the new `dials` / `motion` / `spacing_scale` keys, so **the entire dial payload is visible only under `--json`**. Both other output paths drop it: the default text render (`ground.ts:346`) and the persisted `MASTER.md` (`persist_grounding` → `_render_markdown(grounded, true)`, decision_engine.ts:818). `--density`'s ONLY payload is `spacing_scale`, so that dial is completely inert on both. Verified by running: `ground … "saas dashboard scroll animation" --density 9 --motion 8 --variance 9` prints no dial label, no spacing scale and no motion row; the same run `--json` carries all three. `--persist` writes a MASTER.md whose headings are product/style/color/landing/typography/Evidence gap only. This is the "defined but not wired" class the roadmap's own Step 2 states it deferred Phase 3 to avoid. | open | |
| 2 | high | src/domains/engineering-base/design-system/generate/command.md:22-38 | Roadmap Phase 3 Step 2 (marked `[x]`) promises the dials "surfaced as optional args on `/design-system generate`". They are not: the command file is untouched by this diff and still documents only `--persist` / `--project-name` / `--page`, and its documented invocation carries no `--json`, i.e. it is exactly the path finding 1 shows swallows the dial output. The flags are documented in **no** surface — `grep` for `--variance\|--motion\|--density` across `corpus-grounding/SKILL.md` (whose line 74 lists the `ground` optional-flag surface), `design-intelligence/SKILL.md`, `src/domains/engineering-base/design-system/` and `docs/contracts/command-clusters.md:70` returns zero hits. New public CLI surface shipped with no describing doc updated (`downstream-changes` Doc-Impact). | open | |
| 3 | medium | src/skills/corpus-grounding/scripts/decision_engine.ts:667-675 | The stated invariant is false. The comment ("Variance BIASES the existing priority list rather than replacing it … a preference that silently overrode grounded evidence would invert this engine's whole point") is repeated verbatim in `provenance/borrows.jsonl` `transformation_note` and in the archived roadmap Step 2. Measured: `ground … "luxury e-commerce brand site"` selects style `Liquid Glass`, matching the rule's own `Style_Priority` = `Liquid Glass + Glassmorphism`; the same query with `--variance 1` selects `Exaggerated Minimalism` and demotes `Liquid Glass` to `alternatives[1]`. Two mechanisms: `priority.slice(0, 2)` at line 686 is now always the dial's first two keywords, so the rule's keywords are dropped from the query augmentation entirely; and 5 prepended dial keywords outscore 2 rule keywords in `_select_best_match`. The behaviour may well be what a caller wants — the defect is that a compliance record (`borrows.jsonl`, which `lint_provenance` checks for shape, not truth) misdescribes the transformation. | open | |
| 4 | medium | src/skills/corpus-grounding/scripts/decision_engine.ts:726-731 | Retrieve-then-filter loses tier rows. `search_domain(manifest, …, 'gsap', 5)` retrieves the global top-5 over all 16 rows and only then filters on `Intensity Tier`, so a row at the requested tier that does not survive the global cut is silently absent. Verified on the same query: `search --domain gsap --filter "Intensity Tier=Subtle" -n 5 "scroll reveal fade"` returns 4 Subtle rows; `ground "scroll reveal fade" --motion 1` returns 3 (loses `Stagger List / Subtle`). `search_domain` already accepts a `filters` argument — the `--filter` CLI flag uses it (ground.ts:311) — so filter-first was available and was not used. Separately the `5` is hardcoded and contradicts the manifest's own `domains.gsap.max_results: 3` (manifest.json:281), unlike every other domain whose count comes from `reasoning.plan`. | open | |
| 5 | medium | agents/settings/contexts/upstream-watchlist.md:40 | The `vendored-corpus` watch row is now false in three ways and was not updated by this diff: it states upstream "carries `motion.csv`, `google-fonts.csv` and three design dials **the port lacks**" (motion.csv and all three dials ship in this branch), and it names "`road-to-design-system-onramp` Phase 3 closes it" as the open owner (that roadmap is closed and archived by this same branch). The residual drift the branch deliberately declined — `google-fonts.csv`, six desktop stacks, eleven content-drifted files — is left with no named owner. The row's own following sentence is "This row is why the watchlist exists in this shape: the drift ran for two months with every gate green" — leaving it stale reproduces exactly the failure it documents. | open | |
| 6 | low | tests/scripts/skills_corpus_grounding_dials.test.ts:9-12 | Docstring claims coverage the file does not have: "The no-flag path is pinned too, **and it is the load-bearing one** … a run without flags must carry none of the three output keys." No test in the file calls `ground()`; all 24 exercise `resolve_dial` / `DIAL_TIERS` in isolation, and `resolve_dial(dial, null) === null` is not the same assertion. The roadmap Step 2 repeats the claim ("The no-flag contract is the load-bearing part **and is pinned**"). The property the branch calls its most important contract is asserted by nothing. | open | |
| 7 | low | src/skills/corpus-grounding/scripts/decision_engine.ts:729-739 | Comment and code disagree: "a tier with no rows yields an evidence gap **rather than** a silent empty block". The code emits both — the gap AND `motion: { tier, best: {}, alternatives: [], confidence }`. Verified: a Complex-vocabulary query with `--motion 1` returns the gap plus an empty motion block. | open | |
| 8 | low | src/skills/corpus-grounding/scripts/decision_engine.ts:535-539 | Insertion point orphaned an existing docblock. The `ground()` JSDoc ("Conditional grounding: category → rules → planned multi-domain search … Async because `_load_rules_callable` is async") now sits above the new `DIAL_TIERS` docblock at line 540, so `DIAL_TIERS` binds the second comment and `ground()` (line 596) is left with no docstring at all. | open | |
| 9 | low | src/scripts/external_sources_denylist.json:1-115 | Whole-file reindent from 2-space to 4-space, churning ~110 lines, for a 3-line substantive change (two `skip_paths` entries + one `skip_reason` key). Confirmed against `HEAD~1`. `minimal-safe-diff` — the reformat is not traceable to the task and buries the carve-out a reviewer needs to audit. | open | |
| 10 | low | agents/roadmaps/archive/road-to-design-system-onramp.md:19,273,310,492 | The archive move did not re-depth the file's own outbound links. Four are broken from `agents/roadmaps/archive/`: line 19 `archive/road-to-august-program.md` (resolves to `archive/archive/…`; should be `road-to-august-program.md`) and lines 273/310/492 `../evidence/analysis/design-system-blockers-council.md` (resolves to `agents/roadmaps/evidence/…`; needs `../../`). The inbound links WERE re-pointed correctly, so this is one-directional. Nothing catches it: `check_references.ts:74` excludes `agents/roadmaps/archive`, and its own comment names this exact cause ("moves a file one directory deeper and never re-depths the links"). The three broken links are to the council record that justifies both blocker resolutions. | open | |
| 11 | low | agents/roadmaps/archive/road-to-design-system-onramp.md:567 | Phase 5 Step 1 is marked `[x]` (done) while its own note says "Closed 2026-08-13 by amending the falsifier, **not by building a counter** … The step's premise is false and the instrument it names does not exist." The deliverable ("Count `/design-system` invocations … publish") was refuted, not delivered. The same file uses `[-]` correctly for the equivalent outcome at Phase 4 Step 2 ("merged at adoption"). The prose is honest; the glyph is not, and it feeds the auto-generated `290 / 417 steps done · 70%`. | open | |

## Verified clean (probed, no finding)

- `dist/agent-src/` twins are byte-identical to `src/` for all four touched files (`diff` exit 0 ×4).
- `npx vitest run tests/scripts/skills_corpus_grounding` — 48/48 green, including the 24 new dial tests; `tsc --noEmit` clean.
- `check_no_external_sources` green with the two new `skip_paths`; `lint_provenance` green (1 record, notices in sync). The carve-out reason for `provenance/borrows.jsonl` + `docs/THIRD-PARTY-NOTICES.md` is correctly scoped to those two paths and not to `provenance/*`.
- Pack budget: `design-intelligence` measures 22.36 % of the `dist/agent-src/skills/` payload against the new 23.5 % cap. (`check_pack_size` itself is red on `packed_size_mb` 9.137 vs 7.8 — the pre-existing built-worktree trap the budget file's own note documents, not introduced here.)
- `agents/roadmaps-progress.md` is internally consistent: 31 table rows = 31 open roadmaps; open 127 + done 290 = 417 = the stated denominator.
- `motion.csv` parses correctly through the CSV reader including quoted fields with embedded commas and the `Don't` column; 10 500 bytes and 16 rows match the ATTRIBUTION and budget-note claims.
- The Phase-2 falsifier amendment, the single-member council disclosure in `agents/evidence/analysis/design-system-blockers-council.md`, and the ATTRIBUTION "partial adoptions" section are accurate against the tree as written.
