# Findings: council-remaining-adapters
<!-- completion-review: v1 | reviewed: 2026-08-12 | scope: 506ccec91d50cc0971f8cc7c5a0ba710425e6bbc664ce7ed22b4db2e47060936 | diff: 368c895d78d50b79520b686ade54e7d9aeddfe76 | reviewer: r2-fresh-subagent-council-remaining-adapters | prompt_hash: 052f4105319ab6b66cc81e242d329201222166b4a627398535a25688ecd818b7 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 368c895d78d50b79520b686ade54e7d9aeddfe76
  scope_hash: 506ccec91d50cc0971f8cc7c5a0ba710425e6bbc664ce7ed22b4db2e47060936
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-12T17:29:00Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/ai_council/clients.ts:1748-1789 | Removing `--system` lets the gemini invocation actually run, but the argv still passes neither `-p/--prompt` nor the `query` positional — and the diff's own `_stdin_payload` docstring states `-p` is what selects non-interactive mode. The failure mode is traded, not removed: an instant `exit_2` at argument parsing becomes a probable interactive-mode stall for the full `DEFAULT_CLI_TIMEOUT_SECONDS` (120 s) per call, blocking the fan-out. Worse, the timeout branch in `ask()` returns before `record_cli_call`, so those stalls never decrement the daily quota that the comment at clients.ts:1376-1377 says exists precisely "so a broken CLI cannot burn the whole budget in a tight loop". The open question is documented honestly but the half-fixed invocation ships anyway. | fixed | 368c895d78d50b79520b686ade54e7d9aeddfe76 — the quota half is repaired and is the general defect: timeout and os_error now record the call, so a hanging CLI can no longer evade the cap that exists for it. The stall half was MEASURED and did not reproduce: all three argv shapes (no `-p`, `-p ''`, `-p 'text'`) return in 3-4 s against the real binary. That measurement does not fully discriminate — auth fails first with `IneligibleTierError` — so the headless question stays recorded at the call site and the member now ships `enabled: false` with both caveats in the doc. |
| 2 | medium | tests/scripts/ai_council/clients.test.ts:575-576,606-607,613-614 | The assertions written to pin "the prompt survives the fold" are vacuous — the single-character probes collide with the delimiter text the fold itself emits. In `<<<SYSTEM_INSTRUCTIONS>>>`, `SYS` is at index 3 and `U` (of `INSTRUCTIONS`) at index 15, so `expect(stdin).toContain('U')` and `expect(stdin.indexOf('SYS')).toBeLessThan(stdin.indexOf('U'))` both hold for *any* folded output, including one whose user prompt is empty. Same on the xai/perplexity row: `cmd[2]).toContain('S')` and `toContain('U')` are satisfied by the header alone. These tests cannot fail on a user-prompt-dropping regression, which is the sibling of the exact defect the diff is fixing. The OpenAI equivalents (`'USER'`) are unaffected — use a token that does not occur in the wrapper. | fixed | 368c895d78d50b79520b686ade54e7d9aeddfe76 — probes are now `ZQSYSTEMZQ` / `ZQUSERZQ`, which occur nowhere in the wrapper, plus `endsWith` on the user prompt and an explicit dropped-user-prompt case that fails if the fold loses it |
| 3 | medium | src/scripts/ai_council/clients.ts:1748-1789 · tests/scripts/ai_council/clients.test.ts:22-27 | py2ts parity gap: the test file's own header declares `scripts/ai_council/clients.py` as the differential oracle (ADR-094), and the oracle set covers `_parse_output`, `_classify_stderr`, module constants, `record_cli_call` and `ManualClient` — never `_build_command` / `_stdin_payload`. This branch fixes a *fatal* argv defect on the TS side only and touches no Python file, so any Python counterpart keeps the rejected `--system` argv and the discarded system prompt, with no test that can detect the divergence. Either port the same change or record why the Python side is dead. | accepted-risk | Accepted by the branch author: the premise does not hold, so there is nothing to port. `src/scripts/ai_council/clients.py` does not exist, and `ls src/scripts/ai_council/*.py` matches zero files — the Python side was retired in the py2ts port, so no counterpart can carry the rejected argv. The reading was fair, because the test file's header still described the dead oracle as if it were live; that header is corrected in 368c895d78d50b79520b686ade54e7d9aeddfe76 rather than the code. |
| 4 | low | src/skills/ai-council/SKILL.md:246-260 · dist/agent-src/skills/ai-council/SKILL.md:246-260 | The user-facing doc was updated to assert the `--system` fix but records neither of the two caveats the same change puts in the code: that headless operation without `-p` is unestablished, and that the free tier fails with `IneligibleTierError`. It still presents the transport as wired ("the prompt piped on stdin") under an `enabled: true` example, while clients.ts:1785 justifies shipping the open question on the grounds that "this member ships `enabled: false`". A reader following the doc enables exactly the path the branch treats as unverified. | fixed | 368c895d78d50b79520b686ade54e7d9aeddfe76 — the bullet now carries both caveats (headless-without-`-p` unestablished; free tier refuses this client with `IneligibleTierError`) and the YAML example ships `enabled: false` with a pointer to them |
| 5 | low | src/skills/ai-council/SKILL.md:269-271 | Asymmetric documentation of the same class of change: the codex and gemini system-prompt folds each get an explicit doc sentence, but the xai + perplexity behaviour change — the system prompt went from silently discarded to folded into `-p`, altering what two council members actually receive — is not mentioned anywhere. The paragraph naming those two members still covers billing only. Not a false claim, but the one place a reader would look for it stays silent. | fixed | 368c895d78d50b79520b686ade54e7d9aeddfe76 — the xai/perplexity paragraph now states that the system prompt was discarded before this change, that it now rides in `-p`, and both consequences (argv-borne payload, estimated input cost) |
| 6 | low | src/scripts/ai_council/clients.ts:1904,1973 | The fold enlarges an argv-borne payload the same file elsewhere treats as a limit risk: the codex and gemini adapters pipe on stdin explicitly "to dodge argv limits", while these two put the entire prompt in `-p` and the diff now prepends the system prompt on top, with no size guard. The increment is small next to a bundled diff, but overflow surfaces as `os_error: E2BIG` — a code `_classify_stderr` maps to nothing actionable, so the operator gets no hint that the cause is payload size. | fixed | 368c895d78d50b79520b686ade54e7d9aeddfe76 — the os_error branch attaches `hint: 'argv too long — the prompt exceeded the platform argument limit'` for `E2BIG`, and the doc names the failure for those two members. The argv-borne shape itself is unchanged, for the reason in the commit: moving them to stdin needs a documented stdin mode and neither binary is present. |
| 7 | low | src/scripts/ai_council/clients.ts:1911-1926,1980-1992 | Billable-member accounting drifts further: both community adapters are `billable = true`, yet `_parse_output` hardcodes `input_tokens: 0` while estimating output as chars/4. The diff materially grows the real input (the system prompt is now billed on every call) without touching the estimate, so the USD tracker under-reports these members by a larger margin than before. Mirror the chars/4 estimate on the input side, or state why input is deliberately unmetered. | fixed | 368c895d78d50b79520b686ade54e7d9aeddfe76 — estimated, not merely stated. `_parse_output` could not do it (its signature never sees the prompt), so `ask()` fills `input_tokens` from the payload actually sent, behind an opt-in `estimates_input_tokens` so a provider-reported 0 is never overwritten. Pinned by three tests incl. the never-overwrite case. |
