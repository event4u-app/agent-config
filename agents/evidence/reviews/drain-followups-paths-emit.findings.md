# Findings: drain-followups-paths-emit
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 734dd77fb78fea8771367d2ffd904c505b0cb2614d669f9cce62fc9c5885b2f2 | diff: 3d7aa44b6137a3735342b7af603f359e34670492 | reviewer: r2-fresh-subagent-drain-followups-paths-emit | prompt_hash: c663ce00be77e78391ab45c6f1bb17bad295ece038357e9a59995553be09261b -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-followups-paths-emit"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 3d7aa44b6137a3735342b7af603f359e34670492
  scope_hash: 734dd77fb78fea8771367d2ffd904c505b0cb2614d669f9cce62fc9c5885b2f2
  roadmap: agents/roadmaps/road-to-turnaround-followups.md
  roadmap_hash: dd8876676932e6f288adddabfe394de13647e113e5f4a95008b6cf6028ce4c94
  ac_hash: 6f13f39fffb378cdbb2eb8f4c6ada9acef76e3584b598d9cb2879b677db8d6b7
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T17:46:28Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/install/claudeRuleRewrite.ts:143-154 | `renderClaudeRule` rebuilds each file from `plan.globs` alone, so it discards EVERY frontmatter key — including `package: event4u/agent-config` and `source_path:`, which `_inject_package_tag` (install.ts:2876) writes into each rule during the copy immediately before. The rewrite runs at install.ts:3238, and `reap_tagged_orphans` runs at install.ts:3269 on the SAME run and matches on the literal line `package: event4u/agent-config`. Proven with a fixture: a tagged orphan is reaped before the rewrite (`['.../retired-rule.md']`) and invisible after (`[]`). Consequence: from this change on, zero files under `~/.claude/rules/` carry the ownership tag, so the marker-based reaper is dead for that subtree, and `_check_stale_orphans` in src/scripts/_cli/cmd_doctor.ts:1534-1537 (`tag !== PACKAGE_TAG_ID -> continue`) is reduced to permanently `ok` there. `global_deploy_inventory.reap_tagged_orphans`'s own docstring states the precondition this change falsifies — "every deployed `.md` carries the injected `package:` frontmatter tag" — and calls itself "the **only** path with ownership proof independent of inventory history", i.e. the backstop for rules no inventory ever recorded (legacy installs, renames). Nothing tests it: all 30 install test files pass. | fixed | fixed in 3d7aa44b6 — PRESERVED_KEYS (package, source_path) are spliced back RAW, including onto rules with no paths: block, which is 101 of 104. renderClaudeRule stays activation-only so the byte-identity comparison against condense._emit_claude_rule still holds. Four regression tests: scoped, unscoped, every declared key, idempotence with the tag present. Simulation re-run WITH _inject_package_tag: package tag 104 -> 104. |
| 2 | medium | src/scripts/rule_activation_census.ts:285-288 | The package-only subtraction is applied to EVERY `--projection` target, hardcoding the assumption that the directory is a globally-partitioned host layer. It is wrong for the other two real targets. (a) A full/standalone projection — the fail-safe default `partitionEligibility.ts` returns on a fresh checkout, an absent install record, a version mismatch, or a fingerprint mismatch — carries all 4 scoped rules; measured against a fixture rendered by this branch's own emitter (120 files, 4 declaring `paths:`) the census now prints `⚠️ diverges (3 expected, 4 ...)`, while the same fixture under HEAD~1 printed no warning. That is a regression introduced by this change, not a pre-existing one. (b) Against this repo's correctly generated project layer, `./scripts-run src/scripts/rule_activation_census --projection .claude/rules` prints `1 of those is package-only and never delivered (source-of-truth), so a partitioned projection is expected to declare 3` — `source-of-truth` is the one rule that layer DOES deliver and the only one there declaring `paths:`, so the explanatory line is inverted, not merely noisy. The instrument's stated purpose is to make a divergence readable; it now mis-reads two of its three documented targets. CI is unaffected (`check_rule_activation_census` passes no `--projection`). | fixed | fixed in 3d7aa44b6 — the partition is now DETECTED, not assumed: if a package-only rule file is present in the projection, it is not a global layer and nothing is subtracted. Third polarity test added beside the two existing ones. |
| 3 | medium | src/install/claudeRuleRewrite.ts:112-117 | The dropped-pattern loop calls `warn` with no `quiet` guard, and install.ts:500 `warn` writes to stderr unconditionally. Counted over the globally-delivered rule set (119 rules minus the 15 `isExclusivelyPackageOnly` ones): **19** `⚠️ ... dropped 'paths:' pattern ... (mixed-triggers)` lines are now emitted on every global install, for a condition that is permanent and by design (`_claude_paths_plan` withholds `paths:` from mixed-trigger rules on purpose). Pre-change an install emitted none. The report was authored for the maintainer projection path, where it fires once per `task generate-tools` and names an authoring decision a maintainer can act on; on the consumer install path it names nothing the consumer can act on, survives `--quiet`, and pollutes stderr for scripted/CI installs. condense.ts:1206-1208 records this repo's own verdict on the pattern: "a permanent ... warning that was pure noise. A warning that is always on is a warning nobody reads." | fixed | fixed in 3d7aa44b6 — one counted line under the quiet guard instead of one warn per dropped pattern; the per-pattern detail stays in the returned struct. Test asserts 4 dropped patterns produce exactly 1 line, and none under quiet. |
| 4 | medium | src/install/claudeRuleRewrite.ts:200 | `fs.writeFileSync` is the only filesystem call in `rewriteClaudeRules` not wrapped in try/catch — `readdirSync` (172-175), `lstatSync` (179-183) and `readFileSync` (189-193) all are. `rewriteAndReport` is called at install.ts:3238 with no try/catch, and its caller at install.ts:3670 has none either, so an EACCES/EROFS/ENOSPC on one rule file throws out of the per-tool loop in `_deploy_global_content` and aborts the whole install — skipping the remaining tools, `record_deploy`/`save_inventory` (install.ts:3269-3277), the lockfile correction, and `stampHostLayerFingerprint` (install.ts:3742). Reachable precondition, not hypothetical: a root-owned file under `~/.claude/rules/` left by an earlier `sudo`-run global install. Pre-change nothing in that block could throw on a rules-dir permission problem. | fixed | fixed in 3d7aa44b6 — writeFileSync wrapped; the failure is recorded in failed[] and reported, and the loop continues. Test asserts the rule after the unwritable one in sort order is still rewritten. |
| 5 | low | agents/roadmaps/road-to-turnaround-followups.md:107-135 | Step 3.1 is `[x]` but its `verify:` clause — "after a fresh install, `grep -lE '^paths:' ~/.claude/rules/*.md \| wc -l` is 3" — was not run; the step substitutes a simulation that "applies `isExclusivelyPackageOnly` exactly as `_rule_filter_for_source` does, copies what survives, and runs the rewrite". The substitution is declared rather than hidden, and its stated reason is defensible, but it demonstrably had a hole: the simulation reproduces the filter and the rewrite and omits `_inject_package_tag`, which is the step the real copy performs between them — which is exactly why finding 1 was not observed. The verify clause as written would also not have caught finding 1 (it counts `paths:` files, not tags), so the gap is in the criterion as much as in the execution. | fixed | fixed in 3d7aa44b6 — recorded at the step, with the consequence named: the omission of _inject_package_tag is why the review found finding 1 rather than this step. Simulation re-run with the injector in the loop. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
