# Findings: drain-supervised-telemetry
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: db7d66ecded2127418a54101764ddf3f675179539b61427c01cf9f8b42cc4ca2 | diff: 3a4815f6a926ae80d0c1611011f82039be87ab85 | reviewer: r2-fresh-subagent-drain-supervised-telemetry | prompt_hash: db83b329bba15005bec929e3de3a1ecac154ba75794b95b9cca74dc8b489eeb0 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-supervised-telemetry"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 3a4815f6a926ae80d0c1611011f82039be87ab85
  scope_hash: db7d66ecded2127418a54101764ddf3f675179539b61427c01cf9f8b42cc4ca2
  roadmap: agents/roadmaps/later/road-to-supervised-telemetry-collector.md
  roadmap_hash: eef331465488bbb91aaeb20ba5e3cb7068c12d69710c44bc9fa17db48e391793
  ac_hash: a6b7371bd360a6afc943cb6eaa1acbac169b1e050cb4838eea57d1dd6189a59b
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T03:14:04Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/collector_daemon.ts:428 | `sleepSync` blocks the main thread with `Atomics.wait` for the whole beat interval, so the `SIGTERM`/`SIGINT` handlers installed at `:514-515` cannot run until the sleep returns — a Node signal handler is a JS callback on the event loop and a synchronous block starves it. Stop latency is therefore up to `beatMs`, and the shipped default is `DEFAULT_BEAT_MS = 30_000` (`:85`, used at `:352`) while `terminateCollector`'s default grace is `5_000` (`collector_supervision.ts:535`). In the shipped configuration the `SIGTERM` half of the kill switch can never win: `collector_daemon stop`, launchd and systemd all escalate to `SIGKILL`, so every stop is an unclean stop — lock and heartbeat residue left behind (the PROPERTY 3 state), the SQLite handle never closed by `runLoop`'s `finally`. `docs/contracts/collector-operations.md:34` documents "sends `SIGTERM` and escalates to `SIGKILL` after a grace period" as an escalation for a *wedged* collector; here it is the normal path. PROPERTY 2 ("SIGTERM ends it cleanly: lock released, heartbeat removed") passes only because `collector_lifecycle.test.ts:110` spawns every daemon with `--beat-ms 50` — the one configuration in which the graceful path is reachable — so the suite certifies a beat production never uses. | fixed | 6f082a7eb — the loop is async AND the wait is interruptible — `requestStop` wakes it. A new lifecycle case spawns at the PRODUCTION beat and asserts the process is gone inside 5 s; async alone still measured 29,995 ms. |
| 2 | medium | .github/workflows/tests.yml:488 | The new `collector-lifecycle` job runs vitest (twice over, via `run_lifecycle_suite` and `check_static_parity`) without the two preparation steps every other vitest-running job in this workflow performs first: `./scripts-run src/scripts/build_discovery_manifest --write --quiet` and `npm run build --silent` (`node-tests` at `:222`/`:233`, `golden-tests` at `:479`/`:483`, `workspace-tests` likewise). `check_static_parity` runs the whole dispatcher-reaching set (~551 tests) twice; any of those tests that depends on the generated discovery manifest or on `dist/` reds identically in both runs, and the gate then exits 1 at `check_static_parity.ts:226` ("one of the two runs is red"). The job also carries no `timeout-minutes` while spawning real daemons with 90 s per-case timeouts. | fixed | 6f082a7eb — the job now builds the discovery manifest and `dist/` like every other vitest job, and carries `timeout-minutes: 20`. |
| 3 | medium | .github/workflows/tests.yml:257 | `tests/scripts/collector_lifecycle.test.ts` is not added to the `node-tests` `--exclude` list, so the new subprocess-heavy suite also runs inside the sharded matrix — on `ubuntu-latest` **and** `macos-latest`, in addition to the dedicated `collector-lifecycle` job. That is exactly the pattern the exclusion block at `:235-256` exists for ("the subprocess-heavy clusters ... are excluded here and run in the dedicated job"), and shard 3/4 is already recorded as over the per-job ceiling. Two consequences the diff does not acknowledge: the ~150 s suite is paid three times per CI run rather than once, and the job header's load-bearing claim that the Linux row is "NOT RUN" (`:492-500`) is false — the same file runs on `ubuntu-latest` in `node-tests`, it is merely not certified there. | fixed | 6f082a7eb — `collector_lifecycle.test.ts` added to the `node-tests` `--exclude` list; the header now says NOT CERTIFIED rather than NOT RUN, with the distinction stated. |
| 4 | medium | docs/contracts/ci-cost-budget.md:60 | The new `collector-lifecycle` prose block is inserted **inside** the job table rather than after it: the table's rows run `:49-58`, the prose runs `:60-76`, and five more table rows (`smoke-public-install.yml`, `consistency.yml`, `smoke.yml`, `skill-lint.yml`, `release-guard.yml`) resume at `:78-82` with no header or delimiter row above them. In GFM those five lines no longer render as table rows. The block's own text says the note is "under the table" (`:46`), which is what it was meant to be. | fixed | 6f082a7eb — the prose block moved below the table; the five orphaned rows render again. |
| 5 | medium | src/scripts/_lib/collector_denominator.ts:176 | The denominator has no window and no retention, so the two halves of the ratio are computed over different time bases. `recordOpportunity` writes a UTC date into every line (`:160`) but `readOpportunities` never parses `parts[0]` — it returns lifetime `total` and `byEvent` only, with no date filter — while the numerator side is pruned to `RETENTION_DAYS = 63` on every loop iteration (`collector_daemon.ts:399`). Nothing anywhere prunes, rotates or truncates `opportunities.log` (it is the file's only reader; grep confirms three call sites, all tests plus this one). Two effects: the declared 21-day window (`capture_rate.WINDOW_DAYS`) cannot be applied to the denominator at all, and once the store starts pruning the measured capture rate falls monotonically toward 0 regardless of real capture — the failure mode the module docstring says the two-writer design exists to make impossible. The disk budget compounds it: `RESOURCE_BUDGETS.disk_bytes.basis` derives its 12 MiB expected peak purely from "`RETENTION_DAYS` is 63 and a record is nine small columns", counting only the store, while the unbounded log lives in the same directory the ceiling measures and a breach *stops* the collector. | fixed | 6f082a7eb — `readOpportunities` takes a `{since, until}` window and parses the date; `pruneOpportunitiesOlderThan` runs beside `pruneOlderThan` on the same 63-day clock; the disk-budget basis corrected to name the log. |
| 6 | medium | tests/scripts/collector_vocabulary_parity.test.ts:20 | The vocabulary-drift guard covers only the event axis, and the platform axis has the identical, identically-directional failure. `recordOpportunity` refuses any platform outside `COLLECTOR_PLATFORMS` (`collector_denominator.ts:157`), and the dispatcher validates no platform at all — `EVENT_VOCABULARY` has no platform counterpart, `args.platform` is whatever the trampoline passes, and `dispatch_hook.ts:624` even has a `"generic"` fallback in the concern path. A platform added to `src/scripts/hook_manifest.yaml` (or a trampoline typo) is therefore dropped from the denominator silently, shrinking the opportunity set and biasing the rate **upward** — word for word the bias this file's own header says "a test is the only thing that can catch". No test pins `COLLECTOR_PLATFORMS` against the manifest's `platforms:` keys. | fixed | 6f082a7eb — `COLLECTOR_PLATFORMS` is now pinned against `hook_manifest.yaml`'s platform keys — the manifest is the authority because the dispatcher validates no platform at all. |
| 7 | medium | tests/scripts/collector_daemon.test.ts:162 | The default-off assertion reds on precisely the machine the feature is aimed at. `enumerateDaemonProcesses()` matches every `collector_daemon` line in `ps` and excludes only lines containing `--root` (`:114`) — i.e. it excludes test-owned daemons and keeps real ones. A developer or user who has opted in and has a supervised collector running (no `--root`, since the units never pass it — `collector_daemon.ts:439-443`) fails this test on every suite run, with a message about a fresh install. The property the step wants is "this suite started no daemon"; what is asserted is "no daemon is running on this machine", which the roadmap's own Phase 6 sets out to make false. | fixed | 6f082a7eb — the assertion compares against a baseline of daemons taken before the file ran, so it asks whether THIS SUITE started one rather than whether the machine has one. |
| 8 | medium | src/scripts/check_static_parity.ts:186 | A new CI-wired gate that emits the machine-readable `scanned:` line (via `reportScanned`) was added without a row in `src/config/gate-coverage.yml`. Two consequences, both named by that manifest's own contract header: the gate carries no `min_scanned` floor, so rule 3 ("BASELINE, NOT `> 0`") does not apply to it — `reportScanned` throws only at zero, so a discovery collapse from 31 files to 1 still exits green and reports parity over the wrong population; and the manifest's stated coverage ("77 of them emit ... and 74 of those 77 are listed below ... complete for every gate that can carry a floor") is now stale, as is the computed `count_gate_scripts()` total, since `check_static_parity` matches the `check_*` population it counts. `check_gate_coverage.ts` only verifies listed rows, so nothing reds on the omission. | fixed | 6f082a7eb — row added to `gate-coverage.yml` with `min_scanned: 20` (a discovery collapse, not zero, is this gate's failure shape) and a `// self-test-exempt:` marker in the gate source with its reason. |
| 9 | low | src/scripts/check_static_parity.ts:79 | `parityFiles` is documented as returning "every test file that reaches the dispatcher" (`:73`) and "discovered, never listed" (`:76`), but the mechanism is `grep -rl 'dispatch_hook'` over `*.test.ts` — a literal-token search with no module resolution. A test that reaches `dispatch_hook` transitively (through a helper, a fixture builder or a re-export that does not spell the token) is outside the compared population, and nothing reports the omission. The module's honest-limit paragraph (`:33-36`) names only the "future collector call site placed somewhere else" case, not this one. | fixed | 6f082a7eb — the module docstring now carries TWO honest limits; limit 2 states that grep finds files that SPELL the token, so the set is a lower bound rather than the exact population. |
| 10 | low | src/scripts/check_static_parity.ts:204 | Both runs share one temp `HOME` (created once at `:204`, passed to `runSuite` twice) and run A executes first, so run B starts against whatever state run A's ~551 tests left in that directory. The gate's whole claim is that a per-test verdict difference is attributable to the collector; a difference caused by run-order state leak is indistinguishable from one, and it would surface as a false red on a gate whose failure message says static operation regressed. A fresh `mkdtemp` per run costs nothing and removes the ambiguity. | fixed | 6f082a7eb — a fresh `mkdtemp` per run — run B no longer starts against run A's leftovers. |
| 11 | low | src/scripts/_lib/collector_supervision.ts:371 | Two `acquireRuntimeLock` paths report `reason: 'held-by-live-process'` for states that are not contention. The bounded-out return at `:375` fires after `FENCE_ATTEMPTS` and can be reached with `holder: null` (unparseable or vanished lock), and the whole `catch` at `:341` assumes the `wx` create failed with `EEXIST` — an `EACCES`, `EROFS` or `ENOSPC` create failure reads the (absent) lock, gets `null`, and spins the retry loop to the same "held by a live process" verdict. `start()` then reports `refusal: 'lock-held'` for a permissions or disk problem, which sends an operator to `docs/contracts/collector-operations.md`'s two-collectors row instead of to the real cause. | fixed | 6f082a7eb — `EEXIST` is contention; anything else returns `unwritable` with the errno, and a bounded-out fence loop returns `contested-out`. |
| 12 | low | src/scripts/collector_daemon.ts:449 | Flag parsing accepts malformed invocations that the trailing-value guards were added to catch. `--root --beat-ms 5` passes the `argv[rootFlag + 1] === undefined` check and silently uses the literal string `--beat-ms` as the user root; the same shape applies to `--beat-ms --root x`, where `Number.parseInt('--root')` is `NaN` and is correctly rejected, so the two flags behave inconsistently on the same input class. `--beat-ms 0` and `--beat-ms -5` both pass `Number.isFinite` and turn `sleepSync` into a no-op, i.e. a busy loop that will budget-stop the collector after two iterations. | fixed | 6f082a7eb — a flag value that is missing OR starts with `--` is refused, and `--beat-ms` must be positive rather than merely finite. |
| 13 | low | src/scripts/collector_daemon.ts:354 | `openCollectorStore` is called outside the `try`, so a throw there (a corrupt or unreadable store — `isStoreAvailable()` at `start():303` probes the runtime, not this file) skips the `finally` at `:420` and leaves behind the lock and heartbeat that `start()` has already taken. `main('run')` does not wrap `runLoop` either, so the process dies with an uncaught exception and a supervisor restart loop then meets its own stale lock on every attempt until the fencing path clears it. | fixed | 6f082a7eb — `openCollectorStore` moved inside the `try`, and `main('run')` wraps `runLoop` so an uncaught throw still releases the lock and heartbeat. |
| 14 | low | src/scripts/_lib/collector_denominator.ts:236 | `claimSpool` names the claimed file `draining-${at}-${process.pid}.jsonl` and `renameSync` overwrites silently, so two claims by one process inside the same millisecond destroy the earlier batch rather than colliding. The per-user runtime lock makes this narrow in production, but `claimSpool` and `drainOnce` are exported and driven directly by tests, and the docstring's "no lock, no lost line" guarantee is stated without this qualifier. | fixed | 6f082a7eb — `claimSpool` appends a monotonic per-process counter, so two claims in one millisecond cannot overwrite each other. |
| 15 | low | tests/scripts/check_static_parity.test.ts:4 | The size of the compared suite is stated as three different numbers inside one change: "551 tests" here, "~556 tests" in `.github/workflows/tests.yml:563` and twice in `src/config/ci-local-parity.yml:127,136`, and "31 dispatcher-reaching test files" in the AC-7 rationale. The only observed figure — the roadmap's own recorded run — is 551. | fixed | 6f082a7eb — the count is stated nowhere now and printed by the gate; it moves with the suite. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
