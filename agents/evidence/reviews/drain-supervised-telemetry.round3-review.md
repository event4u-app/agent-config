# Findings: drain-supervised-telemetry
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 0968aaad1d4a841a6ff22a0b6defe8735874a16fab7660c063f6d56484a73b4f | diff: 974abdf957c3d1b71b6cfc15b0faf178614f85d4 | reviewer: r2-fresh-subagent-drain-supervised-telemetry | prompt_hash: 02974d13c838312179bb3797d38ec2a1c8f0a62216e4dbd78ab6766e66a13138 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-supervised-telemetry"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 974abdf957c3d1b71b6cfc15b0faf178614f85d4
  scope_hash: 0968aaad1d4a841a6ff22a0b6defe8735874a16fab7660c063f6d56484a73b4f
  roadmap: agents/roadmaps/later/road-to-supervised-telemetry-collector.md
  roadmap_hash: d098ad395230f7f9c85903e355d4af7569778beec1d870c2d9bd6431e1580550
  ac_hash: ce1369467f86b7090e43e4abfac87dc8114b5755b86e8b31e3802ab4f1adc7df
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T03:58:24Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/collector_denominator.ts:308 | `spoolRecord` has NO production caller — the capture-rate numerator has no writer. The denominator half is wired into `dispatch_hook.main` (dispatch_hook.ts:1170) and `collector_daemon.drainOnce` drains a spool, but `collector_denominator.ts` is new in this branch so its only importers are the files in this diff: `collector_daemon.ts` imports `claimSpool/readClaimedSpool/spoolDir` and never `spoolRecord`, `dispatch_hook.ts` imports only `recordOpportunity`, and every remaining call site is a test. In the field the denominator therefore climbs while the numerator stays 0 by construction. Once 6.1's window and sample thresholds are met, `judgeCapture` returns `eligible: true, meetsTarget: false` and AC-10's miss branch fires 6.3's decision record for a wiring omission rather than for a capture failure. The roadmap never names the gap. | fixed | acf13d360 — `recordCapture` is the numerator's production writer — machine id (locally generated UUID, never derived), episode id (one-way hash of the host session id), per-episode sequence counter — wired into `dispatch_hook.main` beside the denominator, as two calls so they can fail differently. |
| 2 | high | src/scripts/collector_daemon.ts:245 | `drainOnce` DELETES a claimed spool file it could not read. `readClaimedSpool` (collector_denominator.ts:354-360) swallows a read error and returns `{records: [], malformed: 0}`, indistinguishable from an empty file; `drainOnce` then offers zero records and unconditionally `fs.rmSync(file, {force: true})`. A transient EACCES/EMFILE on a claimed batch destroys it silently and reports 0 malformed. This contradicts `drainOnce`'s own docstring ("A file is deleted only after every one of its records has been offered to the store") and `readClaimedSpool`'s ("Unparseable lines are reported, never dropped silently"), and it under-counts the numerator — the same loss class the crash-recovery re-drain exists to prevent. | fixed | acf13d360 — `readClaimedSpool` returns `unreadable`, and `drainOnce` KEEPS the file for the next drain instead of deleting a batch it never read. |
| 3 | medium | src/scripts/hooks/dispatch_hook.ts:1170 | The denominator counts the package's OWN test suite. `recordOpportunity(args.event, args.platform)` is called with no user-root and no self-observation filter, so on an opted-in machine every `dispatch_hook` test that reaches `main` appends to the real `~/.event4u/agent-config` opportunity log. Metric-definition exclusion 3 (roadmap:293) excludes "dispatches in the package's own test suite and CI (self-observation)" and nothing implements it. `check_static_parity` pins HOME for its two runs precisely so the invoking user's state cannot decide the answer, but the ordinary `node-tests` job and a local `npm test` do not — so the exclusion is honoured only inside the one gate that does not need it. Those dispatches spool nothing, so they bias the measured rate DOWN. | fixed | acf13d360 — `isSelfObservation` implements metric-definition exclusion 3, on three signals the RUNNER sets (`VITEST`, `CI`, `NODE_ENV=test`) so the measured thing cannot spoof them. |
| 4 | medium | src/scripts/_lib/capture_rate.ts:230 | `judgeEnablement` treats `undefined` as a PASS. The loop tests `value === null` then `value === false`; a key that is absent at runtime is neither, so it lands in neither `missing` nor `failed` and still yields `flip: true`. The docstring argues that "an optional field would let a caller omit one and still typecheck", but the guarantee is compile-time only and the readings for 6.2 are exactly the kind of object assembled from JSON or a cast — `capture_rate.test.ts:255` builds one with `as unknown as EnablementReadings`. The step's binding rule ("a missing reading blocks the flip") needs `value !== true` as the accept test; no case in the suite passes an omitted key, so nothing catches it. | fixed | acf13d360 — `judgeEnablement` accepts only a literal `true`; an absent key now blocks the flip exactly like an explicit `null`. |
| 5 | medium | src/scripts/_lib/collector_denominator.ts:264 | `pruneOpportunitiesOlderThan` loses concurrently appended opportunities. It is read-filter-write-tmp-rename (`readFileSync` at :272, `renameSync` at :291) over a file that hook processes append to with `appendFileSync` from other processes; any line appended inside that window is on the replaced inode and is dropped. The docstring only claims crash-atomicity ("either the old file or the new one and never a truncated log") and does not mention the concurrent-writer loss. The direction matters and the module states it three functions earlier: "an understated denominator biases the rate UPWARD" — i.e. toward making the 90 % target look met. The daemon runs this every beat (collector_daemon.ts:440). | fixed | acf13d360 — the prune re-reads the tail beyond the bytes it consumed and carries it into the replacement, so a line appended during the filter survives. |
| 6 | medium | tests/scripts/collector_daemon.test.ts:163 | The POSITIVE CONTROL reds on the machine the feature targets. It asserts `expect(enumerateDaemonProcesses()).toEqual([])` with no baseline subtraction, so a developer who has opted in and has a supervised collector running against the real user root fails this case. That is precisely the defect the sibling test 37 lines below was corrected for (R2 round-2 finding 7, comment at :182-190), which now diffs against `baselineDaemonPids` at :200. The same fix was not carried to the control, so one of the two assertions still encodes "no daemon is running on this machine". | fixed | acf13d360 — the positive control diffs against `baselineDaemonPids`, the same baseline its sibling assertion already used. |
| 7 | low | src/config/ci-local-parity.yml:140 | The `run_lifecycle_suite` ci_only reason contradicts itself and the workflow. It states the five properties "run in the ordinary vitest chain" locally (:140) and then justifies the classification by saying a `task ci` wrapper "would also spawn real daemons on every local run for a file the local chain then ignores" (:146). Both cannot be true — if the file runs locally, local runs already spawn the daemons, so that is not a cost the wrapper adds. It is also stale against `.github/workflows/tests.yml:257`, which excludes `collector_lifecycle.test.ts` from `node-tests` in this same diff. | fixed | acf13d360 — the reason no longer says the suite both runs and does not run locally; the CI-only status now rests on the revision-stamped artifact alone, which is the argument that survives. |
| 8 | low | src/config/ci-local-parity.yml:123 | Insertion point orphans an existing comment. The pre-existing `report_pack_delta` explanation, ending "Reachable on demand: `task report-pack-delta -- --base <ref> --head <ref>`." at :123, is now separated from its own `- id: report_pack_delta` entry (:153) by two new entries and about thirty lines, so it reads as the preamble to `check_static_parity`. The new blocks should have gone after `report_pack_delta`, or the old comment moved with it. | fixed | acf13d360 — the two new blocks moved AFTER the `report_pack_delta` entry, so its "Reachable on demand" line sits with its own entry again. |
| 9 | low | docs/contracts/ci-cost-budget.md:64 | The new prose is absorbed into the table. The paragraph beginning "**`collector-lifecycle`, and why it is macOS-only and not free.**" starts on the line immediately after the table's last row (:63) with no blank line between them, so GitHub-flavoured Markdown continues the table and renders the whole paragraph as an extra row in the first cell rather than ending the table. Every other paragraph in the file is blank-line separated. | fixed | acf13d360 — blank line inserted before the prose block; the five orphaned rows render as table rows again. |
| 10 | low | docs/contracts/ci-cost-budget.md:67 | Test-count drift reintroduced in the same diff that forbids it. The doc writes "the static-parity comparison (two 556-test runs)" while the roadmap writes "551 tests" twice (roadmap:780 and :991) — and `tests/scripts/check_static_parity.test.ts:6-9` states that the exact count "is deliberately not written down anywhere in this change (R2 round-2 finding 15)" because "three different figures for it appeared across one diff". Two figures are back, in two files the gate does not check. | fixed | acf13d360 — both figures removed. The count is now stated nowhere and printed by the gate, which is what the round-2 fix said and this change had not finished doing. |
| 11 | low | agents/roadmaps/later/road-to-supervised-telemetry-collector.md:736 | Step 4.1 still carries the falsified cost figure. It reads "One `stat` is the whole cost on a default-off install", which is the number R2 finding 14 corrected: `isCollectorEnabled` probes the OPT-OUT marker before the ENABLED one, so the cost is two `existsSync` calls. The correction was applied to `dispatch_hook.ts:1156` and to the `recordOpportunity` docstring in the same diff and not to the roadmap, leaving the two halves of one change disagreeing on the fact each cites the other for. | fixed | acf13d360 — step 4.1 now says TWO `existsSync` calls, matching the code and the docstring the same change had already corrected. |
| 12 | low | src/scripts/collector_daemon.ts:462 | `runLoop`'s `finally` can skip its own cleanup. It runs `handle?.db.close()` before `stop(userRoot)`, so a throw out of `close()` — a corrupt or already-invalidated handle — leaves the lock and heartbeat `start()` took on disk. That is the orphaned-residue failure R2 round-2 finding 13 moved `openCollectorStore` inside the `try` to prevent, still reachable through the cleanup path itself. `main` masks it on the `run` path via its own `catch { stop(userRoot) }`, but `runLoop` is exported and is driven directly by the daemon tests. Releasing before closing, or wrapping the close, removes the case. | fixed | acf13d360 — `stop(userRoot)` runs BEFORE `db.close()`, and the close is wrapped — an unclosable handle no longer strands the lock and heartbeat. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
