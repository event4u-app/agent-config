# Findings: road-to-journal-host-capture-measurement
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: e4f5ad6bf6d31b4b6d3dc68111151c6d9eb1abf2e3de736b297a7106ca7d796c | diff: ec0a35cb8d15f898e724da853edce7745f032c5a | reviewer: r2-fresh-subagent-road-to-journal-host-capture-measurement | prompt_hash: 37771c9bf61af53cebb0b3e58322d63aaacf9680f63a8513e8f6ecb02f57eb70 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-road-to-journal-host-capture-measurement"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: ec0a35cb8d15f898e724da853edce7745f032c5a
  scope_hash: e4f5ad6bf6d31b4b6d3dc68111151c6d9eb1abf2e3de736b297a7106ca7d796c
  roadmap: agents/roadmaps/archive/road-to-journal-host-capture-measurement.md
  roadmap_hash: 24fa2c7a29bf5f89e7ae1bd8afee8e5f0298a60fe372c53ad4dce5af08814fc0
  ac_hash: 06e8d0e9ff7141df5e1c340f69e5a05409d995e26fe076d416a12126af9529ab
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T17:07:46Z
-->

> **RE-BOUND IN PLACE, 2026-08-29** (contract §2.7 path 1, not a fresh
> skeleton). The review ran against scope `0f9f108e…` / head `2765332d`. Acting
> on its findings moved the reviewed content, so the scope hash moved with it;
> the artefact is re-bound to the scope the fixes produced (`e4f5ad6b…` / head `ec0a35cb`) rather than archived or re-dispatched, because archiving would
> leave the shipping content with no review at all. Every row was already
> terminal before the re-bind. The rows below are the ORIGINAL findings against
> the original scope — the re-bind changes what they are bound to, never what
> was found.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/measure_host_capture.ts:156 | Numerator and denominator are over DIFFERENT populations. The denominator walks every transcript under `~/.claude/projects` (all projects on the machine, 2,303 files); the numerator reads `resolveJournal(process.cwd())`, which is the journal of ONE repository (`<git-common-dir>/agent-journal/journal.sqlite`). Events from other projects can never reach that store, so the ratio is not a capture rate over one population. Harmless in the pinned run only because the numerator is 0 / `store-absent`, but the evidence page's own Revisit-if instructs re-running `measure_host_capture` on an opted-in machine, where this silently deflates the rate by every session outside the measured repo. The caption's "population 2,281 Claude Code sessions" describes the denominator's population and not the numerator's. | fixed | da5e4e3ad — `scope` is now a recorded field on the denominator record, default `repository`, resolved from `git worktree list --porcelain` and failing CLOSED to an empty scope. Published five-cell denominator moved 152,151 → 127,711 (16 % lower). `--scope machine` prints an explicit UNMATCHED warning. |
| 2 | medium | src/scripts/_lib/host_denominator.ts:334 | Refinement 2 (`isSidechain`) is applied only inside the `type === 'user'` branch. Assistant records are never checked, so every tool call a subagent makes inside a sidechain feeds `pre_tool_use` / `post_tool_use` (144,065 events, 97 % of the six-cell total) and `subagent_start` / `subagent_stop`. The published rule table states a sidechain position for the smallest cell and none at all for the largest, and 53,002 sidechain `user` records were excluded — so the share is large. The inclusion may well be correct (parent-session tool hooks do fire for subagent tool calls), but it is an unstated, unversioned rule governing the dominant term of a denominator whose whole claim is that its derivation is pinned at v1. | fixed | da5e4e3ad — the assistant-side sidechain rule is now rule TEXT at v2, and the share is published (46,540 of 121,875 tool_use, 188 of 1,120 Agent/Task) so a reader who disagrees can subtract instead of re-deriving. Records are still INCLUDED; the finding was that the rule was unstated, and the choice is now stated rather than changed. |
| 3 | medium | src/scripts/measure_host_capture.ts:153 | Off-by-one window. `windowStart = isoDay(now - days*86400e3)` and `windowEnd = isoDay(now)` are BOTH inclusive at both comparison sites (a day strictly below `windowStart` or strictly above `windowEnd` is excluded), so `--days 30` spans 31 calendar days. The published caption reads "window 2026-07-30 .. 2026-08-29 (30 days, the journal's own retention TTL)" — that interval is 31 days, and the denominator therefore covers one day more than the numerator's TTL can retain, biasing the rate down. | fixed | da5e4e3ad — `windowStart` is now `now − (days − 1)`, so `--days 30` spans exactly 30 calendar days. Published window is 2026-07-31..2026-08-29. |
| 4 | medium | src/scripts/measure_host_capture.ts:136 | The install-census parse is brittle in a way that fails silently toward `default`. The regex is not anchored to the `hooks:` parent (any 2-space `runtime_journal:` under any section matches), hard-codes exactly 2-space / 4-space indentation, and a non-match is indistinguishable from "key absent" — there is no `parse-failed` status beside the `NumeratorStatus` enum that exists precisely to stop a blind zero. Only two candidate layers are read, so a third layer carrying `enabled: true` is invisible. The population label ("default" vs "opted-in") is the load-bearing caption of both published figures and rests entirely on this read. | fixed | da5e4e3ad — parse anchored to the `hooks:` parent, indentation-tolerant, exported as `readJournalKey` and driven by five direct tests. `layers_with_key` and `layers_parse_failed` are separate published counters, so an absent key and a failed parse are no longer the same fact. |
| 5 | medium | src/scripts/measure_host_capture.ts:189 | A partial numerator is published as if counted. `counted` / `bound` / `all` are incremented inside the `try`; if `readAllEvents` or an iteration throws midway, the catch sets `numerator_status = 'store-unreadable'` but leaves the partial counts in place, and they are returned as `numerator_counted_cells` / `numerator_journal_bound_cells` / `numerator_all_events`. Reset them on the error path, or the enum that exists to prevent an unexplained number ships an unexplained number. | fixed | da5e4e3ad — the three counters are reset on the `store-unreadable` path, so a partial count can no longer ship beside a status saying it could not be read. |
| 6 | medium | src/scripts/measure_host_capture.ts:181 | The numerator is host-agnostic while the denominator is `claude`-only. `JournalEvent` carries no platform field, so any event written by another bound host on the same repository (augment / cowork slots) counts into the numerator against a denominator the module restricts to `platform: 'claude'` by contract. This can only bias the rate upward and is stated nowhere in the code or on the evidence page. | accepted-risk | da5e4e3ad — NOT fixable here: `JournalEvent` carries no `platform` field, and widening the journal's record is outside this roadmap. The bias direction is known (upward only) and is now stated on the evidence page (Limit 4) and printed by the script itself under `host-agnostic`. Accepted rather than silently carried. |
| 7 | medium | src/scripts/measure_host_capture.ts:110 | Unit conflation between the interface and the published claim. `InstallCensus.observable` counts settings FILES ("Settings layers present on this machine and readable") and `opted_in` counts layers that set the key; the evidence page publishes them as "Installs observable from the measuring machine: 1. Of those, installs with `hooks.runtime_journal.enabled: true`: 0". One machine carrying both a project and a user-global layer would report two "installs". The number carrying the measured-empty-population claim is in a different unit from the claim. | fixed | da5e4e3ad — `observable`/`opted_in` renamed to `layers_present`/`layers_with_key`/`layers_enabled`; the interface is `LayerCensus` and the evidence page publishes layers, with an explicit sentence that one machine carrying two layers would report two. |
| 8 | medium | src/scripts/_lib/host_denominator.ts:98 | The `session_start` rule states an iff that does not hold: "One per transcript file. A transcript exists if and only if a session started." `SessionStart` fires again on resume, clear and compact while a resumed session appends to the same transcript file (under-count), and a compacted or forked session can produce a second file for one logical session (over-count on the population side). 2,281 is published both as the `session_start` denominator and as "population 2,281 Claude Code sessions", so the rule's failure direction is unstated in a figure that carries the population claim. | fixed | da5e4e3ad — the rule no longer claims an iff. Both failure directions (resume/clear/compact appending to one file; a compacted session producing a second file) are stated in the rule text and the page says the 1,428 population figure inherits the imprecision. <!-- ref-ignore --> |
| 9 | medium | tests/scripts/host_denominator.test.ts:250 | The test writes its fixture tree INSIDE the repository: `fs.mkdtempSync(path.join(REPO_ROOT, 'agents', 'runtime', 'hd-test-'))`. `agents/runtime` has no tracked files (`git ls-files agents/runtime` is empty), so on a fresh checkout the parent directory need not exist and `mkdtempSync` throws ENOENT — an environment-dependent red. A hard abort before the `finally` also leaves an `hd-test-*` directory in the working tree, where clean-tree gates can see it. `os.tmpdir()` has neither property. | fixed | da5e4e3ad — the fixture tree moved to `os.tmpdir()` via a `withTempTree` helper, so neither the ENOENT-on-fresh-checkout path nor the stray-directory path exists. |
| 10 | low | src/scripts/_lib/host_denominator.ts:384 | Mislabelled census bucket. `if (day < windowStart or day > windowEnd) into.sessions_before_window += 1` files an AFTER-window transcript under a field documented as "Transcripts excluded because their first record predates the window", published as "First record before the window → excluded", count 17. Reachable via `--days`, clock skew, or a future-dated fixture; the totals still reconcile, so the error is silent. | fixed | da5e4e3ad — `sessions_after_window` is its own field, published in the census table, with a regression test. |
| 11 | low | src/scripts/_lib/host_denominator.ts:334 | `isSidechain` is tested before `isMeta`, so a record carrying both is counted only in `excluded_sidechain_user_records`. The evidence page publishes the two counts (768 / 53,002) as independent refinement-1 and refinement-2 exclusions; they are not independent, and the ordering that makes them overlap is undocumented. | fixed | da5e4e3ad — both counters increment for a record carrying both flags, the rule text says they are INDEPENDENT and may sum to more than the records excluded, and a test pins it. |
| 12 | low | src/scripts/_lib/host_denominator.ts:328 | `first_at` is the first record in FILE order that carries a timestamp, not the minimum timestamp. A transcript whose leading record is a back-dated `summary` (or any out-of-order record) is windowed by that record's day, which decides whether the whole file's counts land in the denominator at all. | fixed | da5e4e3ad — `first_at` is now the MINIMUM timestamp in the file, with a test over an out-of-order fixture. |
| 13 | low | src/scripts/_lib/host_denominator.ts:422 | Two silent under-enumeration paths with no audit counter, in a module whose own docstring argues "a denominator that silently misses two thirds of its population is worse than none": `findTranscripts` swallows every `readdirSync` failure, and `entry.isDirectory()` / `entry.isFile()` are false for symlinks, so a symlinked project directory or transcript is skipped; `countTranscriptFile:451` discards unparseable lines without counting them, while every other exclusion class (`sessions_undatable`, both refinements) is published. | fixed | da5e4e3ad — symlinked directories and transcripts are followed via `statSync`; `unreadable_directories` and `unparseable_lines` are published fields. The pinned run reports 0 unreadable and 6,650 unparseable — the second is exactly the number that should not have been invisible. |
| 14 | low | src/scripts/_lib/host_denominator.ts:116 | Code and evidence page disagree on the same measurement: the docstring says the three `stop` candidates "disagree by a factor of 40", the page says "by a factor of 44" (305 / 7 = 43.6). The test only asserts the ratio is `> 10`, so neither number is pinned by anything. | fixed | da5e4e3ad — both surfaces now say 'about 44 (305 / 7 = 43.6)', and the test pins the factor to the 43–44 band instead of `> 10`. |
| 15 | low | agents/roadmaps/archive/road-to-runtime-event-journal.md:146 | Doc drift introduced by this diff: the pointer was repointed to `agents/roadmaps/archive/road-to-journal-host-capture-measurement.md` but the sentence it belongs to still reads "— an ACTIVE roadmap, `status: ready`, carrying both closers as blockers". The path now names an archived file, and the prose contradicts the path one line below it. | fixed | da5e4e3ad — the carry note now says the roadmap was active AT THE TIME OF THE CARRY and has since closed and been archived, naming the evidence page. The prose no longer contradicts the path above it. |
| 16 | low | agents/roadmaps/archive/road-to-journal-host-capture-measurement.md:10 | `relates` key spelling is inconsistent inside one change. This file uses `- roadmap: road-to-runtime-event-journal`; `road-to-supervised-telemetry-collector.md:14` uses `- slug: …`, and this file's own deferred-resolution record asserts the sweep requires the reciprocal link as `{slug: …}`. One of the two spellings is presumably not read by whatever consumes `relates`, which would make the declared relation invisible rather than wrong. | fixed | da5e4e3ad — changed to `- slug: road-to-runtime-event-journal`. The reviewer's conditional reading was right: `_relatesTo` in `archive_completed_roadmaps.ts` matches `- slug: <x>` or a bare `- <x>` and nothing else, so `- roadmap:` declared a relation the sweep cannot see. |
| 17 | low | src/scripts/measure_host_capture.ts:177 | Inefficiency in the measurement path: `readAllEvents(handle)` materialises the entire journal table into an array before the window filter runs in JS, and the 2,303 transcripts are streamed strictly serially (`await countTranscriptFile(file)` inside the loop) with no concurrency. Acceptable for a one-off run, but both costs grow with the store and neither is bounded by the window the caller asked for. | fixed | da5e4e3ad — the journal read is now a windowed `GROUP BY` in SQL using the same day-string comparison the denominator uses, so the unbounded table read is gone. The serial streaming is RETAINED deliberately: 1,485 transcripts complete in about 8 s, and a concurrency pool for a one-off measurement is cost with no beneficiary. Named here rather than left implied. |
| 18 | low | src/scripts/_lib/host_denominator.ts:254 | `validateDenominator` checks the shape of `window_start` / `window_end` but never that `window_start <= window_end`. An inverted window passes validation and yields an all-zero denominator with every transcript filed as `sessions_before_window`, which `rate()` then reports as "undefined (denominator 0)" rather than as a bad input. | fixed | da5e4e3ad — `validateDenominator` refuses `window_start > window_end` with a message naming why (an inverted window reports a zero denominator instead of a bad input); a single-day window is explicitly still valid. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
