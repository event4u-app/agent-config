# Findings: drain-supervised-telemetry
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 328e0c9947a55db4bba6a0bf890cee6b6628122207f85dfaeb711eab4be63043 | diff: f173d9d1e9e3b0635739b26ca8674078ac1c888d | reviewer: r2-fresh-subagent-drain-supervised-telemetry | prompt_hash: fa04aacf77b6fa1dd3b70e954c719215ab71a9af491db745a56cd89cea83f75c -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-supervised-telemetry"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: f173d9d1e9e3b0635739b26ca8674078ac1c888d
  scope_hash: 328e0c9947a55db4bba6a0bf890cee6b6628122207f85dfaeb711eab4be63043
  roadmap: agents/roadmaps/later/road-to-supervised-telemetry-collector.md
  roadmap_hash: 122e9bd13852e50108de2eacce2d6e5a43f86c93284d8ba5e537f358658a74a0
  ac_hash: ce1369467f86b7090e43e4abfac87dc8114b5755b86e8b31e3802ab4f1adc7df
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T04:36:49Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | tests/_lib/collector-absent-stub.ts:16 | The collector-absent stub exports only `recordOpportunity`, while `src/scripts/hooks/dispatch_hook.ts:60-64` imports three names from the aliased module (`isSelfObservation`, `recordCapture`, `recordOpportunity`). Under the alias the two missing bindings resolve to `undefined`, so run B dies with `TypeError: isSelfObservation is not a function`. OBSERVED at this head: `./scripts-run src/scripts/check_static_parity --quiet` prints `scanned: 32`, then `present-but-off: 563 test(s), exit 0` / `absent: 563 test(s), exit 1`, then `one of the two runs is red; parity over a red suite proves nothing`, exit 1. The new `Collector Lifecycle` job's final step (`.github/workflows/tests.yml:590`) is therefore RED on this branch, and AC-7 is flipped `[x]` on the strength of that gate now running in CI. The stub's own docstring claims "leaving one out breaks the alias loudly", which is true — it broke, and the review that flipped AC-7 did not run it. | open | |
| 2 | high | vitest.config.ts:29 | The alias `find` is `/^.*collector_denominator\.js$/`, which rewrites the module for EVERY importer in the run, not just the dispatcher. The stub's stated policy — "only the symbols `dispatch_hook` actually imports are stubbed" — is therefore structurally wrong: `tests/scripts/collector_vocabulary_parity.test.ts:26` is in the parity set (it spells `dispatch_hook`) and imports `isSelfObservation` from the real module directly, so it fails in run B on its own account, independent of finding 1. Completing the stub against the dispatcher's import list alone leaves the gate one new direct import away from red again, with no gate or test guarding the correspondence. | open | |
| 3 | high | src/scripts/hooks/dispatch_hook.ts:1167 | The only production call site is guarded by `if (!isSelfObservation())`, and `isSelfObservation` returns true whenever `VITEST` is set — which vitest always sets, and which `tests/scripts/collector_vocabulary_parity.test.ts:48` asserts is live in-process. So in BOTH parity runs `recordOpportunity` and `recordCapture` are never invoked. `check_static_parity` can only observe module-load differences, never the collector's behaviour on the dispatch path, so the AC-7 comparison is vacuous with respect to the thing it claims to compare. This is the "a gate that scans nothing exits green" shape the gate's own `min_scanned` note in `src/config/gate-coverage.yml` argues against, arriving through the guard instead of through the discovery grep. | open | |
| 4 | medium | src/scripts/_lib/collector_denominator.ts:435 | `nextSequence` writes one `episodes/<episode>.seq` file per episode and NOTHING ever prunes that directory — no daemon step, no retention path, no `uninstall` (grep over `src/` and `tests/` finds `EPISODE_DIR_NAME` only at its declaration and this use). It sits inside the directory `RESOURCE_BUDGETS.disk_bytes` ceilings, and a disk breach STOPS the collector. This is exactly the argument the `disk_bytes` basis makes for `opportunities.log` ("an unbounded file inside a budget whose breach stops the collector is a slow self-shutdown"), left unapplied to the sibling directory added in the same change. `directorySize` in `src/scripts/collector_daemon.ts:152` also re-`stat`s every one of these files on every beat, so the per-beat cost grows without bound too. | open | |
| 5 | medium | src/scripts/_lib/collector_denominator.ts:513 | `spoolRecord` appends to `spool/pending.jsonl` whenever the opt-in marker exists, and the only thing that ever drains or bounds the spool is the daemon. With the collector ENABLED and no daemon running — a supported state, since the daemon is a separate opt-in process and `start()` can refuse for `lock-held` or `store-unavailable` — the spool grows without limit and the disk budget that would notice is enforced only by the process that is not running. There is no age or size cap on the spool anywhere in the change. | open | |
| 6 | medium | src/scripts/collector_daemon.ts:224 | The stated dead-daemon detection property is not what the code produces. `collector_denominator.ts` argues twice that a dead collector yields "a climbing denominator and a flat numerator ... the ratio falls, which is the true statement", but captures are spooled to DISK while the daemon is down and `drainOnce` replays the whole spool on the next start, writing records that still carry their original `occurred_on`. The historical rate is therefore repaired retroactively and the outage becomes invisible in the very window the design says would expose it. The two writers do not in fact fail differently across a restart; they only fail differently while the daemon stays down. | open | |
| 7 | low | src/scripts/_lib/collector_denominator.ts:331 | Two smaller defects in `pruneOpportunitiesOlderThan`. The temp file is a fixed `${target}.tmp`, so two concurrent prunes clobber each other's staging file (the per-user lock narrows this in production, but the function is exported and driven directly by tests, which is the same reason `claimSpool` was given a uniqueness counter). And the tail-carry offset is `Buffer.byteLength(raw, 'utf8')` — a re-encode of the decoded string, not the on-disk length — so a log containing any invalid UTF-8 byte (replaced by U+FFFD on read) yields a wrong offset and the carried tail is misaligned into the rewritten file. | open | |
| 8 | low | src/scripts/collector_daemon.ts:436 | `runLoop` re-derives the heartbeat `mode` from the heartbeat it just read (`beat?.mode ?? 'degraded'`) instead of retaining the `mode` `start()` computed from `detectSupervisor()`. If the heartbeat file is absent for one beat (removed by an operator, a cleanup, or a race), a genuinely `supervised` daemon silently re-labels itself `degraded` for the remainder of its life, and nothing restores it. The probe result is available at start and is discarded. | open | |
| 9 | low | src/scripts/hooks/dispatch_hook.ts:1161 | The cost claim is understated by 2x. The comment says the two calls "cost two `existsSync` calls on a default-off install", but `isCollectorEnabled` performs two probes itself (`isOptedOut` at `collector_store.ts:201`, then the ENABLED marker), and it is called once by `recordOpportunity` and once by `recordCapture` — four `existsSync` per dispatch, not two. `collector_denominator.ts` carries the same two-stat claim, which was already corrected once for undercounting (R2 finding 14) and is still wrong for the new second caller. | open | |
| 10 | low | tests/scripts/collector_supervision.test.ts:508 | The case is named "CONCURRENT fencers of one dead lock produce exactly one winner" but asserts `acquired.length` is between 1 and 2 — the upper bound is tautological, since only two children are spawned, and the lower bound accepts the both-acquired outcome the title denies. The inline comment concedes this ("both acquiring is correct"), so the disclosure is honest, but the test NAME states a property no assertion in the block checks, and a reader scanning names for the one-collector-per-OS-user invariant will read it as covered. | open | |
| 11 | low | src/scripts/_lib/collector_denominator.ts:397 | `UUID_RE` is declared with `const` at line 397 but referenced inside `machineId` at line 383, above its declaration. This is safe only because `machineId` is never called during module evaluation; any future module-level use, or a re-order that pulls a call earlier, hits the temporal dead zone and throws `ReferenceError` inside a function whose whole contract is never to throw. Move the constant above its first use. | open | |
| 12 | low | src/scripts/_lib/capture_rate.ts:139 | `judgeCapture` and `judgeEnablement` have no production caller — grep over `src/` and `tests/` finds them only in `tests/scripts/capture_rate.test.ts`. So does the new `window` parameter of `readOpportunities`, whose R2 round-2 fix is justified by "the two halves of the ratio were computed over different time bases": no code path in the change computes that ratio at all. This is the same "no production caller" class the change itself flags twice (`spoolRecord`, `terminateCollector`), and it is worth naming even though AC-10 is honestly left unchecked, because the instrument-proving argument in the module docstring reads as if a consumer exists. | open | |
