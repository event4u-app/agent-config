# Findings: drain-supervised-telemetry
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 13e2ce514c46c81be8131a797725af40db9120a4b0da85c324750bb3b069f96c | diff: bace3f6dad3e2fda0b558073c7161fac0a7bf3ad | reviewer: r2-fresh-subagent-drain-supervised-telemetry | prompt_hash: 69d0decca365f7583042c19d12258189509211291d95504f5098bdae029f145f -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-supervised-telemetry"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: bace3f6dad3e2fda0b558073c7161fac0a7bf3ad
  scope_hash: 13e2ce514c46c81be8131a797725af40db9120a4b0da85c324750bb3b069f96c
  roadmap: agents/roadmaps/later/road-to-supervised-telemetry-collector.md
  roadmap_hash: e746ebf4eb26cd4c08d2acc4bd3b9dbdc4093eddcf4fd9b788e50240a60787ad
  ac_hash: 3556dc03413e579d1f295a1a0d44209e3230d4bdf0d547f276123eacded86cde
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T02:44:33Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | .github/workflows/tests.yml:530 | The lifecycle evidence artifact is write-only in CI, so the gate it exists for can never be satisfied. `run_lifecycle_suite` writes `internal/reports/supervision-lifecycle.json` inside the new `collector-lifecycle` job in `tests.yml` (macos-latest); `check_supervision_claim_atomicity`, its only reader, runs in a different workflow (`rule-backstops.yml:266`). GitHub jobs and workflows share no filesystem, there is no `actions/upload-artifact` or `download-artifact` anywhere in `.github/workflows/` for this path, and the file is gitignored (`.gitignore:364`). The producing job merely `cat`s it and discards it — and that step has no `if: always()`, so it is skipped on the red runs where the output would matter most. The gate therefore always takes its `no lifecycle evidence at internal/reports/supervision-lifecycle.json` branch, and the AC-8/AC-9 evidence chain the roadmap describes as end-to-end is broken at the workflow boundary. | open | |
| 2 | high | src/scripts/_lib/collector_supervision.ts:339 | Stale-lock fencing is a compare-then-write race that can hand the same lock to two starters, breaking the one-collector-per-OS-user invariant (Q5). The `wx` write is atomic only while a live incumbent holds the lock; in the catch path the code reads the pid, probes liveness, and then does a plain `fs.writeFileSync` with no exclusivity, so two processes that both observe a dead incumbent both overwrite and both return `acquired: true`. The same path is taken when `readLockPid` returns `null` because a releasing process removed the file between the failed `wx` and the read (`Number.parseInt('')` is NaN, so `Number.isFinite` is false). This directly contradicts the function docstring at :311-319 — "two processes racing produce exactly one winner without a compare-then-write window". No test covers it: `collector_supervision.test.ts:305` contends against a *live* holder, and lifecycle PROPERTY 3 fences strictly sequentially. | open | |
| 3 | high | docs/contracts/collector-operations.md:33 | The operator-facing kill-switch page is now wrong about the change that shipped with it, and the second half of the switch has no reachable entry point. The page states "The operator-facing verb for that second half does not exist yet... The collector is Phase 4 of road-to-supervised-telemetry-collector; until it lands there is no process to end" and promises "The verb ships with the collector, in the same change, and this paragraph is replaced by it." Phase 4 landed in this diff (`src/scripts/collector_daemon.ts`, `collector_daemon run` spawns a real supervised process) and the paragraph was not replaced. `terminateCollector` has no production caller anywhere in `src/` — repo-wide grep finds it only in `collector_supervision.ts` and `collector_supervision.test.ts` — and `collector_daemon.main` accepts only `status` and `run`, no `stop`. An operator following this page under pressure is told there is nothing running and is left with `kill` by hand. | open | |
| 4 | medium | src/scripts/check_static_parity.ts:142 | The present-but-off run inherits the ambient environment and never *unsets* `AGENT_CONFIG_COLLECTOR_ABSENT`, so an ambient `AGENT_CONFIG_COLLECTOR_ABSENT=1` makes both runs resolve the stub and the comparison passes by construction while proving nothing. `runSuite` builds `env = { ...process.env }` and only ever *adds* the variable for run B. The gate's whole claim (module docstring: "Run A is the real module with no opt-in marker — present, and off") is unpinned in the same way on the other axis: neither run overrides `HOME`, so whether run A is really "off" depends on the invoking user's `~/.event4u/agent-config/agent-collector/ENABLED` rather than on anything the gate controls. | open | |
| 5 | medium | src/scripts/check_static_parity.ts:166 | The AC-7 gate is invoked by nothing. `check_static_parity` appears in no workflow under `.github/workflows/`, in no `Taskfile.yml` target, in no `package.json` script, and in neither list of `src/config/ci-local-parity.yml` — a repo-wide grep finds only its own source, its unit test, `vitest.config.ts` prose and a roadmap line. The AC-7 evidence recorded at roadmap line 762 ("551 test(s), exit 0... per-test verdicts are identical") is a single hand-run whose result cannot be reproduced or regression-detected by CI, which is the shape the roadmap's own step 5.2 argues against for the sibling suite. | open | |
| 6 | medium | tests/scripts/collector_daemon.test.ts:132 | The default-off process-enumeration assertion is coupled to an unrelated test file that deliberately spawns daemons, with no isolation. `enumerateDaemonProcesses` (:86) accepts any `ps -eo pid,args` line containing the substring `collector_daemon` and excludes only lines containing `collector_daemon.test`; `tests/scripts/collector_lifecycle.test.ts` spawns `tsx <repo>/src/scripts/collector_daemon.ts run --root <tmp>`, whose argv matches. Both files run in the ordinary vitest chain (`ci-local-parity.yml:127` states this deliberately) and `vitest.config.ts` sets no `fileParallelism: false` and no `poolOptions.singleFork`, so vitest may execute them concurrently in one shard and `toEqual([])` reds on a test-owned daemon. The distinguishing token that would resolve it — `DAEMON_PROCESS_MARKER` — exists and is unused. | open | |
| 7 | medium | src/scripts/collector_daemon.ts:77 | `DAEMON_PROCESS_MARKER` is a dead constant whose docstring asserts two facts that are both false. It claims the literal "appears in the daemon's own argv and nowhere else in this package" and that "Process enumeration in step 4.1's test greps for it". It never reaches argv — its only use is the usage string printed on an unrecognised command at :471 — and no test references it: a repo-wide grep over `src`, `tests`, `docs` and `agents/roadmaps` returns exactly two hits, both inside this file. The test that step 4.1 points at greps the broad substring `collector_daemon` instead, which is the coupling in the row above. | open | |
| 8 | medium | tests/scripts/collector_lifecycle.test.ts:121 | Every internal deadline in the process-level suite is unreachable because it exceeds the global per-test timeout, so the intended waiting behaviour degrades into a hard failure on a slow runner. `spawnDaemon` waits up to 30 s for readiness (:121), `signal` up to 20 s (:144), `waitFor` defaults to 15 s (:162) and PROPERTY 5 passes 30 s and 20 s explicitly (:275, :284) — while `vitest.config.ts:63` sets `testTimeout: 10_000` and no `it`/`describe` in the file supplies a timeout override. Each property spawns one or two `tsx` daemons (cold start seconds each) on `macos-latest`, the slowest runner in the matrix and the only platform the new `collector-lifecycle` job certifies, so the margin under 10 s is thin and any overshoot fails the job that AC-8 depends on. | open | |
| 9 | medium | src/scripts/_lib/collector_supervision.ts:432 | `terminateCollector` signals the heartbeat's recorded pid with no staleness check, so the kill switch can SIGTERM and then SIGKILL an unrelated process owned by the same user. The module already owns the vocabulary for this — `livenessFromBeat` and `HEARTBEAT_STALE_AFTER_MS` — and uses neither here; `readHeartbeat` is called at :439 and its `last_heartbeat` and `started_at` are ignored. Lifecycle PROPERTY 3 establishes that a SIGKILLed daemon leaves its heartbeat file on disk indefinitely, and `pidIsAlive` (:302) cannot distinguish the original daemon from a recycled pid, so the documented recovery path after a reboot or a long-lived stale heartbeat is a signal aimed at whatever now owns that pid. | open | |
| 10 | medium | src/scripts/_lib/collector_supervision.ts:354 | `releaseRuntimeLock` deletes the lock with no ownership check while its docstring claims it is "safe to call when someone else fenced us" — it is not. It `rmSync`s `runtimeLockPath` unconditionally, and `collector_daemon.stop` (:317) pairs it with an unconditional `rmSync` of the heartbeat, both reached from `runLoop`'s `finally`. A predecessor that was fenced by the race in the row above, or any process whose lock was taken over, therefore removes the *successor's* lock file and heartbeat on its way out, leaving a live collector with no lock (so the next starter also acquires) and no heartbeat (so `livenessFromBeat` reports `absent` for a running process — the silently-dead-collector reading the design calls decisive, inverted). | open | |
| 11 | medium | src/scripts/_lib/collector_denominator.ts:151 | The denominator's event and platform vocabularies are duplicated literals with no parity test against the dispatcher's, and drift silently shrinks the denominator — biasing the capture rate upward, the one direction that makes the 90 percent target look met. `recordOpportunity` refuses any event not in `COLLECTOR_EVENTS` (:151) and any platform not in `COLLECTOR_PLATFORMS` (:153), both declared in `collector_record.ts:41` and `:67`; `dispatch_hook.ts:101` declares `EVENT_VOCABULARY` as an independent ten-element literal, and the dispatcher applies no vocabulary check to `--platform` at all. The two lists agree today and nothing in the tree asserts that they must, so adding an event or a host to the dispatcher drops those dispatches from the denominator without any refusal being visible — `readOpportunities` counts only lines that were written. | open | |
| 12 | medium | agents/roadmaps/later/road-to-supervised-telemetry-collector.md:1302 | The acceptance-criteria block contradicts the rest of its own file, so an unflipped AC cannot be told from a genuinely open one. AC-6's rationale still reads "The second and third clauses are 3.2 and 3.3 and are OPEN: both need the collector, which is Phase 4", while the same file marks 3.2 (:568), 3.3 (:599) and all of Phase 4 `[x]`, ships `collector_supervision.test.ts` asserting budgets-with-headroom and killing a real wedged process, and opens with "PARKED 2026-08-30 — Phases 1 to 5 complete and verified". AC-7, AC-8 and AC-9 are left `[ ]` with no rationale line at all, although 4.2, 5.1 and 5.2 are all `[x]`. This is the file's own § 4 silent-green discipline running in reverse: the record now understates rather than overstates, and either way it is not checkable. | open | |
| 13 | medium | docs/contracts/ci-cost-budget.md:43 | A PR-blocking CI job was added without the budget row the contract requires of every one. The page's scope line is "Measured durations + trigger surfaces for every PR-blocking CI job in `.github/workflows/`" and :43 states "`tests.yml` declares 6 job keys that matrix-expand to 23 jobs"; after this diff `tests.yml` declares 7 (`install-tests`, `install-aux-tests`, `node-tests`, `static-checks`, `golden-tests`, `collector-lifecycle`, `workspace-tests`). The new job runs on `macos-latest`, does its own `npm ci`, and spawns real daemons, so it is not free, and it carries neither a measured duration nor the written cost justification the 5-minute ceiling clause asks for. | open | |
| 14 | low | src/scripts/hooks/dispatch_hook.ts:1156 | The dispatch-path cost claim is wrong in both its number and its mechanism. The comment reads "Costs ONE `stat` on a default-off install: `recordOpportunity` returns immediately when the opt-in marker is absent", but `isCollectorEnabled` (`collector_denominator.ts:97`) first calls `isOptedOut`, itself an `fs.existsSync`, and only then stats the ENABLED marker — two `existsSync` calls per dispatch on a default-off install, and the opt-out probe rather than the marker is what usually returns first. `collector_denominator.ts:139` repeats the same "one `stat` and a return" claim. | open | |
| 15 | low | src/scripts/_lib/capture_rate.ts:141 | `judgeCapture` handles one class of impossible input as a verdict and the neighbouring class as an uncaught crash. A numerator above the denominator is reported as `numerator-exceeds-denominator` with `meetsTarget: null`, but a negative or non-integer count falls straight through to `wilsonInterval` (:141), which throws `RangeError` or `TypeError` — the very cases `capture_rate.test.ts:134` proves the low-level function raises. A malformed reading arriving from the field therefore aborts the judge rather than being reported ineligible, which is the outcome the module's own three-valued design exists to avoid. | open | |
| 16 | low | src/scripts/collector_daemon.ts:425 | `--root` parsed as the final argv element silently degrades to the real user root instead of erroring. `const userRoot = rootFlag >= 0 ? argv[rootFlag + 1] : undefined` yields `undefined` for a trailing `--root`, and `undefined` is exactly the value that makes `resolveCollectorStore` fall back to `~/.event4u/agent-config`. The flag's own docstring says it exists so the lifecycle suite "cannot be allowed to touch the developer's own `~/.event4u/agent-config`", so the failure mode of a malformed invocation is precisely the thing the flag was added to prevent. | open | |
| 17 | low | src/scripts/_lib/collector_supervision.ts:598 | The darwin branch of `detectSupervisor` assumes rather than probes, contradicting the section heading "Supervisor detection — probed, never assumed" and the module docstring's Q4 answer ("A per-user service manager, positively probed, never assumed"). It returns `tier: 'supported'` unconditionally, never consults the injected `exists` predicate, and computes the `LaunchAgents` path only to interpolate it into the reason string; `collector_supervision.test.ts:402` pins that behaviour with `detectSupervisor({ platform: 'darwin', env: {} })`. The linux branch below it does probe, so the one row the new CI job actually verifies is the assumed one. | open | |
| 18 | low | src/scripts/run_lifecycle_suite.ts:156 | The evidence artifact is written before the red-suite check, so a failing run can still leave a pass-shaped artifact on disk. `fs.writeFileSync(target, ...)` at :156 precedes the `result.status !== 0` branch at :159, and `processes_exercised` depends only on the five named properties passing — a run in which all five pass and a sixth case fails writes `processes_exercised: true, cases_skipped: 0` and then exits 1. The module docstring argues that a missing artifact and a skipped suite "need different remediation and the gate distinguishes them"; a red suite is a third state the artifact does not record. | open | |
