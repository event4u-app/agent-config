# Findings: drain-supervised-telemetry
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 1e11c93435b4fc9bc1848bffaf73314c72f3c61e5f1cb6b9b9e169aa954c3a71 | diff: b4a3c67c66749cc909b3a1451ba115e4e2c7b41d | reviewer: r2-fresh-subagent-drain-supervised-telemetry | prompt_hash: ea1eed57278ab19890e758e4ac79f9ff3f1b011c889678a0b1029fc3d741969d -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-supervised-telemetry"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: b4a3c67c66749cc909b3a1451ba115e4e2c7b41d
  scope_hash: 1e11c93435b4fc9bc1848bffaf73314c72f3c61e5f1cb6b9b9e169aa954c3a71
  roadmap: agents/roadmaps/later/road-to-supervised-telemetry-collector.md
  roadmap_hash: 122e9bd13852e50108de2eacce2d6e5a43f86c93284d8ba5e537f358658a74a0
  ac_hash: ce1369467f86b7090e43e4abfac87dc8114b5755b86e8b31e3802ab4f1adc7df
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T05:28:28Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/dispatch_hook.ts:1176-1177 | The kill switch does not stop hook-side capture. `recordOpportunity` and `recordCapture` are gated only on `isSelfObservation()` and `isCollectorEnabled()` (opt-out marker + ENABLED marker); neither they nor any other code on the dispatch path consults `killSwitchEngaged`. With `STOP` present the hook keeps appending to `opportunities.log` and keeps writing records into `spool/pending.jsonl`, and because `drainOnce` replays the whole spool with each record's ORIGINAL `occurred_on`, the window an operator believed was stopped is captured retroactively once `STOP` is removed. This contradicts two shipped statements: `docs/contracts/collector-operations.md` § Stop it now ("Every dispatch resolves in **static mode** … Nothing is captured") and `collector_supervision.ts:642` `DISPATCH_MODE_CONTRACT` / `resolveDispatchMode` ("there is no collector process and nothing is captured. It is not a second, quieter writer"). The switch stops the daemon; it does not stop collection. | open |  |
| 2 | high | src/scripts/_lib/collector_denominator.ts:353-368 | `pruneOpportunitiesOlderThan` still loses concurrently appended denominator lines over the whole filter window, and the comment above it asserts the opposite. `raw` is read at :321, the cutoff and the `kept` filter run at :325-337, and only then is `consumed = fs.statSync(target).size` taken at :357. Any line another hook process appends between the `readFileSync` and that `statSync` is inside `consumed` (so excluded from the carried `tail`) and absent from `raw` (so excluded from `kept`) — it is silently dropped. The comment claims "the window is now microseconds rather than the whole filter"; the window IS the whole filter. Direction is the dangerous one the same module names three functions earlier: an understated denominator biases the capture rate UPWARD, toward the 90 % target. A single `const buf = fs.readFileSync(target); const consumed = buf.length;` removes the race and also the U+FFFD problem R2 round-4 finding 7 was about (whose fallback at :359 still uses the rejected `Buffer.byteLength(raw)`). | open |  |
| 3 | medium | src/scripts/_lib/collector_supervision.ts:745 · src/scripts/collector_daemon.ts:341 | `mode: 'supervised'` in the heartbeat is asserted from a directory that exists on every macOS account, and nothing in the tree installs a supervisor. The darwin branch returns `tier: 'supported'` when EITHER `exists(agents)` OR `exists(path.join(home, 'Library'))` is true, and `~/Library` is universal on macOS — so the "PROBED, not assumed (R2 finding 17)" fix is still an assumption on the row it was written for. `start()` then maps `tier === 'supported'` to `mode: 'supervised'`, and no launchd plist or systemd unit is generated or installed anywhere in `src/` (grep for `LaunchAgents`/`launchctl`/`systemctl --user` matches only this module). A hand-started daemon with no supervisor at all therefore writes `supervised` into the operator-facing heartbeat, while `collector_daemon.ts:35` states the converse guarantee ("it is never labelled supervised" when none is detected). | open |  |
| 4 | medium | src/scripts/collector_daemon.ts:345-348 | `start()` collapses every non-acquired `LockOutcome` into `refusal: 'lock-held'`, discarding the `reason` field R2 round-2 finding 11 added for exactly this. `acquireRuntimeLock` distinguishes `held-by-live-process`, `kill-switch-engaged`, `unwritable` (with `errno`) and `contested-out`; `StartRefusal` has no member for the last three, so an `EACCES`, an `ENOSPC` or a lost fence race is reported to the operator as `collector: not started — lock-held` and routes them to the "Two collectors" row of `collector-operations.md`. That is the misclassification the reasons were introduced to prevent, reintroduced one layer up. | open |  |
| 5 | medium | src/scripts/_lib/collector_supervision.ts:276-279 · src/scripts/collector_daemon.ts:600 · tests/scripts/collector_lifecycle.test.ts:266-283 | `livenessFromBeat` is purely time-based and never consults `pidIsAlive`, which the same module already defines. For up to `HEARTBEAT_STALE_AFTER_MS` (90 s) after any unclean death — including a `terminateCollector` escalation to `SIGKILL`, which cannot run the `finally` that removes the beat — `collector_daemon status` prints `running (pid N, …)` for a process that is gone, and `collector-operations.md`'s state table defines `running` as "alive". This is the silently-dead-collector reading the three-valued design is claimed to prevent. PROPERTY 4 does not catch it because it evaluates the predicate at a fabricated clock (`beat.last_heartbeat + HEARTBEAT_STALE_AFTER_MS + 1`) rather than at the real one; at the real `now`, immediately after its own `SIGKILL`, the assertion would read `running`. The test proves the staleness arithmetic, not the property it is named for. | open |  |
| 6 | medium | .github/workflows/tests.yml:555-570 | The "Consume the evidence in the SAME job" step never opens the evidence artifact, so the producer→reader link it claims to close is not exercised. `check_supervision_claim_atomicity` calls `readEvidence()` only from inside `scan()` and only after `assertsSupervision(line)` matches on one of the eight `PUBLIC_SURFACES`; none of them carries a present-tense supervision claim (the gate's own docstring and AC-9 both say so), so the evidence branch is unreachable and the step passes identically whether `internal/reports/supervision-lifecycle.json` is correct, stale, malformed, or absent. The only thing actually gating the artifact's content in CI is `run_lifecycle_suite`'s own exit code in the preceding step. The step comment's claim — "Running the reader in the producing job is what closes it" — overstates what was wired, in the same way the roadmap's § 4 silent-green discipline is written to catch. | open |  |
| 7 | medium | src/scripts/_lib/capture_rate.ts:59-62, 156-180 | The eligibility rule drops one of the three sample constraints the pre-registered metric definition fixes. Roadmap 1.2 item 7 requires "≥ 2,000 eligible dispatches across ≥ 5 distinct machines, **with no single machine contributing more than 40 % of the denominator**", and gives the reason ("a rate measured on one machine measures that machine's supervisor, not the population's"). `judgeCapture` implements `MINIMUM_SAMPLE` and `MINIMUM_MACHINES` and nothing else; `EligibilityFailure` has no member for the concentration cap and no constant for 40 % exists anywhere in `src/` or `tests/`. A reading of 2,000 dispatches across 5 machines with 95 % from one of them is judged `eligible` and can set `meetsTarget: true`. The module docstring says it "computes that, and nothing else" and honestly names its no-production-caller state, but does not name this omission (nor item 5's separate `startup_failure` count, also absent). | open |  |
| 8 | medium | src/scripts/_lib/capture_rate.ts:106-107 | `CaptureReading.machines` is documented as "Distinct machines contributing to the **denominator**", but the denominator artifact cannot supply it: `recordOpportunity` writes `date\tevent\tplatform` (`collector_denominator.ts:209`) with no machine identifier, and `readOpportunities` returns no machine dimension. The only place `machine_id` exists is the store — the numerator — so the ≥ 5-machine eligibility floor would have to be counted on the wrong side of the ratio, where a machine that captured nothing contributes zero and the floor is biased in the same upward direction the module warns about. Nothing in the change names this gap; it blocks the 6.1 reading rather than merely awaiting it. | open |  |
| 9 | low | src/scripts/_lib/collector_supervision.ts:642, 666 | `DISPATCH_MODE_CONTRACT` and `resolveDispatchMode` have no production caller — the only references outside their own module are in `tests/scripts/collector_supervision.test.ts`. `resolveDispatchMode`'s docstring presents it as "the reporting view" a CLI would use, but `collector_daemon main`'s `status` verb prints enabled/liveness and the heartbeat's supervisor mode, never the dispatch mode. This is the same no-caller class the change fixed twice (`spoolRecord`, `terminateCollector`) and named honestly for `capture_rate.ts`; here it is unnamed. | open |  |
| 10 | low | src/scripts/run_lifecycle_suite.ts:117-121 | `run_lifecycle_suite` spawns vitest with an inherited environment and does not `delete env.AGENT_CONFIG_COLLECTOR_ABSENT`, which is the guard R2 finding 4 added to the sibling spawner in `check_static_parity.runSuite`. An ambient value stubs `collector_denominator` away for the lifecycle run, making `enableCollector(userRoot)` a no-op so every daemon refuses with `not-enabled`. It fails loudly rather than silently, but the asymmetry between the two spawners is the exact shape the earlier finding was about. | open |  |
| 11 | low | src/scripts/_lib/collector_denominator.ts:381 · :660 | `pruneSequence` is used at :381 and declared with `let` at :660, ~280 lines below — in the module that moved `UUID_RE` above its first use at :420 specifically because "a TDZ ReferenceError inside a function whose whole contract is never to throw is a defect waiting for a re-order" (R2 round-4 finding 11). Both are safe as written (module evaluation completes before any exported function runs), which makes this a consistency defect rather than a live one — but the stated rule is applied to one binding and not to the other added in the same change. | open |  |
| 12 | low | src/config/gate-coverage.yml:1157 | The `check_static_parity` corpus note records "(31 at the time of writing)" while the live discovery on this branch returns 32 files (`grep -rl --include='*.test.ts' dispatch_hook tests src`) — `check_static_parity.test.ts` and `collector_vocabulary_parity.test.ts` are both new and both in the set. The same change's `tests.yml` comment says the count is "deliberately not written here … because it moves with the suite and three different figures for it appeared in one diff"; a fourth figure was then written into this file. | open |  |
