# Findings: gate-autonomy-class-writeback
<!-- completion-review: v1 | reviewed: 2026-08-17 | scope: 3c9bfc9f657ba566211d63aab65c1eb8c5d9043747cbc5169c744c467b50ad6f | diff: 585a1c1abc6ad378721b3f69ce92b007073cbe1e | reviewer: r2-fresh-subagent-gate-autonomy-class-writeback | prompt_hash: 287ce9889f5371bdc4a38f80cfd650593bc7eea15f765341b884501513ea8beb -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-17 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 585a1c1abc6ad378721b3f69ce92b007073cbe1e
  scope_hash: 3c9bfc9f657ba566211d63aab65c1eb8c5d9043747cbc5169c744c467b50ad6f
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-17T16:59:35Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | `agents/roadmaps/road-to-ui-track-integrity-followup.md:241`, `:295` | `Class:` was inserted between a wrapped `Owner:` value and its continuation line, so `_blockerField` truncated both owner values mid-phrase and swallowed the orphaned continuation into `Class:`. Corruption reached `gates --json --all` and the committed dashboard. No gate catches it: `blocker_class` survives the leading-token split and `needsUser` is unaffected. | fixed | Real defect, caused by this branch. The write-back script assumed `Owner:` is single-line. Both sites repaired by moving `Class:` below the continuation; owner strings verified restored in full via `gates --json`. **Sibling search:** the construct is a `Class:` bullet followed by an indented non-bullet continuation — 36 `Class:` fields scanned across `agents/roadmaps/`, plus 0 in `later/`; **0 remaining hits**. |
| 2 | medium | `src/agent-src/scripts/roadmap_gates.ts:430` | `run` emitted the raw authored value with its markdown backticks while the adjacent `class` was normalised, and `commandOf()` already strips them for the sibling surface — two representations of one command, undocumented. | fixed | Resolved by removing `run` rather than by picking a representation: with no consumer (finding 7) there is nothing to say which form is correct. The test that pinned the backticked form is replaced by one pinning the absence. |
| 3 | medium | `agents/roadmaps/road-to-gate-autonomy.md:199`, `:162`; `agents/evidence/analysis/gate-class-sweep-2026-08-17.md:192` | "34 of the 49 open blockers now carry `Class:`" and "15 resolving through the absent-field default" assert tree state but describe what the step *wrote*. The tree carries **36** authored fields (this roadmap's own two predate the branch), so defaults are **13**, not 15. | fixed | Correct, and load-bearing for the 0-of-49 conclusion. Both artefacts now state 36 authored / 13 default and name the conflation. Also corrected downstream: the live count is 50, not 49, because finding 4's fix adds a blocker outside the swept population. |
| 4 | medium | `agents/evidence/analysis/gate-class-sweep-2026-08-17.md:52-65`; `agents/roadmaps/road-to-gate-autonomy.md:182-186` | Three items declared "surfaced as a decision instead" landed in no blocker, step or decision sheet, so `agent-config gates` and the dashboard would never render them and nothing would count them. Prose in an analysis file is a note, not a discharge. | fixed | Correct per `active-remediation`. Added `### blocker: b-estate-prose-pass-from-1-3` (owner user, class 2) carrying all three as enumerated options with a recommendation, an if-you-do-nothing cost and a decidable resolution. It renders in `gates` and survives the turn if the decision is deferred. The reviewer's independent re-verification of the `--budget` defect is recorded in the entry. |
| 5 | low | `agents/roadmaps/road-to-gate-autonomy.md:157-160`, `:188-195` | The step's `verify:` clause was rewritten in the same commit that ticks the step `[x]` — from "lint is green" to "reports no new violation" — so the acceptance originally authored was never met, and its replacement was written by the party it judges. | no_change_needed | Accepted as recorded rather than fixed, and the finding is right to name the pattern. The disclosure is the mitigation this repo asks for: the replacement claim is checkable and was measured twice (28 before, 28 after), and step 1.1 had already recorded the pristine-tree red two steps earlier. Rewriting it silently would have been the violation; leaving an unmeetable clause would have blocked the step on two entries in other people's files. |
| 6 | low | `tests/scripts/roadmap_gates.test.ts:253-262` | The test comment claimed the case makes the synthesised `legacy` note read as class 3, but the test hand-builds an `Entry` and never exercises `parse_blockers`' legacy branch — nothing bound the synthesis path to the default. | fixed | Correct: the comment asserted coverage the test did not provide. The comment now scopes itself to the renderer half, and a new test drives `parse_blockers` over a `> Blocked until …` note and asserts the synthesised entry carries an empty class that `blocker_class` resolves to 3. |
| 7 | low | `src/agent-src/scripts/roadmap_gates.ts:423-431` | The new `run` field had no reader and no data — `run: ""` on all records, `--execute` reads the entry via `locate()`, and nothing references the JSON shape. The comment justified it by describing a consumer that does not exist. | fixed | Correct, and the honest fix is removal rather than a better comment: it was a speculative addition in a branch whose subject is claims matching reality. Removing it also dissolves finding 2. The code now records why it shipped for one commit and why it was withdrawn. |
