# Findings: feat-scheduled-deprecation
<!-- completion-review: v1 | reviewed: 2026-08-15 | scope: e6106fa9024b5152a39496e5e34612c7f06d7a5acca4606b991b0c43231dfc89 | diff: f92bc375fb948fe92f1fa50691d6eb0605236128 | reviewer: r2-fresh-subagent-feat-scheduled-deprecation | prompt_hash: 70040d6b8771b9f7f97385e08e951668eb144ce2315c282a87aeb2eca5751106 -->

<!-- context-manifest: v1
inputs:
  diff_sha: f92bc375fb948fe92f1fa50691d6eb0605236128
  scope_hash: e6106fa9024b5152a39496e5e34612c7f06d7a5acca4606b991b0c43231dfc89
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md
  roadmap_hash: c6f3f486d1766931a488322e69e701f4434796414e2876b3bc2f617e556e82f4
  ac_hash: 4b53e297a51e10e2f762c99a14f20ece1394f927d02f3438e29d6015c03bf229
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-15T13:56:31Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/release.ts:2646 | The new guard sits above the `if (!args.dry_run)` block (2648) and the first dry-run short-circuit is at 2701, so `release --dry-run` can now `die()` with exit 2. `tests/scripts/release.test.ts` still carries the opposite contract: its header (12-14) states "--dry-run returns 0 BEFORE execute() and BEFORE preflight()", and two tests assert exit 0 unconditionally (`--ci --dry-run` at 638-645, era-gate dry-run at 674-679). Neither was updated. Since `code_graph` is overdue today, both go red the first time the auto-detected bump resolves to a major — the exact event this gate exists for. The refusal is intended; the un-migrated downstream contract is not. | fixed | `previewOnly` mode — the refusal is printed in full, the `--dry-run exits 0` contract untouched (f92bc375f) |
| 2 | medium | src/scripts/lint_scheduled_deprecations.ts:242 | The `Finding.overdueBy` contract says zero is "a refusal at a cut and silent on an ordinary branch". `evaluate()` pushes a finding on `overdueBy >= 0` (284) and the branch-mode loop (570) prints every overdue finding, so a row due at the shipped major emits a standing warning on every branch and CI run. Neither the unit fixtures nor the seven self-test cases cover `due == shipped` (both "due AT" cases use due 13 against shipped 12), so code and stated contract disagree with nothing checking. | fixed | branch/cut threshold split; two self-test cases now cover `due == shipped` in both directions (f92bc375f) |
| 3 | medium | src/config/gate-coverage.yml:775 | `argv: []` is not the CI-identical invocation. `taskfiles/ci-fast.yml:1226` calls the gate with `{{.QUIET_FLAG}}`, which `Taskfile.yml:12-13` resolves to `--quiet` unless AGENT_SCRIPT_VERBOSITY=verbose, and the `consistency.yml` step goes through the same task. Sibling rows follow the convention (`lint_settings_classes` → `["--quiet"]`), and this file's own `check_cli_registry_budget_sync` note defines `argv: []` as "both callers call it bare". It passes today only because `reportScanned` happens not to be quiet-gated, unlike the success line and `ledger.report()` in the same function. | fixed | `argv: ["--quiet"]`, with the coincidence that hid it recorded in the row note (f92bc375f) |
| 4 | medium | tests/scripts/release.test.ts:925 | "a resumed major is still checked" is identical in input and assertion to "a due or overdue row refuses the cut" at 917 — same `('13.0.0', stub(1, calls))`, same throw, same calls length. `assert_scheduled_deprecations_clear` never reads the current version, so no input in the test distinguishes resume from any other major cut. The comment claims it pins the `target === current` path; the test cannot fail unless 917 fails too, so that path is unpinned. | fixed | duplicate replaced by asserting the property that makes resume safe — the guard never reads the current version (f92bc375f) |
| 5 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md:147 | The execution note claims, explicitly "Verified rather than assumed", that the regenerated dashboard corrects `-c-release-head-truth` from 10 open / 0 done to 1 open / 9 done. `agents/roadmaps-progress.md` contains no roadmap matching "release-head" or "head-truth" on either side; the diff changes exactly one open-roadmaps row (this roadmap's); and Overall moves 246 → 256, precisely this roadmap's 10 steps, over an unchanged 485 denominator. The recorded delta is absent from the change that records it. | fixed | note rewritten: it was true when written and the merge archived that roadmap outright, so the delta is genuinely absent now (f92bc375f) |
| 6 | low | src/scripts/lint_scheduled_deprecations.ts:180 | The doc says `parseRows` "Reads only the first table under the section heading", but the loop consumes every pipe-prefixed line until the next `##` and only the first becomes the header. This change adds a `### Row status` subsection inside that span (docs/MIGRATION.md:23), which `/^##\s/` does not close, so any table added there is appended as data rows with its header line parsed as a row. | fixed | any heading closes the parse span, so `### Row status` cannot swallow a future table (f92bc375f) |
| 7 | low | src/scripts/lint_scheduled_deprecations.ts:325 | `shippedMajor` runs outside the try/catch in `main`, which catches only `HeaderMismatchError`, and throws a plain Error on a missing or unparsable `package.json`. `repoRoot()` anchors on docs/MIGRATION.md alone, so a root without package.json is reachable. The documented EXIT CONTRACT lists 0/1/2; this path exits with an unlisted code and a stack trace rather than the gate's own message. | fixed | a missing/malformed package.json exits 2 with the gate's own message (f92bc375f) |
| 8 | low | src/scripts/lint_scheduled_deprecations.ts:570 | `--quiet` gates the success line, `ledger.report()` and the "Reported, not refused" note, but not the overdue block, which writes to stdout unconditionally. CI runs this gate quiet by default, so the one output the flag would most plausibly bound — the standing warning the task description itself flags as unclearable — is the one it does not bound. | accepted-risk | deliberate: CI passes `--quiet`, so quiet-gating the overdue block would hide the one finding remote CI exists to surface. The success line and ledger report are noise at scale; a live missed commitment is not. |
| 9 | low | src/scripts/lint_scheduled_deprecations.ts:519 | The `--cutting` value is shape-checked only after `MIGRATION.md` is located, read and fully parsed and after `shippedMajor` reads package.json. A pure usage error (exit 2) is therefore reported behind all the file work it can never need, and a `HeaderMismatchError` on the way masks it with exit 1 instead. | fixed | `--cutting` shape-checked before any file work, so a usage error can no longer be masked by a parse failure (f92bc375f) |
| 10 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md:114 | The note records that 1.2's verify hook "cannot be satisfied as written" because `grep -c 'lint_scheduled_deprecations' Taskfile.yml` returns 0 for a hyphenated task — but the same change adds a comment carrying that exact literal at Taskfile.yml:169-170, so the hook now returns non-zero. The recorded departure describes a constraint this diff removed. | fixed | note corrected — the hook does pass, by coincidence of a comment carrying the literal, and says so (f92bc375f) |
| 11 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md:157 | The acceptance criterion "telegraph is either scheduled or documented as a keep" is ticked while the execution note states the row is deliberately neither, taking a third not-pinned state. The departure is argued openly for step 2.2, but the criterion text was left unchanged, so the checked box asserts an outcome the implementation explicitly declined to produce. | fixed | acceptance criterion reworded to the state that shipped rather than ticking a box for a declined outcome (f92bc375f) |
| 12 | low | src/scripts/lint_scheduled_deprecations.ts:284 | Branch mode compares against the shipped major, so a row due at the NEXT major stays silent on every branch until the cut refuses it. docs/release-runbook.md now tells the releaser the removal column "is checked for you"; for an on-time row the first and only signal is the refused release, with zero lead time — the reporting half of the asymmetry only ever fires on rows that are already late. | accepted-risk | the consequence of finding 2's own fix, and the lesser evil: warning on a row due at the shipped major produces a standing notice for something nobody is late on, for a whole release cycle. Lead time for an on-time row is the release preview (`release --dry-run` now reports the refusal without exiting non-zero) rather than a branch warning. |
