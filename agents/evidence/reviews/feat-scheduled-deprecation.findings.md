# Findings: feat-scheduled-deprecation
<!-- completion-review: v1 | reviewed: 2026-08-15 | scope: 88e46ff2918db4a5e45c99ef0dc6e2feddcb724f8e5c6d145e0313f9fcd77ba4 | diff: cdae71b306da55b6dbb1aa6122c5f228eee98a6a | reviewer: r2-fresh-subagent-feat-scheduled-deprecation | prompt_hash: 1df58af5136e6043c066ddad03792fe414340203810b7100c7cc018a25311de5 -->

<!-- context-manifest: v1
inputs:
  diff_sha: cdae71b306da55b6dbb1aa6122c5f228eee98a6a
  scope_hash: 88e46ff2918db4a5e45c99ef0dc6e2feddcb724f8e5c6d145e0313f9fcd77ba4
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md
  roadmap_hash: 78cb2953c5687fdaffb57d619fb6358bd9f0c5f5a7eddcfbbcf72765c332a08a
  ac_hash: 674a5a52b44626cce87124be57d3828727fd8d345d9a569b73dc1591bfcbb74c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-15T13:21:20Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/lint_scheduled_deprecations.ts:221 | The overdue test compares the due major against the **shipped** major (`overdueBy = currentMajor - removal.major`), never against the major being cut. At the cut to N, `shippedMajor()` (line 389) still reads N-1 from `package.json`, so a row committed to N resolves to `overdueBy = -1` and passes; `release.ts:1659` invokes the gate with `--release-major` but passes no target version, and the gate accepts none. Consequence: the refusal can only fire for rows already ≥1 major late, so the cut that *creates* the miss is never blocked, and the branch report first appears only after N+1 has shipped — which is precisely the one-major `code_graph` lateness this roadmap exists to prevent. Neither the unit fixture (test:75) nor the self-test case (lint:307) exercises `due == target`; both use shipped 12 / due 11, i.e. a miss that already happened. | fixed | `--cutting <X.Y.Z>` — comparand is the target; mutation-pinned in both the unit suite and `--self-test` (cdae71b30) |
| 2 | high | src/scripts/release.ts:1656 | On `--resume` without a `release/X.Y.Z` branch, `_detect_in_flight_target()` (release.ts:2484-2493) returns the `package.json` version, and `current` (release.ts:2594) is that same value — so `target === current`, `targetMajor <= currentMajor` holds, and the function returns before invoking the gate. The refusal is unreachable on the documented recovery path for a partially-completed release. The roadmap's execution note and the doc comment at release.ts:1626-1638 claim a major "can be reached three ways … the target is where all three converge"; resume is a fourth path, and it converges the guard to a no-op. The four tests cover minor / patch / major / multi-major and never `target === current`. | fixed | trigger keyed on the target's `X.0.0` shape, which covers resume; test `a resumed major is still checked` (cdae71b30) |
| 3 | medium | src/scripts/lint_scheduled_deprecations.ts:110 | The numeric fallback `/(?:^\|[^\d.])(\d+)(?:\.\d+){0,2}\b/` matches any digit run anywhere in the cell, so an ISO date resolves to a version: `resolveDueMajor('shipped 2026-07-29 — dormant by default')` → `{kind:'major', major:2026}`. Both live "Deprecation notice due" cells (docs/MIGRATION.md:19,21) resolve to 2026 today. This contradicts the file's own contract ("It also does not invent a due version … a cell it cannot resolve is reported as *unresolved* and reds", lint:17-21): a Removal-due cell carrying a date, an ADR number, or an issue number yields a confidently wrong commitment instead of `unresolved`. Latent only because both live removal cells match `not pinned` first, and because no live row uses a relative form anchored on a date-bearing notice cell. | fixed | explicit form anchored to the whole cell; `shipped 2026-07-29` now resolves `unresolved` (cdae71b30) |
| 4 | medium | docs/release-runbook.md:57-58 | The runbook now states the gate "compares **every** resolved due version against the shipped `package.json` major" and demotes the pre-flight checkbox to "the judgement the gate cannot make … not the arithmetic it can". `evaluate()` (lint:214-226) compares only the **Removal due** column; the Deprecation-notice-due cell is parsed solely as an anchor for relative removal resolution (lint:216-218) and is never compared against anything. The checkbox directly above (release-runbook.md:51-53) instructs acting on rows whose notice due **or** removal due lands on this major — so half of that obligation is now documented as automated and is checked by nobody. | fixed | runbook now states the Removal-due scope and that the notice half stays a human read (cdae71b30) |
| 5 | medium | src/scripts/release.ts:2628-2629 | The assert sits inside `if (!args.dry_run)`, justified at release.ts:1635-1636 as keeping a preview "fast and side-effect-free". This check is already both — one subprocess reading `docs/MIGRATION.md` and `package.json` — unlike the neighbouring dry-run exclusion, which is excluded for real cost (`_render_test_trend_line`, "~15s+ full vitest collection", release.ts:2634-2636). Net effect: `release --dry-run` on a major previews green for the single condition that will refuse the real run, which is the case an operator runs a preview to discover. | fixed | assert moved out of the `--dry-run` guard (cdae71b30) |
| 6 | low | src/config/gate-coverage.yml:777 | The entry contradicts itself: `corpus:` reads "(3 at baseline)" while the `note:` at :781 argues the `min_scanned: 1` floor from "the corpus is two rows"; `tests/scripts/lint_scheduled_deprecations.test.ts:178` repeats "Two rows today". The live table has three rows (docs/MIGRATION.md:19-21) — the branch itself added the third. The floor's stated rationale is therefore derived from a count that was already wrong when written, in the one file whose header (lines 45-58) forbids exactly that. | fixed | note corrected to three rows (cdae71b30) |
| 7 | low | docs/MIGRATION.md:20 | The overdue status, the named removal owner, and the two gating rules were written into the **Reversal condition** cell rather than into a status column or the Removal-due cell. That column no longer contains what its header names, so the table's own schema stops being a reliable contract for the next reader or parser. The gate is unaffected only incidentally — it resolves columns by header name and never reads this one. | fixed | status moved into a `### Row status` section under the table; every column again carries its header (cdae71b30) |
| 8 | low | agents/roadmaps-progress.md:9,40 | The regenerated dashboard carries a delta this branch cannot account for: `road-to-inbox-harvest-2026-08-c-release-head-truth.md` moves 10 open / 0 done → 1 open / 9 done and 1 → 2 blockers, but that roadmap file is not among the branch's changed files. The overall 246 → 265 is exactly this roadmap's 10 plus that roadmap's 9. Either the base dashboard was stale and this regen corrects it, or the regen captured tree state from outside the branch — the diff contains nothing that distinguishes the two, and the committed generated artefact is only correct under the first reading. | fixed | verified, not assumed: `origin/main`'s dashboard says 10 open / 0 done while that roadmap's file at the same commit has 0 open / 7 done — the base was stale and the regen corrects it; recorded in the roadmap's execution notes (cdae71b30) |
| 9 | low | agents/roadmaps-progress.md:15-21 | The branch closes the roadmap (10/10, `count_open == 0`, `count_deferred == 0`) and leaves it in the active tree, with the dashboard instructing `archive_completed_roadmaps --all`. The roadmap still carries an open blocker (`code-graph-removal-authorisation`), which the sweep refuses, so the printed instruction cannot succeed and the completed-but-unarchived state is what ships. The "pending archival" criterion ignores blockers while the sweep does not. | accepted-risk | generator-side inconsistency older than this branch — the dashboard's pending-archival criterion ignores blockers while the sweep honours them. Repairing the generator is outside this roadmap's surface; recorded in its execution notes so the next reader does not re-derive it |
| 10 | low | src/scripts/lint_scheduled_deprecations.ts:167-168 | `header.findIndex` for both columns is recomputed on every data row inside the parse loop, though the header is fixed the moment it is read (lint:161-164). The cost is trivial; the reporting consequence is not — when a column is renamed, both indices are `-1` for every row, so each row independently reds as "removal-due cell not resolvable" with an empty cell quoted, instead of the one true cause (a header that no longer matches) being named once. | fixed | column indices resolved once at the header; `HeaderMismatchError` reds once naming the header, pinned by a two-row fixture (cdae71b30) |
| 11 | low | taskfiles/ci-fast.yml:888 | With the live table already carrying one overdue row, the gate now emits its ⚠️ block on every `task ci` and every consistency run (.github/workflows/consistency.yml:458), exit 0, and nothing any contributor can do clears it until the maintainer answers the blocker. The risk register anticipates a refusal at a cut (rank 1) but not a permanent unclearable warning on every branch — which is the same ignore-the-standing-notice mechanism that made the runbook checkbox fail in the first place. | accepted-risk | deliberate and bounded: the warning is unclearable by design while a commitment is late, and habituation is answered by the hard refusal at the next major cut rather than by suppressing the notice. Recorded in the taskfile description and the roadmap's execution notes |
