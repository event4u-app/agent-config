# Findings: feat-scheduled-deprecation
<!-- completion-review: v1 | reviewed: 2026-08-16 | scope: 433af5ef7f7600dce1bd57971df31252b7a1cb3dc854a83323089f9c381d5789 | diff: a73a563b83e635eb467d20a1b20a9fdc8fcd09ad | reviewer: r2-fresh-subagent-feat-scheduled-deprecation | prompt_hash: 7ccf193f42f360d22bae806f9bab036885b0d60fa717b335a44851b86b9c4dbc -->

<!-- context-manifest: v1
inputs:
  diff_sha: a73a563b83e635eb467d20a1b20a9fdc8fcd09ad
  scope_hash: 433af5ef7f7600dce1bd57971df31252b7a1cb3dc854a83323089f9c381d5789
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md
  roadmap_hash: 139e567a3719c5c10da6a8a1aecd289a13563b01b2c8b244f616b18c4cd90633
  ac_hash: 4b53e297a51e10e2f762c99a14f20ece1394f927d02f3438e29d6015c03bf229
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-16T08:01:58Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/lint_scheduled_deprecations.ts:619 | The published `scanned:` count is rows PARSED, not rows COMPARED. `evaluate()` plans every row, then `complete()`s the unpinned / keep / withdrawn ones at line 289 before any arithmetic runs, so a row that skipped the comparison is indistinguishable in the gate's output from one that passed it. All three rows in the shipped table (`docs/MIGRATION.md:19-21`) currently resolve to a tracked state (unpinned, withdrawn, unpinned), so today the gate parses 3 and compares 0 while printing `scanned: 3` plus a green "none due" line, and `min_scanned: 1` in `src/config/gate-coverage.yml` is satisfied entirely by skipped rows. The roadmap's Risk 2 ("ships green because it parses nothing") is closed by the zero-row fixture; the adjacent live case — parses everything, compares nothing — is not observable from the gate's own output or from its coverage floor. Publishing a compared-count beside `scanned` would close it. | open | |
| 2 | medium | src/scripts/lint_scheduled_deprecations.ts:148 | The documented relative form `next major after the notice -> notice major + 1` cannot resolve under the table's own authoring convention. `evaluate()` (line 298) derives `noticeMajor` by running `resolveDueMajor` over the notice cell, and the anchored version regex (line 157) deliberately refuses to read a date as a version — while every shipped notice in the table is written as a date ("shipped 2026-08-14"). A pinned row using the relative form therefore flips from resolvable to `unresolved`, i.e. exit 1, on the day its notice ships. The `compatibility` row escapes only because `UNPINNED_MARKER` is tested first; removing that phrase to pin the row reds the gate. No test covers the date-notice plus relative-removal pair end to end: the grammar test passes `noticeMajor` in directly, and every fixture uses a bare-version notice. | open | |
| 3 | medium | src/scripts/release.ts:1684 | The `previewOnly` early-return is untested, and it is the exact branch this function's own JSDoc names as a prior regression ("an earlier revision of this guard broke it by sitting above the dry-run branch with no preview mode"). The new describe block in `tests/scripts/release.test.ts` passes no `{ previewOnly: true }` case, and the existing `--dry-run` integration tests cannot reach it: they spawn against the real repo, whose table is currently clean, so the guard returns at `res.returncode === 0` (line 1679) before this branch. A regression re-introducing die-on-dry-run would be caught only if the real table happened to be dirty at that moment. | open | |
| 4 | medium | tests/scripts/release.test.ts:932 | Tautological assertion. `Function.length` counts parameters before the first default, so `assert_scheduled_deprecations_clear.length` is 1 for the signature `(target, runner = ..., opts = {})`, and `toBeLessThanOrEqual(3)` holds for essentially any refactor — including one that adds a `current` parameter with a default. The test's own comment states it asserts "the guard takes ONE version and never reads the current one"; the assertion cannot establish that. Only the second half of the test (exactly one semver reaches the gate, and it is the target) does real work. | open | |
| 5 | medium | src/config/gate-violation-baselines.json:28 | The `lint_roadmap_blockers:decidability` note argues from a state this same diff removes. It justifies the entry by saying the blocker's `If you do nothing:` is one of the few that is not the common no-cost answer, because "the release gate that PR ships refuses the next major cut while the blocker is open". `docs/MIGRATION.md:20` and the roadmap execution notes record the commitment as withdrawn and the blocker as resolved, and the dashboard row for this roadmap shows the blocker column at 0 — so nothing refuses a cut. The roadmap note was corrected for exactly this reason; the baseline note was not. | open | |
| 6 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md:168 | Off-by-one in the note's own arithmetic: it claims "an Overall move of exactly its own 10 steps over an unchanged denominator", but the dashboard moves 236 to 245, which is +9, because row 16 read Open 9 / Done 1 at base rather than 10 / 0. The subject of the surrounding paragraph is a claim that outlived its evidence, which makes the wrong figure worth correcting rather than tolerating. | open | |
| 7 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-d-scheduled-deprecation.md:155 | Stale open-item. The bullet states that the dashboard prints an `archive_completed_roadmaps --all` instruction for this roadmap "that cannot succeed while its blocker is open". The blocker is `Status: resolved` in the same file and the dashboard's blocker column for this roadmap reads 0, so the obstruction does not apply to the instance cited. The generator-side inconsistency it names may still be real in general; the worked example is not. | open | |
| 8 | low | src/scripts/lint_scheduled_deprecations.ts:26 | Exit-contract mismatch, self-cited. The header enumerates exit 2 as "usage error", but line 603 returns 2 for an unreadable or malformed `package.json` while its comment claims to do so "per the contract at the top of this file" — a clause the contract does not contain. The neighbouring environment failure, a missing `docs/MIGRATION.md`, returns 1. Two environment errors, two exit codes, and the contract documents neither. | open | |
| 9 | low | src/scripts/release.ts:1695 | Every non-zero gate exit is diagnosed as a table defect. The guard branches only on `returncode === 0`, but the gate also returns 2 for a usage error or an unreadable `package.json`, so an environment failure dies with "the scheduled-deprecations table ... has a row due at or before this major, or one that cannot be resolved". The gate's raw stdout and stderr are echoed immediately above, so an operator can recover; the headline reason is still wrong, and it is the line that will be pasted into an incident note. | open | |
| 10 | low | Taskfile.yml:171 | The added `ci:` entry is the only one of roughly 200 entries in that list carrying an inline comment, and that comment is what makes step 1.2's `verify:` hook (`grep -c 'lint_scheduled_deprecations' Taskfile.yml`) return non-zero. The execution notes record this as "a coincidence rather than a satisfaction", but the comment is unique in the file and was authored by the change that needed the grep to pass, so the step is ticked against evidence measuring the comment rather than the registration. Kept low because the registration itself is real (`taskfiles/ci-fast.yml` plus `.github/workflows/consistency.yml`). | open | |
| 11 | low | tests/scripts/lint_scheduled_deprecations.test.ts:238 | The test named "real repo: reproduces the reading this gate was built on" no longer reproduces that reading. The gate was built on a table in which `code_graph` was one major overdue and reported by name; the test now asserts status 0 with no unresolved cell, i.e. the opposite outcome. It also watches a shared tracked artefact (`docs/MIGRATION.md`), so an ordinary table edit reds a test whose name promises a historical reproduction rather than a current invariant. | open | |
| 12 | low | src/scripts/lint_scheduled_deprecations.ts:70 | The three escape-hatch markers are bare substring matches anywhere in the removal cell and require nothing beside themselves: no reason, no owner, no review date. The gate's own remediation text (line 639) instructs authors to record "a withdrawn commitment with its reason", but the phrase alone satisfies the parser, so a live commitment can be silenced permanently by adding three words to a cell. Because the marker tests run before the version forms, a cell carrying a real due version plus the words "no removal date" in its prose also resolves as tracked. | open | |
