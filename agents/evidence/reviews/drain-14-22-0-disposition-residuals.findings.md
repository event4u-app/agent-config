# Findings: drain-14-22-0-disposition-residuals
<!-- completion-review: v1 | reviewed: 2026-09-08 | scope: 6c4231b36ded753058c08d2cff7bac5bb32463c59f79cce2e7a7436ef08c6f2d | diff: 4d0f978abf152f69b461bc477df9e9be55df79de | reviewer: r2-fresh-subagent-drain-14-22-0-disposition-residuals | prompt_hash: 2cb6d01b6e0f2850ec16bb58960e6eb0bf3fce2a9f8a956490393137f942fd90 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-14-22-0-disposition-residuals"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-08 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 4d0f978abf152f69b461bc477df9e9be55df79de
  scope_hash: 6c4231b36ded753058c08d2cff7bac5bb32463c59f79cce2e7a7436ef08c6f2d
  roadmap: agents/roadmaps/archive/road-to-the-14-22-0-disposition-residuals.md
  roadmap_hash: a98a04d45128f471f3303d06a26da1c9a319802b0406439e93981377070f0a81
  ac_hash: d103029842b8416c5e7265a20b61b4699ba26763db9422cabdee656358b78896
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-08T23:22:28Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | docs/decisions/ADR-263-skills-are-explicitly-invoked-reference-material.md:102 | Amendment 1 states "189 is the figure that stands" and scopes itself to "one number in a supporting bullet", but `187` occurs TWICE in this record. The second occurrence is line 102, inside the blockquote the Decision introduces with "it is adopted verbatim as the target" — i.e. the wording this package is instructed to publish about its own skills. Only the supporting bullet at :113-118 was annotated `WRONG`. Result: the record's authoritative half still asserts `187 skills declare neither trigger nor eval data` while `docs/CLAIMS.md` (corrected on this branch) and `agents/evidence/metrics/skill-activation-census.json` say 189, so the ADR now contradicts the ledger from the opposite direction, and an executor implementing the ADR's target wording would publish 187. AC-3's "the contradiction is gone, resolved on the side that was wrong" is therefore met for the bullet and not for the Decision. Note the fix is not a figure swap: with 189 the quote's three bullets (12 + 100 + 189) sum to 301, not 299, so the corrected wording must also name the 2-skill overlap. No gate covers this — `check_skill_activation_claim` compares CLAIMS.md to the census and never reads the ADR (verified: gate exits 0 today with the ADR still saying 187). | open | Verified: `grep -n '187\|189' docs/decisions/ADR-263-*.md` -> :102 and :113-114 both carry 187, :118 declares 189 stands; `sed -n '85,106p'` reads the "adopted verbatim as the target" framing; `./scripts-run src/scripts/check_skill_activation_claim` -> exit 0, `✅ census claim matches … (measured 2026-09-06)`; `./scripts-run src/scripts/check_adr_frontmatter` -> no errors (it does not read body figures). |
| 2 | medium | src/scripts/_lib/vendored_grammar_upstream.ts:185 | The anchor resolves the file to hash as `path.join(vendoredDir, path.basename(entry.path))`, discarding the manifest entry's directory, while the roadmap, the module docblock, the manifest `_comment` and `src/vendor/grammars/README.md` all describe it as comparing "every grammar the manifest admits" — which is `entry.path`. Two consequences, both latent today because all three rows live in `src/vendor/grammars/`. (a) A future row outside that directory is never hashed at its own path: either a spurious refusal, or — if a same-named file exists in `vendoredDir` — a `comparisons` row reported `equal: true` for a path whose bytes were never read. (b) Two rows sharing a basename in different directories both resolve to the same vendored file, so one admitted binary is silently unanchored while the verdict reports full coverage. That is a false green in exactly the same-commit threat class the module was written to close. The coverage test cannot catch it: :124 compares `path.basename(c.vendoredPath)` against `path.basename(p)` on both sides, so the basename collapse is applied to the expectation as well. Fix is one line — join on `entry.path` relative to `repoRoot` (keeping the `vendoredDir` seam for fixtures) and compare full paths in the coverage test. | open | Verified by reading :161-204 and :118-125 of the test; `src/config/packed-binary-manifest.json` currently carries three rows, all under `src/vendor/grammars/`, so no live divergence. `npx vitest run tests/scripts/vendored_grammar_upstream.test.ts` -> 13 passed, i.e. the suite is green with the defect present. |
| 3 | low | src/scripts/_lib/vendored_grammar_upstream.ts:209 | `installedVersion: pin.version` sources the field named *installed* from `package-lock.json` rather than from the `package.json` it actually read at :174. It is correct only as a side effect of the strict equality guard at :175-179; the verdict therefore cannot report a version it observed, only the one it expected. If that guard is ever loosened (a semver range, a warn-instead-of-throw path), the verdict silently reports the locked version as observed — the shape of a check that reports what it assumed. `installed.version` is in scope at that point and is the honest source. | open | Read at :174-179 and :206-212; the test at :109-116 asserts the two are equal on the real tree, so it would not surface the substitution either. |
