# Findings: drain-14-22-0-disposition-residuals
<!-- completion-review: v1 | reviewed: 2026-09-08 | scope: 48b0e1a3e7ad075fe83bd993f97eef64900bf389532091c094163031659cef79 | diff: 57dd53925c12ab9140e54d6b3b8e4835cc7f7f76 | reviewer: r2-fresh-subagent-drain-14-22-0-disposition-residuals | prompt_hash: c3a63ed739d404cbac254c0b208da6d99859f82f38ecd276e3c8b5a8e921b9d1 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-14-22-0-disposition-residuals"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-08 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 57dd53925c12ab9140e54d6b3b8e4835cc7f7f76
  scope_hash: 48b0e1a3e7ad075fe83bd993f97eef64900bf389532091c094163031659cef79
  roadmap: agents/roadmaps/archive/road-to-the-14-22-0-disposition-residuals.md
  roadmap_hash: 68da05c819fec69a6179a09434021c80eef262f0cdaf2c3a68694e716c219ac1
  ac_hash: 25c96f6d0ff2d5bf7b19f1025fb4b3339f83e525dca74dce617e9282b983ca73
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-08T23:36:17Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | tests/scripts/vendored_grammar_upstream.test.ts:132 | The test added as the regression guard for round-1 finding 3 cannot detect the defect it names. `it('reports the version it actually read from node_modules, not the one it read from the lock')` asserts `verdict.installedVersion` against the version it re-reads from `node_modules/tree-sitter-wasms/package.json` — but the module refuses at :208-212 unless that value already equals `pin.version`, so on the real tree the two sources are equal by construction and the assertion holds for BOTH sourcings. Sabotage-proven on this tree: restoring the exact round-1 defect (`installedVersion: pin.version` at :242) leaves `npx vitest run tests/scripts/vendored_grammar_upstream.test.ts` at `16 passed (16)`; restored from a pre-mutation copy and re-verified green at 16. This is the same weakness the round-1 disposition itself named about the OLD test ("the test at :109-116 ... would not surface the substitution either") — the production line was fixed, the guard was not. Consequence is the one round-1 finding 3 stated: if the strict-equality guard at :208 is ever loosened (a semver range, warn-instead-of-throw), the verdict silently reports the locked version as observed and no test moves. A sensitive test needs a seam the suite does not currently exercise — e.g. a fixture whose installed `package.json` version differs while the guard is stubbed, or asserting on a verdict produced through `upstreamPackageDir` with a distinguishable version. | fixed | Fixed in bd9962e04 — `buildVerdict` extracted as the seam where the locked and observed versions can differ, and the test drives it with `0.1.13` against `9.9.9-observed`. Sabotage-proven: pointing the field back at `args.pin.version` reds it (`1 failed | 20 passed`); restored and green at 21. The whole-function test is rewritten to assert what it can assert — that the two ARE equal, because anything else refuses. |
| 2 | low | src/scripts/_lib/vendored_grammar_upstream.ts:146 | `readManifest()` re-implements `loadManifest()`, which `packed_binary_predicate.ts:257-262` already EXPORTS — same path construction (`<repoRoot>/src/config/packed-binary-manifest.json`), same `Array.isArray(raw.entries)` shape check, same message text. This module already imports `VERIFIABLE_KIND` and `PackedBinaryManifest` from that exact file (:56), so the reader was one identifier away. Two readers of one manifest on a security-relevant path drift silently: a shape condition added to `loadManifest` (a `schema_version` check, a kind allowlist) leaves the anchor accepting a manifest the predicate rejects, and the anchor's whole value is that it covers what the manifest admits. No live divergence today — the only behavioural difference is the `existsSync` pre-check and the `UpstreamAnchorRefusal` error class, both of which a thin wrapper around `loadManifest` keeps. | fixed | Fixed in bd9962e04 — `readManifest` now calls the exported `loadManifest` and only re-types its failures as refusals, so one reader serves both. |
| 3 | low | src/scripts/_lib/vendored_grammar_upstream.ts:95-107 | All three optional fields of `UpstreamAnchorOptions` are documented as "Fixture seam" and no fixture uses any of them: a tree-wide grep for `upstreamPackageDir` / `vendoredRoot` / `manifest:` finds zero call sites passing them, and every one of the 16 test cases calls `verifyVendoredGrammarsAgainstUpstream({ repoRoot })` alone (test :105, :125, :133, :145, :156, :166, :173-206, :233, :264). So the docblocks state a purpose the tree does not exercise, and the round-1 repair's stated reason for keeping the seam ("keeping the `vendoredDir` seam for fixtures") is unverified by any test — `vendoredRoot` in particular is new, unexercised dead API added in the repair. Either drive one negative case through each seam (the cheapest is the finding-1 sensitivity fixture above, which needs `upstreamPackageDir`), or drop the unused fields; an exported option nothing sets is surface a future caller can get wrong for free. | fixed | Fixed in bd9962e04 — one case per seam: `upstreamPackageDir` (package moved out of node_modules, the default path refuses, the seam finds it), `vendoredRoot` (alternate root with different bytes, the divergence names that path), `manifest` (an injected empty claim set refuses). |
| 4 | low | docs/CLAIMS.md:245 | The amended claim contradicts ADR-263 on the word this branch harmonised it against. The claim now says "What ADR-263 did NOT do is still worth naming: no selection mechanism was built, **no population was reclassified**, and the split above is untouched", while ADR-263:207-209 § Consequences says "The 12 trigger declarations are not deleted. They are **reclassified** as unvalidated metadata with no host contract, which is what they are". The same claim sentence two clauses earlier ASSERTS that reclassification's content ("the 12 are unvalidated metadata with no host contract to bind to ... they are kept, not deleted"), so the tension is inside one sentence, not only across two files. The two senses are separable on a careful read — the claim means the numeric 12/100/2/189 partition, the ADR means the semantic status of the 12 — but the claim is `status: backed` and its stated purpose on this branch was to stop contradicting ADR-263. No gate covers it: `check_skill_activation_claim` compares the nine figures plus `measured`/`last_verified` and never reads the ADR (verified: exit 0 today). `docs/proof.md:98` carries the identical sentence, so a fix is two files plus `build_proof`. | fixed | Fixed in bd9962e04 — the claim now separates the two senses: the NUMERIC split 12/100/2/189 is untouched, while the 12 were reclassified SEMANTICALLY. `docs/proof.md` regenerated. |
| 5 | low | docs/decisions/ADR-263-skills-are-explicitly-invoked-reference-material.md:196 | § Consequences bullet 1 still describes `docs/CLAIMS.md:245` in the present tense — "reads the zero as a defect for the trigger-declaring population" and "Correcting the ledger text ... is tracked as work rather than performed here" — and this branch falsified both: line 245 now reads "the zero WAS published as a defect ... and ADR-263 SUPERSEDES that reading", and the work was performed here. Amendment 1 names the bullet at :168-176 ("the § Consequences bullet below says correcting the ledger text is tracked as work ... That work is `road-to-the-14-22-0-disposition-residuals` step 1.3"), so a top-down reader is warned — but the bullet itself carries no in-place pointer, which is precisely the treatment Amendment 1 argued for and applied at the two `187` sites ("Both sites now carry a pointer here"). A reader who greps for `docs/CLAIMS.md:245`, or who reads § Consequences directly, lands on a falsifiable statement about that line with nothing beside it. Same class as round-1 finding 1, one site short of drained; a one-line pointer at :196 closes it without rewriting the record. | fixed | Fixed in bd9962e04 — the bullet is kept as written and carries the pointer, plus the honest split: the ledger half is done, the consumer-surface limb has no named receiver and no sweep has established whether any such surface exists. |
