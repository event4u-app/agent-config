# Findings: feat-archive-picktier-decision-layer
<!-- completion-review: v1 | reviewed: 2026-08-16 | scope: 40c73f3058df90b9670af2a952fbe8ab756791981dd2c9e78131eb3aa70c2bf3 | diff: 8b402725818fed47424b788de20ef9de516233a5 | reviewer: r2-fresh-subagent-feat-archive-picktier-decision-layer | prompt_hash: c50b322d627359bbac46b264bb4f63b5f1d7c16be6d6a2396ef485b20b756f44 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 8b402725818fed47424b788de20ef9de516233a5
  scope_hash: 40c73f3058df90b9670af2a952fbe8ab756791981dd2c9e78131eb3aa70c2bf3
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-16T20:35:19Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | agents/roadmaps-progress.md:5,39,394 | Dashboard was not regenerated after `picktier-wire-or-archive` was flipped to `Status: resolved` (roadmap line 240). The committed file still renders the whole `blockers-…-d-top-band-model-economy` section as a live blocker, still links it from the roadmap row, and still reports **36** open blockers / **10** need you. Regenerating from the branch's own roadmap sources yields **35** / **9** and drops the section entirely (reproduced by running `update_roadmap_progress.ts --repo-root <tmp copy>`; 138-line delta). `./agent-config roadmap:progress --check` prints "agents/roadmaps-progress.md is stale" and exits 1; that check is `roadmap-progress-check`, wired into `task ci` (Taskfile.yml:341) and into the installed pre-commit hook, so this is a merge blocker and it also publishes a blocker the branch just closed as still open. | open | |
| 2 | high | agents/roadmaps/road-to-inbox-harvest-2026-08-d-top-band-model-economy.md:226 | Closing 4.1 and its acceptance criterion drives the roadmap to `count_open == 0` while acceptance criterion at :226 stays `[~]`, which trips roadmap-progress-sync Iron Law 3. The same gate as finding 1 fails a **second, independent** way — "Iron Law 3 — roadmaps with unresolved `[~]` deferred items must NOT auto-archive … road-to-inbox-harvest-2026-08-d-top-band-model-economy.md (13/13 done · 1 deferred)" — and regenerating the dashboard does NOT clear it: `pending.length` alone sets exit 1. Until the deferral is resolved (spawn follow-up / restore / cancel) `task ci` stays red and the pre-commit hook blocks every further roadmap-touching commit. The branch added the Iron Law 3 banner to the dashboard but left the underlying deferral undecided. | open | |
| 3 | medium | src/scripts/routing_doctor.ts:167-217 | `check_budget_delivery` is the one **live code** surface still monitoring the archived layer, and the sweep missed it while updating the contract, CLAIMS, proof, both subagent-routing copies, the module docstring and the test docstring. Its docstring rationale is now wrong ("budget-aware tier routing has no on/off setting any more … so the doctor always checks the DELIVERY evidence" — the setting is not the issue, the relation itself was deleted), and the emitted warning "budget-aware tier routing shows no delivery evidence … the relation may not be running (adherence != delivery)" is now unconditionally true forever and un-actionable: it invites a reader to wire a relation that no longer exists. This is exactly the permanent-advisory-finding shape the branch's own dashboard prose criticises for `check_council_layout`. | open | |
| 4 | medium | src/server/schemas/settings.ts:83,89 | User-facing setting descriptions still assert the removed relation. `:83` describes `cost.budgets.per_tier.cheap` as a ceiling "under budget-aware delegation (docs/contracts/budget-routing.md)" — pointing a live setting's rationale at a page the branch turned into a retired migration record — and `:89` states outright "with cheap exhausted and strong funded, **budget routing uses the strong tier** rather than blocking work", which is the `pickTier` fallback that no longer exists. These project verbatim into the generated `docs/settings-reference.md:90` and `:92`, so the most consumer-visible surface still documents the archived behaviour as current. | open | |
| 5 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-d-top-band-model-economy.md:165 | The step's verify annotation is inverted and proves the opposite of the claim. `grep -c 'pickTier' src/scripts/_lib/tier_budget_routing.ts` returns **2** and exits **0** on the post-change tree — it matches the archival docstring at tier_budget_routing.ts:5 and :11, not an implementation — so it cannot distinguish "archived" from "still implemented", and it flips to a FAILURE (count 0, exit 1) precisely on the day someone completes the cleanup by trimming the docstring. A removal step needs a negative probe (e.g. no `export function pickTier`), not a bare occurrence count over a file whose surviving comment names the symbol. | open | |
| 6 | low | src/scripts/cost/budget.mjs:13 | `join` is now an unused import — its only two call sites were the deleted `join(dirname(STORE), 'tier-reserves.jsonl')` and the deleted `loadReserveTtlMs` config path. Zero `join(` occurrences remain in the file. An own-orphan the diff created and did not clean up; ESLint does not catch it (no `no-unused-vars` resolves for this file, verified with `--print-config`), so nothing downstream will. | open | |
| 7 | low | src/scripts/_lib/tier_budget_routing.ts:49 · src/scripts/routing_doctor.ts:648 | Latent correctness note on the surviving export: `tripCooldown` was both the only writer AND the only purger of expired entries, and `readCooldowns` returns raw stored values with no expiry filter. Its sole remaining consumer renders the marker on `s.cooldown_until_ms > 0`, not `> now_ms`, so any `tier-cooldowns.json` that ever appears makes `routing:doctor` report that tier as COOLING permanently, with nothing left in the tree able to clear or age it. The `cooldown_store_present` repair only covers the file-**absent** case. Not live today (no such file exists and nothing can create one), but the deletion is what removed the expiry-aware reader that previously masked it. | open | |
| 8 | low | agents/roadmaps/later/road-to-cost-parity-2-state-aware-dispatch.md:237-241 | A parked roadmap still cites the archived page as a binding live constraint — step 4.2 "No budget brake" rests on "`docs/contracts/budget-routing.md` and `contexts/execution/subagent-routing.md` state that work is never blocked to save money". Both cited sources were rewritten by this branch: the contract is a retired migration record and subagent-routing now says the fourth step does not exist. Two sibling roadmaps (`…-b-ledger-truth`, `…-d-top-band-model-economy`) were updated for exactly this reason; this one was not, so a future reader inherits a premise sourced from a retired contract. | open | |
