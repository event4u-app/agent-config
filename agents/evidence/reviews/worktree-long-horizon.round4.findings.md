# Findings: worktree-long-horizon
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: fa369cf83bb27a360c7d0aa0f153b2f9975d4bbadb3b7416b38cd50eaf1b3ddf | diff: 76d1db5b33e33c0c5c17a6203c1c9aee511f01aa | reviewer: r2-fresh-subagent-worktree-long-horizon | prompt_hash: 43d2089b1460b1dcc4c7e76ffb393043462cdc228d692fa93bc2794e7e667305 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 76d1db5b33e33c0c5c17a6203c1c9aee511f01aa
  scope_hash: fa369cf83bb27a360c7d0aa0f153b2f9975d4bbadb3b7416b38cd50eaf1b3ddf
  roadmap: agents/roadmaps/road-to-long-horizon-execution.md
  roadmap_hash: 89257b7a50dc2827c00e9a9f8ca568627c2d70da3aa48b2188d664a14a29884a
  ac_hash: cc0790f4722b1540ced5602c39fd46db00ac1547495b3ab0e6082de1ede2c413
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T03:47:18Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | docs/CLAIMS.md:641 | The new public claim `council-fallback-loses-zero-seats` ships `status: backed` with `evidence: exec:vitest run tests/scripts/ai_council/council_cli.test.ts -> 0`, and that very suite contains the counter-example: `council_cli.test.ts:959` — "a fallback that was REFUSED by the retry budget still counts as absent" — asserts `quorum {present: 0}` and `absent: [{member: 'anthropic', reason: 'no_auth'}]` for a response carrying `error: 'auth_expired'` + `fallback_skipped: 'cost_budget'`. That is precisely the claim's antecedent: `auth_rejected` is an always-eligible class (`transport_resolver.isFallbackEligibleUnder`) and the twin WAS constructible — it was built and then refused by `runGatedRetry`'s own spend gate (`mid_flight_fallback.ts:201`) or by `refuseUnmeteredEscalation` (:281). The claim states no budget precondition, so as written it is false on a path this same change-set built, tests, and publishes as ✅ in `docs/proof.md:345`. `check_claims.ts:478` compares only the command's exit code (`run_exec_evidence` → `outcome.mismatch`), so a suite whose assertions contradict the claim still exits 0 and CI can never see it — the "verifier degraded into a rubber stamp" failure that function's own comment names, in the other direction. Reachability is not exotic: `_breach` (`spend_gate.ts:98`) fires on the TOKEN ceilings unconditionally (`max_input_tokens` default 50 000, `max_output_tokens` 20 000, neither gated on `> 0`), so any pass far enough along breaches on the retry estimate and loses the seat. | open | |
| 2 | medium | src/scripts/_lib/run_checkpoint.ts:337 | `verifyCheckpoint` compares `head` with `cp.head === head`, treating a `null` CLAIM as a claim rather than as "the writer could not tell". But `readHead` is explicitly designed to return `null` on any surprise ("Returns `null` on anything unexpected. A checkpoint without a commit is still useful"), and `buildCheckpoint` stores that `null` verbatim. So a checkpoint written where HEAD did not resolve — an unresolvable `gitdir:` pointer, a container mount without `.git`, a permission error on the Stop path — reports, on resume AT THE SAME COMMIT with every other field agreeing: `CHANGED head: claimed null → actual 76d1db5b…` followed by "the tree moved since the checkpoint — the ACTUAL column is what to resume from." Reproduced against this checkout with a `head: null` checkpoint whose four roadmap fields match exactly: `agrees` is `false` and the render says the tree moved. `res.agrees` is what the loop contract's §5d re-verification hands a resumed run, so the one signal round 3 added inverts on exactly the failure path its writer is built to tolerate. Neither `readCheckpoint` (:228) nor `latestCheckpointFor` (:276) validates `head`, so a pre-`head` checkpoint (field absent → `undefined`) produces the same false "moved" verdict. The six `head` tests assert only non-null values. | open | |
| 3 | medium | src/scripts/interruption_report.ts:446 | `memos: lookupByRunId(memosByRun, runId) ?? 0` joins memo directories to the interruption ledger's `run_id`, which is `derive_session_tag(session_id)` — a 16-hex fingerprint (`chat_history.ts:326`). Nothing tells a caller of `decision:memo write --run <id>` to use it, and nothing can: no command prints the fingerprint, the `_dispatch.bash` help says only `--run <id>`, the loop contract (`roadmap-process-loop.md:302`) and the contract doc (`ai-council-config.md:958`) both write `--run <id>` undefined, and `decision_memo.isSafeRunId` accepts any `[A-Za-z0-9_-]{1,64}` slug. Verified: with the real session id of this run, `lookupByRunId({'road-to-long-horizon-execution': 3}, '80c4601d3ddc254e')` returns `undefined` → `memos: 0`, while `{'<raw session id>': 3}` returns 3. So `median_memos_per_run` reads 0 for every run whose memos were filed under a readable id, and `run:supervise --digest` — which walks the same directory with no join (`run_supervise.ts:370-389`) — simultaneously prints "decisions: 3 memo(s) across 1 run(s)". Two shipped surfaces disagree about the same files. This is the "zero meaning the key did not match, indistinguishable from zero meaning no instrument" failure `lookupByRunId`'s own docblock (:126) says it exists to end, left open on the one axis whose key is model-chosen. | open | |
| 4 | medium | docs/contracts/ai-council-config.md:913 | Three operator-facing surfaces assert that the new `second_model` rung is **"Quota-bounded by the same `cli_call_budget` counter"** — here, in `agents/templates/.ai-council.yml.example:529` ("The rung books against the same `cli_call_budget` counter above — one subscription, one counter"), and in the loop contract (`roadmap-process-loop.md:282`, "bounded by the same `cli_call_budget` counter the council books against"). No mechanism does this. `second_model` is parsed (`fallback_config.buildSecondModel`), stored on `DecisionResolutionEntry`, and read by nothing: it is not forwarded by `council_settings_block.synthesizeAiCouncilBlock` (which omits `decision_resolution` entirely) and has zero non-test references in the tree. Its only implementation is the model-carried instruction "one local vendor-CLI pass" — an agent shelling out to `gemini`/`claude` directly, which books nothing. `cli_call_budget.ts:32-41` states the constraint from the other side: "the consumer set is closed: exactly two files in the tree construct a `CliClient`" (`council`, `team`), and "`unknown` is a FINDING when it appears — it means a third booking path exists". The rung is that third path. Concrete failure: an operator sets `cli_call_budget.max_calls_per_day.gemini: 20` to protect a subscription and `medium_impact.second_model: gemini`; every resolution consumes plan quota outside the cap, `council:quota` reports headroom that does not exist, and the counter that the docs say bounds the rung never moves. Every other unbuilt half in this change-set is named as unbuilt (`--relaunch` "accepted but NOT implemented", `preflight`'s "THE DEDUP PRECONDITION CANNOT FIRE TODAY"); this one is asserted as a live control. | open | |
| 5 | medium | src/scripts/run_supervise.ts:255 | `readAllRecords`'s docblock argues, correctly, that the watcher must not prune: "`read_live_records({ prune: true })` deletes expired records as it walks; deleting the evidence a watcher exists to act on would make the first scan the last one that could see anything." It then guards only its OWN reader. `sessions_cli.ts:174` — `cmd_list`, i.e. the routine `agent-config sessions:list` — calls `read_live_records(dir, { prune: true })` against the same `register_dir(root)`, and `session_register.read_live_records:358-365` unlinks every expired record it encounters. TTL is 4 h on claude / 12 h default (`session_register.ts:102,121`), while the digest is by design a once-a-day report ("what happened while nobody was watching"). So a session that dies at 14:00 expires at 18:00, any `sessions:list` after that deletes its record, and the 09:00 digest reports zero dead runs for a roadmap with open steps — the exact confusion `run_supervise`'s header opens by naming. It also silently defeats round 3's fix for finding 6: `clearCompleted` (:128) can only release a slug that appears as a `complete` CANDIDATE, and a pruned record produces no candidate, so the counter that "releases when the roadmap completes" never releases whenever a `sessions:list` ran between the death and the digest. | open | |
| 6 | low | src/scripts/_lib/unattended_guard.ts:56 | `isProductionRemote` enumerates the URL shapes it considers production-reachable (`http://`, `https://`, `ssh://`, `git://`, scp-style `^[\w.-]+@[\w.-]+:`) and returns `false` for everything else — so an UNRECOGNISED shape reads as safe. The common SSH-alias form is unrecognised: with `Host gh` in `~/.ssh/config` (or `url."git@github.com:".insteadOf "gh:"`), `git remote -v` prints `origin  gh:org/repo.git (fetch)`, which has no protocol prefix and no `@`, so `isProductionRemote` returns `false`, `checkRemotes` returns `{safe: true, reason: 'no production-reachable remote'}`, and `preflight` clears the very precondition UOTL 7.1 exists for — "an unattended push reaching a real remote because a worktree inherited the parent's `origin`". This contradicts the module's own stated posture two functions down: `checkRemotes` fails CLOSED on an unreadable git config because `"I could not tell" and "there is no production remote" must not resolve to the same answer when the consequence of being wrong is an unattended push to a real remote` — which is exactly what the unknown-shape branch does. Latent while the spawn is unbuilt (nothing outside the tests calls `preflight`), but this is the classifier the lane's safety rests on and its default is permissive. | open | |
| 7 | low | src/server/io/yamlIO.ts:194 | `upsertScalar` hardcodes two-space indentation in BOTH halves — the ancestor search matches only `indentLen === '  '.repeat(depth).length` (:166) and the emitted block uses `'  '.repeat(depth)` (:194-196) — while `replaceScalar` at least skips odd indents rather than assuming a width. On any file indented at another width the depth-2 walk round 3 added silently fails to match and the function emits a DUPLICATE mapping key, which is the precise failure its docblock says the insertion-point logic prevents ("an existing `fallback:` section gains a key rather than a second `fallback:` being appended — a duplicate mapping key is a YAML error in strict parsers and a silent last-wins in lenient ones"). Verified on `enabled: true\nmembers:\n    anthropic:\n        enabled: true\n        model: claude-x\n`: `upsertScalar(t, ['members','anthropic','mode'], 'cli')` appends a second `  anthropic:\n    mode: cli` under `members` and the result throws `YAMLException: bad indentation of a mapping entry`. The depth-1 production path corrupts too when the target block exists at another width — `upsertScalar('ai_council:\n    x: 1\n', ['ai_council','y'], 2)` yields unparseable YAML — and `wizard.ts:931-935` writes the result with `writeAtomic` and returns `{ok: true}` without re-parsing, so a hand-authored four-space `.ai-council.yml` carrying a `fallback:` block is destroyed by the toggle and the operator is told it succeeded. The eight `yamlIO.upsert.test.ts` cases are all two-space. | open | |
