| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | docs/decisions/ADR-105-automatic-subagent-orchestration.md:8 (validated by src/scripts/check_adr_frontmatter.ts:274-320,367) | ADR-105 gains a new `amended_by: ADR-117` frontmatter field, replacing the old free-text parenthetical. `amends:`/`amended_by:` are brand-new keys introduced by this very diff (Phase 4 Step 2), and ADR-117 is not in the changed-file list, so it cannot carry the required reciprocal `amends: ADR-105`. `check_amendment_links()` (wired into `check()` at line 367) is unconditional — no date-based grandfathering like `REVIEW_TRIGGER_SINCE` exists for it — so on the real corpus it will push an `error`-level finding either way: if ADR-117 exists without the reciprocal key, "is one-sided" (line ~305); if it doesn't exist at all, "points at an ADR that does not exist" (line ~296). Either path makes `errors.length > 0` at `check_adr_frontmatter.ts:410`+, so `npx tsx src/scripts/check_adr_frontmatter.ts` exits non-zero — directly violating Acceptance Criterion #7 and breaking the pre-existing "ADR frontmatter + review_trigger" step in `.github/workflows/rule-backstops.yml`. Note also that touching ADR-105 at all is outside the roadmap's own declared Phase 4 scope — its Rollback line names only "the two frontmatter keys, and the ADR-035 / ADR-232 edits," not ADR-105/ADR-117. | rejected | REFUTED on evidence. The premise — `amends`/`amended_by` are brand-new keys, so ADR-117 (not in the diff) cannot carry the reciprocal — is false: ADR-117 already carried `amends: 105 (Decision 2 only ...)` as free text BEFORE this change. That pre-existing value is exactly what the new corpus check surfaced, and supplying ADR-105's missing half is why the check passes. Verified: `grep '^amends:' ADR-117` present; `check_adr_frontmatter` exit 0 over 174 ADRs. The reviewer saw only the diff and read 'not in the changed-file list' as 'does not carry the key'. The SCOPE half IS accepted: touching ADR-105 was outside Phase 4's declared rollback line, taken under the active-remediation fix-now bar (two lines, same module, verified in the same commit) and recorded here rather than left implicit. Note for the contract: §2.7's terminal vocabulary (fixed / accepted-risk / deferred) has no value for a refuted finding, which is why this row reads `rejected`. |
| 2 | high | src/scripts/adr_cite_check.ts:63,118,227 | `ADR_DIRS = ['docs/decisions', 'docs/adrs']` and its doc-comment claim the tool "reads the two [surfaces] that carry numbered ADRs and can therefore be cited as `ADR-NNN`." But the file-lookup regex at line 227, `(^|/)ADR-0*${num}[-.]`, requires a literal `ADR-` prefix in the path, while per-area ADR files under `docs/adrs/<area>/` are named `NNNN-<slug>.md` with **no** `ADR-` prefix at all (the exact convention this same roadmap's Phase-2 author documents in `src/skills/adr-create/SKILL.md`: "Per-area — 4-digit numbering (`NNNN-<slug>.md`)", cited elsewhere as `ADR-<area>-NNNN` only in cross-reference *values*, never in the filename). Consequently, `cite_check` can never resolve a citation to any per-area ADR — every such call falls through to `resolved: false` / `UNRESOLVED`, which is precisely the "false not-found" failure Phase 2 Step 2 was written to prevent ("A tool that silently sees one of six surfaces reports false 'not found'"). Worse, this gap is not disclosed: `SURFACES_NOT_SCANNED` (lines 65-69) lists only `docs/contracts/adr-*.md`, `agents/settings/contexts/adr-*.md`, and `agents/decisions/` as deliberately uncovered — `docs/adrs` is claimed as covered when it functionally is not. | fixed | Confirmed and fixed. Per-area files are `<area>/NNNN-<slug>.md` with no `ADR-` prefix, so the lookup could never match one while the directory was advertised as scanned. `normalise_ref` now returns `{id, area, num}` and resolves by path and by the `ADR-<area>-NNNN` citation form; a bare number stays flat-only because per-area numbering restarts per area (five files are `0001`). New `PARTIAL_COVERAGE` export states the residual limit: per-area headers are quote blocks, not YAML, so metadata reads empty. 4 new tests. |
| 3 | high | src/rules/decision-revisit-gate.md:111,114 vs docs/contracts/adr-layout.md:109,112 | Phase 3 Step 5's exit criterion requires the rule's compact owner-reserved-set table to match the contract's "same rows, same order." It doesn't: row 2 in the rule reads "**Lowers** a recorded security / privacy / safety / data-handling floor," dropping "or removes" from the contract's "**Lowers or removes** a recorded security, privacy, safety, or data-handling floor" (adr-layout.md:109) — a transition that eliminates a floor outright is arguably not a "lowering." Row 5 in the rule reads "Creates / removes / weakens a legal, contractual, licensing, compatibility, or public commitment," dropping "regulatory" from the contract's "legal, regulatory, contractual, licensing, compatibility, or public commitment" (adr-layout.md:112). Since the rule is the routing surface explicitly built to be consulted "without loading the contract," these two omissions narrow the owner-reserved trigger on both a security- and a compliance-relevant row relative to the document that is supposed to be authoritative. (The byte-identical `dist/agent-src/rules/decision-revisit-gate.md` projection carries the same drift.) | fixed | Confirmed and fixed. The rule table now reads **Lowers or removes** and `legal, regulatory, contractual, ...`, matching adr-layout.md word for word on both rows. |
| 4 | medium | tests/scripts/check_adr_frontmatter.test.ts | The new test file (added in this diff, per its own header comment "the pre-existing checks ... had no test file at all") tests `split_dimensions`, `check_reopen_authority`, and `check_one` — but never imports or exercises `check_amendment_shape` or `check_amendment_links`, the two functions implementing "the bidirectional check the supersession pair is missing" that Phase 4 Step 2 explicitly asked for. The roadmap's own verify command for that step (`npx vitest run tests/scripts/check_adr_frontmatter.test.ts`) therefore provides zero unit coverage of the exact logic responsible for finding #1 above; it only exercises the corpus-level path indirectly (and non-deterministically, since it depends on the real `docs/decisions/` tree) when the CLI itself is run, not via the test suite. | fixed | Confirmed and fixed. 8 new unit tests over `adr_number`, `check_amendment_shape` and `check_amendment_links`, including the annotated `amends: 105 (...)` form that finding 1 turned on. |
| 5 | low | dist/agent-src/skills/adr-create/SKILL.md:64 | This diff "fixes" the link text from `scripts/audit_adr_coverage.ts` to `src/scripts/audit_adr_coverage.ts` and changes the href from `../../../scripts/audit_adr_coverage.ts` to `../../scripts/audit_adr_coverage.ts`. The new href still does not resolve: from `dist/agent-src/skills/adr-create/SKILL.md`, `../../scripts/audit_adr_coverage.ts` points at `dist/agent-src/scripts/audit_adr_coverage.ts`, but `dist/agent-src/scripts/` only carries 6 curated files (`archive_completed_roadmaps.ts`, `gate_execute.ts`, `resume_probe.ts`, `roadmap_gates.ts`, `scan-seeder-violations.php`, `update_roadmap_progress.ts`) and does not include `audit_adr_coverage.ts`. The identical edit in `src/skills/adr-create/SKILL.md` is correct there (`src/scripts/audit_adr_coverage.ts` exists), but the same relative path is wrong once mechanically applied to the `dist/agent-src` mirror. The link was already broken before this diff (at a different depth, `dist/scripts/...`, which also doesn't exist) so this is not a newly-introduced break, but the "fix" did not actually fix the dist copy. | fixed | Confirmed, fixed differently than proposed: no relative href resolves in BOTH `src/skills/` and the `dist/agent-src/skills/` projection, whose sibling `scripts/` carries six curated files. The reference is plain text naming the path, with that reason stated inline. |
| 6 | low | docs/decisions/adr-reopen-sweep-2026-08.md:85 | The reference-style link target `[adr-layout-reopen-authority]: ../contracts/adr-layout.md#reopen-authority` is reused for both the "§ Reopen authority" citation and the "§ Reopen record" citation earlier in the file. `docs/contracts/adr-layout.md` has "The reopen record" as its own `###` subsection (distinct from the `##` "Reopen authority" heading), which would auto-generate the anchor `#the-reopen-record`. Following the "Reopen record" link lands the reader at the top of the parent "Reopen authority" section rather than scrolled to the reopen-record schema itself. Cosmetic — the target file and general section are correct, only the specific-subsection anchor is imprecise. | fixed | Confirmed and fixed. Separate `[adr-layout-reopen-record]` target pointing at `#the-reopen-record`. |
