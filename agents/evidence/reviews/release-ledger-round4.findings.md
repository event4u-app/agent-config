# Findings: release-ledger-round4

<!-- completion-review: v1 | reviewed: 2026-09-11 | scope: 4c7447c9a710960160467b4de3dd70e238c388775b47af3a51abf163d30174ba | diff: 582e34ca55ab348e600cd576b061b4a35b0bc010 | reviewer: fresh-subagent-release-ledger-round4 | prompt_hash: d0326ce1006572d8303b1d85cf98b38e5163ed23d4f9820d216de53d0c5e62ac -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["fresh-subagent-release-ledger-round4"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-11 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 582e34ca55ab348e600cd576b061b4a35b0bc010
  scope_hash: 4c7447c9a710960160467b4de3dd70e238c388775b47af3a51abf163d30174ba
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, implementation-context]
tools: [git-diff-range-scoped, file-read-branch-paths, scratch-git-repo]
dispatched: 2026-09-11T07:52:00Z
-->

Scope: `git diff 7d1d33cfe..HEAD`, the four commits of the fourth attempt at the
findings-ledger mechanism. `scope_hash` is the sha256 of that diff text and
`prompt_hash` the sha256 of the committed prompt beside this file — both
computed here rather than by `dispatch_r2_reviewer`, because this round was
dispatched by hand. The reviewer read the three prior rounds' artefacts, ran
read-only git commands, and measured the non-fast-forward path in a scratch
repository under `/tmp`.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/release.ts:1034-1043 · src/scripts/_lib/release_findings_ingest.ts:71-73 | Step 7's "is it on the REMOTE" question reads a **local remote-tracking ref that nothing in the run refreshes after the ledger is committed**, so on a first run it can never see the file the mechanism produces. `ledgerOnBranchArgv` returns `cat-file -e origin/<branch>:<rel>` (`release_findings_ingest.ts:72`) — a read of `.git`, not of GitHub. The only fetch of that ref on a first run is preflight's `git fetch REMOTE --tags --prune --force` (`release.ts:871`), which happens before step 1 and therefore before the branch exists, plus step 4's `git push -u` (`release_publication.ts:750`), which sets `origin/<branch>` to the pushed commit. `integrate_main_if_behind` fetches only `MAIN_BRANCH` (`release_publication.ts:670`) and is not reached on a freshly created branch (`:648-655`). The CI ledger commit lands on the remote strictly after that push, so `origin/<branch>` is stale by exactly that commit and `cat-file -e` exits non-zero → `die(ledgerAbsentMessage(...))` at `:1039-1043`. The docstring at `:1001-1005` ("The REMOTE, because that is the ref the merge reads") asserts a property this argv does not have. Observable consequence, worst on the path the runbook calls normal: `release.yml:186-198` runs `--ci --yes` from a fresh `main` checkout, waits at step 6 until the ledger is committed and dispositioned, then stops at step 7 with "not on origin/release/X.Y.Z" and an instruction (`git pull`, resume) the runner cannot follow — `release.yml:169-183` states `--resume` "recovers a crash AFTER the release PR merged … but not a crash BEFORE merge". So every CI-native release now ends with an open, unmerged release PR requiring a local resume, which is the unattended path the change exists to create. A `git fetch REMOTE <branch>` before the probe, or `git ls-remote --exit-code`, is what the stated question needs. No test can see it: the drill answers every `git cat-file -e` from one boolean (`release_drill.ts:507-509`). | open |  |
| 2 | high | src/scripts/release.ts:1051-1055 · src/scripts/check_finding_dispositions.ts:484,504,651-654 | The step's two questions are asked of **two different refs**, and this diff's own `--pr` addition converts that into a false failure. Question 1 reads a ref (`:1036`); question 2 runs `./scripts-run src/scripts/check_finding_dispositions` through `run()`, whose cwd is `REPO_ROOT` (`release_publication.ts:93`), and the gate resolves its ledger under `REPO_ROOT` (`check_finding_dispositions.ts:484`, `:504`) — i.e. the **working tree**. Nothing between step 4's push and step 7 brings the CI ledger commit into the working tree on a first run (`push_release_branch` only fetch-merges on a non-fast-forward, `release_publication.ts:765-778`). With `--pr` now passed on a first run (`release.ts:1291`, `:1053`), `unrecorded_findings` (`check_finding_dispositions.ts:654`) compares the newest self-review comment against an **empty** ledger and reports every blocking finding as missing → exit 1 → `die(dispositionStopMessage(...))` at `:1057-1067`, on a release whose remote ledger is complete and fully dispositioned. Before this diff the flag was dropped on a first run and the absent-file path passed (`:568-592`, unreleased ⇒ allowed), so the ref mismatch was latent; it is now load-bearing. The drill cannot catch it either — the world answers `check_finding_dispositions --release` from a fixed verdict (`release_drill.ts:510-512`) regardless of what is on disk, and the new `resume: false` scenario (`:548`) asserts only that `--pr ` appears in the argv (`:583-589`). | open |  |
| 3 | high | .github/workflows/self-review-gate.yml:169-172,282-285 · :66-84 | The recursion bound discards the findings of the run it triggers, and nothing can ever ingest them — so `finding-dispositions` can be red with no automated remedy. The PAT push creates a `pull_request: synchronize` run (that is the stated point, `:258-266`). In that run `live-advisory` has **no** head-subject guard and re-runs the full paid review against the base (`:84`, `--base origin/${{ github.base_ref }}`), posting a new comment (`self_review_gate.ts:670-682`, `gh pr comment`, always a new comment). `parse_comment_findings` takes the **newest** machine block (`check_finding_dispositions.ts:447-457`), so that second review is what `finding-dispositions` compares the ledger against. But both ledger jobs exit 0 on the head-subject check (`:169-172`, `:282-285`), so nothing ingests it — and every later run on that unchanged head does the same. Any finding whose id differs is then `unrecorded_findings` red forever: ids are `sha256(kind\|title\|file)` over an LLM-authored title (`self_review_gate.ts:62-67`), and the second run's diff contains a file the first never saw (`agents/evidence/release-findings/<v>.json`), which the workflow's own comment concedes at `:165-168` — understated as "one marginal finding" when a blocking one is a hard block. The only exits are a hand-written ledger or the manual `--ingest`, i.e. the six-release failure this change retires. Nor is convergence guaranteed once a human does push: run N+1 ingests run N+1's findings, adds new empty dispositions, commits and pushes again (`:326-338`), so the fill→push→re-ingest loop terminates only when two consecutive LLM reviews emit identical titles. | open |  |
| 4 | high | .github/workflows/self-review-gate.yml:342-357 | The retry path **silently reverts a maintainer's dispositions** — the very scenario its own comment names as the reason it exists ("a maintainer pushing a disposition fix", `:342-343`). On a rejected push it does `git fetch origin "$HEAD_REF"`, `git reset --hard "origin/$HEAD_REF"`, then `place_and_stage`, which `cp`s the whole ledger from `/tmp/ledger/$version.json` (`:314`) — the file the ingest job built against the tip it checked out, before the maintainer's commit. `merge_ingest` preserves existing dispositions (`check_finding_dispositions.ts:170-177`), but that merge happened in the other job against the OLD ledger, so the replay is a whole-file overwrite of the new one; `:344-345` states this as a virtue ("a whole file from the artifact, so replaying it needs no merge"). Measured in a scratch repo: upstream `{"ledger":1,"status":"fixed by maintainer"}`, reject → `fetch` (confirmed to advance `origin/<branch>` under the default refspec) → `reset --hard` → `cp` → `git diff --cached --quiet` exits 1 → commit → push exits 0 → upstream is `{"ledger":1}`. The human adjudication the whole mechanism exists to produce is discarded, `finding-dispositions` goes red again, and the run log says only "push rejected — rebuilding the ledger commit on the current tip". Re-running the ingest against the new tip, or merging field-wise, is what a replay here needs. | open |  |
| 5 | high | src/scripts/release_publication.ts:231-238 · src/scripts/release.ts:1270-1276 | The message the operator actually sees on the canonical path prescribes the manual command this change retires. `release.ts:1271-1276` now concedes that a red `finding-dispositions` stops the release at step 6 rather than step 7 — which is guaranteed, because the workflow commits the ledger with **empty** dispositions by design (`self-review-gate.yml:235-238`) and `finding-dispositions` has no `continue-on-error` (`release-validation.yml:356-390`), so `watch_pr_checks` dies at `release_publication.ts:405`. The remediation block it prints for that check still reads "Ingest + adjudicate it in … `./scripts-run src/scripts/check_finding_dispositions --ingest <findings.json>`" (`:231-238`) — a hand-ingest that now races the CI job for the same file and that `docs/release-runbook.md:159-168` tells the operator not to do. Step 7's replacement text (`dispositionStopMessage`, rebuilt this round to carry the gate's stderr and a blocking count, `release.ts:1057-1067`) is unreachable on the wait path; only `--no-wait` reaches it. | open |  |
| 6 | medium | .github/workflows/self-review-gate.yml:66-84,169-172 | Every release now pays for one full `ANTHROPIC_API_KEY` review that is guaranteed to be thrown away. The PAT push re-enters the workflow; `live-advisory` carries no head-subject guard (`:66-84`) and runs the real review with spend, while both ledger jobs exit before touching its artifact (`:169-172`, `:282-285`). The workflow header names the spend as an open maintainer blocker (`:13-16`, `self-review-gate-cost`) and the concurrency group (`:27-29`) cannot prevent it — the new run is what cancels the old, not the reverse. Guarding `live-advisory` on the same head subject, or gating the second run's review off, is the whole saving. | open |  |
| 7 | medium | src/scripts/_lib/release_findings_ingest.ts:166 · src/scripts/release.ts:1297-1304 | On the newly-enabled merged path the disposition-refusal message instructs an impossible action. `settle_findings_ledger` now runs the gate when `merged` is true (`release.ts:1297-1304`), and on a refusal `dispositionStopMessage` closes with "Resolve what the report names, **push the release branch**, then: task release -- --resume --yes" (`release_findings_ingest.ts:166`). The branch does not exist: step 8 merges with `--delete-branch` (`release.ts:1309`), and the sibling message written this round for the same state gets it right ("The release branch is gone … Write the ledger on the trunk instead", `:130-132`). Reachable exactly where the diff says it is — the ingest no-ops, step 7 stops, the PR is merged by hand with an undispositioned ledger — leaving `main` bumped, untagged, and an operator told to push a deleted ref. | open |  |
| 8 | medium | src/scripts/release_drill.ts:507-509,619-643 | The new scenario cannot test the property it is named for. `findings-ledger-absent-after-a-hand-merge-still-stops` (`:619`) asserts that the merged path "asked the trunk whether the ledger is there" by checking that some `git cat-file -e ` call contains `/main:` (`:635-639`) — but the world answers **every** `cat-file -e` from the single `ledger_on_branch` boolean regardless of ref (`:507-509`), so the two states that distinguish right from wrong (on the branch but not the trunk; on the trunk but not the branch) are inexpressible. What is pinned is the string in the argv, not the ref that was read — which is also why finding 1 above is invisible to the drill. Round-2 finding 9 named the same missing knob ("the world has no knob"); it was not added. The third assertion is weaker still: `!w.tag_remote` (`:640`) holds for any run that dies anywhere before step 9. | open |  |
| 9 | medium | src/scripts/release_drill.ts:644-672,315,335 | `findings-ledger-undispositioned-stops-and-shows-why` asserts a step-7 stop the real system cannot reach, because the world models the check wait and the disposition verdict as independent. `checks_fail` defaults to false (`:315`, `:335`) while `disposition_verdict` is set to status 1 (`:647-653`), so `gh pr checks --watch` returns success (`:440-444`) on the exact state that makes `finding-dispositions` — a check on the same PR — red. In production that ordering dies at `release_publication.ts:405` with "PR checks failed", as `release.ts:1271-1276` itself now states. The scenario therefore certifies the step-7 message on a state where step 7 does not run, and there is no scenario asserting the coupling. | open |  |
| 10 | medium | .github/workflows/self-review-gate.yml:308-336 · tests/scripts/release_findings_ingest.test.ts:66-72 | The job holding the write credential commits artifact bytes it never validates, and the test forbids it from validating. `commit-release-ledger` downloads `release-ledger` (`:309`) and `cp`s it straight into `agents/evidence/release-findings/<v>.json` (`:314`), then commits (`:336`); the test asserts the job contains no `./scripts-run`, no `setup-node`, no `npm ci` (`test:66-72`). The artifact is produced by `ingest-release-ledger`, which runs `npm ci` from the checked-out release branch (`:146`) — i.e. third-party lifecycle scripts. The split correctly keeps the token away from that install, but it leaves the **content** path unguarded: a compromised dependency writes a ledger whose findings all carry plausible `{status, rationale, verified_by}`, `missing_dispositions` and `unrecorded_findings` both pass (`check_finding_dispositions.ts:651-654`), and the release ships with a forged audit record. A schema/shape check on the downloaded file, or a signature the build job cannot forge, is the missing half; it does not require reinstating `npm ci` in this job. | open |  |
| 11 | medium | docs/succession.md:65 · docs/release-runbook.md:149,154-158,317-320 | Both docs name the wrong job as the one that pushes and the one that needs the PAT, and the staleness grep added in the same commit cannot catch it. `succession.md:65` says "the `ingest-release-ledger` job refuses to push without it"; `release-runbook.md:149` says that job "commits it to the release branch" and `:154` that "*That job needs `RELEASE_PR_TOKEN`*". `ingest-release-ledger` declares `contents: read` (`self-review-gate.yml:134-136`), checks out with `persist-credentials: false` (`:141`), reads no secret, and contains no `git push`; every one of those belongs to `commit-release-ledger` (`:251-267`, `:276`, `:338`). The new gate at `release-runbook.md:319-320` greps the workflow for the literal `RELEASE_PR_TOKEN`, which is present, so the wrong attribution passes — and an operator debugging a missing ledger will read the run log of a job that was never involved. | open |  |
| 12 | low | docs/release-runbook.md:189-197 | §3.A, the route the doc labels "normal", still promises the CI-native run reaches green unattended — "Watch the `release.yml` run to green. It merges the release PR, tags, cuts the GitHub Release" (`:196-197`) — while §2 step 7, edited in this same commit, adds a human checkpoint that path cannot resume through (finding 1; `release.yml:169-183`). Step 3 (`:189-195`) compounds it: it presents the approval click as the only thing `RELEASE_PR_TOKEN` buys ("Manual checkpoint (only without `RELEASE_PR_TOKEN`)"), a claim the same file's own succession table records as measured false on 14.0.0, and which now also omits that without the PAT no ledger is pushed at all. Round-2 finding 10 named §3.A; it is unchanged. | open |  |
| 13 | low | .github/workflows/self-review-gate.yml:6-11 | The workflow header still declares a property the pipeline no longer has: "ADVISORY + INERT-WITHOUT-SECRET … No key → logged no-op, never a failure" (`:9-11`). With no `ANTHROPIC_API_KEY` the review writes no `--findings-out` file, `upload-artifact` carries `if-no-files-found: ignore` (`:104`), the ingest job takes the "nothing to ingest" exit (`:183-189`), no `release-ledger` artifact is built, and step 7 stops every release with `ledgerAbsentMessage`. True of the two advisory jobs, false of the release that now depends on them — the same contradiction round-2 finding 3 raised. The runbook states the cost (`:161-166`); this header, which the permissions comment right below it was rewritten this round, does not. | open |  |
| 14 | low | .github/workflows/self-review-gate.yml:309,347,348,354 | Four unguarded commands under `set -euo pipefail` (`:278`) convert the deliberate stand-down into the red check it exists to avoid. `:358-362` explains that a failing check here makes the release "die naming a bot job instead of naming the ledger", and the two pushes are correctly wrapped in `if` (`:338`, `:355`) — but `gh run download` (`:309`), `git fetch origin "$HEAD_REF"` (`:347`), `git reset --hard "origin/$HEAD_REF"` (`:348`) and the second `git commit` (`:354`) are not. A transient artifact-download failure, or a fetch failure, exits the job non-zero, `watch_pr_checks` reports "Commit the findings ledger (release PRs)" as failing (`release_publication.ts:395-405`), and `_failed_checks_report` has no line for that name — exactly the confusing failure the comment rejects. | open |  |
| 15 | low | .github/workflows/self-review-gate.yml:266-267,287-296 | The PAT fallback is dead and leaves a write-capable credential on disk for a job that then does nothing. The checkout takes `token: ${{ secrets.RELEASE_PR_TOKEN \|\| github.token }}` with `persist-credentials: true` (`:266-267`), but when the PAT is absent the first shell step exits 0 before any git write (`:287-296`) — so in that configuration the job's only effect is to persist `GITHUB_TOKEN` with `contents: write` (`:252`) into `.git/config` and quit. Gating the checkout itself on `secrets.RELEASE_PR_TOKEN != ''`, or dropping the fallback, removes a credential the job has no use for, in the one workflow whose permissions comment cites `road-to-ci-supply-chain-integrity` (`:31`, `:124-129`). | open |  |
