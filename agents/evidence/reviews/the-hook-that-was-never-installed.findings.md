# Findings: the-hook-that-was-never-installed

> **Round 2** of the R2 completion review. The eleven rows below were written
> by a fresh blind reviewer against head `6b7e6c2a6`; the header is rebound to
> the current head because the fixes those rows asked for are now IN the scope,
> and every disposition cites the commit that closed it. Round 1, and the
> reason its rows were NOT re-pointed this way, is in the sibling
> `.round1.findings.md`.
>
> Rebound artefact-only against `origin/main`, which is the base the gate
> resolves. Binding against a pinned older sha is what made the dispatcher and
> `check_completion_review` disagree on the scope hash across three attempts.
<!-- completion-review: v1 | reviewed: 2026-09-05 | scope: 69e98156c44d0620ffa57c72c88c355bff477b5c65a704302fdf549f42c869b4 | diff: beef00285bf60d25c471fc9eb9485a4906b3cd15 | reviewer: r2-fresh-subagent-the-hook-that-was-never-installed | prompt_hash: 53bb51660f6af5b686e8246a88970ca42c19b99d584a723c2b63e82522310bd2 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-the-hook-that-was-never-installed"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-05 -->

<!-- context-manifest: v1
inputs:
  diff_sha: beef00285bf60d25c471fc9eb9485a4906b3cd15
  scope_hash: 69e98156c44d0620ffa57c72c88c355bff477b5c65a704302fdf549f42c869b4
  roadmap: agents/roadmaps/archive/road-to-the-hook-that-was-never-installed.md
  roadmap_hash: 5444239b2d926f568afe8d3d35a8866abc6203a15611f119b0b161aea1f9d6eb
  ac_hash: 443f1d62fce313e67684d11973d07f9326ea0f0e4d346273884db751471e96d4
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-05T14:32:17Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/check_installed_hooks_fresh.ts:32 | Module docstring states the pre-push binding is "blocking, with AGENT_CONFIG_SKIP_PREPUSH_HOOKFRESH=1 to bypass". The shipped carrier is advisory: install-hooks.sh:193 says "nothing here sets fail=1 or exits", docs/development.md marks it "Advisory — it does not refuse", and AC-1 records the de-blocking as the corrected behaviour. The gate's own contract header is where a maintainer reads whether a push can be refused, and it asserts the property that was reversed. | fixed | 89305f333 — docstring rewritten: "Two carriers, and NEITHER REFUSES", with the non-unique-predicate reason and the council date. |
| 2 | medium | src/scripts/install-hooks.sh:516-517 | Only post-merge and post-checkout receive the auto-sync block, so only they carry the detector. `git pull --rebase` runs neither — it fires post-rewrite, which gets the chat-history body only (:426) and no detector. AC-2 claims the drift is "reported at the pull or branch switch that caused it"; under a rebase-pull configuration nothing reports until the next push, and no prose in the diff names the exclusion. | fixed | 89305f333 — named, not closed. The rebase-pull exclusion is written into the gate docstring, docs/development.md and AC-2. post-rewrite also fires on every `git commit --amend`, so wiring it there trades a real gap for a notice on an operation that cannot have moved the installer; the push-time carrier still catches it. |
| 3 | medium | src/scripts/check_installed_hooks_fresh.ts:104-110 | resolveHooksDir composes `--git-common-dir` + "/hooks" and therefore ignores core.hooksPath; `git rev-parse --git-path hooks` is the primitive that honours it. With core.hooksPath set, the gate compares a directory git never executes and can report "6 installed hook(s) match" while the hooks that actually run are unrelated — the vacuous-green class the docstring claims to avoid. install-hooks.sh:23-38 shares the blind spot, and core.hooksPath is the setting this change names as its own reopen condition. | fixed | 89305f333 — resolveHooksDir now uses `git rev-parse --git-path hooks`. Verified it returns the SHARED hooks dir from this linked worktree (identical to the old composition), pinned by a new test. The core.hooksPath branch is git-documented and NOT exercised: this repo's block-no-verify guard refuses any command carrying core.hooksPath, read-only probes included — it blocked the probe twice. Stated in the code. |
| 4 | medium | src/scripts/install-hooks.sh:23-32 | AGENT_CONFIG_HOOKS_DIR is an unannounced global destination override. Only the set-but-empty case is refused; a stray or leftover non-empty value silently redirects a real `task install-hooks`, which then prints six "hook installed" lines (:282, :386, :420) without ever naming the destination. The freshness gate afterwards compares .git/hooks and reports every hook missing, with nothing pointing at the cause. Echoing HOOKS_DIR once would remove the failure mode. | fixed | 89305f333 — the installer echoes its destination once before writing. |
| 5 | medium | agents/roadmaps/archive/road-to-the-hook-that-was-never-installed.md:AC-1 | Evidence line citations do not resolve. AC-1 cites the pre-push carrier at install-hooks.sh:98-132 — the block is at 149-194, while 98-132 is the delete-only and base-freshness region — and the auto-sync carrier at :476-494, which is at 492-511. Step 1.2 cites guards at :116 and :491-492, actually 176-178 and 507-508. A reader verifying AC-1 lands on the other gate, which is exactly the distinction the R2 correction turned on. | fixed | a4f75c7a2 — already corrected one commit before this round ran; the round read a package built at the prior head. Every cited range re-read at the commit: :16-38, :149-194, :176-177, :451-515, :492-510. |
| 6 | low | tests/scripts/check_installed_hooks_fresh.test.ts:350 | `expect(r.hookAfter).not.toContain("AGENT_CONFIG_HOOKS_DIR")` is tautological: that string lives in the installer preamble and never in a rendered hook body, so it passes whether or not a repair rewrote the hook. It is presented as pinning the refused mutation; only the adjacent hookAfter === hookBefore assertion actually does. | fixed | 89305f333 — the tautology is gone, and the fixture now carries a stand-in installer that rewrites ./post-merge, so adding a real repair to the carrier reds this test. Proven by doing exactly that. |
| 7 | low | tests/scripts/check_installed_hooks_fresh.test.ts:249-250 | The "never promoted back to a blocker" pin is form-specific. `/^\s*exit 1\s*$/m` and `/^\s*fail=1\s*$/m` do not match `exit 1  # blocked`, `exit "$hookfresh_rc"`, `return 1`, or `fail=$hookfresh_rc`, so the advisory property can be undone with the test still green — and the roadmap counts this test as the sabotage-proven pin for that property. | fixed | 89305f333 — the pin is token-level over the block's code lines (comments excluded so the explanatory paragraph cannot trip it). `exit "$hookfresh_rc"`, `return 1` and `fail=$hookfresh_rc` each red it, verified one at a time. |
| 8 | low | src/scripts/check_installed_hooks_fresh.ts:122-135 | The guard comment claims it defends against "a redirect that does not take effect", but the check inspects only `stage`, which mkdtempSync produced two lines earlier; it cannot observe whether the env var reached the child. The real defence is the installer's set-but-empty refusal. On a path whose stated risk is overwriting the pre-push that is running it, a comment overstating the guard is worse than none. | fixed | 89305f333 — comment corrected to say what it checks (the local stage path) and to name the installer's set-but-empty refusal as the half that can see the child. |
| 9 | low | src/scripts/check_installed_hooks_fresh.ts:175 | `(fs.statSync(target).mode & 0o111) !== 0` is POSIX-only. Node on Windows reports 0o666 or 0o444, so every managed hook resolves to `not-executable` and the gate exits 1 permanently. The test file explicitly accommodates win32 (tsx.cmd at :25), so the clean-direction test cannot pass on the one platform it claims to support. | fixed | 89305f333 — the executable check is skipped on win32, which has no executable bit. |
| 10 | low | agents/roadmaps/archive/road-to-the-hook-that-was-never-installed.md:1-14 | The archived frontmatter carries claims the file's own body refutes: `status: ready` on a fully-closed archived roadmap; estate_offset_exempt asserts "All twelve active roadmaps are unstarted" while step 1.3 records active_roadmaps 2; estate_growth_exempt claims the PR #1843 concern that 1.3 records as already inside main's floor and therefore having nothing to spend. | accepted-risk | 89305f333 — `status: ready` is this repository's archival convention, not a live claim: 340 of the archived roadmaps carry it, including the one archived the same day. Changing it here would diverge from the corpus. The two stale exempt fields are annotated in place instead of rewritten — they record what was claimed at authoring time, and the live readings are in step 1.3's DONE note. |
| 11 | low | src/scripts/check_installed_hooks_fresh.ts:105 and :136 | Neither spawnSync sets a timeout, and both run on the push path through the pre-push carrier. A wedged `git rev-parse` or a slow or unwritable TMPDIR leaves the push blocked with no bound and no diagnostic; the exit-2 "could not render" branch is only reached if the child returns at all. | fixed | 89305f333 — both spawnSync calls carry timeouts (git 5s, installer 30s) and a spawn error is reported as "did not complete" rather than swallowed. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
