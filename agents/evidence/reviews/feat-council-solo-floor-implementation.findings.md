# Findings: feat-council-solo-floor-implementation
<!-- completion-review: v1 | reviewed: 2026-08-11 | scope: 604bf21dcd9af9d373227b1de8209ffa3d4653746e04a9536327418ea3a4d9c9 | diff: 9cde092cf6c745c55d9468ebb4480ffb2a11259a | reviewer: r2-fresh-subagent-feat-council-solo-floor-implementation | prompt_hash: d98064c8db88c5a22ffd0c04670c5c583e1f0ea9dea2f5a1c4389a8cef3c7a45 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 9cde092cf6c745c55d9468ebb4480ffb2a11259a
  scope_hash: 604bf21dcd9af9d373227b1de8209ffa3d4653746e04a9536327418ea3a4d9c9
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-11T21:07:09Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/ai_council/quorum.ts:169 | The floor is clamped against `result.total`, so it is structurally unable to fire on a construction-degraded pass — the exact degraded solo case ADR-224's 12.5 % rate counted. `_postRunQuorum` sets `total = members.length` (council_cli.ts:742-746, and the comment at council_cli.ts:2663-2667 names this shape explicitly), so a 2-member council where one member fails to construct produces post_run `total=1, present=1` and `floor = min(2,1) = 1`, giving `floor_would_hold: false`. The same pass emits `floor_would_hold: true` on its `pre_run` line, where `total = total_enabled = 2` (council_cli.ts:1035-1046) — one pass, two contradictory records, and `shadow_floor_fire_rate` filters `post_run`, so the registered metric reads the "no". `configuredTotal` is in scope at the emit site (events_log.ts:357) and is not passed to the predicate, so the distinction the line already carries is discarded by the only consumer that needs it. Net effect: the series ADR-224 review trigger (b) is meant to accumulate is biased low by construction, and no test covers the `configuredTotal > total` with `present === 1` shape. | fixed | 9cde092cf — ceiling is now max(total, configured_total); 3 tests added for the shape nothing covered |
| 2 | medium | src/config/quorum-attendance-budget.json:27 | The published `shadow_floor_fire_rate` definition states the divergence from `solo_conclusion_rate` is "a council configured with one member is solo: true and floor_would_hold: false, deliberately" — i.e. one case, deliberate. Falsified in both directions: a construction-degraded roster (3 configured, 1 constructed) is also `solo: true` / `floor_would_hold: false` and is not deliberate (finding 1); and with `quorum_min_present: 3` a `total=3, present=2` pass is `solo: false` / `floor_would_hold: true`, a divergence the sentence does not admit exists. An analyst scoping the metric from this definition will mis-attribute both. | fixed | 9cde092cf — definition restated with both divergence directions and the agreeing case |
| 3 | medium | agents/roadmaps/archive/road-to-council-solo-floor-implementation.md:15,19,27 | Three relative links were not re-depthed when the file moved into `archive/`. From `agents/roadmaps/archive/`, `../../` resolves to `agents/`, so `../../docs/decisions/ADR-224-…`, `../../src/config/quorum-attendance-budget.json` and `../../src/scripts/ai_council/quorum.ts` all point at paths that do not exist. Proven by the sibling in the same directory: `agents/roadmaps/archive/road-to-inbox-harvest-2026-08-b-council-integrity-followup.md:73` links the same ADR as `../../../docs/decisions/…`. The inbound link in that sibling was correctly re-depthed in this diff; the moved file's own outbound links were not. | fixed | 9cde092cf — three links re-depthed to ../../../ |
| 4 | medium | docs/decisions/ADR-224-gate-scoped-solo-attendance-floor.md:3 | The record still carries `status: proposed` and its Status section (lines 29-34) still asserts "This record changes no runtime behaviour by itself — it decides, and names the implementation as separate work. Acceptance is the maintainer's call." The same file now carries an amendment describing landed runtime behaviour: a new operator-facing config key, two new event fields, and a wire-schema bump v2 to v3. A proposed ADR documenting its own shipped implementation leaves a reader unable to tell whether the decision was accepted or the implementation pre-empted acceptance. | deferred | accepting ADR-224 is the maintainer call the record itself names; an implementing agent cannot self-grant it. Real inconsistency, left visible rather than resolved by the party with an interest in it. |
| 5 | low | src/scripts/ai_council/config.ts:611 | `quorum_min_present` is a new operator-facing `.ai-council.yml` key, and the only surface updated for it in this diff is `config.ts`'s own header block (item 9b). No template, example config, or council-config contract document is touched by the change, so any such surface that exists is now missing the key and an operator has no discovery path outside the source file. | fixed | 9cde092cf — documented in docs/contracts/ai-council-config.md, the one operator-facing surface it existed on |
| 6 | low | src/scripts/ai_council/config.ts:1942 | The docstring justifies the guard with "booleans are integers in JS's `typeof` world and are excluded explicitly" — false for JS/TS (`typeof true === 'boolean'`); this is a Python-ism, where `bool` really is an `int` subclass. `_isInt` (config.ts:316) already requires `typeof value === 'number'`, so the `!_isBool(raw)` conjunct at config.ts:1950 is dead. The guard shape pre-exists at `_build_quorum`, but the false rationale is new in this diff and will propagate to the next author who copies it. | fixed | 9cde092cf — false Python-ism rationale replaced, dead conjunct dropped from the new validator |
| 7 | low | src/scripts/ai_council/config.ts:79 | `config.ts` now takes a value import from `./quorum.js` while `quorum.ts:27` imports `QuorumSetting` back from `./config.js`, inverting the layering both module headers document (`quorum.ts` describes itself as the pure consumer of config's validated shape). It is safe only because the back-edge is `import type` and is erased. Converting that one line to a value import — or adding any runtime import from quorum to config — creates a real ESM cycle in the loader's initialisation path. Worth an explicit note at the import, since nothing in the tree records the constraint. | fixed | 9cde092cf — constraint noted at the import |
| 8 | low | src/scripts/ai_council/events_log.ts:317 | `QuorumEventInput.gateClass` has no producer and no reachable way to be set: `_emitQuorumEvent`'s ctx object deliberately omits it (council_cli.ts:702-708), so `gate_class` is hard-wired `false` on every line the tree can emit. The rationale for writing the *field* is recorded and sound; the rationale for shipping an *input parameter* nothing can supply is not, and it will read as a wiring bug to the next reader who tries to set it. | fixed | 9cde092cf — documented as having no producer BY DESIGN, naming which consumer becomes the first |
| 9 | low | tests/scripts/ai_council/events_log.test.ts:129 | The describe block still reads "events_log — quorum attendance (schema v2)" while the suite it contains now pins the wire version at 3 and asserts the two v3-only fields. Stale label on the block whose entire subject is the version. | fixed | 9cde092cf — describe title now reads schema v3 |
| 10 | low | agents/roadmaps/archive/road-to-council-solo-floor-implementation.md:3 | `parent_roadmap: archive/road-to-inbox-harvest-2026-08-b-council-integrity-followup.md` was correct while the file lived in `agents/roadmaps/`; it was not adjusted on the move into `archive/`. The re-depthing applied to the sibling's inbound link in this same diff resolves paths relative to the file's own directory, under which this value now points at `agents/roadmaps/archive/archive/…`. Flagged as uncertain rather than asserted: the resolver for this frontmatter key is outside the review scope and may anchor at `agents/roadmaps/` instead. | accepted-risk | the key takes a SLUG per src/agent-src/templates/roadmaps.md, no resolver for it exists anywhere in src/, and the value was already path-shaped before this diff. Editing an inert field would be churn; the reviewer flagged it uncertain and the uncertainty resolves to no-change. |
