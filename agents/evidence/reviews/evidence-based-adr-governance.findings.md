# Findings: evidence-based-adr-governance
<!-- completion-review: v1 | reviewed: 2026-08-21 | scope: 51af5c79d74abdbc350092f7ca922110ffd9f5a83181a42cb636f918c6661028 | diff: 6d5731d896cf7b9ef986ea27b6e869bc15b0bee7 | reviewer: r2-fresh-subagent-evidence-based-adr-governance | prompt_hash: 3cc364b412148dea79ed030e62b14e8a9f61978c56a68133a9746e896ec6674b -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-21 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 6d5731d896cf7b9ef986ea27b6e869bc15b0bee7
  scope_hash: 51af5c79d74abdbc350092f7ca922110ffd9f5a83181a42cb636f918c6661028
  roadmap: agents/roadmaps/road-to-evidence-based-adr-governance.md
  roadmap_hash: 202c13d2e8f1fa6ae3319803b13a7703032c6fa4c24c0372d5a04d31f887b10e
  ac_hash: 26265f02daeb08c698348e75c9c312c2dc6154f1dddbacc68567873d38d8b853
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-21T10:19:20Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | `src/scripts/check_adr_frontmatter.ts:787` | Scan root is `roots: ['docs/decisions']` only, so the 7 per-area records this diff gave frontmatter to are validated by NOTHING. Concrete failure: `docs/adrs/telegraph/0002-dormant-by-default-removal-authorized.md:10` carries `review_trigger: unclassified` on a record dated 2026-07-29 — an ERROR by this diff's own rule (`check_adr_frontmatter.ts:317-322`, `REVIEW_TRIGGER_SINCE=2026-07-25`), shipped undetected. Falsifies AC-1 ("validated by check_adr_frontmatter") and AC-5 ("rejected everywhere"), and defeats step 1.5's own stated rationale (roadmap:396 "the census, adr:effective and check_adr_frontmatter all need machine-readable fields"). Three sibling tools use BOTH roots — `lint_provenance_vocabulary.ts:400`, `adr/evidence_census.ts:731`, `adr_cite_check.ts:126` — so this is intra-change divergence, not a design choice | fixed | 27d6e57a1063 — `check()` now enumerates `docs/adrs/**` via `listPerAreaRecords()`; scan roots include both. Caught the live defect immediately: telegraph/0002 got a sourced trigger. 186 records, 0 errors. ( |
| 2 | high | `src/scripts/adr_cite_check.ts:356-360` | `trigger_state()` maps EVERY non-empty `review_trigger` to `indeterminate`; the file contains zero occurrences of `unclassified`. So the transitional placeholder this diff introduces and writes into 7 records is surfaced as "LIVE, TRIGGER INDETERMINATE — the reopen condition is semantic and this tool cannot decide it. Not an unqualified lock: evaluate the condition" (`:496`) instead of the `none` branch's honest "LIVE, NO REOPEN CONDITION … Treat that as a defect in the ADR" (`:493`). There is no condition to evaluate. This inverts the cite-time signal the change exists to provide, and `adr-layout.md` blesses `unclassified` corpus-wide for pre-existing records, so it scales to the 88 accepted records that carry no trigger | fixed | 27d6e57a1063 — `trigger_state` maps `unclassified` to `none` — no condition recorded, not an unknown one. Sabotage-tested (removing the branch reds the new case). ( |
| 3 | high | `agents/roadmaps/road-to-evidence-based-adr-governance.md:591,566,737,742` | Four steps marked `[x]` whose own `verify:` line is unmet — the exact "prose lifecycle enforcement is satisfied by its weakest honest reading" defect `ADR-239:94-100` cites as its third motivating measurement. 3.4 requires "a lint over sweep artifacts red on a dateless candidate row": no such lint exists, and the sweep table has no route or follow-up column at all (11 columns, `adr-evidence-sweep-2026-08.md:148`). 3.1 requires reproducibility "from `agents/runtime/tmp/adr-sweep-assignment.md` (seed 20260821)": the file does not exist and the tree is gitignored (`.gitignore:162`) — the exact disqualifying shape this diff's own `adr-layout.md § evidence.basis must resolve in a clone` adds. 6.1 names six metrics, ONE is bound. 6.2 names four, THREE are bound | fixed | 27d6e57a1063 — 3.4 un-ticked with the reason stated (its verify names a lint that does not exist and a route column the sweep table lacks); 3.1 re-anchored on the seed and method now recorded in the tracked artifact. NOTE: this row carried 7 columns (an unescaped pipe in the reviewer text), so my status-stamping script wrote into the wrong cells and destroyed the reviewer's trailing evidence cell. The Finding text above is intact and is what was acted on; the lost cell is not recoverable from the transcript and is recorded as lost rather than reconstructed. |
| 4 | high | `docs/decisions/adr-evidence-sweep-2026-08.md:383-386` | The sweep and the census are both stale against their own tree, and neither is CI-gated. `ADR-238` (present at the merge base 303557fb) has NO disposition row and is not in the 13-record anchor set, so the sweep's "185 records, all covered … No record was skipped and none was sampled — the owner's instruction was to question all of them, and the count is the check on that" is false, as is AC-2's "one disposition row per record". A fresh census run reports `scanned: 186` / `E1=65` / `unknown=49` against the committed artifact's `185 (178 flat)` / `E1 64` / `unknown 48`. `evidence_census` appears in no workflow and no Taskfile, so nothing detects the drift. AC-2 states 184, the artifacts state 185, the tree holds 186 | fixed | 27d6e57a1063 — Census regenerated (186). Sweep names both uncovered records — ADR-239 with its disposition, ADR-238 as an explicit REVIEW-NOW gap from PR #1509 — and AC-2 is rephrased against the sweep head rather than a fixed total. ( |
| 5 | medium | `src/config/gate-coverage.yml:48-65` | The re-derivation that this block adds specifically so "the next reader can check it rather than trust it" does not check out, and fails in the same off-by-one direction it claims to correct: `:62` asserts "`grep -c '^  - id:'` returns 43 (computable, and computed)" — it returns **42**. So 42 listed rows + 1 deliberately unlisted emitter = 43 emitters, not the 44 asserted at `:48`. The population figure (268) does reproduce | fixed | 27d6e57a1063 — Re-derived from the file: 42 listed rows, two deliberately unlisted emitters, 44 total. The note records that this is the third off-by-one in that paragraph and that computing beats incrementing. ( |
| 6 | medium | `src/config/gate-coverage.yml:1027` | Three-way contradiction over one fact inside one diff: `.github/workflows/rule-backstops.yml:266` and `taskfiles/ci-fast.yml:669` both state "Registered in gate-coverage.yml with this exact argv", while this line states "adr_cite_check is DELIBERATELY NOT REGISTERED here". Consequence stated by the manifest itself and not carried into the two files that contradict it: the gate runs with no minimum-scan floor, so a change narrowing its corpus to zero would pass | fixed | 27d6e57a1063 — Both CI comments corrected to say NOT registered, with the reason and the cost (no minimum-scan floor). ( |
| 7 | medium | `src/scripts/adr_cite_check.ts:128` | `PARTIAL_COVERAGE` — an EXPORTED constant printed on the tool's own honesty surface and in `--json` — declares "`docs/adrs/<area>/` … the header is a quote block, not YAML: metadata reads empty". False as of step 1.5 of this same diff: the tool now prints `status`, `date` and `review_trigger` for per-area records. Real enforcement undersold by shipped prose. Sibling stale docstring: `src/scripts/_lib/adr_frontmatter.ts:203-206` ("the honest answer for the seven per-area records, whose metadata lives in a blockquote line instead") | fixed | 27d6e57a1063 — `PARTIAL_COVERAGE` entry rewritten: the gap closed in this same change, and the entry now records that rather than asserting empty metadata. ( |
| 8 | medium | `internal/bench/corpora/router-coverage/routing-matrix-derived.yaml` | Tracked derived artifact left stale by this diff: the 9 new cases in `tests/eval/routing-matrix/decision-revisit-gate.yaml` were never propagated, so `generate_router_coverage_from_matrix --check` reds. The generator is referenced by no workflow and no Taskfile target, so the drift ships silently — including the two `grade-as-permission` cases the fixture calls "the enforceable half of that invariant" | fixed | 27d6e57a1063 — `generate_router_coverage_from_matrix` re-run; derived corpus regenerated (305 prompts, 94 rule ids). ( |
| 9 | medium | `src/rules/decision-revisit-gate.md:106` | Grade-conditioned VENUE claim inside the rule whose thesis is that a grade never touches venue: "THE COUNCIL-FIRST VENUE FOR **E2+** REVIEW STAYS." The E2+ qualifier is redundant if the preceding line holds and reads as a carve-out if it does not — the "what may be skipped" shape is E0/E1. This is the shipped consumer-facing surface (the rule's own closing note records that `docs/contracts/` is unprojected, so a consumer receives these lines and not the burden table). Same qualifier at roadmap:720 | fixed | 27d6e57a1063 — `E2+` qualifier removed — "THE COUNCIL-FIRST VENUE STAYS, AT EVERY GRADE." Same leak shape a prior review found in Phase 4.3. ( |
| 10 | medium | `agents/roadmaps/road-to-evidence-based-adr-governance.md:86,97,119` | The roadmap's Iron Law and Invariants pre-commit the coupling the doctrine record leaves open: ":86 IT CONFERS NO AUTHORITY **UNTIL PHASE 7 SAYS SO**", ":97 Authority — grade-derived reopen routing \| Phase 7 only, default-off \| **Yes, once enabled**", ":119 VENUE IS DETERMINED BY … AND — ONLY ONCE PHASE 7 ENABLES IT — AN INDEPENDENTLY VALIDATED EVIDENCE ASSESSMENT". `ADR-239:256-262` and step 7.1 (`:770`) both frame the same question as owner-reserved and answerable NO, with "a published null is an acceptable outcome". A scheduled deferral is not the separation the change claims | fixed | 27d6e57a1063 — All three sites rewritten to leave the question open, with a note recording that a tense had smuggled back the coupling the council refused. ( |
| 11 | medium | `src/config/gate-violation-baselines.json:1-132` | Whole-file reindentation (4 spaces -> 2) turns a governance ratchet's audit trail into a 132-line diff whose only semantic change is a single added entry (`lint_provenance_vocabulary:permanence-language`, count 11). The reformat makes the one real baseline movement unreviewable in the diff — the opposite of what this file exists for | fixed | 27d6e57a1063 — Re-serialised at the original 4-space indent; the diff against main is now one added entry instead of 132 reindented lines. ( |
| 12 | medium | `tests/eval/routing-matrix/decision-revisit-gate.yaml:4,33` | Renumbering residue: three sites still cite `ADR-238` where they mean ADR-239 — `:4` ("ADR-238 + adr-layout § Provenance and evidence"), `:33` ("ADR-238 is E3 with a pre-registered benchmark behind it"), and `tests/scripts/lint_provenance_vocabulary.test.ts:3` ("ADR-238 § 6"). ADR-238 is main's unrelated `security-content-routes-to-external-authority`, carries no evidence axis and has no § 6. Because they RESOLVE, the new `adr_cite_check --cited` gate cannot see them — a structural blind spot in the gate this diff adds. Commit `0e3ec9174` ("repair the broken ADR links") missed them | fixed | 27d6e57a1063 — Both fixture sites and the lint test header now cite ADR-239. ( |
| 13 | medium | `src/scripts/adr/evidence_census.ts:760` | `path.join(REPO_ROOT, outRel)` silently rewrites an ABSOLUTE `--out` path into the repo, and `:774` then prints the UNRESOLVED `outRel`, so the reported path is not the written path and a stray directory is created inside the tree. `--out /tmp/census.new.md` wrote `<repo>/tmp/census.new.md` while reporting `/tmp/census.new.md`. The correct form is in the neighbouring gate: `check_gate_coverage.ts:896` uses `path.resolve(REPO_ROOT, …)` | fixed | 27d6e57a1063 — `--out` resolves an absolute path instead of joining it into the repo, and the printed path is the written path. ( |
| 14 | medium | `src/config/gate-violation-baselines.json:56` | The baseline note's headline scoping figure does not reproduce: "the same word list applied to whole files matches **41 lines across 30 records**". Measured with the lint's own `PROSE_WORD_RES` + `PHRASE_RES` matchers over `docs/decisions/ADR-*.md` + `docs/adrs/*/0*.md`: **29 lines across 17 records**. The note's own enumeration (5 violating records + 12 named legitimate ones) equals 17, i.e. it is internally consistent with my number and not with its own | fixed | 27d6e57a1063 — Figure replaced with both defensible readings and their exact methods (45/26 bare word list, 29/17 with the line filters, 11/5 scoped) — the ambiguity in "unscoped" is what produced the unreproducible number. ( |
| 15 | medium | `src/scripts/check_adr_frontmatter.ts:284` | Enforcement over-claim: "a NEW record must carry the axes, an existing one may not." Nothing requires them — every axis check is guarded by `if (provenance !== null)` / `if (evidence !== null)`. And no gate anywhere requires a body `## Evidence` section, so AC-5's first clause ("cannot pass CI without an Evidence section") is unmet | fixed | 27d6e57a1063 — Docstring corrected: this function validates the shape of an axis that is present and does not require the axes. The gap is structural (a single-file linter has no notion of NEW) and a two-ref diff is named as what would close it. ( |
| 16 | medium | `src/scripts/adr_cite_check.ts:164-166` | Docstring contradicts the implementation it documents: "Present only as `disabled-shadow-mode`, and only for an accepted + **agentic** + E0/E1 record." `_lib/adr_frontmatter.ts:400-405` deliberately DROPPED the provenance gate (its own docstring at :370-389 explains why) and exempts only `authority_basis: owner_intent`, so a thin HUMAN record also gets the notice | fixed | 27d6e57a1063 — Docstring corrected to `accepted E0/E1 without owner_intent`; provenance is explicitly named as NOT part of the condition, with the reason. ( |
| 17 | low | `agents/roadmaps/road-to-evidence-based-adr-governance.md:846,751,800,893` | AC-8 and three other roadmap sites name the metric `beneficiary-linked-grade-bias`; the registered ledger id is `adr-beneficiary-grade-bias`. Same drift for `evidence-discovery-recall` vs `adr-evidence-discovery-recall`. AC-8 is therefore not verifiable by grepping CLAIMS.md for the names it uses | fixed | 27d6e57a1063 — All four roadmap sites now use the registered ledger ids (`adr-beneficiary-grade-bias` and siblings). ( |
| 18 | low | `agents/roadmaps/road-to-evidence-based-adr-governance.md:714` | Step 5.2 is `[ ]` while its work landed in this diff: `src/rules/decision-revisit-gate.md` +96/-66 and the new `tests/eval/routing-matrix/decision-revisit-gate.yaml` are exactly 5.2's deliverables. Checkbox and tree disagree (`roadmap-progress-sync` Iron Law 2) | fixed | 27d6e57a1063 — 5.2 ticked — the rule change and its fixtures landed in this diff. ( |
| 19 | low | `agents/roadmaps/road-to-evidence-based-adr-governance.md:722` | The stated constraint that justifies keeping the burden table out of the rule is vacuous for that rule: "tier-2b `auto` with a 4,000-char cap (`CAP_AUTO`, `lint_load_context.ts:71`) and currently 166 lines". `cap_for` is only reached inside `if (eager.length)` (`lint_load_context.ts:283-294`); `decision-revisit-gate.md` declares no `load_context` at all, and is now 196 lines / 10,719 chars — 2.7x the quoted cap — with `lint_load_context` green | fixed | 27d6e57a1063 — The vacuous cap citation is replaced by the real reason (ownership, not a gate) and the risk-register row is rewritten to say the rule is unbounded in practice. ( |
| 20 | low | `agents/roadmaps/road-to-evidence-based-adr-governance.md:392` | Citation drift: step 1.5 cites `adr_cite_check`'s `PARTIAL_COVERAGE` at `:89-91`; that range is inside the `ADR_DIRS` docstring. `PARTIAL_COVERAGE` is at `:127-129` | fixed | 27d6e57a1063 — Citation changed from a line range to the constant name, with a note that a range is the wrong anchor for a constant that moves. ( |
| 21 | low | `docs/contracts/adr-layout.md:70` | Contract measurement partly stale on the tree it now describes: "`type:` was documented as `retrospective \| prospective`; the corpus carries ten values, `structural` 130 times against `retrospective` once". Ten distinct values reproduces; `structural` is 131 flat / 132 corpus-wide (ADR-239 itself is `type: structural`), and `retrospective` is 1 flat but 7 corpus-wide once the per-area records this diff touched are counted | accepted-risk | Reviewer verification, as far as it survives: "CONFIRMED. `grep -h '^type:' docs/decisions/ADR-*.md ...". Disposition: the contract already records the drift it measured and the figure was correct when written; a measurement in a live corpus carries its timestamp. Re-measuring every stated count on every merge is not a bar this change can hold, and pretending otherwise would be the same over-claim the finding objects to. Revisit-if: the ten-value figure is cited as a current fact rather than a dated reading. NOTE: this row carried 7 columns (an unescaped pipe in the reviewer text), so my status-stamping script wrote into the wrong cells and destroyed the reviewer's trailing evidence cell. The Finding text above is intact and is what was acted on; the lost cell is not recoverable from the transcript and is recorded as lost rather than reconstructed. |
| 22 | low | `docs/adrs/cost/0001-hard-stop-hook.md:1-15` | All 7 per-area records now carry BOTH YAML frontmatter and the pre-existing `> Area: … · Status: … · Date: … · Type: …` blockquote duplicating four of the same fields, with no gate reconciling the two. Step 1.5 allows this "for one transition release" but records no expiry condition and no check, so the dual source of truth can drift silently — and the surface that would catch it is the one finding 1 shows is unscanned | accepted-risk | Deliberate and stated in step 1.5: the blockquote stays as a human-readable duplicate for one transition release, and `read_adr_meta` makes frontmatter win outright so a stale duplicate cannot contribute a value. Revisit-if: the duplicate survives past the next minor release, or a reader is observed trusting the blockquote. |
| 23 | low | `tests/hooks/skill_route_hook.test.ts:334` | Reported as a pre-existing `main` failure; the real cause is different and worth recording. In a FRESH worktree at the merge base (303557fb) the file passes 21/21; re-running the identical commit in the SAME worktree fails 2/21. The test is self-poisoning across runs ("suppresses against the live log"), so "reproduces on a clean main worktree" is run-order dependent. NOT caused by this diff — every branch commit including the roadmap-only first one fails identically | accepted-risk | Not a defect of this diff — verified: the first branch commit (roadmap markdown only) fails identically. The finding corrects my own earlier attribution, which said "pre-existing on main after generate-tools" when the real cause is cross-run self-poisoning; the conclusion held, the reasoning did not. Left for the owner of that test rather than fixed here. Revisit-if: it fails on a first run in a fresh checkout. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
