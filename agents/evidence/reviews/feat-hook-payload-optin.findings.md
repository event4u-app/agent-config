# Findings: feat-hook-payload-optin
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: a68fe34188335d3b42a6e8ef266113980f05c4b6ecb45cec430692db6e3b46b5 | diff: 7de21d6a788ce73f2df8c78c8728b00b90c8161f | reviewer: r2-fresh-subagent-feat-hook-payload-optin | prompt_hash: 19ac1a5f62a9c33a19a6c2d8484bdd7f8dacaf405aef8b5ba451341b23708cf3 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 7de21d6a788ce73f2df8c78c8728b00b90c8161f
  scope_hash: a68fe34188335d3b42a6e8ef266113980f05c4b6ecb45cec430692db6e3b46b5
  roadmap: agents/roadmaps/road-to-per-turn-hook-economy.md
  roadmap_hash: 4b7328ddb9fd347360aa09f3d90d39fc375aacea46797bc7d4dcd998402c08b4
  ac_hash: 8b3d8200231b13c05e62f5b58611857c85c94ade33509e924fb0420f8e18c9ba
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T20:04:09Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/dispatch_hook.ts:1370 | The registered `verify:` for step 2.1 cannot observe the failure it is registered against. `payload_stubs` (and the `payload_stubs_served` roll-up at :991) count what the dispatcher OMITTED, which is a pure function of the manifest declaration plus the payload shape: the number is byte-identical whether the concern needed that body or never touched it. A rising count means "more tool calls", not "a concern lost a body". Nothing anywhere counts a concern READING a stub: `isPayloadStub` / `stubbedBytes` are consumed only by `tool_result_bytes_hook.ts:122` and the test fixture. The roadmap's own falsifiable revisit-if ("one recorded occurrence of a concern silently losing a body it needed") therefore has no recorder, which is the same unfalsifiability it explicitly rejects one sentence earlier. | fixed | d80398ab2 — claim corrected, not defended. The counter is relabelled an EXPOSURE denominator in code and roadmap; the detector moved to authoring time, where it is decidable: lint_hook_manifest now reads each tool-slot concern SOURCE and requires the matching declaration for every body key it references. 8e7a1dd24 records it at the step. |
| 2 | medium | src/scripts/lint_hook_manifest.ts:266 | The only authoring-time gate on the new field covers GUARDS (`isGuard && !declaresInput`). The additive case that the change itself names as its cost is ungated: a new advisory `post_tool_use` concern that reads `tool_response` without declaring `[result]` passes `lint_hook_manifest`, passes every case in `payload_optin.test.ts` (which pins only five pre-existing `result` declarations and five guard `input` declarations), and then reads `undefined` in silence. Combined with finding 1 there is neither an authoring-time nor a runtime detector for the one new failure mode. | fixed | d80398ab2 — same check as finding 1, which is the same gap seen from the additive side. Over-detection is the deliberate direction; escape hatch is a `payload-bodies-waiver: <class> — <reason>` line in the script. A temp-manifest test proves the check REFUSES an undeclared body reader. |
| 3 | medium | src/scripts/hooks/payload_stub.ts:131 | `bodyBytes` re-serialises the very body it is omitting, and `stubPayloadBodies` builds a fresh stub per keep-set with no memoisation of the length. On claude's `post_tool_use` the four distinct keep-sets in the shipped manifest (none, input, result, both) mean an object or array `tool_response` is `JSON.stringify`-ed TWICE extra (the keep-none and keep-input shapes) and `tool_input` twice extra, on the hot path, to fill a `bytes` field that only `tool-result-bytes` (one concern, the keep-none shape) ever reads. Memoising per key and value, or computing `bytes` lazily for the keep-none shape only, removes it. Added hot-path serialisation in a phase whose published null it may partly explain. | fixed | d80398ab2 — measureBodies() runs once per dispatch and the map is threaded through; stubPayloadBodies REQUIRES it rather than taking an optional one, so the per-stub call cannot return by omission. A doctored-map test pins bytes to the measurement. The cell was re-measured afterwards and the null survives (8e7a1dd24); the superseded first numbers are marked as confounded rather than replaced. |
| 4 | medium | src/scripts/lint_hook_manifest.ts:228 | The docblock of the check that exists to keep the manifest honest makes two false statements about the code it guards. It says a guard "must declare `needs_payload_bodies: true`" — a value that `_check_concerns` rejects thirty lines above (:196, "must be a list of body classes") and that `parseBodyClasses` deliberately ignores, so a maintainer following this line writes YAML the lint then fails. And :234 cites `_concern_needs_payload_bodies` as the dispatcher-side function; no such symbol exists, the function is `_concern_body_classes` (dispatch_hook.ts:355). | fixed | d80398ab2 — the docblock was rewritten with the check; both false statements are gone, and a tree-wide grep for `needs_payload_bodies: true` and `_concern_needs_payload_bodies` returns zero. |
| 5 | low | src/scripts/hooks/payload_stub.ts:158 | `makePayloadStub` copies `Object.keys(value)` verbatim into the stub, so payload-derived strings survive the omission, while the module header at :38 claims absolutely that "Nothing that could hold file contents, command output, or an API response survives into the stub". An object-shaped tool result keyed by ids, filenames or addresses contradicts that claim. The no-content-leak test exercises only a string body, and the object-shape test asserts `keys` equals `["stdout", "stderr"]`, i.e. it PINS key propagation rather than probing a content-derived key. Exposure does not regress (an undeclared concern previously received the whole body), so this is an overstated invariant, not a new leak. | fixed | d80398ab2 — the stub carries `key_count` (a number) instead of the key names, so no field of PayloadStub can hold a payload-derived string. The no-content test now probes a content-derived object KEY rather than pinning stdout/stderr propagation. |
| 6 | low | src/scripts/hooks/dispatch_hook.ts:1369 | `payload_bodies` records the DECLARATION, not what was served, so the per-concern record is wrong in both directions on any body-less event. `turn-end-gate` is `severity: blocking` and binds `stop` only, so `_concern_body_classes` returns the guard floor and every Stop dispatch records `payload_bodies: "input,result"` for a concern that can never receive either; symmetrically every advisory concern on `stop` records `"none"` although nothing was stubbed and the full envelope was served. The dispatcher already computes the honest value (`present` at :1273) and does not use it here. | fixed | d80398ab2 — payload_bodies records the intersection of declared and PRESENT classes, i.e. what was served. A blocking stop concern now records "none" instead of "input,result". |
| 7 | low | agents/roadmaps-progress.md:9 | The regenerated dashboard carries drift that is not attributable to this branch and cannot be verified from the diff. Overall goes 291/578 to 301/576 (+10 done, -2 total) while the only roadmap this diff touches contributes exactly +2 done and 0 total (its row goes 6/9/1/2 to 4/11/1/2, 18 steps unchanged). The residual +8 done and -2 total come from roadmap files outside the diff, so a reviewer cannot reproduce the committed numbers from the committed tree of this branch, and the hunk mixes unrelated regeneration drift into the change. | accepted-risk | 8e7a1dd24 — verified pre-existing, not branch-introduced: `git diff --stat origin/main -- agents/roadmaps/` shows this branch touches exactly ONE roadmap file, whose own row moves 9/15 to 11/14 (+2 done, -1 total). The residual (+8 done, -2 total) is the committed dashboard on main being stale against main own roadmap files. roadmap-progress-sync Iron Law 1 mandates the regeneration on any roadmap touch, so the drift arrives with the mandated regen and cannot be split out without hand-editing a generated file. |
