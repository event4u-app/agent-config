# Findings: zcs-closure-round2
<!-- completion-review: v1 | reviewed: 2026-08-06 | scope: 858cba8cddb5a52649d66fa2fab5ad84de5a4050b1b67766ba3759551850c544 | diff: ca55813f3a4fd994f72a3f538da358f082b77c7c | reviewer: r2-fresh-subagent-zcs-closure-round2 | prompt_hash: 457a1ba86295b1052215da98bd07017447eacd2850e724fed37598d30ac4e88b -->

<!-- context-manifest: v1
inputs:
  diff_sha: ca55813f3a4fd994f72a3f538da358f082b77c7c
  scope_hash: 858cba8cddb5a52649d66fa2fab5ad84de5a4050b1b67766ba3759551850c544
  roadmap: agents/roadmaps/archive/road-to-zero-ceremony-settings.md
  roadmap_hash: 9c37e0c04db1af62f45e35b0ac0a3c6d2b6ebde585fc544920ef21fef616033a
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-06T22:42:44Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/memory_learn_hook.ts:121 | The justification for the consent wiring states two things the code cannot do. (a) "a reclassification out of B refuses the hook instead of silently leaving it enabled" — `LEARN_KEY_CLASS` (:60) is a hardcoded constant, so no runtime reclassification is possible; a contract change reds a test and leaves hook behaviour untouched. The roadmap's own wording (`§ Phase 5 step 4`, "reds CI instead of silently unbinding the gate") is the accurate version, so the two disagree. (b) "the value goes through the shared conservative-default notion rather than a local `=== 'true'`" — the local `=== 'true'` is still the whole normalisation, at :103, and `learnConsent` (:138) hands `consentVerdict` an already-reduced boolean. With `cls` constant, `handEdited` constant `true`, and the value pre-normalised, every discriminating input is fixed: `enabled()` is behaviourally identical to the pre-change read. | fixed | e1-fix — the overclaim is retracted in the module docstring: with cls/handEdited/value all fixed, enabled() is behaviourally identical; what the call buys is stated exactly |
| 2 | medium | src/server/routes/ping.ts:33 | `execFileSync` now runs on the request path of the liveness probe (`readGitUserName`, src/server/nicknameResolver.ts:32, `timeout: 1000`). Memoisation bounds it to once per process, but the *first* `GET /api/v1/ping` — the request the UI bundle blocks on at startup — pays the full fork with the Fastify event loop blocked, stalling every concurrent request behind it. The docstring's claim that the cache prevents the stall holds only for polls 2..n; there is no server-start warm-up and no async variant. | fixed | e1-fix — warmNicknamePrefill() at pingRoute registration; the fork leaves the request path entirely, not just polls 2..n |
| 3 | medium | src/scripts/memory_learn_hook.ts:33 | The hook's stated contract is fail-open ("any error → exit 0", :20; `fail_closed: false`), enforced by the `try/catch` in `main()` (:169). The new top-level import of `../shared/settingsConsent.js` is evaluated *before* that guard, so an import-time failure (resolution/packaging gap in an installed or bundled hook, transitive load error) exits non-zero at session teardown — outside the fail-open guarantee. The hook previously imported only Node built-ins and its sibling `learning_sidecar.js`; the diff widens that footprint without any packaging evidence. | fixed | e1-fix — MEASURED not asserted: esbuild inlines both modules into the hook bundle (built to a scratch path and grepped), and both are pure declarations with no top-level side effects; recorded at the import with the condition that would invalidate it |
| 4 | medium | src/server/nicknameResolver.ts:57 | `resolveNicknamePrefill` hardcodes both impure probes (`readGitUserName`, `userInfo`) instead of injecting them, so the `os-account` floor — the only behaviour this module adds over the pure chain — has no deterministic coverage. Its test (tests/server/nicknameResolver.test.ts:84) guards the assertion behind `readGitUserName() === undefined && got.source !== 'none'`, so on any machine with a git identity the entire body is skipped and the case passes green having asserted nothing about the floor. The pure half took the injection lesson (`NicknameProbe.gitUserName`); the impure half did not. | fixed | e1-fix — ResolverProbes injects gitUserName + osAccount; the floor now has 4 deterministic cases instead of one machine-conditional skip |
| 5 | medium | agents/roadmaps/archive/road-to-zero-ceremony-settings.md:294 | Phase 4 is recorded ✅ done / 100% (also agents/roadmaps-progress.md:42) while its unchanged exit criterion "a fresh interactive session asks exactly one question" is unmet by construction: step 2's own note states no chat-side trigger code exists and the ask is `enforced_by: none`, and § Findings item 4 records that `planSettingsAsks` / `nonInteractiveReason` are imported only by their tests. The gap is disclosed in three places, which is honest, but the phase table and the exit criterion in the same file still contradict each other. | fixed | e1-fix — step 4.2 reverted to [ ] and the Phase-4 exit criteria annotated per criterion; marking a step done for its buildable fraction is the overclaim this run exists to correct |
| 6 | low | tests/server/nicknameResolver.test.ts:84 | Title/assertion mismatch: the case is named "…never as env-user" but asserts `not.toBe('env-username')`. With `env: {}` neither source is reachable, so both readings are vacuous — the only unconditional assertion in the case cannot fail. | fixed | e1-fix — case rewritten with injected probes; asserts the os-account label directly instead of a vacuous not.toBe |
| 7 | low | tests/scripts/jit_ask_budget.test.ts:486 | `seeAlsoBoundary` returns `lines.length` when no line matches `/^#{1,6}\s+see also\s*$/iu` exactly (a heading with trailing text, or no See-also section at all). The `inBody` check at :509 then degenerates to the plain `toContain('settings-ask-protocol')` that the block's own comment rejects as insufficient against the stated mutation. Nothing asserts a boundary was actually found, so the discriminator can be silently absent for some of the four MIGRATED files. | fixed | e1-fix — seeAlsoBoundary returns null for "no section" and the two cases assert differently; a near-miss heading now fails loudly instead of degrading to plain presence |
| 8 | low | src/scripts/memory_learn_hook.ts:73 | The tri-state paragraph says "`undefined` means the key is absent", contradicting the same docstring's opening line and the code at :87: an unreadable settings file (permissions, a directory at the path, I/O error) also yields `undefined`, and `learnConsent` then reports `withheld-default`. An unreadable-but-permissive file is therefore indistinguishable from an absent one — the same silent-no-op class the docstring flags for malformed scalars, but not listed among them. | fixed | e1-fix — docstring names BOTH collapses (absent/unreadable, deliberate-false/malformed); a test pins the unreadable-file case |
| 9 | low | tests/scripts/memory_learn_hook.test.ts:121 | "does NOT distinguish a deliberate false from a malformed value" asserts only equality between the two reads, so it would still pass if the parser regressed to returning `undefined` for both — i.e. it cannot fail for the reason it names. The preceding case pins the concrete `false`, so the behaviour is covered; this case adds no independent signal. | fixed | e1-fix — asserts the concrete false on both reads, so a regression to undefined fails the case it is named after |
