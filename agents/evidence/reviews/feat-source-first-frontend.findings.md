# Findings: feat-source-first-frontend
<!-- completion-review: v1 | reviewed: 2026-08-13 | scope: 1676b3404ac8a5fdb33a5493072880eec9cf692230ecccc2a022765359be285d | diff: fde4d00cd943f7296c10b98bde0ce67a05bbaa8a | reviewer: r2-fresh-subagent-feat-source-first-frontend | prompt_hash: bd89486e898b16ef01c67313fdb944dbe0bb0da2c3a3a2acbed8e489d476ddea -->

<!-- context-manifest: v1
inputs:
  diff_sha: fde4d00cd943f7296c10b98bde0ce67a05bbaa8a
  scope_hash: 1676b3404ac8a5fdb33a5493072880eec9cf692230ecccc2a022765359be285d
  roadmap: agents/roadmaps/road-to-source-first-frontend.md
  roadmap_hash: 90c24dd587be53b88047538ba2408431baa5f98174fe7e93df053cd1695cc0d4
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-13T00:34:36Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/rules/design-fidelity.md:67 | The new Iron-Law line `A SCREENSHOT IS VALIDATION, NEVER THE INPUT YOU BUILD FROM.` ships **unqualified**, and the same diff shows the authors knew it needed a qualifier: the guideline's copy of the identical law (docs/guidelines/design-fidelity-mechanics.md:708-709) reads `... NEVER THE INPUT YOU BUILD FROM, WHILE ANY HIGHER RUNG IS REACHABLE`. Read as written, the rule contradicts (a) its own § What counts as the spec, which lists "a mockup or screenshot" as the spec, and its opening sentence ("When the user provides a finished prototype, mockup, screenshot ... that artifact is the spec"); (b) the guideline's rung 4, explicitly "not a grudging exception"; (c) the **Image artifact** workflow this same diff adds at src/skills/design-review/references/verification-automation.md:49-56, whose step 1 is "Open the mockup — use the provided image/screenshot". The unqualified form sits in the projected, always-loaded rule; the qualifier sits only in an on-demand guideline, so the strict reading is the one an agent gets by default — and it forbids the screenshot-only handover class the rule is supposed to govern. | open | |
| 2 | medium | src/scripts/hooks/ui_route_nudge_hook.ts:143-167 | The artifact-read instrument is implemented **twice** and the two implementations measure different quantities, while only the one the roadmap did *not* name is published. Hook side: `artifactRead` + `artifactReadBeforeFirstUiWrite`, a per-session latch taken at the FIRST UI write — exactly the metric roadmap Phase 1 Step 3 names ("artifact read before first UI write"). Analyzer side (src/scripts/report_consultation_rate.ts:154-198): a per-turn count over ALL UI-write turns, recomputed from transcripts. Nothing reads the hook's two fields: they are persisted to `agents/runtime/state/ui-route-nudge.json` and no consumer exists — the fields are new in this branch, so no pre-existing reader is possible, and the only other branch-touched consumer (`report_consultation_rate.ts`) imports the predicate, never the state file. Net effect: the named metric is computed and discarded, the published metric is unnamed, and `stateChanged` was widened to persist data nobody reads. | open | |
| 3 | medium | src/skills/fe-design/SKILL.md:43 | `2b.` is not a valid ordered-list marker in CommonMark (a marker is digits + `.`/`)`), and there is no blank line between the end of item 2 (line 42) and line 43. Item 2's content indent is 3 columns, so line 43 is a **lazy continuation of item 2's paragraph**: the new mandatory Inventory step renders glued onto the end of "Brief" rather than as a step of the loop, and its indented sub-blocks (the honoured/translated/flagged table at lines 48-52) become continuation blocks of item 2. Verified against the raw bytes, not inferred. For a skill whose stated contract is "the loop below is not optional context, it is the work", a step that does not render as a step is a real delivery defect. | open | |
| 4 | medium | tests/scripts/design_fidelity_routing.test.ts:232-239 | The near-miss row paired with the three new builder triggers guards the wrong direction. `near-bare-host-mention` tests a **protocol-less** mention ("We looked at v0.dev and bolt.new"), which was already silent before this change — its own `baseline: false` says so — so it cannot detect over-broadness that the new phrases introduce. What the new `https://v0.dev/` / `https://lovable.dev/` / `https://bolt.new/` phrases actually open is any *non-handover* URL on those hosts: `https://v0.dev/docs`, a pricing page, a blog post, a changelog link all now activate design-fidelity and treat the run as a spec handover. No row covers that class. src/rules/design-fidelity.md:148-152 claims each of the three trigger classes "ships with its own near-miss row" and names this row — the discipline is satisfied in letter while the failure mode it exists to catch stays untested. | open | |
| 5 | medium | tests/scripts/design_fidelity_routing.test.ts:242-253 | `known-gap-protocolless-share-link` is commented "DELIBERATELY RED, and not a near-miss", but it is encoded `routes: false` and placed **inside** the `--- Near-misses: must stay silent ---` block, so it is a passing assertion, not a red one. Two consequences: the comment misleads a maintainer into believing CI is signalling the gap when CI is asserting the gap stays open; and a later fix that closes the gap makes this test fail, presenting the improvement as a regression. Nothing except prose distinguishes the row from the genuine near-misses beside it. The roadmap repeats the claim at agents/roadmaps/road-to-source-first-frontend.md:411-412 ("a **deliberately red** ... row"). | open | |
| 6 | medium | src/scripts/report_consultation_rate.ts:320-335 | The report publishes `ARTIFACT READ BEFORE WRITE` over the whole UI-write population and then, four lines later, tells the reader that number is not usable: "A session with no handover contributes 0/N to it by construction, so read it per-handover, never as an estate average." No per-handover denominator is emitted anywhere — not in the text form, not in `RateReport`, not in `--json` — so the caveat names the correct reading while making it uncomputable from the output. A published rate whose own adjacent prose forbids reading it as published is an instrument that will be quoted at its wrong value. | open | |
| 7 | medium | agents/roadmaps/road-to-source-first-frontend.md:5 | The roadmap carries no Acceptance Criteria section (headings run Context, Symptom map, External sources, Phases 1-6, Risk Register, Non-goals; `grep -i acceptance` returns nothing). The review package reflects it: `acceptance-criteria.md` is 0 bytes and the manifest's `ac_hash` is `e3b0c442...b7852b855`, the SHA-256 of the empty string. A completion review over 11 closed steps therefore has no criteria to check completion against — the phase-level Falsifier blocks are the only stated bar, and Phase 1's was explicitly met-and-not-honoured (line 126). | open | |
| 8 | low | src/rules/design-fidelity.md:73 | "The last three lines route to ... § Data-basis ladder and § Adopt the code" — the Iron-Law block gained **five** lines (67-71 in the current file). The last three are the adopt-the-code sentence alone; the data-basis half of the pointer names lines it excludes. A reader following the sentence literally never reaches § Data-basis ladder from here. | open | |
| 9 | low | src/scripts/report_consultation_rate.ts:180-186 | `isArtifactRead` is evaluated before `isConsultation` with an early `return`, so any event satisfying both predicates is removed from the consultation numerator — the precise failure ("would make the consultation rate move for a reason it never measured") that the adjacent comment claims this ordering prevents; placing it first with an early return causes that failure rather than preventing it. Overlap is reachable by construction, not excluded: `isArtifactRead` accepts any non-write path ending `design.html`, and `isConsultation` accepts any non-write path containing `skills/<surface>/`, so e.g. `src/skills/design-review/references/design.html` satisfies both. It would also drop out of the discharge-proxy latch at line 189. The hook carries the same ordering (src/scripts/hooks/ui_route_nudge_hook.ts:237-242). Disjointness is asserted on two hand-picked paths (tests/scripts/ui_route_nudge_artifact_read.test.ts:76-79), never established over the predicates. | open | |
| 10 | low | src/scripts/report_consultation_rate.ts:97-99 | `artifactRead` is declared as the **first** member of `RateReport`, ahead of `store`, separating it from `consulted` / `reviewOpenedAfter` / `uiWriteTurns` — the counters it shares a denominator with, and the grouping the interface otherwise keeps. `measureStore` (line 240-250) initialises it last, so declaration and construction order now disagree, and the `--json` key order changes for any existing consumer of that output. | open | |
