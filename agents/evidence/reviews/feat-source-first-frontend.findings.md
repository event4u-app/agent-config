# Findings: feat-source-first-frontend
<!-- completion-review: v1 | reviewed: 2026-08-13 | scope: 9ff18dd17eba429e1d1c69af6a85db242d73f2caa6cfe4cfa8fca7183aaf39c6 | diff: 14252809d1b1fc92e849f520b70b0113490fc27f | reviewer: r2-fresh-subagent-feat-source-first-frontend | prompt_hash: 98024f955187cef4bf4b0b27d1e08183343bb913438ac1e7b3eae73142246c5c -->

<!-- context-manifest: v1
inputs:
  diff_sha: 14252809d1b1fc92e849f520b70b0113490fc27f
  scope_hash: 9ff18dd17eba429e1d1c69af6a85db242d73f2caa6cfe4cfa8fca7183aaf39c6
  roadmap: agents/roadmaps/road-to-source-first-frontend.md
  roadmap_hash: f63163684eb2a9e8337154a4311a9095729a35df5c7c7a248a8536d6b45b0b1c
  ac_hash: e03ec29dc72033d4c8d6693ceaab4473032eaa46e5f81970ebeab800a6d6320b
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-13T00:50:22Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | agents/evidence/analysis/source-first-frontend-phase1.md:87-89 | The committed evidence artefact makes three claims the shipped tree contradicts. (a) "`artifactRead` and `artifactReadBeforeFirstUiWrite` are now captured by `ui-route-nudge`" — the hook captures neither; `SessionState` is still `{consulted, nudges}` and `ui_route_nudge_hook.ts:237-248` states the two session fields were removed because nothing read them. (b) `artifactReadBeforeFirstUiWrite` names nothing in `src/` or `tests/` (the fields are `readBeforeFirstWrite` / `handoverReadFirst`). (c) "the rate is published **on the consultation rate's own denominator** so the two are comparable" is the denominator the shipped `render()` explicitly tells the reader NOT to quote (`report_consultation_rate.ts:356-373`). The roadmap's AC A8 claims the denominator was corrected; the evidence file its own phase produced still endorses the superseded one, so a reader who trusts the evidence file quotes the wrong rate. | open | |
| 2 | medium | src/scripts/report_consultation_rate.ts:170-229,262-321 | The new measurement logic is untested. `measureSession`'s three new branches — the `isArtifactRead` latch (204), the `artifactTurns.set` insertion (193), and the `firstWrite` ordering computation (218-221) — and `measureStore`'s `handoverSessions` / `handoverReadFirst` accumulation (315-318) are exercised by nothing. `consultation_rate.test.ts` touches the three report fields only inside a hand-built `RateReport` literal fed to `render` (211-223), and `ui_route_nudge_artifact_read.test.ts` never calls `measureSession`. The ordering the code itself calls load-bearing ("`artifactTurns` is insertion-ordered … so its first value is that write's state") is exactly the part with zero coverage, and a first-write regression would surface as a plausible number rather than a failure. Contradicts `source-first-frontend-phase1.md:101` ("its unit tests exercise every branch"). | open | |
| 3 | medium | agents/settings/contexts/design-corpus-upstream-harvest.md:3-5 | The anonymisation is nominal and defeats its own stated purpose. The file records derivation verdicts ("what this suite takes, refuses, and parks from **Source C**") and then dereferences Source C in the next sentence — "Its real identity and pin live in `design-intelligence/ATTRIBUTION.md`" — a plaintext tracked file naming the upstream repo and org. Those exact tokens are denied tree-wide by `src/scripts/external_sources_denylist.json:11,41`, so the tracked tree deliberately forbids what this file resolves to in one hop; `check_no_external_sources` stays green only because the pointer carries no token, not because the identity is opaque. `source-confidentiality`'s stated remedy for a retained source link is an `ENC1:` token, not an in-repo pointer. Roadmap AC A7 ("recorded anonymised") does not hold in substance. | open | |
| 4 | low | src/scripts/hooks/ui_route_nudge_hook.ts:150-154 | `isArtifactRead` latches on any non-write tool that merely *carries* a path, not one that reads a file: `toolUseToEvent` maps `ti['path']` (`report_consultation_rate.ts:153`), so a `Grep`/`Glob` whose `path` is `.claude/design-system/` sets `artifactRead = true` with nothing read. The sibling `isConsultation` documents this exact false-positive class as having "corrupted both the gate and the metric" (98-113) and was narrowed for it; the new predicate inherits it unnarrowed while its own docblock (132-137) claims the predicates are kept beside each other so the analyzer and the nudge "cannot classify the same event differently". Inflates the READ-BEFORE-FIRST-WRITE numerator; the output's blind-spot note (376-386) names only the other cause. | open | |
| 5 | low | src/skills/fe-design/SKILL.md:47 | "using the engine's own names so the two surfaces cannot drift" is a mechanism claim with no mechanism. Nothing asserts the skill's `honoured`/`translated`/`flagged` table (49-53) against `src/agent-src/templates/scripts/work_engine/directives/ui/apply.ts:116` `COVERAGE_BUCKETS`; the values match today by hand. The same branch wrote precisely this guard for the other copied constant (`ui_route_nudge_artifact_read.test.ts:107-129`, parsing the rule's frontmatter because "a copy nothing pins is a copy that rots"), so the omission is asymmetric with the branch's own standard on the same class of copy. | open | |
| 6 | low | src/rules/design-fidelity.md:166-170 | "The row that would have caught it — a documentation URL on the same host — was the one nobody wrote" is false in the tree this branch ships: `near-builder-host-non-handover-url` (`tests/scripts/design_fidelity_routing.test.ts:222-234`) is exactly that row, added by this diff, and the roadmap's own risk register (`road-to-source-first-frontend.md:505`) says so. The same paragraph (162) and the § Routing bullet (150) also credit only `near-bare-host-mention` to the withdrawn class while two rows were left behind, so a future retry reading the rule under-counts the tripwires it has to clear. Same text in the `dist/agent-src/rules/design-fidelity.md` projection. | open | |
| 7 | low | src/scripts/report_consultation_rate.ts:350 | Column drift in the published report: the `artifact read (all UI turns)` label pads its value to column 36 while every other row in the block pads to 35 (`sessions scanned` 338, `CONSULTATION RATE` 344, `discharge PROXY` 347, `READ BEFORE FIRST WRITE` 353). The one new row is the one that does not line up, in the block a reader is told to quote from. | open | |
| 8 | low | agents/roadmaps/road-to-source-first-frontend.md:48 | "The remaining eight are **blocked on named dependencies, not on effort**, and each is recorded at its own phase" does not describe eight steps. The bullets enumerate seven (Phase 3 ×3, Phase 4 Steps 1-2, Phase 6 ×2); the eighth is Phase 4 Step 3, which the same block (43-47) describes as attempted-and-withdrawn — a decision, not a dependency — and which stays `[ ]`, so the dashboard reports a step the roadmap says was decided against as open work blocked on something. | open | |
| 9 | low | src/scripts/hooks/ui_route_nudge_hook.ts:251 | Downstream miss: `nudgeReason` names the loop as "the fe-design loop (brief → build → review)", but this diff adds a fourth step to that loop (`src/skills/fe-design/SKILL.md:43-60`, Inventory). The runtime string a nudged agent actually reads now omits the step the branch added to stop the "missing JavaScript" failure, which is the one stage the nudge most needs to name. | open | |
