# Findings: anchor-owner-ruling
<!-- completion-review: v1 | reviewed: 2026-09-10 | scope: 8806f6f85d20c0bb41d2183ffaddd99e2ac1de1aae586f9bc661aa6ffe35f92b | diff: 4d4152f26c3ed8e663ec2b190231bb84adf9705e | reviewer: r2-fresh-subagent-anchor-owner-ruling | prompt_hash: 35dac6770ebae0fe6160377919f026af43b9bea4fbcceb2c0d256d2815248b4c -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-anchor-owner-ruling"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-10 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 4d4152f26c3ed8e663ec2b190231bb84adf9705e
  scope_hash: 8806f6f85d20c0bb41d2183ffaddd99e2ac1de1aae586f9bc661aa6ffe35f92b
  roadmap: agents/roadmaps/road-to-typed-grants-that-persist.md
  roadmap_hash: e2e5c8bb3332519ded1afbec8429283a85f1adaaa60196841821cfbd392f3831
  ac_hash: 41b7b784a462de3c07c67dc8ece3c0c82e4d53bcf1f6855152c7521afd993856
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-10T14:20:08Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | agents/roadmaps/road-to-typed-grants-that-persist.md:482 | The blocker's live `What to do` field still says "What is left is a repository-settings change only a human with admin rights can perform" and enumerates "**What the human must change** - three items": (1) set `required_approving_review_count` to at least 1, (2) set `require_last_push_approval` true, (3) remove the bypass actor. Items 1-2 are exactly the dimensions this change removes by owner ruling, and item 3 the change reports as already done. The one action actually left - restoring `strict_required_status_checks` - is named nowhere in `What to do`. The diff's added paragraph marks only the old `Status` sentence as "Superseded reading"; `What to do`, `Recommendation` and `Resolved when` were left standing as live instructions, so the blocker now instructs the reversal of the ruling it sits under. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 2 | high | src/scripts/_lib/platform_anchor.ts:122 | `required_review_thread_resolution` was removed from `NON_NEGOTIABLE_FLOOR` as well, and the code comment states this one "is the implementer's judgement rather than a council ruling". The change's own record says the opposite is required: `owner_ruling_2026_09_10` reads "only the owner may AUTHORIZE a floor reduction; an agent may implement one", and `agents/evidence/ratifications/drain-anchor-owner-ruling.md:47` confirms the owner ruled on two dimensions only. A third floor dimension therefore left the floor on implementer authority. It is also absent from `owner_ruling_2026_09_10`, which claims the cost as "`minimum_approving_reviews` and `require_last_push_approval` are gone from this file AND from `NON_NEGOTIABLE_FLOOR`" - so the record a later auditor opens under-reports the reduction by one dimension. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 3 | high | src/scripts/_lib/platform_anchor.ts:378 | The narrowed floor CAN still be lowered by a policy-only edit, for exactly one dimension. `enforceFloor` no longer rejects `required_review_thread_resolution: false`, and `evaluateAnchor` (:642) raises `thread-resolution-missing` only `if (policy.required_review_thread_resolution && ...)`. Flipping that one boolean in `src/config/platform-anchor.json` therefore deletes the check silently and the anchor still reads compliant - the precise failure the floor was built against per the openai condition quoted at :100-105 ("even a ratified edit cannot go below these values"). `tests/scripts/platform_anchor.test.ts:222` pins the hole as intended (`toEqual([])`). Either floor the dimension or drop it from the expectation; keeping it enforced-but-unfloored is the one combination that reads protected and is not. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 4 | high | docs/contracts/ratification-artifact.md:244 | Surviving prose contradicts this change's own new claim in the same file. :244-250 still reads "**Whether repository administrators are inside the threat model is unanswered, and this contract must not pretend otherwise** ... The question is recorded in `threat_model_note` in `src/config/platform-anchor.json` and is owner-reserved" - while `src/config/platform-anchor.json:19` now opens `owner_ruling_2026_09_10` with "THE THREAT-MODEL QUESTION IS ANSWERED" and :21 marks `threat_model_note` "SUPERSEDED IN PART". The same paragraph also describes an unconditional administrator bypass that the change reports as removed. A reader following the contract's own pointer lands on a note the change superseded. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 5 | medium | docs/contracts/ratification-artifact.md:197 | `required_review_thread_resolution` is the only dimension the evaluator still enforces whose current live value is measured nowhere in this change. The four-row measurement table omits it, yet it is the third parameter of the same pull_request (the ruleset rule type) rule whose other two parameters the table shows going 1 -> 0 and true -> false in the owner's reset. The claim that `strict_required_status_checks` is "the one dimension still failing" (:210) and "the single dimension the anchor now reds on" (ratification artifact :110) is therefore asserted, not established: if the reset cleared the pull_request (the ruleset rule type) rule the anchor reds on two, and the code comment's live-state claim "the forge currently sets it" (platform_anchor.ts:127) has no reading behind it. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 6 | medium | src/config/platform-anchor.json:2 | The `$comment` still motivates the floor with the dimension this change deletes: "openai additionally required a non-negotiable floor embedded in the verifier itself, so that an edit HERE cannot set `minimum_approving_reviews` to 0 and make itself green". There is no such key in `required` any more, and the adjacent `owner_ruling_2026_09_10` records that the dimension is gone - so the file asserts and denies the same property four lines apart. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 7 | medium | src/scripts/_lib/platform_anchor.ts:13 | The module header still frames the module as the human-review limb: ":9-13 What it cannot prove is that the artifact was reviewed by anyone the repository required - that half lives in repository settings ... **This module is the second limb, made checkable.**" After this change the module deliberately checks no review requirement at all, which is what `docs/contracts/ratification-artifact.md:112` now states ("The platform holds the branch, and deliberately not the reviewer"). The shipped docstring describes a capability the code no longer has. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 8 | medium | src/scripts/_lib/platform_anchor.ts:30 | Second stale claim in the same header: constraint 2 says "a policy-only edit could set `minimum_approving_reviews` to 0 and make itself green. `NON_NEGOTIABLE_FLOOR` below is the floor such an edit cannot cross". The field no longer exists, and the property is now false for the one dimension left unfloored (see the `required_review_thread_resolution` finding). Constraint 1 (:25-26) also still advertises "the approval count takes the MAXIMUM" as a judged threshold; it is now evidence only. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 9 | medium | agents/roadmaps/road-to-typed-grants-that-persist.md:433 | "**Limb 1, verified rather than asserted:** the owner changed the three ruleset settings and ... exits **0**, reporting `platform anchor COMPLIANT` with `approving reviews required: 1 (policy floor 1)`" is left standing as verified fact. All three parts are now false: the gate does not exit 0 (strict checks are off per the new table), the ruleset values quoted at :438-441 were reset, and the evidence string `approving reviews required: N (policy floor M)` was deleted by this diff (platform_anchor.ts:639 now emits "observed, not required here"). :495 likewise still reads "On 2026-09-10 it exits 1 and names exactly those three" against the new "exactly one dimension". | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 10 | medium | agents/roadmaps/road-to-typed-grants-that-persist.md:519 | The blocker's `Resolved when` condition 1 still reads "exits 0 against the live repository - which requires the gate (landed 2026-09-10) and the **three settings changes named above**". Two of those three settings were removed from the enforced set by this change, so the stated closing condition over-specifies what must happen and points at an enumeration that no longer describes the gate. A later reader screening this blocker on its closing condition (the documented way to avoid trusting a stale `Status`) is given the pre-ruling criterion. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 11 | medium | agents/roadmaps/road-to-typed-grants-that-persist.md:509 | Dangling section reference: cites "`docs/contracts/ratification-artifact.md` § The platform anchor as measured", a heading this same diff renamed to "What a green platform anchor does and does not establish". The sentence that follows also restates the retired framing ("the mechanism supplies process evidence rather than a platform-enforced guarantee"). | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 12 | medium | docs/contracts/ratification-artifact.md:218 | "The three facts, from `gh api`, so a reader can re-run them rather than trust this paragraph" now sits inside the new `### The measurement this section was written from` and duplicates two of its facts (the 404 at :221-224 repeats :206-209; the bypass actor at :229-230 repeats the table's `bypass_actors` row) while contradicting the change's framing on the third: ":225-228 A pull request is required; an approving reviewer is not. So a ratification artifact can land reviewed by nobody the repository insisted on" is stated as a deficiency, which the new sections argue it is not. "this paragraph" no longer has a referent. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 13 | low | docs/contracts/ratification-artifact.md:114 | The rewritten layer-2 enumeration of what the anchor enforces - "an active ruleset over the default branch, deletion and force-push blocked, strict required checks, the context that carries the ratification gate, no unconditional bypass actor" - omits `required_review_thread_resolution`, which `evaluateAnchor:642` still enforces and can still red the gate. The list reads exhaustive and is not. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 14 | low | docs/contracts/ratification-artifact.md:296 | § Honest enforcement still says the gate "does not verify the platform controls named in § Where the trust actually comes from - that is the open residual", which the rewritten § 2 in the same file now contradicts. Pre-existing drift, but it survives inside the section this diff rewrote and is the last statement a reader gets on the subject. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 15 | low | src/config/platform-anchor.json:3 | `schema_version` stays `1` although the accepted shape of `required` changed incompatibly: two previously mandatory keys are now refused. Because `readAnchorPolicy` rejects unknown keys before it reaches any version-specific handling, a schema-1 expectation file that still carries them fails with `policy-unknown-field` rather than a version diagnostic. Either bump the version or say in the file why the version is stable across a breaking field change. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 16 | low | agents/evidence/ratifications/drain-anchor-owner-ruling.md:1 | Filename does not follow the convention the contract this same diff edits still states at `docs/contracts/ratification-artifact.md:21-25`: "`agents/evidence/ratifications/<pr>.md` - one file per pull request, named for the PR number (`1981.md`)". Both siblings deviate the same way, so the convention is now documented and unpracticed three times over; the contract was open in this diff and was not reconciled. | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
| 17 | low | tests/scripts/platform_anchor.test.ts:216 | `it('refuses each remaining weakening independently')` does not do what its name says: its first assertion is an acceptance (`enforceFloor(policy({ required_review_thread_resolution: false }))).toEqual([])`), and it exercises 2 of the 7 keys left in `NON_NEGOTIABLE_FLOOR`. The name will read to the next author as coverage of the whole remaining floor. Also note :226-239 is now near-identical to the `refuses an unknown key` test that follows it (same input shape, same asserted code). | deferred | deferred to round 2 at agents/evidence/reviews/anchor-owner-ruling-r2.findings.md — addressed in 4e6d25289, but that commit PREDATES this artefact first-add, so a fixed claim here is exactly what contract §2.5 refuses and is not made |
