# Findings: topology-drain12
<!-- completion-review: v1 | reviewed: 2026-09-01 | scope: ebc6bc3374f04460af64aa77315b59a32a0dd49e00b1f53d6b7e2676f86e8e2c | diff: c4fb2771e6428879346e1050c25b85418a7dbd0c | reviewer: r2-fresh-subagent-topology-drain12 | prompt_hash: 19dd370dfbd2f29a9877417941093e97c1697bce1d2c0a18fa757fe79eb88012 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-topology-drain12"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-01 -->

<!-- context-manifest: v1
inputs:
  diff_sha: c4fb2771e6428879346e1050c25b85418a7dbd0c
  scope_hash: ebc6bc3374f04460af64aa77315b59a32a0dd49e00b1f53d6b7e2676f86e8e2c
  roadmap: agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: 507e4833787616161eeb045982cb89a44090a6745be141569337f7e65545c501
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-01T07:58:15Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:15 | 42 steps are marked `[-]`, which this tree's own contract defines as CANCELLED — scope dropped, won't happen at all (`docs/guidelines/agent-infra/roadmap-progress-mechanics.md:218-219`; `update_roadmap_progress.ts:25` and its `count_checkboxes` at :356-360 bucket `-` as `cancelled` and only `~` as `deferred`). The same line, the three stubs and both council seats describe those identical steps as DEFERRED with resumption triggers, which is the definition of `[~]`. The glyph choice is not cosmetic: `count_deferred` stays 0, so Iron Law 3 (`update_roadmap_progress.ts:1022,1427` — a roadmap at `count_open == 0` carrying `[~]` items blocks silent auto-archive) never fired, and `deferralProblems` (`archive_completed_roadmaps.ts:567`) never validated that the three stubs are live receivers. The branch then archived the roadmap in c4fb2771e. 42 steps are permanently recorded in the estate as cancelled work. | open | |
| 2 | high | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:3574 | Class-3 blocker `phase-2-benchmark-cost` is closed on an AI-council verdict, but its own retained `Recommendation` field at :3599 says "(c), and take it to the OWNER rather than the council … A re-scope that changes what Phase 2's results may claim touches the roadmap's declared purpose, which is owner-reserved." The closure records `Owner: council` and nowhere addresses the routing its own text had already decided. That `Owner` line also asserts the disposition "descopes nothing" 53 lines below a `Status` line reading "DESCOPED by AI council verdict A3". | open | |
| 3 | high | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:2812 | Step 12.3 is three-way self-inconsistent as landed. The glyph is `[-]` (cancelled); the prose at :2828 says "GUARDED BASELINE 2026-09-01 … the box stays `[ ]`"; and its `guarded_baseline:` block declares `category: absence-assertion`, which per `guarded_baseline.ts:39-43` is the category that MAY close `[x]` once sabotage-verified — and the block records a passing sabotage `red_proof`. The prose rationale actually given ("the population they police is empty") is the `future-mechanism` argument, not the category declared. | open | |
| 4 | high | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:2823 | The `guarded_baseline` record this change authors for 12.3 is born stale: `recheck_when: src/scripts/council_cli.ts` names a path that EXISTS today, so `guardedBaselineStaleness` (`guarded_baseline.ts:233-255`) classifies it STALE — "the mechanism the baseline was written against has arrived" — the moment it is written. All six pre-existing records in the file name paths that do not exist (`topology_selector.ts`, `training-rows.jsonl`), which is the intended semantics. The defect is invisible only because the `roadmap-status: guarded-baseline` marker was removed in the same change, so `parseGuardedBaselines` no longer sees the block. | open | |
| 5 | high | tests/scripts/ai_council/force_topology_prohibitions.test.ts:121 | Step 12.3's verify line is "one test per prohibition" and the file claims to deliver one per named authority, but P3 (destructive authorization) and P4 (the Hard Floor) share a single `it` whose only assertion is that `/topolog/i` does not match the council surface (:122-129). That assertion names neither authority, exercises neither, and is already made by P0a (:63) and, for the CLI file, by P2 (:117). The second `it` in the P3/P4 block (:131) is a spend assertion, i.e. P2's subject filed under P3/P4. Two of the five prohibitions therefore have no distinct check. | open | |
| 6 | medium | tests/scripts/ai_council/force_topology_prohibitions.test.ts:136 | The pinned count of three `if (!args.confirm)` sites rests on a rationale that is factually wrong in both the test comment (:133-135) and the roadmap (:2861): "three … one per spending subcommand". Verified against the tree: `:2490` and `:2542` are BOTH inside `cmd_run` (2399-2834) and only `:3080` is in `cmd_debate`, so there are two spending subcommands, not three. `:2490` is a bare one-line `if (!args.confirm) _stdout('… DRY PASS …')` with no `return` — a notice, not a gate. The pin therefore admits the state it exists to exclude (delete the real gate at :2542, add two notices, still three) and reds on a harmless cleanup of the redundant notice. | open | |
| 7 | medium | tests/scripts/ai_council/force_topology_prohibitions.test.ts:108 | The test titled "the --confirm gate returns before any seat is contacted" asserts only that two literal strings appear in the file. Nothing matches a `return`, and nothing establishes ordering relative to a provider call. The title claims control flow the assertions do not reach, and one of the three sites its sibling test pins (`council_cli.ts:2490`) demonstrably does not return. | open | |
| 8 | medium | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:37 | The closing note frames the guarded-baseline marker removal as a pure contract-legality matter and states "No evidence was removed … The marker went; the proof did not." `parseGuardedBaselines` (`guarded_baseline.ts:129-146`) keys on the marker, so with it gone three declared semantics stop applying to those steps: separate reporting, `recheck_when` staleness detection, and `archive_completed_roadmaps.ts:548-560`, which refuses to archive any roadmap carrying a guarded-baseline step. That refusal was the mechanism blocking the archival this same branch performs, and the note does not say so. | open | |
| 9 | medium | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:37 | "the seven `roadmap-status: guarded-baseline` markers this close removed" — six were removed, not seven. The pre-change file carried 7 occurrences, of which 6 sat on list items (7.3, 10.1, 10.6, 11.1, 12.1, 12.2) and 1 is a prose mention still present at :3957. Group 1 has seven STEPS, but 12.3 never carried a marker. A headline count in the audit record is off by one. | open | |
| 10 | medium | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:3535 | "Both seats reached A3 independently with no dissent recorded on it" overstates the artefact. `topology-blockers-drain12.json` records `rounds: 2` with a populated `blind_review_map`, and both round-2 texts are explicit reactions to each other's round-1 output — the anthropic reply is literally structured as "Points I Agree With / From Reviewer A / From Reviewer B". Convergence after cross-reading is not independence, and the entry does not distinguish the two. | open | |
| 11 | medium | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:3137 | The B1-to-B3 justification is stronger than the recorded verdict in three places. (a) "both attached the same hard precondition" then quotes two different ones — anthropic's is one coherent session, openai's is two genuinely reserved UTC windows. (b) "The precondition fails on arithmetic, not judgement" (:3144): the 60-calls-against-a-50-cap arithmetic is the premise the council ALREADY built B1 on, since both seats prescribe RAW on one UTC day and STRIPPED on the next and anthropic explicitly accepts the cap risk; what actually fails is the unrecorded self-assessment that "this run cannot guarantee" a cross-boundary session. (c) openai assigned that determination to the maintainer — "the maintainer must establish that the roadmap can remain active and that two post-decision provider-cap windows are genuinely reserved" — while the entry records `Owner: council` at :3219. | open | |
| 12 | medium | tests/scripts/ai_council/force_topology_prohibitions.test.ts:24 | The test header states "That is why 12.3 closes as a guarded baseline and not as a discharged claim." The roadmap landed 12.3 as `[-]` and removed the guarded-baseline marker set 18 minutes later (26a78f050 at 09:29, 3100b5ca6 at 09:47). The shipped test documents a disposition the tree does not carry, and it is the first place a future reader will look when P0 goes red. | open | |
| 13 | low | tests/scripts/ai_council/force_topology_prohibitions.test.ts:55 | In `FORCE_TOPOLOGY_RE` the second alternative (`forceTopolog`) is subsumed by the first (`force[-_ ]?topolog`) under the `i` flag, and the polarity test P0b (:68) plants only a `--topology` flag, so it scores exactly one of the three branches. The file's claim that "the DENIAL is tested explicitly (P0b), so a zero in P0a means something" holds for the `--topology` branch alone; an edit that broke either force-spelling branch would leave P0b green. | open | |
| 14 | low | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:787 | Step 1B.1 carries two paragraphs in direct opposition, both tagged drain run 12: "NOT RE-ATTEMPTED on 2026-08-31 (drain run 12), and the reason is quota" (:787) and "The run was made on 2026-09-01 and it did not close the step" (:809). Both are true of different days, but the run label is identical and the first is not marked superseded — the same staleness the file corrects explicitly elsewhere at :3153. | open | |
| 15 | low | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:8 | The `estate_growth_exempt` frontmatter still ends "No offsetting disposal is claimed: this roadmap has 46 open steps and is nowhere near archival" in the file this change archived at 0 open steps. Frontmatter is the first thing both a reader and a linter parse. | open | |
