# Findings: drain-conformance-check
<!-- completion-review: v1 | reviewed: 2026-09-13 | scope: 7c92f2661e60e03f594df5368fd89dd8d4d7d83e3f5c1cbe3e60e63036a2fdde | diff: 529c28e35f3f4ee0baeee98b6dba0167edf33a14 | reviewer: r2-fresh-subagent-drain-conformance-check | prompt_hash: 77461cf63050e5a5ab999ad5d06f8e52001ea80722e9c7db15b55bc0d4df3025 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-conformance-check"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-13 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 529c28e35f3f4ee0baeee98b6dba0167edf33a14
  scope_hash: 7c92f2661e60e03f594df5368fd89dd8d4d7d83e3f5c1cbe3e60e63036a2fdde
  roadmap: agents/roadmaps/road-to-a-conformance-check-that-can-fail.md
  roadmap_hash: 2fad8c3ee48a7b5197da2f5d9428bf99340317d9d2c1175c96882bed37ca3515
  ac_hash: ebe783239432bbcac597f235e7ea538c20a95d8100836f075fe3c99b19440d79
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-13T20:25:54Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
| 1 | high | src/install/preflight.ts:164 | The new `recorded-modified` remedy states `a default install leaves it alone; --force overwrites your edit`. Both halves are false against the only writer in the tree: `src/scripts/install.ts:556-558` `_resolve_file_conflict()` returns `'write'` unconditionally for deployed files, its header (line 23 and lines 27-29) states `installs always overwrite` and `A run always refreshes every deployed file with the current package content`, and `--force` is documented there as an accepted no-op. `computeConflicts` results are informational only — nothing in `src/scripts/install.ts` reads `conflicts` or `ConflictResolution`. So the branch tells an operator their edit is safe from a default `agent-config init` that will in fact overwrite it. This is the same defect class the branch exists to remove (a published claim the tree cannot back), and the new guard test `claims no recovery this tree cannot perform` scans only `txlog-clean` remedies, so nothing covers this string. | open | |
| 2 | high | docs/contracts/gui-wizard.md:154 | The same unbacked claim is published in the contract (`"your edit"; a default install leaves it alone`, and line 159 `recorded-modified changes no write: such a file already survived a default refresh`) and asserted twice in `src/install/conflict.ts:79` (`such a file already survived a default refresh ('skip')`) and `src/install/conflict.ts:197` (`it still survives`). It is also the premise of AC-6's first half (`a user-modified managed file survives a refresh`), which the roadmap flips to `[x]`; only the second half (`is named in the report`) is demonstrated by the diff. Same evidence as finding 1. | open | |
| 3 | medium | src/install/recordedOwnership.ts:107 | The new module re-implements `src/scripts/_lib/install_drift.ts`, which already parses the same manifest with the same `yaml` package for the same stated reason (the hand-rolled reader drops nested `files[]`) and already reads `tools[].files[].sha256` against an on-disk digest. That module's header explicitly keeps `installed_tools.manifest_path()` so that `there is exactly one place that decides "where is the manifest"`. `MANIFEST_RELATIVE` hardcodes the path instead, so the `AGENT_CONFIG_INSTALLED_TOOLS` override (`src/scripts/_lib/installed_tools.ts:94-100`) is ignored; and `readRecordedHashes` does not expand a leading `~`, which both existing readers do (`install_drift.resolve_entry_path`, `cmd_doctor._resolve_path:467-473`), so a `~`-recorded user-scope entry resolves under the project root. Both cases degrade every file to `unknown` silently, with no signal. The claims `mirrors installed_tools` (line 107) and `mirroring cmd_doctor._collect_manifest_entries` (line 72) are therefore inaccurate. The same branch also introduces a second, env-aware resolution of the same manifest at `cmd_conformance.ts:134`, so two new code paths resolve one file differently. | open | |
| 4 | medium | src/install/conflict.ts:224 | The unconditional `recorded-unchanged` skip silently removes findings from the one live caller. `cmd_preflight.ts:116-131` builds its policy with `knownPaths: new Set()`, so before this change every existing managed file whose planned bytes differ was reported; now every such file that still matches its recorded digest (i.e. every plain package upgrade) is dropped. The docstring at line 190 (`Skipped, as before`) and line 199 (`reports exactly what it reported before`) hold only for trees with no readable manifest — for a manifest-carrying tree with an empty `knownPaths` the output shrinks, which is the opposite direction from the roadmap goal and is undocumented. No test covers the empty-knownPaths plus populated-manifest combination; `tests/install/conflict.test.ts:309` pins the same case only with an explicitly populated `knownPaths`. | open | |
| 5 | medium | src/scripts/_cli/cmd_conformance.ts:166 | The `unknown` discriminator pairs a machine-global artefact with a project-local one: `installLogPath()` (line 113-119) is one `~/.event4u/agent-config/install-log.jsonl` per machine, while `_installLogExpected` reads a per-project manifest. Because `unknown` requires the log to be ABSENT, any machine that has ever run `agent-config setup` once in any project has a log with a non-abort tail and therefore returns `ok` for every headless install in every other project afterwards. The false green the roadmap targets survives exactly for the users who have used the shipped install path, and neither the check message nor the contract table says so. The same mismatch runs the other way on the `fail` branch, where one project's `abort` reddens every other project on the machine. | open | |
| 6 | medium | tests/install/conflict.test.ts:373 | Making `ConflictEntry.ownership` required left the `ce()` factory in this same file returning an object without it. `npx tsc -p tsconfig.test.json --noEmit` reports `TS2741: Property 'ownership' is missing ... but required in type 'ConflictEntry'` at line 374 — a type error newly introduced by this branch in a file the branch edits. It is not caught today because the root `tsconfig.json:26` excludes `tests` and `tsconfig.test.json` is referenced by no Taskfile target, workflow or package script; the runtime suite passes because the annotation is erased. | open | |
| 7 | low | src/install/types.ts:137 | The `ConflictEntry` preamble still defines the type as meaning the target `is not in policy.knownPaths`. The diff adds `ownership` to this very interface and makes that sentence false: a `recorded-modified` entry is emitted precisely when the path IS in `knownPaths`. Doc drift inside the changed declaration. | open | |
| 8 | low | src/install/recordedOwnership.ts:111 | `recordedHashesForRoot(plan.root)` treats `plan.root` as the project root, but `InstallPlan.root` is documented (`src/install/types.ts:118-121`) as the install anchor — `~/.event4u/agent-config/` for `target: 'global'`, which is not where the manifest lives, while `FileEntry` (types.ts:52-55) notes user-scope files are not under the project root at all. Ownership therefore degrades to `unknown` for any plan whose anchor is not a project root, silently and untested; `cmd_preflight.ts:127` already passes `target: 'global'` with a caller-supplied `root`. | open | |
| 9 | low | docs/contracts/conformance.md:43 | The new status is documented only in its own subsection and not reconciled with the rest of the page it sits in. The § 1 row still reads `txlog-clean proves the last install completed`, which is what the branch just established it does not prove on a headless tree; § 2's exit table still says `Green — no check failed, no drift. Warnings allowed`, with no mention that `unknown` also exits 0; and § 3's sample line `"5/5 ok; fails: none"` becomes `4/5 ok; fails: none` for an `unknown` row, which reads as a silent failure (`appendConformanceReport` counts only `ok` and `fail`). The heading also promises `the three absent-log cases` while two of the table's four rows are absent-log cases; `cmd_conformance.ts:147` repeats it as `Three absent-log outcomes`. | open | |
| 10 | low | src/scripts/_cli/cmd_conformance.ts:736 | An `unknown` row leaves the operator's verdict line printing `✅ conformance: green — installed and firing`, because `failed` (line 702) keys off `fail` only. The per-row `❔` is the sole signal, and the banner asserts exactly the proposition the `unknown` row says cannot be confirmed. `warn` behaves identically today, so this is precedent rather than a regression, but the verdict wording is the surface the roadmap's goal statement names. | open | |
| 11 | low | tests/scripts/_cli/cmd_conformance.test.ts:135 | `names a verb the dispatcher actually registers` resolves only the abort remedy, and its regex takes the first `agent-config <verb>` match in the string. The new `unknown` remedy (`cmd_conformance.ts:170-172`) names two verbs — `setup` and `doctor` — and no test resolves either through `findCommand`, so the newly added remedy is the one remedy whose verbs are unchecked. | open | |
