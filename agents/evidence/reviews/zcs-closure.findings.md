# Findings: zcs-closure
<!-- completion-review: v1 | reviewed: 2026-08-06 | scope: 3bb5bb4e4c99f082a223ff4758fddc8de76d2834cb8e91f27c2b38013df4fec0 | diff: 2c70288e372254be0cd0a23acfdd76f3b1318cb1 | reviewer: r2-fresh-subagent-zcs-closure | prompt_hash: 003da7b0007034eceedd50116ffd4e228a9b7f328a47634f529c7e51f3b3f638 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 2c70288e372254be0cd0a23acfdd76f3b1318cb1
  scope_hash: 3bb5bb4e4c99f082a223ff4758fddc8de76d2834cb8e91f27c2b38013df4fec0
  roadmap: agents/roadmaps/road-to-zero-ceremony-settings.md
  roadmap_hash: 7300f102b3ca16ebb9fa2339a96378cccebb9d0c69061f7408bddaabbb77dedb
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-06T22:27:00Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/server/routes/ping.ts:135 | `systemUser: systemUserName()` is evaluated **inside the request handler**, and `systemUserName()` now delegates to `resolveNicknamePrefill()` → `readGitUserName()`, which runs `execFileSync('git', ['config','user.name'], { timeout: 1000 })`. Every `GET /api/v1/ping` therefore forks a subprocess and blocks the single-threaded Fastify event loop until git returns — up to a full second when git is slow, hung, or on a cold/network-backed PATH, during which no other route is served. The prior implementation was a cheap `userInfo()` syscall, so this is a regression on the endpoint the file's own header calls a "liveness probe" (polled by the UI bundle and CI smoke tests). The resolved value is machine-static for the process lifetime and is never memoised: computing it once at `pingRoute()` construction, or caching the first result, removes the cost entirely without changing the response. | fixed | e0-fix — ping uses cachedNicknamePrefill(); memo + reset, 3 cases incl. object-identity |
| 2 | medium | src/server/nicknameResolver.ts:60 | The `userInfo()` floor returns `source: 'env-user'` — the label `src/shared/nicknamePrefill.ts:597` reserves for `$USER`. `NicknameSource` has no member for the OS-account rung, so a caller cannot distinguish "resolved from `$USER`" from "resolved from the OS account after `$USER` was absent". This contradicts the change's own stated design (`userInfo()` "kept as a floor … what changed is its rank"): the rank is unobservable, and the rule the module exists to satisfy asks the caller to *say* where the name came from. Any future consumer that reports provenance reports the wrong rung on exactly the path this diff added. | fixed | e0-fix — NicknameSource gains `os-account`; the floor reports its own rung |
| 3 | medium | src/server/nicknameResolver.ts:45 | No test covers `resolveNicknamePrefill` or `readGitUserName`. The roadmap pins Phase 4 step 2 with `<!-- verify: npx vitest run tests/shared/nicknamePrefill.test.ts -->`, but that suite only exercises the injected-pure `nicknamePrefill(probe)`. The half that actually ships behind `ping.ts` — the git invocation, the collapse-every-failure-to-`undefined` contract, the floor's position *and* its label (finding 2) — is unverified, so the declared verify command cannot detect a regression in production behaviour. The stated rationale for the pure/impure split ("the chain stays testable without a machine") holds for the chain and silently leaves the resolver untested. | fixed | e0-fix — tests/server/nicknameResolver.test.ts, 10 cases (git contract, floor label, memo) |
| 4 | medium | tests/scripts/jit_ask_budget.test.ts:483 | The migration pin is two unanchored substring checks: `toContain(key)` and `toContain('settings-ask-protocol')`, matched anywhere in the file. A file that restores its full bespoke ask prose and merely keeps a see-also link to the protocol passes. The block's own comment (:455) states it guards precisely that regression — "a rewrite of any of these four files could restore its bespoke ask prose and no gate would notice" — and the roadmap step it pins is "delete their bespoke per-setting prose". Nothing in the assertion detects restored prose, so the pin does not pin what it claims. | fixed | e0-fix — body-vs-see-also discriminator; proved by mutation (proximity ALSO passed the demotion, so it was wrong too) |
| 5 | medium | tests/scripts/jit_ask_budget.test.ts:500 | "the carved-out keys did not quietly get migrated anyway" iterates the four `MIGRATED` files and asserts they do not contain `worktrees.mode` / `decision_engine.on_block`. Those four files have no reason to mention either key regardless, and a real migration of a carved-out key would land in `src/rules/settings-ask-protocol.md` (the carve-out prose asserted at :492) or in `scope-control.md` — neither of which is scanned. The test therefore cannot fail for the reason its comment gives ("it should break this test rather than pass silently") and passes vacuously; the comment at :458 calls this the "load-bearing half" of the suite. | fixed | e0-fix — repointed at the carve-out HOME files, where a real migration would land |
| 6 | medium | src/server/routes/ping.ts:50 | `PingResponseSchema.systemUser` still documents "Best-effort **OS account name** (e.g. `matze`)". After this change the field returns `git config user.name` first — a display name ("Mathias Berg"), not an OS account. The same diff updated `docs/contracts/gui-wizard.md:328` for exactly this fact, so the in-diff doc set is now internally inconsistent, and the missed surface is the one the file header calls the source of truth tests assert against. A UI or test consumer following the schema comment is misled about the field's shape. | fixed | e0-fix — PingResponseSchema comment states the chain and that it is NOT an OS account |
| 7 | medium | agents/roadmaps/road-to-zero-ceremony-settings.md:175 | The decision to keep the Phase-2 item at `[~]` rests on "with the three Phase-3 steps re-encoded to `[ ]` this roadmap keeps `open_ ≥ 3`, so it does not archive". That premise expires the moment the recorded blocker `absent-is-not-default-for-projection-mode` clears and Phase 3 closes: `count_open` returns to 0 with one `[~]` still present, re-firing the same `roadmap-progress-sync` Iron Law 3 condition the § Glyph re-encoding section documents as a repo-wide **commit refusal** with no agent-side bypass. The section declines the council's fix on a temporary property without recording the recurrence as a blocker, a follow-up, or an exit condition — so the next contributor to close Phase 3 hits an unannounced commit block. | fixed | e0-fix — recorded as an exit condition on the step AND in the blocker resolution |
| 8 | low | src/scripts/memory_learn_hook.ts:73 | The docstring states `readLearnValue` returns "The raw scalar … or `undefined` when the file or the key is absent", and a sibling test (`memory_learn_hook.test.ts:113`) pins "distinguishes an absent key from a key set to false". Neither is accurate: a present-but-non-`true` scalar (`yes`, `1`, `on`, `maybe`) also returns `false` via `m[1] === 'true'` at :94, so a malformed value is indistinguishable from a deliberate opt-out. The fail-safe direction is right, but the exported tri-state does not mean what it documents, and a user who wrote `learn_on_session_end: yes` gets a silent permanent no-op with no diagnostic on either the value or the shape. | fixed | e0-fix — docstring states the false/malformed ambiguity; a test pins it |
| 9 | low | tests/shared/nicknamePrefill.test.ts:73 | The rule file is read via `path.resolve('src/rules/settings-ask-protocol.md')` — CWD-relative, and evaluated in the describe body (i.e. at collection time), so a runner rooted anywhere but the repo root turns a targeted assertion into a whole-file collection error. Same pattern at `tests/scripts/memory_learn_hook.test.ts:84`. The sibling suite for the same feature already establishes the robust convention: `const REPO = path.resolve(__dirname, '..', '..')` (`jit_ask_budget.test.ts:40`), anchored on the test's own location rather than on the process CWD. | fixed | e0-fix — both suites anchor on __dirname, not the process CWD |
