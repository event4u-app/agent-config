# Findings: drain-roadmap-6-one-resolver-r2
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: 9d20281bcd94da3c27257021be546286ec3f71cce60448fbfd49d6e72afbeccf | diff: fc606f1fbe623c69bcfa199f8dd8a9dfb559ad6b | reviewer: r2-fresh-subagent-drain-roadmap-6-one-resolver-r2 | prompt_hash: 51a3c7301d70eae8509bf13a653eaf9a1d18faedc383f5cc4de28e1912c4f12e -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-roadmap-6-one-resolver-r2"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: fc606f1fbe623c69bcfa199f8dd8a9dfb559ad6b
  scope_hash: 9d20281bcd94da3c27257021be546286ec3f71cce60448fbfd49d6e72afbeccf
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: e36275a9854bef2e4a1dc4892fb74f3160dc74be6a2a0dd4481287516c4166d7
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T12:20:33Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/one_resolver_invariant.ts:116 | `stripNonCode` and `stripComments` remove block comments FIRST with an unbounded `/\*[\s\S]*?\*/`, so any `//` line comment or string containing the two characters `/*` opens a spurious block comment that runs to the next `*/` anywhere in the file and deletes the real code between them. Measured end-to-end: a synthetic tree whose second resolver file opens with `// Routes live under packages/*/commands/` followed by `export class CouncilTopologyRouter` returns ZERO violations while the file IS present in `scanned`, so the finding-1 anti-vacuity discriminator cannot catch it; the same trick on `judgment_ladder.ts` hides a literal `import { NECESSARY_TRIGGERS } from '../ai_council/necessity.js'`. Both halves of the invariant are evaded by one comment. This is not hypothetical: 34 non-test `.ts` files under `src/` already carry such a comment today, and in 12 of them `stripNonCode` deletes top-level `export` declarations outright, e.g. `src/scripts/backfill_model_tier.ts:176` (`// Commands live under packages/*/commands/`) removes 6510 of 13270 characters including `export function run`. R1 finding 5's defect is moved, not removed: its false positive is gone, and a wider false negative replaces it that now also reaches `declaresRouter`, which had no stripper before. Defeats AC 1. | open | |
| 2 | medium | src/scripts/_lib/one_resolver_invariant.ts:189 | The finding-4 repair asserts only that the sanctioned path EXISTS (`fs.existsSync`), never that it is a resolver, and line 205 still excludes `SANCTIONED_RESOLVER` from `declaresRouter` entirely. Verified: a tree whose `judgment_ladder.ts` contains nothing but `export const NOTE = "moved"` scans green with `scanned` naming that file. `rm` is caught, gutting or moving-behind-a-stub is not, so the positive half of AC 1 ("remains the single task-side orchestration resolver") is still enforced by file presence rather than by content. R1 finding 4 is only partially repaired. | open | |
| 3 | medium | src/scripts/_lib/one_resolver_invariant.ts:145 | `importsCouncilInternal` misses the side-effect import form, which needs neither `from` nor a paren. Verified: `import '../ai_council/necessity.js';` and `import '../ai_council';` both return `false`, while `import type`, re-export `from` and the three added forms return `true`. The test at tests/scripts/one_resolver_invariant.test.ts:162 is titled "covers every import form, and only real ones" and the module docstring says "all three misses in finding 5" are covered, so the enumeration is presented as complete while a fourth ES import form is absent. R1 finding 5 is only partially repaired on the false-negative half. | open | |
| 4 | medium | src/scripts/_lib/one_resolver_invariant.ts:84 | `EXPORT_FORMS` is described as "deliberately exhaustive over the shapes the reviewer measured", but three further ordinary export syntaxes still evade it, verified: `export declare class CouncilTopologyRouter {}`, `export enum CouncilTopologyRouter { a }` and `export function* resolveCouncilRoute()` all return `declaresRouter === false`. `export declare class` is one keyword away from `export abstract class`, which IS covered, so a reader has no signal that the neighbouring form is not. The `function` alternation requires `\s+` immediately after the keyword, which is what excludes the generator form. R1 finding 2 is repaired for the nine measured shapes only. | open | |
| 5 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:238 | The closure record says "Four denial arms must stay GREEN" and enumerates four, but the polarity block at tests/scripts/one_resolver_invariant.test.ts:190 contains five. The unlisted arm is "stays green on a router name inside a comment or a string" (line 213) - the one arm that exercises the comment stripper this review's finding 1 shows to be unsound. Everything else checked in the closure prose is accurate: 32 tests exist and pass, the four sensitivity arms do each call `expectCleanBaseline()`, and the reproducer claim was re-run - with `SCAN_ROOT` mutated to `srcTYPO` the suite fails exactly 15 of 32. | open | |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
