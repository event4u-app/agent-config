# Findings: estate-triage-batch-1
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 16a0b81ef2e582392e03e277464cf7e25f96fb3b638f7f4350b4e27e4d272f2b | diff: f893f035485f7f7603125e2a2c32781da7396715 | reviewer: r2-fresh-subagent-estate-triage-batch-1 | prompt_hash: 633ded9cda53546685013141e649e1c4910067ca94ac321d409791374f26a43a -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: f893f035485f7f7603125e2a2c32781da7396715
  scope_hash: 16a0b81ef2e582392e03e277464cf7e25f96fb3b638f7f4350b4e27e4d272f2b
  roadmap: agents/roadmaps/road-to-estate-drawdown.md
  roadmap_hash: 9a03bfa1a46fb4dd14d16b5cc06a411d147e66eee7b016ad4afea117e5593943
  ac_hash: 0308d5582ca2f060845f17b92dd930b0fe6865baf31272e8b3fad4c2880611bd
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T03:52:03Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | agents/roadmaps/later/road-to-skill-ecosystem-executable-payloads.md:17 | The resume condition's content bar is split into a sibling `**What it must record:**` field, explicitly "on purpose" so the probe stops seeing a conjunction. `_truncateAtNextField` cuts the condition at that label, so `COMPOUND_RE` and `EXISTENCE_PREDICATE_RE` never see the bar and the probe will report FIRED the moment an empty file appears at that path. `later/road-to-catalogue-host-fit.md:15` has the identical logical shape (existence + content bar) written inline and correctly resolves `undecidable` — same condition, opposite verdict, decided only by which bolded field the bar sits in. Worse, this is documented as the intended authoring pattern, so it is a general bypass of the guard added in this same diff, whose own docblock (resume_probe.ts:322-329) justifies itself with exactly this failure ("an existence test would have reported FIRED and un-parked a roadmap whose bar is 13 observations away"). | open | |
| 2 | medium | agents/roadmaps/road-to-estate-drawdown.md:184 | Batch note states "**Estate effect:** active 37 → 33". Three artefacts written in the same change say 34: `src/config/estate-count-budget.json` sets `active_roadmaps: 34` and its own `why` says "active_roadmaps 37 -> 34 is WALKED DOWN"; the regenerated `agents/roadmaps-progress.md:5` header says "34 open roadmaps". The live tree agrees with 34 (37 top-level `*.md` minus 3 carrying `status: draft`). The narrative number is off by one against the ratchet the same PR registers. | open | |
| 3 | medium | src/agent-src/scripts/resume_probe.ts:19-26 | The module docblock still states the pre-change contract — "Two shapes are machine-decidable", `fired` = "every roadmap the condition names has archived...", `unmet` = "at least one named roadmap is still active...", `undecidable` = "the condition names no roadmap this tree can resolve". After this diff all three verdicts also arise from the path branch, and the `why` string at line 460 was updated to "no roadmap or repo path" while the docblock defining the exported `Verdict` contract was not. A reader of the header now gets a definition the code contradicts. | open | Mirrored byte-identically in dist/agent-src/scripts/resume_probe.ts |
| 4 | medium | src/agent-src/scripts/resume_probe.ts:330 | `EXISTENCE_PREDICATE_RE = /\bexists\b/i` is tested against the whole clause, so it checks word PRESENCE rather than a predicate bound to the matched path. A clause with a detached or negated "exists" takes the path branch with wrong semantics — e.g. "`docs/legacy.md` no longer exists" reports FIRED while the file is still there, and "…once a workaround exists" pairs the existence word with a path it does not govern. There is no negation guard and no test for either shape; the suite only covers a clause with no "exists" at all. Given the docblock's own framing that a false FIRED un-parks live work, the check is weaker than the reasoning it carries. | open | |
| 5 | medium | src/agent-src/scripts/resume_probe.ts:441 | The path branch is reachable only when `refs.length === 0`, so a clause naming one roadmap AND one path silently drops the path and decides on the roadmap half alone — a partial-match resume. `COMPOUND_RE` does not close this: the `REPO_PATH_RE` docblock (line 100-104) already notes it "does not catch a comma", and applies that reasoning to reject two paths, but the same comma-joined path-plus-roadmap clause takes the roadmap branch and can report FIRED with the path absent. Asymmetric guard, no test. | open | |
| 6 | low | src/agent-src/scripts/resume_probe.ts:269 | `_sentenceHead` toggles `inCode` on every backtick and silently returns the entire remainder when no terminator is found outside code. An odd backtick count in the tail leaves `inCode` stuck true, so the whole commentary paragraph becomes the clause — which can pull a backticked path out of the *commentary* into `referencedPath` and decide the verdict on prose the condition never stated. No balance check, no fallback, and no test for the unbalanced case. | open | |
| 7 | low | agents/decisions/estate-triage-dispositions.yml:66-70 | The recorded `probe:` reason for `road-to-live-app-verdict` is "undecidable BY DESIGN. The condition names an external fact and a directory rather than a file, and a directory exists in every checkout." That is not the mechanism that decides it: the authored clause contains "and" ("...against a live app and that run is recorded under `agents/evidence/`"), so `COMPOUND_RE` returns `undecidable` with `why` = "compound condition" and the directory/extension reasoning is never reached. The park note at `later/road-to-live-app-verdict.md:11-13` compounds this — it says naming the whole conjunction "would read as FIRED to the resume probe", yet the condition it substitutes is itself a conjunction. The file header declares verdicts terminal and the register append-only, so a wrong recorded mechanism cannot be corrected in place. | open | |
| 8 | low | agents/decisions/estate-triage-dispositions.yml:24 | `schema_version: 1` is declared while no schema file or validator ships with it, and the ten rows carry seven distinct ad-hoc optional keys (`thin_tail_recorded`, `side_effect`, `unlisted_blocker_finding`, `file_untouched_because`, `live_defects_recorded`, `scheduled_into`, `note`) beyond the common set. A version number nothing reads cannot detect the drift it exists to prevent, and the next batch has no machine-checkable row shape to append against. | open | |

<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
