# Findings: run-continuation-provenance — round 4
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 57e6acd5b451f923a1a38c66666d77a3faf44ec9fba2e1b7fee7f3cf22760733 | diff: 5fa347a5ae9496eb0b4876ffd0e93361f03048ef | reviewer: r2-fresh-subagent-run-continuation-provenance | prompt_hash: 6a93033dc920cd1091569930f1635bdf3e237406a535a6842439129a949ff915 -->
<!-- evidence-type: v1 | type: original-review | declared: 2026-08-19 -->
<!-- Round 4 of this branch, archived. Bound to scope
     57e6acd5b451f923a1a38c66666d77a3faf44ec9fba2e1b7fee7f3cf22760733, which the
     fixes below moved past; the live `findings.md` carries the current round. -->

<!-- context-manifest: v1
inputs:
  diff_sha: 5fa347a5ae9496eb0b4876ffd0e93361f03048ef
  scope_hash: 57e6acd5b451f923a1a38c66666d77a3faf44ec9fba2e1b7fee7f3cf22760733
  roadmap: agents/roadmaps/road-to-run-continuation-observation.md
  roadmap_hash: 1dd7051bf33c25b94468e2a1fdec0b13b9a67786d690fb0bfdac3c487809d0db
  ac_hash: bb34537a4ce90a2ac144c0346d9d3817fc8ddd788722900f17cdb6b7ed59bea7
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T17:50:31Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/hooks/run_continuation_hook.ts:578 | `resolveRoadmap` cannot distinguish "the session tree has no copy yet" from "this run removed the copy", so a run that completes its roadmap and archives it with the mandated same-change `git mv` to `agents/roadmaps/archive/` finds `<sessionRoot>/agents/roadmaps/<slug>.md` gone on the next stop fire, silently falls back to the parent's un-archived copy, reads its still-open steps, and BLOCKS with "continue with this step now" against a path that no longer exists in the session's tree — the run is re-engaged until the iteration cap or a stall halt instead of ever reaching `complete`. | fixed | a01b5a0bc |
| 2 | medium | src/scripts/hooks/run_continuation_hook.ts:823 | `state.history` accumulates open-step counts whose source file is re-chosen per fire by `resolveRoadmap`, and `ladder`'s stall test compares those numbers for equality — a worktree session whose branch predates the roadmap reads the parent copy at fire 1 and its own copy from fire 2 on, so `history` mixes counts from two different documents: a genuinely frozen session-tree count preceded by a differing parent-tree count resets the stall streak (run keeps engaging past the window), and a parent count that happens to equal a moving session count contributes a false match toward `halt-stall`. Nothing records which file each entry came from. | fixed | a01b5a0bc |
| 3 | medium | src/scripts/hooks/run_continuation_hook.ts:477 | The docblock asserts that on a degraded resolution `session_cwd` "is then a path that is neither `session_root` nor under it, which no healthy resolution produces", and three reachable inputs falsify it: a cwd inside a repository nested under the parent checkout (vendored clone, fixture repo) fails the same-repo identity check, a cwd that does not exist under the parent survives `normalizeDir`'s catch branch, and a relative `cwd` is `path.resolve`d against the reader's root — in all three `session_checkout` falls back yet `session_cwd` sits under `session_root`, so the ledger line reads as a healthy same-tree run, the exact blindness round 3 finding 2 removed only for the subdirectory case. | fixed | a01b5a0bc |
| 4 | medium | src/scripts/hooks/run_continuation_hook.ts:670 | The event carries no field naming the roadmap file actually read, while `resolveRoadmap` may have taken either tree; on a line with `session_root !== workspace_root` and `open: 2` a ledger-only reader cannot tell whether the count came from the session copy (the fix working) or from the parent copy after a silent fallback (the finding-1 defect still live) — which is the ambiguity the six provenance fields exist to remove, left open on the one tree-dependent number the line carries. | fixed | a01b5a0bc |
| 5 | medium | src/scripts/session_register_hook.ts:461 | `session_checkout` returns a realpath-normalised path on its resolve branch (lines 451-461) but the raw `workspace_root` on all four fallback branches, and `foreign_sessions_block` compares the two stored forms with `path.resolve`, which does not resolve symlinks (line 670) — so under a repo reached through a symlinked ancestor, session A (host reports `cwd`, record stores `/private/var/…/repo`) and session B (host silent, record stores the envelope's `/var/…/repo`) read as DIFFERENT worktrees, and two sessions sharing one working tree and one git index get the benign "Separate trees … NOT a reason to stop" note instead of the same-worktree COLLISION prompt. The walk-up widens the exposure by moving subdirectory sessions onto the normalising branch. | fixed | a01b5a0bc |
| 6 | medium | tests/scripts/session_register.test.ts:62 | The git-env/config hardening this diff adds to the sibling fixture helper (`run_continuation_dispatch.test.ts:117-137`, round 3 finding 7) was not applied here, while the same diff adds a new `git worktree add` case to this file (line 867): with `GIT_DIR` exported — the suite run from inside a git hook, the case the sibling docblock names — `make_repo`'s `git config user.email` (line 71) writes the REAL repository's local config instead of the fixture's, and a global `commit.gpgsign` with no available key reds `make_repo`'s commit for a reason unrelated to the concern. | fixed | a01b5a0bc |
| 7 | low | src/scripts/hooks/run_continuation_hook.ts:579 | `resolveRoadmap` interpolates the claim file's `slug` into a path with no containment check, while `claim_is_stale` in the module it imports from treats exactly that check as necessary before any read (`session_register_hook.ts:357-361`) — a claim whose `slug` carries `../` or a leading separator now makes the concern read a file outside `agents/roadmaps/` in two trees rather than one, and `_read_claim_file` accepts any non-empty trimmed string as a slug. | fixed | a01b5a0bc |
