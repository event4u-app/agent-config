# Findings: standing-context-40k-phase4
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 81f9ca8aea66364a3cbbe1d039037fe2c5e51266b9c646756a7dce6e1127d03b | diff: 016cd6232719792909e66c21019878efca69f5eb | reviewer: r2-fresh-subagent-standing-context-40k-phase4 | prompt_hash: 8320fa4731a9312a494e76f7fbf4bb5901ac3f3c008f38ac6eea18c197c7023c -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 016cd6232719792909e66c21019878efca69f5eb
  scope_hash: 81f9ca8aea66364a3cbbe1d039037fe2c5e51266b9c646756a7dce6e1127d03b
  roadmap: agents/roadmaps/road-to-standing-context-40k.md
  roadmap_hash: 425882734a4d2f592dad8547d0bd655197e662dc4008f468f199f4cc065b2a45
  ac_hash: 59f47cf4c6dc28cfbcf68350c5ef223a056bd45105fe2bd7560bb8db7891f7c4
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T11:27:09Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/injection_budget.ts:207-221 | Documented invariant is false and the operator message contradicts the code. `injection_budget.ts:43-44` and the contract at `docs/contracts/hook-architecture-v1.md:242-243` both state that when the exempt set alone exceeds the ceiling the dispatcher "drops nothing"; `dispatch_hook.ts:1500-1502` prints "nothing dropped". The loop at :208-220 in fact drops EVERY droppable advisory first, without checking whether dropping can ever bring the total under the cap, so with one 5000 B exempt concern and a 1000 B cap the advisories are deleted for zero benefit while the stderr claims none were. Pinned in that shape by `tests/scripts/hooks/injection_budget.test.ts:170-181`. | open | |
| 2 | high | src/scripts/hooks/dispatch_hook.ts:1469-1472, 1507-1513 | Turn-start slot is budgeted against the PREVIOUS turn's spend. `spent_before` is read before shaping, but the reset is applied only on the write (`reset: args.event === "user_prompt_submit"`, :1511). So on `user_prompt_submit` — the slot the design names as the turn start — `readTurnSpend` still returns the leftover total of the turn that just ended, and a turn that finished near the ceiling drops the next turn's opening advisories. This is exactly the failure `injection_budget.ts:261-263` says must never happen ("an accounting error must never be the reason an advisory disappears"). `tests/hooks/injection_budget_dispatch.test.ts:160-175` asserts only the write side, so nothing covers it. | open | |
| 3 | medium | src/scripts/hooks/dispatch_hook.ts:1473-1516 | Shaping input and emission set are computed from different predicates. `shapeEmissions` is fed every entry of `concern_messages`, but only entries with `rc === final_rc` ever reach `emitFor` (:1515-1516). `keptBytes` — persisted as the turn spend and compared against the ceiling — therefore counts messages the dispatcher never writes, and a `*_drop` record can be logged for a line that would not have been emitted anyway. The module header's claim that "what the dispatcher WRITES per turn now has a ceiling" does not hold for the quantity actually measured. | open | |
| 4 | medium | src/scripts/hooks/dispatch_hook.ts:1473-1513 | Shaping runs on platforms that emit nothing. `host_semantics.ts:54` sets `VERIFIED_PLATFORMS = {claude}` and `emitFor` returns empty stdout/stderr for every other platform (:113-115). Shaping, the `*_drop` records into the consumer's dispatch-issues log, and the persisted counter increment all still run there, so on non-claude hosts the log reports advisory suppressions for output that never left the process. | open | |
| 5 | medium | src/scripts/hooks/dispatch_issues.ts:45-53 | Routine policy outcomes are written into a rare-failure diagnostic log with no dedupe. The four pre-existing codes fire on exceptional failures; `injection_budget_drop` / `nudge_interference_drop` fire as a normal per-dispatch outcome. `log_dispatch_issue` rewrites the whole file per call and rotates at `LOG_CAP = 200` (:171-191), and `hooks_doctor.ts:245` surfaces only the LAST 20 entries — so routine drops crowd out the `script_not_found` / `permission_denied` records the log and the doctor view exist for. The distinct codes keep the classes nameable but no consumer filters on them. | open | |
| 6 | medium | src/scripts/hooks/dispatch_hook.ts:1495-1503 | `ceilingExceeded` stderr blames the wrong cause. The flag is `spent + total(survivors) > cap` (`injection_budget.ts:221`), which is equally true when the whole overflow comes from `spentBytes` accumulated by earlier dispatches and no exempt concern is involved. The message states "exceeded by exempt concerns alone", which misdirects debugging of precisely the accumulation failures in findings 2 and 3. | open | |
| 7 | medium | src/scripts/hooks/dispatch_hook.ts:1465-1471 | Turn counter keyed on an identifier that is unique per invocation when absent. `_resolve_session_id` (:187-196) falls back to `dispatch-<ts>-<pid>`, documented there as deliberately "not stable across invocations". On any host that does not send `session_id`, `readTurnSpend` never matches, the per-turn ceiling silently degrades to per-dispatch, and the state file is rewritten every dispatch with a key that can never be read back. | open | |
| 8 | medium | src/scripts/hooks/dispatch_hook.ts:1511 | Counter never resets where `user_prompt_submit` is not bound. Reset is tied to that one event, but the slot is absent on hosts this repo documents as lacking it. There the counter is only ever incremented (`pre_tool_use`, `post_tool_use`, `stop`) and grows monotonically for the whole session, so after the ceiling is reached every droppable advisory is dropped for the remainder of the session and the `ceilingExceeded` stderr fires on every dispatch. | open | |
| 9 | medium | src/scripts/bench_hook_injection.ts:99-104 | An UNRESOLVED aggregate passes the gate green. `perTurnAggregate` returns `breach: false` alongside `bytes: null`, and `main` reds only on `turnAggregate?.breach === true` (:277). Once `gate_on_ceiling` is armed, a composite derived over a subset of its counted slots — the case the file header calls load-bearing because "low is the direction that makes a ceiling look met" — exits 0. The protection is a printed word, not a gate. Pinned as-is by `tests/scripts/bench_hook_injection_aggregate.test.ts:62-73`. | open | |
| 10 | medium | src/scripts/bench_hook_injection.ts:277-279 | Aggregate breach produces a CI red that names no cause. When only `turnAggregate.breach` is true the stderr line reads "0 concern breach(es), 0 slot-sum breach(es)" and the process exits 1; the aggregate is not mentioned in the failure message at all. | open | |
| 11 | low | src/scripts/bench_hook_injection.ts:194-207 | Partial slot failure is reported as a resolved number, not UNRESOLVED. A concern missing from `CONCERN_REGISTRY` pushes to `errors` and `continue`s (:197-200), but `slotSums[event]` is still written (:207-209), so the slot contributes a silently low sum. Only a wholly missing fixture file (:190-191) yields UNRESOLVED — weaker than `perTurnAggregate`'s docstring claim that "a missing slot is an unresolved measurement, never a small one". | open | |
| 12 | low | src/scripts/hooks/injection_budget.ts:192-194 | Drop detail promises a comparison and supplies one side: it prints the loser's `nudge_rank` and then the literal text "vs the winner's", with the winner's rank never interpolated. | open | |
| 13 | low | tests/scripts/bench_hook_injection_aggregate.test.ts:129 | `if (twin === undefined) return;` makes the test pass silently if the latency twin's `per_turn_composite` row disappears — the exact drift the test exists to catch becomes a green run rather than a red one. | open | |
| 14 | low | tests/scripts/nudge_interference.test.ts:26, 52 | The corpus runs `routePointers` against the live `src/skills` catalogue, so an unrelated SKILL.md description edit can flip the `fires` set of the near-miss rows and red this file with a nudge-interference message. The sibling end-to-end fixture deliberately uses a temp two-entry catalogue for this exact reason (`tests/hooks/injection_budget_dispatch.test.ts:18-21`); the two files disagree on the hazard. | open | |
| 15 | low | tests/scripts/nudge_interference.test.ts:64 | Corpus candidates are built with `rc: RC_ALLOW`, but both nudge hooks return `EXIT_WARN` in production (`delegation_nudge_hook.ts:518`, `skill_route_hook.ts:365`). Inert while `capBytes: null`, but the fixture exercises `evictionOrder` under an rc the dispatcher never produces for these concerns, so it cannot catch an ordering regression under a live cap. | open | |
