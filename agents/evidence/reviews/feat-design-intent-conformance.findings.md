# Findings: feat-design-intent-conformance
<!-- completion-review: v1 | reviewed: 2026-09-09 | scope: 4e5a46567233db6f4c19dc9f981e5ecc180848817356d062c2e3e0ef4f05d24d | diff: e930ee01fff507f7f8076b81b7eb407dfe2fadfc | reviewer: r2-fresh-subagent-feat-design-intent-conformance | prompt_hash: 029faf30afe11266271b279f9744d0fb72431f54d32f9d1ffa76b4fb314d6518 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-feat-design-intent-conformance"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-09 -->

<!-- context-manifest: v1
inputs:
  diff_sha: e930ee01fff507f7f8076b81b7eb407dfe2fadfc
  scope_hash: 4e5a46567233db6f4c19dc9f981e5ecc180848817356d062c2e3e0ef4f05d24d
  roadmap: agents/roadmaps/road-to-design-intent-conformance.md
  roadmap_hash: a687c857874c63a758c5d34cf0082b7ed021a1027f5f12ab66c77b4fbcf7f142
  ac_hash: 02c0f26b97b640a7ab76f607a650987e4af29023b1c220b503b727b704cd181c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-09T10:27:42Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/skills/tailwind-engineer/SKILL.md:67-70 | The gate added for round-1 finding 1 closes the `hard-floor` branch only, and buys the default branch with a reading of `design-fidelity` that the rule does not carry. The skill states as fact that "`strict` and `structural` scope their confirmation requirement to a *visible* deviation". Verified against the rule: the `structural` row (`design-fidelity.md:120`) carries **no** visibility qualifier — its only grant is filling a spec that is "genuinely **silent**", and an artifact stating `#3B82F6` is not silent — and the Iron Law (`:57-69`) forbids swapping "SPACING, OR COLOUR … WITHOUT EXPLICIT CONFIRMATION", unqualified, above the mode table it is being read under. So under the DEFAULT mode row 2 still writes the project token in place of the artifact's value against a *report* rather than a confirmation, which is the behaviour round-1 finding 1 named. The roadmap knows this reading is not in the rule: its Context records `grep -cE 'reconcil\|tolerance\|approximat\|nearest'` = 0/0 over the rule and its guideline (re-run on this branch: 0 and 0) and "`strict` literally forbids the second owner directive", and re-framing `strict` so an in-tolerance value becomes a reported reconciliation is this roadmap's own **step 3.4**, gated on `blocker: approximation-tolerance` and `blocker: rule-body-cap`, both `Status: open`. Step 1.2 shipped that re-framing in the skill instead of the rule, ahead of both blockers — the same pre-emption round 1 flagged, one level down. | fixed | f1ed5f44f |
| 2 | high | src/skills/tailwind-engineer/SKILL.md:72-73 | The `hard-floor` carve-out is unreachable from the layer the rule tells the user to set. The skill instructs `agent-config settings:get design.fidelity_mode`, "missing → `strict`". `MERGEABLE_KEYS` (`src/scripts/_lib/agent_settings.ts:265-296`, read on this branch) contains no `design.` key, so a user-global `design.fidelity_mode` is filtered out and resolves as unset — and `design-fidelity.md:113-115` explicitly tells the reader the cascade "starts user-global". A user who sets `hard-floor` globally therefore gets `strict`, and the skill then applies row 2: the one branch that must never be autonomous fails OPEN, silently, with no drop warning (the roadmap's second half of that Context bullet: `userGlobalDrop()` probes only the flat file). AC-2 is `[x]` on "gates the reconciliation on `design.fidelity_mode`" — true for the project layer only. The fix is this branch's own step 2.3 / AC-5, both still open, and nothing in the shipped skill says the gate is half-wired. | fixed | f1ed5f44f |
| 3 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:155-158 | `_WRITING_MARKERS` closes three of the writing forms round-1 finding 2 named and leaves siblings of the same class open. Probed against `creatableTokens` on this branch (each returns ZERO candidates, i.e. allowed, where the token scan this commit replaced blocked): `sed 's/a/b/w <inbox>/out.txt' in.txt` — the `s///w` write flag, missed because `[wW]\s` requires the preceding char in `['"\s;]` and here it is `/`, while the docstring at :144-145 asserts "`sed`'s `w` command opens a file" is covered and the suite pins only the standalone-`w` form; `awk 'BEGIN{print "" \| "mkdir -p <inbox>"}'` — awk's output-pipe-to-command, missed because `[^\|]*>` deliberately stops at `\|`, and this one genuinely creates the directory; `sort -o <inbox>/out.txt in.txt` and `yq -i '.a=1' <inbox>/x.yaml` — two allowlisted verbs (:136, :137) that write by the same mechanism `sed -i` does and carry no marker at all. Risk-register row 1's mitigation ("an unknown verb … is still judged") does not reach a known verb that writes or executes. | fixed | f1ed5f44f |
| 4 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:158 | The awk marker re-introduces the false positive the change exists to remove, in exactly the direction :151-153 says a shared regex would ("Scoped per verb rather than as one combined pattern, because … a shared regex would reintroduce the false positive at the other end"). `\bprintf?\b[^\|]*>` does not stop at the end of the awk program, so it matches a *shell* redirect that follows it. Probed: `awk '{print $1}' <inbox>/x.txt > /tmp/out.txt` is JUDGED (refused) — a pure read of an inbox path writing to an unrelated file — while the same command without the redirect is allowed. The suite cannot catch this: its `still allows the plain reads` case uses the redirect-free form of the same command. | fixed | f1ed5f44f |
| 5 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:238-247 | `hasHeredoc` drops the read-only skip and scans the RAW command for path tokens, so a heredoc whose body merely QUOTES a speaking inbox path is refused even when the write target is unrelated and nothing creates anything. Probed: `cat <<'EOF' > tests/hooks/x.test.ts` with `const p = '<inbox>/x.md';` in the body → JUDGED. That is mention-blocking, which `invokedSegments`' own docstring (`git_command_classifier.ts:613-614`) calls "the worst false-positive shape a refusing gate can have", and it is asymmetric with the case this diff fixed — `printf '%s' '<inbox>/x.md'` is allowed. It is also self-inflicted on this branch: `tests/hooks/block_speaking_inbox_dir.test.ts` carries 48 occurrences of a speaking `agents/tmp/<name>/` literal, so authoring that very file through a heredoc is refused by the guard it tests, with the kill switch as the only way through. Closing it needs the body scanned for creating constructs, not for every token. | fixed | f1ed5f44f |
| 6 | low | src/scripts/hooks/block_speaking_inbox_dir.ts:239 | `creatableTokens` fails OPEN when the segmenter returns nothing: `[].every(_segmentIsReadOnly)` is `true`, so `allReadOnly` becomes true and the function returns no candidates at all. Probed with a four-deep `sh -c` nest, which trips `invokedSegments`' `depth > 3` guard (`git_command_classifier.ts:635-636`) and returns `[]` → zero candidates, where the pre-diff scan blocked. This inverts the invariant the same file states two docstrings earlier (:97-99, "a key that cannot decide must not be what silences a blocking guard") and that `main()` implements for an absent tool name (:413-415, an empty name is judged). Contrived trigger, hence low — but "cannot parse" is being read as "read-only" rather than as "judge it". | fixed | f1ed5f44f |
| 7 | low | agents/roadmaps/road-to-design-intent-conformance.md:119-122 | Step 1.1 is `[x]` and its verify line names two figures the branch contradicts: "35 assertions green" — `node_modules/.bin/vitest run tests/hooks/block_speaking_inbox_dir.test.ts` on this branch reports **41 tests passed**; and "with both allowlists emptied exactly the five new allow-cases go red", which no longer matches the case set after the round-1 fixes added `_WRITING_MARKERS`, the heredoc branch and the quote strip (the test docstrings themselves now split sensitivity across three mechanisms, not one pair). The verify line predates its own commit's test additions. | fixed | f1ed5f44f |
| 8 | low | agents/roadmaps/road-to-design-intent-conformance.md:87 | The Context block, declared "verified at `origin/main@4be5f59`", cites `src/scripts/hooks/hook_manifest.yaml:235-240`. That path does not exist — the file is `src/scripts/hook_manifest.yaml`, with no `hooks/` segment. The line range is exactly right (235-240 is the `block-speaking-inbox-dir` entry, `severity: blocking`, no `tools:` key), so the claim is true and its citation is unfollowable. Same class as round-1 finding 9, whose two wrong figures in this same block ARE fixed — re-verified: `condense.ts` is 2712 lines and `AUGMENT_SYMLINK_DIRS` is at :2461. | fixed | f1ed5f44f |
| 9 | low | src/skills/tailwind-engineer/SKILL.md:139 | `1b.` is not an ordered-list marker. In the deliverable list (`1.` … `2.` … `3.`) it renders as a lazy continuation of item 1 rather than as its own item, so the field AC-2 calls "a named output field" has no number a reader can cite, and the surrounding list's numbering silently absorbs it. The `Reconciled:` line in the concrete-shape block (:154) is unaffected, so the obligation still has a destination — this is the index, not the field. | fixed | f1ed5f44f |

## Scope note — where this stops, and why

Two rounds ran, each dispatched by `dispatch_r2_reviewer.ts` so neither prompt
was written by the implementing session. Round 1: ten findings over scope
`beb47058`, nine `fixed` against `e69fd4f00`, one `accepted-risk`. Round 2: nine
findings over scope `4e5a4656`, all nine `fixed` against `f1ed5f44f`. Both
tables are preserved beside this file as `.round1.findings.md` and
`.round2.findings.md`, because `--force` overwrites the filled artifact in place.

**The current head is `f1ed5f44f`, scope `2b7c9481`, and no round has reviewed
it.** That is not an oversight and not a claim of cleanliness — it is where the
loop is stopped on purpose. Fixing a finding changes the reviewed content, so
every round produces a new scope hash and a fresh `stale-review`; a round 3
would review the round-2 repairs and, if it found anything, leave scope 4
unreviewed in exactly the same way. Three attempts on one target is the recorded
budget (`autonomous-execution` § Validation-loop budget), and this is the third:
verb allowlist → per-verb writing markers → the glob axis that replaced both.

What makes stopping here defensible rather than merely convenient: round 2's own
handoff said the durable form stops classifying commands, and the third attempt
did exactly that — it REMOVED the mechanism both rounds attacked instead of
patching it again. Every writing form either round probed is now judged by the
token scan that predates this branch, so the surface the findings were about no
longer exists. The residual, stated plainly: a read naming a not-yet-existing
speaking directory still blocks. That is pre-existing over-broadness, it is
unchanged by this branch, and the two rounds are the evidence that narrowing it
by classifying commands opens a hole somewhere else.
