# Findings: feat-runtime-skill-routing
<!-- completion-review: v1 | reviewed: 2026-08-16 | scope: 9da643ff7fdebb6241196409a7abdd8c466cf2cc881ce30651787b8dcda38afd | diff: 179fc09e9f8734b335aac5dc222ef243da76cef9 | reviewer: r2-fresh-subagent-feat-runtime-skill-routing | prompt_hash: e3e541615abfe0775d22db1b513295fcaaa6807b65cc99083225a1ed41baa541 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 179fc09e9f8734b335aac5dc222ef243da76cef9
  scope_hash: 9da643ff7fdebb6241196409a7abdd8c466cf2cc881ce30651787b8dcda38afd
  roadmap: agents/roadmaps/archive/road-to-inbox-harvest-2026-08-d-runtime-skill-routing.md
  roadmap_hash: b0c140f9189e5b56cde85411c494d5e8cd3405814093a0909b5b2e777ea7e2cc
  ac_hash: 40570cff383bec4918684bc965b86fd94477bd611de4363080845deecfc0fef0
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-16T11:29:09Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/mcp_server/tools.ts:1268 | Promoting `suggest_skill_for_task` into `ALLOWLIST` breaks the pinned allowlist test and the branch ships red: `tests/scripts/mcp_server_tools.test.ts:51` asserts the exact 19-name set and `expect(Object.keys(ALLOWLIST).length).toBe(19)`, both of which now fail (20 keys, `+ "suggest_skill_for_task"`). Reproduced: `npx vitest run tests/scripts/mcp_server_tools.test.ts` → 1 failed. The catalog, inventory and stub-source were all updated; the test that pins the same fact was not. | fixed | fixed in 35735256cbef — allowlist pin 19 -> 20, with the promotion reason inline |
| 2 | high | src/scripts/hooks/skill_route_hook.ts:102 | The floor does not transfer to short prompts, so the concern speaks loudest exactly where it knows least. `score_skill_relevance._score` computes `overlap = \|task∩skill\| / \|task_terms\|`, so the score is inversely proportional to prompt length, while `MIN_TOP_SCORE = 30` is the p90 of 496 *routing-matrix* lines (long, deliberately authored). Live run against `.claude/skills`: prompt `"fix it"` → `command-routing (70), contribution-precheck (70), conventional-commits-writing (70)`; prompt `"developer"` → `developer-like-execution (70), readme-writing-package (70)`. A one-content-word prompt that hits any skill scores 70/100 and injects three alphabetically-first pointers presented as top-decile confidence — the roadmap's own Risk 1, and the measured 13.9 % fire rate cannot hold on real prompt shapes. | fixed | fixed in 35735256cbef — MIN_TASK_TERMS=3 added, checked before the catalogue read |
| 3 | high | src/scripts/_lib/skill_catalogue.ts:836 | The branch records a `scoped` and a `legacy-all` codex observation on the same `observed_at`, but no consumer reads the new `projection_mode` field and the two reducers break the date tie in opposite directions: `knownHostLimits:836` uses `previous.observedAt > record.observed_at` (last-in-file wins → the scoped arm) while `formatPerHostVerdicts:1007` uses `reduce(a.observed_at >= b.observed_at)` (first wins → the legacy-all arm). Reproduced: one `capture_skill_catalogue --limits` run prints `dropped 402` and `codex: dropped 330 entries … at a projection of 426` for the same host and date. `install.ts:3975` (deploy warning) and `install.ts:4026` (legacy-all migration prompt) now quote the scoped arm's 330/226, i.e. a measurement taken under the very mode the prompt is arguing for. | fixed | fixed in 35735256cbef — one `_supersedes` selector for both reducers; --limits now prints 402/497 in both blocks |
| 4 | medium | src/rules/missing-skill-recovery.md:27 | "dropped **402** skills from the model-visible list" restates an entry-level count as a skill count. The record is `entries_total: 497` (skills + commands) with `dropped_count: 402` and `projected_skill_count: 297`, and the branch's own evidence page (`scoped-projection-host-delivery.md`) states the denominators do not match ("330 dropped against 226 projected skills plus 36 plugin skills plus 200 commands"). Same mis-attribution ships consumer-visible in `src/scripts/mcp_server/tools.ts:1218` and `:1275` and in `consumer_tool_catalog.json:547` ("a measured host dropped 402 of them"). | fixed | fixed in 35735256cbef — reworded to `402 entries` plus the denominator caveat, rule + both MCP surfaces |
| 5 | medium | src/scripts/hooks/skill_route_hook.ts:74 | `MIN_TOP_SCORE = 30` coincides exactly with the persona-only score (`overlap 0 × 70 + persona 1.0 × 30`) and the guard is `top[1] < MIN_TOP_SCORE`, so 30 passes. A prompt that merely contains a persona slug clears the floor with zero keyword overlap, and the resulting ties resolve alphabetically by name. Live: prompt `"developer"` emits `estimate-ticket (30)`; prompt `"the developer wants a permission check"` emits `code-review (35), design-review (35), learning-tutor (35)` while the topically correct `authz-review` does not appear. | fixed | fixed in 35735256cbef — MIN_TOP_SCORE 30 -> 31, strictly above a persona-only match |
| 6 | medium | src/scripts/hooks/skill_route_hook.ts:97 | `rank()` re-walks and re-parses the whole catalogue on every submitted prompt — `readdirSync` plus two `statSync` and one `readFileSync` per skill, 337 skills in this tree, ~20 ms warm-cache measured — and it runs *before* `MIN_TOP_SCORE` is applied, so ~86 % of turns pay the full walk to produce silence. There is no cache, no bounded read, and no latency evidence in the change; `src/config/hook-latency-budget.json` caps `any_hook_event` at p95 250 ms CI (400 ms on the 1-vCPU floor). The change registers a byte budget only. | fixed | fixed in 35735256cbef — measured (12.3 ms warm / 0 ms below the term floor vs a 250 ms p95 budget) and recorded in the header; no cache added |
| 7 | medium | src/scripts/hooks/skill_route_hook.ts:1 | The branch adds zero tests (`git diff --name-only <base>..HEAD -- tests/` is empty). `skill_route_hook.ts` is the only `user_prompt_submit` concern in the tree without a `*_hook.test.ts` (delegation_nudge, language_mirror, session_canary, edit_shape, code_graph_nudge, ui_route_nudge … all have one), so `MIN_TOP_SCORE`, `TOP_K`, the silence default, the synthetic-prompt skip and the warn-at-exit-2 shape are unpinned — as are `resolveSkillsRoot`, `buildNoTruncationRecord`, `classifyHostProjection`, `formatProjectionModes`, `loadSkillTriggers`, `runSkillScope`, the `--scope` parser and `_suggestSkillForTaskHandler`. | fixed | fixed in 35735256cbef — tests/hooks/skill_route_hook.test.ts, 10 tests; mutating both floors back fails 3 |
| 8 | medium | docs/CLAIMS.md:203 | The count bump left the claim internally contradictory: the headline now reads "15 of 117 governed rules (12.9%)" while the same claim body still states "116 is the governed-rule count (enforced by the counts updater)" and "88 in the 116-frame". 15/117 is 12.82 %, and `docs/proof.md:290` states the same measurement as "117 rules · 15 blocking (12.8%)" — so proof.md carries 12.9 % on line 66 and 12.8 % on line 290. The entry whose whole purpose is to disambiguate two denominators now mixes three. | fixed | fixed in 35735256cbef — 12.9% -> 12.8%, body 116 -> 117, (88 in the 116-frame) -> (89 in the 117-frame); the historical citation left intact |
| 9 | low | src/scripts/_lib/skill_catalogue.ts:613 | `buildNoTruncationRecord` writes `described_count: 0` while the `truncation_mode: 'none'` doc it ships with defines that mode as "every entry arrived and every description survived". The field states the exact inverse of the fact the record asserts; any consumer reading `described_count` on a `none` record gets 0 where `entries_total` descriptions survived. `bare_count: 0` is correct; `described_count` should be the offered count or be omitted. | fixed | fixed in 35735256cbef — documented as `not counted`, matching the strip-and-drop record beside it |
| 10 | low | src/scripts/trigger_coverage.ts:194 | `loadMatrixPrompts` hand-parses YAML with `/^\s*-\s*prompt:\s*"(.*)"\s*$/` although `parse as parseYaml` is already imported in the same module at line 40. All 496 corpus lines happen to be plain double-quoted today, so the count is currently exact — but an unquoted, single-quoted, block-scalar or backslash-escaped prompt is silently skipped or captured with its escapes intact, moving the published denominator with no signal. A silently-shrinking denominator is the failure this scope exists to measure. | fixed | fixed in 35735256cbef — parsed with the already-imported YAML reader; denominator re-verified at 496 |
| 11 | low | src/rules/missing-skill-recovery.md:18 | `# obligation: line 30` points at a blank line inside the Context section; the obligation fence opens at line 36 and its first law line is 37. The convention it copies is accurate in its stated twin (`external-code-graph-interop.md:20` → line 37, the first law line), so the pointer is wrong on the day it ships. | fixed | fixed in 35735256cbef — obligation marker 30 -> 41 (the Iron Law fence) |
| 12 | low | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-d-runtime-skill-routing.md:118 | Step 2.1 records "measured emission is 323 bytes", but the gate's own harness reports 271 B: `npx tsx src/scripts/bench_hook_injection.ts` → `user_prompt_submit skill-route 271 B (cap 512)`. The stated measurement is not reproducible from the instrument that enforces the budget. | fixed | fixed in 35735256cbef — both readings stated: 323 B on a firing prompt, 0 B from the bench probe |
| 13 | low | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-d-runtime-skill-routing.md:218 | AC #4's justification does not cover what the branch actually changed in the router. "Unchanged, and structurally unreachable from this scope — skills are not compiled into the router" argues only about the skill seed, while step 4.2 of the same branch adds a routed tier-2 rule with six unanchored `phrase` triggers (`dist/router.json` tier_2 77 → 78) — which *does* feed the unintended-activation census. Replaying `match_prompt` over the matrix corpus shows `missing-skill-recovery` contributes 0 unintended activations, so the outcome holds; the premise was asserted, not re-derived, against the house rule the same roadmap cites in 4.2. | fixed | fixed in 35735256cbef — argument narrowed to the seed; the routed rule's contribution replayed and measured at 0 |
