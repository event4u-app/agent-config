# Findings: drain-road-to-drain-commands
<!-- completion-review: v1 | reviewed: 2026-08-21 | scope: 6c4d2979f331049419136a244fe0599af49c524adb7d3e682b8204820f3e16cc | diff: 9ee309dbb4cc83713aa7e586741cd9b39f416261 | reviewer: r2-fresh-subagent-drain-road-to-drain-commands | prompt_hash: 63bd4e98307fced8cf8e72fdc4032eb6d774a36138104f98edb6d4e9ab7307dd -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-21 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 9ee309dbb4cc83713aa7e586741cd9b39f416261
  scope_hash: 6c4d2979f331049419136a244fe0599af49c524adb7d3e682b8204820f3e16cc
  roadmap: agents/roadmaps/road-to-drain-commands.md
  roadmap_hash: 97802fd8d31e236df3331bb9ba24f4c8cc933130a103a98cfba81812f0ac4c97
  ac_hash: 16b55082249ff985ced2406760973d278691b88735455be1a785cd8709e73f9e
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-21T08:27:56Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | blocker | src/flows/surface-map.yaml:112 | The new command is never classified: `lint_command_flow_coverage` exits 1 — `1 violation(s) (201 commands, 200 mapped) / orphan: command 'pr/merge' has no flow/surface in surface-map.yaml`. The file's own header states "Adding a command → add its ref here (the lint fails the build otherwise)". `docs/command-flows.md:10` bumps the headline to 201 commands while its delivery-surface list (lines 120–121) still enumerates only `git-pr-create` / `git-pr-create-description-only`, so count and list disagree. | open | AC-8 |
| 2 | blocker | agents/evidence/reviews/drain-road-to-drain-commands.review-input/diff.patch:1 | The reviewed scope is not the branch. `git diff --name-only origin/main...HEAD` lists 30 files; `diff.patch` contains 29. The omitted file is `src/config/estate-count-budget.json`, whose branch change is a ratchet loosening — `baseline.active_roadmaps` 11→12 and `baseline.open_blockers` 43→44 — i.e. the one class of change a review must see, delivered outside the review. Branch HEAD is `d6ce9fd9`; the scope is pinned to `9ee309db`, and three post-pin commits are unreviewed. | open | contract §5 (stable scope) |
| 3 | major | src/scripts/check_hook_bundle_content.ts:112 | `spawnSync(rewritten, { cwd: REPO_ROOT, shell: true })` inherits the ambient PATH. The sibling that owns the same command string, `rebuild_hook_bundle.ts:130`, deliberately injects `env: { PATH: node_modules/.bin:… }`. `build:hooks` begins with a bare `esbuild`, so on any shell without `node_modules/.bin` on PATH the gate exits 2 ("rebuild failed") and fails the whole pre-push target while the bundle is in fact correct. | open | AC-2 |
| 4 | major | src/scripts/check_hook_bundle_content.ts:147 | Entry-point guard is a string compare where the sibling (`rebuild_hook_bundle.ts:160`) compares `fs.realpathSync` on both sides. Under a symlinked checkout, or a repo path containing a space or non-ASCII char (percent-encoded in `import.meta.url`, not in `path.resolve`), the guard is false, `main()` never runs, the process exits 0 and the gate reports nothing — a silent green on an unverified invariant, the exact failure class this gate was written to remove. | open | AC-2 |
| 5 | major | src/domains/git/pr/merge/command.md:85 | The § 1 manifest snapshot captures no `baseRefName` and no base SHA. § 2 (`git merge origin/<base>`) and § 4 (`git diff origin/<base>...HEAD`) both consume `<base>`, which is therefore undefined by the command's own procedure, and § 8's kill switch "the base advanced by an actor other than this run" is undetectable with no recorded base SHA. | open | AC-3 |
| 6 | major | src/domains/git/pr/merge/command.md:66 | Internal contradiction on the safety property AC-5 names. The banner says the merge step "IS SPECIFIED AND NOT YET ACTIVE… A BARE INVOCATION BEHAVES AS IF IT CARRIED `--no-merge`", but Dispatch row 1 reads "Prepare and merge exactly PR N", row 3 "Drain the open-PR list", and § 9 gives an ungated `gh pr merge <N>`. The Rules line resolves it a third way. A reader following the Dispatch table merges; a reader following the Rules never can. | open | AC-5 |
| 7 | major | src/domains/product-basic/roadmap/process-full/command.md:182 | The Iron-Law amendment names three run-ending conditions under `--all` four lines above the unamended "The five — and only five — halt conditions (exhaustive; nothing else stops the run)", and the forbidden-non-halt block beneath it still lists "the merge base needs updating" / "there are conflicts" as reasons that must never stop a run — precisely two of `/pr:merge` § 8's kill switches. Step 4.8 claimed the block was reconciled. | open | AC-4 / roadmap 4.3, 4.8 |
| 8 | major | src/domains/product-basic/roadmap/process-full/command.md:106 | `--all` step 3 instructs "take the next roadmap against the new default — every merged PR moves it", but `--merge` ships inert and nothing in the run merges, so the default never advances and the described state cannot occur. Combined with the file's own conflict arithmetic, at most the first PR of an `--all` run is mergeable at hand-off, which contradicts the Iron Law's "THE RUN ENDS AT A MERGEABLE PR" and AC-4. | open | AC-4 |
| 9 | minor | src/domains/product-basic/roadmap/process-full/command.md:112 | With `--merge` inert the run performs no `BLOCK_OPS` operation (`push`/`commit` are `WARN_OPS`), so the `LEDGER_MAX_AGE_MS` window cannot close the run. A stop condition that cannot fire is documented as one of only three that can. | open | AC-4 |
| 10 | minor | src/domains/git/pr/merge/command.md:12 | `skills:` declares `ai-council`, but "council" appears nowhere in the body and `routes_to:` omits it. Not inert: it added `ai-council` to `src/domains/git/pack.yaml` dependencies, widening the git pack's install surface for a skill the command never uses. | open | — |
| 11 | minor | src/agent-src/commands/evals/roadmap.json:41 | Roadmap step 6.2 requires the estate-drain prompts to route so "the flag is reachable by intent and not only by flag literal". All three added drain cases carry `"expected": "roadmap"` (the cluster head), so the fixture proves only that the prompt reaches the dispatcher and tests nothing about `--all` reachability. | open | roadmap 6.2 |
| 12 | minor | docs/decisions/ADR-238-drain-command-surface-and-merge-authority.md:28 | Dead citation `ADR-044-pack-prefixed-command-slugs.md`. The file is `ADR-044-command-naming-scheme-hyphenated.md`. `check_references` passes because `docs/decisions/` is in its excluded set, so nothing catches this. | open | AC-7 |
| 13 | minor | src/domains/git/pr/merge/command.md:58 | Mixed link depth inside one file. From `src/domains/git/pr/merge/`, `../../../../` is `src/`, so `../../../../rules/git-history-discipline.md` is correct while `../../../../docs/contracts/command-clusters.md` resolves to `src/docs/contracts/…`, which does not exist. Same broken form in `process-full/command.md`. | open | — |
| 14 | minor | src/domains/git/pr/merge/command.md:85 | `gh pr list --state open --limit 100` silently truncates the manifest at 100 PRs, while § 6's termination proof states the bound as "INITIAL MANIFEST + ONE BATCH" without noting the initial manifest is itself capped — a >100-PR queue drains partially and reports as complete. | open | AC-3 |
| 15 | minor | src/domains/git/pr/merge/command.md:225 | `--<detected-method>` is a placeholder with no detection procedure; the following sentence forbids inventing a method mid-queue without saying how the repository's method is determined. | open | AC-3 |
| 16 | minor | src/scripts/hooks/block_unauthorized_git.ts:492 | The restored constant's own header now contains the literal widened expression, so the obvious grep-based regression check for it — the shape of step 1.1's own verify — matches the comment in the guard source. | open | AC-1 |
| 17 | minor | taskfiles/ci-fast.yml:144 | The content gate runs a full esbuild bundle unconditionally whenever a local bundle exists, so every pre-push pays one bundle build in addition to the healer's conditional one. The surrounding comments justify each neighbouring gate by measured sub-second cost; no cost is stated for this one. | open | — |
| 18 | nit | src/scripts/hooks/block_unauthorized_git.ts:492 | Duplicated word across the line break: "with a / a temporary, revert after marker". | open | — |
| 19 | nit | src/agent-src/commands/evals/roadmap.json:3 | The edit re-encoded the em dash in `intent` as an escape while every other string in the file uses literal UTF-8. Needless encoding divergence in a hand-maintained fixture. | open | — |

