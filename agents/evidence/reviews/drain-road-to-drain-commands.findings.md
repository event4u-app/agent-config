# Findings: drain-road-to-drain-commands
<!-- completion-review: v1 | reviewed: 2026-08-21 | scope: 9a444f8e59089e22e78f86cd032fbbf20a2f7bb75778beb76ab7e5704c1e2799 | diff: 73d720501cd89606812727281de0f680410f89e5 | reviewer: r2-fresh-subagent-drain-road-to-drain-commands | prompt_hash: edd2174cf8556af8f8f69aeaf293d937a84f3938126e5ef01938f7ec8d7413f5 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-21 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 73d720501cd89606812727281de0f680410f89e5
  scope_hash: 9a444f8e59089e22e78f86cd032fbbf20a2f7bb75778beb76ab7e5704c1e2799
  roadmap: agents/roadmaps/road-to-drain-commands.md
  roadmap_hash: 983c6551f07be9fa1371a10fefbe264ea2ac92938543a0cddecbd64f6a33ba5a
  ac_hash: 188021fb998afe8f9cc88bcdcd23e8762e75b922de5a4b76cab8729d3de506a2
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-21T08:52:01Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | blocker | src/domains/product-basic/roadmap/process-full/command.md:204 | The new sentence reclassifies all five halt conditions as "intra-roadmap: under `--all` they stop the roadmap being worked, and the estate loop records it and takes the next one". Halt 1 is Hard-Floor and halt 3 is security-sensitive. `non-destructive-by-default` requires a Hard-Floor trigger to stop and obtain this-turn user confirmation and states no roadmap authorization lifts it. As written, an `--all` run hitting a production-branch / deploy / bulk-delete trigger on roadmap n records it and continues to n+1 — the roadmap-as-authorization failure that rule names. The carve-out (Hard-Floor and security-sensitive end the LOOP) is absent. | open | AC-5 / non-destructive-by-default |
| 2 | major | src/domains/git/pr/merge/command.md:103 | The immutable-manifest check is invalidated by the command's own steps. § 1 says refuse when the head SHA moved; § 2 merges the base in and § 5 pushes, so every prepared PR has a head SHA different from the snapshot. Nothing says the run's own pushes update the record, or that only third-party movement counts. § 5's "the exact head SHA that will be merged" is by construction not the manifest SHA. The kill switch would fire on every PR, or be ignored. | open | AC-3 |
| 3 | major | src/domains/product-basic/roadmap/process-full/command.md:196 | Three incompatible statements of what ends an `--all` run in one file: the Iron Law names three conditions, the halt prose names two and drops the kill switch, and the `--all` section names two and excludes window expiry while `--merge` is gated. | open | AC-4 |
| 4 | major | src/domains/git/pr/merge/command.md:182 | § 6 and § 7 are written as operative while the banner makes merging inert. With nothing merging the base never advances, no PR leaves the open list, and the whole queue is pre-greened — the wasted work the same section forbids. § 7's expiry state is likewise unreachable; process-full carries that qualification and `/pr:merge` does not. | open | AC-4 / AC-5 |
| 5 | major | src/domains/git/pr/merge/command.md:145 | § 4 authorises closing a third party's PR inside an autonomous loop, but the probe it shows does not implement the carve-out stated one sentence earlier. Followed literally it closes any PR whose remaining diff is only regenerated churn — which per § 3 is every roadmap PR in this repo after a base sync. `pr-close` is in neither BLOCK_OPS nor WARN_OPS, so no guard sees it and § 8 lists no kill switch for it. | open | AC-3 |
| 6 | major | src/domains/git/pr/merge/command.md:167 | "Six fix iterations per PR per pass" contradicts process-full's halt condition 5 (N=3) and the always-loaded `autonomous-execution` cap on the same surface. process-full's delivery loop delegates here, so one run is governed by both 3 and 6 for the same failing CI job. Neither file names the interaction nor scopes "iteration" to a different unit than "validation target". | open | AC-3 |
| 7 | major | src/domains/git/pr/merge/command.md:199 | Neither the window-aware-scheduling override nor the expiry state names a readable signal for "the remaining window", while § 7 forbids anything but read-only verification without saying what is read. Roadmap step 3.7's verify requires "the signal it reads to know the window is under pressure" and is marked done. | open | AC-3 / roadmap 3.7 |
| 8 | minor | src/domains/git/pr/merge/command.md:255 | The summary schema has no base-SHA field, while roadmap step 4.5's verify requires the "mergeable against base <SHA>" wording in the command AND in the summary schema. Step 4.5 is marked done with the schema half unmet. | open | roadmap 4.5 |
| 9 | minor | src/domains/git/pr/merge/command.md:182 | "Process the manifest in order" leaves `all`'s traversal undefined: the priority rule is scoped to the single-PR auto-select row, and `gh pr list` carries no sort. Two runs over the same queue may order it differently, and § 6's straggler bound is stated against an undefined traversal. | open | AC-3 |
| 10 | minor | src/domains/product-basic/roadmap/process-full/command.md:99 | The `--all` queue order is not a total order: the >=10% bucket is sorted by descending progress with no tiebreak, while the tiebreak chain is given only for the <10% bucket. Two roadmaps at equal progress order non-deterministically. | open | roadmap 4.2 |
| 11 | minor | src/domains/git/pr/merge/command.md:128 | Only two of the four conflict classes carry a file glob; the other two are prose. Since § 3 halts on "a conflict outside these four classes", membership must be decidable from the path — step 3.3's verify demands a glob per class and is marked done. | open | AC-3 / roadmap 3.3 |
| 12 | minor | src/domains/git/pr/merge/command.md:239 | § 9 never maps the probe's JSON fields onto the merge flags, so `<detected-method>` is not derivable from the text, and the multi-allowed branch has no rule for a mixed history — a case that ends in the invented flag the next sentence forbids. | open | AC-3 |
| 13 | minor | dist/agent-src/commands/pr/merge.md:58 | The dist projection is byte-identical to the source, so src-relative links do not resolve in the shipped tree. The structural sibling at identical depth uses a 3-level prefix for the same target, so the two files now disagree on the convention. | open | — |
| 14 | minor | src/domains/git/pr/merge/command.md:73 | The Dispatch table's `--no-merge` row ends mid-sentence and inverts the relationship: the object of "delegates to" is missing, and process-full is the caller, not the entry point. | open | — |
| 15 | minor | src/domains/product-basic/roadmap/process-full/command.md:132 | An unconditional behaviour change is filed under the `--merge` heading: delivery for a bare invocation now includes the full prepare loop and CI wait, but a reader not using the flag has no reason to read that section and the pre-existing Delivery text is not updated. | open | AC-4 |
| 16 | nit | src/domains/product-basic/roadmap/process-full/command.md:182 | The `--worktree` section restates part of the delegated seeding list it is supposed to defer to, creating a second copy that can drift. Step 4.6's verify requires delegation rather than restatement and is marked done. | open | roadmap 4.6 |
