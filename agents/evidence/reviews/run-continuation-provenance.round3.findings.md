# Findings: run-continuation-provenance — round 3
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: e77b7a46ebec6e9255b28c38e0b9f7661ec43ef0e043790e98c379bb6976d797 | diff: 7db889696374c2c9f8d001d5b905d262f1e6cecf | reviewer: r2-fresh-subagent-run-continuation-provenance | prompt_hash: 34768b72b3c5761dac9f5b8a70a6e936d194dac8c3097e4ba41d67b68d45dc79 -->
<!-- evidence-type: v1 | type: original-review | declared: 2026-08-19 -->
<!-- Round 3 of this branch, archived. Bound to scope
     e77b7a46ebec6e9255b28c38e0b9f7661ec43ef0e043790e98c379bb6976d797, which the
     fixes below moved past; the live `findings.md` carries the current round. -->

<!-- context-manifest: v1
inputs:
  diff_sha: 7db889696374c2c9f8d001d5b905d262f1e6cecf
  scope_hash: e77b7a46ebec6e9255b28c38e0b9f7661ec43ef0e043790e98c379bb6976d797
  roadmap: agents/roadmaps/road-to-run-continuation-observation.md
  roadmap_hash: e2fa5c102fa0fb735e3123b5ea28627a29c81a036595cd31fe0fcf50cb556742
  ac_hash: bb34537a4ce90a2ac144c0346d9d3817fc8ddd788722900f17cdb6b7ed59bea7
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T17:25:21Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/run_continuation_hook.ts:601 | The progress signal is still read from the READER's tree (`path.join(workspaceRoot, 'agents','roadmaps', slug)`) while this diff's own docblock now declares `session_root !== workspace_root` the NORMAL healthy arrangement — so in the live layout (reader = parent checkout, session = worktree) the agent flips checkboxes in the worktree copy and `scanOpenSteps` keeps counting the parent's unchanged copy: the concern re-engages naming an already-completed step, `scan.open` never moves, and `ladder` emits `halt-stall` after 3 engagements on a run that was making real progress. | fixed | 5fa347a5a |
| 2 | high | src/scripts/hooks/run_continuation_hook.ts:461 | The `session_cwd` mitigation for the degraded resolution is false in the layout the roadmap itself names as live (`<parent>/.claude/worktrees/long-horizon`, i.e. a worktree NESTED under the reader root): for `cd <parent>/.claude/worktrees/long-horizon/src && claude`, `session_checkout` degrades so `session_root == workspace_root == <parent>`, `git_dir == git_common_dir`, and `session_cwd` IS under `session_root` — so all three documented discriminators read "healthy same-tree run" for a genuine two-tree run, exactly the round-1 false negative the field was added to expose; unreachable by the suite because `writeWorktreePair` places the worktree outside `main` on purpose (tests/hooks/run_continuation_dispatch.test.ts:161). | fixed | 5fa347a5a |
| 3 | medium | src/scripts/hooks/run_continuation_hook.ts:400 | `provenance()`'s docblock heading says "The five fields" while the function emits six (`workspace_root`, `session_root`, `session_cwd`, `git_dir`, `git_common_dir`, `claim_path`) and both the file header (line 55) and the call-site comment (line 644) say six — the identical doc-vs-code count drift the header credits round 2 finding 4 with catching, re-introduced by the edit that added `session_cwd`, so a later edit can again drop a field without contradicting the function's own docblock. | fixed | 2c73756b0 |
| 4 | medium | agents/roadmaps/road-to-run-continuation-observation.md:100 | Step 0.0 sub-bullet 2 states the change "adds `workspace_root` / `session_root` / `git_dir` / `git_common_dir` / `claim_path` to every event" — five fields, omitting `session_cwd` — while the same bullet 40 lines down says the fix takes "the line to six fields"; a reader auditing a ledger line against the roadmap's enumeration concludes an extra undocumented field is present, or that `session_cwd` is not part of the contract. | fixed | 2c73756b0 |
| 5 | low | src/scripts/hooks/run_continuation_hook.ts:654 | The position comment claims the provenance call "sits below every early return and above every `appendEvent`, which is the only position where the cost is paid exactly when a line is written" — false in the current state: the duplicate-stop-fire branch (line 702) returns `EXIT_BLOCK` with no `appendEvent`, and the failed-state-write branch (line ~752) returns `EXIT_ALLOW` with no `appendEvent`, so the resolution is still paid on two reachable no-line paths (duplicate fires being routine for this concern) — the same residual round 2 finding 6 raised, now asserted away rather than fixed. | fixed | 2c73756b0 |
| 6 | low | src/scripts/hooks/run_continuation_hook.ts:538 | `provenance` normalises the raw `payload.cwd` without the `.trim()` that `session_checkout` applies to the same string, so a host sending a whitespace-padded value (`" /Users/x/wt "`) yields `session_root = /Users/x/wt` (validated) while `session_cwd = path.resolve(" /Users/x/wt ")` resolves the non-absolute string against the reader's cwd — a fabricated path that is neither `session_root` nor under it, i.e. exactly the shape the docblock says "no healthy resolution produces", making a healthy line read as degraded; a whitespace-only `cwd` similarly emits `<workspaceRoot>/   ` instead of the `''` the docblock reserves for host silence. | fixed | 2c73756b0 |
| 7 | low | tests/hooks/run_continuation_dispatch.test.ts:130 | The hardened `git()` helper strips the four `GIT_*` env pointers but still inherits global git config, so the ambient-bleed class R2 finding 4 fixed on the env axis remains on the config axis: a maintainer with global `core.hooksPath` set has the real hook set executed against the fixture repo on `git commit`, and a global `commit.gpgsign = true` without an available key makes the fixture commit fail and throw, reding the three worktree-pair cases for a reason unrelated to the concern. | fixed | 2c73756b0 |
| 8 | low | agents/roadmaps/road-to-run-continuation-observation.md:111 | The verification claim "which reds against the un-enriched event while the other eight cases stay green" is stale after this diff: the file now holds 12 `it(` cases (9 before), and three of them — not one — assert the provenance, so the falsifiable count a reader would check the artefact against is wrong in a document whose whole purpose is auditable evidence. | fixed | 2c73756b0 |
