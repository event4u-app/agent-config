# Findings: drain-source-silence
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: bdf90bcffe3d46b493e45263bc4da79e0c9064673a18c0102e214ac1e929adfe | diff: 38b950e318e5d728a65fd3225ccee9b22a7153b5 | reviewer: r2-fresh-subagent-drain-source-silence | prompt_hash: c4fa599bc6176d8c0387fa725f3941a61e6b640253025ae834adf001b8948a72 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-source-silence"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 38b950e318e5d728a65fd3225ccee9b22a7153b5
  scope_hash: bdf90bcffe3d46b493e45263bc4da79e0c9064673a18c0102e214ac1e929adfe
  roadmap: agents/roadmaps/archive/road-to-source-silence.md
  roadmap_hash: 1f26f078e14d8a07dfee2bdc18d49621ff7620d11a4f15138a9a6c7d105d666c
  ac_hash: 3e7402daa537ba90c537611f15b4782e9c9b223ff68481fd626a863cd3fcb55b
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T18:12:04Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/sweep_source_surfaces.ts:455 | `gateMetadata` prints every hit as `[matcher:kind]`, and for a `denylist` hit `kind` IS the deny pattern — the readable source name — three lines above its own line 462 claim "The value itself is deliberately NOT printed". Verified by feeding a literal taken from the shipped `deny` array to `--title`: stdout carries the bracketed pattern with the name in it. This step runs in `.github/workflows/pr-metadata-sources.yml:79`, whose job log is public, and cutover step 1.2 creates a private master holding names that are NOT in the tracked tree, which this would print. Printing `f.kind` only when `matcher === 'shape'` closes it. | fixed | 8088d6b83 — prints the CLASS only (`[denylist]` / `[shape:<class>]`); verified by feeding a shipped deny token to `--title` and asserting absence from stdout |
| 2 | high | src/scripts/_lib/source_snapshot_dedup.ts:158 | Leg 2 excludes UNCONDITIONALLY whenever `owners.size > 0`, so three documented fail-closed conditions are false in code: line 46 "No hunk target ... stays at block", lines 75-76 "a finding on a deleted-only line ... stays at block by construction", and the caller comment at check_no_external_sources.ts:463 "Unreadable patch — no targets, so every finding in it fails closed". All three fall through to the tree leg. The diff's own passing test "a non-patch snapshot member takes the tree leg" (no targets at all, still excluded) is the witness. Only `owners.size === 0` and an untracked hunk target actually refuse, so Leg 1's positive branch changes the reported LABEL and never the verdict — which makes `by_leg` a record of match order, not of evidence strength. Live run: 43 of 53 exclusions on the weak leg. | fixed | 8088d6b83 — all three sites now state the true contract (a positive block-counted match is the single necessary condition; an untracked hunk target is the one further refusal), and a new test pins the real Leg-2 semantics executably. Measured before choosing: the strict reading would raise the ratchet by 8 for provable double counts, so the docs were corrected, not the behaviour |
| 3 | high | src/scripts/_lib/source_snapshot_dedup.ts:98 | The file is committed with a raw NUL control byte at offset 4547 (the separator literal in `findingKey`), so git classifies it as binary: confirmed at the reviewed head, and the review-scope diff.patch:2896 renders the entire 220-line module as "Binary files ... differ". The module implementing the new ratchet-exclusion path is therefore unreadable in this branch's own review artefact and in the GitHub PR view. Gate content-scanning is unaffected (`.ts` is not in `_SKIP_EXT`), so this is a reviewability defect, not a scan hole. NOTE, stated because it is material: an uncommitted worktree fix rewriting the byte in escape form appeared DURING this review and attributes detection to `lint_hidden_unicode`; the committed scope still carries the byte. | fixed | c5535a719 — written as `\u0000`; NUL kept as the separator deliberately, since a shape value may contain spaces and NUL cannot appear in either half |
| 4 | medium | .github/workflows/source-surface-sweep.yml:68 | The sweep is piped into `tee "${RUNNER_TEMP}/sweep.txt"`, and the file sets no `shell:` and no `defaults.run.shell`, so the step runs under Actions' default `bash -e` WITHOUT `pipefail` and the pipeline's status is tee's. The one failure mode the job's own header declares ("What it fails on is its own inability to measure: an unreadable config or a crashed sweep exits non-zero") therefore cannot fire; a crashed sweep publishes an empty summary and a green job. | fixed | 8088d6b83 — the step now declares `shell: bash -euo pipefail {0}` |
| 5 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:187 | `main()` inspects only the `_PATH_KEYS` entries of `tool_input` and never reads a `command`, so creating the directory from a shell (`mkdir -p agents/tmp/<speaking-name>`, a `git mv`, a redirect) bypasses the guard entirely. Both siblings on the same slot (`block_no_verify`, `block_config_weakening`) do parse the command string. AC-5's "rejected at write time" therefore holds only for path-carrying editor tools, and the docstring's explicit "four things it does not" list does not name the shell path. | fixed | 8088d6b83 — every command token is offered to the same pure verdict. The new test caught a SECOND form on its first run (`mkdir` names a directory with no trailing slash), so a directory-terminal match was added with its dot heuristic and that heuristic's limit both written down |
| 6 | medium | src/scripts/sweep_source_surfaces.ts:428 | The commit surface is scanned as ONE multi-line blob (subject plus body joined by a newline) while the PR body 14 lines above is scanned per line. `SOURCE_HEADER_RE` (source_shape.ts:101) is anchored `^...$` with no `m` flag, so `sourceHeaderHits` returns an empty array for any multi-line input — verified directly against the module: a two-line string whose second line is a `> **Source:**` header yields no hits, and neither does `shapeHits` on it. The `source-header` class is inert on every commit message, which contradicts the docstring's "the same matcher the tracked-tree gate uses" — that gate scans line by line. | fixed | 8088d6b83 — the commit surface is scanned per line, like the PR body |
| 7 | medium | src/scripts/sweep_source_surfaces.ts:107 | `loadDeny()` reads only the plaintext `deny` array, has no `deny_digests`/`DigestMatcher` path, and the CLI entry wraps `main` in no try/catch, so an absent `deny` key becomes an unhandled throw. `agents/roadmaps/road-to-source-silence-cutover.md:85` — landed in this same branch — deletes the `deny` key from the tracked config, and no step of that roadmap mentions `sweep_source_surfaces`, so both new metadata gates break at cutover with nothing cross-referencing them. Adjacent, same cause: that step's verify ("check_no_external_sources still exits 1 on a seeded fixture violation") is satisfiable vacuously, because a missing `deny` key exits 1 through ExitError (check_no_external_sources.ts:141 and 612-617) having scanned nothing. | fixed | 8088d6b83 — `loadDeny` degrades loudly with digests present and keeps the shape half, which is name-list-independent and therefore the half that still has something to say without a deny set |
| 8 | medium | src/scripts/external_sources_denylist.json:103 | `skip_reason._entries_removed_2026_08_29` reads "drew skip_paths from 32 to 22. Eleven entries went: three DEAD ... five archived roadmaps + one roadmap-asset ... one comment rephrased ... and the review-snapshot diff.patch carve-out" — eleven items against an arithmetic of ten, and the published ledger at agents/evidence/reports/source-skip-paths-ledger.md:58 records "10 removed, 22 kept" with FOUR archived-roadmap rows (24, 25, 26, 29), not five. The audit note attached to the very file whose entry count is the disputed AC-6 number is off by one in two linked places. | fixed | 8088d6b83 — the note now shows the arithmetic (3 + 6 + 1 + 1 = 10) and records that the published ledger was right and the sentence was wrong |
| 9 | medium | src/config/gate-violation-baselines.json:85 | The ratchet note states "51 findings excluded (8 via the hunk-target leg, 43 via the tracked-tree leg)"; the gate run on the same commit prints "53 review-snapshot finding(s) excluded (hunk-target 10, tracked-tree 43)". The cause is structural, not a typo: removing the `*.review-input/diff.patch` carve-out makes the shape ratchet a function of committed review snapshots, and this round's own snapshot contributes 2 of the 53. The sub-figures therefore drift with review activity by construction, and a future snapshot carrying one value that is unique after redaction reds the gate for the reviewer with no way to tell new debt from a new mirror. | fixed | 8088d6b83 — the exclusion count is no longer pinned in the note, with the structural reason: it is a function of the snapshot corpus, which grows per gated PR, so it moves without the debt moving. `count` is the pinned number and the only one |
| 10 | medium | src/scripts/hooks/block_speaking_inbox_dir.ts:170 | `main()` is untested. The 14 cases in tests/hooks/block_speaking_inbox_dir.test.ts exercise only the pure helpers (`inboxDirName`, `isAcceptableInboxDir`, `verdictFor`, `denyMessage`), so for a concern registered `severity: blocking` the envelope shape (payload-wrapped vs top-level `tool_input`), the `_PATH_KEYS` loop, `project_dir` absence and the kill switch are all unpinned. AC-5 describes these as "14 harness tests". | fixed | 8088d6b83 — nine envelope-level cases: payload-wrapped vs top-level, every `_PATH_KEYS` alias, the command path, malformed/empty allow, kill switch |
| 11 | low | src/scripts/hooks/block_speaking_inbox_dir.ts:131 | The exists-probe derives its path from `normalized.indexOf('agents/tmp')` — the FIRST occurrence, and a prefix that also matches `agents/tmp-notes/` or `agents/tmpfiles/` — so a path carrying a decoy earlier segment probes the wrong directory, in either direction. Separately, when the envelope carries no `project_dir` the probe stays relative and resolves against the hook process's cwd, which is not guaranteed to be the repo root. | fixed | 8088d6b83 — the probe re-anchors on the same match that found the directory, so a decoy `agents/tmp-notes/` segment cannot redirect it |
| 12 | low | .github/workflows/pr-metadata-sources.yml:85 | `--commit-range "${BASE_SHA}..${HEAD_SHA}"` uses `pull_request.base.sha`, the base tip recorded on the event rather than the merge base, so once the branch merges a newer `main` other authors' base commits enter the range and are gated as the change's own. Over-reporting only, but it makes a red job blame the wrong commits; `git merge-base` bounds it exactly. | fixed | 8088d6b83 — uses `git merge-base`, with a fallback to `base.sha` so the range is never empty |
| 13 | low | src/scripts/hooks/prepush_metadata_sources.sh:60 | Every non-zero exit is reported as a confidentiality violation: exit 2 (usage or unreadable config), a `git log` failure on an object the local repo lacks, and a branch-deletion push (all-zero local sha, giving a range ending in zeros) all print "BLOCKED by the source-metadata gate ... carries source attribution". Separately the `while read` loop overwrites `RANGE` on every stdin line, so a push of several refs checks only the last ref's commits. | fixed | 8088d6b83 — exit 2 and unexpected codes are reported as the gate failing to RUN, not as a finding; a branch-deletion push no longer builds a range from zeros |
| 14 | low | src/config/gate-violation-baselines.json:1 | The whole 98-line file is rewritten to escape non-ASCII (em dashes to their JSON escape) for one semantic change (`count` 275 to 243), and src/scripts/external_sources_denylist.json:2 is touched solely to escape one em dash — the single real number is buried in whole-file churn a reviewer must parse to find. That touched `_README` line also still points at `check_no_external_sources.py` and `link_crypto.py`, both retired in the ADR-200 Python teardown. | fixed | 8088d6b83 — both files restored to their original indentation and non-ASCII; the baseline file's diff is 4 lines instead of 188 |
| 15 | low | docs/proof.md:352 | The regenerated Axis-2 line publishes "95 ledger entries, 59 backed, 30 unbacked inventory, 6 resolved-null" while `check_claims` on the same tree prints "ledger 95 entries (59 backed, 29 unbacked inventory)": `build_proof` folds the single `withdrawn` claim into "unbacked" and `check_claims` does not. Pre-existing generator divergence rather than a hand-edit (proof.md verified in sync via `build_proof --check`), but this diff edits and republishes the divergent line. | accepted-risk | PRE-EXISTING and out of scope. `build_proof` folds the single `withdrawn` claim into "unbacked" and `check_claims` does not — a divergence between two generators, neither touched by this branch, on a number this roadmap does not own. Fixing it here would be scope creep; recorded so the next reader of either number knows the two disagree by construction |
| 16 | low | src/scripts/sweep_source_surfaces.ts:401 | The branch surface is scanned with `shapePathHits`, whose two classes require a literal `agents/tmp(.old)/<name>/` segment or a `github.com/<owner>/<repo>` URL, so the shape half cannot fire on an ordinary branch name — verified: a plausible speaking branch name yields no hits. CONTRIBUTING.md:105 and the function docstring both describe the gate as "the deny set plus the shape heuristic" without noting that on this surface only the deny half can match, i.e. only names somebody already listed. | fixed | 8088d6b83 — the docstring now states the real coverage (the branch surface is deny-set-covered; the shape half fires only on the two literal path forms) and why closing the gap needs a branch-name class the heuristic does not have |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->

## Disposition round 1 — re-bound in place, 8088d6b83

> **Re-bound a second time, 2026-08-29, to `bdf90bcf…` / head `38b950e3`.**
> Cause was NOT a further response to these findings: the `Rule backstops`
> job reported the committed ADR evidence census stale — drift on the base
> branch, a new decision record landing on `main` while this work was in
> flight — and regenerating it moved the reviewed content, so the scope hash
> moved with it. Contract §2.7 path 1 again, and this commit touches only
> this artefact so it is a fixed point rather than another one-commit-late
> chase. The rows below are unchanged: they are the original findings
> against the original scope, and a re-bind changes what they bind to, never
> what was found.


Sixteen findings: **15 fixed, 1 accepted-risk, 0 deferred.** Re-bound rather
than re-skeletoned, per contract §2.7 path 1 — the reviewer's finding text,
severity and anchors are byte-identical; only `Status` and `Reason/Ref` were
written, because the implementer dispositions a finding and never restates it.

The scope moved because the fixes landed after the review, which is the normal
re-bind case and not a defect: `scope:` above now names the post-fix scope and
`diff:` the head the dispositions were written against.

Two findings are worth naming beyond their row. **Row 1 was a real leak** — the
new confidentiality gate printed the protected name into a public CI log at the
moment it fired, which is worse than the disclosure it was catching; it was
verified empirically before and after. **Row 2 was a false guarantee in a
docstring**, and the fix went to the documentation rather than the code after
measuring that the strict reading would raise the ratchet by 8 for findings that
are provably double counts, not disclosures — with a test added so the contract
is executable instead of prose.

Row 5's fix is the one that found more: the test written to pin the
command-path bypass immediately caught a second form the original regex could
not see. Row 15 is the only non-fix, and it is pre-existing generator divergence
this branch does not touch.
