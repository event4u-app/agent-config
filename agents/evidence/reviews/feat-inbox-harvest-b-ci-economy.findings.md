# Findings: feat-inbox-harvest-b-ci-economy
<!-- completion-review: v1 | reviewed: 2026-08-11 | scope: 62a5614c35b6e08d12a06fc43c9395e48c2662f45c6b4bba08736bc986e39dab | diff: 59b45613fdd7eb3ffef275b5062f36c366428015 | reviewer: r2-fresh-subagent-feat-inbox-harvest-b-ci-economy | prompt_hash: 45cfe03e28f46712f1d040c902d9428de6166b1804baad90c27faaf87bf89664 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 59b45613fdd7eb3ffef275b5062f36c366428015
  scope_hash: 62a5614c35b6e08d12a06fc43c9395e48c2662f45c6b4bba08736bc986e39dab
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md
  roadmap_hash: 9602345b53d325352839e99391b9793d57711520a96ab9e260453ed9587e801d
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-11T07:09:53Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md:180 | Step 1.4 still prescribes the design its own repair rejects. Lines `:180-182` read "Scope `cancel-in-progress` to `pull_request` refs only — a cancelled push-to-main backstop run loses the only signal for that commit", four lines after `:164-169` states that conditioning `cancel-in-progress` alone "was wrong" and introduced the loss. With `acceptance-criteria.md` empty by design the step text is the specification of record, so the record prescribes the rejected mechanism and the shipped one at once. Two attached defects in the same block: the inventory at `:176-178` is stated in the present tense — "23 of 28 carry it; `rule-backstops.yml`, `self-review-gate.yml`, `adoption-snapshot.yml`, `cross-model-canary.yml`, `release-adjacent-health.yml` do not" — and this diff made it false (verified: 28 workflow files, 28 `^concurrency:` blocks); and the `<!-- verify: -->` comment inserted at `:176` still splits the original sentence, leaving "23 of 28 carry / it;" dangling across the comment. This is round-2 finding 3 surviving its repair: the new prose was prepended and the pre-existing body below it was never reconciled — the same not-reconciled-with-its-own-repair shape that finding named. | fixed | fixed 57867dfd5 — step 1.4 rewritten end to end: the rejected `cancel-in-progress`-only prescription is gone, the inventory is stated as found-state (23 of 28) with all 28 carrying one now, and the split `<!-- verify: -->` sentence is repaired. |
| 2 | medium | docs/contracts/ci-cost-budget.md:173 | The ceiling section judges a run-level figure against a per-job ceiling, in the file that ships the rule forbidding exactly that. Under the heading "Per-job cost ceiling — 5-min average" (`:140`), `:173-174` reads "`smoke-public-install.yml` was the previous entry here at 413 s and is now 291 s, under the ceiling." 291 s is matrix-level: it appears in no table row, and the table (`:46`) gives that workflow as six jobs at 24–30 s / 39–47 s / 159–169 s. The same file contradicts the line twice — `:71-74` ("its slowest job is the Windows leg at 159–169 s; the retired 413 s figure was matrix-level, not per-job, so the two are not directly comparable") and the Method note at `:26-33` ("A run-level figure … is **not** interchangeable with a job figure and is not used here … Comparing one against the other manufactures regressions"). Round-2 finding 9's repair pointed `release-pr-gating.md:19-22` at this file for "current per-job numbers", so a reader following that redirect lands on the one surviving run-level claim. | fixed | fixed 57867dfd5 — the roadmap prose now says `tsconfig.scripts.json` and only there, and states why `tsconfig.test.json` was dropped (nothing passes it to tsc; eslint uses it as a `project`, which writes no buildinfo). |
| 3 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md:220 | Step 1.7 records "20 bare call sites across the 10 named workflows, now zero." The 20 conversions are real (counted in the diff), but the tree still carries bare `npm ci` in two of those ten workflows: `site.yml:49` (inside `run: \|` → `cd site` / `npm ci`) and `consumer-matrix.yml:75` and `:166` (both root `npm ci` inside `run: \|` blocks, in jobs whose `setup-node` does carry `cache: 'npm'`, i.e. exactly the sites the flags were meant to pay off on). An eleventh workflow the sweep never counted, `deploy-site.yml:47`, carries one too. The cause is visible in the step's own verify comment at `:232` — `grep -nE '^\s*-?\s*run:\s*npm ci\s*$'` matches only the single-line form by construction, so every `run: \|` block was invisible to the check that was supposed to confirm the claim. | fixed | fixed 57867dfd5 — the ceiling paragraph now labels 413 s as matrix-level, gives the per-job Windows figure, and states that the ceiling is per job. |
| 4 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md:210 | Step 1.6 describes a two-config layout the tree does not have. `:202` asserts "`incremental` is on the two intrinsically-`noEmit` configs ONLY"; `:210-212` says it was "moved to `tsconfig.scripts.json` / `tsconfig.test.json`. Buildinfo goes to a gitignored `.tscache/`, one file per config so the two do not clobber each other." Verified: `tsconfig.test.json` (13 lines) carries neither `incremental` nor `tsBuildInfoFile`, only `tsconfig.scripts.json` does, and `.tscache/` holds one file (`scripts.tsbuildinfo`). This is the exact sentence round-2 finding 6 quoted as falsified by the tree; its repair deleted the dead config and left the claim standing, so the step still documents a second buildinfo that is never written. Same class in the code comment the repair added at `tsconfig.scripts.json:5` — "`incremental` lives ONLY on the configs that are intrinsically noEmit" (plural) — while `tsconfig.test.json` is intrinsically `noEmit` and does not carry it. | fixed | fixed 57867dfd5 — four bare `npm ci` inside `run: |` blocks flagged (`site.yml`, `deploy-site.yml`, `consumer-matrix.yml` x2). Real total 24 across 11 workflows, verified zero remaining in any form; the verify command now matches both syntactic forms. |
| 5 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md:225 | Step 1.7's stated CI benefit does not hold for a fifth of the sites it changed. "Unlike 1.6 this one does pay in CI: `setup-node` restores the npm cache before these run, so `--prefer-offline` skips revalidation on a warm cache." Four of the 20 converted call sites sit behind a `setup-node` with no `cache:` key at all — `self-review-gate.yml:43-46` and `:62-65`, `cross-model-canary.yml:42-45` and `:58-61` — so there is no npm cache to be warm and `--prefer-offline` can skip nothing there (`--no-audit --no-fund` still apply). Step 1.6 was rewritten in this same diff specifically because its stated benefit was asserted rather than checked; 1.7's correction of 1.6 repeats the omission one paragraph later. | fixed | fixed 57867dfd5 — the CI-benefit claim is qualified: `--prefer-offline` pays where `setup-node` declares `cache: 'npm'`, and 4 of the 24 sites do not. |
| 6 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md:176 | The verify command added for step 1.4 requires a toolchain this same diff declares absent. It is `python3 -c "import yaml; …"` — `yaml` is PyYAML, a third-party package, not stdlib — while `docs/development.md:6` is edited in this diff to drop "Python 3.10+" and state "`no-python-in-src.yml` asserts there is no Python left to install", and `ci-cost-budget.md:110-112` records that the Python leg is gone. It is also the only `python3` verify comment in `agents/roadmaps/` (all others are `grep` / `./scripts-run` / `npx`), so it is a one-off convention whose dependency the repo now tells contributors not to install. | fixed | fixed 57867dfd5 — the `python3` + PyYAML verify command is removed from step 1.4; it now verifies with `grep`, so nothing in the roadmap depends on a toolchain the prerequisites just dropped. |
| 7 | low | .github/workflows/release-drift.yml:37 | The concurrency exposure surfaced by round-2 finding 2 was closed by deleting the claim, not by recording the instance. That finding's substance was that a false class-wide safety sentence "closes off the audit that would find the sibling"; the repair removed the sentence from `rule-backstops.yml`, and the sibling is still live and now unrecorded anywhere. Verified: `release-drift.yml` triggers only on `schedule` (`:29`) and `workflow_dispatch` (`:32`), and carries `group: release-drift` with `cancel-in-progress: true` — a global, per-run-unkeyed group on a workflow with no PR trigger, so a manual dispatch evicts a queued daily run, which is the precise pending-eviction loss step 1.4 and Risk-Register row 5 (`:427`) exist to prevent. Neither the step (which frames the five workflows as the whole exposed set), nor the register row, nor a follow-up step names it. | fixed | fixed 57867dfd5 — the `release-drift.yml` instance is now recorded in step 1.4 as found-and-deliberately-not-changed, with the reason, instead of only the false class claim being deleted. |
