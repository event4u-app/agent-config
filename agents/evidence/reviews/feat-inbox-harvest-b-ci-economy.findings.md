# Findings: feat-inbox-harvest-b-ci-economy
<!-- completion-review: v1 | reviewed: 2026-08-11 | scope: 136ff91b84ce8d51d7e269182b8adbc1b034262fafb9203e4b698d9313b29bf7 | diff: 910f7c4e85886f194f214ceeafe06c2b479b154e | reviewer: r2-fresh-subagent-feat-inbox-harvest-b-ci-economy | prompt_hash: 03119a7107714dc0bcd2c6449b213847b7d285a8158f9e1b3d222f37040fca65 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 910f7c4e85886f194f214ceeafe06c2b479b154e
  scope_hash: 136ff91b84ce8d51d7e269182b8adbc1b034262fafb9203e4b698d9313b29bf7
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md
  roadmap_hash: ea54587aa9c701deebb670cbd10d647d5133a35036fdcaf0c226c59866b7a6a2
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-11T06:49:35Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | docs/contracts/ci-cost-budget.md:44 | The kill criterion this file ships at `:173` — "Every job named in the table resolves to a live job in `.github/workflows/`" — is falsified by the table it governs, in the same commit. Row `:44` names job `smoke-contracts`; `smoke.yml` declares four jobs (`smoke-kernel` `:47`, `smoke-router` `:63`, `smoke-schema` `:79`, `smoke-skills` `:95`) and none of them is that. `smoke-contracts` is the *workflow* display name slugified (`smoke.yml:1` `name: Smoke Contracts`) — and the companion contract cross-linked from this file's own header already lists the four correctly (`branch-protection-policy.md:60`). Two further rows misdescribe their shape: `:45` annotates `skill-lint.yml` "(single)" though it declares three jobs (`skill-lint` `:51`, `originality-gate` `:207`, `skill-lint-strict` `:301`), and `:44` presents `smoke.yml` as one ubuntu job. That in turn falsifies the Method paragraph at `:25` — "those workflows are single-job, so run-level is the job figure plus queueing" — for three of the five non-`tests.yml` rows (`smoke-public-install` is 3 OS × 2 Node by its own row text, `smoke.yml` is 4, `skill-lint.yml` is 3); only `consistency.yml` and `release-guard.yml` are single-job. The two deltas flagged as regressions at `:56-59` (`smoke.yml` 18 s → 82 s) and the "no longer a ceiling violation" call (413 s → 291 s) therefore compare a per-job 2026-05-26 figure against a multi-job run-level figure, which is not like-for-like. This is round-1 finding 3 surviving its repair: the repair narrowed the criterion to "the direction that actually failed" and asserted it now holds, without checking the table against it — the row was re-measured (18 s → 82 s) while its wrong job name was carried through untouched. | open | Verified: job keys via `awk` over `.github/workflows/*.yml`; `grep -rn smoke-contracts .github/` returns only `docs/contracts/smoke-contracts.md` path references. |
| 2 | medium | .github/workflows/rule-backstops.yml:62 | The new justification comment claims "the 23 workflows that carry an unconditional `cancel-in-progress: true` are all PR-only". Both halves are false. Of the 23 pre-existing concurrency blocks, 16 carry `true` and 7 carry `false` (`cloud-release`, `deploy-mcp-worker`, `deploy-site`, `publish-npm`, `release-guard`, `release`, `sync-visibility`). Of the 16 with `true`, `release-drift.yml` has no `pull_request` trigger at all — `schedule` `:29` plus `workflow_dispatch` `:32` — and sits in the global group `release-drift` with `cancel-in-progress: true`, i.e. the exact pending-eviction exposure this comment cites as absent from the class, on a daily run its own header calls "a 24h detection window". `evaluator-umbrella` and `consumer-matrix` also carry `schedule`; `no-python-in-src`, `site`, `smoke` and `tests` also carry `push`. The comment is load-bearing: it is the stated reason the five repaired workflows are treated as the whole exposed set, so a false class-wide safety claim closes off the audit that would find the sibling. | open | Sibling exposure itself is pre-existing and outside this diff; reported as the evidence falsifying a claim the diff adds, not as a fix demand. |
| 3 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md:161 | Step 1.4's body was not reconciled with its own repair, and the file now contradicts itself. `:161` still asserts "The group still serialises overlapping non-PR runs instead of racing them" — false under the shipped key `github.workflow-…github.run_id`, which puts every non-PR run in a group of one, so overlapping non-PR runs race and are never serialised. `:157` still asserts the `cancel-in-progress` expression alone "satisfies the step's requirement everywhere", which is precisely the claim round-1 finding 1 falsified. The Risk Register in the same file at `:427` says the opposite in bold ("Scoping `cancel-in-progress` was NOT sufficient … Fixed by the group KEY: non-PR runs are keyed by `github.run_id`, putting each in a group of one"). With `acceptance-criteria.md` empty by design, the step text is the specification of record, so the record now documents the rejected design. Consequence beyond prose: for `adoption-snapshot.yml` and `release-adjacent-health.yml` (no `pull_request` trigger) the block is permanently a group of one with `cancel-in-progress: false` — inert configuration that reads as a control, which `:161` is what obscures. Secondary: the `<!-- verify: -->` comment inserted at `:164` splits the original sentence, leaving "23 of 28 carry / it;" dangling mid-paragraph. | open | Group semantics read from the five shipped blocks; contradiction read from the same file at `:154-166` and `:420`. |
| 4 | medium | docs/development.md:6 | The Prerequisites list still requires "Python 3.10+ (for linter, condensation tools)" while the same file, edited by this diff 70 lines below at `:76-77`, states "There is no Python leg: the toolchain moved to TypeScript". `src/` contains zero `.py` files (`no-python-in-src.yml` gates that permanently), the linter is `src/scripts/skill_linter.ts` and condensation is `src/scripts/check_condensation.ts`. Step 3.3 claims this edit "repaired a drift cluster found in place" and it did repair the task list and the CI matrix — but left the same defect class, in the same file, above the fold, where a new contributor reads it first and installs a toolchain nothing uses. | open | Verified: `ls src/scripts/*.py` → no matches; `no-python-in-src.yml:11-13` scopes the permanent guard to `src/**/*.py`. |
| 5 | medium | docs/contracts/ci-cost-budget.md:195 | The Quarterly review checklist was not reconciled with the re-measurement it now sits below. Item 1 (`:195`) still prescribes re-capturing the baseline via `gh run list --branch main --limit 50 --json name,createdAt,updatedAt` — run-level wall-clock — which the new Method paragraph at `:18-24` explicitly supersedes for every `tests.yml` row in favour of the per-job API (`/actions/runs/<id>/jobs`). A reviewer following item 1 next quarter re-derives run-level numbers and compares them against per-job rows, silently getting incomparable figures: the exact measure-the-wrong-thing failure this change exists to correct. Item 4 (`:202`) mandates "Update the 'Expected savings' table once optimisations land" against a section this same diff retitled historical at `:104` and declared "never re-derived after that" at `:110` — a standing instruction to maintain a frozen section. | open | Introduced by the round-1 finding 4 repair (freezing the section) without touching the checklist that governs it. |
| 6 | low | tsconfig.test.json:7 | `incremental` and `tsBuildInfoFile: ".tscache/test.tsbuildinfo"` were added to a config that nothing ever passes to `tsc`. `typecheck` is `tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json`; a repo-wide search finds `tsconfig.test.json` referenced only by `eslint.config.js:38` as a typescript-eslint `project` entry, which creates a Program and writes no buildinfo. Empirically confirmed on this machine after the author's own runs: `.tscache/` holds `scripts.tsbuildinfo` only. The roadmap at `:198-200` nevertheless describes "one file per config so the two do not clobber each other", i.e. a second buildinfo that is never written — and round-1 finding 2's own repair note records "only `scripts.tsbuildinfo` is written" while leaving both the config and the claim in place. Harmless at runtime (`incremental` is an emit option and is ignored for program creation), but it is dead config plus a claim the tree falsifies. | open | `ls .tscache/` → `scripts.tsbuildinfo` only; `grep -rn tsconfig.test.json` excluding node_modules/dist. |
| 7 | low | docs/migration/node-floor.md:13 | Line-reference drift caused by this diff and not updated with it. The row cites `tsconfig.test.json:6` as the source of the `lib` value; the three lines this diff inserts (comment, `incremental`, `tsBuildInfoFile`) move `"lib"` to `:9` and put the comment at `:6`. Its sibling citation `tsconfig.ui.json:4` is untouched and still correct, so the row is now half-right — the same stale-pointer class Phase 0 spent its budget removing. | open | `cat -n tsconfig.test.json` vs `docs/migration/node-floor.md:13`. |
| 8 | low | tests/scripts/build_proof.test.ts:20 | One measurement, three values, all authored in this commit. The file's pre-fix cost is stated as 290 s (`ci-cost-budget.md:145` and roadmap step 3.1), ~269 s (this comment) and ~260 s (`ci-cost-budget.md:151` and roadmap step 3.2). The comment also carries "8.7x the next one", a ratio that only holds against 290 s (290 ÷ 33.5 = 8.66, using the next-slowest file the roadmap names); at 269 s it is 8.0x. In a change whose thesis is that measurement beats assertion, the headline number should be one number. | open | Ratio arithmetic against the 33.5 s next-slowest figure recorded in roadmap step 3.1. |
| 9 | low | docs/contracts/release-pr-gating.md:19 | This diff re-measures `smoke-public-install.yml` from 413 s to 291 s and states at `ci-cost-budget.md:159` that it "is now 291 s, under the ceiling. No open ADR is required for it." The companion contract — cross-linked from `ci-cost-budget.md`'s own header block — still opens with "avg **413 s** (3-OS × 2-Node matrix), `Tests` avg **218 s**". The two contracts previously agreed on a stale figure and now disagree on a current one; a reader arriving via the header link gets the number the same change just retired. | open | Directionally the release-PR-skip argument still holds, so the contradiction is cosmetic in effect but is the diff's own doc-impact. |
