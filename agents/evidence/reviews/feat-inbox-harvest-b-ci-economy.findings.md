# Findings: feat-inbox-harvest-b-ci-economy
<!-- completion-review: v1 | reviewed: 2026-08-11 | scope: 2f87177e55a3395b27422340e7a10280bfe7d4572e07bdc6ff8367ce874e2d09 | diff: 3a3523de29b269b9b580fd9bd071d94cafa0a2cd | reviewer: r2-fresh-subagent-feat-inbox-harvest-b-ci-economy | prompt_hash: 70931bcb5a2c3a2e8e3597933270868ca6990bb5be882e4c5bfd5188d0b43a57 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 3a3523de29b269b9b580fd9bd071d94cafa0a2cd
  scope_hash: 2f87177e55a3395b27422340e7a10280bfe7d4572e07bdc6ff8367ce874e2d09
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md
  roadmap_hash: 0febf637a304c30205b58c616f9f1eec1f79ae5835c900463086fda38ace374c
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-11T06:30:14Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | .github/workflows/rule-backstops.yml:63 | The added `concurrency` group carries no per-commit key, so on `push: main` a *pending* run is cancelled as soon as a third qualifying push arrives — GitHub cancels the previously-pending run in a group even when `cancel-in-progress` is false. The block's own comment promises the opposite ("on main this workflow is the ONLY run for that commit, so cancelling it would leave the commit with no backstop signal at all"), and before this diff no group existed so every push-to-main run executed. Conditioning only `cancel-in-progress` addresses in-progress cancellation, never queue cancellation. Same shape in the four sibling blocks (self-review-gate.yml:24, adoption-snapshot.yml:34, cross-model-canary.yml:26, release-adjacent-health.yml:24), where a queued schedule/dispatch run can likewise be dropped. A per-commit group key for non-PR events (`github.sha` / `github.run_id`) is what the stated guarantee needs. | fixed | fixed efa7cc1d1 — non-PR runs keyed by `github.run_id` (group of one) in all five blocks; each comment now names the group key, not `cancel-in-progress`, as the guarantee. |
| 2 | high | tsconfig.json:15 | `incremental: true` is added to the project that `build:cli` **emits** from (`tsc -p tsconfig.json && chmod +x dist/cli/agent-config.js`), not only to the `--noEmit` typecheck path the change is justified by. Verified with the repo's own TypeScript 5.9: an emit run, then deleting the output, then re-running emits nothing and exits 0 (noEmit→emit is handled correctly, deleted-output is not). `dist/install/*.js` is tracked build output guarded by tests.yml:302 "Committed build output is fresh", a gate whose comment says it exists precisely because MISSING output is invisible to `git diff` — its remediation text ("Run 'npm run build:cli' and commit the result") becomes a no-op while `.tscache/root.tsbuildinfo` survives, and that file is gitignored so it survives any `git restore`/`git clean -f` of `dist/`. Nothing in the diff or in docs names deleting `.tscache/` as the recovery. | fixed | fixed efa7cc1d1 — `incremental` removed from the base config and from the emitting UI config, added explicitly to `tsconfig.scripts.json` / `tsconfig.test.json`. Verified: only `scripts.tsbuildinfo` is written, `build:cli` emits and exits 0, typecheck 8.2s cold to 3.2s warm. |
| 3 | medium | docs/contracts/ci-cost-budget.md:171 | The kill criterion this change ships is falsified by the table it governs. It states "every job key in the table resolves to a live job … and every job that runs on a PR appears in the table" and calls it "decidable in one pass"; 19 workflows carry a real `pull_request:` trigger key and the table names 5 of them (tests, smoke-public-install, consistency, smoke, skill-lint). A reviewer applying the criterion as written must fail the change; roadmap step 0.5 nevertheless closes on it. | fixed | fixed efa7cc1d1 — criterion narrowed to the resolves-to-a-live-job direction, which is decidable and is the one that failed; the converse is explicitly disclaimed with the 19-vs-5 count. |
| 4 | low | docs/contracts/ci-cost-budget.md:110 | The "Expected savings" section still credits Phase C with "removing the Windows leg (60–90 s) and the 3-version Python sweep", and its scenario table (:118) still prices "Feature PR touching Python paths only on 3.12" — both describe workflows this same file declares absent eight lines earlier (:48-52, :96-102). The diff annotated the adjacent "Critical path observations" as historical but left this section unmarked, so the dead-claim class step 0.4 widened its scope to remove survives one section further down. | fixed | fixed efa7cc1d1 — section retitled historical with a note that both sibling workflows were later removed outright. |
| 5 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-b-ci-economy.md:408 | The Risk Register was not reconciled when the steps it anchors to closed. Row 1 mitigates via step 2.3 ("consumers assert the artefact rather than rebuilding on cache miss (2.3)"), which this diff cancels; row 4 (:411) asserts "the sweep in 1.3 stays report-only" while 1.3 shipped strict with a non-zero exit, as its own step text records. The register now prescribes mitigations for a design the roadmap abandoned. | fixed | fixed efa7cc1d1 — rows 1, 3, 4 and 5 reconciled; row 5 now records that the first mitigation was insufficient and why. |
| 6 | low | tests/scripts/build_proof.test.ts:28 | `const PROOF = render()` runs during module evaluation, and `render()` can throw — it calls `renderPreventedFailures` (src/scripts/build_proof.ts:167), which throws on a claim with no ledger entry or downgraded to unbacked. A real occurrence now aborts collection of the entire file, taking out the five tests at :62-93 that exist to diagnose exactly that cause. Before the change a `render()` failure left those five running and reporting the precise reason. Deferring the call (a lazily-memoised getter) keeps the saving without the coupling. | fixed | fixed efa7cc1d1 — lazily memoised; a `render()` throw now surfaces in the first test that calls it and the five diagnosing tests still run. 8 tests pass, file 109s. |
| 7 | low | docs/development.md:95 | The new snippet imports `from '../_lib/run_in_process'` without the extension, while every in-repo call site uses `'../_lib/run_in_process.js'`. It resolves today only because `tsconfig.test.json` inherits `moduleResolution: "Bundler"`; a reader copying the documented form diverges from the house specifier convention this repo uses everywhere else, in a doc section added to stop exactly that class of drift. | fixed | fixed efa7cc1d1 — snippet uses `../_lib/run_in_process.js`, matching all 42 in-repo call sites. |
