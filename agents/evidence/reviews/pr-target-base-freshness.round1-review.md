# Findings: pr-target-base-freshness
<!-- completion-review: v1 | reviewed: 2026-08-13 | scope: 3a10dca09001b706d8de57c0b4df42104affe91a4216febc1fdfb3b48711656a | diff: 4e8988172a773be43b8a9b3d93d3676f76fc5adf | reviewer: r2-fresh-subagent-pr-target-base-freshness | prompt_hash: 3535d9921e4c5c57f53a25923405e89df0fbd330fb4c0a0f68bea53e57af43f7 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 4e8988172a773be43b8a9b3d93d3676f76fc5adf
  scope_hash: 3a10dca09001b706d8de57c0b4df42104affe91a4216febc1fdfb3b48711656a
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-13T18:43:15Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | tests/scripts/check_branch_freshness.test.ts:154-176 | The test for the headline safety property (loud even under `--quiet`) asserts inside `if (out.includes("could not ask the forge"))`. Wherever `gh` answers, the guard is false and the test asserts nothing — it passes identically whether the warning is emitted or suppressed. `main()` exposes no forge seam (`resolveBase(argv, branch)` uses the module default), so the test cannot force the path it claims to cover. The inline comment asserts an environment fact the test never verifies. | fixed | `main(argv, forge?)` seam added; the test now stubs the unavailable answer and asserts unconditionally, plus a negative twin asserting the warning is absent when the forge DID answer |
| 2 | medium | src/scripts/check_branch_freshness.ts:342-350 | Both new self-test cases invoke `runTwoBase(base)` with `--base`, exercising only the PRE-EXISTING flag path, never the new forge resolution. The removed code produces identical verdicts on both. The fixture docstring claims it demonstrates the defect and that a regression reinstating a hardcoded base flips exactly one case — true only for a regression that also ignores `--base`; deleting `askForgeForBase` entirely leaves both green. `SELF_TEST_MIN_CASES` 3 to 5 and `SELF_TEST_MIN_REJECT` 1 to 2 were raised on coverage the new behaviour does not have. | fixed | Fixture now sets the bare origin's own HEAD and the cases run WITHOUT `--base`, so the resolution decides the verdict; overclaiming docstring rewritten to state what the pair actually proves |
| 3 | medium | src/domains/git/pr/create/command.md:82 | New prose states exit 0 = current, 1 = behind, and the matrix maps exit 0 to Proceed. A third path exists and is unrepresented: `remoteHead(base) === null` prints NOT VERIFIED and returns 0. The matrix gained a row for the forge-unavailable warning but not this one, so an unverified run reads as a clean pass — contradicting the change's own stated principle. Newly reachable via machine-chosen bases: a `baseRefName` that `ls-remote` cannot resolve converts what the old hardcoded check would have refused into a green-reading exit 0. Repeated in src/skills/git-workflow/SKILL.md:55-56. | fixed | Both docs now state that exit 0 carries two verdicts and that the status must be read together with the line; matrix gained the NOT-VERIFIED row incl. the deleted/renamed/fork-base case |
| 4 | medium | src/domains/git/pr/create/command.md:74 | The command doc (also projected to dist) instructs `./scripts-run src/scripts/check_branch_freshness`, a package-internal shim absent from a consumer repo, while the sibling doc changed in the same commit uses the consumer form. Two invocations for one gate across two docs an agent may read in the same turn, and the mandatory-gate one is the unrunnable one downstream. | fixed | Command doc switched to the same consumer form as the skill; the two surfaces now name one invocation |
| 5 | medium | taskfiles/ci-fast.yml:41-45 | The budget note claims ~1-2s against the 25s ceiling, but `FORGE_TIMEOUT_MS = 10_000` permits a single cold or hung `gh` to consume 10s — 40 percent of the stated ceiling — before falling back. The degrades-rather-than-fails clause enumerates only fast failures; the slow path, the one a budget claim exists to bound, is unaddressed. | fixed | Note now states the typical figure AND the 10s worst case with the constant that causes it, and separates fast failures from the timeout path |
| 6 | medium | src/scripts/check_branch_freshness.ts:411 | `main()` shells out to the real `gh` on every non-CI call, including from tests, each spawning `gh pr list` inside the temp fixture with live credentials. `gh` resolves its repo from ambient state, so `GH_REPO` or a host override can return a real PR `baseRefName` for a same-named branch and silently change the base under test — a hermeticity hole in a suite whose header argues for real repos over mocks. It also emits the warning line into unrelated test output. | fixed | Every `main()` call in the suite now passes a forge stub; no test spawns `gh` |
| 7 | low | src/scripts/check_branch_freshness.ts:160-169 | `repoDefaultBase()` reads `refs/remotes/origin/HEAD`, a LOCAL ref written at clone time and never refreshed without `git remote set-head`. The module thesis is asked-of-the-forge and never-a-tracking-ref, yet the base NAME comes from exactly that class of stale local state, and `describeBase` reports it unhedged. `git ls-remote --symref origin HEAD` asks the server at the cost the gate already pays. | fixed | Split into `serverDefaultBase` (`ls-remote --symref`, new `origin-symref` provenance) and `localDefaultBase`, which is now used only for the free short-circuit and as a fallback that `describeBase` labels as possibly stale |
| 8 | low | src/scripts/check_branch_freshness.ts:411-417 | `resolveBase` runs before the `branch === base` short-circuit, so standing on the base branch spends a full forge round trip to compute a value the next line discards, and `gh pr list --head main` can never usefully answer. Resolving flag and origin/HEAD first, short-circuiting, then asking the forge preserves behaviour at zero cost on that path. | fixed | Short-circuit moved ahead of the forge call using the zero-cost local default; covered by a test asserting the stub is never invoked |
| 9 | low | src/scripts/check_branch_freshness.ts:156 | Asymmetric handling of the same gh-answered-without-the-field condition: a missing `baseRefName` returns unavailable, but a missing or non-numeric `number` is coerced to 0, and `describeBase` then prints "base of open PR #0" — a PR that cannot exist, in the one message the change insists must be unambiguous about provenance. | fixed | A missing, non-integer or non-positive number now returns `unavailable`, symmetric with `baseRefName`; test pins that no message can claim PR #0 |
| 10 | low | src/scripts/check_branch_freshness.ts:285-313 | `buildTwoBaseFixture` duplicates `buildFixture` wholesale — the helpers verbatim plus the identical init/remote-add/first-commit/push preamble. Two copies of the same scaffolding will drift independently; the difference between them is four lines. | fixed | Shared preamble extracted into one `scaffold()` helper both fixtures build on |
| 11 | low | src/scripts/check_branch_freshness.ts:177-182 | `--base` followed by another flag yields `base = "--quiet"`, since the guard rejects only undefined and empty. Pre-existing shape, but this diff rewrote the surrounding semantics (a bare trailing `--base` no longer falls back to the default), so the parsing was in hand. The resulting `remoteHead("--quiet")` failure lands on the exit-0 NOT-VERIFIED path from finding 3. | fixed | Parsing extracted to `explicitBase()`, which rejects a value starting with `-`; four-case test pins the shape |
