# Findings: council-codex-transport
<!-- completion-review: v1 | reviewed: 2026-08-12 | scope: 113b7f805b50a4eaa4a9ef0aa483424f02d326b749e672c9831afe38bfe12567 | diff: cd8e518c115b2b873e2c85fc8aaf1c85edc37f67 | reviewer: r2-fresh-subagent-council-codex-transport | prompt_hash: 37002cf1c49a13bf630c1d10adca2e368eb4f39b378bbe107356e15018e1c7aa -->

<!-- context-manifest: v1
inputs:
  diff_sha: cd8e518c115b2b873e2c85fc8aaf1c85edc37f67
  scope_hash: 113b7f805b50a4eaa4a9ef0aa483424f02d326b749e672c9831afe38bfe12567
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-12T13:28:46Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/council_cli.ts:2792 | The new post-run print does not correct the stale banner, it adds a second one. A real (non-injected) degraded run now emits TWO `council:quorum · …` lines with an identical prefix and contradictory content: line 2624 prints the pre-run reading FIRST (`2/2 present, needed 1 — concluded.`), line 2792 prints `0/2 present … INCONCLUSIVE` many lines later, after the estimate table, quota summary, cost disclosure and the whole consult. Neither line says which phase it is — the event log solved exactly this with an explicit `phase: pre_run\|post_run` field (`_emitQuorumEvent`), stdout did not. An operator skimming, or anything grepping `council:quorum ·`, can still take the first, which is the precise misread the change was written to stop. The added comment reasons carefully about a missing line meaning two things while leaving two same-prefixed present lines meaning two things. Label the phase in `_format_quorum_line` (or suppress/mark the pre-run line once a post-run reading exists). | open | |
| 2 | medium | tests/scripts/council_cli.test.ts:590 | The regression test cannot reproduce the shape it documents. `cmd_run` only calls `build_members` when `opts.members` is null (council_cli.ts:2545); the harness injects members, so `quorum_out.result` stays `null`, the `if (quorum_out.result !== null)` pre-run print at council_cli.ts:2623-2625 never fires, and the run emits exactly ONE quorum line — the one under test. The measured two-line failure (`2/2 present … concluded` followed by silence) is therefore structurally unreachable in this harness, and finding 1 above would not be caught by it. | open | |
| 3 | medium | tests/scripts/council_cli.test.ts:607 | Consequence of 2: the "the pre-run reading must not be the LAST word on attendance" assertion inspects a one-element array that the fix itself wrote, so `quorumLines.length > 0` and `last contains '0/2 present'` are re-assertions of line 601, not an independent check. The in-test comment claims the form "keeps the test honest if a future pre-run banner legitimately prints first" — it cannot, because no pre-run banner can print on this path. | open | |
| 4 | medium | tests/scripts/council_cli.test.ts:595 | The harness runs the real `cmd_run` with `cwd: REPO_ROOT` and `env: { ...process.env }` (no sink override), so `_emitQuorumEvent('post_run', …)` at council_cli.ts:2768 appends a synthetic `{present: 0, total: 2, status: inconclusive}` record through `events_log.js` to the production events sink on every test run. council_cli.ts:598 states attendance rates are computed over exactly the `post_run` population, and that `pre_run` lines are excluded "for exactly that reason" — this is the first test to write into the clean population. The responses JSON is removed in `finally`; the appended event line cannot be, and nothing marks it synthetic. | open | |
| 5 | medium | src/scripts/ai_council/clients.ts:1718 | Sibling construct not swept. The diff's own rationale is that a vendor CLI rejecting an unknown flag fails 100% of that member's calls with exit 2; the byte-identical construct `if (system_prompt) { cmd.push('--system', system_prompt); }` survives in `GeminiCliClient._build_command`, and nothing in the branch records whether `gemini` accepts `--system`. Either verify it against the CLI's help and say so, or apply the same removal — a fix of one instance without a search of the population is how the openai path shipped broken for as long as it did. | open | |
| 6 | low | src/scripts/ai_council/clients.ts:1834 | Second half of the same sweep. The new docstring cites dodging "the argv limit a whole roadmap file would otherwise hit" as a reason for stdin; `XAICliClient._build_command` (`[binary, '-p', user_prompt]`) and `PerplexityCliClient._build_command` (clients.ts:1900, same shape) still put the entire user prompt on argv, so the argv-limit exposure the branch just removed for codex remains unchanged for two other members and would surface as an `os_error: E2BIG` rather than as anything legible. | open | |
| 7 | low | src/scripts/ai_council/clients.ts:1596 | System and user prompt are concatenated with a bare `\n\n` into a single untrusted-content channel. `codex exec` offering no privileged channel is the given, but that makes an explicit boundary the only available mitigation, and there is none: prompt content (a diff, a roadmap, fetched text) sits in the same undelimited blob as the instructions and can restate or contradict them. A fenced/spotlighted system block would cost one template string and keeps the approximation honest about where the instructions end. | open | |
| 8 | low | tests/scripts/ai_team/team_dispatch.test.ts:467 | The assertion is weaker than the comment above it. `expect(calls[0]?.stdin ?? '').toContain('findings')` is satisfied by the system prompt alone, so it does not show that "system prompt + bundle ride stdin as one payload" — the bundle half is unverified, and a regression that dropped the bundle from the payload would still pass. The file already imports `TEAM_REVIEW_SYSTEM_PROMPT` (used at line 393) and has `FAKE_HEAD`, so a two-part assertion is available at no cost. | open | |
