# Findings: dispatch-safety-do-not-touch
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 685536f5a1f98e533d1b0751b65bc0a489bee5f675b22eb151e9fedf7146c576 | diff: 0bc79b09bae0c025ea54920616c195667e0c76a2 | reviewer: r2-fresh-subagent-dispatch-safety-do-not-touch | prompt_hash: 592cdda3719972bd6b72463412bcec734f2a4a310a088b27a09fa3a549aeead9 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 0bc79b09bae0c025ea54920616c195667e0c76a2
  scope_hash: 685536f5a1f98e533d1b0751b65bc0a489bee5f675b22eb151e9fedf7146c576
  roadmap: agents/roadmaps/archive/road-to-inbox-harvest-2026-08-b-dispatch-safety.md
  roadmap_hash: 01e0b86fd5583b3cedcd967777d5797a0e395d33b0e0c4d3765ac0ab1884cc65
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T17:11:04Z
-->

**Acceptance criteria — checked, genuinely absent.** `roadmap.md` carries neither an `## Acceptance criteria` heading (its headings are Context / Phase 1-4 / Risk Register / Blockers) nor any `- **AC-n:**` bullet; the string `acceptance` occurs once, in unrelated prose at :314. Per-step `<!-- verify: -->` annotations (14 of them) carry that role instead. So the empty `acceptance-criteria.md` reflects a roadmap that declares none, not an extractor that missed them — no finding is raised for it, and the review below is scoped to the diff.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/_lib/subagent_capsule.ts:143 | The shipped grammar is wider than the comparison the field promises, and the relocated guard is told to use it as a matcher. `isPathRef` accepts a bare directory (`docs`), a trailing-slash relative ref (`../other-worktree/`), a glob (`src/**/*.ts`) and a `file:line` ref — all pinned as accepted in tests/scripts/session_recycle.test.ts:357-368 — while the field TSDoc (:305-307) justifies its existence as "was this path off limits? is answerable by comparison instead of by reading", and road-to-subagent-lifecycle-integrity.md:527 instructs the deferred guard to "Match on `isPathRef`-shaped entries via the exported predicate, never a second definition of path ref". `isPathRef` answers "is this one token", not "does this entry cover that write target": path normalisation, directory-prefix matching and glob matching are specified nowhere in the change. The deferred step therefore inherits an unstated and non-trivial semantics decision while reading as though the predicate already settled it, and a `../` or glob entry that passes validation would silently match nothing. | open | |
| 2 | low | src/scripts/_lib/subagent_capsule.ts:161 | `checkPathRefList` suppresses the shape check on ANY error `checkList` pushed, including the orthogonal entry-count error at :190-191. A 41-entry list of prose sentences therefore reports only "carries 41 entries (max 40)"; every prose violation stays invisible until the author trims to 40 and re-validates — an extra refuse/repair round-trip on the write path the disposition cites as the repair mechanism. The stated intent (the helper comment and the "does not double-report an entry that already failed the budget" spec) is to suppress a duplicate BUDGET report only, so gate on that specific condition rather than on `errors.length` moving. | open | |
| 3 | low | tests/scripts/session_recycle.test.ts:325 | The negative assertion in "names the offending entry" passes only because of the quote characters: `expect(...).not.toContain('"src/generated/api.ts"')`, while the error message itself hardcodes the bare string `src/generated/api.ts` in its advisory text (subagent_capsule.ts:169). The spec therefore asserts a JSON-quoting artefact rather than the property it names (the valid entry was not listed as an offender), and it flips silently the moment the advisory example path or the quoting of `shown` changes. Use a fixture path that does not appear anywhere in the message. | open | |
| 4 | low | src/scripts/_lib/subagent_capsule.ts:152 | The new helper names its offending entries on the stated argument that "one of your twelve entries is wrong" is not actionable — but `checkList`, which it wraps and which runs first on the same field, still reports its own bad entries as an unnamed count (:193-197). The standard the change introduces is not applied to the check one screen below it, so an over-budget or multi-line `do_not_touch` entry is still reported un-named. The disposition's "Known and deliberately not fixed here" note (agents/settings/contexts/do-not-touch-guard-disposition.md:80-92) scopes only the path-shape construct, so this asymmetry is neither fixed nor recorded. | open | |
| 5 | low | agents/roadmaps/road-to-subagent-lifecycle-integrity.md:523 | The relocated Step 4 is parked inside "Phase 4: Role axis binds on payload, not env", whose falsifier cancels the whole phase when `agent_id` is absent from tool events. The exemption ("Step 4 is independent of that falsifier ... and survives a cancelled role axis") is prose only; nothing structural distinguishes the step, so a mechanical phase-level cancellation, or a reader applying the falsifier as written, sweeps away the very item the relocation existed to preserve. That is Risk 6 of the same file ("nothing re-raises it") reproduced one level down, inside the mitigation. Anchor the step in a phase whose falsifier cannot reach it, or restate the falsifier as step-scoped. | open | |
| 6 | low | agents/roadmaps/archive/road-to-inbox-harvest-2026-08-b-dispatch-safety.md:375 | "14 new specs" matches neither available counting convention. The change adds 8 `it` / `it.each` blocks (5 in the `do_not_touch` describe, 3 in the `isPathRef` describe) which expand to 20 executed cases (5 + 8 accepted values + 6 rejected values + 1 non-string). 14 is the sum of the two parameterised tables only, so it silently omits the six specs that carry the validator behaviour this step actually shipped. A checkable number in a closing note should match what a reader gets by counting. | open | |
