# Findings: long-horizon-phase4-close-r2
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 1a239e019b3d39895754279c9374ebd82c530c45056cbd069a3f8fc5bfddf6b8 | diff: a822341cff699b56e53f03d92bf8b84f1385d3cc | reviewer: r2-fresh-subagent-long-horizon-phase4-close-r2 | prompt_hash: e2b8efee02aef9d3eb18dd8624e23f921408ec8dd28c37b64be64508d68f9c43 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: a822341cff699b56e53f03d92bf8b84f1385d3cc
  scope_hash: 1a239e019b3d39895754279c9374ebd82c530c45056cbd069a3f8fc5bfddf6b8
  roadmap: agents/roadmaps/road-to-long-horizon-execution.md
  roadmap_hash: 4f552891284ac41d4ccc163ae4959a718e209d3580e53fead9ffe43871d6fd89
  ac_hash: 5b49f85313e27ace1904b342e01046c25c050944e168cd7068c07276f60bc133
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T09:34:56Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/interruption_report.ts:590 | The new per-axis N labels a line it does not describe, reproducing the exact misreading it was written to remove. `CONTACT AXIS` carries `power(report.contact_axis_runs, …)` = `runsWithLedger` (ledger-sourced, 19 on the measured day), but the second line under that header, `median user wait` (:592), is `median(allGaps)` — gaps accumulated from `bySession` chat-history turns (:444, :529), i.e. the WALL-CLOCK source whose own N is 4. `roadmap-wall-clock-baseline` in docs/CLAIMS.md defines WAITING as its instrument, so the metric belongs to the other axis by the claim's own definition. Two errors in one line: the reader is told n=19 for a figure resting on 4 timing-bearing runs, and the unit is runs where the denominator is gaps. The roadmap AC repeats the conflation verbatim ("user wait median 68.9 min" quoted beside "the contact axis stands at 19"). Fix: print the wait line under the wall-clock axis, or give it its own per-source N. | fixed | Confirmed. user wait moved under WALL-CLOCK AXIS, where roadmap-wall-clock-baseline names WAITING as its own instrument, and given a THIRD count of its own because its unit is gaps and not runs. The roadmap AC that quoted the conflated pair is rewritten per axis. |
| 2 | high | src/scripts/interruption_report.ts:613 | Operator-facing output still asserts what this change-set falsified, in a file the diff edits. The report prints "the unattended lane cannot run yet (the spawn is unbuilt and the budget defaults to disabled). The threshold is pre-registered as `unattended-demotion-gate`". After this change the spawn is a published REFUSAL, not unbuilt-yet, and `unattended-demotion-gate` took its honest-null path and is declared closed in the same diff (docs/CLAIMS.md:680). So the report emits precisely the "wait for a release nobody is preparing" sentence that step 4.0 rewrote `run_supervise.ts` to eliminate, and points a reader at a closed threshold as if live. Sibling-search for the construct across src/scripts + src/agent-src: 6 hits, 4 are historical quotations inside explanatory comments (run_supervise.ts:548, headless_invocation.ts:210, handoff_generate.ts:62, project-intelligence.md:179) and 2 are live operator-facing text — this one and finding 9. | fixed | Confirmed. Rewritten to name the refusal and the closed claim. The sibling-search that should have caught it during 4.0 did not run over this file — recorded as the miss it was. |
| 3 | medium | docs/CLAIMS.md:681 | `unattended-demotion-gate` keeps `status: unbacked` while its own evidence block now reads "HONEST-NULL PATH TAKEN … the capability is closed". The ledger already has the terminal status for exactly this state: `resolved-null`, used by 5 entries and treated by `check_claims.ts:349` as the closed state (`superseded_by` is only legal on it). Left `unbacked`, a closed question stays inside the documented-debt inventory that `check_claims.ts:15` defines and inflates the unbacked count at :500 — the indefinite-pending shape the entry itself argues against, in the entry that argues against it. | fixed | Confirmed. status: resolved-null, last_verified stamped. check_claims green, unbacked inventory 17 -> 16. |
| 4 | medium | agents/roadmaps/road-to-long-horizon-execution.md:413 | `verify:` claims `tests/scripts/headless_invocation.test.ts` — 18 green. The file shipped in this same change-set contains 20 `it(` blocks (5 describes: 2 + 4 + 3 + 8 + 3), none skipped, no `it.each`. The three sibling counts in the same step check out (run_supervise 38, interruption_report 41, unattended_guard 31), and line 282 two steps above carries `decision_memo.test.ts — 18 green`, so the figure reads as copied from the neighbouring line rather than run. The same step spends nine lines correcting a stale count for another file, which is the argument for not shipping a second one. | fixed | Confirmed, and not a copy: 18 was right when written and went stale in the round-1 fix pass, which added two budget-probe cases. Now 20, re-run. |
| 5 | medium | tests/scripts/headless_invocation.test.ts:76 | The test asserts less than its name claims, in the file whose thesis is "asserted rather than promised". `describe('the module cannot start a process')` and the inline note "a module that cannot reach child_process cannot grow a spawn by accident" are both false: `planResume` → `preflight` (unattended_guard.ts:355) → `checkRemotes` → `spawnSync('git', ['remote','-v'])`, one subprocess per plan, so `--print-relaunch` starts N git processes. The scan at :82 is a source-text grep over one file and structurally cannot see the transitive path. What is actually pinned is "this file contains no spawn token" — true and worth keeping, but the describe name, the module docblock's bolded claim, and the `--print-relaunch` help line "Starts nothing, spends nothing" (run_supervise.ts:501) all over-read it. | fixed | Confirmed. planResume -> preflight -> checkRemotes does shell out, one git per plan. The describe, the module docblock and the --print-relaunch help all narrowed to the true claim (no AGENT starts here), and a second test now PINS the transitive git probe so the honest scope cannot drift back. |
| 6 | medium | src/scripts/_lib/headless_invocation.ts:93 | Unreachable branches plus a test that certifies them without exercising them. `PLATFORM_TO_HOST` maps only the three Tier-1 hosts, so `buildResumeArgv` returns at :91 for every other platform and `entry === undefined`, `entry.tier !== 1`, `entry.cli === null` and the `default:` arm at :105 can never be taken. The test at tests/scripts/headless_invocation.test.ts:122 states "Reaching the entry is not the same as being drivable, and the tier is what decides" and passes `'augment'` — which short-circuits at the mapping and never reaches the tier check, so the tier guard has no coverage at all. A fixture that agrees with the code instead of with reality is the failure run_checkpoint.ts:145 names in its own comment. Either drop the dead guards or map a Tier-3 platform so the guard is the thing under test. | fixed | Confirmed. The inventory is injectable now, so the tier guard, the null-cli guard and the missing-entry guard are each exercised by a mapped host; the old case is renamed for what it actually tests. The default arm stays fail-closed and is covered by a map/switch agreement test instead of a fixture that reaches it. |
| 7 | medium | src/scripts/run_supervise.ts:411 | The release line still follows the candidate set rather than the ledger delta, so the digest reports releases that did not happen. `released` counts candidates with `disposition === 'complete'`, not entries actually removed from the ledger, so a completed roadmap that never had a counter prints "released: 1 completed roadmap(s) — relaunch budget reset" and makes a read-report create `supervise-relaunches.json` holding `{}`. With no relaunch mechanism shipping, the ledger is always empty, so today every such line is false. The new comment at :402 claims "The report follows the WRITE, not the intent" — it follows the write's success, not whether anything was released, which is the same gap one level up. `released = Object.keys(readLedger(…)).length - Object.keys(cleared).length` closes it, and the write can then be skipped when the delta is 0. | fixed | Confirmed, and it was the same defect one level up from the round-1 fix. released is the ledger delta now, so a read-report with nothing to release prints nothing and writes nothing. |
| 8 | medium | src/scripts/_lib/headless_invocation.ts:17 | The load-bearing record is an unreachable pointer. This module ("The resolution taken is recorded in the run's decision memo") and the roadmap (:375 "recorded in decision memo 001", :450 and :489 "decision memo 002") rest the entire 4.0 refusal and the 4.3 will-not-fund closure on memos that live in `agents/runtime/state/decisions/<run>/NNN.md` (interruption_report.ts:272) — gitignored, run-scoped, auto-pruned runtime state, and absent from this change-set. A tracked source file citing a transient artefact is the shape `no-roadmap-references` forbids; a reader cannot verify either decision. Promote the durable conclusion into the tracked artefact instead of pointing at the memo. | fixed | Confirmed. The reasoning is written into the tracked roadmap step, the blocker and the module docblock; the memo is named as a local supplement, never as the record. |
| 9 | low | src/scripts/run_supervise.ts:559 | Two surviving "lands with the Phase 4.0 primitive" promises, in the same strings the diff rewrote. The no-`--once` exit says "looping is not implemented yet … the loop driver lands with the Phase 4.0 primitive it would call", and `--interval` (:494) is "accepted and IGNORED — reserved for the loop driver that lands with the Phase 4.0 primitive". That primitive is now a published refusal, so both promise a landing tied to something that will not be built — the D-5 wording the `--relaunch` help ten lines below was deliberately rewritten to remove. | fixed | Confirmed. Both promises rewritten — nothing is scheduled to land. |
| 10 | low | src/scripts/run_supervise.ts:539 | Silent flag precedence. `--digest --print-relaunch` prints only the digest and drops the plans without a word (:537 returns first); `--print-relaunch --relaunch` prints the plans and returns 0, so the published refusal is never emitted and the exit code reads as success on the one flag whose whole purpose is to refuse. Neither combination warns, and the help text does not say the flags are mutually exclusive. | fixed | Confirmed. The three report-shape flags are mutually exclusive and refuse with exit 2 instead of one silently winning. |
| 11 | low | src/scripts/interruption_report.ts:619 | The artefact blamed for the live misreading is unchanged. The roadmap AC states the misreading came from "`runs: 21` under a single ⚠️ SHORT WINDOW banner"; the bare `runs: N` total and the `window_short` banner (:586, still driven by `sessions_found`) both ship exactly as before. `buildReport`'s comment says "the total may not be read as either axis's N", but nothing in the rendered output says so — the reader is expected to infer it from two headers printed 30 lines earlier. | fixed | Confirmed. The runs: total carries its caveat and both per-axis counts inline. |
| 12 | low | src/scripts/_lib/headless_invocation.ts:100 | The gemini argv is read off a contract that contradicts itself, and the diff picks a side silently. `docs/contracts/host-agent-protocol.md:45` says `gemini --output-format json` consumes stdin (what `buildResumeArgv` + `promptOnStdin` build); :83-84 of the same file says `gemini -p … --output-format json`. The module states the shape is "read off that contract rather than invented here", so a reader auditing it lands on a 50/50. Pin the contract or name which row is normative; a pasted command built on the wrong half hangs on stdin. | fixed | Confirmed, and sharper than reported. The contradiction hid a real bug: buildResumeArgv hand-built codex AND gemini as stdin consumers while HOST_CONFIGS passes the prompt in argv for both, so a pasted command would have piped into a CLI not reading one. Fixed by DELETING the second derivation — buildResumeArgv delegates to HOST_CONFIGS, promptOnStdin is gone, and the contract table is corrected with the correction stated. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
