# Findings: drain-a-question-that-survives-the-turn
<!-- completion-review: v1 | reviewed: 2026-09-11 | scope: 921e7fcfff0fc2a05c9f1f49bf179d66f6e62ac617dafdb4906f7b3eb75dca9b | diff: c510f016302a6e7e3af379c30e1717d6df2d399e | reviewer: r2-fresh-subagent-drain-a-question-that-survives-the-turn | prompt_hash: 2895eaa71a19b6d417bf6fe15f16b4f444721a8fa05d3f4e2be6e31ecde8e27d -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-a-question-that-survives-the-turn"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-11 -->

<!-- context-manifest: v1
inputs:
  diff_sha: c510f016302a6e7e3af379c30e1717d6df2d399e
  scope_hash: 921e7fcfff0fc2a05c9f1f49bf179d66f6e62ac617dafdb4906f7b3eb75dca9b
  roadmap: agents/roadmaps/archive/road-to-a-question-that-survives-the-turn.md
  roadmap_hash: 9c416b5e4b0aa6af8c87bed63aa98102b125dedfe26f0022000d7b5f9ef144f2
  ac_hash: 14fc2b05ffce5600556dbb4bb45136de845567885cb616d0e0dfb1fba1981993
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-11T20:04:14Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/measure_turn_end_gate.ts:348 | The instrument's turn boundary is NOT the gate's, and detector E is the detector that breaks on it. `userText` (conformance_scan.ts:451-467) returns null for any user entry whose text *contains* `<system-reminder>` anywhere, or starts with `<local-command-stdout>`; such an entry is skipped whole, so `pendingTexts` and `pendingCalls` are never reset. `readTranscriptTail` instead runs `isSyntheticPrompt`, which matches those markers only at the START of the head, so it DOES reset. The ordinary Claude Code user turn — typed prompt plus an appended `<system-reminder>` text block, joined by `entryText` with a newline — lands on exactly that disagreement, so the instrument merges consecutive genuine turns into one. Reproduced on a constructed store: `[user("what next?")+reminder, reply(ASKED), user("2")+reminder, reply(DROPPED)]` scores `1 turns` and `detector E fires on 1 turns (100.0%)` — E firing on an ask the user demonstrably answered, where the gate would see two turns and stay silent. The error direction inflates the new E rate and the `dropped_with_tool` split, and it also deflates the `c.turns` denominator every rate is divided by. The file header asserts the opposite twice ("the SAME filter the gate uses for the ordinal — not `isInjectedBody` … Population parity is the entire point of this correction"), and the new comment at :272-286 declares exactly ONE known divergence (whitespace-only closing reply) while this larger one is undisclosed. The new boundary test at tests/scripts/measure_turn_end_gate.test.ts:163 uses a bare-string user entry and therefore cannot see it. Pre-existing for A/B/C/F, but newly load-bearing here: E is the only detector whose semantics is "an ask in the SAME turn", so a merged turn manufactures fires rather than merely shifting a denominator. | fixed | `_userTurnText` replaces `conformance_scan.userText` for the turn boundary: it nulls only on a missing text block, exactly as `_messageText` does, leaving `isSyntheticPrompt` as the sole filter — the gate's own shape. Test: measure_turn_end_gate.test.ts, 'ends the turn on a prompt carrying an appended reminder block', proven RED against the old reader (`expected 1 to be 2`, the reviewer's own reproduction). One direction of the mechanism claim is corrected: the divergence is real but NOT 'the ordinary Claude Code user turn' in this store — reminder blocks arrive as their own user entries STARTING with the marker, which `isSyntheticPrompt` already caught, so the two readers disagree on 10 of 4,337 user entries (0.23%). Re-measured after the fix: 51 fires / 2,623 turns / 1,370 multi-text, against 51 / 2,622 / 1,369 before. The published rate survives the correction; the +1 turn is this session's own transcript growing the store. (`c510f0163`) |
| 2 | medium | src/rules/user-interaction.md:63 | Iron Law 3 closes with an unqualified "Mechanically enforced by the `pending-decision` detector in `src/scripts/hooks/turn_end_gate_hook.ts`, which refuses the turn-end". `turn-end-gate` is bound in exactly one slot in src/scripts/hook_manifest.yaml:1324 — `claude`'s `stop` — and on no other platform; the rule ships to ten workspaces and every host. Sibling rules in this tree state the same limit explicitly ("on `claude`, the one host that both binds … and honours a deny", git-history-discipline / autonomous-execution, both of which carry recorded corrections for having over-claimed the enforcing host set). An unqualified enforcement claim in shipped rule prose is the same over-claim in a new place, and the Iron Law reads as a guarantee the manifest does not give. The same sentence is projected verbatim into dist/agent-src/rules/user-interaction.md:63. | fixed | The unqualified 'Mechanically enforced' is gone. The law now says the detector refuses such a turn-end 'but it binds on `claude`'s `stop` slot alone, so everywhere else this law is model-carried and nothing catches a dropped block' — the same honesty shape the sibling rules carry. (`c510f0163`) |
| 3 | medium | src/scripts/hooks/turn_end_gate_hook.ts:765 | Detector E's docstring still argues from "No existing rule catches this: `user-interaction` Iron Law 1 inspects a reply that HAS an options block, `check_reply_consistency` receives a draft and not a transcript, …". That premise is falsified by this same diff, which adds Iron Law 3 stating precisely the continuity obligation, and it is contradicted 35 lines below by the refusal string the diff rewrote to cite "user-interaction Iron Law 3". The diff updated the citation and left the justification that says the citation cannot exist, so the function now asserts both. This is the doc-drift class the tree gates on elsewhere (a doc making a claim the code now contradicts), and it is the WHY-THIS-EXISTS paragraph rather than an incidental comment. | fixed | 'No existing rule catches this' → 'No rule stated this when the detector was written', followed by a sentence naming Iron Law 3 as the closure and the refusal below as its citation. The paragraph now records why the detector exists rather than asserting a gap the same diff closed. (`c510f0163`) |
| 4 | low | src/rules/user-interaction.md:46 | Iron Law 3 is inserted directly beneath the Iron Law 2 block whose last sentence reads "Mechanical backstop: `check_reply_consistency --stdin < draft.md` (non-zero exit on any rule below)". After the insertion, the nearest "rule below" is Iron Law 3, which that script structurally cannot check: it implements Iron Law 1's per-block recommendation rule only (check_reply_consistency.ts:16-25), and detector E's own docstring states the reason — the script "receives a draft and not a transcript", while Iron Law 3 is a cross-reply property. A reader following the backstop sentence will believe the new law has a deterministic net it does not have. Either the backstop sentence needs scoping to Iron Laws 1-2, or Iron Law 3 needs to sit where that sentence does not reach it. | fixed | The backstop sentence is scoped: 'Mechanical backstop for Iron Laws 1 and 2 … (non-zero exit on either). It takes a DRAFT, so it cannot see Iron Law 3, which is about a transcript.' The structural reason is stated rather than the adjacency merely rearranged. (`c510f0163`) |
| 5 | low | agents/roadmaps/archive/road-to-a-question-that-survives-the-turn.md:429 | AC-3 is checked `[x]` on a claim the tree contradicts literally: it asserts `DETECTOR_IDS` in src/scripts/_lib/turn_end_refusals.ts "carries five ids"; it carries six (promissory, language, verification, completion, pending-decision, untested), and docs/contracts/turn-end-detector-demotion.md:32 says so in as many words ("`pending-decision` made it five, and `untested` made it six on the same day"). The substance AC-3 was written for holds — pending-decision is registered and the demotion doc carries its pre-registered Q1/Q2 bar at :138 — so this is a stale count frozen into an archived record, not a missing deliverable. Worth naming because this roadmap goes out of its way to correct a different stale AC in the note under the same list (AC-6), so the count reads as verified rather than as inherited. | fixed | AC-3 now names `pending-decision` instead of counting, with a `corrected-from-reproduction` note citing `turn_end_refusals.ts:67-74` (six ids) and `turn-end-detector-demotion.md:32`. Two sibling staleness defects in the same archived file are corrected alongside: the Non-goal and `estate_growth_exempt` both gave the false kernel-rule reason the blocker resolution already refuted. (`c510f0163`) |
