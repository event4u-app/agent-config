You are reviewing a change in the git worktree at /Users/mathiasberg/projects/galawork/galawork-packages/event4u/agent-config/.claude/worktrees/inbox-2026-09-y. Work only in that directory.

Scope: everything in `git diff 7d1d33cfe..HEAD` on branch `worktree-inbox-2026-09-y`. That is four commits touching `.github/workflows/self-review-gate.yml`, `.github/workflows/release.yml`, `src/scripts/release.ts`, `src/scripts/_lib/release_findings_ingest.ts`, `src/scripts/release_drill.ts`, `src/scripts/release_env.ts`, `src/scripts/release_publication.ts`, `src/scripts/check_release_published.ts`, `tests/scripts/release_findings_ingest.test.ts`, `docs/release-runbook.md`, `docs/succession.md`, `docs/decisions/ADR-113-ci-native-release-label-trigger.md` and one markdown lint marker.

Context you need to judge it: the repository publishes releases with `src/scripts/release.ts`. A release must carry a "findings ledger" at `agents/evidence/release-findings/<version>.json`; `src/scripts/check_finding_dispositions.ts` fails any pull request in the repository when a SHIPPED version has none. The change makes `.github/workflows/self-review-gate.yml` build and commit that ledger onto the release branch, and makes the release's step 7 verify it rather than produce it. Prior review artefacts for earlier attempts at the same goal are at `agents/evidence/reviews/release-findings-ingest.findings.md`, `release-step7-round2.findings.md` and `release-ledger-in-ci.findings.md` — read them, since a defect they already named and that is still present is the most useful thing you can report.

Report findings as a markdown table with columns: `#`, `Severity` (critical/high/medium/low), `File:Line`, `Finding`, `Status`, `Reason/Ref`. Put `open` in Status and leave Reason/Ref empty. Order most severe first. Each finding must name a concrete failure: inputs or state, and the wrong output, exit code, or unreachable path that follows. Cite file:line for every claim. If you assert a tool or git behaviour, verify it — you may run read-only commands and may create scratch git repositories under /tmp to check behaviour. Do not write, edit, or create any file inside the worktree or the repository. Do not run `task release`, any release command, any push, or anything that contacts the network beyond reading the repository.

Return the table as your final message, nothing else before it except one line naming what you actually read and ran.
