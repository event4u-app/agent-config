# Findings: drain-continuity-writer-3-1
<!-- completion-review: v1 | reviewed: 2026-09-09 | scope: ffe5473d7c8226c1d651f5b659dae89a02ad9633fc3ebe0d142146a7adb9b4fd | diff: 1f8e05166d22c5c67da69b568daba27bba54a459 | reviewer: r2-fresh-subagent-drain-continuity-writer-3-1 | prompt_hash: 4b593d8e0c44889daa7f1c98f6365b6bb8d25fbfaa445a919e8280ecdc30ff56 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-continuity-writer-3-1"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-09 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 1f8e05166d22c5c67da69b568daba27bba54a459
  scope_hash: ffe5473d7c8226c1d651f5b659dae89a02ad9633fc3ebe0d142146a7adb9b4fd
  roadmap: agents/roadmaps/road-to-continuity-writer-activation.md
  roadmap_hash: 5aa524e5d8817169fe82e6de5d9ddef6195f405a3d6b3a331599331f15764b72
  ac_hash: a2d23fec4136c73cd496deda835e1486b10f3aa05b20ba557d302cdc468c1d1b
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-09T06:59:05Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hot_context_hook.ts:525 | P5 (duplicate invocation) is INERT whenever the session id lives only on the dispatcher envelope. The hook reads `payload.session_id ?? payload.sessionId` only, but `dispatch_hook.ts:488-496` sets the canonical `envelope.session_id` to `payload.session_id || process.env.AGENT_SESSION_ID || ""` — the env fallback exists precisely for hosts whose native payload carries no id. Empty id makes `claimSessionOnce` return `{ok:true}` without latching, so the block is injected on every `session_start` of that session. PROVEN: two dispatcher runs, `--platform claude`, payload `{"source":"startup"}` with `AGENT_SESSION_ID=probe-sess-1` — the `<memory-index>` block is emitted BOTH times and `agents/runtime/state/session-index-latch/` is never created. Every sibling concern reads the envelope FIRST and documents why (`edit_shape_hook.ts:192-193`, `end_review_nudge_hook.ts:640-643,659`: "present on every dispatcher path ... falls back to the platform payload for a host that carries no session_id at all"); this change inverts that order and drops the envelope field. Both new tests put `session_id` in the payload (`session_index_trust_e2e.test.ts:107`), so the covered path is the only one that works. | open |  |
| 2 | high | src/scripts/session_memory_index.ts:64-77 | P4 does NOT choose which rows survive the cap, and the docblock asserts that it does. `session_index_rows` calls `retrieve_v1(..., SESSION_INDEX_ROW_CAP)`; `retrieve_v1`->`retrieve`->`_retrieve_internal` truncates at `memory_lookup.ts:995` (`.slice(0, limit)`) BEFORE `orderRows` runs, and with `keys: []` every curated hit scores an identical 0.1 (`_score` at `memory_lookup.ts:543-545`), so `_sortMerged` leaves them in STORE order. `orderRows` then only re-sorts the 30 survivors retrieval already picked. `capRows` clamps any cap to `<= SESSION_INDEX_ROW_CAP`, so there is no cap value for which the claim holds. PROVEN: a 40-entry `product-rules.yml` whose 5 cheapest entries sit last in store order yields `rows returned: 30 / cheap rows present: 0 / expensive rows present: 30` (first row `expensive-001:127`). The comment at :65-68 ("the declared order below chooses which rows survive truncation instead of the retrieval layer choosing for it ... order first, then cap, and never the reverse") and `_lib/session_index_trust.ts:224-234` ("so a fixed cap carries the most entries it can" / "which 30 of 200 entries reach the model is whatever the filesystem happened to yield") are both falsified for any corpus above the cap. Fix shape: ask retrieval for an unbounded/large ceiling, then order, then cap. Both P4 tests use 2-entry corpora and cannot see this. | open |  |
| 3 | high | src/scripts/_lib/session_index_trust.ts:183-195 | P1/P2 canonicalize only the memory ROOT, so a symlinked curated FILE (or type directory) INSIDE `agents/memory` serves another repository's memory — the exact threat the module docblock names at :16-27. `verifyMemoryRoot` realpaths and containment-checks `agents/memory` itself and nothing below it; `newestCuratedMtimeMs` uses `statSync` (follows links) and `memory_lookup._iter_curated_entries` reads through them. PROVEN: victim workspace with `agents/memory/product-rules.yml -> /tmp/donor2/agents/memory/donor-rules.yml`; the dispatcher emits `- DONOR-SECRET-001 - donor tree memory - ~51 tok` into the victim's block. The "WHAT IT DOES NOT CLAIM" carve-out at :29-36 covers wrong or hostile content AT THE CORRECT PATH INSIDE THE CORRECT WORKSPACE — it does not cover another tree's bytes, which is what this is. Both P1 tests symlink the root itself (`session_index_trust.test.ts:61-76`, `session_index_trust_e2e.test.ts:152-162`), so the one-level-deeper variant is untested. Mitigation on the shipped path today: `memory.session_index` defaults off. | open |  |
| 4 | medium | src/scripts/_lib/session_index_trust.ts:122-140 | P3 (freshness) silently no-ops on store layouts `memory_lookup` fully supports. `newestCuratedMtimeMs` is NON-RECURSIVE and reads only `*.yml` directly at the root, but `_iter_curated_entries` (`memory_lookup.ts:346-383`) also reads `agents/memory/<type>/**/*.yml` and `_iter_intake_entries` reads `agents/memory/intake/` — and intake hits do reach the index. For those layouts `newestSourceMs` is 0, which :118-120 treats as "nothing to be stale about", so BOTH directions of P3 (the 400-day abandonment bound AND the future-mtime skew/tamper refusal) are skipped while the block is still emitted. PROVEN: a workspace whose only curated source is `agents/memory/product-rules/old.yml` with an mtime of 2023-09-10 (~1095d, well past `MAX_ROOT_AGE_DAYS = 400`) is served: `- ancient-001 - ancient rule - ~46 tok`. Every P3 fixture writes a root-level `product-rules.yml`, so the gap is invisible to the tests. This repo's own `agents/memory/` uses the root-level layout, so P3 holds here — but `agents/memory/intake/` is already unreadable to it. | open |  |
| 5 | medium | src/scripts/hot_context_hook.ts:444 | The session's single latch is claimed BEFORE the block is rendered, so a run that injects nothing still burns it. `claimSessionOnce` runs at :444 and `build_session_index_block` at :453 can still return `null` (empty corpus, or any render failure). PROVEN: armed workspace with an empty `agents/memory`, session `burn-probe` -> no block, yet `agents/runtime/state/session-index-latch/burn-probe.json` is written; after curating `product-rules.yml`, a second `session_start` for the SAME session emits nothing. Reachable in normal use: a fresh clone or a workspace whose memory is curated during the session (`memory:add`), then a resume/reconnect/compaction `session_start`. This is the same defect class the e2e case at :164-187 ("a refused root does NOT burn the session latch") was written to prevent, left standing one branch later — the claim should be made after a non-null render, or released when the render yields nothing. The `curated: false` e2e case uses its own session id, so it cannot observe the burn. | open |  |
| 6 | low | src/scripts/session_memory_index.ts:110-114 | The trust check is OPT-IN on the contract's own primary render function: `build_session_index_block` runs `session_index_trust` only `if (workspaceRoot !== undefined)`, so the default call renders from whatever `MEMORY_ROOT` resolves to with P1-P3 unchecked. `session_index_cost()` in the same file (:132-134) is exactly that call and reads memory from an unverified root. The docblock says the seam is kept "ONLY for the test seam", but nothing enforces that, and a future caller that omits the argument gets pre-contract behaviour silently. A required root (with an explicit, named test-only escape) would make the bypass impossible rather than merely discouraged. | open |  |
| 7 | low | src/scripts/hot_context_hook.ts:430 | The trust verdict is computed twice per armed `session_start`: once at :430 and again inside `build_session_index_block` at :453 (`session_memory_index.ts:112`). That is two `realpathSync` passes plus a second `readdirSync` + `statSync` sweep of the memory root on the injection hot path. The code and the e2e docstring (:143-151, :91-99) already record that the hook's copy enforces nothing (the builder re-checks) and that its stderr diagnostic reaches nobody (the dispatcher does not forward a concern's stderr) — so the only thing the duplicate call buys is the ORDERING the comment at :440-445 describes. Passing the verdict (or its `memoryRoot`) down to the builder would keep the ordering and drop the second pass. | open |  |
