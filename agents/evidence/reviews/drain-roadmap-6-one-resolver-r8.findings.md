# Findings: drain-roadmap-6-one-resolver-r8
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: 4db51c80fd551a706762231883f4be97cb47fd8463069779e6e907b76dbde761 | diff: 4782ce3c8eb532a6b6422efe5ba44fbd31885f41 | reviewer: r2-fresh-subagent-drain-roadmap-6-one-resolver-r8 | prompt_hash: e31c5cbc824b400b0365fe8f7ecd11f4d22c46d89b438f4c4a5bf49be70e0a90 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-roadmap-6-one-resolver-r8"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 4782ce3c8eb532a6b6422efe5ba44fbd31885f41
  scope_hash: 4db51c80fd551a706762231883f4be97cb47fd8463069779e6e907b76dbde761
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: fe6a3459ac8f52e835ff0668662c2e8a1e0e731a840a766cac996fe76646f94d
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T14:02:57Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | low | agents/evidence/drain-run-summary.md:84 | NON-BLOCKING, and it is the incomplete half of round 7's own repair. Round 7 corrected the section heading at :81 to "seven review rounds, 33 findings" and appended a seventh table row at :95, but left the sentence that INTRODUCES that table reading "Six fresh-subagent R2 rounds — none of whose prompts this session authored — killed four implementations:". A colon-introduced count of six therefore heads a seven-row table, two lines under a heading that says seven. Seven fresh-subagent R2 rounds exist as committed artefacts: r1 through r7 each carry `reviewer: r2-fresh-subagent-...` and `"context_relation":"fresh"`, and each is one row of that table. The same stale count is back-referenced at :105 ("The second recurred in **five** of the six rounds"), where the instance count five is correct and only the denominator is stale. No charitable reading recovers six: four implementations died in rounds 1, 2, 3 and 5, so the span that covers the killings is five rounds, not six. Direction is worth naming — it understates, which flatters the process, the same direction as r7 #1. Seventh appearance of the closure-record-accuracy class (r1 #6, r2 #5, r3 #5, r4 #4, r6 #1, r7 #1 and #2), and the first instance caused by a prior round’s fix rather than by drift. Opens no hole; the fix is one word in two sentences. | accepted-risk | probe: findings-artefact census r1..r7 = 7 files, every one `reviewer: r2-fresh-subagent-*` with `context_relation: fresh`; mechanical severity-row count 6+5+5+4+6+4+3 = **33**, and the disposition split 24 `fixed` + 9 `accepted-risk` = 33 independently, so the heading’s own two numbers DO reproduce and only :84 and :105 do not; code confirmed unchanged (`git diff --quiet f4860a31 HEAD` on both source files, exit 0), vitest 80/80 green, `tsc --noEmit -p tsconfig.json` exit 0, `eslint` on both files exit 0 — DISPOSITION: recorded, not repaired, under the rule set before round 5 ran: fix what blocks, record what does not. Both are documentation-only and open no hole. The reviewer names the reason the alternative does not terminate: every round-N record is blind to round N+1, so a count corrected in round 8 is stale again in round 9. Nine prior non-blocking findings took the same disposition. |
| 2 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:372 | NON-BLOCKING, stale counterfactual inside the very paragraph round 7 updated. :369 now opens "Seven rounds, 33 findings" and :372–:375 still argues "A sixth round would be measuring a guard that has already had four defect classes removed from it". A sixth round is documented in this same record at :338 ("**ROUND 6 — a bounded confirmation, and the blocking finding is closed.**"), and a seventh is tabulated in the sibling summary at :95, so the stated reason for stopping reasons against an event that has already happened twice. The round-6 reviewer note quoted at :366–:367 uses the same phrase correctly and is in quotation marks, because it was written while a sixth round was still hypothetical; the sentence at :372 is the roadmap’s own unattributed prose. Pre-existing rather than introduced by round 7 — the prior revision read "Six rounds, 25 findings ... A sixth round would be", already self-contradictory — so it has now survived two count corrections without being re-read. Nothing behavioural rides on it, and every other measurement in the surrounding block reproduces. | accepted-risk | probe: roadmap :338 ROUND 6 heading present, :369 "Seven rounds, 33 findings", :372 "A sixth round would be"; surrounding block re-measured and green — `find src -name '*.tsx' -not -name '*.test.tsx'` = **28** against `SCAN_ROOT = 'src'` (the 11 further non-test `.tsx` all sit under `tests/` fixtures, outside the scanned root), 4 sensitivity arms, 5 conforming resolver spellings, 5 denial assertions across the polarity and round-1 regression blocks, `check_portability.ts:741` holds a regex literal containing a backtick, the round-3 trigger, roadmap census 77 steps with 4 closed and 0 deferred, all six PRs in the queue table live-verified MERGED — DISPOSITION: recorded, not repaired, under the rule set before round 5 ran: fix what blocks, record what does not. Both are documentation-only and open no hole. The reviewer names the reason the alternative does not terminate: every round-N record is blind to round N+1, so a count corrected in round 8 is stale again in round 9. Nine prior non-blocking findings took the same disposition. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
