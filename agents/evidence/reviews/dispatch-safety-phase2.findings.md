# Findings: dispatch-safety-phase2
<!-- completion-review: v1 | reviewed: 2026-08-11 | scope: 71e38946075bc95d678ab5d9e45f51885b5d6869147d8abadd4a30a6255d2b18 | diff: d6905af2385948a7bb44dbdb3a9025834b91afb2 | reviewer: r2-fresh-subagent-dispatch-safety-phase2 | prompt_hash: 7ef1311d5c593a331fe0b2c1bdd7cd45f3aa880fd4ac366e5325eb99498bd848 -->

<!-- context-manifest: v1
inputs:
  diff_sha: d6905af2385948a7bb44dbdb3a9025834b91afb2
  scope_hash: 71e38946075bc95d678ab5d9e45f51885b5d6869147d8abadd4a30a6255d2b18
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-b-dispatch-safety.md
  roadmap_hash: 0ec1dc41ecf2206ca1b5a585668d2fd282bf67e373103338592fa2d4b8e75a72
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-11T23:45:45Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/agent-src/templates/scripts/work_engine/hooks/builtin/confirmation.ts:195 | `listPending` crashes on a structurally-partial record. `isStaged` (:102-112) validates `token`/`gate_id`/`action`/`object` but **not** `staged_at`, so a record that is valid JSON yet missing `staged_at` passes the guard, and the sort `a.staged_at.localeCompare(...)` throws `TypeError: Cannot read properties of undefined (reading 'localeCompare')` once two or more records are present. `hooks_status.ts:310` calls `listPending` outside any try/catch, so `agent-config hooks:status --pending` dies with a stack trace instead of an enumeration. This is exactly the guarantee `confirmation_exactly_once.test.ts:114` claims ("a malformed record is skipped rather than crashing the enumeration") — that test only covers unparseable JSON, which is the case the guard does handle, so the assertion reads as robustness the code does not have. The type guard is also unsound: it returns `v is StagedAction` while permitting `staged_at: undefined` and `phase: undefined`. | open | |
| 2 | high | src/agent-src/templates/scripts/work_engine/hooks/builtin/confirmation.ts:98 | The token is interpolated straight into a filesystem path with no validation — `recordFile` does `path.join(dir, token + '.json')`. `stageAction(root, input, { token: '../../escaped' })` writes the record outside the store. The consumption side is worse than the staging side: `confirmAction` (:161-172) turns a caller-supplied token into an unguarded `rename(2)` of an arbitrary `<token>.json` into `consumed/`, so a token the human retypes from a prompt — the module's own design ("surface it to the human so the confirmation can reference exactly this staging") — is an arbitrary-path move. Both directions are latent today because no shipped path calls either function, but the module is exported, mirrored into `dist/agent-src/templates/`, and consumed across the package boundary by `hooks_status.ts`, so the surface ships. A safety primitive whose only destructive verb accepts an unvalidated path fragment inverts its own purpose. | open | |
| 3 | medium | src/agent-src/templates/scripts/work_engine/hooks/builtin/confirmation.ts:168 | `confirmAction`'s bare `catch` collapses every rename failure into the ENOENT reading, so a token that **is** still pending is reported as `unknown` — "No such token was ever staged here". With `consumed/<token>.json` occupied (any EEXIST/ENOTEMPTY/EACCES/EXDEV class works the same way), the call returns `status: 'unknown'` while `pending/<token>.json` is still on disk. The module header and the roadmap both make the `unknown` vs `already_executed` distinction load-bearing ("one is a typo or a wrong project root, the other a real second approval"), but a third case — the store is broken and the action is still held — is silently folded into the first, telling the operator their correct token was a typo while the action stays pending forever. | open | |
| 4 | medium | src/agent-src/templates/scripts/work_engine/hooks/builtin/confirmation.ts:166 | A malformed pending record is consumed and reported as executable. `readAt(from)` returns `null` for unparseable content, the rename still succeeds, and the fallback `record ?? readAt(to)` re-reads the same unparseable bytes, so the call returns `{ status: 'executed', record: null }`. The caller's contract is "execute on `executed` and on nothing else", so this instructs execution of an action whose `object` cannot be named, which is precisely what the module's own `StageInput` doc cites `non-destructive-by-default` to forbid ("an approval that does not name the exact object"). It also contradicts the declared shape of `ConfirmOutcome.record` ("`null` on `unknown`"). | open | |
| 5 | medium | src/agent-src/templates/scripts/work_engine/hooks/builtin/decision_gate.ts:151 | Staging is performed as a side effect of a surface-*formatting* method, and mints a fresh token on every halt. `_surface_with_confirmation` is documented and named as the numbered-option surface builder, but calls the injected stager (:156) — an unconditional filesystem write — while building a string array. Two consequences: (a) any caller that re-renders the surface stages a second, unreferenced hold; (b) `_halt` runs on every `AFTER_STEP` the gate blocks, and there is no dedup on `(gate_id, phase)`, so a repeatedly-blocked advance accumulates one pending record per attempt and `hooks:status --pending` lists N identical holds for one held action. `confirmation.ts` ships no prune, TTL, or cap for either `pending/` or `consumed/` — unlike the `self_repair_store` placement it cites as precedent, whose `agents/runtime/` neighbours are retention-managed. | open | |
| 6 | medium | tests/scripts/requires_confirmation_contract.test.ts:77 | The declared test contract is symmetric; the coverage is not. The file docstring states "both schemas accept a boolean, both reject a non-boolean … and neither has stopped rejecting unknown keys", and roadmap step 2.1 records `additionalProperties: false` as "pinned as its own spec rather than assumed" for both surfaces. Only the skill surface is actually exercised. The command half asserts only that `properties.requires_confirmation` exists with `type: boolean` — a shape read of the JSON, never a `validate()` call — so a command document carrying `requires_confirmation: "true"`, or the misspelling `require_confirmation`, is untested on the surface whose gate the step claims to have pinned. | open | |
| 7 | low | src/agent-src/templates/scripts/work_engine/hooks/builtin/confirmation.ts:144 | `stageAction` writes with plain `fs.writeFileSync`, so staging a token that already exists in `pending/` silently overwrites the earlier record and the previously held action disappears with no trace. The module argues at length that consumption must be an atomic `rename(2)` rather than a read-check-write because of the window between read and write; the same argument applies to creation and was not applied — an exclusive create (`flag: 'wx'`) would make a token collision a visible error instead of a silent loss. `randomUUID` makes an accidental collision negligible, but the API accepts caller-supplied tokens (`StageOptions.token`) and a re-stage after a wrong-root confirm is a realistic operator path. | open | |
| 8 | low | src/scripts/hooks_status.ts:309 | `--pending` returns before `--strict` is honoured. The early-return branch exits `0` unconditionally, so `hooks:status --pending --strict` — a plausible CI invocation, since both flags are documented on the same verb in `_dispatch.bash:236-239` — reports green regardless of missing hook bridges, silently discarding the strict gate rather than rejecting the flag combination. | open | |
