# Findings: feat-solution-minimalism-complexity-endpoint
<!-- completion-review: v1 | reviewed: 2026-08-16 | scope: e07a3f507a86bad02561079f18f2e2ed1c9ab3d4df0627a6aec25e7820904524 | diff: d6f5d5929596ce813a4920bc78dfc95525adba0d | reviewer: r2-fresh-subagent-feat-solution-minimalism-complexity-endpoint | prompt_hash: 01139735e0761157db6115b75c832d8fecf7c5c4f5be990f09b756202e9169a1 -->

<!-- context-manifest: v1
inputs:
  diff_sha: d6f5d5929596ce813a4920bc78dfc95525adba0d
  scope_hash: e07a3f507a86bad02561079f18f2e2ed1c9ab3d4df0627a6aec25e7820904524
  roadmap: agents/roadmaps/road-to-solution-minimalism.md
  roadmap_hash: 5a103ae42eb278f74e3d4109dc10a2c4ec0e06676d83b53d0beecbf4a1c26378
  ac_hash: 0288b531fe3efa314748a8bffda7c66675fec33fa750739e74d4d389f4a83e0e
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-16T18:55:05Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/bench_ab_complexity.ts:282-290 | `scoreIfInterior` walks an `else if` interior at `nesting + 1`, one level deeper than the equivalent `else { ... }` and than the PHP `else_if_clause` path, which walks at `nesting`. Verified by probe: `function f(a,b,c){ if(a){ if(b){} } else if(c){ if(b){} } }` scores **7**; the semantically identical `else { if(b){} }` form scores **6**; the PHP `elseif` twin scores **6**; Campbell's value is 6 (if 1, nested if 2, else-if 1 flat, nested if 2). The code contradicts its own comment on line 248 ("score its condition and body at the SAME nesting level"). Consequence: the metric is both language-asymmetric and code-shape-dependent, so the module header's defence ("neither favours a treatment arm, because the metric is only ever compared against itself across arms") does not hold — the arms are compared precisely because they emit different code shapes, and an `else if` chain with any nested body is inflated on JS/TS only. The existing guard test at tests/scripts/_lib_bench_ab_complexity.test.ts:76-84 uses **empty** else-if bodies, so it pins the flat +1 and is structurally unable to see this. | open | |
| 2 | high | src/scripts/bench_ab_v2_stats.ts:396-404 | `safety_tier_pass` has no producer anywhere in `src/`. The only occurrences in the tree are the two reads here and a synthetic injection in tests/scripts/bench_ab_v2_stats.test.ts:530. T4 is therefore unmeasured on every real report, and because the missing-endpoint check at src/scripts/bench_ab_v2_stats.ts:766-779 runs BEFORE the `cx_rose` branch, `size_claim_verdict` returns `INCONCLUSIVE` on production data and can never reach `REFUSED-GOLFING`, `NO-SIZE-WIN`, or `PASS`. The roadmap closes an acceptance criterion on exactly the verdict that cannot occur (acceptance-criteria.md:51-53, "Lines win significantly and the verdict is still `REFUSED-GOLFING`"). internal/bench/REPRODUCE-ab-v2.md states the T4 gap honestly in the same diff; the roadmap AC does not, so two documents in one change disagree about what the scorer will report. The failure direction is conservative, so this is a claim-versus-reachability defect rather than a false-pass hole. | open | |
| 3 | medium | src/scripts/bench_ab_v2_stats.ts:380-394 | T1 and T2 are collected over **different pair samples** (each endpoint is gated on both sides carrying its own key), and `size_claim_verdict` compares only the `measured` flags — never `size.n_pairs` against `complexity.n_pairs`. A comparison with `complexity.n_pairs = 1` and `size.n_pairs = 90` still authorises `PASS`, so the anti-golfing guard can police a strict subset of the sample the size win is claimed on. Two concrete mechanisms put pairs in that gap: a trial whose changed files are all non-code (every `.md`, `.json`, `.yaml` change lands in `unsupported_files`), and a trial whose changed source fails to yield a function unit — verified by probe that a syntactically broken `.js` parses to zero functions, so `median` is null while `added_lines` still counts. That is "half a pair" at the sample level, which is the failure the same function's docstring says it exists to forbid. | open | |
| 4 | medium | src/scripts/_lib/bench_ab_complexity.ts:150-159 | PHP `match` is absent from `NESTING_NODES`, so a `match` expression scores **0** while its `switch` twin scores **1** — verified by probe on a three-arm `match` versus a two-arm `switch`. Rewriting a `switch` as a `match` is the idiomatic modern-PHP smaller form a minimalism arm is most likely to emit, and it reduces measured complexity to zero regardless of how many arms it carries. The module's stated doctrine (lines 55-59: a zero would read as "this file is simple", which is a different claim from "we cannot measure it") is applied at file granularity only; an unmodelled *construct* inside a supported file is silently scored zero, and it is scored zero in exactly the golfing direction the endpoint exists to detect. | open | |
| 5 | medium | src/scripts/_lib/bench_ab_complexity.ts:198-214 | The `binaryOperator` fallback is simultaneously unreachable and incorrect, and nothing tests it. Verified by probe: `childForFieldName('operator')` resolves on all three grammars, so the loop never runs; and `isNamed` compares node wrappers with `===` while the API returns a fresh wrapper per call — `bin.child(0) === bin.child(0)` is `false` — so `isNamed` can never return `true` and the loop's second guard is always satisfied. If the documented failure mode it exists for ever occurred, `(a === b) && c` would yield `'parenthesized_expression'` as the operator (verified), the logical sequence would be silently dropped, and the under-count would be invisible because no test exercises the path. The first guard (`c.type !== c.text`) additionally drops case-differing keyword operators such as PHP `AND`. | open | |
| 6 | medium | acceptance-criteria.md:49-53 | The closure evidence does not match any code in the diff. It claims "the same golfed pair the unit suite scores — a flat three-branch `classify` … 1 added line and complexity 3 against the original's 1", but the two suites define **different** `FLAT` constants: tests/scripts/_lib_bench_ab_complexity.test.ts:144 is three-branch and scores **2**, while tests/scripts/bench_ab_v2_complexity.test.ts:43 is **two**-branch and scores **1** (both verified by probe). The quoted 3-versus-1 is the two-branch pair, whose "twin" is not behaviourally equivalent — the golfed form handles `n === 0` and the flat form does not — so part of that delta is an extra branch rather than golfing. And the *benchmark* scorer test that actually produces `REFUSED-GOLFING` uses neither fixture: it hardcodes added lines 10 versus 30 and complexity 9 versus 3 at tests/scripts/bench_ab_v2_stats.test.ts:559-566. | open | |
| 7 | medium | acceptance-criteria.md:32-38 | The "Phase 3 either reports from the full tier" criterion was re-annotated to `phase3-harness-deltas-9-10`, but its body still reads "Spend is the user's grant; the metric pair additionally needs a complexity endpoint that does not exist yet." Both halves are false as of this diff — the spend blocker reads `Status: resolved` since 2026-08-14 by this change's own correction note, and the complexity endpoint landed in this change. Every sibling stale claim in the same diff was struck through and visibly superseded; this one was left standing, which is verbatim the defect the diff's own "Why this entry exists at all" paragraph says re-costs every feasibility screen. | open | |
| 8 | low | agents/roadmaps/road-to-solution-minimalism.md:996 | The new `### blocker: phase3-harness-deltas-9-10` heading (line 964) was inserted between the spend blocker's `Resolved when` (line 953) and its closing paragraph "Firing a paid external run without **that grant** is a Hard-Floor action" (line 996). That sentence now renders under the *new*, open blocker, where "that grant" has no antecedent, while the resolved spend blocker loses its Hard-Floor close. A reader screening `phase3-harness-deltas-9-10` can read the Hard-Floor line as still gating the sweep, which is the opposite of what the correction note two sections up establishes. | open | |
| 9 | low | tests/scripts/_lib_bench_ab_complexity.test.ts:187-193 | The test named "a workspace whose changed files are all **unsupported** yields median null" passes `/nonexistent-root`, so `a.py` is never read and lands in `missing_files` rather than `unsupported_files` — which is what the assertions actually check. `unsupported_files` is never asserted non-empty anywhere in the suite, so the branch behind the module's headline honesty claim (src/scripts/_lib/bench_ab_complexity.ts:55-59, "UNSUPPORTED FILES ARE REPORTED, NEVER SCORED ZERO") is untested, and the name of the test that appears to cover it hides that. | open | |
| 10 | low | src/scripts/bench_ab_v2_complexity.ts:152-158 | The comment opens "Only set the complexity key when something was scored" and the next statement sets it unconditionally, including `null` — the comment's own second sentence then concedes the opposite, so the block contradicts itself in three lines. Separately, `main` at lines 210-212 rewrites the entire report through `JSON.stringify(payload, null, 2)` whenever `--write` is passed, even when every trial was skipped and nothing changed, which reformats the pinned artefact the file header (lines 25-27) says must never be rewritten silently. | open | |
