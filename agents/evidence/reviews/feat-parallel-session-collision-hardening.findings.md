# Findings: feat-parallel-session-collision-hardening
<!-- completion-review: v1 | reviewed: 2026-08-12 | scope: d996e8e86a6e21a7cad66963f70219e59fe8cb96bca0bd1adceeea7ad2565c4b | diff: e7352ccedbd9cd996220db0293faf6d42a67529e | reviewer: r2-fresh-subagent-feat-parallel-session-collision-hardening | prompt_hash: d996e8e86a6e21a7cad66963f70219e59fe8cb96bca0bd1adceeea7ad2565c4b -->

<!-- context-manifest: v1
inputs:
  diff_sha: e7352ccedbd9cd996220db0293faf6d42a67529e
  scope_hash: d996e8e86a6e21a7cad66963f70219e59fe8cb96bca0bd1adceeea7ad2565c4b
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-12T00:52:07Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/session_register_hook.ts:192 | `claim_is_stale` decides whether a PEER's claim is live by `existsSync` in the CALLER's working tree, but worktrees share one register while sitting on different commits. A long-lived worktree branched before the roadmap file was added (or whose checkout is simply unsynced) sees a genuinely live peer claim as stale, and both consumers then treat it as no claim at all — `claim_conflicts` filters on `!claim_is_stale` (sessions_cli.ts:298) and `foreign_sessions_block` rewrites the slug to `null` (session_register_hook.ts:283, 288). The refusal and the duplicate-work warning are silently disabled in exactly the multi-worktree case the change exists for, with no signal that the staleness verdict was locally derived. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 2 | medium | src/scripts/sessions_cli.ts:288 | `if (sid !== null && r.session_id === sid) continue;` — when `env_session_id()` returns `null` the self-exclusion is skipped entirely, so the caller's OWN register record becomes a `session` conflict. That state is reachable on every host that supplies `session_id` in the hook envelope but exports no shell variable (only `CLAUDE_CODE_SESSION_ID` / `AGENT_CONFIG_SESSION_ID` are read, sessions_cli.ts:249): the first claim lands in the legacy file, the heartbeat lifts it into the own record, and every later `sessions:claim <same-slug>` exits 1 citing the session itself. Re-claiming is not idempotent precisely on the fallback path the docs describe as degrading gracefully. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 3 | medium | src/scripts/session_register_hook.ts:101 | `roadmap_claim_rel` mints one claim file per session id (`roadmap-claim-<id>.json`), and nothing in this diff ever removes them: `--release` is the only deleter (sessions_cli.ts:329) and the `session_end` path deletes the register record only (session_register_hook.ts:365-369). With UUID session ids that is a new file per session, retained forever, and a file keyed to a dead id is never read again. The register library treats exactly this as a defect worth code ("leaving it would make the register grow without bound", _lib/session_register.ts read_live_records doc) and no equivalent prune exists for the claim files the change introduces. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 4 | medium | src/scripts/sessions_cli.ts:296 | The branch axis of the refusal is a substring match on a 2-token slug tail, yet it produces a non-zero exit that requires `--force` — contradicting its own docstring ("The name match is a heuristic and says so — it is a reason to look, not a verdict", sessions_cli.ts:279-281). Two concrete false positives: (a) only the CALLER's worktree is excluded (sessions_cli.ts:143), not the caller's own branches elsewhere, so the one-worktree-per-roadmap flow refuses a session against its own `feat/<...>-dispatch-safety` checkout; (b) a date-suffixed slug such as `road-to-inbox-harvest-2026-08` yields the tail `2026-08`, which passes the `length >= 4` guard and matches every branch containing that string. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 5 | medium | src/scripts/session_register_hook.ts:304 | The new roadmap-collision block is added to a renderer the same branch argues cannot fire it: the block is emitted on `session_start` only (session_register_hook.ts:390) and `claim_conflicts`'s docstring states that there this session's slug "is `null` by construction … so a roadmap-collision warning in that renderer can therefore never fire on the session doing the picking" (sessions_cli.ts:269-274). The one path that does make `my_slug` non-null at session start is a leftover legacy claim inherited from a previous session in the same checkout — the state this branch elsewhere calls the measured defect. Two artefacts in one branch assert opposite things about the same code path, and ~14 lines plus a `Collision` import ride on the losing one. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 6 | medium | src/scripts/session_register_hook.ts:167 | The "a peer's claim can no longer be inherited" guard is unreachable for files this codebase writes. `_read_claim_file` keeps `session_id` only when it is a non-empty string (session_register_hook.ts:134), and `cmd_claim` writes the legacy path only when `sid === null`, serialising `"session_id": null` (sessions_cli.ts:324, 366) — so `legacy.session_id !== undefined` is never true and a legacy claim is always accepted as "mine". The measured defect (two sessions, one checkout, host with no id) is therefore unchanged, which the prose admits ("the legacy shared path is still written and still read"), while tests/scripts/session_register.test.ts:1022-1031 hand-writes `{slug, session_id:'peer-session'}` and labels that assertion "the measured defect" — a green test over a state production cannot produce. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 7 | low | src/scripts/sessions_cli.ts:174 | The `records.length === 0 && held.length === 0` branch is redundant: the following `records.length === 0` branch emits the identical line and `writeHeldBranches([])` returns immediately (sessions_cli.ts:220). Two branches, one behaviour. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 8 | low | src/scripts/sessions_cli.ts:160 | `other_worktree_branches(root)` runs unconditionally before the `as_json` early return, so `sessions:list --json` (without `--branches`) pays two `git` subprocesses — each with a 10 s timeout — and discards the result. The JSON form is the scripted, latency-sensitive path. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 9 | low | src/scripts/sessions_cli.ts:64 | `usage()` still advertises `sessions:list [--json]` only, while the module docstring (sessions_cli.ts:20), `src/scripts/_dispatch.bash:216-221` and `src/cli/registry.ts:69` all document `--branches`. A user who reaches the usage text cannot discover the flag. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 10 | low | src/scripts/sessions_cli.ts:329 | `--release` unlinks the shared legacy claim file regardless of who wrote it, so a session that has an id destroys a peer's claim in a mixed-host checkout; that peer's next heartbeat then reports "no roadmap claimed" with no notice to either side. Consistent with `read_claimed_slug` treating an id-less legacy claim as "mine", but it is still a cross-session delete performed silently. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 11 | low | src/scripts/sessions_cli.ts:288 | Session ids are compared raw here while the library compares them normalised (`safe_stem(r.session_id) !== own`, _lib/session_register.ts:367). Any host whose exported id differs from the envelope id only in a character `safe_stem` rewrites makes the self-exclusion fail silently — the same outcome as finding 2. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 12 | low | src/scripts/sessions_cli.ts:117 | When `git branch --no-merged origin/main` fails (no `origin/main`) the filter is dropped and every foreign worktree is reported, yet the human output still asserts "N unmerged branch(es) checked out in another worktree" (sessions_cli.ts:224). The degraded state is invisible to the reader, and an un-fetched `origin/main` produces the same overstatement without any error at all — in a tree the comment itself describes as "30+ here, all long merged". | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 13 | low | src/scripts/sessions_cli.ts:363 | `cmd_claim` never checks that the slug it is about to write names an open roadmap, so a typo or an already-archived slug is accepted and then rendered `← STALE` on every subsequent screen. The branch's own thesis is that stale slugs are read as "taken"; the cheapest guard against creating one sits at the only write site and is absent. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 14 | low | src/scripts/session_register_hook.ts:290 | The renderer keeps only the first hit (`collisions.find(...)`), so with several peers on one roadmap the block names one — while `classify_collisions` deliberately returns all of them and tests/scripts/session_register.test.ts:1005-1012 pins that ("reports every peer on the same roadmap, not just the first"). The tested capability has no consumer, and under the multi-record state described in finding 3 the warning understates the peer count. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
| 15 | low | src/scripts/sessions_cli.ts:219 | `writeHeldBranches` is camelCase in a file whose every other function is snake_case (`other_worktree_branches`, `cmd_list`, `env_session_id`, `claim_conflicts`); the JSON key `otherWorktreeBranches` (sessions_cli.ts:168) makes the same switch in the emitted contract, where it is no longer cosmetic. | fixed | all 15 repaired in 871c00a62 + the PR-comment pass |
