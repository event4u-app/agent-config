# Findings: typed-grants-platform-anchor
<!-- completion-review: v1 | reviewed: 2026-09-10 | scope: 2692d657f367f4cb262f1a692ed276b10c0c3969c66f1f49f5791e8a2b9ed816 | diff: 2ea42016ea0776da426f4218f2350fc2af6f4ac2 | reviewer: r2-fresh-subagent-typed-grants-platform-anchor | prompt_hash: 62e788b730af4303bfadcec55fba806e45a2e78d50a976148d1b2e2343c14422 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-typed-grants-platform-anchor"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-10 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 2ea42016ea0776da426f4218f2350fc2af6f4ac2
  scope_hash: 2692d657f367f4cb262f1a692ed276b10c0c3969c66f1f49f5791e8a2b9ed816
  roadmap: agents/roadmaps/road-to-typed-grants-that-persist.md
  roadmap_hash: c794908ed635d617dfb08a3b1c75178ee683d26355bb4bb18e768b2f3b99bba6
  ac_hash: 41b7b784a462de3c07c67dc8ece3c0c82e4d53bcf1f6855152c7521afd993856
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-10T06:30:03Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | critical | src/scripts/_lib/platform_anchor.ts:109,382-388 | `NON_NEGOTIABLE_FLOOR` guards the six threshold fields but NOT the three SELECTOR fields (`enforcement`, `target`, `covers_default_branch`) that decide WHICH rulesets `effectiveProtection` reads. A ratified policy-only edit therefore neutralises the whole check without lowering any floored value — defeating the invariant `enforceFloor`'s own docstring states ("even a ratified edit cannot go below these values"). Probed live against this branch: `enforcement: "evaluate"` makes a NON-ENFORCING (dry-run) ruleset read as full protection -> verdict `compliant`, findings `[]`; `covers_default_branch: false` skips the ref filter at :388 so a `refs/heads/release`-only ruleset is credited to `main` -> `compliant` while `main` carries no ruleset at all. Both are exactly the "policy-only edit makes itself green" attack the floor was commissioned to stop, taking the selector door instead of the threshold door. `required_review_thread_resolution` is unfloored too (lesser: it only drops one finding). | fixed | 217139b67 — `enforcement`, `target`, `covers_default_branch` and `required_review_thread_resolution` added to `NON_NEGOTIABLE_FLOOR`; both probed cases now refused, each with its own regression test |
| 2 | critical | src/scripts/_lib/platform_anchor.ts:264,289-337,499 | `minimum_approving_reviews: Number(req[...])` has no `Number.isFinite` guard, so a non-numeric value yields NaN; `NaN < 1` is false at :296, the floor stays silent, and the policy is ACCEPTED. Downstream `eff.approvingReviews < policy.minimum_approving_reviews` at :499 is `0 < NaN` -> also false, so `approvals-below-minimum` NEVER FIRES. Probed: `"minimum_approving_reviews": "one"` is accepted with zero findings, and against the measured 0-approval ruleset the verdict drops `approvals-below-minimum` entirely (only `last-push-approval-missing` + `unconditional-bypass` remain). On a repo where those two are fixed — the exact state blocker `ratification-platform-anchor` demands — this reads `compliant` with zero required reviewers. Inconsistent with the same module's own ruleset-side parse at :396, which DOES use `Number.isFinite`. Same class for `String(req['enforcement'])`/`String(req['target'])`, which coerce any JSON value to a string that then silently matches nothing. | fixed | 217139b67 — non-finite, non-integer and out-of-range values refused outright instead of compared; the probed `"one"` case is a regression test |
| 3 | high | src/scripts/_lib/platform_anchor.ts:335-346 | `coversDefaultBranch` compares `conditions.ref_name.include`/`exclude` as EXACT strings, but GitHub ruleset ref conditions are fnmatch patterns. Probed: `include: ['~ALL']` + `exclude: ['refs/heads/m*']` returns `true` — the ruleset is credited with protecting `main` when GitHub excludes it (the fail-OPEN direction, and the only place in this evaluator that overstates protection); `include: ['refs/heads/**']` returns `false` — real protection understated. Neither polarity is covered by `tests/scripts/platform_anchor.test.ts`, whose ref-matching block tests only literal refs and the two `~` tokens, so the gap is invisible to the suite that claims "both polarities on every rule". | fixed | 217139b67 — `refPatternMatches` implements fnmatch (`**` crosses `/`, `*`/`?` stay in a segment, metacharacters escaped); both probed directions plus an empty-include case are regression tests |
| 4 | medium | src/scripts/check_platform_anchor.ts:83-84 | `gh api repos/{repo}/rulesets` is called without `--paginate`, so only the first page (default `per_page=30`) is read. A repository with more rulesets yields a SILENTLY TRUNCATED reading that is then treated as complete — precisely the partial verdict the same function's docstring says must never happen ("A single failed detail read makes the whole reading `null` rather than a partial one"). Truncation can drop an unconditional `bypass_actors` entry on a later page, i.e. it can overstate safety, not only understate it. | fixed | 217139b67 — `--paginate --slurp` on the rulesets listing, with page-array flattening; `--slurp` is required because bare `--paginate` emits one array per page |
| 5 | medium | src/scripts/_lib/platform_anchor.ts:104-107,233-238 | The council condition quoted VERBATIM three lines above `NON_NEGOTIABLE_FLOOR` requires the verifier to "Reject unknown fields, missing requirements, and policy values weaker than a ... floor". Only the second and third are implemented: `readAnchorPolicy` loops `POLICY_FIELDS` for presence and never inspects `Object.keys(req)` for extras. A misspelled or stale key in the expectation file passes unnoticed while the code documents itself as enforcing the opposite. | fixed | 217139b67 — `readAnchorPolicy` now refuses any key in `required` outside `POLICY_FIELDS`, with a regression test |
| 6 | medium | src/config/platform-anchor.json:3-4; src/scripts/check_platform_anchor.ts:254-265 | `schema_version` and `repository: "event4u-app/agent-config"` are declared in the expectation and read by NOTHING. `resolveRepo` takes `--repo` or whatever `origin` points at and never cross-checks it against the committed `repository`, so the gate can report `compliant` for a fork or a caller-named repository while the expectation it is quoting names another. `schema_version` being unread also means a future schema bump cannot be detected by the reader that would need to act on it. | fixed | 217139b67 — `checkAnchorIdentity` checks `schema_version` and cross-checks `repository` against the resolved repo; the gate refuses a mismatch, and the older tests were repointed at the real repo name, which is what surfaced it working |
| 7 | medium | src/scripts/check_platform_anchor.ts:1-297 | Unlike `check_kernel_edit_ratified.ts` — the gate this one is documented as the "second limb" of, which builds a `GateLedger`, `plan()`s every changed path and `complete`/`fail`/`outOfScope`es each one (:251-282) — this gate imports only `reportScanned` and does no per-target accounting. Its verdict is whole-diff, so a reader cannot see which gated path drove the result, and it is absent from the ledger surface its sibling participates in. | fixed | 217139b67 — `GateLedger` adopted: every changed path is planned, and completed / failed / out_of_scope on every exit path |
| 8 | medium | agents/roadmaps/road-to-typed-grants-that-persist.md:460-462 | The blocker's machine-checkable `Resolved when` is only `check_platform_anchor --files src/rules/commit-policy.md` exiting 0. Wiring the gate into CI is stated as "the second half" in PROSE (:409-415) but is not in the closing condition, so the blocker can be resolved with the gate still unwired and inert — the mechanical replacement stays prose. The deliberate unwired landing is sound and well argued; the gap is that nothing in the closing condition forces the wiring to ever follow. | fixed | 217139b67 — the blocker `Resolved when` gains a second limb requiring the gate in `ci-fast` and in a workflow step |
| 9 | low | src/scripts/check_platform_anchor.ts:248-256 | `--files` followed by an empty list (or immediately by another flag) leaves `files.length === 0`, which silently falls through to `gitChangedFiles(resolveBaseRef(...))`. A caller that computed an explicitly empty changed-set gets a git-derived list instead — possibly a gated one — so an explicit "nothing to check" and "work it out yourself" are indistinguishable at the CLI boundary. Untested: no test exercises `main()`'s argv parsing. | fixed | 217139b67 — keyed on the `--files` FLAG rather than the list length, so an explicit empty changed-set no longer falls through to a git-derived list |
| 10 | low | src/scripts/check_platform_anchor.ts:124 | `resolveBaseRef` tries only `['origin/main', 'main']`, where the sibling gate's identically-named function tries `['origin/main', 'origin/master', 'main', 'master']` (`check_kernel_edit_ratified.ts:215`). On a `master`-default repository this gate silently falls back to `HEAD~1` and reads a one-commit diff — an odd narrowing in a file that otherwise goes out of its way to resolve the default branch dynamically. | fixed | 217139b67 — `resolveBaseRef` tries the same four refs as the sibling gate |
| 11 | low | src/scripts/check_platform_anchor.ts:174-178 | `source.rulesets(repo)` and `source.defaultBranch(repo)` are both evaluated as arguments, so the `repos/{repo}` metadata call always fires even when the ruleset read already returned `null` and `evaluateAnchor` will short-circuit to `unverifiable` at its first branch. One wasted API round-trip on every failure path. | fixed | 217139b67 — `source.rulesets` is evaluated first and the default-branch call is skipped when it returns null; a regression test counts the calls |
| 12 | low | agents/evidence/analysis/platform-anchor-measured-2026-09-10.md:1; agents/evidence/ratifications/drain-typed-grants-platform-anchor.md:12 | Both new evidence artifacts carry the short marker form `<!-- evidence-type: analysis -->` / `<!-- evidence-type: ratification -->`, while the versioned form in use elsewhere in `agents/evidence/reviews/` is `<!-- evidence-type: v1 \| type: <t> \| declared: <date> -->`. If `lint_evidence_artifacts` requires the versioned form these two files red once committed. Flagged as a format risk rather than asserted: confirming it needs a read outside the branch-touched set this review is scoped to. | accepted-risk | verified NOT a defect, accepted by the implementer on measured output rather than on reading: `lint_evidence_artifacts` ran over this branch at push time and reported "all 2 added artifact(s) declare a type", exit 0 — the short marker form is accepted and there is nothing to change. The row was correctly filed as an unconfirmed format RISK; confirming it needed a read outside the review scope. |
