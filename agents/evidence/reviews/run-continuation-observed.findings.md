# Findings: run-continuation-observed
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 0f45393b5ea0eadbf2d262a9bdea98fb4c30c0d2bb256aedbf1a60b7cf86929c | diff: d9e040bd2bc6199363b367aa784a42a8cbbcccc6 | reviewer: r2-fresh-subagent-run-continuation-observed | prompt_hash: f943fcdadfbfb5f9f33eb7f35614f54d2a2b180b6b481e9b3e73b8fc59d515ad -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: d9e040bd2bc6199363b367aa784a42a8cbbcccc6
  scope_hash: 0f45393b5ea0eadbf2d262a9bdea98fb4c30c0d2bb256aedbf1a60b7cf86929c
  roadmap: agents/roadmaps/road-to-run-continuation-observation.md
  roadmap_hash: 544f64b50aa2a7ba4da1661c397f266cb45f0fe4e91ec2c6d45ed545704abbcd
  ac_hash: 9b0a88c777f11681e03f799b5ece675baa0b47c84a1f2110d8b652110db3749b
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T14:34:33Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/run_continuation_hook.ts:429 | `provenance()` derives BOTH git fields from `workspaceRoot` — the tree the *reader* resolved — so it cannot record the writer's tree at all. In the arrangement the roadmap documents as the live one (the concern runs under `--project-dir <parent checkout>` because `CLAUDE_PROJECT_DIR` resolves to the parent even in a worktree session), `git_dir(parent)` and `git_common_dir(parent)` both return `<parent>/.git`, i.e. they are EQUAL. Concrete failure: a real worktree-started `process-full` run writes an `engage` line whose fields make the docblock's own discriminator `git_dir !== git_common_dir → this IS a linked worktree` FALSE, so an auditor applying the documented rule reads a worktree-started run as same-root and the two-tree property stays unauditable — the exact gap the change exists to close. `claim_path` cannot disambiguate either: `claim_file` writes under `git_common_dir` in a plain checkout too, so `claim_path` ⊂ `git_common_dir` holds in both arrangements. | open | |
| 2 | medium | tests/hooks/run_continuation_dispatch.test.ts:296 | The only test asserting the two-tree property dispatches with `--project-dir <worktree>` (`dispatchStop(worktree, …)`), i.e. a reader rooted IN the worktree — not the parent-checkout-rooted reader that produced the shipped defect and that the roadmap records as the live configuration. No case drives `dispatchStop(main, …)` against a worktree-written claim. Concrete failure: finding 1 is invisible to the suite — the fields can report same-root for every real worktree run while all nine cases stay green, the same "green suite over the arrangement that never fails" shape the file's own header warns about for `CONCERN_REGISTRY`. | open | |
| 3 | medium | src/scripts/hooks/run_continuation_hook.ts:431 | The four fields are normalised inconsistently: `git_dir`/`git_common_dir` come back realpath-resolved from `_lib/git_common_dir.ts`, while `workspace_root` is the envelope string verbatim and `claim_path` is `path.join(workspace_root, rel)` on the per-tree fallback. The docblock promises "absolute paths, deliberately" and that a third party holding "nothing but the ledger line" can check `claim_path` under `git_common_dir`. Concrete failure: on macOS or any symlinked checkout the line carries `workspace_root: /var/…` / `claim_path: /var/…` against `git_common_dir: /private/var/…`, so the documented prefix test fails on a healthy run; a relative `--project-dir` (e.g. `.`) produces a relative `workspace_root` and `claim_path` beside absolute git fields. The test at :299–301 demonstrates the divergence by realpath-ing BOTH sides before comparing — normalisation a ledger-only reader cannot perform. | open | |
| 4 | medium | tests/hooks/run_continuation_dispatch.test.ts:130 | The new `git()` helper runs `spawnSync('git', args, { cwd })` without stripping inherited `GIT_DIR`/`GIT_WORK_TREE`, which `_lib/git_common_dir.ts`'s own docblock names as the reason it refuses to shell out at all. Concrete failure: run the suite from inside a git hook (which exports `GIT_DIR`) and `git(main,'init')`, `git(main,'config','user.email',…)` and `git(main,'commit')` resolve against the HOOK's repository instead of the temp dir — the fixture rewrites the real repo's local `user.email`/`user.name` and can commit the real staged index instead of failing loudly. | open | |
| 5 | medium | agents/roadmaps/road-to-run-continuation-observation.md:131 | Acceptance criterion 2 is marked `[x]` while its own body states the criterion as literally written "cannot be satisfied … by any number of runs", that the aggregate `median re-engagements:` "stays **0**", and that a reader taking the aggregate reading "should reopen this box". A `[~]` would route this through the deferred-resolution gate; `[x]` makes the generator count it done. Concrete failure: once step 0.1 closes, the archival sweep sees `count_open == 0` and `count_deferred == 0` and archives the roadmap over an acceptance criterion the roadmap itself flags as unresolved — the silent-archive-of-unresolved-work case the deferred gate exists to catch, routed around by glyph choice. | open | |
| 6 | low | src/scripts/hooks/run_continuation_hook.ts:495 | `provenance()` is invoked immediately after the claim gate, before the roadmap read (:501), the `execution.mode: autonomous` check (:506) and the transcript checks (:512–:526). Concrete failure: every `stop` fire of a session holding a claim on a `phase-checkpoints` roadmap — which the branch's own blocker text says "makes the concern a no-op" — pays `git_dir` + `git_common_dir` (2 `statSync` + 2 `realpathSync` in a plain checkout, up to 2 further `readFileSync` plus an `existsSync` in a worktree) to build a value discarded at the next line. Moving the call below the mode gate keeps the value on every path that can emit an event. | open | |
| 7 | low | tests/hooks/run_continuation_dispatch.test.ts:112 | `fixtureRoadmap()` is documented as "The roadmap body every fixture in this file uses", but `writeWorkspace()` (:97) keeps its own byte-identical inline copy and never calls it — so exactly one of two fixtures uses it. Concrete failure: a future edit to `fixtureRoadmap()` (a second open step, a different `execution.mode`) changes only the worktree fixture while both fixtures still share `SLUG`, and cases asserting open-step counts (`expect(engaged[0]?.['open']).toBe(1)`) then pin two different roadmaps under one name. | open | |
| 8 | low | tests/hooks/run_continuation_dispatch.test.ts:353 | `expect(typeof deferred[0]?.['claim_path']).toBe('string')` pins only the type, while the sibling git fields two lines below are pinned to exact values. Concrete failure: a regression that makes `resolve_claim` return an empty `path` (or `provenance` emit `claim_path: ''`) leaves the ledger without the one field that locates the contract, and this assertion still passes because `typeof '' === 'string'`. The fixture's claim path is deterministic (`path.join(root, roadmap_claim_rel(SESSION))`) and could be asserted exactly. | open | |
| 9 | low | src/scripts/hooks/run_continuation_hook.ts:493 | The call-site comment reads "The two-root provenance … See `provenance()` for why these three and not a boolean", but `provenance()` emits FOUR fields and its own docblock (:396) says "The four fields". Concrete failure: a reader auditing which fields the ledger carries counts three from the call site and does not look for `claim_path` — the one field carrying the relation the change is built on. | open | |
