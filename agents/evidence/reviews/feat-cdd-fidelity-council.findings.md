# Findings: feat-cdd-fidelity-council
<!-- completion-review: v1 | reviewed: 2026-09-09 | scope: 60c4434e239957b9c39ac228186bf1beeb70551ea833ee92f8e1936715b7cb78 | diff: 30aa704cf91612f3675838202e902bdda3f98cf6 | reviewer: r2-fresh-subagent-feat-cdd-fidelity-council | prompt_hash: 7f2d7f06f678183b2092b714a4234d28af1495919de86be5f61c370c021ffcd1 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-feat-cdd-fidelity-council"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-09 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 30aa704cf91612f3675838202e902bdda3f98cf6
  scope_hash: 60c4434e239957b9c39ac228186bf1beeb70551ea833ee92f8e1936715b7cb78
  roadmap: agents/roadmaps/archive/road-to-design-intent-conformance.md
  roadmap_hash: 94935a5f189a51a9f458773b4e90ccb584e36449f5682c8f06316a18c9fa9781
  ac_hash: 9b391452c941756f547aa4657cea3c99a034568e74f11e0d88733ccc77736f23
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-09T17:45:43Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/condense.ts:881 | The new guidelines lane ships 120 files whose own links are dead in the projection. `_rewrite_body_links` rewrites only the `docs/guidelines` and `docs/contracts` prefixes; it never rewrites `(?:../)+src/rules/…`, `(?:../)+src/skills/…` or `../decisions/…`, which is how a file in `docs/guidelines/` points at a rule, a skill or an ADR. Measured on the shipped tree: 253 of 268 relative `.md` links under `dist/agent-src/guidelines/` resolve to nothing, across 70 of the 120 files. `dist/agent-src/guidelines/design-fidelity-mechanics.md` — the file Phase 2 exists for — carries 5, including `../../src/rules/design-fidelity.md` (the rule that routes to it; the correct projected target is `../rules/design-fidelity.md`) and `../../src/skills/wireframe/SKILL.md`. AC-4 makes the body reachable and leaves the body's own pointers unfollowable, which is the defect the roadmap opens with, one layer down. `check_projected_rule_routes` scans `dist/agent-src/rules/` only, so this population is unmeasured and unpinned. | fixed | Fixed in 9717ee3ac. `_rewrite_body_links` now folds `(../)+src/(agent-src/)?<projected-dir>/…` onto the projection prefix; `src/domains` is deliberately left out because a domains command re-shapes to `commands/<subpath>.md` rather than losing a prefix, and guessing it would produce a link that looks right. Measured on the same corpus the finding used: 253/268 → 44/268. The residue is `docs/contracts` (34), `docs/decisions` (10), `agents/` and two template placeholders — none a projected tree. And the population is no longer unmeasured: `check_projected_rule_routes` gains `UNRESOLVED_GUIDELINE_FLOOR = 44`, a SEPARATE shrink-only floor from the rules one so a regression in either cannot hide behind an improvement in the other. |
| 2 | high | src/server/schemas/settings.ts:246 | `design.approximation.enabled`, `.tolerance.color` and `.tolerance.length` are added to `src/config/agent-settings.template.yml:741-775` and to the class contract (`docs/contracts/settings-classes.md:534-536`, counts raised 142→145) but not to the zod settings schema. Two consequences. `docs/settings-reference.md` is generated from that schema and titled "every key, its class, and its default"; its `## design` section still lists only `fidelity_mode`, and `--check` stays green because the page still matches what the schema produces. And the server/GUI parse strips the unknown block, so a value set there does not survive a round-trip. `lint_settings_classes` compares template against contract only, so no gate sees the gap. | fixed | Fixed in 9717ee3ac. `design.approximation.enabled` / `.tolerance.color` / `.tolerance.length` added to the zod schema with their full describe() text; `docs/settings-reference.md` regenerated and now carries all three. The gap the finding names in `lint_settings_classes` (template↔contract only, never the schema) is real and is NOT closed here — recorded as finding 17 rather than folded into this row, because it is a gate gap and this is a missing key. |
| 3 | high | src/scripts/hooks/source_first_gate_hook.ts:75 | The tolerance shadow window records nothing, and the same diff adds a header asserting that it does: *"`_lib/tolerance_shadow.ts` appends `record: "tolerance_shadow"` lines here."* `tolerance_shadow.ts` imports no `fs`, writes no file, and `toleranceShadowRecord` has no caller anywhere outside `tests/scripts/tolerance_shadow.test.ts` — nothing in the production tree builds or appends a record. `FLIP_CRITERION`'s reverse trigger ("fewer than 20 records in 8 weeks → evaluate removing the mechanism") is therefore satisfied by construction, and AC-7's "shadow records exist" holds for no runtime path. | fixed | Fixed in 9717ee3ac. `recordToleranceShadow(root, rows)` added, reusing `appendShadowLine` rather than a second writer to the same log, and `buildConformanceReport` calls it behind an opt-in `shadowRoot`. Opt-in on purpose: a report is also built by tests and by a reader inspecting a port, and a window recording those would measure its own fixtures. Three regression cases pin the chain, including that nothing is written when no root is given. |
| 4 | medium | src/scripts/_lib/conformance_report.ts:72 | `MEDIA_RE` requires a literal `@media` before each captured bound, so a compound query captures only the first: `@media (min-width: 48rem) and (max-width: 90rem)` yields `48rem` and not `90rem`. `90rem` therefore survives `setMinus(…, breakpoints(…))` and stays in the value set, so a port that drops the upper bound reds `values` instead of `responsive`. That is the same cross-dimension bleed the module already patched for the single-bound case (comment at :208-213), and it breaks the "reds exactly the dimension corrupted, and no other" property AC-7 rests on. The fixture at `tests/scripts/conformance_report.test.ts:36` uses a single-bound query, so no case exercises it. | fixed | Fixed in 9717ee3ac. `breakpoints()` now walks every width bound in a query instead of the first, so a compound `@media (min-width: …) and (max-width: …)` contributes both. Regression case added at both levels — the extractor, and the one-dimension property over a compound-query corruption. |
| 5 | medium | src/scripts/_lib/design_system_import.ts:254 | `src/skills/design-system-capture/references/design-system-json.md` gains a `spec.maturity` / `maturity_source` / `maturity_signal` block described as part of "the contract", but the module implementing that contract was not updated: `NATIVE_KEY_SHAPE` has no `spec` entry and the `DesignSystem` interface (:80, commented "The contract shape") has no `spec` field. `mapNative` therefore emits the note *unknown top-level key "spec" is not in the contract — kept under `_meta.unmapped`* for the contract's own new field, and the maturity verdict is dropped from the imported object. | fixed | Fixed in 9717ee3ac. `spec: 'object'` added to `NATIVE_KEY_SHAPE` and a `SpecBlock` field to the `DesignSystem` interface, so the module implementing the contract carries the field the contract documents. Regression case asserts the block passes through and produces no off-contract note. |
| 6 | medium | src/scripts/_lib/design_tolerance.ts:118 | `parseHex` accepts the 8-digit `#rrggbbaa` form and then slices only the first six digits, discarding alpha. `deltaEOK('#ff000080', '#ff0000')` is therefore `0` — a fully transparent and a fully opaque colour measure as identical, and would be reconciled onto each other once any threshold is configured. That is precisely the reading the module's header says must not be produced for values that are not identical ("an unparseable value is reported as unmeasured rather than assigned a distance of zero, which would read as identical"). Compounding it, `conformance_report.ts:69`'s `HEX_RE` cannot match an 8-digit hex at all, so the same value is invisible to the `values` dimension. No test covers the 8-digit form. | fixed | Fixed in 9717ee3ac. `parseHex` now REFUSES the 8-digit form rather than truncating it. Null is the honest answer for a three-channel metric with nothing to say about alpha; reporting the row as unmeasured is what the module's own header demands, and returning `0` said 'identical' about two values a designer chose to differ. |
| 7 | medium | src/scripts/_lib/artifact_maturity.ts:150 | The three new Phase 3/4 modules — `artifact_maturity.ts`, `design_tolerance.ts`, `conformance_report.ts` — have no production caller. `resolveArtifactMaturity`, `reconcileValue` and `buildConformanceReport` are referenced only from their own test files; no CLI verb, gate, hook, Taskfile target or skill script invokes them, and nothing reads the `design.approximation.*` keys the template now documents (`SHIPPED_TOLERANCE` is a hardcoded constant, not a settings read). AC-6 ("fields with provenance, not prose") and AC-7's first clause are satisfied by unit tests over pure functions plus a hand-written fixture value in `tests/design-artifacts/fixtures/handoff-bundle/design-system.json:5-9`; nothing produces the field in a real run. | accepted-risk | Partly closed and partly accepted, stated separately. CLOSED: `conformance_report` now calls `tolerance_shadow`, so the Phase 4 chain has a real caller and the shadow window is fed. ACCEPTED: the ENTRY point — what invokes `buildConformanceReport` or `resolveArtifactMaturity` on a real port — is skill-driven prose, not a CLI verb, and inventing a verb nobody asked for to satisfy a caller check would be worse than the gap. The honest reading is that AC-6 and AC-7 are satisfied at the library layer and not end to end, which the roadmap's own Phase 4 framing ('turn the claim into a measurement') already scopes. Revisit-if a port flow lands that needs the entry point. |
| 8 | medium | src/scripts/_lib/guidelines_lane.ts:74 | Projecting the whole of `docs/guidelines/` duplicates content the package already publishes: `docs/guidelines/` is an existing `files[]` entry (`package.json:39`), so the npm tarball now carries the same 980 KB twice — ~296 KB compressed added, measured with `tar` piped through `gzip -9` over `dist/agent-src/guidelines`. Against `src/config/pack-size-budget.json` (`max: 9.1`, `last_measured: 8.475`) that consumes roughly half the remaining headroom on a cap already re-baselined upward four times, and it is ~3.2× the 92.9 KB `road-to-npm-payload-reduction` had just recovered. Step 2.1's verify clause checks `check_source_size_budget`, a source-line ratchet that cannot see tarball growth, so the cost was never measured. | accepted-risk | The duplication is real; the cost is measured and small. `check_pack_size` — the gate that owns this number, run before and after — reads 11.446 MB → 11.488 MB BUILT, +42 KB compressed, green with headroom. The finding's ~296 KB comes from `gzip -9` over the directory, which is not what `npm pack` produces for content that largely re-compresses against its own source copy. The gate's reading is the one that governs. Removing `docs/guidelines/` from `files[]` would make it net-zero and is the obvious follow-up; not taken here because a `files[]` change moves the prepack whitelist and the binary/content classes, which is a wider blast radius than a +42 KB fix warrants. |
| 9 | medium | src/scripts/_lib/conformance_report.ts:73 | `HANDLER_RE`'s listener branch requires an all-lowercase quoted event name, so a mixed-case event never matches — `addEventListener('DOMContentLoaded', …)`, `'DOMNodeInserted'` and any custom `'itemAdded'` are invisible to `behaviourHooks`, and a port that drops one stays green on the `behaviour` dimension that exists to catch exactly that. The attribute branch has the opposite failure: it matches any token beginning with `on` followed by letters and `=`, with no attribute boundary, so `onboarding="x"` registers a hook named `boarding`. | fixed | Fixed in 9717ee3ac. Both halves. The listener branch is now case-insensitive and accepts a custom event name. The attribute branch is matched against a CLOSED SET of inline handler names rather than by shape — a boundary cannot separate `onclick` from `onboarding`, since both begin with `on` and both are followed by `=`, so the namespace is what discriminates. Regression cases both directions. |
| 10 | medium | src/scripts/lint_settings_classes.ts | Raised by this branch while dispositioning finding 2, not by the review: `lint_settings_classes` compares the settings TEMPLATE against the class CONTRACT and never against the zod schema, so a key present in two of the three surfaces and missing from the third passes every gate. That is how finding 2 shipped — the template and the contract both had `design.approximation.*`, the schema did not, and `docs/settings-reference.md --check` stayed green because the page still matched what the schema produced. The three-way check is the fix and it is not made here: it is a gate change with its own corpus, unrelated to the roadmap this review covers. | deferred | Recorded rather than fixed. The instance is closed (finding 2); the class is not, and the next key added to two surfaces will repeat it. |
| 11 | low | src/scripts/_lib/artifact_maturity.ts:178 | An **unrecognised** declaration returns early with `finished` / `source: 'default'`, skipping rungs 3–5 entirely. An artefact declaring `maturity: draft` while carrying a `wireframe` filename, a lorem-ipsum body and a greyscale-only palette resolves `finished`, discarding three signals the resolver does recognise — an unparseable string outranking every recognised inference, which inverts the ladder the header describes. The verdict also reports `source: 'default'` while `signal` names a declaration, so a consumer keying on `source` cannot tell this case from rung 6. | fixed | Fixed in 9717ee3ac. An unrecognised declaration no longer returns. It is remembered and reported: the remaining rungs run, and the verdict's `signal` carries both what decided it and the declaration that could not be read. Regression case is the finding's own scenario — `maturity: draft` with a wireframe filename and a lorem-ipsum body now resolves `low` / `inference`. |
| 12 | low | src/scripts/_lib/guidelines_lane.ts:5 | Stated measurements in the new code do not reproduce on the tree they shipped with. `guidelines_lane.ts:5` says "31 rules declare `routes_to: guideline:<slug>`"; `grep -l 'guideline:'` returns 22 in `dist/agent-src/rules/` and 22 in `src/rules/`. `check_projected_rule_routes.ts:53` says "256 relative links across the projected rules, 36 of them unresolved"; re-running that module's own `REL_LINK_RE` over the same 119 files returns 262 and 36 — the floor is right, the denominator is not. The roadmap's step 2.2 asserts "43 distinct targets"; the gate itself reports 22. | fixed | Fixed in 9717ee3ac. All three corrected to the readings the code prints: `guidelines_lane.ts` header 31 → 22 targets; `check_projected_rule_routes.ts` denominator 256 → 262; the roadmap's step 2.2 31/43 → 119/22. Each correction says what the wrong figure was and that it reproduced on nothing, because a silently corrected number teaches the next reader nothing. The floor was right in every case, which is exactly why a wrong denominator could sit unnoticed beside it. |
| 13 | low | src/scripts/check_projected_rule_routes.ts:275 | Half B's failure message reports only an aggregate ("unresolved relative links rose to N, floor is 36") and never names the file or the link, so the operator who reds it has nothing to act on — `evaluate` increments `unresolvedRelative` at :121 while holding the resolved path and discards it. Two smaller shape issues in the same file: `listRules` (:77) is non-recursive, so a rule in a subdirectory of `dist/agent-src/rules/` would be silently unscanned; and `isFixtureRoot = root !== REPO_ROOT` (:235) is a raw string comparison, so a `--root` pointing at the real repo through a symlinked or `/private`-prefixed path silently disables the ratchet. | fixed | Fixed in 9717ee3ac. All three. The half-B failure now names every unresolved link (`file → link`) instead of an aggregate the operator cannot act on — `evaluate` already held the paths and threw them away. `listRules` is recursive, via the same `walkMarkdown` the guideline half uses. The `isFixtureRoot` string comparison is UNCHANGED and that half is accepted rather than fixed: every caller in the tree passes either no `--root` or a `mkdtemp` path, and a realpath comparison would add a syscall to defend against a caller that does not exist. |
| 14 | low | src/scripts/check_projected_rule_routes.ts:59 | `ROUTES_TO_RE` scans the whole rule body rather than the `routes_to:` frontmatter key, and its character class includes `.`, so a prose mention of the form `guideline:foo.md` is captured as slug `foo.md` and checked against `foo.md.md`. No live instance exists today (22 targets, 0 unresolved), but the half is graded HARD, so the first body that spells a guideline reference that way reds CI for something that is not a defect. | fixed | Fixed in 9717ee3ac. `.` removed from the slug character class. No live instance existed, but the half is graded HARD, so the first body spelling `guideline:foo.md` in prose would have reddened CI for a non-defect — the correct time to fix a latent false positive in a blocking gate is before it fires. |
| 15 | low | agents/roadmaps/archive/road-to-design-intent-conformance.md:769 | AC-9's second clause — "no sentence in a projected rule instructs the reader to open a path the projection does not contain" — is marked `[x]` while the gate this branch ships pins 36 such links as a standing shrink-only floor, one of them in `dist/agent-src/rules/ui-audit-gate.md` (`../docs/contracts/hook-architecture-v1.md`), a rule this branch edited. The gate's header is honest about the 36; the AC is not scoped to match it. | fixed | Fixed in 9717ee3ac. AC-9's second clause is now scoped to what shipped: the criterion is about an INSTRUCTION to open an unprojected path — the imperative 1.5 removed — not about every reference. The 36 pinned see-also links are named in the criterion itself with the reason they stay. The gate's header was honest about them from the start; the AC was not. |
| 16 | low | src/scripts/_lib/conformance_report.ts:74 | `INLINE_STYLE_RE` has no boundary before `style` and accepts only double quotes, so the carrier dimension both false-positives on any attribute ending in `style` (`data-style="…"`, `hover-style="…"`) and misses single-quoted `style='…'` and JSX `style={{…}}` entirely. `inlineSvgCount` and `structuralCounts` likewise scan raw text with no comment or script stripping, so a tag named inside an HTML comment, a JS template literal or a `<script>` string is counted as structure. | fixed | Fixed in 9717ee3ac. `INLINE_STYLE_RE` is now attribute-bounded and accepts both quote styles, so `data-style=` no longer false-positives and `style='…'` is no longer missed. The JSX `style={{…}}` form and the no-comment-stripping observation are NOT fixed and are accepted: this module's own header scopes it to static text checks over two documents, and a JSX-aware or comment-aware parser is a different tool. |
| 17 | low | src/scripts/_lib/design_tolerance.ts:171 | `parseLengthPx` resolves `em` against the 16px root exactly like `rem`, but `em` is relative to the element's own font size, so a nested `1em` is scored against the wrong base and the reported distance is wrong rather than absent. Meanwhile `conformance_report.ts:70`'s `LENGTH_RE` extracts only `px` and `rem`, so extractor and parser disagree about which units exist, and `%`, `pt`, `vh`, `ch` and `em` values in an artifact are never rowed at all. | accepted-risk | Correct on both counts and both are scope, not defects to fix here. `em` against a 16px root is wrong for a nested element, and `%`, `pt`, `vh`, `ch` are never rowed. The module ships with NO tolerance configured, so no distance it reports drives any behaviour today — which is exactly the window the roadmap opened to find out which units matter before widening the parser. Adding units now would be guessing at a corpus the shadow window exists to measure. Revisit-if the shadow distribution shows a unit class with volume. |
