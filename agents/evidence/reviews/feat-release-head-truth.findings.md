# Findings: feat-release-head-truth
<!-- completion-review: v1 | reviewed: 2026-08-15 | scope: 29a9b9b448bb977f4ffa4544c063b6ea0b48fdc927215f0c64436013ef598a3d | diff: acbe275c5f0dbe1172d7b2a162fbcf0b526fdd03 | reviewer: r2-fresh-subagent-feat-release-head-truth | prompt_hash: 0b788881ae651df2040e8926712e9d66eb408e4d3febac431ae17f5b2b2b0de5 -->

<!-- context-manifest: v1
inputs:
  diff_sha: acbe275c5f0dbe1172d7b2a162fbcf0b526fdd03
  scope_hash: 29a9b9b448bb977f4ffa4544c063b6ea0b48fdc927215f0c64436013ef598a3d
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-c-release-head-truth.md
  roadmap_hash: 4948ba91d1da76bdaa53a6bb2619f16c8fa034b7109bd66fe292f9b60e13783e
  ac_hash: 512f303acf2a5a63a3a50f2b7c807f34efb540bb841a48d4745b3cfd2ab5c8da
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-15T07:20:54Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | docs/contracts/CHANGELOG-conventions.md:126-134 | The branch adds a paragraph re-affirming the rejected-branch decision directly above a pre-registered falsifier that has already fired in this tree. Falsifier (2) reads "a shipped marked line survives its next release cycle uncurated". Marked lines shipped in 10.1.0, 10.2.0, 10.3.0, 10.4.0, 11.0.0 and 12.0.0, and none has been curated in place since — they are still verbatim in the archives and in the live head. The widening makes this worse by construction: `Security and correctness` now derives on 6 of 6 spans against 1 of 6 before, so it adds a marked line to essentially every future release on a label that previously shipped `_none_`. The branch's only counterweight (step 3.2, the publish-side marker invariant) is `[~]` behind an open blocker, so the net effect of merging as-is is more unrewritten markers on a mechanism the same contract records as already failing. | fixed | 03732f5da — the paragraph now states that falsifier (2) has fired, with the measured 11-marked-lines-across-6-of-6 count, and that the widening raises the rate by construction; surfaced as the maintainer's reopening call, not decided. Measured: docs/archive/CHANGELOG-pre-10.3.0.md:23,24,133; docs/archive/CHANGELOG-pre-12.0.0.md:21,93,168,169; CHANGELOG.md:365-366. Derivation counts measured by running the shipped `derive_category_hits` over the six spans (3/15/12/5/4/8). |
| 2 | medium | agents/evidence/analysis/release-head-derivation-recall.md:77-78 | The headline "1 derived / 45 in-category" matches neither definition the same file uses, and it is the number three other artefacts quote. Running the shipped rule over the six spans gives 46 correctness hits (47 total, incl. 1 legacy security-only hit in `10.1.0..10.2.0`). § 3 states the criterion fires 46 times with 44 hand-confirmed and names the two judged misses. § 1's hand column subtracts one of them (`f525ed3`, span 6, row reads 7 not 8) and not the other (`f87240d`, span `10.1.0..10.2.0`, row reads 14 = the mechanical count). So 45 is the mechanical total minus exactly one of two known false positives — neither 46 (mechanical, which is what § 1a's stated criterion computes) nor 44 (hand-confirmed). | fixed | 87b85c499 and c94b9eebd — the analysis now carries all three scopes in one table (1 old-rule / 47 mechanical / 45 hand-confirmed) and says explicitly that § 3's 96 % is the correctness criterion alone, which is the one-hit difference; the replicated headline is corrected in the source docblock and the test docblock. Verified `f87240d` is `fix(contracts)` touching `src/scripts/check_rule_projection_integrity.ts` and first contained in 10.2.0, i.e. mechanically caught. |
| 3 | medium | src/scripts/_lib/release_highlights.ts:101-102 | `_commit_type` compares case-sensitively against `'revert'`, so git's own revert subject `Revert "<original subject>"` yields type `Revert` and never matches. The documented correctness half — "a `fix` or `revert` commit touching executable surface", stated identically in the source docblock, the analysis § 1a and the contract — therefore does not apply to the revert form `git revert` generates by default; only a hand-written `revert(scope):` is caught, and only that form is tested. A revert of shipped behaviour is the commit class most likely to be a correctness repair, so the miss is silent and lands on exactly the failure this branch exists to remove. | fixed | c94b9eebd — `_commit_type` now returns `revert` for the `Revert "<subject>"` form git writes, and the case is pinned by a new test. Zero `Revert "` subjects exist in 10.0.0..12.0.0, so the gap was forward-looking and unmeasured, not historical. |
| 4 | medium | src/scripts/_lib/release_highlights.ts:89-98 | The executable-surface criterion carries a second recall limit that is never named. `.github/actions/*/action.yml` (composite CI actions, present in this repo) is shipped executable surface matched by neither `_EXECUTABLE_SUFFIX` nor `_EXECUTABLE_PREFIXES`, which lists only `.github/workflows/`. Shebang scripts with no extension are also invisible: `scripts-run` and `src/scripts/agent-config` (a `#!/usr/bin/env bash` entry point) both fail the suffix test. The analysis names one recall limit explicitly (the author's own conventional type, § 1a) and none other, so a reader takes the path set as complete. | fixed | c94b9eebd — `.github/actions/` added to the prefix list and the two extensionless entry points to an exact set, both covered by a test; the residual limit (a NEW extensionless entry point is invisible until added) is now named in the source docblock. |
| 5 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-c-release-head-truth.md:132-147 | AC 3 is left open carrying an explicit maintainer decision ("accept the false-positive reading and close this, or re-cut the criterion") that is not registered in `## Blockers`. The roadmap uses the blocker mechanism correctly for the publish-side question one section above, so the surface the dashboard advertises (`agent-config gates`) will show one blocker for this roadmap and not this decision. A decision recorded only in an acceptance-criterion body is invisible to the mechanism built to surface decisions, and this one gates the roadmap's closure. | fixed | acbe275c5 — registered as `blocker: ac3-false-positive-reading`, same shape as the publish-side blocker one section above, with the two readings as mutually exclusive options; the criterion body now points at it. |
| 6 | medium | docs/contracts/CHANGELOG-conventions.md:101-117 | The contract's published marked-line rate is stale by three releases and the branch had the data to refresh it. Standing text: "3 of the 3 releases since, 7 marked lines", with a table ending at 10.3.0. Measured now: 10.1.0 (2), 10.2.0 (3), 10.3.0 (2), 10.4.0 (1), 11.0.0 (1), 12.0.0 (2) = 6 of 6 releases, 11 marked lines. The section is date-stamped 2026-08-13, which mitigates but does not remove the drift: this branch edited the paragraph immediately above it after measuring all six spans and after reading the era archives specifically (analysis § 0), so the refreshed number was in hand. | fixed | 03732f5da — table extended to 10.4.0, 11.0.0 and 12.0.0 and the summary refreshed to 6 of 6 releases, 11 marked lines, with the stale 3-of-3 figure named as what it replaced; independently re-counted per release before publishing. |
| 7 | low | agents/evidence/analysis/release-head-derivation-recall.md:169-173 | § 4's per-span split of the six added honest-null hits contradicts § 1 and the shipped rule. It reads "2 in `11.0.0..12.0.0`, 3 in `10.1.0..10.2.0`, 1 in `10.3.0..10.4.0`". Measured: the widened rule derives 1/3/0/3/0/2 across the six spans against the old rule's 1/1/0/1/0/0, so the additions are 2, 2 and 2. The split is wrong under both readings — as additions (2/2/2) and as post-widening totals (2/3/3) — while the total of 6 and the conclusion are right. | fixed | 87b85c499 — § 4 now reads 2/2/2 with the post-widening totals stated beside it. |
| 8 | low | agents/evidence/analysis/release-head-derivation-recall.md:4,106,110,124,130 | Every `release_highlights.ts:NNN` citation in the analysis is stale in the merged tree, because this branch moved the code the analysis cites. `:106` (`derive_category_hits`) is now 158, `:116` (the security rule) is now 171, `:137` (the honest-null rule) is now 193, `:101` (Known limitations) is now 145, `:103-104` (the conservative-stance comment) is now 147-148. The file's SHA pin to `origin/main c37cb8de5` mitigates for a careful reader, but the sentences are present tense and read as pointers into the current file. | fixed | 87b85c499 and acbe275c5 — every `file:NNN` citation replaced by a symbol name in both the analysis and the roadmap Context table, with a note saying why a file documenting a diff cannot cite that diff by line. |
| 9 | low | src/scripts/_lib/release_highlights.ts:101-102 | `_commit_type` extracts the leading `\w+` without requiring the conventional `:` or `(scope):` separator, so it disagrees with the `scopeMatch` regex three lines below it in the same function and with all three prose descriptions of the rule ("conventional type `fix` or `revert`"). A non-conventional subject such as `fix the flaky runner` classifies as type `fix`. No such subject exists in the six measured spans, so this is a documentation-versus-behaviour mismatch rather than a live false positive. | fixed | c94b9eebd — `_commit_type` now requires the conventional `:` separator, so it agrees with the scope regex beside it and with all three prose descriptions; `fix the flaky runner` is pinned as not-a-fix. Measured 0 such subjects in 10.0.0..12.0.0, so no count moves. |
| 10 | low | tests/scripts/check_release_highlights.test.ts:235-242 | The negative test for the recorded-null forms exercises only subjects with no verb-plus-`null` shape (`is null by design`, `a nullable owner field`). The loosest new pattern is applied to subject *and body*, and bodies are where it will actually match — a commit body recording a null value ("records null token_delta") would derive as an Honest null. No body-level near-miss is pinned. Precision over the six measured spans is 9 of 9, so this is a coverage gap rather than a live defect. | fixed | c94b9eebd — a same-line field-name exclusion plus two body-level tests. Worth recording: the FIRST version of the exclusion used `\s+` and silently dropped 92f9b9a, a true positive whose subject ends on "a published null" and whose body opens with `count_open` on the next line. Re-measuring the six spans after a narrowing that looked safe is what caught it; both the exclusion and the line-break case are now pinned. |
| 11 | low | docs/contracts/CHANGELOG-conventions.md:80-81 | "On those two labels `_none_` could not be contradicted and shipped uncontested on five of six curated heads" holds for `Security and correctness` (5 of 6) but not for `Honest nulls`, which derived under the old rule on three of the six spans and shipped a derived line on those heads. The sentence attributes one label's rate to both. | fixed | 03732f5da — the sentence now attributes the five-of-six `_none_` rate to `Security and correctness` alone and states that `Honest nulls` fired on three of the six and shipped a derived line on those. |
