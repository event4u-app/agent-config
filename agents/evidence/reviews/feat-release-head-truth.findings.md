# Findings: feat-release-head-truth
<!-- completion-review: v1 | reviewed: 2026-08-15 | scope: 5056f22678a072e3d0fed7edaeb3fe10cab8c8079dc974b94f41f2368666a944 | diff: 6f5d0f4aaeb85732c28ac29a70a260b852e56baf | reviewer: r2-fresh-subagent-feat-release-head-truth | prompt_hash: 00f20d9dc1e69ac27f19ddf8590328a2052c5d2a907d249eadc6e372675b62e8 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 6f5d0f4aaeb85732c28ac29a70a260b852e56baf
  scope_hash: 5056f22678a072e3d0fed7edaeb3fe10cab8c8079dc974b94f41f2368666a944
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-c-release-head-truth.md
  roadmap_hash: 8bf0f3daa2fcd135a609b7f416ea011a588817e09eb680e4d500163f843c3bd6
  ac_hash: bea2f14c0c6128b6a024cc89bcba1bf4b2e9b4239a00306ea1873d7f1ebef089
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-15T06:55:30Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | docs/contracts/CHANGELOG-conventions.md:126-134 | The branch adds a paragraph re-affirming the rejected-branch decision directly above a pre-registered falsifier that has already fired in this tree. Falsifier (2) reads "a shipped marked line survives its next release cycle uncurated". Marked lines shipped in 10.1.0, 10.2.0, 10.3.0, 10.4.0, 11.0.0 and 12.0.0, and none has been curated in place since — they are still verbatim in the archives and in the live head. The widening makes this worse by construction: `Security and correctness` now derives on 6 of 6 spans against 1 of 6 before, so it adds a marked line to essentially every future release on a label that previously shipped `_none_`. The branch's only counterweight (step 3.2, the publish-side marker invariant) is `[~]` behind an open blocker, so the net effect of merging as-is is more unrewritten markers on a mechanism the same contract records as already failing. | open | Measured: docs/archive/CHANGELOG-pre-10.3.0.md:23,24,133; docs/archive/CHANGELOG-pre-12.0.0.md:21,93,168,169; CHANGELOG.md:365-366. Derivation counts measured by running the shipped `derive_category_hits` over the six spans (3/15/12/5/4/8). |
| 2 | medium | agents/evidence/analysis/release-head-derivation-recall.md:77-78 | The headline "1 derived / 45 in-category" matches neither definition the same file uses, and it is the number three other artefacts quote. Running the shipped rule over the six spans gives 46 correctness hits (47 total, incl. 1 legacy security-only hit in `10.1.0..10.2.0`). § 3 states the criterion fires 46 times with 44 hand-confirmed and names the two judged misses. § 1's hand column subtracts one of them (`f525ed3`, span 6, row reads 7 not 8) and not the other (`f87240d`, span `10.1.0..10.2.0`, row reads 14 = the mechanical count). So 45 is the mechanical total minus exactly one of two known false positives — neither 46 (mechanical, which is what § 1a's stated criterion computes) nor 44 (hand-confirmed). | open | Verified `f87240d` is `fix(contracts)` touching `src/scripts/check_rule_projection_integrity.ts` and first contained in 10.2.0, i.e. mechanically caught. Number replicated at src/scripts/_lib/release_highlights.ts:152, docs/contracts/CHANGELOG-conventions.md:77, tests/scripts/check_release_highlights.test.ts:127. |
| 3 | medium | src/scripts/_lib/release_highlights.ts:101-102 | `_commit_type` compares case-sensitively against `'revert'`, so git's own revert subject `Revert "<original subject>"` yields type `Revert` and never matches. The documented correctness half — "a `fix` or `revert` commit touching executable surface", stated identically in the source docblock, the analysis § 1a and the contract — therefore does not apply to the revert form `git revert` generates by default; only a hand-written `revert(scope):` is caught, and only that form is tested. A revert of shipped behaviour is the commit class most likely to be a correctness repair, so the miss is silent and lands on exactly the failure this branch exists to remove. | open | src/scripts/_lib/release_highlights.ts:170; test covers only the conventional form at tests/scripts/check_release_highlights.test.ts:174. Zero `Revert "` subjects exist in 10.0.0..12.0.0, so the gap is forward-looking and unmeasured, not historical. |
| 4 | medium | src/scripts/_lib/release_highlights.ts:89-98 | The executable-surface criterion carries a second recall limit that is never named. `.github/actions/*/action.yml` (composite CI actions, present in this repo) is shipped executable surface matched by neither `_EXECUTABLE_SUFFIX` nor `_EXECUTABLE_PREFIXES`, which lists only `.github/workflows/`. Shebang scripts with no extension are also invisible: `scripts-run` and `src/scripts/agent-config` (a `#!/usr/bin/env bash` entry point) both fail the suffix test. The analysis names one recall limit explicitly (the author's own conventional type, § 1a) and none other, so a reader takes the path set as complete. | open | Confirmed `.github/actions/*/action.yml` and the two extensionless bash entry points exist in the branch tree. Precision was measured; recall of the path predicate was not. |
| 5 | medium | agents/roadmaps/road-to-inbox-harvest-2026-08-c-release-head-truth.md:132-147 | AC 3 is left open carrying an explicit maintainer decision ("accept the false-positive reading and close this, or re-cut the criterion") that is not registered in `## Blockers`. The roadmap uses the blocker mechanism correctly for the publish-side question one section above, so the surface the dashboard advertises (`agent-config gates`, "9 need you") will show one blocker for this roadmap and not this decision. A decision recorded only in an acceptance-criterion body is invisible to the mechanism built to surface decisions, and this one gates the roadmap's closure. | open | agents/roadmaps-progress.md shows this roadmap with 1 blocker and 1 open step; the open step is the AC checkbox, which carries no blocker anchor. |
| 6 | medium | docs/contracts/CHANGELOG-conventions.md:101-117 | The contract's published marked-line rate is stale by three releases and the branch had the data to refresh it. Standing text: "3 of the 3 releases since, 7 marked lines", with a table ending at 10.3.0. Measured now: 10.1.0 (2), 10.2.0 (3), 10.3.0 (2), 10.4.0 (1), 11.0.0 (1), 12.0.0 (2) = 6 of 6 releases, 11 marked lines. The section is date-stamped 2026-08-13, which mitigates but does not remove the drift: this branch edited the paragraph immediately above it after measuring all six spans and after reading the era archives specifically (analysis § 0), so the refreshed number was in hand. | open | Counted from docs/archive/CHANGELOG-pre-10.3.0.md, docs/archive/CHANGELOG-pre-12.0.0.md:21,93,168,169 and CHANGELOG.md:365-366. Same evidence as finding 1. |
| 7 | low | agents/evidence/analysis/release-head-derivation-recall.md:169-173 | § 4's per-span split of the six added honest-null hits contradicts § 1 and the shipped rule. It reads "2 in `11.0.0..12.0.0`, 3 in `10.1.0..10.2.0`, 1 in `10.3.0..10.4.0`". Measured: the widened rule derives 1/3/0/3/0/2 across the six spans against the old rule's 1/1/0/1/0/0, so the additions are 2, 2 and 2. The split is wrong under both readings — as additions (2/2/2) and as post-widening totals (2/3/3) — while the total of 6 and the conclusion are right. | open | Measured by running the shipped `derive_category_hits` over the six spans; § 1's own table gives the same 2/2/2 by subtraction. |
| 8 | low | agents/evidence/analysis/release-head-derivation-recall.md:4,106,110,124,130 | Every `release_highlights.ts:NNN` citation in the analysis is stale in the merged tree, because this branch moved the code the analysis cites. `:106` (`derive_category_hits`) is now 158, `:116` (the security rule) is now 171, `:137` (the honest-null rule) is now 193, `:101` (Known limitations) is now 145, `:103-104` (the conservative-stance comment) is now 147-148. The file's SHA pin to `origin/main c37cb8de5` mitigates for a careful reader, but the sentences are present tense and read as pointers into the current file. | open | The roadmap's Context table at lines 32-33 carries the same `:116`/`:137` citations without a SHA pin; those lines are untouched by the diff but were made stale by it. |
| 9 | low | src/scripts/_lib/release_highlights.ts:101-102 | `_commit_type` extracts the leading `\w+` without requiring the conventional `:` or `(scope):` separator, so it disagrees with the `scopeMatch` regex three lines below it in the same function and with all three prose descriptions of the rule ("conventional type `fix` or `revert`"). A non-conventional subject such as `fix the flaky runner` classifies as type `fix`. No such subject exists in the six measured spans, so this is a documentation-versus-behaviour mismatch rather than a live false positive. | open | src/scripts/_lib/release_highlights.ts:166-170; measured 0 subjects that begin with `fix` or `revert` outside the conventional form in 10.0.0..12.0.0. |
| 10 | low | tests/scripts/check_release_highlights.test.ts:235-242 | The negative test for the recorded-null forms exercises only subjects with no verb-plus-`null` shape (`is null by design`, `a nullable owner field`). The loosest new pattern is applied to subject *and body*, and bodies are where it will actually match — a commit body recording a null value ("records null token_delta") would derive as an Honest null. No body-level near-miss is pinned. Precision over the six measured spans is 9 of 9, so this is a coverage gap rather than a live defect. | open | Pattern at src/scripts/_lib/release_highlights.ts:115; applied to `subject + body` at :192-193. |
| 11 | low | docs/contracts/CHANGELOG-conventions.md:80-81 | "On those two labels `_none_` could not be contradicted and shipped uncontested on five of six curated heads" holds for `Security and correctness` (5 of 6) but not for `Honest nulls`, which derived under the old rule on three of the six spans and shipped a derived line on those heads. The sentence attributes one label's rate to both. | open | docs/archive/CHANGELOG-pre-10.3.0.md:24,133 and docs/archive/CHANGELOG-pre-12.0.0.md:93 carry shipped Honest-nulls derived lines; analysis § 1 gives old-rule Honest-nulls hits of 1/1/0/1/0/0. |
