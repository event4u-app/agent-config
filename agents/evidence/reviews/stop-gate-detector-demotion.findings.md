# Findings: stop-gate-detector-demotion
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: bf9a9f1890d1f8658a7dd97ff33a2f7c0fe06057eee2f4c69171fc2561422c7b | diff: 1b020ca38ecb860101b7661786513646b9e6a87f | reviewer: r2-fresh-subagent-stop-gate-detector-demotion | prompt_hash: 1dd50edf42e0b799b00c4ada9627615986bbd33c2b10fbfddf73b9bcc7591df7 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 1b020ca38ecb860101b7661786513646b9e6a87f
  scope_hash: bf9a9f1890d1f8658a7dd97ff33a2f7c0fe06057eee2f4c69171fc2561422c7b
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T23:29:43Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/check_estate_count.ts:462 | The reasoned-raise carve-out is unbounded and scoped to the raising change only, so it opens a fresh path to the exact permanently-red trunk this change cites as its own justification. `reasonedRaise` exempts a metric whenever base<head carries a reason, with no check that the new baseline equals the live count, so a raise may bank arbitrary headroom and still exit 0 — pinned at +2 by `tests/scripts/check_estate_count.test.ts:404` (live 3, baseline raised to 5, expects 0) and at +6 by the self-test case at `check_estate_count.ts:613` (live 3, baseline 9). Once such a change lands, the following change detects no raise, so `reasonedRaise` is empty, `unwalked` is non-empty, and that PR plus main fail on drift they did not cause. Bounding it (for a raised metric require `after === live`, i.e. fail when the raise overshoots) keeps a legal raise satisfiable without leaving headroom behind. | open | |
| 2 | medium | src/scripts/check_estate_count.ts:25-27 | The file header still documents the opposite of the new behaviour: "A count BELOW the baseline is green and prints the free tightening, in the shape `check_preamble_payload_budget` already uses". After this change a below-baseline count is exit 1, so the gate's own top-of-file contract now contradicts its code, and the cross-reference misdescribes both gates. This is the same stale-header defect the diff corrects in `turn_end_gate_hook.ts`, left in the file it actually changed. | open | |
| 3 | medium | src/scripts/check_estate_count.ts:723-727 | The new failure class has no `GateLedger` row. Each metric task fails only on `growth` and otherwise calls `ledger.complete(metric)`, so an un-walked tightening exits 1 while the ledger reports every planned target complete and zero failures — in text mode and in `--json`, where the ledger is the stderr audit surface. A red exit over a fully green completeness ledger is unreadable for exactly the audience the ledger exists for; `ledger.fail(metric, ...)` on a tightening would keep the two consistent. | open | |
| 4 | medium | src/scripts/check_estate_count.ts:386-395,462 | The carve-out silently disappears whenever the raise half skips, which makes the legal act it protects unsatisfiable in that state. When `raiseSkipReason` is set (no base ref resolved, or the budget is absent/unparseable at the base ref) `raises` is empty, so `reasonedRaise` is empty and a legitimate reasoned raise is refused as un-walked tightening with no green path except abandoning the raise. The two halves also treat the same missing information oppositely — the raise check skips permissively while the derived tightening check hard-fails — and this state was green before the change. | open | |
| 5 | medium | src/config/hook-latency-budget.json:18 | The derivation names an authority whose rule yields a different number. `rule` cites `concern-activation-policy.md` § Deriving a threshold as "the observed legitimate distribution's p99, plus headroom … max + ~11%", but that section reads "the observed legitimate distribution's 99th percentile, plus one". Over the recorded distribution p99+1 is 158 ms, not 175 ms. The substitution of "+1" with "+11%" is unacknowledged, so the block does not support its own cap against the rule it invokes — either restate the applied formula as a deliberate departure with its reason, or take 158. | open | |
| 6 | medium | src/scripts/hooks/turn_end_gate_hook.ts:1027-1030 | The comment immediately above the detector array still asserts the unconditionality this diff exists to correct: "Every detector runs on every turn-end. The gating is INSIDE each one — no promise, no pin mismatch, no unverified edit ⇒ no finding". It claims no allow path outside the detectors and enumerates three of four, three lines above the two `dispatchOpen ? null :` ternaries and in direct contradiction with the header block added at :26-33. The new D comment even defers to "the note at the top of this loop", which sits below this sentence, so a reader meets the false claim first. | open | |
| 7 | low | src/config/hook-latency-budget.json:19-24 | `measured_failure_rate` claims "5 of 33 tests.yml runs on main" but the value list beside it records only four red main-run values; the fifth red value is labelled `pr_run_comment_only_diff`, i.e. not a main run. Only 10 of the 33 cited runs carry a recorded value at all, with no stated selection rule, so "with n=10 the p99 is the max" is a max over roughly 30% of the population the rule asks for — the true max over 33 runs is unrecorded and may exceed 157 ms. | open | |
| 8 | low | src/config/hook-latency-budget.json:25 | Both percentages in `spread` are wrong: 42 ms is 24% of the new 175 ms cap and 28% of the old 150 ms cap, not "28% of the new cap and 37% of the old one". The quoted 28% is in fact the old cap's figure and 37% matches neither cap (42/115 — the fastest observed run). Wrong arithmetic inside the block whose sole purpose is making the cap auditable. | open | |
| 9 | low | src/config/hook-latency-budget.json:28 | `revisit_if` fires inside the variance it was raised to absorb. A green run above 160 ms sits 15 ms below the new 175 ms cap and only 3 ms above the observed max, well inside the 42 ms run-to-run spread the same block attributes to runner class — so the first green 161 ms run both passes the gate and trips a trigger that declares "that is no longer runner variance". As written the condition contradicts the derivation it closes. | open | |
| 10 | low | src/scripts/check_estate_count.ts:153 | An exported field changed meaning while its doc comment did not. `EstateVerdict.tightened` is documented as "Counts strictly below their baseline" but is now assigned the post-carve-out subset (`tightened: unwalked`, :469), so a `--json` consumer reads `tightened: []` for a state that is below baseline and cannot distinguish "no tightening" from "tightening exempted by a reasoned raise". Keeping both (e.g. `tightened` plus `unwalked`) preserves the machine surface. | open | |
| 11 | low | src/scripts/check_estate_count.ts:502 | `selfTest()` exists to "prove, on demand, that this gate's rejections still fire against its own CLI", yet the rejection class this change adds has no self-test case and `SELF_TEST_MIN_REJECT` stays at 4, so the truncation floor cannot notice the omission. The vitest suite does cover it at the CLI, but the contributor-facing self-test now under-covers the gate's rejections by one class. | open | |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
