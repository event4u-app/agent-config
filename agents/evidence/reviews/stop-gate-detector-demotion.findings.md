# Findings: stop-gate-detector-demotion
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: 1c84f6f56aff0a64ede24c62c6e6cb1c5178203153536754f176eb4e78dc16b8 | diff: 5ae35caaea12523660adc4b9f86b22b0ef478e43 | reviewer: r2-fresh-subagent-stop-gate-detector-demotion | prompt_hash: 5e1cc4010805c267a9e07e3bf54f9a2e44d029089137458acdcbc0cd903ec468 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 5ae35caaea12523660adc4b9f86b22b0ef478e43
  scope_hash: 1c84f6f56aff0a64ede24c62c6e6cb1c5178203153536754f176eb4e78dc16b8
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T22:28:45Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | docs/contracts/turn-end-detector-demotion.md:127-141 | The per-detector sample floor is never reconciled with the 90-day refusal-state TTL the same roadmap shipped (`pruneAgedRefusalState`), which deletes a session record 90 days after its last refusal and therefore caps accumulation at 90 days. On this branch's own published reading (promissory 5 refusals over the 5-day window 2026-08-12..08-17, about 1/day) detector A needs roughly 100 days to reach condition 1's 100 eligible refusals, and dormant D needs unbounded time, so their eligible corpus is pruned before the floor is met and neither bar can ever be read. That is the bar-that-cannot-fire shape this same document rejects at :71-77 for Q2's denominator, reintroduced through retention instead of through the denominator. No clause names the interaction. | fixed | fix 403084968 · TTL: agents/roadmaps/archive/road-to-stop-gate-honesty.md:118-125; floor condition 1: :131 |
| 2 | medium | docs/contracts/turn-end-detector-demotion.md:122-126 | The differentiated-bars `Revisit-if` is unreachable as written: it is conjunctive on "all four Q2 medians within +/-1 **and** all four Q1 shares within +/-10 points", while :48 declares Q1 zero by construction and not measurable until an instrument that does not ship here lands, and "the first qualifying sample" additionally requires dormant D to meet its own floor. So the one escape clause that could collapse the four bars to one can never fire - an unfalsifiable revisit condition inside a document whose stated purpose is being falsifiable rather than aspirational (:199). | fixed | fix 403084968 · Q1 inert: :48-66; instruments unshipped: :197-206 |
| 3 | medium | src/scripts/hooks/turn_end_gate_hook.ts:23-25 | The header block added by this diff states detector D "landed under conformance round 7 Phase 1, in the same unconditional list as the other three", but `main()` gates D on `dispatchOpen` at :1039 and A likewise at :1029, and the comment at :969-975 in the same file documents that narrowing explicitly ("Applied per detector below"). Two of the four detectors are therefore conditional, and the new sentence asserts the opposite. This is precisely the stale-header shape the very next paragraph (:27-31) claims to be correcting. | fixed | fix 403084968 · :1029, :1039, :969-975 |
| 4 | medium | docs/contracts/turn-end-detector-demotion.md:171-183 | The contract models only two allow paths (layers 1 and 2) and never models the third, `dispatchOpen`, which suppresses A and D for the whole turn whenever a subagent dispatch is open. Two consequences: (a) "Detector D is dormant, not successful" enumerates three explanations for D's zero (rare violation, subsumed by A or C, dead code) and omits the fourth the code supplies, in a suite whose delegation policy dispatches by default, so a configuration artefact is presented as an exposure fact; (b) the attribution rules at :79-92 declare only "a retry aborted for an unrelated reason" as censored, so A's and D's Q2 counts are censored by a condition the standard does not declare, biasing both denominators downward. | fixed | fix 403084968 · src/scripts/hooks/turn_end_gate_hook.ts:980-984, :1029, :1039 |
| 5 | medium | docs/contracts/turn-end-detector-demotion.md:134-136 | Sample-floor reset condition 4 scopes the reset to "that detector's logic, or the refusal instrumentation", which misses every shared path that changes a detector's exposure without touching either: layer 1 (`stop_hook_active`), layer 1b (`dispatchOpen` and `openRecordStats`' own TTL filter), layer 2's session-key/ordinal path (whose documented cross-session collision under-refuses), and `TRANSCRIPT_READ_MAX_BYTES`. A window can therefore be invalidated with no reset. In the other direction the clause is too wide: instrument 1 writes `would_refuse_again` counts onto the session record, which *is* a change to the refusal instrumentation, so shipping the enabler Q1 waits on resets qualification for all four detectors - meaning no window accumulated before it lands is ever readable, a qualification the "Q2 is measurable today" claim at :76 does not carry. | fixed | fix 403084968 · :197-206; src/scripts/hooks/turn_end_gate_hook.ts:950, :980-984, :1018 |
| 6 | medium | agents/roadmaps/archive/road-to-stop-gate-honesty.md:151 | The roadmap is archived as `completed` 7/7 in the same change in which AC-1 (:151) and AC-3 (:375) are stated as unmet, and the archive index publishes it as `completed` with no trace of that. AC-3 is delegated to a named open roadmap, which is a real carrier; AC-1's colleague-machine reading has none, and neither do the two instruments the new contract says it is waiting on (contract :197-206). Archiving removes the last tracked surface that could schedule them, so the standard's only stated route out of inertness has no owner. The `[~]` to `[ ]` glyph correction recorded at :186-231 is what cleared the deferred-resolution gate that would otherwise have forced this to a decision. | fixed | fix 403084968 · contract :197-206; archive/INDEX.md row; roadmap :369-375 |
| 7 | low | docs/contracts/turn-end-detector-demotion.md:104-116 | The stated rationale does not produce the registered table. "C is highest" is false as read - D ties C exactly, at 40 percent and 3 - and "A and D sit high" groups two detectors the table separates (A 30 percent, D 40 percent). D, which has never fired, ends up carrying the hardest-to-cross bar under a reason that belongs to C alone ("the detector doing the most work is the one to demote last"). A reader deriving the bars from the rationale would produce a different table, which matters in a document that calls the numbers policy choices to be argued rather than findings. | fixed | fix 403084968 · bars table :95-100 |
| 8 | low | docs/contracts/concern-activation-policy.md:99-108 | The new paragraph tells the reader the demotion contract "records the two ways such a standard is born inert", naming a friction metric whose denominator makes the bar unfireable as the second. In that contract only Q1 is born inert; the denominator hazard is identified and deliberately avoided by definition ("never across all sessions"), and the bars table marks only the Q1 column inert. A reader following the pointer for two inert quantities finds one. | fixed | fix 403084968 · turn-end-detector-demotion.md:71-77, :93-100 |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
