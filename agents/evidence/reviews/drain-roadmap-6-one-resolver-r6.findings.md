# Findings: drain-roadmap-6-one-resolver-r6
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: 2ae6b12bf95d03f236c55ce7f8d4f29b860f35c3c7db89058d5c9cc56c0e9763 | diff: 9474c9335b6fca72a601217b9fbd9f1e18a3aa4a | reviewer: r2-fresh-subagent-drain-roadmap-6-one-resolver-r6 | prompt_hash: 954981d48e83c687b6eadefd2e4c0b4499a5b5c1716eaf91c1c1408a7234ab6b -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-roadmap-6-one-resolver-r6"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 9474c9335b6fca72a601217b9fbd9f1e18a3aa4a
  scope_hash: 2ae6b12bf95d03f236c55ce7f8d4f29b860f35c3c7db89058d5c9cc56c0e9763
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: 65aad95c26aa53a76cf32fea562e41761f935406cd9ed56cde53c221d71df6c3
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T13:22:05Z
-->


> **REBOUND 2026-08-29 to the post-merge scope, and the reason is recorded here
> because a rebind is exactly how a stale review gets hidden.** This artefact was
> authored against scope `4d4045889d90…`, head `f4860a313`. The scope hash then
> moved **without either reviewed file changing**: `origin/main` advanced under
> the branch when three sibling PRs merged, and the review scope is computed
> from the diff against it.
>
> Verified before rebinding, not asserted: `git diff --quiet f4860a313 HEAD --`
> over `src/scripts/_lib/one_resolver_invariant.ts` and
> `tests/scripts/one_resolver_invariant.test.ts` exits **0**, so both files are
> byte-identical to what round 6 read. The review's subject is unchanged; only
> its coordinates are. No finding below was re-derived, softened, or dropped.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | tests/scripts/one_resolver_invariant.test.ts:398 | NON-BLOCKING, and the sharpest thing in the delta. Of the four new arms in `describe('round 5: a .tsx module is parsed AS JSX')`, **three go red with the fix reverted and one does not**. The insensitive arm is the one that carries the round-3 half of the blocking finding: `'a backtick inside JSX TEXT does not hide the router below it'` uses ``<p>use `npm run x`</p>`` — a **balanced pair** of backticks, which closes the template literal on the same line, so the round-3 defect class (a template opener that does not end at a newline) never fires. Round 5's own reproducer was ``<p>press ` to open</p>`` — an ODD backtick. Measured, same tree, same router body, `declaresRouter(src, 'src/ui/Nav.tsx')`: shipped arm returns `true` under BOTH the shipped parser and a parser with the ScriptKind expression reverted to `ts.ScriptKind.TS`; round 5's odd-backtick source returns `true` shipped and **`false` reverted**; a three-backtick variant likewise `true`/`false`. So the arm is a tautology with respect to the property it is named for. Consequence, and it is narrow: the guard itself is correct — reverting the fix is still caught, by arms `a`, `c` and `d`, **all three of which exercise the `/*` comment-opener class** (`src/*/ui`, `src/*/ui`, `a/*b`), so **zero** arms pin the backtick/template class in `.tsx`. The over-claim is in the artefacts around the test, not in the count: the roadmap's `'three tests go red without it'` is accurate, but the block comment above the arms says `'These arms carry real JSX, which is the actual discriminator'` and the roadmap names both classes (`'a `/*` in JSX text opening a block comment, a backtick in JSX text opening a template'`) as what the new arms close. One of the two is unpinned. This is the second council principle recurring for the fifth consecutive round — testing a weakened spelling of the reproducer rather than the violated property — which is why it is recorded at `medium` rather than `low` despite opening no hole. Repair is one character class: use an odd backtick, e.g. ``<p>press ` to open</p>``, which is round 5's measured discriminator. | accepted-risk | probe: shipped arm and round-5 reproducer run against the shipped module and against a scratch copy with `const kind` forced to `ts.ScriptKind.TS`; 4 arms x 2 variants, plus a 3-backtick mutation. Shipped arm PASS/PASS, r5 reproducer PASS/FAIL, arms a/c/d PASS/FAIL — DISPOSITION: recorded, not repaired. The review loop was terminated at round 5 on blocking findings only, and this round confirms the blocking one closed. Repairing these would move the review scope again for changes that open no hole; the reviewer states the medium is small enough to land without re-reviewing guard behaviour. |
| 2 | low | src/scripts/_lib/one_resolver_invariant.ts:79 | NON-BLOCKING, disclosure fidelity. Recorded limit 2 carries only the FIRST of the two measured halves of round-5 finding 3. It states the `exportsRouterFunction` / `exportedNames` namespace asymmetry (verified: `export namespace L { export function classifyLadder(x: unknown) { return x; } }` gives `declaresRouter=true`, `exportsRouterFunction=false`). Round 5 also measured, in the same function and flagged as `'a second, unrelated false positive ... verified'`, that a **factory-wrapped resolver is REJECTED** because a `CallExpression` initializer is not in the callable set. Re-measured against the shipped module, three idiomatic spellings all return `exportsRouterFunction=false`: `export const classifyLadder = memoize((x) => x);`, `... = once(function (x) { return x; });`, `... = withTelemetry(() => 1);`. On the sanctioned file that is a false POSITIVE — `resolver-is-not-a-resolver` on a genuine resolver — i.e. the direction round 4 named as `'a gate that reds on a legitimate refactor is a gate that gets bypassed'`. Round 5's own disposition for finding 3 reads `'recorded as a disclosed limit in the module frozen-claim block'`, and this half is not in that block. It is also not covered by the neighbouring docstring at :244, which enumerates `'a string, a number or an object literal'` as the stub set and says nothing about a call expression. Cost of the fix is one clause in limit 2. | accepted-risk | probe: three wrapper spellings against the shipped `exportsRouterFunction`, all false; namespace-asymmetry half re-measured true — DISPOSITION: recorded, not repaired. The review loop was terminated at round 5 on blocking findings only, and this round confirms the blocking one closed. Repairing these would move the review scope again for changes that open no hole; the reviewer states the medium is small enough to land without re-reviewing guard behaviour. |
| 3 | low | src/scripts/_lib/one_resolver_invariant.ts:84 | NON-BLOCKING, disclosure fidelity, same shape as finding 2 and with a live carrier class. Recorded limit 4 states only the non-exported-namespace half of round-5 finding 5 (verified: `namespace Internal { export class CouncilTopologyRouter {} }` plus `export const used = 1` reports `second-resolver`). The omitted half is the ambient one — `declare module "vendor-x" { export class CouncilTopologyRouter {} }`, an ambient declaration DESCRIBING a third party's API, which declares nothing in this tree — re-measured against the shipped module as `declaresRouter=true`, so it too would report `second-resolver` on code that holds the invariant. Round 5 named this one specifically as `'the plausible future trigger'` for a `.d.ts`-style declaration, and the live scan reaches that class today: `checkOneResolver` on this repo returns 1271 scanned files of which one is a `.d.ts` (`src/scripts/_lib/node_sqlite.d.ts`). The half recorded in the module is therefore the half with no live carrier, and the half dropped is the one with one. Same one-clause fix. | accepted-risk | probe: ambient `declare module` form RED against the shipped module; live `checkOneResolver` scan shows 1271 files, 28 `.tsx`, 1 `.d.ts`, 0 violations — DISPOSITION: recorded, not repaired. The review loop was terminated at round 5 on blocking findings only, and this round confirms the blocking one closed. Repairing these would move the review scope again for changes that open no hole; the reviewer states the medium is small enough to land without re-reviewing guard behaviour. |
| 4 | low | src/scripts/_lib/one_resolver_invariant.ts:156 | NON-BLOCKING, and the honest cost of an otherwise complete fix. Deriving the script kind from the extension closes the class for **valid** TSX — swept 12 legal TSX constructs (fragment, self-closing, template-valued attribute, regex-valued attribute, a `/* */` inside an expression container, a generic component, escaped `</`, apostrophe plus quote plus `/*` in text, multiline JSX, JSX under `export default`, a `<`/`>` ternary, a `satisfies`-wrapped JSX arrow), each above a literal router: **12 of 12 SEEN, 0 parse diagnostics**. It also introduces a narrower silent-green class that did not exist before the delta, because the guard never inspects `parseDiagnostics`: a `.tsx` whose source does not PARSE as TSX loses its exports with no signal while still being counted in `scanned`. Measured, router hidden under the shipped module and seen under the reverted one: `const f = <T>(x: T) => x;` (a generic arrow without the `.tsx`-required trailing comma), `const a = <string>someVal;` (an angle-bracket assertion, forbidden in `.tsx`), and an unclosed JSX tag. All three are non-compiling source, and `tsc --noEmit` is a CI gate, so a second resolver cannot sit behind them in a green tree — which is why this is `low` and not the round-5 class repeating. It is worth a line because round 5 measured the opposite property under the TS parser (`'syntactically broken source ... a broken file still yields its exports'`) and recorded it as acceptable, that measurement no longer holds for `.tsx`, and recorded limit 5 — which does cover the sibling silent-green cases, UTF-16 and symlinked directories, both re-verified as stated — does not mention it. | accepted-risk | probe: 12-arm legal-TSX sweep, 12 SEEN / 0 diagnostics; 3-arm invalid-TSX sweep hidden-under-fix and seen-under-revert; live blast radius across all 1271 scanned files = 0 names gained, 0 lost, identical `scanned`, 0 violations under both variants — DISPOSITION: recorded, not repaired. The review loop was terminated at round 5 on blocking findings only, and this round confirms the blocking one closed. Repairing these would move the review scope again for changes that open no hole; the reviewer states the medium is small enough to land without re-reviewing guard behaviour. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
