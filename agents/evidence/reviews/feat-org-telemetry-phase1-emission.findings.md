# Findings: feat-org-telemetry-phase1-emission
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: 3b9732275dd08dd97d05fabaab881576e334e486417ede8d8422853af95b68c3 | diff: d9165fe086012703aa1ff25b19239c7447de5345 | reviewer: r2-fresh-subagent-feat-org-telemetry-phase1-emission | prompt_hash: 0f5c59414b39bd005fe1cbe1f2375a5c40ed3329d6de5b0b73218ec8db2aa507 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: d9165fe086012703aa1ff25b19239c7447de5345
  scope_hash: 3b9732275dd08dd97d05fabaab881576e334e486417ede8d8422853af95b68c3
  roadmap: agents/roadmaps/road-to-org-telemetry.md
  roadmap_hash: 5aae2970114892c403c5810b63040adc9a42fb1f2e5ea87ec3dc3aa0b8699f20
  ac_hash: 62d3483b628c50db130545b5ffbacbfd2cbdc618a6f39774b7b6465d1176fc1a
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T21:57:00Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/telemetry_usage_hook.ts:125 | `extractRuleTier` scans for the **superseded** key. `agent-settings.md:61-66` states `discipline_profile` "WINS over the legacy rule_loading_tier below" and that the installer fills it per preset (`balanced`→`auto`); the shipped template carries `discipline_profile: auto` at line 66. So on a normally-installed consumer the emitted `rule_tier` names a knob that does not decide which surfaces load, and the roadmap's "active tier" claim (roadmap:83) is not what gets measured. The function's own safety argument — "`build_class_a_record` rejects anything outside that enum, so a mis-parse produces a dropped field, never a wrong one" — does not cover this case: `balanced` is inside `ALLOWED_RULE_TIERS`, so it is accepted while being the wrong dimension. A measurement surface whose purpose is replacing a wrong number with a trustworthy one ships a systematically misleading one. | fixed | a1b3177cd — field is now `discipline_profile` with the legacy mapping as documented fallback; a test asserts a legacy value is REJECTED so the two enums cannot be confused again |
| 2 | medium | src/scripts/hook_manifest.yaml:480 | `tools: [Skill]` is bound on six platforms (manifest:789, 797, 851, 874, 889, 925) but the hook fires only when `extractToolName(payload) === 'Skill'` (hook:186), and a tool named `Skill` is a Claude-family surface. On augment/cursor/cline/gemini the concern is inert, so a zero from those installs is indistinguishable from non-adoption — the exact blind-zero class this roadmap exists to remove, reproduced one layer up. Phase 0 spike 1's 164/164 evidence is single-host and cannot support the other five bindings. Neither the manifest comment, the hook header, nor roadmap:83 records the limitation; roadmap:83 reads "bound on all six platforms carrying `post_tool_use`" as coverage. | accepted-risk | a1b3177cd — recorded in the manifest as a Phase 4 reading caveat instead. Every record carries `host`, so the denominator is reconstructable per host; unbinding would convert a readable absence into a guaranteed one, which is the blind zero this roadmap exists to remove |
| 3 | medium | src/scripts/hooks/telemetry_usage_hook.ts:112 | `extractSessionId` returns `''` when no session id is present, and `derive_session_hash` (remote.ts:143) hashes the empty string into a valid, stable digest rather than refusing. Every session-id-less record across an entire org therefore shares one `session_hash`, collapsing into a single apparent session. This contradicts the module's own stated doctrine — `package_version` and `rule_tier` record `null` "rather than a guess" (hook:141, roadmap:83) — and the failure is invisible in the data because a digest is not legible as a fallback. Phase 6's "at least three distinct sessions" threshold and any distinct-session denominator are silently deflated. | fixed | a1b3177cd — `derive_session_hash` returns null on an absent id rather than the empty-string digest |
| 4 | medium | src/agent-src/templates/agent-settings.md:644 | The `salt` row instructs the operator that it "Belongs in the machine-local override layer, never in a committed `.agent-settings.yml`" (repeated in the example block at :536), but `read_remote_settings` (settings.ts:293) resolves exactly one file and the hook passes `<consumer_root>/.agent-settings.yml` (hook:61, :164). `.agent-settings.local.yml` is a real, separately-gitignored layer (gitignore-block.txt:44) that this reader never consults, and no user-global layer is read either. An org admin following the documentation gets a silently inactive install: `missing` computes the diagnostic (settings.ts:270) but no caller surfaces it, so the failure mode is "no records, no message". | fixed | a1b3177cd — both the example block and the key row now name the file the reader actually resolves |
| 5 | medium | src/agent-src/templates/agent-settings.md:641 | Six new consent-bearing settings keys ship with no settings-class declaration — the changed-file list touches no settings-class contract, not `src/config/agent-settings.template.yml`, and no schema. Nothing therefore constrains who or what may write `telemetry.remote.enabled`. The activation gate is value-shaped (`enabled` + `endpoint` + `org_id` + `salt`, settings.ts:284) rather than provenance-shaped, while AC4 requires "a consent-bearing provenance record" and the roadmap Context names the existing human-chosen-vs-auto-detected provenance machinery as already available for reuse. Deferring the provenance check to Phase 3 is defensible, but no marker in the code names the line that must gain it. | deferred | Phase 3 owns the provenance gate and its exit criterion already states it. The class-contract half does not attach: `lint_settings_classes` is green, and the sibling `telemetry.artifact_engagement` namespace carries no row either — the contract covers keys in the SHIPPED template, which this family deliberately is not |
| 6 | low | src/agent-src/templates/scripts/telemetry/remote.ts:39 | `ALLOWED_RECORD_CLASSES` is exported and never referenced; `build_class_a_record` hardcodes `record_class: 'usage'` (remote.ts:204). The "Closed vocabulary — see `ALLOWED_RECORD_CLASSES`" contract the interface cites (remote.ts:60) is enforced by nothing, and the constant that exists specifically to bound Phase 5's second class will not bound it when that class arrives. | fixed | a1b3177cd — `record_class` is an input validated against `ALLOWED_RECORD_CLASSES` |
| 7 | low | src/agent-src/templates/scripts/telemetry/settings.ts:228 | The docstring says "The four fields that must ALL carry a value" and "None of the four has a usable default" (:233) for a three-element `REMOTE_REQUIRED_FIELDS` (:237). `enabled` is the fourth *condition* but is not a member and does have a default (`false`, :297). `agent-settings.md:641` states "all three" correctly, so the two descriptions of one gate disagree, and a reader auditing the privacy posture from the source comment counts wrong. | fixed | a1b3177cd — docstring says three fields on top of `enabled` |
| 8 | low | src/scripts/hooks/telemetry_usage_hook.ts:127 | The regex `^rule_loading_tier:\s*(\S+)\s*$` anchors on end-of-line after the value, so `rule_loading_tier: balanced  # keep` yields `null`. The file it parses is the comment-dense settings template, where inline comments are the prevailing style, and no test covers the trailing-comment shape (tests cover bare, quoted, placeholder, and absent only). | fixed | a1b3177cd — `_scalar` accepts a trailing inline comment, with a test on the template's own commented form |
| 9 | low | src/scripts/hooks/telemetry_usage_hook.ts:231 | `_resolveRoot` trusts `envelope.cwd` verbatim with no ascent toward the project root, so a session started in a subdirectory reads no settings file and the concern is silently inactive on a legitimately enabled install. Neither `main()` nor `_resolveRoot` is exercised by a test — the suite calls `run()` with an explicit `consumer_root` throughout — so the root-resolution branch that production actually uses is untested. | fixed | f13b8a56c — `resolveSettingsPath` walks up to the project root, and the log resolves against that root rather than the session cwd; both paths tested |
| 10 | low | src/scripts/hooks/telemetry_usage_hook.ts:197 | `extractHost(envelope) ?? 'unknown'` fabricates a host id where the same record's sibling fields record `null` for an unresolvable value. `'unknown'` is written as a normal string into the `host` field and becomes an aggregation bucket that cannot be distinguished from a real host id by a downstream reader that does not know the sentinel. | fixed | a1b3177cd — an absent host records null, matching the sibling fields |
| 11 | low | src/agent-src/templates/scripts/telemetry/remote.ts:226 | `append_class_a_record` appends one line per skill invocation with no size cap, rotation, or retention path, while `flush: never` is documented (agent-settings.md:645) as "a legitimate way to run the local half" indefinitely — i.e. unbounded growth is an explicitly endorsed steady state. The roadmap's own Phase 0 note requires Phase 2 to establish "the queue's growth bound across a multi-day outage"; the file Phase 1 creates already has none. | deferred | f13b8a56c — filed as a Phase 2 roadmap step with the R-A7 growth-budget obligation named, and the `flush` docs now state that `never` is an unbounded steady state |
| 12 | low | src/scripts/hook_manifest.yaml:789 | Every `post_tool_use` cell now carries 11 concerns (12 on claude, :797) against the documented `hooks.concern_budget.max_per_event` default of `8` (agent-settings.md:602). The gate is warn-only by default (`hard_fail: false`) so nothing blocks, and the overflow predates this diff — but the diff moves all six cells one further past the documented cap without a note acknowledging it. | accepted-risk | a1b3177cd — warn-only (`hard_fail: false`), exceeded by 2 before this diff, and the threshold is a placeholder awaiting session evidence per its own comment. Now acknowledged in the manifest entry |
