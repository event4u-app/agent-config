# Findings: frontend-skill-application
<!-- completion-review: v1 | reviewed: 2026-08-12 | scope: c2714066cf267193967db04e055c68c1776a51ec6ae693ebc080a118c0f317fe | diff: 396d58fbff2704cddce10577e2cbd82db9c5003c | reviewer: r2-fresh-subagent-frontend-skill-application | prompt_hash: cccf6fbf0f41d8fb6c3d42caa8373c2da50180031965cd568d957697970371e3 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 396d58fbff2704cddce10577e2cbd82db9c5003c
  scope_hash: c2714066cf267193967db04e055c68c1776a51ec6ae693ebc080a118c0f317fe
  roadmap: none
  roadmap_hash: none
  ac_hash: none
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-12T03:25:00Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/rules/ui-audit-gate.md:104 | The nudge does not read either rule's triggers, but four shipped prose surfaces say it does. `ui-audit-gate` ("reads this rule's triggers at session time — the first consumer they have had"), `design-review-after-ui-write.md:98` ("the first thing in the tree that ever consumed this rule's triggers at session time"), the hook header (`ui_route_nudge_hook.ts:6`) and both settings descriptions (`src/server/schemas/settings.ts:518`, `agent-settings.template.yml:1251`) assert a mechanism that no code path implements: the hook imports only `isUiPath`/`isUiTreePath` from `_lib/ui_surface.ts` and never parses `src/rules/*.md` frontmatter. The two sets also diverge — the hook fires on `.html .htm .css .scss .sass .less .astro`, which neither rule declares, and ignores the `keyword:` triggers entirely — so they are parallel constants that can drift, not a consumer relationship. This is an unbacked mechanism claim in consumer-facing rule prose, in the same paragraph that tells the reader to distrust prose and run `hooks:status`. | fixed | 3c20d47d — all six surfaces now say parallel-to, not consumer-of; construct swept, 0 remaining |
| 2 | medium | src/scripts/_lib/ui_surface.ts:64 | `pages/` as a substring fragment reproduces exactly the over-fire the header argues `app/` was excluded to avoid. `isUiTreePath` requires no UI extension, so `pages/api/users.ts` — server-only Next.js code — is a UI write, as is any `docs/pages/…`, `src/components/README.md` <!-- ref-ignore -->, or `packages/x/components/util.ts`. The nudge fires on backend files, and (worse) the same predicate is the pre-registered UI-turn denominator, so the consultation rate is computed over a population that includes non-UI writes. The `app/` exclusion is pinned by a test; the identical `pages/api/` case is not tested at all. | fixed | 36e16322 — pages/api/ excluded from the shared predicate and pinned by test |
| 3 | medium | src/scripts/hooks/ui_route_nudge_hook.ts:99 | The consultation latch is a one-way, session-long substring match over `file + query`, where `query` concatenates `pattern`, `query`, `glob` and `command`. Any incidental hit silences the nudge for the whole session and is recorded as a consultation: reading `src/rules/design-review-after-ui-write.md`, grepping for `design-review` while writing a review artefact, or a `git log -- src/skills/fe-design` all latch it. There is no expiry, no requirement that the read precede the UI write it excuses, and no distinction between reading the skill and reading a file whose path merely contains the string. Both the gate and the metric it claims to share an event stream with are silenced by one false positive. | fixed | 36e16322 — latch narrowed to a non-write touching skills/<surface>/; search text no longer collected |
| 4 | medium | src/scripts/_lib/hook_settings.ts:61 | Section scoping is hard-coded to ≤3-space indentation: `if (inSection && /^\s{0,3}\S/.test(line)) inSection = false;`. With a 4-space-indented settings file (`hooks:` → `    design_slop:` → `        enabled: true`) a sibling section key never closes the current section, so `hookSectionEnabled(root, 'ui_route_nudge')` returns `true` on any later section's `enabled: true` — a default-OFF hook silently turns ON from an unrelated flag, and the reverse false-negative is equally possible. `.agent-settings.yml` is user-editable and YAML does not mandate 2-space indentation. The module is the only new file in this diff shipping with no test, which is why no indentation variant was exercised. | fixed | 36e16322 — section indent tracked from the header; module now has 11 tests incl. the 4-space case |
| 5 | medium | agents/settings/contexts/skill-catalogue-baseline.md:63 | "Observation 1" publishes the load-bearing `no-selector` verdict and states it was "recorded to `agents/evidence/metrics/skill-catalogue.jsonl`", but that file is not among the branch's changed files. The record that gates Phase 2 ("no fix selectable on observation 1") is therefore not reproducible from the branch — a reader cannot check the 16/19/336 counts or re-run `analyzeSelector` against the observation, only re-read the prose asserting them. | fixed | 396d58fb — file IS tracked; invisible to this review because the scope hash excludes agents/evidence/metrics. Baseline now names the inputs and the reproduce command |
| 6 | medium | src/scripts/lint_rule_skill_pack_reach.ts:187 | `unrouted-skill` cannot fire for the case its own header defines. `routedSkills` is built as `new Set(rules.flatMap(r => r.routesToSkills))`, so only skills that are already routed by some rule are examined; a skill that no rule routes to anywhere — the literal reading of "some pack delivers a skill while delivering no rule that routes to it" — is never inspected. The printed count ("still 14") therefore cannot detect growth in the largest instance of the class the check names, and the header's stated scope exceeds what the scan walks. | fixed | 396d58fb — header states the subject set is already-routed skills and why |
| 7 | medium | agents/settings/contexts/skill-catalogue-baseline.md:74 | The published candidate table contains a row — candidate `any frontmatter key`, separates `no`, detail "no key's presence tracks the split" — that the tool structurally cannot emit. `analyzeSelector` pushes a `frontmatter:<key>` candidate only when it separates (`if (!separates) continue;`, capture_skill_catalogue.ts:260), so `formatReport` never prints a non-separating frontmatter row. The table presents hand-written reasoning and tool output in one undifferentiated block, in the document whose stated purpose is that a later rate be comparable against something measured rather than asserted. | fixed | 396d58fb — candidate table sourced per row; the derived row is labelled as read off an absence |
| 8 | low | src/scripts/_lib/ui_surface.ts:68 | `'src/components/'` is dead in `UI_PATH_FRAGMENTS`: `isUiTreePath` uses `String.includes`, and `'components/'` already matches every path containing `src/components/`. The same two values are load-bearing and distinct in the rules' `path_prefix` list, where matching is anchored. One list, two matching semantics, with the redundancy visible only in one of them. | fixed | 36e16322 — src/components/ dropped as dead under substring matching |
| 9 | low | src/scripts/hooks/ui_route_nudge_hook.ts:136 | `writeState` re-reads the state file inside a `try`, falls back to `all = {}` on any parse error, and then writes only the current session — discarding every other session's `consulted`/`nudges`. The file also accumulates one entry per session id forever with no prune, cap, or TTL. | fixed | 36e16322 — bounded at MAX_SESSIONS with a prune, parse-failure branch documented |
| 10 | low | src/scripts/capture_skill_catalogue.ts:121 | `descriptionOf` takes the remainder of the `description:` line verbatim, so a folded or literal block scalar (`description: >-`, or the literal-block form) yields the two-character indicator: `hasDescription: true` with `descriptionLength: 2`. The `description-length` selector candidate is then computed over a corrupted value for any such entry, and a length-based selector could be reported or refuted on noise. | fixed | 396d58fb — block scalars folded before measuring length |
| 11 | low | src/scripts/capture_skill_catalogue.ts:187 | Input validation is asymmetric on a self-reported input: `unknownObserved` is computed from `observedBare` only, while `observedDescribed` is filtered through `known.has(n)` silently (line 189). A typo in `--described` shrinks the described set with no warning and weakens the analysis, while the same typo in `--observed` is surfaced. | fixed | 396d58fb — unknownObserved computed from both self-reported lists |
| 12 | low | taskfiles/ci-fast.yml:386 | The task passes `{{.QUIET_FLAG}}`, but `lint_rule_skill_pack_reach` reads only `--strict` from `process.argv`; the flag is silently ignored. Every `task ci` run prints all 26 known advisory findings plus the scan line regardless of quiet mode, which is permanent output noise for a gate that is expected to stay non-empty for some time. | fixed | 396d58fb — --quiet suppresses per-finding lines, never the count |
| 13 | low | tests/scripts/fe_design_triggers.test.ts:50 | The test titled "keeps at least five queries on each side" asserts `>= 5` for should-trigger and `>= 4` for should-not-trigger — the negative side dropped to four when the implementation query flipped, and the assertion was lowered while the title kept the old claim. Related overfit in the same file: `IMPLEMENTATION_VERBS` (line 42) matches only prompts *starting* with `implement`/`build`/`improve`, which is precisely what lets the one implementation-shaped negative ("change the button colour to green") pass `does not let a near-miss be an implementation prompt` — the verb list, not a stated rule, is doing the work. | fixed | 396d58fb — title matches the assertion; the verb-list limit is stated where it is used |
| 14 | low | agents/roadmaps/road-to-frontend-skill-application.md:105 | Phase 4 Step 1 is marked `[x]` and names `app/` among the prefixes to add; `app/` was deliberately dropped (argued in `ui_surface.ts` and pinned by a test), but the step carries no deviation note. Every other step in this roadmap that landed differently from its text carries an explicit `<!-- landed as … -->` comment, so the one silent deviation reads as completed-as-written. | fixed | 396d58fb — step carries the app/ deviation note |
