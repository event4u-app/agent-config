# Findings: feat-standing-context-40k-phase4
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 3a8b913921438a63ab87a4045f16b6945937e35ced463d0703080488da60173c | diff: b7f6aaed3a0fc4fca256db3521f2714768a4410a | reviewer: r2-fresh-subagent-feat-standing-context-40k-phase4 | prompt_hash: 48fa06caf6b1634caa0a2bb0bff8125493996d72c8c83c73f939e2dd4b86b768 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: b7f6aaed3a0fc4fca256db3521f2714768a4410a
  scope_hash: 3a8b913921438a63ab87a4045f16b6945937e35ced463d0703080488da60173c
  roadmap: agents/roadmaps/road-to-standing-context-40k.md
  roadmap_hash: 1993f815dc5e06a517f2dba14a2e63e4b08cb62cfaf4b7bc0539e1242ce4cd5d
  ac_hash: 4b51f97b062e06a87573430211a06fda68685a963df397874e4d3c1bbd02c49f
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T11:20:08Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/dispatch_hook.ts:1089 | On a host that binds no `user_prompt_submit`, the accumulator NEVER resets and the per-turn cap silently becomes a per-SESSION cap. `opensTurn` is `event === "user_prompt_submit"` and nothing else zeroes `spent_bytes`; `hook_manifest.yaml` binds augment to `pre_tool_use`/`post_tool_use`/`stop` — all three non-exempt — and no `user_prompt_submit`. So on augment `spent_bytes` grows monotonically for the whole session and, once past 10240 B, every advisory message is dropped for the rest of that session (each drop also rewriting the 200-line dispatch-issues ring). The roadmap note asserts the opposite — "a host without that slot gets no turn accounting at all, and the module fails open" — but exemption is per slot, not per host, so the non-exempt slots keep accumulating with no boundary. | fixed | 73f712ee5 |
| 2 | high | src/scripts/hooks/dispatch_hook.ts:1078 | `if (messages.length === 0) return messages;` short-circuits BEFORE the turn reset, so a `user_prompt_submit` that produced no deciding message does not zero the accumulator — even on `claude`. By this branch's own measurement (corpus header, `tests/eval/nudge-interference/user-prompt-submit.yaml`: 463 of 510 routing-matrix prompts fire neither nudge) the empty-message prompt is the COMMON case, so the reset is the exception rather than the rule and bytes leak across turns exactly as in finding 1. `tests/scripts/hooks/turn_injection_budget.test.ts:259` pins the early return as intended, so the suite cannot see it. Fix is order, not policy: decide `opensTurn` and write the zeroed state before the empty-list return. | fixed | 73f712ee5 |
| 3 | medium | src/scripts/hooks/dispatch_hook.ts:1412 | A crashed non-`fail_closed` concern consumes turn budget for text that is never injected. `rc >= 3` is fail-opened to `EXIT_ALLOW`, then `stated` falls back to `stderr_text` (:1439), so the crash output becomes a message with `rc = 0`. When `final_rc` is 0 that message is "deciding", is charged in full to `spent_bytes` — and `host_semantics.emitFor:118` returns `{exit:0, stdout:"", stderr:""}` for `severity === "allow"`, i.e. emits nothing. A stack trace is also typically the largest candidate, and if the crashing concern is `severity: blocking` it is undroppable, so one crash can wedge the cap and trip the `overCapAfterDrops` record on every subsequent fire. | fixed | 73f712ee5 |
| 4 | medium | src/scripts/hooks/dispatch_hook.ts:1499 | The cap runs AFTER `_write_feedback` and `_record_rule_trips` (:1512), so a dropped advisory is still counted as a fire in `rule-trips.json`. That file is the registered instrument for `skill_route_pointer_rate`, `skill_route_bare_suppression_rate`, `full_rewrite_small_diff_rate`, `duplicate_read_rate` and `recycle_advisory_adoption` in this very budget file (`src/config/hook-token-budget.json:59-102`). The diff therefore redefines those numerators from "fired" to "fired-or-dropped" and records nothing about it — a silent change to pre-registered metrics whose whole point is a comparable baseline at the review date. | fixed | 73f712ee5 |
| 5 | medium | src/scripts/hooks/turn_injection_budget.ts:152 | The accumulator is keyed on a session id whose instability is documented as deliberate elsewhere. `readTurnState` returns a zeroed record whenever `doc["session_id"] !== sessionId`; `dispatch_hook._resolve_session_id:158` falls back to `dispatch-<ts>-<pid>` — "Not stable across invocations — that is the point" — for any envelope without `session_id`. On that path every fire reads zero, so the cap is silently unenforced with no signal that it did nothing. The same single per-workspace file also makes two concurrent sessions (or a subagent carrying its own id) mutually zero each other every fire. The estate's own pattern for per-session state is a per-session directory (`agents/runtime/state/end-review-nudge/`). | fixed | 73f712ee5 |
| 6 | medium | src/scripts/bench_hook_injection.ts:184 | The bench comment claims the fixture aggregate is "the reading the runtime enforcer accumulates against", and the two measure different populations. Bench sums every bound concern's `reason + additional_context + context` per slot regardless of exit code (:141-149, :197-199); the runtime sums only messages whose `rc === final_rc`, never reads `context` outside the exempt `session_start`, and counts neither the ` · ` join nor the `hookSpecificOutput` JSON envelope the host actually receives. AC-4's evidence (`turn-aggregate 1140 B`) is therefore not comparable to the number the cap is enforced against, in either direction. | fixed | 73f712ee5 |
| 7 | low | src/config/hook-token-budget.json:50 | The `enforcement` prose describes an ordering the code does not implement and the manifest cannot express: "drops advisory messages — lowest severity first, largest first within a severity". `applyTurnCap:202` has no severity ordering at all (binary `m.advisory && !m.failClosed`, then largest-first), and `hook_manifest.yaml` declares only `advisory` and `blocking` — zero `severity: warn` concerns — so there is no within-ladder step to order by. The positive `m.advisory` test also means a future `severity: warn` concern silently becomes UNDROPPABLE, which is the inverse of what the prose promises; `!blocking && !failClosed` would match the stated policy. | fixed | 73f712ee5 |
| 8 | low | src/scripts/hooks/turn_injection_budget.ts:170 | `writeTurnState` uses a plain `fs.writeFileSync` while the sibling counter in the calling file (`_record_rule_trips`, dispatch_hook.ts:1166) uses `atomic_write_json` for the same class of artefact in the same directory. Concurrent dispatches (parallel tool calls, a subagent fire interleaving with the parent) can interleave the read-modify-write and lose or truncate the accumulator. Fail-open bounds the damage to "no cap applied", which is why this is low rather than higher — but it is an unexplained divergence from the neighbouring pattern. | fixed | 73f712ee5 |
| 9 | low | src/scripts/hooks/dispatch_hook.ts:1079 | Per-fire I/O added to the hottest hook path. Every non-exempt dispatch — `pre_tool_use`/`post_tool_use` fire once per tool call — now re-reads and `JSON.parse`s the whole ~8 KB budget file plus a state read and a state write; each drop additionally does a full read-modify-write of `dispatch-issues.jsonl`, whose 200-line cap then evicts the resolver-failure records the log exists for (`dispatch_issues.ts:190-193`). `applyTurnCap` also recomputes `Buffer.byteLength` per candidate inside both `total()` and the sort comparator rather than measuring once (turn_injection_budget.ts:190-203). | fixed | 73f712ee5 |
| 10 | low | tests/scripts/hooks/nudge_interference.test.ts:93 | The corpus's ≤N direction cannot distinguish "did not fire" from "crashed": `nudgeFires` swallows every exception and returns `false`, so a broken nudge leaves every `max_nudges` class green — including `silent` (cap 0), which is then vacuously true. Only the single `overlap` class carries liveness. The "assertion has teeth" test does not close this either: its final expectation (:190) restates the line above it, since `forced.length` was already asserted to be 2 while `singles[0].max_nudges` is 1 by the filter that selected it. | fixed | 73f712ee5 |
