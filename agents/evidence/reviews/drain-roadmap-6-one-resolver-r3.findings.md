# Findings: drain-roadmap-6-one-resolver-r3
<!-- completion-review: v1 | reviewed: 2026-08-29 | scope: ca18030fd5753cad6d14e29bf451c8b5be3b3e63625f6ec88a3435642083d80e | diff: 0e9599d590310058b4e05e784088abdef589d5e0 | reviewer: r2-fresh-subagent-drain-roadmap-6-one-resolver-r3 | prompt_hash: b787e9cb37b7e15da25c5fadadab1be6b671984435c15076165d361d151040ee -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-roadmap-6-one-resolver-r3"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-29 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 0e9599d590310058b4e05e784088abdef589d5e0
  scope_hash: ca18030fd5753cad6d14e29bf451c8b5be3b3e63625f6ec88a3435642083d80e
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md
  roadmap_hash: bd90bd8a83629cc075257bc8483ea26881e22189295e313dc15bfcc1039f0904
  ac_hash: 74f91625620e0a3d46195fc92ae7c09d9087b5b4e43a8dd1db68fe362a80dc5c
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-29T12:33:28Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/one_resolver_invariant.ts:150 | `segment()` treats a backtick inside a **regex literal** as a template-literal opener, and template literals do not end at a newline — so the spurious string runs to the next backtick anywhere in the file and blanks the real code between. Measured over the live tree: **54 non-test `.ts` files under `src/` lose top-level `export` declarations from `codeOnly`, 231 exports in total** (`_lib/bench_telegraph.ts` 23 to 5, `_lib/install_reach_checks.ts` 18 to 3, `bench_honesty_score.ts` 46 to 31). The trigger is live and ordinary, not exotic: `const re = /` + backtick + `([^` + backtick + `]+)` + backtick + `/g;` is `src/scripts/check_portability.ts:741`. End-to-end, that exact line placed above `export class CouncilTopologyRouter {}` yields **zero violations while the file IS in `scanned`** — the same shape as R2 round-2 finding 1, re-triggered by a backtick instead of `/*`, and again invisible to the round-1 anti-vacuity discriminator. The stated limit at :131-138 is also wrong in kind twice over: it names only "a regex literal whose body contains a comment opener" (a regex containing a **quote** is the actual live trigger), and it asserts "the failure is a false NEGATIVE" while a false **positive** is reachable — when the spurious template closes inside a real string the remainder of that string is parsed as code, and `declaresRouter('const re = /' + BT + '/;\nexport const NOTE = "see ' + BT + 'x export class CouncilTopologyRouter here";\n')` returns `true` for a name that exists only in a string. Defeats AC 1. | open | probes: 54 files / 231 exports lost tree-wide; hidden-router e2e green with file in `scanned`; false-positive reproduced |
| 2 | medium | src/scripts/_lib/one_resolver_invariant.ts:289 | The `resolver-is-not-a-resolver` check is unsound in **both** directions, because it tests for the identifier `classifyLadder` in an export position rather than for resolver behaviour. Fails to fire on a gutted resolver: `export const classifyLadder = "moved";` scans green, while R2 round-2's own reproducer `export const NOTE = "moved";` is caught — the two differ only in whether the stub kept the name, so the repair is pinned to the literal reproducer rather than to the property. `export { classifyLadder } from './moved_elsewhere.js';` is green too, and if the target is outside the scan root (a `.js` file, `dist/`, `node_modules/`) nothing else catches it — which is verbatim the scenario the violation detail at :296-299 says it exists to catch ("quietly moved somewhere unscanned"). Fires on a legitimate resolver: renaming the entry point to `resolveLadderRung` — an ordinary refactor — produces `resolver-is-not-a-resolver` with that same misleading detail. R2 round-2 finding 2 is repaired against its reproducer, not against its finding. | open | probe: 4 ladder bodies, 3 green incl. the gutted one, 1 red on a legitimate rename |
| 3 | medium | src/scripts/_lib/one_resolver_invariant.ts:84 | Three further ordinary TypeScript export syntaxes still evade `declaresRouter` and return **zero** violations from `checkOneResolver` end-to-end: `export type { CouncilTopologyRouter } from './impl.js';`, `export * as CouncilTopologyRouter from './impl.js';`, and `export const { CouncilTopologyRouter } = mod;`. The first is the sharpest: it is **one keyword** from `export { X } from './x.js'`, which IS covered and IS pinned as the "re-export" shape at test line 131 — the exact "sits ONE keyword from a covered form, so a reader has no signal" argument the comment at :86-89 gives for having added `declare`. Type-level routers are in scope by the module's own design (`type` is in form 1's alternation and `export type CouncilTopologyRouter = ...` is pinned at test line 133), so this is an inconsistency inside the declared scope rather than a naming-narrowness question. | open | probe: 9 further shapes through `declaresRouter`; 3 evade, confirmed e2e through `checkOneResolver` |
| 4 | low | src/scripts/_lib/one_resolver_invariant.ts:240 | `importsCouncilInternal` has one miss and one surviving false positive. Miss: the docstring at :234 claims dynamic `import()` is covered, but every pattern requires a `'` or `"` specifier, so `await import(` + backtick + `../ai_council/necessity.js` + backtick + `)` returns `false` — a template-literal specifier is idiomatic for dynamic import. False positive: the docstring at :237-238 says it "Runs over comment-stripped text with string literals intact, so a docstring mentioning the path cannot red the gate" — true of comments, and R1 finding 5's defect is again moved rather than removed, because a **string** mentioning it still reds the gate. Verified end-to-end: a `judgment_ladder.ts` containing `export const TEMPLATE = ` + backtick + `import { x } from '../ai_council/y.js';` + backtick + `;` yields `council-import-in-resolver` with no import present. | open | probe: 7 import shapes, 2 wrong; string false positive reproduced through `checkOneResolver` |
| 5 | low | agents/roadmaps/road-to-inbox-harvest-2026-08-e-council-topology-evidence.md:256 | The step 0.5 sensitivity contrast is not reproducible as written. The source the sentence itself names — the glob line comment followed by `export class CouncilTopologyRouter {}` — is **80** characters, not 104; the nearest variant in the test file (the `import` arm, test line 250) is 90, and no reading of the named source reaches 104. "The scanner preserves all 104" is additionally true only of the character **count**: `segment` blanks the comment to spaces, so its content is not preserved — length is, which is a different claim. Separately at :224, "the suite **now** fails 15 of 32 tests" is stale in the present tense against the closure's own headline of 45 tests at :186. Everything else checked in the closure prose is accurate: 45 tests exist and pass, the five denial arms are five, the four sensitivity arms each call `expectCleanBaseline()`, the real-tree test does assert `scanned` above 100 and contains the resolver by path, and the "What this step does NOT close" disclosure is correct. | open | probes: source measured at 80 chars across 4 readings; `vitest run` = 45 passed |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
