# Findings: feat-inbox-harvest-b-quorum-telemetry
<!-- completion-review: v1 | reviewed: 2026-08-10 | scope: f56b17ff0f70cff5639922440276c03362b99aedf4dea81ff60f51f90c7e5f09 | diff: 12e0bc14f7813691c8708d25b6eb23c8bcf4f116 | reviewer: r2-fresh-subagent-feat-inbox-harvest-b-quorum-telemetry | prompt_hash: c79cee68d5df64a2621793e07cdf6ab94043b183f74eb4e9fb6f5ef844fac477 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 12e0bc14f7813691c8708d25b6eb23c8bcf4f116
  scope_hash: f56b17ff0f70cff5639922440276c03362b99aedf4dea81ff60f51f90c7e5f09
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-b.md
  roadmap_hash: 612e3d5fc5867da15ee5f5b3727a423e686790d59a284552796c33371f259387
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-10T21:58:50Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/council_cli.ts:712 | `attendance_rate` cannot see construction-time absence. The registered instrument is `post_run` `present`/`total` only (budget json:9 forbids using `pre_run` for rates), but `_postRunQuorum` computes `total` as `members.length` — the roster that survived construction. A pass with 3 configured members where 2 fail to construct and 1 answers emits `post_run {total:1, present:1}` and reads as 100 % attendance. The headline metric is structurally blind to exactly the "council degraded by configuration" case it was registered to measure. | fixed | 8f9290203 — `configured_total` carried forward from the pre-run reading; attendance_rate is now present / configured_total |
| 2 | medium | src/scripts/council_cli.ts:2422 | `--single` inflates `solo_conclusion_rate` and defeats its documented disambiguation. `_apply_solo_dispatch` replaces `members` with the single picked client *before* `_postRunQuorum`, so a deliberate solo dispatch emits `post_run {total:1, present:1, verdict:concluded}` — byte-identical to a configured one-member council. The budget file's stated remedy ("a consumer separating configured-solo from degraded-solo reads `total` alongside", json:16) does not work, because `total` is 1 in both cases. This is the rate the quorum-solo-floor blocker is pre-registered to decide against. | fixed | 8f9290203 — `dispatch: full \| single` recorded on the line; the budget file now splits the rate on dispatch + configured_total and states that the solo floor is decided on the degraded case only |
| 3 | medium | src/scripts/council_cli.ts:989 | `pre_run` lines are emitted from paths that never run a council pass, contradicting the registered `phase_split` invariant "Every pass emits TWO lines" (budget json:9). `cmd_estimate` — a cost preview that spends nothing — passes `quorum_out` (`:1757`) and therefore writes a `pre_run` line on every invocation; `cmd_run` emits `pre_run` inside `build_members` *before* the necessity gate can abort the run (`:2440`, early return `:2448`) and before `_size_fit_gate` (`:2451`). `absent_reason_distribution` is explicitly defined over `pre_run` absences (json:22), so its counts are inflated by previews and gate-blocked runs with no way to filter them out — no field on the line records whether a pass followed. | fixed | 8f9290203 — `command: run \| estimate \| debate` recorded; absent_reason_distribution now excludes `estimate`, and the phase_split text no longer claims every pre_run line is a pass. The "aborted after the gate" case stays unrecordable at emit time and is declared as an upper bound in honest_gaps |
| 4 | medium | src/scripts/council_cli.ts:2881 | `cmd_debate` runs a real metered council pass but is invisible to all three registered metrics: it calls `build_members` without `quorum_out`, so no `pre_run` line is written, and no `_postRunQuorum` call exists on that path at all (the only one is `cmd_run:2593`). Debate passes therefore contribute nothing to attendance, solo-conclusion, or absent-reason data while consuming the same providers. The budget file's HONEST GAP paragraph declares only the empty `lens`/`invocation` gap (json:25) and does not disclose this one, so a reader at the review date will read the numbers as covering all council passes. | fixed | 8f9290203 — cmd_debate now passes `quorum_out` + `command: debate`, so it carries the pre_run half. The post_run half needs a usable-response definition for a multi-round debate that this change does not have; declared in honest_gaps rather than left as a missing row |
| 5 | low | src/scripts/council_cli.ts:660 | `_emitQuorumEvent` calls `appendQuorumEvent(input)` with no `AppendEventOptions`, so the write always targets the process-wide default log (`events_log.ts:216`) with no injection point. The diff's own rationale at `:646-648` argues a write must not be buried in "an exported and unit-tested" function — yet `build_members` is exported (`:3827`) and the `pre_run` write is buried in it. Any in-process caller that reaches it with `quorum_out` appends real `quorum_result` lines to the same log the registered metrics are computed from, contaminating the dataset with fixture councils. (The pre-existing `_necessity_gate` emitter at `:1992` uses the same default-path pattern, so the convention is inherited — but its `skip_necessity` lines feed no metric.) | skipped | Real, and deliberately not fixed here. The default-path pattern is inherited from the `_necessity_gate` emitter the finding itself cites, so an injection point is a change to the module's convention rather than to this diff — and the contamination path is already closed in practice: `council_cli.test.ts:66` sets the kill-switch, so no fixture council writes. Adding a threaded `AppendEventOptions` to make it structural is its own change, on its own evidence |
| 6 | low | src/scripts/ai_council/quorum.ts:102 | `isSoloConcluded` has no production caller. The `council_cli.ts:79` import list takes only `evaluateQuorum` and `QuorumResult`, and the predicate is new in this diff, so nothing outside it can reference it; the only consumer is `tests/scripts/ai_council/quorum.test.ts:105`. Meanwhile the budget file re-states the predicate's logic as prose ("verdict=concluded and present=1", json:16) for the log-side computation. Two independent definitions of one rule with no shared code path — a later change to the predicate (e.g. consulting `total`, which its own docstring argues against) leaves the JSON's definition silently stale. | fixed | 8f9290203 — `appendQuorumEvent` calls `isSoloConcluded` and writes the result as the `solo` field, so the predicate has a production caller and the budget file reads that field instead of restating the rule |
| 7 | low | tests/scripts/ai_council/events_log.test.ts:88 | The diff removes the only assertion that pinned the on-wire schema version: `expect(rec.schema_version).toBe(1)` became `toBe(SCHEMA_VERSION)`, which is tautological with respect to the constant (the sibling at `:50` was already written that way). The collision half of that test still holds (999 is overridden), but after this change no test fails if `SCHEMA_VERSION` is bumped to 3 or reverted to 1 — for a module whose entire contract is a versioned wire format, and in the same diff that bumps it. | fixed | 8f9290203 — a test pins the on-wire value to the literal 2; the tautological assertion stays where it belongs (the reserved-fields-win case) |
