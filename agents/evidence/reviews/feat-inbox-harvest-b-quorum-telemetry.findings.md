# Findings: feat-inbox-harvest-b-quorum-telemetry
<!-- completion-review: v1 | reviewed: 2026-08-10 | scope: 97ba888abc816323ab67f03068932d1c611b4527c984da280b764c826f5a84ed | diff: baba417297bbb6db0d0d250785e3c049f7e33fd7 | reviewer: r2-fresh-subagent-feat-inbox-harvest-b-quorum-telemetry | prompt_hash: 4d0e3aa3a98ed5eba84b32156162038f11f1a2dee485c03ff0e9ba84b800a8d9 -->

<!-- context-manifest: v1
inputs:
  diff_sha: baba417297bbb6db0d0d250785e3c049f7e33fd7
  scope_hash: 97ba888abc816323ab67f03068932d1c611b4527c984da280b764c826f5a84ed
  roadmap: agents/roadmaps/road-to-inbox-harvest-2026-08-b.md
  roadmap_hash: 612e3d5fc5867da15ee5f5b3727a423e686790d59a284552796c33371f259387
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-10T22:16:45Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/scripts/council_cli.ts:2638 | `dispatch` is derived from the `--single` FLAG, not from whether solo dispatch actually happened. `_apply_solo_dispatch` (council_cli.ts:2364-2407) returns the UNFILTERED roster on three documented paths — empty `routing.solo_member_fallback_chain`, no chain member runtime-present, selected member vanished between probe and filter — each printing a "escalating to full council" WARN and leaving `members` untouched (2447-2454). The post_run line still records `dispatch: 'single'`. A full-attendance pass is then filed under the exact bucket `quorum-attendance-budget.json:16-17` tells the reader to split out as "the operator asking for one voice, not the council failing to assemble", so the degraded-council population the quorum-solo-floor blocker is decided on is contaminated by escalated passes. The truthful signal is available at the call site (roster length before vs after `_apply_solo_dispatch`, or the returned banner). | open | |
| 2 | medium | src/config/quorum-attendance-budget.json:11 | `attendance_rate = present / configured_total` mixes two different rosters and is not bounded by 1. `configured_total` is `total_enabled` — enabled config ENTRIES (council_cli.ts:852, carried forward at 2625) — while `present` comes from `_postRunQuorum`, whose `n` is `members.length`, the RUNTIME client list. `--siblings` fans one entry out into N clients (council_cli.ts:874-897 pushes one client per sibling model and `continue`s without a second `total_enabled += 1`), so a 1-member council run with two siblings that both answer emits `configured_total: 1, total: 2, present: 2` — an "attendance rate" of 200%, and a `total` that exceeds the `configured_total` the same definition calls the pre-failure roster. The `--single` path skews the same ratio the other way (present counted over the filtered roster, denominator over the full config). The definition names `--siblings` as a "filter" it is not. | open | |
| 3 | medium | src/config/quorum-attendance-budget.json:22 | The pre-registered exclusion `command != estimate` does not exclude the largest spend-free preview path. `council run` and `council debate` both default to `confirm: false` (council_cli.ts:3457) and return before any provider call — "No --confirm flag — estimate only" at council_cli.ts:2545-2551 and 3023-3028 — but `build_members` has already written a `pre_run` line tagged `command: 'run'` / `'debate'` (council_cli.ts:1010-1012). The stated rationale for dropping `estimate` ("a preview reaches construction without running anything, and counting its absences inflates the configuration half") applies verbatim to those lines, so `absent_reason_distribution` counts them anyway. `phase_split` (line 9) enumerates the abort paths as "the necessity and size-fit gates" and omits the `--confirm` gate, which is the default. | open | |
| 4 | medium | src/scripts/council_cli.ts:679 | The CLI emit wiring — the actual Phase 1.1 deliverable — has no test, and cannot easily get one: `_emitQuorumEvent` calls `appendQuorumEvent(input)` with no second argument, so it can only ever write to `defaultLogPath()`; `AppendEventOptions` is never threaded through even though `appendQuorumEvent` accepts it. The new tests (tests/scripts/ai_council/events_log.test.ts:129-296, tests/scripts/ai_council/quorum.test.ts:105-135) exercise `appendQuorumEvent` and `isSoloConcluded` directly and never reach `build_members`, `cmd_run`, or `cmd_debate`. Nothing asserts that a pass emits one `pre_run` and one `post_run` line, that `configuredTotal` survives the overwrite at council_cli.ts:2625-2627, or that `dispatch` is correct — findings 1 and 2 are exactly what such a test would have caught. | open | |
| 5 | low | src/scripts/ai_council/events_log.ts:301 | The fail-open docstring contradicts itself and its own test: "`appendEvent` itself throws only on an invalid action, which this function cannot produce" is false — `appendEvent` calls `fs.mkdirSync` / `fs.appendFileSync` unguarded (events_log.ts:217-219), which is precisely why the preceding sentence promises "an unwritable log dir, a full disk" are swallowed, and precisely what the ENOTDIR test at events_log.test.ts:271-283 exercises. As written the comment argues the `try/catch` is unreachable, inviting a later reader to delete it. | open | |
| 6 | low | src/config/quorum-attendance-budget.json:21 | `absent_reason_distribution` lists `no_binary` (from `AbsentReason`, transport_resolver.ts:65) and `binary_missing` (the literal written at council_cli.ts:975) as if they were different findings, but both mean "this provider's CLI binary is not usable" — one is raised at transport resolution, the other when the CLI client constructor refuses. The metric whose whole output IS the distribution therefore splits one cause across two buckets, on a boundary that is an implementation detail rather than the configuration-vs-provider-behaviour split the definition claims to draw. | open | |
| 7 | low | src/scripts/ai_council/events_log.ts:6 | Schema prose left at v1 after the bump: the module header still reads "Schema v1 carries the minimum needed" (line 6), the pass-through comment still speaks of "the schema-v1 reserved set" and "the schema-v1 fields above always win" (lines 206-207), and the first test is still titled "writes a v1 record" while asserting `SCHEMA_VERSION` (events_log.test.ts:36, 51). Same commit also converted that assertion from the literal `1` to the constant, which the new wire-pin test at events_log.test.ts:142-149 explicitly calls tautological. | open | |
