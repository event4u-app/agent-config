# Findings: drain-experience-loop-phase0
<!-- completion-review: v1 | reviewed: 2026-08-30 | scope: 48743d1e6db23962533722b8bf9e209cea78c776313480a034f1d21dc06323ca | diff: 61adeeb2b010c776c8f210272baebe1a9a218de3 | reviewer: r2-fresh-subagent-drain-experience-loop-phase0 | prompt_hash: 5a3d799cfa2b2a8a8c033db7d7ab365519a08283ecb1e007bee1a37cd16c07b6 -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-drain-experience-loop-phase0"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-30 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 61adeeb2b010c776c8f210272baebe1a9a218de3
  scope_hash: 48743d1e6db23962533722b8bf9e209cea78c776313480a034f1d21dc06323ca
  roadmap: agents/roadmaps/road-to-experience-loop-broadening.md
  roadmap_hash: 3ebedc4d8128ea76c4360c3e7a0ae5fd5010a78d80afbca3c969f3a8a08bdedb
  ac_hash: 0ec7970d98cf2cd8b26f1ce75d55160ea7d8f9d35a1f11b3598d56a9cc5cf6c8
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-30T05:32:32Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | medium | src/config/gate-coverage.yml:45 | Stale denominator in the sentence the diff itself edited. The hunk bumps `77 -> 78` emitters and `74 -> 75` listed, but leaves the population at "304 `lint_*` / `check_*` / `audit_*` scripts" two lines above — and the comment states that number "is computed, not asserted — `check_gate_coverage.count_gate_scripts`, re-run on the tree carrying these rows". Measured on this branch: `count_gate_scripts()` returns **305**, because the diff adds `src/scripts/lint_metric_consumers.ts`. The file's own history block records this exact drift recurring ("the fourth time this file has met the case"), so the miss is the known failure mode, not a novel one. | open | Verified: `tsx` import of `count_gate_scripts` from `src/scripts/check_gate_coverage.ts` prints 305 against the header's 304. |
| 2 | medium | src/scripts/lint_metric_consumers.ts:116 | The BOILERPLATE filter is defeated by any punctuation after the token. The predicate accepts a value only when it equals the token exactly, or begins with the token followed by a **space** — so the natural writing forms `TBD:`, `TODO:`, `N/A —`, `unknown,` pass. Measured against a fixture registry: `consumer: "TBD: nobody reads this yet, decide later"` and `decision: "TODO: figure out what this actually changes"` were both **accepted**; only `absent: "N/A - honestly nothing …"` reddened, and only because "n/a" happened to be followed by a space. That is precisely the class the docstring says the field exists to catch ("placeholders that pass a length check and answer nothing"). A word-boundary test (token followed by end-of-string or a non-alphanumeric) closes it without turning the list into a vocabulary filter. | open | Reproduced with `--root <tmp fixture>`; exit 0 on the `TBD:`/`TODO:` pair. |
| 3 | medium | src/scripts/lint_metric_consumers.ts:223 | The self-test case named "a too-short field is refused" does not exercise the length branch. Its fixture is `absent: nothing much`, and `nothing` is in `BOILERPLATE`, so line 116 matches `nothing ` and `continue`s before the `MIN_FIELD_CHARS` check at line 121 is ever reached. Confirmed by the gate's own output on that fixture: the reason printed is `boilerplate ("nothing much")`, not the char-floor message. Consequence: `MIN_FIELD_CHARS` has **zero** self-test coverage — deleting the length branch leaves `--self-test` at 7/7 green — so the one control that is supposed to prove this gate discriminates has unknown sensitivity on a third of its rule surface. `gate-coverage.yml`'s `no_canary_reason` leans on that self-test as the substitute for a canary, and the roadmap's 0.3 verify clause cites it as evidence. A non-boilerplate short value (e.g. `absent: it breaks`) reproduces the intended branch and would fix the case. | open | Verified both directions: `nothing much` -> boilerplate reason; `it breaks` -> "9 chars, below the 20-char floor". |
| 4 | medium | src/scripts/lint_metric_consumers.ts:1 | The new gate is registered under the `ci` task root but adopts neither `_lib/gate_ledger.ts` nor a `// ledger-exempt: <reason>` marker, which `check_gate_completeness` states as a standing obligation on arrival ("Shrink-only from here — the number may not grow, so a NEW gate must adopt or exempt"). Verified: `registeredGateIds()` includes `lint_metric_consumers`, and `classifyGateSource()` returns `{"status":"unledgered"}`. It is green today only because the measured un-adopted count sat one below the baseline — the tree now reads 215 against a baseline of 215 (214 without this gate). The ratchet's own note says "headroom is zero either way, so the next un-adopted arrival reds this gate immediately", so this change hands the next contributor an unrelated red. | open | `check_gate_completeness` exit 0, "215 un-adopted · 293 registered"; classification probed directly. |
| 5 | low | src/scripts/lint_metric_consumers.ts:98 | A null list element crashes instead of producing a finding. `metrics:\n  -\n` parses to `[null]`; `findingsFor` dereferences `entry.id` unguarded and throws `TypeError: Cannot read properties of null (reading 'id')` with a stack trace. Exit is still 1 (unhandled rejection), so there is no false green, but the run bypasses the careful `readRegistry` error path and the operator sees a crash rather than the gate's own message. A non-object element (`- "foo"`) is handled correctly, so the gap is null/undefined only. | open | Reproduced: `printf 'metrics:\n  -\n'` fixture, stack trace at lint_metric_consumers.ts:98, exit 1. |
| 6 | low | src/scripts/lint_metric_consumers.ts:33 | Docstring contradicts the implementation on exit codes. It declares "0 clean · 1 finding · **2 usage or config error**", but every config error returns **1**: a missing registry, a YAML parse failure, a non-mapping root, and a missing `metrics:` list all take the `return 1` path at the `read.error` branch. Only the `--root` argument error returns 2. Either the contract line or the branch should move; as written a caller distinguishing "the registry is broken" from "an entry is bad" cannot. | open | Verified: `--root /nonexistent-xyz` exits 1, not 2. |
| 7 | low | src/scripts/lint_metric_consumers.ts:176 | Dead work in the self-test hot path. `fixture()` shells out to `git init` for every one of the seven cases, justified by a comment as making the root "look like a checkout to any helper that asks" — but nothing in this gate's code path consults git: the only helpers are `gate_self_test` (spawns tsx) and `scan_scope` (statSync/stdout). Seven process spawns bought for a hypothetical consumer, in the control that is supposed to stay cheap enough to run on demand. | open | Read of both imported helpers; no git usage on any reachable path. |
| 8 | low | agents/roadmaps/road-to-experience-loop-broadening.md:6 | The roadmap stays `status: draft` while three implementation steps land shipped code, and its `estate_offset_exempt` reason — still unedited — asserts "Added as a draft proposal, not as active work. Archiving is impossible (nothing has run)". This diff falsifies that sentence: a gate, a registry, a contract page and a task wiring have now run. The status also has live effects rather than being cosmetic — the roadmap is absent from `agents/roadmaps-progress.md` (2 open roadmaps listed, this one not among them) and `lint_plan_risk_register` reports it `draft-exempt` — so progress and risk-register obligations go unreported for work that has already shipped. | open | Verified: dashboard table lists 2 open roadmaps, neither this one; `lint_plan_risk_register` prints `draft-exempt: agents/roadmaps/road-to-experience-loop-broadening.md`. |
