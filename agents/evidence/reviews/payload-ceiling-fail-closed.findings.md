# Findings: payload-ceiling-fail-closed
<!-- completion-review: v1 | reviewed: 2026-09-10 | scope: f3213c11d05f9bdc043ce9ede9e21b22c5c9b7f55cf004d29d8e3160a7812c1a | diff: 908354e8773b9d7ca7ef6cf631aca99c31ccc8ca | reviewer: r2-fresh-subagent-payload-ceiling-fail-closed | prompt_hash: 52bdc61c7e1adf32f399afec08cf2138049601b069ce47330167d951d7aa0f2b -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-payload-ceiling-fail-closed"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-10 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 908354e8773b9d7ca7ef6cf631aca99c31ccc8ca
  scope_hash: f3213c11d05f9bdc043ce9ede9e21b22c5c9b7f55cf004d29d8e3160a7812c1a
  roadmap: agents/roadmaps/road-to-delivery-for-every-host.md
  roadmap_hash: 13b9cd9ca375774d949430e79ccededd5ce3e9f25391e11e2d4f67fc31cb06f1
  ac_hash: 336cf5acce8b7138ae8feb08225583eb81d7918be2b99ee60f039864561202b0
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-10T14:41:21Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/check_preamble_payload_budget.ts:468,609,642 | The new `requireBase`-refusal verdict is `{ok: false, note: null}` — the exact same shape `renderBounds()` and the CLI's stderr header already use for an actual ceiling rise. `renderBounds()` (line 468: `state = b.ok ? 'ok' : 'ROSE'`) prints `ROSE — n/a at <ref>, shrink-only (ADR-264)` on stdout, and line 642 hardcodes `'❌  the standing-payload ceiling rose in this change:'` on stderr, for ALL FOUR of the new unverifiable-base causes (no base ref, unreadable config, unparseable config, no bound at base) — none of which is a rise. The `violations[0]` text (correctly worded, "the shrink-only bound could not be verified: …") is printed underneath the wrong header, so a reader scanning the summary line or the stderr header — which is what CI annotations typically surface first — is told the ceiling grew and will look to lower it, when the actual fix is an infrastructure one (fetch the base ref). Exit code (1) is unaffected; this is a message-correctness bug, not a fail-open. Neither call site was updated when `requireBase` introduced this new `ok:false`-without-a-rise case. | fixed | fixed in 908354e87 — `BoundsRatchetVerdict.verified` is the discriminant; `boundsRefusalHeader()` and `renderBounds()` branch on it, so the four unverifiable-base causes print UNVERIFIED and never ROSE |
| 2 | medium | tests/scripts/check_preamble_payload_budget.test.ts (whole file); src/scripts/check_preamble_payload_budget.ts:475-492,535-544 | No test exercises `--require-base` / `decide({requireBase: true})` through the CLI (`main()`) layer — the new flag-parsing at line 492 and its threading into `decide()` at line 539 are untested end-to-end. The 5 new tests in `tests/scripts/standing_bound_ratchet.test.ts` all call `assertBoundsDidNotRise()` directly, bypassing `decide()`, `main()`, `renderBounds()`, and the stderr header entirely. This is why finding #1 was not caught by the "12/12 green" sensitivity suite the roadmap update cites as verification — nothing in the suite asserts the actual printed output for the enforcing posture. | fixed | fixed in 908354e87 — `boundsRefusalHeader` and `renderBounds` exported and pinned over synthetic verdicts for all three states (ok / ROSE / UNVERIFIED) in tests/scripts/check_preamble_payload_budget.test.ts |
| 3 | low | src/scripts/_lib/standing_bound_ratchet.ts:96,150,176-181 | Consolidating the four inline returns into `unverified()`'s shared advisory template (line 96: `` `${reason}, so the shrink-only bound was NOT verified` ``) introduced two small prose regressions versus the pre-diff text it replaced. (a) The "no `ci_delivery.grace_ceiling`" branch's reason string already ends its own clause ("…so there is no earlier bound to ratchet against"); the template appends a second ", so the shrink-only bound was NOT verified" onto it, producing a doubled "so … so …" sentence in the human/JSON `note` for that branch that the pre-diff code did not have. (b) The "no base ref resolved" branch's note lost the pre-diff text's trailing "in this run" (pre-diff: "…NOT verified in this run"; now: "…NOT verified"). Neither changes gating behaviour and no existing test pins the exact wording (both existing assertions use `.toMatch()` on a substring), so nothing failed — but the prose no longer matches what the pre-diff module said, which the round's own docstring explicitly claims care about ("Printed, never silent"). | fixed | fixed in 908354e87 — the fourth reason no longer carries its own trailing clause, and the template restores "in this run" |
| 4 | low | .github/workflows/standing-payload-delta.yml:62,141 | The new comment at line 141 states unconditionally "This job checks out with a base available, so the strict posture is free." The workflow also triggers on `workflow_dispatch` (line 62), which — unlike `pull_request` — sets no `GITHUB_BASE_REF`; `resolveBaseRef()` then falls through to the `origin/main`/`main` fallback (src/scripts/_lib/ratchet_base_ref.ts:88-92). On a manual dispatch against a branch with no shared history with `main` (first commit / orphan branch), the base would legitimately fail to resolve — previously a SKIP, now a hard failure under `--require-base`. The claim holds for the primary `pull_request` trigger this step is designed for, but is stated without qualifying the second trigger the same workflow declares. | fixed | fixed in 908354e87 — the comment now states the claim per trigger and names the `workflow_dispatch` case that now reds where it used to skip |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
