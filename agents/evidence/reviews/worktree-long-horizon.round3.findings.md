# Findings: worktree-long-horizon
<!-- completion-review: v1 | reviewed: 2026-08-19 | scope: 02922a59531e1c7d1f04601e9552f247071ab484af067baa37b0955b956bf5b7 | diff: 561b486e0c1a4d8bfa2aba3e6b6dd82bf0803c64 | reviewer: r2-fresh-subagent-worktree-long-horizon | prompt_hash: c3fddeba99b1110254513072ceeb192cf270759e56176851f47bdda7b5983837 -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-19 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 561b486e0c1a4d8bfa2aba3e6b6dd82bf0803c64
  scope_hash: 02922a59531e1c7d1f04601e9552f247071ab484af067baa37b0955b956bf5b7
  roadmap: agents/roadmaps/road-to-long-horizon-execution.md
  roadmap_hash: 89257b7a50dc2827c00e9a9f8ca568627c2d70da3aa48b2188d664a14a29884a
  ac_hash: cc0790f4722b1540ced5602c39fd46db00ac1547495b3ab0e6082de1ede2c413
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-19T03:24:37Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/run_checkpoint.ts:147 | `readHead` resolves a symbolic HEAD by reading `<gitDir>/refs/heads/<name>` and, on failure, `<gitDir>/packed-refs`. In a git WORKTREE `gitDir` is the per-worktree directory the `.git` FILE points at (`.git/worktrees/<name>`), and that directory holds only `HEAD`, `ORIG_HEAD`, `commondir`, `index`, `logs` — branch refs and `packed-refs` live in the COMMON dir named by `commondir`, which this function never reads. Both reads ENOENT, the outer `catch` swallows, and `head` is `null` again. Reproduced in this worktree: `ls .git/worktrees/long-horizon` shows no `refs/` and no `packed-refs`; `buildCheckpoint(cwd, 'k', 'road-to-long-horizon-execution').head` returns `null`. The one production caller (`session_eol_hook.ts:384`) passes the envelope's `workspace_root`, i.e. the worktree root, so every checkpoint a worktree session writes still carries `head: null` — the exact state R2 round 2 finding 5 reported and this fix claims to close, in the exact environment its own docblock singles out ("This package is developed in worktrees, so the naive version would have returned null exactly where it was being exercised"). The new test "resolves HEAD in a WORKTREE, where .git is a file" passes only because it writes `refs/heads/feature` INSIDE the per-worktree gitdir, a layout git never produces — the fixture conceals the defect rather than pinning it. | open | |
| 2 | medium | src/scripts/_lib/run_checkpoint.ts:295 | `verifyCheckpoint` recomputes four fields (`open_steps`, `done_steps`, `parked_steps`, `next_step`) and `renderVerification` prints those four; `head` appears in neither. So the loop contract §5d — "open / done / parked counts, the next open step, and the commit the tree was on. Every field is recomputed from the roadmap on disk" — is false for `head`, which is written and never compared or shown. R2 round 2 finding 5 named both halves ("`verifyCheckpoint` does not check `head` either, so a resumed run cannot see that the tree moved commits — the one thing the field exists for"); the fix populated the field and left the read half untouched, so even where `readHead` works the stated purpose is unreachable. Concretely: a session dies at commit A, a sibling worktree lands commit B, the resumed run performs §5d's `latestCheckpointFor` + `verifyCheckpoint` and is told "the tree still matches the checkpoint" whenever the four counts agree. The six new tests assert `head` only through `buildCheckpoint`, never through `verifyCheckpoint`, so the suite cannot see the gap. | open | |
| 3 | medium | src/scripts/hooks/run_continuation_hook.ts:67 | The module docblock still declares "FAIL-OPEN THROUGHOUT. severity: advisory, fail_closed: false" while `hook_manifest.yaml:696` now declares `severity: blocking`. `advisory` was R2 round 1's CRITICAL finding — the dispatcher enforces it as a ceiling, so the concern's `return EXIT_BLOCK` became exit 0 and the whole re-engagement mechanism was inert — and the manifest comment added by that very fix names this docblock as one of the three surfaces that "described behaviour the declaration forbade", yet the docblock was left at the forbidden value. A maintainer reconciling the two in the direction the source file states re-introduces the critical defect. `concern_severity.test.ts` cannot catch that: its `BLOCKING_ALLOWLIST` permits `blocking`, it does not require it, so a revert to `advisory` passes that gate and is caught only by the exit-code assertion in `run_continuation_dispatch.test.ts`. | open | |
| 4 | low | src/scripts/ai_council/mid_flight_fallback.ts:193 | `refuseUnmeteredEscalation`, added this round for finding 8, rolls back the twin map AND calls `ledger.release(...)`, and its docblock asserts "The shape matches `runGatedRetry`'s refusal path exactly". It does not: `runGatedRetry`'s refusal (:200-204) stamps `fallback_skipped: cost_budget` and emits the outcome but never releases the claim `establishTwin` took at :107. For a BILLABLE community-cli seat (`transport: 'cli'`, `billable: true` — the xai/perplexity wrappers) a round-1 refusal, e.g. a large restate-round question whose twin estimate breaches `max_input_tokens`, leaves the provider marked used; in a later, smaller repair round the cli fails again, `ledger.attempt` returns `'stop'`, no twin is built at all, and the seat is lost while headroom exists. That is finding 8's failure mode left standing at the sibling call site the new code claims parity with, and the parity sentence now reads as coverage the code does not have. | open | |
| 5 | low | src/server/io/yamlIO.ts:148 | `upsertScalar`'s ancestor walk computes `const from = depth === 0 ? 0 : insertAt === lines.length ? 0 : 0;` — every branch yields `0`, so the inner search always scans from line 0 rather than from inside the ancestor block matched so far, and for any path deeper than one section the first same-named sub-key anywhere in the file wins. Verified: on `a:\n  b:\n    x: 1\nc:\n  b:\n    y: 2\n`, `upsertScalar(t, ['c','b','z'], 3)` writes `z: 3` under `a.b` and the parsed document contains no `c.b.z` at all — a silent write to the wrong parent. Latent only because the single caller (`wizard.ts:931`) passes a one-section path, but the function is exported and its docblock promises the general contract ("Insertion point is the end of the deepest EXISTING ancestor block"); the new `yamlIO.upsert.test.ts` has five cases, all depth-1, so nothing pins the promise it makes. | open | |
| 6 | low | src/scripts/run_supervise.ts:159 | Re-keying the relaunch ledger from `session_id` to `roadmap_slug` (finding 9) replaced a cap that could never bind with one that binds across UNRELATED runs and never resets. `MAX_RELAUNCHES_PER_RUN` is still documented "at most three relaunches per run", but nothing deletes a slug's entry — not the `complete` disposition, not archival, and `writeLedger` only overwrites the whole object — so three separately claimed, separately died runs of roadmap R over three weeks (one relaunch each) leave `ledger['R'] === 3`, and the fourth, healthy run is classified `budget-exhausted` with the reason "a run that dies this often has a problem a fourth session will not fix" although no run died more than once. The same key change makes `interruption_report.ts:444` report the roadmap's lifetime total as each run's `relaunches`, so the digest's "median relaunches per run" counts one roadmap's relaunches once per run on it. Inert today because `writeLedger` still has no caller, but the key space is wrong now, in the opposite direction from the defect it replaced. | open | |
