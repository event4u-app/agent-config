# Findings: feat-subagent-lifecycle-integrity
<!-- completion-review: v1 | reviewed: 2026-08-13 | scope: 2ae974e06a060deea9efe6157c7634873e67912ff7abc451fbfbefdea1c8d482 | diff: 3fc433269cc36520bb10e5f08f302cdfdffc5d5a | reviewer: r2-fresh-subagent-feat-subagent-lifecycle-integrity | prompt_hash: b7f6b87e3c3652187426e2ea0e1a082e97ad1ec7eb39a370019a6d873656226e -->

<!-- context-manifest: v1
inputs:
  diff_sha: 3fc433269cc36520bb10e5f08f302cdfdffc5d5a
  scope_hash: 2ae974e06a060deea9efe6157c7634873e67912ff7abc451fbfbefdea1c8d482
  roadmap: agents/roadmaps/road-to-subagent-lifecycle-integrity.md
  roadmap_hash: d04953737521c057dab3f9972cfc91b41ae5b954d06cbc3e0143928860697310
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-13T07:10:19Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/subagent_ledger_hook.ts:316,350 | A payload without `agent_id` makes both handlers `return` before any ledger line is written, so the instrument emits NOTHING rather than recording the absence. Phase 0 Step 2 ("assert `last_assistant_message` and `agent_type` arrive as documented on THIS host version") is still `[ ]` open, and the evidence page states the string-table check is "a **presence** check … not a proof that the fields arrive on a given event's stdin". On the one host shape not yet ruled out, the Phase-1 Step-4 baseline reads identically to "no dispatches happened" — an empty ledger is indistinguishable from zero traffic. The file's own contract says the opposite ("recording absence as data", "absence is written down as absence"). A `{event, ts, ref: null, reason: "no agent id"}` line costs nothing and carries no host token. | open | |
| 2 | high | src/scripts/hooks/subagent_ledger_hook.ts:299,305 | Open records are repo-global, carry no `session_id`, and are never reaped. A dispatch that never emits `subagent_stop` — symptom #1 the roadmap exists to measure — leaves `agents/runtime/state/subagent-ledger/open/<ref>.json` on disk permanently. `concurrent_open` and `depth` therefore drift upward monotonically across sessions, machines and months, with no TTL, no start-of-session sweep and no CLI surface to clear it. Phase 3 Step 1 is pre-registered to refuse a spawn at `concurrent-open >= M` (M=4) reading this same set, so four leaked records would deny every subsequent spawn forever. The instrument's own leak is invisible in its output because a leaked record is only ever counted, never listed. | open | |
| 3 | high | src/scripts/hooks/subagent_ledger_hook.ts:322,344 | `readOpenRecords` -> compute -> `writeOpenRecord` is an unserialised read-modify-write, and `writeOpenRecord` uses a plain `fs.writeFileSync` under `agents/runtime/state/`. `docs/contracts/hook-architecture-v1.md:364-380` states concerns writing there MUST take `agents/runtime/state/.dispatcher.lock`, write `<dest>.tmp.<pid>`, then `os.replace` — the helpers exist (`state_io.atomic_write_json`, `LOCK_BASENAME`) and are not used. The sibling precedent does not cover this: `orchestration_record_hook.ts:266-268` scopes its no-lock carve-out to appends only ("appends are not the torn-write risk `atomic_write_json`/`atomic_write_text` guard"), whereas this is a whole-file state write another process reads back. Consequence in the primary dispatch mode (parallel fan-out): N concurrent `SubagentStart` processes each read the pre-write set and every one records `concurrent_open: 1`, so the concurrency metric systematically undercounts in exactly the scenario it exists to measure. | open | |
| 4 | medium | src/scripts/hooks/subagent_ledger_hook.ts:206-223 | `_extractJsonObject` falls back to the outermost brace span `message.slice(first, last + 1)`. A valid bare envelope followed by ANY later `}` in prose fails `JSON.parse` and returns `null`, which `classifyEnvelope:192-193` reports as `absent`. Verified: `'… {"summary":"s","handoff":"h","confidence":"high","findings":[],"risks":[]} — done. See {done}.'` throws `Unexpected non-whitespace character after JSON at position 7`. This routes an extraction failure into the "the worker never returned a structured result" bucket, which the same file (`:174-179`) names as the distinction the whole measurement exists to preserve, and it biases the Phase-1 falsifier (envelope return rate >=95%) downward. No test covers envelope-plus-trailing-prose; the four `classifyEnvelope` cases are bare, fenced, malformed and empty. | open | |
| 5 | medium | src/scripts/hooks/subagent_ledger_hook.ts:283 | A named parent whose record is not in the open set returns `{depth: 2, depth_basis: 'observed'}`. The payload asserted the FACT of nesting; the number 2 is a guess — a grandchild whose parent already closed is depth 3+. Labelling a fabricated number `observed` is precisely what the file's header discipline forbids, and any consumer filtering on `depth_basis === 'observed'` to get "measured" depths will ingest it. It also mis-feeds Phase 3's pre-registered `depth >= N` (N=2) threshold. Finding 2 makes this branch rarer today, but a `readOpenRecords` failure (unreadable dir, `:249-251` returns an empty map) turns it into the default for every nested spawn. | open | |
| 6 | medium | docs/contracts/hook-architecture-v1.md:423,430-433,453 | `EVENT_VOCABULARY` gained two entries but `tests/fixtures/hooks/` gained no fixtures, and the drift is inside a file this diff edits: the Event row (`:26`) was updated while the fixture section four lines below still says "One JSON file per event in the agent-config event vocabulary", still enumerates the same 8 files, and still claims the replay test "asserts 1:1 mapping between `EVENT_VOCABULARY` and this directory". Effects: `bench_hook_injection.ts:141-142` does `if (!fs.existsSync(fixtureFile)) continue;`, so the two new slots are silently dropped from the injection-budget measurement with no warning; `hooks:replay` (`replay_hook.ts:11-12,60`) has nothing to replay for either event, leaving the dispatcher path for the two new events unexercised end to end. | open | |
| 7 | medium | src/scripts/hooks/subagent_ledger_hook.ts:257-258 | `readOpenRecords` validates only that `ref` is a string, then casts the parsed object straight to `OpenRecord`. A record missing `depth` (older schema, partial write, hand edit) makes `resolveDepth:284` compute `undefined + 1` = `NaN`, which `JSON.stringify` serialises as `"depth": null` on a line still labelled `depth_basis: "observed"` — a null-depth "observed" record. Separately, the map is keyed by the record's self-declared `ref` while `removeOpenRecord:308` unlinks `${ref}.json`, so a file whose internal ref disagrees with its filename is loaded, counted, and never deleted — compounding finding 2. | open | |
| 8 | low | src/scripts/hooks/subagent_ledger_hook.ts:344,378 | `concurrent_open` carries two different definitions under one field name: the start line includes the record being opened (`open.size + 1`), the stop line excludes the one being closed (`open.size - 1`). Only the start-side convention is documented (header `:65-67`); the stop-side exclusion is stated in an inline comment only. Any aggregate computed over the `.jsonl` — max concurrency, distribution — mixes the two scales, and the tests pin only the start side (`subagent_ledger.test.ts:135-141`). | open | |
| 9 | low | src/scripts/hooks/subagent_ledger_hook.ts:402 | `run(stdin_text, options)` is exported and referenced nowhere in `src/` or `tests/` — the dispatcher reaches the concern via `main` through `CONCERN_REGISTRY` (`concern_registry.ts:110`) and the test file imports 7 of the module's 15 exports. Dead export. `unwrapPayload`, `extractEvent`, `extractAgentId`, `extractAgentType`, `extractParentId`, `extractLastMessage` and `ledgerFileFor` are likewise exported, unreferenced outside the module and untested. | open | |
| 10 | low | src/scripts/hooks/subagent_ledger_hook.ts:414-421 | `_resolveRoot` reads `cwd` at the ENVELOPE top level, but `dispatch_hook._build_envelope:542-554` puts the host payload — where `cwd` actually lives — under `payload`, alongside a top-level `workspace_root`. The `cwd` branch is therefore unreachable on the dispatcher path and only `workspace_root` ever resolves. Copied verbatim from `orchestration_record_hook.ts:323-330`, so the dead branch is inherited rather than introduced, but it is now duplicated. | open | |
| 11 | low | src/scripts/hooks/subagent_ledger_hook.ts:207 | The fenced-block regex takes the first match ANYWHERE in the message. With a preceding non-JSON fence (a triple-backtick block tagged `bash`) the `(?:json)?\s*\n` alternation cannot match at that opening fence, so the engine advances and matches at its CLOSING fence, capturing the prose between the two blocks — a candidate that can never parse. Harmless in isolation (it falls through to the brace span) but the fallback it falls into is finding 4. | open | |
| 12 | low | agents/roadmaps/road-to-subagent-lifecycle-integrity.md:363 | Two citations of the same call site disagree by one line inside the same section: the pre-existing prose says "the fork is decided — `delegation_nudge_hook.ts:342` wires it" while the note added directly beneath says "`recommendSliceTier` (`delegation_nudge_hook.ts:341`) calls the resolver", matching the evidence page's step-1 row (`subagent-lifecycle-phase0-host-pin.md:97`). One of the two is stale; the step's finding is published on the strength of that trace, so the citation should be single-valued. | open | |
