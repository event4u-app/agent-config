# Findings: feat-subagent-lifecycle-integrity
<!-- completion-review: v1 | reviewed: 2026-08-13 | scope: de440ba0d93ebaf2537518782922d87799e56aec7dbaf974bce274d30d6f8c00 | diff: 4d49029266a04d8d025e110be0a574553634acea | reviewer: r2-fresh-subagent-feat-subagent-lifecycle-integrity | prompt_hash: 843052bde01466bcdb6fdf4c637c19749339393f32b5c8fce63ab0ffd6b1ef02 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 4d49029266a04d8d025e110be0a574553634acea
  scope_hash: de440ba0d93ebaf2537518782922d87799e56aec7dbaf974bce274d30d6f8c00
  roadmap: agents/roadmaps/road-to-subagent-lifecycle-integrity.md
  roadmap_hash: a5c76c39162bd3147b9862798d900884985ce048fb6d238688846ba67861ab94
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-13T08:32:40Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/turn_end_gate_hook.ts:861 | A leaked open record disables the whole turn-end gate indefinitely, and the TTL that exists is not applied at this read. `openRecordStats` → `readOpenRecords` returns every file under `open/` with no age filter; reaping happens only inside `subagent_ledger_hook.processEnvelope` on the NEXT `subagent_start`/`subagent_stop` event. A dispatch that never returns — symptom #1, the thing this roadmap exists for — leaves a record; if no further subagent event occurs, nothing ever reaps it, and every turn-end in that workspace returns EXIT_ALLOW with no signal to anyone. The ledger header already anticipated this exact leak for the spawn guard ("four leaked records would eventually deny every spawn", subagent_ledger_hook.ts:66-70) and solved it with reap-on-next-event; the new consumer inherits the leak with the opposite polarity and no mitigation. Fix shape: filter by `OPEN_RECORD_TTL_MS` (subagent_ledger_hook.ts:142) inside `openRecordStats`, or give the gate a TTL-aware variant. The two new tests never exercise a stale record — `still refuses once the dispatch closes` removes the file by hand. | open | |
| 2 | high | src/scripts/hooks/turn_end_gate_hook.ts:861 | The allow path is whole-gate, not the scope the step specifies. Roadmap Step 2: "an open dispatch is an explicit **allow** path for **the completion-adjacent detectors**". The implementation `return EXIT_ALLOW`s before the detector list at :899, so it also disables detector B (language mismatch) and detector C (unverified edit). Neither is completion-adjacent: a reply in the wrong language is wrong whether or not a subagent is open, and "edited a file and ran nothing" is a fact about this turn's tool calls, not about outstanding async work. The file's own claim that this "only ever lets a turn END that would otherwise have been refused" is true and is precisely the problem — B and C had 19 and (per the header) measured occurrences behind them, and a single open dispatch now suppresses both. | open | |
| 3 | high | src/scripts/hooks/spawn_guard_shadow_hook.ts:126 | The depth arm is confounded by construction, so the curve this step exists to produce cannot support the derivation it is for. `depthEstimate = stats.max_depth + 1` maxes over ALL open records regardless of lineage, and every open record has depth ≥ 1 — so `depth_estimate ≥ 2` whenever anything at all is open. Candidate `n2m4` (`max_depth: 2`) therefore trips its `depth` arm on every spawn while any dispatch is open, including a flat parallel fan-out with zero nesting; its depth signal degenerates to "is anything open" and duplicates the concurrency arm. Parallel fan-out is the suite's own default orchestration mode and the ledger header calls it "the mode this instrument exists to measure". Consequence: `docs/contracts/concern-activation-policy.md` requires the shipped default to sit at the legitimate distribution's 99th percentile + 1, and a reader deriving that from this log reads the estimator's artefact as the distribution. The header's "upper bound, exact only for a linear chain" is honest about precision but not about the arms no longer being separable — which is exactly what tests/hooks/spawn_guard_shadow.test.ts:768 claims the record buys. | open | |
| 4 | high | src/scripts/hooks/spawn_guard_shadow_hook.ts:130 | An all-zero shadow line is emitted identically for three different states and no field distinguishes them: (a) a genuinely quiet estate, (b) a blind ledger — Phase 0 Step 2 ("assert `agent_id` arrives on this host") is still open, and `handleStart` writes an `unidentified` line and creates NO open record when the id is missing, so `open_count` would be 0 forever, (c) a producer/consumer root mismatch (finding 6). This reintroduces one layer up the exact failure the ledger's own R2 finding 1 fixed for itself — "An instrument that goes quiet is worse than one that reports nothing" (subagent_ledger_hook.ts:53-62): there, an empty ledger was made distinguishable from "no dispatches happened"; here, `concurrent_open: 0, depth_estimate: 1` is byte-identical in all three cases. tests/hooks/spawn_guard_shadow.test.ts:804 (`reads an empty estate as depth 1, not 0`) encodes the ambiguous reading as the expected one. The record needs an honesty marker (open-dir existence, or an unidentified-line count) before any percentile is read off it. | open | |
| 5 | medium | src/scripts/hooks/subagent_ledger_hook.ts:505 | The reap-side stop-loss arm is constant and therefore carries no information. `OPEN_RECORD_TTL_MS` is 24 h and the largest arm in `WALL_CLOCK_ARMS_MS` is 2 h, so a record only reaches this line when its age already exceeds every arm: `armsExceeded(now - started)` is always exactly `['5m','15m','30m','2h']`. The roadmap justifies this call site as "the reap is the only place a never-returning dispatch — the case the stop-loss actually targets — is observable at all", but the field there can never discriminate between arms. Discriminating data comes only from the stop path (:596), i.e. from dispatches that DID return — the population the stop-loss is not for. As shipped, Step 3's shadow cannot answer its own question. | open | |
| 6 | medium | src/scripts/hooks/turn_end_gate_hook.ts:844 | Producer and consumer resolve the consumer root by three different rules, so the ledger can be written somewhere the gate never reads. Ledger `_resolveRoot` (subagent_ledger_hook.ts:633): `workspace_root` → `project_root` → `payload.cwd` → `process.cwd()`. Gate (:844): `envelope.workspace_root` → `process.cwd()`, with no `project_root` and no `payload.cwd` fallback. Spawn guard (spawn_guard_shadow_hook.ts:149) is a third variant. On a host that supplies `project_root` or only `payload.cwd`, the producer writes under one root and the gate reads another; the allow path then never fires and Step 2 is silently inert — indistinguishable from "no dispatch was open" (finding 4). Both new gate tests plant the record under the same directory the envelope names, so neither can catch the divergence. One shared resolver, or the gate reusing the ledger's, removes the class. | open | |
| 7 | medium | agents/roadmaps/road-to-subagent-lifecycle-integrity.md:122 | Step 3 is marked `[x]` and Phase 3 is published as ✅ done / 100% while one of the two arms the step names is deliberately absent. The step reads "Wall-clock **and** tool-call-count stop-loss per open dispatch"; the note under it states plainly that the tool-call-count arm "is NOT implemented, and not faked". The honesty is right and the checkbox is not: `[x]` plus a 100% phase row in agents/roadmaps-progress.md tells every downstream reader (dashboard, archival sweep, the next planner) that the step's scope shipped. Same shape one step up — Step 1 is `[x]` while shipped in a posture (shadow) its own text does not describe (warn-first). Either narrow the step text to what shipped, or carry the residue as a `[~]`/blocker rather than as done. | open | |
| 8 | medium | agents/roadmaps/road-to-subagent-lifecycle-integrity.md:136 | The Step-1 falsifier was not updated when the posture changed, and as written it can no longer fail. It reads "The **warn window** shows zero would-have-fired events across ≥20 dispatches → the caps are solving a problem this estate does not have" — but Step 1 shipped shadow, so there is no warn window, and per finding 3 `n2m4`'s depth arm fires whenever anything is open, so "zero would-have-fired events" is close to unobservable regardless of whether the caps are warranted. A falsifier that survives its own mechanism's redefinition and can only resolve one way is not a falsifier; it should be restated against the shadow log and against a specific candidate/arm. | open | |
| 9 | low | src/scripts/hooks/spawn_guard_shadow_hook.ts:9 | "Exit is always 0" is asserted absolutely but one path is uncovered: `readHookStdin()` at :162 sits outside the `try`, so a throw there escapes `main()` and the process exits non-zero — on `pre_tool_use` a non-zero exit is a decision code, not a no-op. The same shape exists in `subagent_ledger_hook.main()`, so this is a pattern rather than a novelty; what is new is the unqualified claim. The tests reinforce the gap: `never denies: every input returns EXIT_ALLOW` (tests/hooks/spawn_guard_shadow.test.ts:735) exercises `processEnvelope` only, never `main()`, which is the function the manifest actually binds. | open | |
| 10 | low | src/scripts/hooks/turn_end_gate_hook.ts:862 | The `try`/`catch` around `openRecordStats` is unreachable and its comment claims a guarantee it does not provide. `readOpenRecords` (subagent_ledger_hook.ts:358) already swallows the `readdirSync` failure and every per-file read/parse failure, returning an empty map; there is no throwing path left for this catch to see. Harmless at runtime, but "an unreadable ledger is not a reason to change the verdict" now documents a guard one layer above the one that actually enforces it — the next reader who moves the swallow out of `readOpenRecords` will trust the wrong line. | open | |
| 11 | low | src/scripts/hooks/spawn_guard_shadow_hook.ts:59 | Envelope helpers are duplicated instead of reused, and that duplication is what makes finding 6 possible. The file re-declares `JsonValue`/`JsonObject` (:59-60), `isObject` (:62), `unwrapPayload` (:106) and `_resolveRoot` (:149), while its sibling in the same directory imports `unwrap`, `JsonObject` and `JsonValue` from `./envelope.js` (turn_end_gate_hook.ts:128). Three independent copies of root resolution now have to agree by inspection; nothing fails if they drift. | open | |
| 12 | low | src/scripts/hook_manifest.yaml:578 | The manifest cannot express the posture the new policy just introduced. `docs/contracts/concern-activation-policy.md` defines three postures and separates shadow ("emits to the model: nothing") from advisory ("one line of context"), but `severity:` accepts only `advisory`/`blocking`, so this concern declares `advisory` and its shadow status survives only in prose plus a `posture: 'shadow'` string inside its own output. The policy's honest-enforcement section then claims "a concern cannot exceed its declared posture by accident, only by declaring a different one" — true for the two severities the manifest has, not for the rung the policy adds: a shadow concern declared `advisory` has headroom to emit context with nothing mechanical objecting. | open | |
| 13 | low | src/scripts/hooks/subagent_ledger_hook.ts:66 | Header prose went stale in a file this diff edits. "Phase 3's pre-registered spawn guard reads this same set at `concurrent-open >= 4`, so four leaked records would eventually deny every spawn" describes a guard that, as shipped in this same branch, has no code path that can deny. The paragraph is also the natural place to record the leak's new consequence (finding 1) — the same leaked-record failure now silently disables a gate rather than denying a spawn — and it does not mention the second consumer the diff added directly beneath it. | open | |
