# Findings: road-to-governed-evidence-production
<!-- completion-review: v1 | reviewed: 2026-09-02 | scope: 90a10f5fe0ebfb2841ffdb85d7694ca53554c2a5b4db3f08d5f809a547a21824 | diff: 78d2f2ebe6601ffcf3843499ae9fd9bfa4927b83 | reviewer: r2-fresh-subagent-road-to-governed-evidence-production | prompt_hash: 86006587b2489632dacd68a3a248f7bb1c403a45a6161e6136969e6bd68d80db -->
<!-- {"review-independence":{"review_independence":"single-member","context_relation":"fresh","acceptance_status":"provisional","assurance":"single-pass","reviewers":["r2-fresh-subagent-road-to-governed-evidence-production"]}} -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-09-02 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 78d2f2ebe6601ffcf3843499ae9fd9bfa4927b83
  scope_hash: 90a10f5fe0ebfb2841ffdb85d7694ca53554c2a5b4db3f08d5f809a547a21824
  roadmap: agents/roadmaps/road-to-governed-evidence-production.md
  roadmap_hash: 7fbb5c4fe6e43ef975754fbf184e41fe00a03d22a37390a4502281353c12cbde
  ac_hash: 12be74a0757549057f43220e2303ac14f6185b864429738de565002226893c34
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-09-02T04:40:25Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/_lib/candidate_pair_delta.ts:289 | `comparisonVector`'s mandatory artifact-count row is a structural constant zero, so the sprawl ceiling it exists to police can never fire. `pairingKey` (:71-74) folds the sorted mutation-path list into the key, and `pairCandidates` (:129-148) forms a pair only on an exact key match, so control and treatment of every pair carry identical path multisets; the distinct-path difference at :290-291 is therefore identically 0. Probed: delta = 0, and the one shape that would make it non-zero (treatment mutating an extra path) is refused by pairing before the vector is built. Consequence: `gateVerdict`'s `ARTIFACT_COUNT_METRIC` branch (`evaluation_vector.ts:255`) is unreachable for every vector this producer builds, defeating `buildVector`'s stated invariant (`evaluation_vector.ts:114-118`) that a candidate cannot reach promotion without a measured artifact delta. Unpinned: the only test (`tests/scripts/candidate_pair_delta.test.ts:194-210`) asserts the row's presence and never its value, so the constant survives any future change. | fixed | `0fa4a850` — the derivation is deleted and the delta is an argument, refused unless a finite integer. The row's VALUE is now asserted for four deltas, a positive delta is asserted to block a vector that otherwise passes (the ceiling branch, reachable), and a separate pin asserts both arms of a pair always carry the same path set, so a future re-derivation has to delete that assertion first. Sensitivity: restoring a pair-derived delta reds 2 tests; restored byte-identically, `shasum -c` OK. |
| 2 | medium | src/scripts/_lib/corpus_manifest.ts:222 | The refusal that `enumerateCorpus` says it implements — "a capture before generation would pin an empty subject and read as a successful pin of nothing" — only covers a MISSING corpus directory. An existing-but-empty `.claude/rules` returns `included: []`, `captureManifest` produces a well-formed manifest, and two such captures compare `SUBJECT EQUIVALENT`. Probed on a temp tree: `included = 0`, `digest = d6aab110d5473b73`, `subjectsEquivalent = true`. `verify` prints `SUBJECT EQUIVALENT` with no subject count and exits 0, so the contract's equivalence Iron Law certifies two runs that measured nothing as having measured the same thing. Reachable: the directory survives a generation that produces zero files or an operator deleting its contents. Test coverage stops at the missing-directory case (`tests/scripts/corpus_manifest.test.ts:80-83`). | fixed | `3fba34ea` — `enumerateCorpus` refuses an existing-but-empty corpus directory with its own message, and `parseManifest` refuses an empty `included` so a hand-written empty pin cannot enter `verify` either. Sensitivity: removing the refusal reds 2 tests; restored byte-identically, `shasum -c` OK. |
| 3 | medium | src/scripts/_lib/corpus_manifest.ts:258 | `causes_skip` asserts a skip the generator does not perform. `captureUserScope` calls `dedupableRules` unconditionally, while the generator calls it only when `projection.scope_dedup` resolves true (`src/scripts/condense.ts:1106-1112`, `:445-459`). With the setting off — the state this very contract measured, and the state reproduced here (`generator_config['projection.scope_dedup'] = 'absent:default-off'`) — a byte-identical twin is recorded `causes_skip: true` although nothing skips, and the CLI prints the count bare as "N user-scope skip(s)" (`src/scripts/corpus_manifest.ts:161`). This contradicts the function's own claim at :243-245 that "a manifest and a generation cannot disagree about what a skip is", and re-introduces exactly the mis-attribution the new contract section was written to correct. Second axis: the rule population passed is every `*.md` under `dist/agent-src/rules` (119 here) whereas the generator passes `_scoped_rule_basenames()`, which filters by workspace scope and manual type first. Latent rather than live in this checkout only because 0 of 104 twins are currently byte-identical. `diffManifests` compares `user_scope.skipping` (:517-519) but never `generator_config`, so the one field that would disambiguate is outside the comparison. | fixed | `3fba34ea` — the twin carries `byte_identical` (the replayed comparison) and `causes_skip` (that fact AND the setting), `dedupEnabled` is a required argument read off the same string the manifest records, and `diffManifests` now compares both lists plus every `generator_config` key. RESIDUAL, recorded not narrowed: the population is still the projection-source superset, because replicating the generator's four private filters (`_scoped_rule_basenames`) here is the re-implementation this module refuses everywhere else — so it is recorded in the manifest as `user_scope_population` and documented in the contract, and a caller holding the generator's list passes it. Sensitivity: folding `causes_skip` back onto byte identity reds 2 tests; restored byte-identically, `shasum -c` OK. |
| 4 | medium | src/scripts/_lib/corpus_manifest.ts:313 | `projection.partition_active` is not a fact about the captured `(repoRoot, userHome)` that every sibling field in the same struct is derived from. `partitionActive(repoRoot)` resolves through `resolvePartitionVerdict` (`src/install/partitionEligibility.ts:274-297`), which reads `hostLayerInputs(os.homedir())` and `read_lockfile()` with no path — so it ignores `userHome` entirely, and the CLI deliberately prefers `process.env['HOME']` over `os.homedir()` (`src/scripts/corpus_manifest.ts:118`), making the two diverge whenever HOME is overridden (containers, sudo, CI). It is also process-memoised, so a second capture in one process reuses the first verdict. Demonstrated: running the two new test files prints `projection mode: standalone/full — version mismatch (installed 14.13.0, building 0.0.0)`, i.e. the temp-tree fixtures read the operator's real installed lockfile and real home layer — contradicting the test file's own header claim (`tests/scripts/corpus_manifest.test.ts:9-10`) that "nothing here reads or writes the repository it runs in", and making the field, and the `projection.partition_active` diff line, machine-dependent. | fixed | `3fba34ea` — the field is nullable (`boolean` or `null`), `null` by default, and the predicate is injected; the CLI supplies it because it is the one caller for which those process globals are the run's own host. The two new test files now print no `projection mode:` line at all (measured: 0 occurrences), so the temp-tree fixtures no longer read the operator's lockfile and home layer, and the test header's claim holds. Sensitivity: defaulting the field to a value instead of `null` reds 1 test; restored byte-identically, `shasum -c` OK. |
| 5 | low | src/scripts/_lib/candidate_pair_delta.ts:238 | `compareArms` checks only one direction of the outcomes-to-pairs correspondence. A pair with no trials is refused (:239-244) but an `outcomes` key matching no pair contributes no deltas and raises nothing, so a mis-keyed or stale measurement set silently shrinks the denominator — the same failure the refusal one line above names, and the opposite of `pairCandidates`, which refuses an unmatched record on BOTH arms (:130-148). A key is easy to mis-derive: it is a formatted string over dimension plus sorted paths, not a value the caller receives from this module. Related: the duplicate-`trial_id` refusal in `pairedDeltas` (:186-191) is scoped per pair, so one id reused across two pairs is not detected. | fixed | `0fa4a850` — an `outcomes` key matching no pair is refused, so the correspondence is checked in both directions like `pairCandidates`. The duplicate-id half is closed by stating the scope the module never stated: `TrialOutcome.trial_id` is unique across the WHOLE comparison, not within its pair, because `compareArms` pools every pair's deltas into one sample; `compareArms` refuses a reuse across pairs. Sensitivity: each refusal removed reds 1 test; restored byte-identically, `shasum -c` OK. |
| 6 | low | src/scripts/corpus_manifest.ts:181 | `--limit` is documented for `verify` (usage line :56, option text :61-62) and is silently ignored by it: the re-capture uses `expected.included.length` whenever the manifest carries any subject, which is every real manifest. An operator passing `--limit` to a verification gets no effect, no warning, and a green result that did not honour the flag they supplied. | fixed | `3fba34ea` — `parseArgs` refuses `verify --limit` as a usage error rather than accepting a flag it cannot honour, the usage text says so, and `verify` no longer reads the pre-registered budget at all. Sensitivity: removing the refusal reds 1 test; restored byte-identically, `shasum -c` OK. |
| 7 | low | src/scripts/_lib/corpus_manifest.ts:547 | The pin's internal consistency is never checked. `parseManifest` validates only `version` and the presence of `subject_digest` and `included`; nothing recomputes `subjectDigest(expected.enumeration_rule, expected.included)` against the stored digest, and `subjectsEquivalent` (:536-541) compares the stored value. A manifest whose digest and body disagree — hand-edited, truncated, or emitted by a future bug — is then adjudicated on a value its own `included` list does not support, and the per-path rows printed alongside describe a different subject than the verdict does. Cheap to close: the recomputation is one call to an already-exported function. | fixed | `3fba34ea` — `parseManifest` recomputes `subjectDigest(enumeration_rule, included)` and refuses a mismatch, and validates `enumeration_rule` (which is inside the digest) rather than only the two fields it read before. Sensitivity: removing the recomputation reds 2 tests; restored byte-identically, `shasum -c` OK. |
| 8 | low | docs/contracts/metered-proposer-protocol.md:346 | Normative sentence does not parse: "`decidePairedVerdict` would return `underpowered` — which the roadmap's own record rules does not satisfy the acceptance criterion." The subject of "does not satisfy" is missing, so the clause reads as its own inverse. This is the load-bearing sentence of the STILL UNSET amendment, i.e. the recorded reason the protocol slot was not frozen; the parallel sentence in `agents/evidence/analysis/drain16-phase2-preparatory-work.md` §3 leg 3 is well-formed and shows the intended wording. | fixed | `78d2f2eb` — reworded to the shape the well-formed parallel in `drain16-phase2-preparatory-work.md` uses: the record rules THAT `underpowered` does not satisfy the criterion, because it records that adjudication was unavailable. |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
