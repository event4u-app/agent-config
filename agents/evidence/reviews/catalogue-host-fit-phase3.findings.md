# Findings: catalogue-host-fit-phase3
<!-- completion-review: v1 | reviewed: 2026-08-18 | scope: 1b7f3c9807423b8252e9a305987de87563323d907bc64d9e9b1c6c0aee50387c | diff: 0720e1d434bf658dd668bef64ca742897f72594d | reviewer: r2-fresh-subagent-catalogue-host-fit-phase3 | prompt_hash: f1e80e7d94529d5cf35aa0e4374b1b90a19004ff345c36bd12eca24282111b4c -->
<!-- evidence-type: v1 | type: current-binding | declared: 2026-08-18 -->

<!-- context-manifest: v1
inputs:
  diff_sha: 0720e1d434bf658dd668bef64ca742897f72594d
  scope_hash: 1b7f3c9807423b8252e9a305987de87563323d907bc64d9e9b1c6c0aee50387c
  roadmap: agents/roadmaps/road-to-catalogue-host-fit.md
  roadmap_hash: cc9a9e07ade96e25d9a03b5909bfc44f9ce719e07944b00b24862bc9c0b4f16f
  ac_hash: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-18T12:25:48Z
-->

**Re-bound in place** (contract §2.7 path 1) after the fix pass: the fixes moved the
review scope, so `scope:` and `diff:` now name the fixed head and every row carries a
terminal status with its commit. The findings text is the reviewer's, unedited — only
the Status and Reason/Ref cells and the binding hashes changed. Row 4 is the one
`accepted-risk`: the wrong inference it named is fixed, the instrument gap behind it is
declared rather than closed, and the reason cell says why closing it would be worse.

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/hooks/skill_route_hook.ts:257 | The registered numerator counts the wrong population. `suppressed = rows.length - deliverable.length` counts drops across the ENTIRE ranked list — `rank` returns every non-zero-scoring skill (header line 22: it "almost always returns SOMETHING") — while the metric registered in `hook-token-budget.json:60` defines it as "how often the ranker wanted to POINT AT a skill the current host is KNOWN to have delivered without its description". A bare name sitting at rank 40 was never pointable, yet it bumps the count. With 16 bare names spread over a several-hundred-row list, most fires will carry a non-zero count, so the live rate approaches 100 % and the registered upper falsifier ("far above the 16/338 share ⇒ the ranker preferentially ranks what the host degrades ⇒ a Phase 2 finding") fires on a perfectly healthy join. Count the drops inside the pointer window (`rows.slice(0, TOP_K)` vs `deliverable.slice(0, TOP_K)`) or restate the definition to match the code. | fixed | c41b89a1c — `suppressed` scoped to `TOP_K` of the unfiltered ranking; fixture suppresses the whole tail below the window and asserts 0 |
| 2 | high | src/scripts/_lib/skill_catalogue_series.ts:470 | A superseded observation keeps suppressing, which is the exact failure the function documents itself as preventing. The `per-entry` filter runs BEFORE the latest-wins pick, so a NEWER non-per-entry record for the same host never invalidates an older per-entry one: a host that switches to `budget-strip-and-drop` (the header notes host behaviour does move — "the codex drop moved 393 → 401 → 402 → 330 across four readings") goes on being filtered against a stale bare set forever. That contradicts both documented invariants — "LATEST WINS … a union would let a superseded observation keep suppressing a skill the host now delivers fine" and "a `budget-strip-and-drop` host is therefore always `null` here", which holds only when the host has no earlier per-entry record. Nothing pins it: the fixture at `tests/scripts/catalogue_capture.test.ts:747` mixes two DIFFERENT hosts, never one host across a mode change. | fixed | 42e895514 — mode is now read OFF the winning record; `headlineRecordPerHost` picks it. Fixture is ONE host across a mode change, both input orders, plus the reverse direction |
| 3 | high | tests/hooks/skill_route_hook.test.ts:139 | The AC-3 filtered-direction fixture asserts that the defect still exists, so the sibling phase succeeding reds it. It reads the live committed log (`knownBareForHost('.', 'claude')`) and asserts `bare!.size > 0` plus "the unfiltered line named a bare skill" against the live `src/skills` tree. Phase 2 AC-2 of this same roadmap is "the capture records no bare entries" — the moment that lands, the next claude observation is legitimately an EMPTY SET, `knownBareNames` returns it, and this test fails with no defect present. The vacuity guard is the right idea but is bound to mutable data: derive the suppressed name from the log (skip/soft-pass when the latest observation is clean) instead of requiring it to be non-empty. The same coupling makes `:177-178` (`rows[0] ≥ 31`, `rows[1] < 31` on the live corpus) red on any catalogue edit that reshuffles two scores, and `rows[1]!` throws outright if the prompt ever ranks a single row. | fixed | c41b89a1c — a clean or absent observation is now a legitimate branch asserting a no-op; the silence test asserts the property instead of two literal live scores; the `[1]!` index is derived |
| 4 | medium | src/scripts/hooks/skill_route_hook.ts:325 | The one case the roadmap calls load-bearing is the one the instrument cannot see. When suppression drops the line to silence, `main` returns `EXIT_ALLOW` before any stdout, so the `, N suppressed as host-bare` reason is never emitted. Consequence for the registered falsifier: total suppression on a host that DOES have a per-entry observation yields a live rate of 0, which `hook-token-budget.json:60` reads as "the ranker and the host's bare set do not intersect in practice" — the precise opposite of what happened. The prose claim that "`suppressed` is what distinguishes that silence from an unranked prompt" holds at the API boundary (`routeDecision`) and not at the log, which is where the metric is read. | accepted-risk | Instrument gap accepted, wrong inference fixed in 03fe4732d. Emitting a warn line with no pointers to make the counter observable would inject an empty context block into a real turn — a worse trade than a declared blind spot. The registration now states that a 0 rate does NOT establish non-intersection and names `capture_skill_catalogue --pointable-bare` as the instrument that separates the two readings |
| 5 | medium | src/scripts/hooks/skill_route_hook.ts:320 | The only unpinned link in the chain is the production wiring, and it fails open silently. The host comes from `env["platform"]` and is matched by exact string equality against the log's `host` labels; the diff adds no test that exercises `main()` or an envelope (the pre-existing suite has none either), so neither the envelope key nor the label vocabulary is pinned anywhere. Both failure modes are indistinguishable from "no observation": wrong key or absent field ⇒ `host === null` ⇒ `null` ⇒ no filtering. Phase 3 could therefore be inert on every host in production with all seven new fixtures green — the silent-no-op shape, on a feature whose whole value is that it fires. | fixed | c41b89a1c — three tests drive `main()` over a real envelope; one reads the host label vocabulary out of the log rather than hardcoding it. Probed live: `platform: claude` 9 suppressed, absent none, `codex` none |
| 6 | medium | src/scripts/_lib/skill_catalogue_series.ts:472 | `observed_at` is compared with no type guard while `bare_names` gets `Array.isArray`. A record whose `observed_at` is missing or non-string is accepted when it is the first candidate (`latest === null`) and then wins permanently, because BOTH `undefined >= "2026-08-12"` and `"2026-08-12" >= undefined` are false — so no well-formed later record can ever displace it. One malformed line in an append-only log with more than one producer (the module's own stated threat model) pins the filter to that line's bare set. It also inverts the module header's rule that "an unparseable date reads as DUE, never as fresh": here it reads as the freshest record in the series. Guard with `typeof record.observed_at === "string"` beside the array guard. | fixed | 42e895514 — `typeof observed_at === "string"` pre-filter before the shared reducer, chosen over changing `_supersedes` because that reducer has other callers |
| 7 | medium | src/scripts/_lib/skill_catalogue_series.ts:467 | A third per-host latest-record reducer, in the file whose docstring argues against exactly that. This module already imports `headlineRecordPerHost` (used at `:121`) and defines `latestPointableBarePerHost` (`:336`), yet `knownBareNames` restates the tie-break inline. The comment cites `_supersedes` as the precedent for why duplicate reducers are dangerous and then adds one, so any precedence rule the parent applies (e.g. `observation_source`: a `host-event` reading outranking a same-date `self-report`) is silently not inherited by the runtime filter — two readers breaking the same tie in different directions is the recorded failure this file names. | fixed | 42e895514 — delegates to the `headlineRecordPerHost` this module already imports, so parent precedence is inherited rather than restated |
| 8 | medium | agents/roadmaps/road-to-catalogue-host-fit.md:290 | AC-3 alone became a checkbox, so an acceptance criterion is now counted as a step. AC-0/AC-1/AC-2 (`:82`, `:193`, `:220`) remain plain `- **AC-n:**` bullets; converting only AC-3 to `- [x]` moved the dashboard total 8 → 9 and helped carry the published figure 29 % → 62 % (`agents/roadmaps-progress.md:29`) with work that is a verification claim rather than a step. Either every AC in the file is a tracked step or none is; a mixed convention makes the roadmap's own denominator unreadable and the percentage non-comparable with its siblings. | fixed | 0720e1d43 — AC-3 reverted to a plain bullet, matching AC-0/1/2; dashboard regenerated back to 289/578 |
| 9 | low | agents/roadmaps/road-to-catalogue-host-fit.md:289 | The 3.2 `<!-- verify: -->` annotation carries prose ("agent-config settings dump is not the instrument — read src/config/hook-token-budget.json") where every other HTML verify comment in the file carries a runnable command (`:167`, `:192`, `:267`). The point it makes is a real one and belongs in the step body; as a `verify:` value it leaves the step with nothing mechanically re-runnable and hands any consumer of the annotation an unexecutable string. | fixed | 0720e1d43 — the prose moved into the step body; the annotation is now a runnable `node -e` check over the registry row |
| 10 | low | src/scripts/hooks/skill_route_hook.ts:184 | The cost paragraph was re-argued only on the free path. Every firing prompt now additionally reads and JSON-parses the whole observation log, uncached, per prompt (`knownBareForHost` → `readObservationLog`), yet the header still presents the pre-branch "12.3 ms warm … about 5 % of the 250 ms p95 budget" as the cost of a prompt that reaches the ranker. The new paragraph only establishes that the SUB-floor path stays at 0 ms. The added cost is likely small, which is an argument for measuring it once and saying so, not for leaving a measured claim describing a path that changed. | fixed | c41b89a1c — measured: 0.015 ms median / 0.022 ms p95 (n=2000, warm) against an 8.3 ms ranked pass. Header also states that `bench_hook_latency` cannot see it (its payload carries neither `prompt` nor `platform`) |
| 11 | low | src/config/hook-token-budget.json:60 | The falsifier baseline is a bare ratio with an ambiguous denominator: `16/338` "of the observed catalogue", while the ranker catalogue is cited as 337 entries in the consuming hook (`skill_route_hook.ts:236`). A future reader cannot tell whether 338 is the host-observed entry count or the ranker catalogue size, and the two are the populations the join deliberately keeps apart. Name the denominator (`entries_total` of the 2026-08-12 claude observation, or the ranker catalogue) so the comparison is reproducible. | fixed | 03fe4732d — all three populations named: `entries_total` 336, 35 enumerated (16 bare + 19 described), ranker catalogue 338. The comparable share is 16 of 35 |
<!-- reviewer fills the table; 0 findings => replace the table with the exact honest-null line per docs/contracts/plan-review-gates.md §2.3 AND change the evidence-type to `honest-null` per docs/contracts/evidence-artifact-types.md §4 -->
