# Findings: skill-catalogue-budget
<!-- completion-review: v1 | reviewed: 2026-08-15 | scope: f4e56896fa1dbbc2d41c5601f0f9ad46bf612958e9e263a4b4c450fec23ab628 | diff: 2bf0c479582fae419a6bc634f6380c3aaefa56b8 | reviewer: r2-fresh-subagent-skill-catalogue-budget | prompt_hash: 5dd35cc9e26aa9f51b883a8698eed1adc8e907f6944b3aacde78664e330298cd -->

<!-- context-manifest: v1
inputs:
  diff_sha: 2bf0c479582fae419a6bc634f6380c3aaefa56b8
  scope_hash: f4e56896fa1dbbc2d41c5601f0f9ad46bf612958e9e263a4b4c450fec23ab628
  roadmap: agents/roadmaps/road-to-skill-catalogue-budget.md
  roadmap_hash: fc13a6e4754a4af2dbba20aafdbc3675cf99b42b9d7ee4838c405fad1893ce75
  ac_hash: d2c4dd7abfeaa43dcd07e365807c5d0b868b49e27ada64588e2eda83531ecffb
excluded: [session-history, agents/runtime, implementation-context]
tools: [git-diff-branch-scoped, file-read-branch-paths]
dispatched: 2026-08-15T00:00:00Z
-->

| # | Severity | File:Line | Finding | Status | Reason/Ref |
|---|----------|-----------|---------|--------|------------|
| 1 | high | src/scripts/capture_skill_catalogue.ts:678-681, 711-720 | The under-coverage condition guards only the human stdout branch; the persisted record and the `--json` output still publish the clamped survivor count with nothing marking it underived. `codexProjectionUndercovers` is computed inside the `else` branch; `--json` never computes it and `--record` appends `record` verbatim. CONFIRMED by running the script against a fixture (2 skills + 1 command, host event `dropped: 10`): `--json` emits `bare_count: 0` with no signal, and `--record` persists it. A later reader of the corpus cannot distinguish a measured 0 from a clamp. This is the exact failure the code comment and the analysis doc claim was avoided — avoided on the one channel nobody persists. The committed row is not under-covering, so the defect is latent, not live. | fixed | 2bf0c4795 |
| 2 | medium | src/scripts/capture_skill_catalogue.ts:700-701 | The under-coverage warning names a different denominator than the predicate that fired it: the predicate compares against skills + commands, the message prints skills only. CONFIRMED by running: output reads `catalogue entries offered: 3` and four lines later `while this root offers only 2`. | fixed | 2bf0c4795 |
| 3 | medium | src/scripts/ai_council/low_impact.ts:631 | The low-impact fast path re-pins a concrete model id onto the CLI client, bypassing the new `auto` sentinel: `client.model = member.model_ladder[0]` immediately before `ask()`. With the template this diff ships, an openai member on the cli transport gets `--model gpt-4o-mini` — the pinned-id shape the fix exists to remove. `model_downgrade.enabled` defaults to true. CONFIRMED by reading; no test covers it. | fixed | 2bf0c4795 |
| 4 | medium | docs/contracts/ai-council-config.md:247-255 · docs/contracts/ai-team-config.md:88-89 | Two contract docs now state the opposite of the code and neither is in the branch: both describe `DEFAULT_OPENAI_CLI_MODEL` as a deliberate PIN and cite `CLI gpt-5` as the proof value. After this change the omitted default is `auto` and that value no longer exists. CONFIRMED by reading both. | fixed | 2bf0c4795 |
| 5 | medium | src/scripts/ai_council/config.ts:1854 · src/scripts/ai_council/clients.ts:563, 862, 1698, 2011 | `model: auto` is now blessed for every provider but honoured only for openai. The ladder exemption is provider-agnostic and the new test exercises it on anthropic, but the anthropic and gemini clients emit `--model` unconditionally and their api clients have no `_isAutoModel` mapping. The diff traded a fail-fast config error for an opaque runtime failure on two providers. CONFIRMED by reading. | fixed | 2bf0c4795 |
| 6 | low | src/scripts/capture_skill_catalogue.ts:530-545 | Two defects in `countCommandBodies`: (a) the `.md` suffix test precedes the directory branch, so a directory named `*.md` counts as one command and is never descended into (CONFIRMED empirically: 4 counted where 5 is correct); (b) the `fs.existsSync(path.join(abs, '.'))` guard is a no-op because `path.join` normalises the `.` away, and the symlink handling the comment credits it with is done by `statSync` alone. | fixed | 2bf0c4795 |
| 7 | low | src/scripts/capture_skill_catalogue.ts:718 | `--json --record` writes the `recorded →` note to stdout after the JSON document, so the machine-readable mode is not pipeable. Belongs on stderr. CONFIRMED by reading. | fixed | 2bf0c4795 |
| 8 | low | src/scripts/ai_council/transport_resolver.ts:377-384 · tests/scripts/ai_council/clients.test.ts:676 | The api-rung fall-through the new comment and test assert is performed by no shipped path: `isFallbackEligible` and `MidFlightFallback` have zero call sites outside their own module and tests. Pre-existing dead scaffolding, but this diff adds a live-sounding claim on top of it. CONFIRMED by grep. | fixed | 2bf0c4795 |
| 9 | low | src/scripts/ai_council/transport_resolver.ts (absentReasonFromCliFailure doc block) | The doc block still enumerates the unmapped classes without `model_unservable`, which is now a fourth and falls to the `unavailable` bucket the commit message says this replaced. The `detail` field does carry the reason, so this is an incomplete improvement rather than a regression. CONFIRMED by reading. | fixed | 2bf0c4795 |
| 10 | low | agents/roadmaps/road-to-skill-catalogue-budget.md:75 vs agents/evidence/analysis/skill-catalogue-budget-codex.md | Two figures for the same quantity conflict inside this branch: the roadmap Context says 60,483 bytes across 298 skills, the analysis doc says 55,114 across 297. The hand-computed figure is what `description_bytes` was added to replace and was left standing with no note saying which supersedes. | fixed | 2bf0c4795 |
| 11 | low | agents/evidence/reviews/skill-catalogue-budget.review-input/diff.patch | Review-scope gap: `git diff --stat` reports 18 files, `diff.patch` carries 17 and the prompt lists 16. The omitted file is `agents/evidence/metrics/skill-catalogue.jsonl` — the corpus row acceptance criteria A and C are graded on. CONFIRMED by `git diff --stat`. | accepted-risk | Dispatcher-side scope construction, not a branch defect — nothing in this diff can fix it. Accepted for THIS review because the reviewer read the omitted corpus row out-of-band and confirmed it present and well-formed, so criteria A and C were graded on real evidence. The tooling gap itself outlives this branch. |
| 12 | low | src/scripts/capture_skill_catalogue.ts:625-633 | Codex mode still requires the repo default catalogue root to resolve and be non-empty before reaching the `--codex-events` branch, although `projected` is unused there. A capture run from a checkout without that tree fails with a message about a tree the invocation deliberately replaced. CONFIRMED by reading. | fixed | 2bf0c4795 |
