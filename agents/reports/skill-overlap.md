# Skill-family overlap report (6.0.0-C Phase 4 Step 8)

> Content cosine-similarity over 292 skills; pairs at ≥ 70%. **Same-domain pairs (shared `packs:`) are the merge candidates** consumed by [`evidence-based-pruning.md`](../../docs/contracts/evidence-based-pruning.md). This report merges NOTHING — it is input to a future, human-driven consolidation roadmap.


- Skills scanned: **292**

- Overlap pairs ≥ 70%: **0** (0 same-domain merge candidates, 0 cross-domain)


## Merge candidates — same-domain, ≥ threshold

None — no same-domain pair exceeds the overlap threshold.


## Cross-domain overlaps (informational, not merge candidates)

None.

