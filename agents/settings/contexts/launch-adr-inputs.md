# Launch-ADR inputs — positioning findings filed for the decision

> Durable input file for the launch-decision ADR owned by the
> feedback-9.8.0 followups roadmap (Phase 1). Filed by the
> road-to-ai-employee-borrowings roadmap (2026-07-27) so the ADR reads
> recorded findings, not session memory. Roadmaps named by slug per
> `no-roadmap-references`; external sources anonymized per
> source-confidentiality.

## Category-absence finding (2026-07-27)

A public "AI employees" category map (a curated awesome-list; anonymized —
maintainer-recoverable link in the ai-employee-borrowings roadmap's
provenance block) lists ~12 software-engineering agents. **None is a
governance layer.** Our category does not exist on the map that a buyer
in the adjacent category would consult. This cuts both ways and the ADR
should weigh both readings: (a) zero competition for the governance-
substrate position — the positioning candidate "governance substrate
underneath AI coworkers; category vendors are customers, not competitors"
has an empty field; (b) zero demonstrated demand — nobody is searching
for what the map does not name, which is the adoption roadmap's H3
hypothesis (category absence, not quality, as the adoption constraint)
and must be tested, not asserted.

## Positioning candidate (routed from the same intake)

"Governance substrate underneath AI coworkers — the AI-employee vendor is
a customer, not a competitor." Status: CANDIDATE input, one among the
launch ADR's inputs, never a decided fork. Supporting artifact: the
honest eight-capability audit
(`agents/evidence/reports/coworker-capability-audit.md`) — six N/A/No
rows, two Strong rows (identity/permissions/audit; model flexibility),
the two the category treats as an afterthought.
