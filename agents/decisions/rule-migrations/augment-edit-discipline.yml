# Migration ledger for `augment-edit-discipline` — what the body-migration transform kept, folded, and lost.
#
# `source_headings` is the HARVEST, not a cache: the pre-migration commits are
# not ancestors of HEAD and a remote branch prune would destroy them, so the
# headings are recorded here to survive that. See lint_rule_migration_ledger.
rule: augment-edit-discipline
source: born_thin
source_commit: f0d1b3c089f425e4c295a38f537600fbe4c65b9a
source_headings: []
rows: []
# No pre-migration body ever existed: this file was ADDED already carrying
# its migration pointer. That is a different fact from 'the body is lost',
# which is why it has its own `source:` value rather than an empty row set.
