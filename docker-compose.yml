services:
  test:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    entrypoint: ["sh", "-c"]
    command: ["composer install --quiet && vendor/bin/phpunit && python3 -m unittest tests.test_compress -v"]
