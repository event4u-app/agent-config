node_modules/
dist/
.wrangler/
*.log
.dev.vars

# Generated by `python scripts/pack_mcp_content.py` at deploy time —
# never hand-edit. The committed `content.json` is the dev stub and is
# overwritten by the packer; the .gz + manifest are deploy-only artefacts.
content.json.gz
manifest.json
