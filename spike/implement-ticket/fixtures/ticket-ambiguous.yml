# Ambiguous ticket — `refine` step should return `blocked` with
# numbered options. Used to prove block-on-ambiguity works end-to-end.
id: DEV-1002
title: Make the export faster
body: |
  The CSV export is too slow. Make it faster.
acceptance_criteria: []
source: jira
persona_hint: senior-engineer

# Expected spike behaviour:
#   refine    -> blocked (empty AC + vague body — asks numbered questions)
#   remaining steps never run
#   report    -> partial report up to the block
expected_final: blocked
expected_block_step: refine
