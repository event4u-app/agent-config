# Seed profile — developer
# Schema: docs/contracts/profile-system.md
profile:
  id: developer
  # Base capability packs for scoped projection (ADR-040 / capability-packs.md).
  # `meta` is always_on (seeded by the resolver). Language/framework packs
  # (laravel, php, react, …) are added per session via the runtime overlay.
  packs:
    - engineering-base
  audience:
    label: "IC engineer"
    readme_anchor: "developer"
  defaults:
    preset_id: balanced
    personas:
      - reviewer
      - security
    skills_hint:
      - developer-like-execution
      - verify-completion-evidence
      - minimal-safe-diff
      - systematic-debugging
      - test-driven-development
  surface:
    commands_hint:
      - work
      - implement-ticket
      - review-changes
      - fix
      - commit
    docs_first_pointer: "docs/getting-started-by-role.md#developer"
