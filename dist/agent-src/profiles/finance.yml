# Seed profile — finance
# Schema: docs/contracts/profile-system.md
profile:
  id: finance
  # Base capability packs for scoped projection (ADR-040 / capability-packs.md).
  # Self-sufficient: dcf-modeling + scenario-modeling are finance-advanced, so
  # a CFO profile must ship it. AI-council resolved Option A, 2026-06-03.
  packs:
    - finance-basic
    - finance-advanced
  audience:
    label: "CFO / fractional finance / FP&A"
    readme_anchor: "finance"
  defaults:
    preset_id: strict
    personas:
      - finance-partner
      - reviewer
    skills_hint:
      - dcf-modeling
      - forecasting
      - scenario-modeling
      - unit-economics-modeling
      - runway-cognition
  surface:
    commands_hint:
      - work
      - council
      - challenge-me
    docs_first_pointer: "docs/getting-started-by-role.md#finance"
