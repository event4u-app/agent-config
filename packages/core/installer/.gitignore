# Build output (regenerated by `npx tsc -p tsconfig.json`)
dist/

# Local install (hoisted to root node_modules in the monorepo)
node_modules/

# Vitest / TS / IDE noise
*.tsbuildinfo
.vitest-cache/
coverage/
