{
    "name": "@event4u/installer-retired",
    "version": "0.0.0",
    "private": true,
    "description": "Retirement stub — see tsconfig.json. The real TS installer was retired in v4.0.0 (road-to-unified-setup § D1); active code lives at src/install/ under the root package.",
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run"
    }
}
