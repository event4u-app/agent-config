{
    "//": "Stub kept alive so the legacy `.github/workflows/tests.yml` Node-Tests matrix stays green until a workflow-scope-authenticated push removes the three deprecated `Installer (...)` steps. The TypeScript installer at `packages/core/installer/{src,tests}` was retired in v4.0.0 (road-to-unified-setup § D1); the active TS engine now lives at `src/install/`.",
    "compilerOptions": {
        "target": "ES2022",
        "module": "ESNext",
        "moduleResolution": "Bundler",
        "strict": true,
        "noEmit": false,
        "outDir": "dist",
        "rootDir": "src"
    },
    "include": ["src/**/*.ts"]
}
